---
phase: 03-auth-failure-ux-admin-credential-removal
plan: 05
type: execute
wave: 5
depends_on: ["03-02"]
files_modified:
  - kinetica_bi/server/src/index.ts
  - kinetica_bi/server/.env.example
  - kinetica_bi/server/tests/setup.ts
  - kinetica_bi/server/tests/routes.sql.spec.ts
  - kinetica_bi/server/tests/routes.discovery.spec.ts
  - kinetica_bi/server/tests/routes.wms.spec.ts
  - kinetica_bi/server/tests/routes.materialize.spec.ts
  - README.md
autonomous: true
requirements: [ADMN-01, ADMN-02, ADMN-03]
must_haves:
  truths:
    - "requireConfig narrowed to a single env-var check: KINETICA_URL only — KINETICA_USERNAME and KINETICA_PASSWORD are no longer read anywhere on the boot path"
    - "kinetica_bi/server/.env.example contains zero references to KINETICA_USERNAME or KINETICA_PASSWORD"
    - "README.md contains zero references to KINETICA_USERNAME or KINETICA_PASSWORD"
    - "kinetica_bi/server/tests/setup.ts does not prime KINETICA_USERNAME / KINETICA_PASSWORD env vars"
    - "Negative-assertion env-var reads (process.env.KINETICA_USERNAME) in route specs are replaced with the hardcoded sentinel string \"admin-env-user\""
    - "git grep -nE 'KINETICA_USERNAME|KINETICA_PASSWORD' -- ':!.planning/' returns ZERO matches"
  artifacts:
    - path: "kinetica_bi/server/src/index.ts"
      provides: "requireConfig checks only KINETICA_URL"
      contains: "Missing KINETICA_URL"
    - path: "kinetica_bi/server/.env.example"
      provides: "Reduced env contract — KINETICA_URL, AUTH_SECRET, SESSION_ENCRYPTION_KEY, PORT, CORS_ORIGIN, DB_PATH only"
    - path: "README.md"
      provides: "Env-var table without KINETICA_USERNAME/KINETICA_PASSWORD rows"
  key_links:
    - from: "requireConfig middleware"
      to: "process.env.KINETICA_URL"
      via: "1-var presence check"
      pattern: "!process\\.env\\.KINETICA_URL"
    - from: "Test specs that assert env-var-NOT-leaked"
      to: "hardcoded sentinel string"
      via: "expect(decoded).not.toContain(\"admin-env-user\") — no env-var read"
      pattern: "not\\.toContain\\(\"admin-env-user\"\\)"
---

<objective>
Delete every remaining KINETICA_USERNAME / KINETICA_PASSWORD reference outside `.planning/`. Narrow `requireConfig` to KINETICA_URL only. Convert test specs that read `process.env.KINETICA_USERNAME` for negative assertions to use the literal `"admin-env-user"` instead. Remove .env.example and README rows. Final SC#5 verification: `git grep -nE 'KINETICA_USERNAME|KINETICA_PASSWORD' -- ':!.planning/'` returns zero matches.

Purpose: ADMN-01 (env vars removed), ADMN-02 (requireConfig narrowed), ADMN-03 (.env.example + README cleaned). Mechanical enforcement of "no admin-credential fallback when user creds fail" — the env vars literally don't exist in the codebase anymore, so no future code can accidentally fall back to them.

Output: Smaller env contract, narrower middleware, cleaner test fixtures, zero matches on the audit grep.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-auth-failure-ux-admin-credential-removal/03-CONTEXT.md
@.planning/phases/03-auth-failure-ux-admin-credential-removal/03-02-SUMMARY.md
@kinetica_bi/server/src/index.ts
@kinetica_bi/server/.env.example
@kinetica_bi/server/tests/setup.ts
@kinetica_bi/server/tests/routes.sql.spec.ts
@kinetica_bi/server/tests/routes.discovery.spec.ts
@kinetica_bi/server/tests/routes.wms.spec.ts
@kinetica_bi/server/tests/routes.materialize.spec.ts
@README.md

<interfaces>
<!-- Current state of the env-var reads to remove. -->

In kinetica_bi/server/src/index.ts (current — Phase 2 left it untouched per Phase 2 CONTEXT.md decision):
```typescript
const requireConfig = (req: Request, res: Response, next: NextFunction) => {
  if (!process.env.KINETICA_URL || !process.env.KINETICA_USERNAME || !process.env.KINETICA_PASSWORD) {
    return res.status(500).json({
      error: "Missing KINETICA_URL, KINETICA_USERNAME, or KINETICA_PASSWORD environment variables."
    });
  }
  return next();
};
```
After Plan 03-05:
```typescript
const requireConfig = (req: Request, res: Response, next: NextFunction) => {
  if (!process.env.KINETICA_URL) {
    return res.status(500).json({ error: "Missing KINETICA_URL environment variable." });
  }
  return next();
};
```

In kinetica_bi/server/tests/setup.ts (current): no priming of KINETICA_USERNAME/PASSWORD found in the file as read — verify by grep before edit. Confirmed: setup.ts as-of Phase 2 sets SESSION_ENCRYPTION_KEY, KINETICA_URL, DB_PATH, AUTH_SECRET only. No env-var priming to remove. (Confirm during execution; if Phase 1/2 added priming since the read, remove it.)

In test specs (current Phase 2 state):
- routes.sql.spec.ts:61: `expect(decoded).not.toContain(process.env.KINETICA_USERNAME || "ADMIN_NEVER_SET");`
- routes.sql.spec.ts:65-67: temporarily-sets pattern `process.env.KINETICA_USERNAME = "admin-env-user"` ... `process.env.KINETICA_USERNAME = originalUser;`
- routes.discovery.spec.ts:88: `expect(decoded).not.toContain(process.env.KINETICA_USERNAME || "ADMIN_NEVER_SET");`
- routes.discovery.spec.ts:92-95: same temp-set pattern
- routes.wms.spec.ts:73-101: same temp-set pattern (lines 73-101)
- routes.materialize.spec.ts: VERIFY whether any env-var reads exist (grep before editing).

The conversion: every `process.env.KINETICA_USERNAME` read becomes the literal `"admin-env-user"`. Every temporary-set + restore block (the `originalUser = process.env.KINETICA_USERNAME; process.env.KINETICA_USERNAME = "admin-env-user"; try { ... } finally { process.env.KINETICA_USERNAME = originalUser; }`) becomes just the test body without the priming — the assertion `expect(decoded).not.toContain("admin-env-user")` doesn't need the env to be set at all.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Narrow requireConfig + delete env-var references in source, env file, README, test setup, test specs</name>
  <files>
    kinetica_bi/server/src/index.ts,
    kinetica_bi/server/.env.example,
    README.md,
    kinetica_bi/server/tests/setup.ts,
    kinetica_bi/server/tests/routes.sql.spec.ts,
    kinetica_bi/server/tests/routes.discovery.spec.ts,
    kinetica_bi/server/tests/routes.wms.spec.ts,
    kinetica_bi/server/tests/routes.materialize.spec.ts
  </files>

  <read_first>
    - kinetica_bi/server/src/index.ts (current requireConfig at lines 64-71)
    - kinetica_bi/server/.env.example (current — 13 lines; lines 3-4 = KINETICA_USERNAME/KINETICA_PASSWORD)
    - README.md (lines 85-86 = the two env-var rows)
    - kinetica_bi/server/tests/setup.ts (current — verify no priming exists; if any KINETICA_USERNAME/PASSWORD assignment is present, remove)
    - kinetica_bi/server/tests/routes.sql.spec.ts lines 61-89
    - kinetica_bi/server/tests/routes.discovery.spec.ts lines 88-114, 216-232
    - kinetica_bi/server/tests/routes.wms.spec.ts lines 72-103
    - kinetica_bi/server/tests/routes.materialize.spec.ts (search for KINETICA_USERNAME/PASSWORD; remove if found)
    - .planning/phases/03-auth-failure-ux-admin-credential-removal/03-CONTEXT.md §"Admin credential removal mechanics"
  </read_first>

  <action>
    1. Edit `kinetica_bi/server/src/index.ts`:
       Replace the requireConfig body (lines 64-71) with:
       ```typescript
       const requireConfig = (req: Request, res: Response, next: NextFunction) => {
         if (!process.env.KINETICA_URL) {
           return res.status(500).json({ error: "Missing KINETICA_URL environment variable." });
         }
         return next();
       };
       ```

    2. Edit `kinetica_bi/server/.env.example`:
       Delete lines 3 (`KINETICA_USERNAME=[redacted]`) and 4 (`KINETICA_PASSWORD=[redacted]`). The resulting file should have:
       ```
       PORT=4000
       KINETICA_URL=https://localhost:9191
       CORS_ORIGIN=http://localhost:5173
       DB_PATH=./data/kinetica.db
       # Random string >= 16 chars used to sign session cookies. Generate with: openssl rand -hex 32
       AUTH_SECRET=replace-me-with-a-long-random-string
       # 32 bytes (64 hex chars) used to encrypt session passwords at rest with AES-256-GCM.
       # Independent from AUTH_SECRET. Rotation forces all currently-logged-in users to re-login.
       # Generate with: openssl rand -hex 32
       SESSION_ENCRYPTION_KEY=replace-me-with-64-hex-chars-from-openssl-rand-hex-32
       ```
       Do NOT add any replacement comment for the deleted lines — git history is the canonical record.

    3. Edit `README.md`:
       Delete the two table rows for KINETICA_USERNAME and KINETICA_PASSWORD (lines 85-86 of the current file). Adjacent rows shift up. The Env Vars table after editing:
       ```
       | Variable           | Default                   | Description                      |
       |--------------------|---------------------------|----------------------------------|
       | `PORT`             | `4000`                    | Backend server port              |
       | `KINETICA_URL`     | `https://localhost:9191`  | Kinetica database URL            |
       | `CORS_ORIGIN`      | `http://localhost:5173`   | Allowed CORS origins (comma-sep) |
       | `DB_PATH`          | `./data/kinetica.db`      | SQLite database file path        |
       ```
       (AUTH_SECRET and SESSION_ENCRYPTION_KEY are not in the existing README table — Phase 1's deploy runbook documents them. Leave that as-is.)

    4. Edit `kinetica_bi/server/tests/setup.ts`:
       Search for any line setting `process.env.KINETICA_USERNAME` or `process.env.KINETICA_PASSWORD`. Per Phase 2 read of the file there is none, but the planning context references "tests/setup.ts KINETICA_USERNAME priming to remove". Verify with `grep -nE 'KINETICA_USERNAME|KINETICA_PASSWORD' kinetica_bi/server/tests/setup.ts`; if matches found, delete those lines. If no matches, leave the file unchanged. Document the result in the SUMMARY.

    5. Edit `kinetica_bi/server/tests/routes.sql.spec.ts`:
       - Line ~61: change `expect(decoded).not.toContain(process.env.KINETICA_USERNAME || "ADMIN_NEVER_SET");` to `expect(decoded).not.toContain("admin-env-user");`.
       - Lines ~64-89 (the "per-user Authorization header does NOT use KINETICA_USERNAME env var" test): delete the `const originalUser = process.env.KINETICA_USERNAME;`, `process.env.KINETICA_USERNAME = "admin-env-user";`, the try/finally restoration, and replace with the inner test body. Rename the it() title to "per-user Authorization header is built from session creds, not from any env var (sentinel check)". Final assertion `expect(decoded).not.toContain("admin-env-user");` survives — the sentinel proves no env was used because no env was ever set.

    6. Repeat the same conversion in `kinetica_bi/server/tests/routes.discovery.spec.ts`:
       - Line ~88 + lines ~92-114 + lines ~216-232: same pattern. Both occurrences (the schemas test and the schemas/:schema/tables test).

    7. Repeat in `kinetica_bi/server/tests/routes.wms.spec.ts` lines ~72-103.

    8. Check `kinetica_bi/server/tests/routes.materialize.spec.ts` with `grep -nE 'KINETICA_USERNAME|KINETICA_PASSWORD'`. If any reference exists, apply the same conversion. If none, no edit.

    9. Run `cd kinetica_bi/server && npm test -- --run`. Expect 0 failed (the conversion preserves the test logic — sentinel string check is equivalent).

    10. Run `cd kinetica_bi/server && npm run build`. Expect 0 errors.

    11. Run the canonical SC#5 audit:
       ```
       git grep -nE 'KINETICA_USERNAME|KINETICA_PASSWORD' -- ':!.planning/'
       ```
       Expect ZERO output (exit code 1 from git grep means no matches — that's success). If any match remains, identify and delete it.

    12. Special note on `kinetica_bi/server/dist/`: this is a TypeScript build output. Check if it's in `.gitignore` (`grep -E '^dist|^kinetica_bi/server/dist' .gitignore`). If `dist/` is gitignored, no concern — it's a local build artifact and `git grep` skips it. If it's tracked, two options:
       a. Run `cd kinetica_bi/server && npm run build` to regenerate dist/ from the current narrowed source — this naturally removes the env-var references from the compiled output.
       b. Add `kinetica_bi/server/dist/` to `.gitignore` and `git rm -r --cached kinetica_bi/server/dist/` to untrack.
       Choose option (a) — it's less invasive and aligns with the build pipeline. Document in SUMMARY which option was used.
  </action>

  <verify>
    <automated>cd kinetica_bi/server && npm test -- --run 2>&1 | tail -10 && cd kinetica_bi/server && npm run build 2>&1 | tail -5 && git grep -nE 'KINETICA_USERNAME|KINETICA_PASSWORD' -- ':!.planning/' ; echo "GREP_EXIT=$?"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'KINETICA_USERNAME\|KINETICA_PASSWORD' kinetica_bi/server/src/index.ts` returns 0
    - `grep -c 'Missing KINETICA_URL environment variable.' kinetica_bi/server/src/index.ts` returns 1
    - `grep -c 'KINETICA_USERNAME\|KINETICA_PASSWORD' kinetica_bi/server/.env.example` returns 0
    - `grep -c 'KINETICA_USERNAME\|KINETICA_PASSWORD' README.md` returns 0
    - `grep -c 'KINETICA_USERNAME\|KINETICA_PASSWORD' kinetica_bi/server/tests/setup.ts` returns 0
    - `grep -c 'process.env.KINETICA_USERNAME\|process.env.KINETICA_PASSWORD' kinetica_bi/server/tests/routes.sql.spec.ts` returns 0
    - `grep -c 'process.env.KINETICA_USERNAME\|process.env.KINETICA_PASSWORD' kinetica_bi/server/tests/routes.discovery.spec.ts` returns 0
    - `grep -c 'process.env.KINETICA_USERNAME\|process.env.KINETICA_PASSWORD' kinetica_bi/server/tests/routes.wms.spec.ts` returns 0
    - `grep -c '"admin-env-user"' kinetica_bi/server/tests/routes.sql.spec.ts` returns >= 1
    - `grep -c '"admin-env-user"' kinetica_bi/server/tests/routes.discovery.spec.ts` returns >= 1
    - `grep -c '"admin-env-user"' kinetica_bi/server/tests/routes.wms.spec.ts` returns >= 1
    - `git grep -nE 'KINETICA_USERNAME|KINETICA_PASSWORD' -- ':!.planning/' | wc -l` returns 0 (the SC#5 audit — ZERO matches)
    - `cd kinetica_bi/server && npm run build` exits 0
    - `cd kinetica_bi/server && npm test -- --run` exits 0 with all tests passing (no regression from Plan 03-02 baseline)
  </acceptance_criteria>

  <done>
    Admin env vars erased from the working tree (excluding .planning/ history). requireConfig narrowed. Tests use sentinels not env reads. Build clean. The SC#5 audit grep returns zero matches.
  </done>
</task>

</tasks>

<verification>
1. `git grep -nE 'KINETICA_USERNAME|KINETICA_PASSWORD' -- ':!.planning/'` returns no output (zero matches).
2. `cd kinetica_bi/server && npm run build` exits 0.
3. `cd kinetica_bi/server && npm test -- --run` exits 0 with all tests passing (no regressions).
4. The narrowed requireConfig still allows the routes that depend on KINETICA_URL to work (verified by green test suite).
</verification>

<success_criteria>
- ADMN-01: `KINETICA_USERNAME` / `KINETICA_PASSWORD` env vars are no longer read anywhere outside `.planning/`.
- ADMN-02: `requireConfig` narrowed to `KINETICA_URL` only.
- ADMN-03: `.env.example` and `README.md` cleaned.
- ROADMAP SC#5: `git grep ... -- ':!.planning/'` returns zero matches.
- Test suite green; build clean.
</success_criteria>

<output>
After completion, create `.planning/phases/03-auth-failure-ux-admin-credential-removal/03-05-SUMMARY.md` documenting:
- Whether `kinetica_bi/server/dist/` was in `.gitignore` (no action needed) or required rebuild/untrack.
- Confirmation grep of `git grep -nE 'KINETICA_USERNAME|KINETICA_PASSWORD' -- ':!.planning/'` returning zero matches.
- The post-narrowing requireConfig body verbatim (so Plan 03-06's bootstrap-gate work doesn't accidentally re-add the old check).
- Test count delta (should remain at the Plan 03-02 baseline of 210+).
</output>
