---
phase: 02-per-user-credential-passthrough-on-every-kinetica-call
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-per-user-credential-passthrough-on-every-kinetica-call/SPIKE.md
autonomous: false
requirements: [CRED-04]
gap_closure: false

must_haves:
  truths:
    - "A SPIKE.md file exists in the phase directory documenting how a least-privileged Kinetica user fares against CREATE OR REPLACE MATERIALIZED VIEW"
    - "The SPIKE.md contains an explicit Recommendation section that either confirms 'loud-failure acceptable' (Phase 2 proceeds with per-user materialize) or recommends a different approach (triggers /gsd:insert-phase 2.1)"
    - "User has reviewed SPIKE.md and decided whether to proceed with Phase 2 as planned or insert Phase 2.1"
  artifacts:
    - path: ".planning/phases/02-per-user-credential-passthrough-on-every-kinetica-call/SPIKE.md"
      provides: "Materialize-permission spike result + recommendation"
      contains: "Recommendation"
  key_links:
    - from: "SPIKE.md Recommendation"
      to: "Plan 02-05 (materialize refactor) or /gsd:insert-phase 2.1"
      via: "user decision at the checkpoint:decision task"
      pattern: "Recommendation:.*(loud-failure|service-account|migration|REFRESH)"
---

<objective>
Run the materialize-permissions spike that Phase 2 hinges on (Pitfall 16 from PITFALLS.md, the verification flag in ROADMAP.md). Produce SPIKE.md documenting whether a least-privileged Kinetica user can execute `CREATE OR REPLACE MATERIALIZED VIEW`. Get the user's explicit go/no-go decision before any helper-module or refactor code lands.

Purpose: The default Phase 2 design — per-user materialize with loud KineticaPermissionError on insufficient grants — is principled (no shared-admin escape hatch), but it only ships if operationally tolerable. If typical BI users cannot materialize and loud failure breaks the dashboard for them, the team must choose service-account / migration / REFRESH-only via `/gsd:insert-phase 2.1`. Locking that decision before code lands keeps the helper API clean and avoids rework.

Output: `SPIKE.md` in the phase directory + a user decision recorded at the checkpoint:decision task; this plan does NOT modify production code, only adds documentation.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-per-user-credential-passthrough-on-every-kinetica-call/02-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@.planning/REQUIREMENTS.md
@kinetica_bi/server/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the materialize spike and write SPIKE.md</name>
  <files>.planning/phases/02-per-user-credential-passthrough-on-every-kinetica-call/SPIKE.md</files>
  <read_first>
    - .planning/phases/02-per-user-credential-passthrough-on-every-kinetica-call/02-CONTEXT.md (`<decisions>` → "Materialize service-account spike" — read entire subsection; this task implements that protocol verbatim)
    - .planning/research/PITFALLS.md Pitfall 16 (the original DDL-permission concern)
    - .planning/ROADMAP.md "Phase 2 verification flag" (the spike requirement)
    - kinetica_bi/server/src/index.ts lines 250-296 (the materialize endpoint — the exact DDL the spike must run)
    - .planning/REQUIREMENTS.md "Out of Scope" row "Admin-credential fallback when user creds fail" (the policy backstop justifying loud-failure default)
  </read_first>
  <action>
    Execute the materialize spike as specified in 02-CONTEXT.md `<decisions>` → "Materialize service-account spike":

    1. Identify a least-privileged Kinetica test user. Required grants: `SELECT` on at least one schema/table the BI app can target (e.g., `ki_home.test_table` or any table currently visible in the BI dashboard). Required NON-grants: NO DDL grants (no `CREATE`, no `MATERIALIZED VIEW` privilege on the target schema). If such a user does not exist in the team's Kinetica instance, document the steps to create one in SPIKE.md and either (a) ask the user to create it or (b) proceed with whichever least-privileged user is available, noting the gap.

    2. Replicate the exact request the BI app makes today. The `index.ts:264-279` materialize handler POSTs to `${KINETICA_URL}/execute/sql` with body:
       ```json
       {
         "statement": "CREATE OR REPLACE MATERIALIZED VIEW <view_name> AS SELECT * FROM <schema>.<table>",
         "offset": 0,
         "limit": 1,
         "encoding": "json",
         "request_schema_str": "",
         "data": [],
         "options": {}
       }
       ```
       and `Authorization: Basic <base64(username:password)>`. Use `curl` or any HTTP tool. Pick a non-destructive view name like `__phase2_spike_test_view`.

    3. Capture the response: HTTP status, body (especially `body.status` and `body.message`), and any error shape Kinetica returns when the user lacks DDL permission.

    4. Repeat the same statement as an admin-equivalent user (the existing `KINETICA_USERNAME` from the dev `.env` is fine) to confirm the DDL itself is well-formed and only the permissions distinguish the two outcomes.

    5. Drop the spike view if it was created: `DROP MATERIALIZED VIEW IF EXISTS __phase2_spike_test_view`.

    6. Write `.planning/phases/02-per-user-credential-passthrough-on-every-kinetica-call/SPIKE.md` with these required sections (markdown headings, exact spelling):
       - `# Phase 2 Materialize Permission Spike`
       - `## Setup` — test user identity (NOT password), enumerated Kinetica grants, target schema/table used
       - `## Test` — the exact `curl` (or equivalent) command issued, with credential placeholder
       - `## Result` — HTTP status code, response body (verbatim, possibly redacted), classification per CONTEXT.md error taxonomy: which of `KineticaAuthError` (401), `KineticaPermissionError` (403), `KineticaUpstreamError` (anything else / `body.status: ERROR`), or `success` (200 + non-error body)
       - `## Recommendation` — exactly ONE of the four phrases, copied verbatim:
         - `Recommendation: loud-failure acceptable — proceed with Phase 2 per-user materialize as planned`
         - `Recommendation: needs service-account approach — insert Phase 2.1 before continuing`
         - `Recommendation: needs pre-create-via-migration approach — insert Phase 2.1 before continuing`
         - `Recommendation: needs REFRESH-only approach — insert Phase 2.1 before continuing`
       - `## References` — link to PITFALLS.md Pitfall 16, ROADMAP.md Phase 2 verification flag, 02-CONTEXT.md `<decisions>` → "Materialize service-account spike", REQUIREMENTS.md "Out of Scope" row

    Default policy reminder (from 02-CONTEXT.md): if the spike result is "user lacks DDL but failure is a clean 403 / `KineticaPermissionError`", the recommended Recommendation is `loud-failure acceptable` — Kinetica's permission model is doing exactly what the milestone's principle demands. Only escalate to a 2.1 if the team determines loud failure breaks an actual operator workflow.

    Do NOT modify any code in `kinetica_bi/server/src/`. This task is documentation-only.
  </read_first>
  <verify>
    <automated>test -f .planning/phases/02-per-user-credential-passthrough-on-every-kinetica-call/SPIKE.md && grep -q "^## Setup$" .planning/phases/02-per-user-credential-passthrough-on-every-kinetica-call/SPIKE.md && grep -q "^## Test$" .planning/phases/02-per-user-credential-passthrough-on-every-kinetica-call/SPIKE.md && grep -q "^## Result$" .planning/phases/02-per-user-credential-passthrough-on-every-kinetica-call/SPIKE.md && grep -q "^## Recommendation$" .planning/phases/02-per-user-credential-passthrough-on-every-kinetica-call/SPIKE.md && grep -qE "^Recommendation: (loud-failure acceptable|needs service-account approach|needs pre-create-via-migration approach|needs REFRESH-only approach)" .planning/phases/02-per-user-credential-passthrough-on-every-kinetica-call/SPIKE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-per-user-credential-passthrough-on-every-kinetica-call/SPIKE.md` exists
    - SPIKE.md contains all five required headings: `## Setup`, `## Test`, `## Result`, `## Recommendation`, `## References` (verifiable: `grep -c "^## " SPIKE.md` returns 5)
    - The `## Recommendation` section contains exactly one of the four canonical recommendation phrases listed in `<action>` above (verifiable: the grep -E in the verify command matches)
    - SPIKE.md does NOT contain the test user's password (verifiable: spike author confirms; do not commit a password hash either)
    - No production code under `kinetica_bi/server/src/` was modified by this task (verifiable: `git diff --name-only kinetica_bi/server/src/` returns empty)
  </acceptance_criteria>
  <done>SPIKE.md written and committed to the phase directory with a clear, parseable Recommendation. Production code untouched.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: User reviews SPIKE.md and decides Phase 2 trajectory</name>
  <files>.planning/phases/02-per-user-credential-passthrough-on-every-kinetica-call/SPIKE.md</files>
  <decision>Does Phase 2 proceed with per-user materialize and loud `KineticaPermissionError` failure (as planned), or does it pause for `/gsd:insert-phase 2.1` to handle materialize differently?</decision>
  <context>
    The SPIKE.md from Task 1 documents whether a typical BI user can materialize against the team's Kinetica instance. The default Phase 2 design assumes loud failure is acceptable — that's the milestone's no-shared-admin-escape-hatch principle made concrete. Only the user can confirm whether that's operationally tolerable for the team's actual workflows. If materialize is genuinely required for non-admin users, Phase 2.1 must spawn before Plan 02-05 (materialize refactor) lands.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed with Phase 2 as planned (loud failure acceptable)</name>
      <pros>Honors the milestone's no-escape-hatch principle. Smaller diff. UX-02 (403 inline error on widget) lands earlier than scheduled.</pros>
      <cons>Users without DDL grants see materialize errors. Requires Kinetica admin to grant CREATE on relevant schemas if those users genuinely need to materialize.</cons>
    </option>
    <option id="insert-2-1">
      <name>Pause and run /gsd:insert-phase 2.1 to redesign materialize handling</name>
      <pros>Preserves materialize functionality for users without DDL grants via service-account, pre-create-via-migration, or REFRESH-only.</pros>
      <cons>Adds a phase. Helper API may need expansion (route-specific bypass for materialize). Risks reintroducing a shared-admin code path that the milestone is explicitly killing — careful design required.</cons>
    </option>
  </options>
  <action>
    Display the SPIKE.md Recommendation to the user. Wait for the user to type "proceed" (continue with Plan 02-02) or to invoke `/gsd:insert-phase 2.1` (pause Phase 2 to redesign materialize handling). Do NOT modify any files in this task — this is a pure decision gate. After the user signals, record the decision in 02-01-SUMMARY.md (the plans output step writes this) so downstream plans (especially 02-05) can reference it via `grep -q "loud-failure acceptable" SPIKE.md`.
  </action>
  <verify>
    <automated>grep -q "loud-failure acceptable\|needs service-account\|needs pre-create-via-migration\|needs REFRESH-only" .planning/phases/02-per-user-credential-passthrough-on-every-kinetica-call/SPIKE.md</automated>
  </verify>
  <done>User has explicitly chosen to proceed (or initiated /gsd:insert-phase 2.1). The SPIKE.md Recommendation field is the durable artifact of the decision; downstream plans gate on it.</done>
  <resume-signal>Type "proceed" to continue with Plan 02-02 onward, or run `/gsd:insert-phase 2.1` and re-enter the planner once the inserted phase is plotted</resume-signal>
</task>

</tasks>

<verification>
- SPIKE.md exists with all 5 required sections
- Recommendation phrase is one of the four canonical options
- User has read SPIKE.md and chosen "proceed" or initiated /gsd:insert-phase 2.1
- No production code under `kinetica_bi/server/src/` modified
- No test code under `kinetica_bi/server/tests/` modified
- All 56 Phase 1 tests still pass: `npm --prefix kinetica_bi/server test -- --run` exits 0 (sanity check; should be unchanged since no code moved)
</verification>

<success_criteria>
1. `.planning/phases/02-per-user-credential-passthrough-on-every-kinetica-call/SPIKE.md` exists with the 5 required sections.
2. SPIKE.md contains exactly one of the four canonical Recommendation phrases.
3. User has explicitly chosen to proceed (or initiated 2.1 insertion) — not implicit.
4. Phase 1's 56-test green baseline preserved (no code touched).
</success_criteria>

<output>
After completion, create `.planning/phases/02-per-user-credential-passthrough-on-every-kinetica-call/02-01-SUMMARY.md` with the spike result, the user's decision, and a one-line summary of what unblocks (or what 2.1 must address if inserted).
</output>
