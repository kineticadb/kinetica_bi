---
phase: 17-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kinetica_bi/server/scripts/test-fixtures/v13-filter-fixture.sql
  - .planning/phases/17-verification/17-VERIFICATION.md
  - .planning/phases/17-verification/screenshots/
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - VERIFY-V13-01
  - VERIFY-V13-02

must_haves:
  truths:
    - "A low-cardinality, spatially-spread Kinetica fixture exists (5–10 distinct values, 500–2000 rows, CONUS spread) that the operator created from a committed reference SQL script"
    - "Operator walked all 4 user flows (chart drill+clear single-table, map drill LAYERS-swap, multi-widget single-table chart+map+records-table, lifecycle resets logout+dashboard-switch) against the fixture and recorded PASS/FAIL per flow in 17-VERIFICATION.md"
    - "Operator walked all 4 edge cases (V13-LIMIT-01 records-table-only, map-only-on-tableId, multi-table A/B isolation, empty/zero-widget dashboards) and recorded each as PASS or accepted-limitation in 17-VERIFICATION.md"
    - "All 8 chart types (bar, line, pie, scatter, table, records-table, big-number, map) were exercised against the fixture and the FROM-swap landing was confirmed for each in 17-VERIFICATION.md"
    - "Before/after screenshots of the map widget (filter applied vs. cleared) are committed under .planning/phases/17-verification/screenshots/ and embedded/linked from 17-VERIFICATION.md — closes Phase 16 deferred Truth 1 visual side and TD-V12-04 reproducibly"
    - "Phase 16 deferred 4 human-needed checks (visual narrowing, network-tab WMS URL inspection, _mv cache-bust runtime, StrictMode v1.2 lifecycle) are explicitly closed in 17-VERIFICATION.md with evidence for each"
    - "TTL recovery is verified across three exercises with verbatim commands in 17-VERIFICATION.md: proactive (DevTools Console snippet mutating expiresAt), reactive (manual Kinetica DROP TABLE), and max-1-retry cap (DROP-twice rapid → fall-through to raw FROM <table> silently, no infinite loop, no error toast)"
    - "OIDC S2.b live DDL re-probe is documented as DEFERRED in 17-VERIFICATION.md with NO citation of Phase 13 supertest mock coverage as substitute; deferred item also surfaces in PROJECT.md backlog and MILESTONES.md v1.3 known-gaps line"
    - "Backend supertest coverage (Phase 13 + Phase 15 specs covering POST + DELETE under both AUTH_MODE=password and AUTH_MODE=oidc with stubbed sessions) and frontend vitest coverage (Phase 14/15/16 store + FROM-swap + LAYERS-swap + lifecycle reset specs) are cited with file paths and counts in 17-VERIFICATION.md Test Coverage Citation section"
    - "17-VERIFICATION.md frontmatter overall_status = passed (or tech_debt if UAT surfaces inline-fixed gaps); per-criterion (a/b/c/d) status map + gaps array populated"
    - "Full vitest suite remains 332/332 green and tsc --noEmit exits 0 after Phase 17 (no regression from doc work or fixture script addition)"
    - "Operator dropped the fixture table from Kinetica after VERIFICATION.md was finalized (table is temporary; reference SQL committed for reproducibility)"
  artifacts:
    - path: "kinetica_bi/server/scripts/test-fixtures/v13-filter-fixture.sql"
      provides: "Reproducible synthetic CREATE TABLE + INSERT script — 5–10 distinct category values, 500–2000 rows, CONUS lat/lon spread; reference artifact, not auto-run"
      contains: "CREATE TABLE"
    - path: ".planning/phases/17-verification/screenshots/"
      provides: "Before/after PNGs of map widget filter behavior (closes TD-V12-04 visual evidence)"
      min_files: 2
    - path: ".planning/phases/17-verification/17-VERIFICATION.md"
      provides: "v1.3 milestone-close end-to-end verification report consumed by /gsd:audit-milestone"
      contains: "overall_status"
    - path: ".planning/STATE.md"
      provides: "Phase 17 marked complete; v1.3 milestone-close prep noted; resume pointer updated"
      contains: "Phase: 17"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 17 status [x] with completed-on date; v1.3 progress count incremented"
      contains: "[x] **Phase 17: verification**"
    - path: ".planning/REQUIREMENTS.md"
      provides: "VERIFY-V13-01 + VERIFY-V13-02 status set to Complete in traceability table"
      contains: "VERIFY-V13-01 | Phase 17 | Complete"
  key_links:
    - from: "17-VERIFICATION.md frontmatter"
      to: "criteria_status (a)/(b)/(c)/(d) per ROADMAP Phase 17 success criteria"
      via: "structured YAML frontmatter map"
      pattern: "criteria_status:"
    - from: "17-VERIFICATION.md TTL Recovery section"
      to: "useFilterViewStore.setView Console snippet + Kinetica DROP TABLE SQL"
      via: "verbatim command listings (reproducible by future operators)"
      pattern: "useFilterViewStore.getState\\(\\).setView"
    - from: "17-VERIFICATION.md Phase 16 Deferred Checks section"
      to: "16-VERIFICATION.md frontmatter human_verification: 4 items"
      via: "1:1 closure mapping with PASS evidence per item"
      pattern: "Phase 16 Deferred"
    - from: "17-VERIFICATION.md Open Gaps section"
      to: "OIDC S2.b DEFERRED entry"
      via: "explicit deferral with rationale; NO Phase 13 supertest substitution claim"
      pattern: "OIDC S2.b"
    - from: ".planning/REQUIREMENTS.md Traceability table rows VERIFY-V13-01 / VERIFY-V13-02"
      to: "status column updated to Complete"
      via: "in-place table edit"
      pattern: "VERIFY-V13-0[12].*Complete"
---

<objective>
Close the v1.3 milestone with a single end-to-end verification cycle. Two locked deliverables: (1) a low-cardinality, spatially-spread synthetic Kinetica fixture (closes TD-V12-04), and (2) `17-VERIFICATION.md` documenting full UAT coverage (closes TD-V12-01 and produces the milestone-audit input).

Purpose: Ship v1.3 cleanly. Phases 13–16 built the FROM-swap + LAYERS-swap + lifecycle pipeline; Phase 17 proves it works against a fixture where filter effects are unambiguous to the naked eye, and writes the artifact that `/gsd:audit-milestone` consumes.

Output:
  - `kinetica_bi/server/scripts/test-fixtures/v13-filter-fixture.sql` (committed reference DDL/INSERT)
  - `.planning/phases/17-verification/screenshots/*.png` (before/after map-filter evidence)
  - `.planning/phases/17-verification/17-VERIFICATION.md` (the deliverable)
  - Updated `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`

Phase 17 produces ZERO new src/ code in the happy path. The Gap-handling protocol (CONTEXT.md user-lock 6) allows follow-up `17-NN-PLAN.md` plans IF UAT surfaces real bugs — not pre-created here.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/MILESTONES.md
@.planning/phases/17-verification/17-CONTEXT.md
@.planning/phases/16-map-filtering/16-VERIFICATION.md
@.planning/phases/12-dashboard-layers-panel/12-VERIFICATION.md
@.planning/phases/15-chart-filtering/15-VERIFICATION.md
@.planning/phases/13-spikes-and-endpoint/13-SPIKE-NOTES.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md

<interfaces>
<!-- Key types and contracts the executor needs for the UAT runbook + DevTools snippet authoring. -->
<!-- Extracted directly from src so the executor doesn't have to re-explore. -->

From kinetica_bi/src/store/filterViewStore.ts (Phase 14 store — DevTools UAT mutates this):
```typescript
export type FilterViewEntry = {
  viewName: string;
  expiresAt: number;          // epoch ms; client compares against Date.now() in LIFE-V13-01
  materializing: boolean;
  materializeVersion: number; // increments per CREATE OR REPLACE; _mv cache-buster source
  dashboardId: number;
};

export type FilterViewState = {
  views: Record<number, FilterViewEntry>;
  setView: (tableId: number, view: { viewName: string; expiresAt: number }, dashboardId: number) => void;
  clearView: (tableId: number) => void;
  markMaterializing: (tableId: number, dashboardId: number) => void;
  bumpMaterializeVersion: (tableId: number) => void;
  reset: () => void;
};

export const useFilterViewStore: ...; // exposed on window via Zustand convention; getState() reachable in DevTools Console
```

Note on DevTools Console mutation pattern (proactive TTL UAT): the `setView` signature is 3-arg
`setView(tableId, { viewName, expiresAt }, dashboardId)`. The CONTEXT.md sample console line
included `materializeVersion` and `dashboardId` inside the second arg — that is INCORRECT against
the locked store signature. The verbatim Console snippet must use the actual 3-arg form (see Task 4
action body).

From kinetica_bi/src/lib/kineticaErrors.ts (Phase 15 reactive recovery — UAT triggers this):
```typescript
const VIEW_NOT_FOUND_RE = /SqlEngine: Object '[^']+' not found/i;
const KINETICA_VIEW_NOT_FOUND_CODE = "S/SDc:1513";
export const isViewNotFoundError = (err: unknown): boolean => /* matches regex OR code */;
```

The Phase 13 spike S3 verbatim error string the reactive UAT must produce:
  `SqlEngine: Object '<view-name>' not found (S/SDc:1513)`  (HTTP 400)

From server/src/index.ts:667-728 (Phase 13 endpoint — UAT Network tab evidence target):
  - POST /api/filter/materialize → { viewName: string, expiresAt: number }
  - DELETE /api/filter/materialize → { dropped: true }
  - View name regex: /^_kbi_filt_u\w+_d\d+_t\d+_s\w{8}$/
  - Operator's default Kinetica schema: ki_home (per STATE.md)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author + commit fixture SQL DDL/INSERT reference script</name>
  <files>kinetica_bi/server/scripts/test-fixtures/v13-filter-fixture.sql</files>
  <read_first>
    - .planning/phases/17-verification/17-CONTEXT.md (decisions § "Test fixture sourcing" — exact cardinality / spread / column-shape locks)
    - .planning/STATE.md (Operator's default Kinetica schema = `ki_home`; recorded under "Phase 13 Plan 01 spike findings")
    - .planning/codebase/CONVENTIONS.md (2-space indent; no formatter; SQL is operator-run reference, not vitest fixture)
    - .planning/codebase/STACK.md (Kinetica DDL syntax — needed if any nonstandard column types creep in; for this fixture only DOUBLE/REAL/VARCHAR are used)
  </read_first>
  <action>
    Create the file `kinetica_bi/server/scripts/test-fixtures/v13-filter-fixture.sql` (a new directory `kinetica_bi/server/scripts/test-fixtures/` will be implicitly created — confirm via `ls kinetica_bi/server/scripts/` after writing).

    The SQL must be a self-contained reference script the operator (BI-user creds, default schema `ki_home`) can copy/paste into Kinetica Workbench, the Kinetica SQL tab, or `curl` against `/execute/sql`. NOT auto-run by CI; NOT loaded by any vitest. Pure reference material that captures the contract: 5–10 distinct categorical values × 500–2000 rows × CONUS lat/lon spread.

    Concrete required content:

    ```sql
    -- v1.3 Phase 17 verification fixture (VERIFY-V13-01 / TD-V12-04 closure)
    --
    -- Purpose: Low-cardinality, spatially-spread synthetic table for end-to-end
    -- filter UAT. Each filter value owns ~10–20% of points; filtering one value
    -- visibly removes a chunk at default dashboard zoom (continental US scale).
    --
    -- Run by: Phase 17 operator (BI-user creds; default schema ki_home).
    -- Drop after VERIFICATION.md is written:
    --   DROP TABLE IF EXISTS ki_home.v13_filter_fixture;
    --
    -- This file is a REFERENCE artifact. It is NOT auto-run by CI, NOT loaded
    -- by vitest, and NOT executed by the BI app at runtime. Operator copies and
    -- pastes (or runs via curl) directly against deployed Kinetica.

    DROP TABLE IF EXISTS ki_home.v13_filter_fixture;

    CREATE TABLE ki_home.v13_filter_fixture (
      id          INT NOT NULL,
      category    VARCHAR(8) NOT NULL,    -- filter column: 8 values 'A'..'H' (cardinality target 5–10)
      lat         DOUBLE NOT NULL,         -- CONUS spread: ~24..49
      lon         DOUBLE NOT NULL,         -- CONUS spread: ~-125..-67
      measure     DOUBLE NOT NULL,         -- aggregation target for bar/line/pie/scatter
      bucket      VARCHAR(4) NOT NULL,     -- secondary dimension for chart variety: 'low'|'med'|'high'
      PRIMARY KEY (id)
    );

    -- Insert ~1000 rows (target band 500–2000) using Kinetica's GENERATE_SERIES.
    -- One INSERT INTO ... SELECT for compactness; if the deployed Kinetica build
    -- lacks GENERATE_SERIES, the operator may substitute hand-crafted INSERTs.
    --
    -- Distribution: id i ∈ [1..1000]
    --   category = CHAR(65 + (i % 8))                   -- 'A'..'H' uniform
    --   lat      = 24 + (i % 250) * 0.1                  -- 24.0..49.0 step 0.1
    --   lon      = -125 + (i % 580) * 0.1                -- -125.0..-67.0 step 0.1
    --   measure  = 10 + ((i * 7) % 100)                  -- 10..109 (chart aggregations have variation)
    --   bucket   = CASE WHEN measure < 40 THEN 'low' WHEN measure < 80 THEN 'med' ELSE 'high' END

    INSERT INTO ki_home.v13_filter_fixture (id, category, lat, lon, measure, bucket)
    SELECT
      i AS id,
      CHAR(65 + MOD(i, 8))                       AS category,
      24.0 + (MOD(i, 250) * 0.1)                 AS lat,
      -125.0 + (MOD(i, 580) * 0.1)               AS lon,
      10.0 + (MOD(i * 7, 100))                   AS measure,
      CASE
        WHEN (10.0 + (MOD(i * 7, 100))) < 40.0  THEN 'low'
        WHEN (10.0 + (MOD(i * 7, 100))) < 80.0  THEN 'med'
        ELSE                                          'high'
      END                                         AS bucket
    FROM TABLE(GENERATE_SERIES(1, 1000)) AS t(i);

    -- Quick verification queries (operator runs to confirm shape before UAT):
    --   SELECT COUNT(*)                                          FROM ki_home.v13_filter_fixture;       -- expect 1000
    --   SELECT category, COUNT(*) FROM ki_home.v13_filter_fixture GROUP BY category ORDER BY category;  -- expect 8 rows, ~125 each
    --   SELECT MIN(lat), MAX(lat), MIN(lon), MAX(lon)            FROM ki_home.v13_filter_fixture;       -- expect CONUS bbox
    ```

    Notes:
    - Schema-qualify as `ki_home.v13_filter_fixture` per CONVENTIONS observed in app (`<schema>.<table>` form like `demo.nyctaxi`); Phase 13 spike S4 confirmed both qualified and unqualified work.
    - Use `DOUBLE` for lat/lon (matches Phase 11 latlon spatial mode default; WKT/WKB is over-engineering per CONTEXT.md).
    - 8 distinct categories ('A'..'H') sits in the locked 5–10 sweet spot; ~125 rows per value gives unambiguous narrowing on filter.
    - GENERATE_SERIES is the most compact insertion path; if the deployed Kinetica lacks it, operator can fall back to hand-crafted INSERTs (the file's comments tell them so).
    - DO NOT add a CI hook, package.json script, vitest setup, or any auto-runner. This file is operator-only reference.
    - DO NOT touch any other src/ or server/ file in this task.
  </action>
  <verify>
    <automated>test -f kinetica_bi/server/scripts/test-fixtures/v13-filter-fixture.sql &amp;&amp; grep -q "CREATE TABLE ki_home.v13_filter_fixture" kinetica_bi/server/scripts/test-fixtures/v13-filter-fixture.sql &amp;&amp; grep -q "category    VARCHAR(8)" kinetica_bi/server/scripts/test-fixtures/v13-filter-fixture.sql &amp;&amp; grep -q "GENERATE_SERIES(1, 1000)" kinetica_bi/server/scripts/test-fixtures/v13-filter-fixture.sql &amp;&amp; grep -q "DROP TABLE IF EXISTS ki_home.v13_filter_fixture" kinetica_bi/server/scripts/test-fixtures/v13-filter-fixture.sql</automated>
  </verify>
  <done>
    `kinetica_bi/server/scripts/test-fixtures/v13-filter-fixture.sql` exists; contains the CREATE TABLE statement for `ki_home.v13_filter_fixture` with the locked schema (id INT, category VARCHAR(8), lat DOUBLE, lon DOUBLE, measure DOUBLE, bucket VARCHAR(4)); contains the INSERT INTO ... SELECT ... FROM TABLE(GENERATE_SERIES(1, 1000)) row-generator; contains the DROP TABLE IF EXISTS cleanup statement; contains the three verification SELECT comments. No other files modified.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator runs fixture against Kinetica + sets up UAT dashboards</name>
  <files>Kinetica DB row only (no repo file). Operator-driven; not committed to repo (the reference SQL was committed in Task 1).</files>
  <action>Operator-driven Kinetica-side setup. Executor MUST present the <how-to-verify> block verbatim to the user and PAUSE for the resume-signal. Do NOT proceed to Task 3 until operator types `fixture-ready` (or reports a `fixture-gap: ...` blocker). Concrete operator steps: (1) run `kinetica_bi/server/scripts/test-fixtures/v13-filter-fixture.sql` against deployed Kinetica with BI-user creds (Workbench / SQL tab / curl); (2) verify with the three sanity SELECTs (COUNT=1000, 8 categories ~125 each, CONUS bbox); (3) boot the BI app (`cd kinetica_bi && npm run dev` + `cd kinetica_bi/server && npm run dev`); (4) configure Dashboards A–H spanning the 4 user flows, 4 edge cases, and 8 chart types listed in <how-to-verify>; (5) capture verification numbers + dashboard IDs in a scratch note for Task 5. See <how-to-verify> for the full enumerated procedure.</action>
  <verify>
    <automated>echo 'CHECKPOINT — operator confirms fixture-ready resume-signal; no automated verification possible (Kinetica state + dev-server boot + browser dashboards are operator-side).'</automated>
  </verify>
  <done>Operator typed `fixture-ready` after completing all <acceptance_criteria> items: 1000 rows in `ki_home.v13_filter_fixture`, 8 categories confirmed, CONUS bbox confirmed, dev servers running, Dashboards A–H configured covering all 8 chart types, scratch note populated with verification SELECT outputs + dashboard IDs.</done>
  <what-built>
    Reference SQL script `kinetica_bi/server/scripts/test-fixtures/v13-filter-fixture.sql` from Task 1, ready for the operator to run against deployed Kinetica with BI-user creds. This task is the manual Kinetica-side setup that the executor cannot automate (operator must use Workbench / SQL tab / curl with their own creds).
  </what-built>
  <how-to-verify>
    Operator-driven steps (operator records evidence in a scratch note that feeds Task 5 VERIFICATION.md):

    1. **Run the fixture DDL/INSERT.** Open Kinetica Workbench (or SQL tab, or curl `/execute/sql` with your BI-user Bearer/Basic auth). Copy the contents of `kinetica_bi/server/scripts/test-fixtures/v13-filter-fixture.sql` and execute. Confirm:
       - `CREATE TABLE ki_home.v13_filter_fixture` succeeds (or the existing-table DROP runs cleanly first).
       - `INSERT INTO ... SELECT ... FROM TABLE(GENERATE_SERIES(1, 1000))` returns `count_affected: 1000` (or operator's hand-crafted-INSERT equivalent if GENERATE_SERIES is unavailable).
       - The three verification queries return: COUNT(*) = 1000; 8 distinct categories ~125 rows each; lat MIN..MAX ≈ 24..49; lon MIN..MAX ≈ -125..-67.
       - Record the actual numbers in the scratch note (these become the "Walkthrough Log" preamble in VERIFICATION.md).

    2. **Confirm the Kinetica session matches the BI app.** Make sure the auth-mode + creds you ran the DDL with match the BI app's runtime auth (operator-default is AUTH_MODE=password). If running OIDC, note that OIDC live DDL re-probe is explicitly DEFERRED — see Task 4 / VERIFICATION.md Open Gaps.

    3. **Boot the BI app.** In two terminals:
       - `cd kinetica_bi && npm run dev` (frontend on http://localhost:5173)
       - `cd kinetica_bi/server && npm run dev` (backend on http://localhost:4000)
       Log in as the BI user.

    4. **Set up UAT dashboards.** Recommended (per CONTEXT.md Claude's Discretion #5: per-flow dashboards for clean before/after screenshots):
       - **Dashboard A — single-table chart drill** (Flow 1): one chart widget on `ki_home.v13_filter_fixture` (e.g., bar chart `category` × `COUNT(*)`).
       - **Dashboard B — map drill + LAYERS-swap** (Flow 2): one map widget with a layer pointing at `ki_home.v13_filter_fixture` (latlon mode using `lat`/`lon`) + one chart widget on the same table to drive the filter.
       - **Dashboard C — multi-widget single-table** (Flow 3): chart + map + records-table all backed by `ki_home.v13_filter_fixture`. Verifies single materialize feeds all three.
       - **Dashboard D — lifecycle resets** (Flow 4): any dashboard with at least one chart on the fixture; you'll filter, then switch to another dashboard / log out.
       - **Dashboard E — V13-LIMIT-01 edge case**: records-table-only on `ki_home.v13_filter_fixture` (no chart, no map). Verifies materialize never fires.
       - **Dashboard F — map-only edge case**: map-only on `ki_home.v13_filter_fixture` (no chart). Verifies LAYERS=<table> permanently.
       - **Dashboard G — multi-table A/B isolation**: widgets on `ki_home.v13_filter_fixture` (table A) AND on a second pre-existing table (e.g., `demo.nyctaxi`, table B). Filter on A; verify B widgets unchanged.
       - **Dashboard H — empty/zero-widget**: empty dashboard, OR widget with no associated table. Verifies no errors and clean lifecycle reset.

    5. **Configure widgets to exercise all 8 chart types.** Across Dashboards A–H, ensure at least one widget per type uses `ki_home.v13_filter_fixture`: **bar**, **line**, **pie**, **scatter**, **table**, **records-table**, **big-number**, **map**. Use `category` as the filter dimension (8 values), `measure` as the aggregation, and `bucket` as the secondary dimension where useful. The big-number widget can show e.g. `COUNT(*)` or `AVG(measure)`.

    6. **Capture preliminary evidence** in a scratch text file (operator-local; you'll paste these into VERIFICATION.md in Task 5):
       - Verification-query output numbers (Step 1).
       - Dashboard IDs (visible in URL `/dashboards/<id>`) and widget tableId (visible in widget config or Network tab).
       - Confirmed: app loads, login works, fixture-backed widgets render against the raw table (no filter applied yet — should show all ~1000 points / 8 categories).
  </how-to-verify>
  <resume-signal>
    Type "fixture-ready" when: (a) `ki_home.v13_filter_fixture` exists in Kinetica with 1000 rows / 8 categories / CONUS bbox confirmed by the three verification SELECTs, (b) frontend + backend dev servers are running, (c) at least Dashboards A through H are configured with widgets covering all 8 chart types against the fixture, (d) you have a scratch note with the verification numbers + dashboard IDs ready to paste into VERIFICATION.md. If the fixture DDL fails (permissions, GENERATE_SERIES unavailable, etc.), report the verbatim error and we'll either route to a 17-NN gap-closure plan or fall back to hand-crafted INSERTs in the SQL script.
  </resume-signal>
  <acceptance_criteria>
    Operator confirms via resume signal "fixture-ready" with the following recorded in their scratch note (which Task 5 will then paste verbatim into VERIFICATION.md):
    - Verbatim output of `SELECT COUNT(*) FROM ki_home.v13_filter_fixture;` (expect 1000).
    - Verbatim output of `SELECT category, COUNT(*) FROM ki_home.v13_filter_fixture GROUP BY category ORDER BY category;` (expect 8 rows).
    - Verbatim output of `SELECT MIN(lat), MAX(lat), MIN(lon), MAX(lon) FROM ki_home.v13_filter_fixture;` (expect CONUS bbox).
    - Dashboard IDs for A through H (or a justification if any dashboard is consolidated; at minimum A, B, C, D must be distinct for clean per-flow evidence).
    - Confirmation that all 8 chart types (bar, line, pie, scatter, table, records-table, big-number, map) have at least one widget against the fixture across the dashboards.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator UAT walkthrough — 4 flows × 4 edge cases × 8 chart types + before/after map screenshots</name>
  <files>.planning/phases/17-verification/screenshots/map-filter-before.png, .planning/phases/17-verification/screenshots/map-filter-after.png</files>
  <action>Operator-driven UAT in browser against the running BI app (frontend :5173, backend :4000). Executor MUST present the <how-to-verify> block verbatim and PAUSE for the resume-signal. Do NOT proceed to Task 4 until operator types `uat-pass` OR `uat-gap: <description>` (gap routes to a 17-NN-PLAN.md follow-up per CONTEXT.md user-lock 6 Gap-handling protocol). Concrete operator coverage: (a) all 4 user flows with Network tab POST/DELETE/WMS URL evidence; (b) all 4 edge cases (V13-LIMIT-01, MAP-LIMIT-01, multi-table A/B, empty-state); (c) all 8 chart types (bar, line, pie, scatter, table, records-table, big-number, map); (d) before/after map screenshots saved at the exact paths in <files>; (e) capture 3 verbatim WMS URL excerpts: filtered (`LAYERS=_kbi_filt_<…>&_mv=N`), same-name re-materialize (`_mv=N+1`), cleared (`LAYERS=ki_home.v13_filter_fixture` no `_mv`); (f) StrictMode lifecycle observations (single mount, no blank-tile flash, ResizeObserver fires); (g) singular-POST evidence for Flow 3 multi-widget. See <how-to-verify> for the full enumerated procedure with expected behaviors per step.</action>
  <verify>
    <automated>test -s .planning/phases/17-verification/screenshots/map-filter-before.png &amp;&amp; test -s .planning/phases/17-verification/screenshots/map-filter-after.png</automated>
  </verify>
  <done>Operator typed `uat-pass` (or `uat-gap: ...` with follow-up plan created) and the two PNG screenshots exist non-empty at the paths above. All <acceptance_criteria> items satisfied: 4 user flow PASS/FAIL outcomes recorded with Network excerpts; 4 edge cases observed; 8 chart types one-liners; 3 WMS URL excerpts; StrictMode 3-sub-check observations.</done>
  <what-built>
    Per Task 1+2, the fixture is loaded into Kinetica and Dashboards A–H are configured with widgets across all 8 chart types. The BI app is running (frontend :5173, backend :4000). All Phase 13–16 src/ code is shipped and the executor has performed no src/ modifications. This task drives the operator through the comprehensive UAT walkthrough that closes the v1.3 milestone.
  </what-built>
  <how-to-verify>
    **Operator opens DevTools (Network tab + Console tab visible) on `http://localhost:5173`. For each step: record PASS/FAIL + evidence excerpt in the scratch note that feeds Task 5.**

    --- USER FLOWS (4) ---

    **Flow 1 — Chart drill → Clear All (Dashboard A, single-table).**
    1. Click an element in the bar chart (e.g., a `category='A'` bar). Expected within ~300ms: filter chip appears in Filter Bar; `<FilteringBadge>` with "filtering..." text appears in widget header; Network tab shows `POST /api/filter/materialize` with body `{ dashboardId, tableId, filters: [{ column: 'category', op: 'eq', value: 'A' }] }`; response is `{ viewName: "_kbi_filt_u<id>_d<id>_t<id>_s<8hex>", expiresAt: <epoch_ms> }`. The chart re-renders showing only `category='A'` data.
    2. Verify other chart-driving WMS or SQL queries on this table now use `FROM <viewName>` (Network tab: backend `/api/sql` request body contains the view name in the FROM clause; for the records-table widget on Dashboard C, row count narrows to ~125).
    3. Click `×` on the chip OR click "Clear All". Expected: chip disappears; chart reverts to all 1000 rows / 8 categories; `DELETE /api/filter/materialize` fires fire-and-forget in Network tab returning `{ dropped: true }`.
    4. Record PASS/FAIL + verbatim Network tab POST/DELETE URL excerpts.

    **Flow 2 — Map drill verification (LAYERS swap + tile narrow) (Dashboard B). CLOSES PHASE 16 DEFERRED CHECKS.**
    1. **Take a "before" screenshot** of the map widget (full unfiltered fixture, ~1000 points across CONUS). Save as `.planning/phases/17-verification/screenshots/map-filter-before.png`.
    2. Click a `category='A'` bar in the co-located chart. Expected: filter chip appears; `<MapFilteringBadge>` shows "filtering..."; chart narrows; map re-fetches WMS tiles.
    3. **In the Network tab, find the WMS GetMap request that fired AFTER the materialize.** Confirm the URL contains the EXACT substring pattern `LAYERS=_kbi_filt_u` AND `&_mv=` (any positive integer). Copy this URL for the report. Per `wmsUrlBuilder.ts:145-147`, `_mv` is emitted only when `materializeVersion !== undefined`, and per Phase 16 viewName regex `_kbi_filt_u\w+_d\d+_t\d+_s\w{8}`.
    4. **Take an "after" screenshot** of the map widget (only `category='A'` points visible — should be roughly 1/8 the density of the before image). Save as `.planning/phases/17-verification/screenshots/map-filter-after.png`. The visual difference must be unambiguous to the naked eye (this is the Phase 16 deferred Truth 1 visual side closure + TD-V12-04 closure).
    5. Apply a different filter value (e.g., `category='B'`) to the same chart on the same table. Expected: same view name (`CREATE OR REPLACE` semantics), but `_mv` value INCREMENTS in the URL (Phase 16 deferred check 3 closure). Record both URLs side-by-side.
    6. Click "Clear All". Expected: WMS GetMap re-fires with `LAYERS=ki_home.v13_filter_fixture` (raw table, schema-qualified) AND NO `_mv` substring at all (Phase 16 Truth 2 / deferred check 2 closure).
    7. **StrictMode lifecycle preservation check (Phase 16 deferred check 4):** Open DevTools Console and Network tab. Reload the dashboard. Confirm: only ONE OL Map construction occurred (no double-mount due to React 18 StrictMode dev-build effect-double-invocation); the filter-clear cycle did not produce a blank-tile flash (prior tiles remained until new tiles loaded); ResizeObserver `updateSize` fires when the widget moves into view (e.g., after a grid-cell drag). Record observations verbatim.
    8. Record PASS/FAIL + the 3 URL excerpts (LAYERS=view+_mv=N filtered, LAYERS=view+_mv=N+1 same-name re-materialize, LAYERS=ki_home.v13_filter_fixture cleared).

    **Flow 3 — Multi-widget single-table (Dashboard C, chart + map + records-table on one tableId).**
    1. Apply a filter via the chart. Expected: in Network tab, EXACTLY ONE `POST /api/filter/materialize` fires (verifies `AggregatedWidgetRenderer` is the sole trigger per VSTORE-V13-02 / MAP-V13-05 / FILT-V13-02 architecture). All three widgets (chart, map, records-table) re-query against the same view name.
    2. Confirm: chart narrows to filtered data; map tiles narrow (LAYERS=<view>&_mv=<n>); records-table row count drops to ~125.
    3. Clear filter. Expected: ONE `DELETE /api/filter/materialize` in Network tab. All 3 widgets revert.
    4. Record PASS/FAIL + the singular-POST evidence.

    **Flow 4 — Lifecycle resets — logout + dashboard-switch (Dashboard D + dashboard nav + logout).**
    1. Apply a filter on Dashboard D. Confirm `POST /api/filter/materialize` fires; view exists.
    2. Click another dashboard in the sidebar (e.g., Dashboard A or any pre-existing dashboard). Expected within the cleanup useEffect: `DELETE /api/filter/materialize` fires fire-and-forget in Network tab for the active view; `useFilterViewStore.getState().views` is empty (verify in Console: `Object.keys(useFilterViewStore.getState().views)` returns `[]`); `useFilterStore` chips also cleared.
    3. Re-apply a filter on the second dashboard. Click "Logout". Expected: `DELETE /api/filter/materialize` fires for the active view; both stores reset; user redirected to login.
    4. Record PASS/FAIL + Network tab DELETE excerpts.

    --- EDGE CASES (4) — observe and document, do not fix ---

    **Edge V13-LIMIT-01 — records-table-only on tableId X (Dashboard E).**
    1. Apply a filter intent (none possible without a chart driver — try clicking on a row in the records-table; expected: NO drill behavior, no chip added — records-table is a pure consumer per FILT-V13-02 lock).
    2. Confirm in Network tab: NO `POST /api/filter/materialize` ever fires; the records-table query uses `FROM ki_home.v13_filter_fixture` (raw, never `FROM _kbi_filt_…`).
    3. Document as **accepted limitation** (not a regression) per Phase 15 lock.

    **Edge map-only on tableId X (Dashboard F).**
    1. Confirm Network tab: WMS GetMap requests use `LAYERS=ki_home.v13_filter_fixture` permanently — no materialize trigger ever fires (no chart driver on this table).
    2. Document as **accepted limitation** (not a regression) per Phase 16 lock.

    **Edge multi-table A/B isolation (Dashboard G).**
    1. Apply filter on table A (`ki_home.v13_filter_fixture`). Expected: only A's widgets re-query (FROM-swap to view); table B widgets unchanged (Network tab confirms B's SQL still uses raw table; B's WMS, if any, still uses LAYERS=B-table).
    2. Verifies the per-tableId C-02 / S-02 scope lock — the v1.3 architectural goal.
    3. Record PASS/FAIL.

    **Edge empty-state widgets / zero-widget dashboards (Dashboard H).**
    1. Confirm: console shows no errors; no spurious `POST /api/filter/materialize` calls; dashboard-switch from H still runs lifecycle reset cleanly (Network: no DELETEs because no active views, but no console errors either).
    2. Record PASS/FAIL.

    --- 8 CHART TYPES — exercise each ---

    For each of the 8 types (bar, line, pie, scatter, table, records-table, big-number, map):
    1. Trigger a filter that affects this widget (via a co-located chart driver where the type itself can't drill, e.g., big-number / map / records-table).
    2. Confirm the widget's data narrows correctly after the filter materializes (FROM-swap landed for SQL-querying types; LAYERS-swap landed for map; records-table row count narrows; big-number aggregate updates).
    3. Record one-line PASS/FAIL per type in the scratch note (mark "via chart driver" for the non-drillable types).
  </how-to-verify>
  <resume-signal>
    Type "uat-pass" when ALL of the following are true; OR type "uat-gap: <description>" if you found a real bug (we'll route to 17-NN-PLAN.md per Gap-handling protocol; do NOT defer to v1.3.1):

    - All 4 user flows recorded with PASS/FAIL + Network tab evidence.
    - All 4 edge cases observed and documented (PASS or accepted-limitation).
    - All 8 chart types exercised with PASS/FAIL one-liners.
    - Before/after map screenshots saved at `.planning/phases/17-verification/screenshots/map-filter-before.png` and `map-filter-after.png` (visually unambiguous narrowing).
    - 3 verbatim WMS URL excerpts captured: filtered (`LAYERS=_kbi_filt_<…>&_mv=N`), same-name re-materialize (`_mv=N+1` with same LAYERS), cleared (`LAYERS=ki_home.v13_filter_fixture` no `_mv`).
    - StrictMode lifecycle observations recorded (single mount, no blank-tile flash, ResizeObserver fires).
    - Singular-POST evidence recorded for Flow 3.
  </resume-signal>
  <acceptance_criteria>
    - The two screenshots `.planning/phases/17-verification/screenshots/map-filter-before.png` and `.planning/phases/17-verification/screenshots/map-filter-after.png` exist on disk and are non-empty PNGs (`test -s ...`).
    - Operator's scratch note (which Task 5 pastes into VERIFICATION.md) contains:
      - 4 PASS/FAIL outcomes for the 4 user flows + raw Network tab POST/DELETE/WMS URL excerpts for at least Flows 1, 2, 3, 4.
      - 4 outcome-or-limitation notes for the 4 edge cases.
      - 8 PASS/FAIL one-liners for the 8 chart types.
      - The 3 WMS URL excerpts (filtered+_mv=N, same-name+_mv=N+1, cleared no-_mv).
      - StrictMode observations (3 sub-checks: single mount, no blank-tile flash, ResizeObserver fires).
    - If "uat-gap: ..." instead of "uat-pass", a follow-up `17-NN-PLAN.md` is created (separate plan, separate `/gsd:plan-phase` invocation if needed) BEFORE Task 5 runs. Real bugs are not papered over.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator TTL recovery exercise — proactive (DevTools), reactive (Kinetica DROP), max-1-retry cap (double-DROP)</name>
  <files>No repo files modified. Operator records verbatim commands + observations in scratch note for Task 5.</files>
  <action>Operator-driven TTL recovery exercise against the running BI app (still booted from Task 2/3) with Dashboard A or B + active filter. Executor MUST present the <how-to-verify> block verbatim and PAUSE for the resume-signal. Do NOT proceed to Task 5 until operator types `ttl-pass` OR `ttl-gap: <description>`. Three exercises: (1) Proactive (LIFE-V13-01) — verbatim 3-arg `useFilterViewStore.getState().setView(<tableId>, { viewName: live.viewName, expiresAt: Date.now() - 1000 }, live.dashboardId)` Console snippet; observe re-materialize fires + chart re-renders + no error toast. NOTE: this snippet uses the CORRECT 3-arg signature per `kinetica_bi/src/store/filterViewStore.ts:53`; the CONTEXT.md sample (1-arg form with materializeVersion in the second arg) is INCORRECT and must be flagged in VERIFICATION.md. (2) Reactive (LIFE-V13-02) — verbatim `DROP TABLE _kbi_filt_<live-view-name>;` against Kinetica; click chart; observe HTTP 400 error `SqlEngine: Object '<view>' not found (S/SDc:1513)` matched by `isViewNotFoundError` regex; reactive re-materialize + retry; chart updates silently. (3) Max-1-retry cap (Pitfall 3) — DROP twice rapidly; observe ≤ 2 materialize attempts; fall-through to raw `FROM ki_home.v13_filter_fixture`; no infinite loop; no error toast. OIDC TTL exercise SKIPPED per CONTEXT.md user-lock 4 (TTL is mode-agnostic). See <how-to-verify> for the full procedure with verbatim commands.</action>
  <verify>
    <automated>echo 'CHECKPOINT — operator confirms ttl-pass resume-signal; no automated verification possible (DevTools Console mutation + Kinetica DROP + observed Network sequence are operator-side).'</automated>
  </verify>
  <done>Operator typed `ttl-pass` (or `ttl-gap: ...` with follow-up plan created). All <acceptance_criteria> items recorded in scratch note: 3-arg setView Console snippet captured verbatim; `DROP TABLE _kbi_filt_<…>;` SQL captured verbatim; verbatim Kinetica error string `SqlEngine: Object '<view>' not found (S/SDc:1513)` captured; double-DROP no-infinite-loop confirmation; no error toast across all three exercises.</done>
  <what-built>
    Phase 15 LIFE-V13-01 (proactive `expiresAt` check), LIFE-V13-02 (reactive `isViewNotFoundError` recovery), and the Pitfall-3-locked `retryRef` max-1-retry cap. All three exist in src/ at this point and are covered by vitest. This task verifies they work end-to-end with verbatim commands documented for future reproducers.
  </what-built>
  <how-to-verify>
    Operator runs all three exercises against the running BI app (still booted from Task 2/3) with Dashboard A or B open and a filter applied so a view exists. Record verbatim commands + outcomes in the scratch note for Task 5.

    --- 1. PROACTIVE TTL (LIFE-V13-01) — DevTools `expiresAt` mutation ---

    Goal: Force the proactive `Date.now() >= expiresAt` branch in `MapChartRenderer` and `AggregatedWidgetRenderer` without waiting 5 minutes.

    Operator opens DevTools Console on the dashboard with an active filter (so `useFilterViewStore.getState().views[<tableId>]` is populated). Note the actual `tableId` (visible in widget config / Network tab — e.g., `7`).

    Run the VERBATIM Console snippet (the executor must paste this exactly into VERIFICATION.md as the reproducible reference):

    ```js
    // Replace <tableId>, <viewName>, <materializeVersion>, <dashboardId> with the live values from
    // useFilterViewStore.getState().views — read them first:
    //   useFilterViewStore.getState().views
    // Then mutate expiresAt to a past timestamp. Note: setView is 3-arg per Phase 14 store signature
    // (see kinetica_bi/src/store/filterViewStore.ts:53). expiresAt is inside the second arg; viewName
    // is inside the second arg; dashboardId is the third positional arg. materializeVersion is
    // managed by the store (auto-bumped on same-name setView), so the snippet does NOT pass it.
    const live = useFilterViewStore.getState().views[<tableId>];
    useFilterViewStore.getState().setView(
      <tableId>,
      { viewName: live.viewName, expiresAt: Date.now() - 1000 },
      live.dashboardId
    );
    ```

    Then trigger a re-query: click another value in the chart (or click the same value again to re-fire the filter intent) so a chart re-render happens. Expected:
    - Pre-query, the renderer's `isViewExpired(entry)` check returns `true` (`Date.now() >= expiresAt`).
    - Renderer silently re-materializes via `POST /api/filter/materialize` (visible in Network tab).
    - Chart still renders correctly with no error toast, no error UI.
    - `useFilterViewStore.getState().views[<tableId>].expiresAt` has been overwritten by `setView` with the fresh server-returned value.

    Record verbatim Console snippet + Network POST excerpt + observed expiresAt before/after.

    Note on CONTEXT.md: the CONTEXT.md sample console line (under "TTL expiry verification > Proactive") incorrectly suggested a 1-arg form passing materializeVersion+dashboardId inside the second arg. The locked store signature in `filterViewStore.ts:53` is 3-arg: `setView(tableId, { viewName, expiresAt }, dashboardId)`. **Use the snippet above (3-arg form) — it matches the actual code.** Record this fact in VERIFICATION.md so the next operator doesn't trip.

    --- 2. REACTIVE TTL (LIFE-V13-02) — Manual Kinetica DROP TABLE ---

    Goal: Force the Kinetica error path `SqlEngine: Object '<view-name>' not found (S/SDc:1513)` (Phase 13 spike S3 verbatim) and observe the `isViewNotFoundError` reactive re-materialize + retry.

    1. Apply a filter so a view exists. Read its name from Console: `useFilterViewStore.getState().views[<tableId>].viewName` — e.g., `_kbi_filt_uadmin_d3_t7_s1234abcd`.
    2. In Kinetica Workbench (or SQL tab, or curl `/execute/sql`), run the VERBATIM SQL:

       ```sql
       DROP TABLE _kbi_filt_uadmin_d3_t7_s1234abcd;
       ```

       (Substitute the live view name. Use UNQUALIFIED form per Phase 13 spike S4 / endpoint contract.) Confirm Kinetica returns OK / count_affected: 0 or 1.
    3. Back in the BI app, click a different filter value on the chart (or trigger any chart re-query — e.g., click a different bar). Expected:
       - First chart query fires `FROM _kbi_filt_…` and returns HTTP 400 with verbatim error string `SqlEngine: Object '_kbi_filt_uadmin_d3_t7_s1234abcd' not found (S/SDc:1513)` (visible in Network tab response body).
       - `isViewNotFoundError(err)` matches (`/SqlEngine: Object '[^']+' not found/i` regex per `kinetica_bi/src/lib/kineticaErrors.ts:20`).
       - Renderer clears `useFilterViewStore.views[tableId]`, re-fires `POST /api/filter/materialize`, gets a fresh viewName (or same name with bumped materializeVersion), and retries the chart query.
       - Chart updates correctly. NO error toast. NO error UI surface.
    4. Record verbatim Kinetica DROP SQL + verbatim Kinetica error string from Network tab + retry POST URL.

    --- 3. MAX-1-RETRY CAP (Pitfall 3 lock) — Double-DROP rapid sequence ---

    Goal: Verify the `retryRef` budget caps at 1 retry per fresh materialize. Second consecutive view-not-found falls through silently to raw `FROM <table>`.

    1. With a filter still active and view re-materialized from exercise 2, IMMEDIATELY (within ~1 second) DROP the new view AGAIN via Kinetica:

       ```sql
       DROP TABLE _kbi_filt_uadmin_d3_t7_s1234abcd;
       ```

       (Same view name — retains the same retryRef budget; if retryRef resets per fresh materialize, that's the cap.)
    2. Click the chart to trigger another query. Expected:
       - First chart query fires `FROM _kbi_filt_…` → HTTP 400 view not found.
       - Renderer attempts ONE recovery (re-materialize POST + retry).
       - If the retry's chart query ALSO returns view not found (because the view was DROPped a second time even faster than the retry), the renderer must NOT loop infinitely. It must fall through silently to raw `FROM ki_home.v13_filter_fixture` (or whatever table) — chart shows UNFILTERED data without error.
       - NO error toast. NO error UI. NO infinite POST loop in Network tab (verify by waiting 5+ seconds; Network tab should show at most 2 materialize attempts and then settle).
    3. This exercises the `retryRef` budget: 1 retry per chart-query lifecycle, fall-through on second failure (Pitfall 3 lock per Phase 15 + 15-CONTEXT.md "Reactive recovery max 1 retry").
    4. Record verbatim 2× DROP SQL + verbatim Network tab sequence (POST materialize 1, GET sql, POST materialize 2, GET sql falls through to raw table — confirm via FROM clause in the SQL request body).

    --- OIDC TTL — SKIPPED ---

    Per CONTEXT.md user-lock: TTL behavior is Kinetica-side; mode-agnostic. Do NOT exercise OIDC-mode TTL recovery. No notes needed.
  </how-to-verify>
  <resume-signal>
    Type "ttl-pass" when all three exercises are recorded with verbatim commands + observations in the scratch note for Task 5. Specifically:
    - Proactive: verbatim Console snippet + observed expiresAt before/after + Network POST URL.
    - Reactive: verbatim Kinetica DROP SQL + verbatim Kinetica error string + retry POST URL.
    - Max-1-retry cap: verbatim 2× DROP SQL + Network tab sequence showing fall-through (at most 2 materialize attempts, no infinite loop, no error toast).

    OR type "ttl-gap: <description>" if any exercise reveals a real bug (route to 17-NN-PLAN.md).
  </resume-signal>
  <acceptance_criteria>
    Operator's scratch note for Task 5 contains:
    - The exact 3-arg `useFilterViewStore.getState().setView(<tableId>, { viewName, expiresAt: Date.now() - 1000 }, dashboardId)` Console snippet (executor will paste this verbatim into VERIFICATION.md TTL Recovery section).
    - The exact `DROP TABLE _kbi_filt_<…>;` Kinetica SQL (with the live view name from operator's session).
    - The exact Phase 13 spike S3 error string format observed: `SqlEngine: Object '<view>' not found (S/SDc:1513)`.
    - Confirmation of "no infinite loop" on double-DROP: Network tab shows ≤ 2 materialize attempts and then settles into a raw-table query.
    - Confirmation of "no error toast" across all three exercises.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Author 17-VERIFICATION.md from operator notes (combined Phase 12 narrative + Phase 16 frontmatter format)</name>
  <files>.planning/phases/17-verification/17-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/17-verification/17-CONTEXT.md (decisions § "VERIFICATION.md structure" — required sections list)
    - .planning/phases/16-map-filtering/16-VERIFICATION.md (template precedent: frontmatter + criteria_status + human_verification + observable-truths table + required-artifacts table + key-link-verification table + requirements-coverage table)
    - .planning/phases/12-dashboard-layers-panel/12-VERIFICATION.md (template precedent: narrative Walkthrough Log + per-criterion table with PASS/SUPERSEDED markers + Follow-up Fixes section + Open Gap pattern + Recommended Next Step routing)
    - .planning/phases/15-chart-filtering/15-VERIFICATION.md (precedent for v1.3-style coverage citation tables + 332/332 vitest claim format)
    - .planning/phases/13-spikes-and-endpoint/13-SPIKE-NOTES.md (S2.b deferral context — VERIFICATION.md may add a brief reference, NOT a substitute)
    - .planning/REQUIREMENTS.md (VERIFY-V13-01 + VERIFY-V13-02 verbatim spec — for the per-criterion table rows)
    - .planning/ROADMAP.md (Phase 17 success criteria a/b/c/d verbatim)
    - .planning/STATE.md (decisions list — for Tech Debt Informational section)
    - The operator's scratch note from Tasks 2, 3, 4 (verification numbers, dashboard IDs, PASS/FAIL outcomes, verbatim URLs, verbatim Console snippet, verbatim Kinetica SQL, verbatim error string)
  </read_first>
  <action>
    Create `.planning/phases/17-verification/17-VERIFICATION.md` combining the Phase 12 narrative-first walkthrough format with the Phase 16 frontmatter-rich tables format, per CONTEXT.md decisions § "VERIFICATION.md structure".

    The file MUST contain these EXACT top-level sections in this order, with these EXACT names:

    1. **Frontmatter block (`---` YAML)** with these required keys:
       - `phase: 17-verification`
       - `verified_at: "2026-05-07"` (or actual UAT date if different — operator-supplied)
       - `verified_by: RPereira@kinetica.com` (or actual operator email)
       - `overall_status:` — set to `passed` if all 4 user flows + 4 edge cases + 8 chart types + 3 TTL exercises are PASS/accepted-limitation and no real bugs surfaced; set to `tech_debt` ONLY if UAT surfaced gaps that were inline-fixed (per Gap-handling protocol) and resulted in `17-NN-PLAN.md` follow-ups
       - `criteria_status:` map with 4 entries — `criterion_a:`, `criterion_b:`, `criterion_c:`, `criterion_d:` — each PASS / DEFERRED / FAIL (these map to ROADMAP Phase 17 success criteria a/b/c/d)
       - `gaps:` array — for each accepted limitation or DEFERRED item, an entry with `id`, `title`, `status` (accepted_limitation | deferred | resolved_inline), `description`. Mandatory entries: V13-LIMIT-01 (records-table-only on tableId X), MAP-LIMIT-01 (map-only on tableId X), OIDC-S2B-DEFERRED.
       - `human_verification:` — empty array `[]` if all visual/runtime checks closed in this UAT (which they should be); otherwise list any remaining checks
       - `env:` map — `frontend: vite-dev :5173`, `backend: node :4000`, `kinetica_schema: ki_home`, `auth_mode: password` (or oidc if operator ran OIDC), `browser: <operator-supplied>`, `os: <operator-supplied>`

    2. **`# Phase 17: verification — End-to-End Verification Report`** (top-level header)

    3. **`## Verdict`** — one-line summary, e.g.:
       > "All 4 user flows + 4 edge cases + 8 chart types + 3 TTL recovery exercises PASSED against the synthetic `ki_home.v13_filter_fixture` (1000 rows, 8 categories, CONUS spread). v1.3 milestone closes `passed` with one explicit DEFERRED item (OIDC S2.b live DDL re-probe — auth-deployment-readiness, not v1.3 feature scope) and two accepted limitations (V13-LIMIT-01, MAP-LIMIT-01)."

    4. **`## Walkthrough Log`** (Phase 12 narrative idiom) — prose paragraph(s) describing the UAT session. Include: fixture setup verification numbers (Task 2 step 1 outputs); dashboard layout (A through H); UAT order; any minor observations not gating. Reference the operator's scratch note details verbatim for command outputs.

    5. **`## Per-Criterion Status`** (Phase 16 table idiom) — table mapping ROADMAP Phase 17 success criteria a/b/c/d to PASS/DEFERRED/FAIL with evidence pointers:
       | # | Criterion (verbatim from ROADMAP) | Status | Evidence |
       |---|---|---|---|
       | a | Filter click → chart narrows → map narrows → Clear All → both revert | PASS | Flow 1 + Flow 2 evidence; before/after screenshots |
       | b | TTL expiry recovery (proactive + reactive) verified | PASS | TTL Recovery section below |
       | c | Supertest backend DDL coverage (password + oidc) cited | PASS | Test Coverage Citation section |
       | d | Vitest frontend FROM-swap + store coverage cited | PASS | Test Coverage Citation section |

    6. **`## Per-Flow + Edge-Case Status`** (matrix) — collapsible table:
       - 4 user flows × PASS/FAIL + Network tab POST/DELETE/WMS URL excerpts
       - 4 edge cases × PASS/accepted-limitation + observation note
       - 8 chart types × PASS/FAIL one-liners (mark "via chart driver" for non-drillable types)

    7. **`## Phase 16 Deferred Checks Closure`** — 4-row table mapping the verbatim items from `16-VERIFICATION.md` frontmatter `human_verification:` array to PASS evidence in this UAT. Each row cites the Flow / Edge that closed it:
       - Visual filter narrowing → Flow 2 (before/after screenshots)
       - Network-tab WMS URL inspection → Flow 2 (filtered/cleared URL excerpts)
       - Same-name re-materialize cache-bust → Flow 2 step 5 (`_mv` increment URL)
       - StrictMode v1.2 lifecycle preservation → Flow 2 step 7 (single mount + no blank-tile flash + ResizeObserver fires)

    8. **`## TTL Recovery`** — three subsections with VERBATIM commands from Task 4:
       - `### Proactive (LIFE-V13-01)` — verbatim 3-arg `useFilterViewStore.getState().setView(<tableId>, { viewName, expiresAt: Date.now() - 1000 }, dashboardId)` Console snippet + observed expiresAt before/after + Network POST URL excerpt. Include the explanatory note about the CONTEXT.md sample being incorrect (1-arg form vs the locked 3-arg signature) so the next operator doesn't trip.
       - `### Reactive (LIFE-V13-02)` — verbatim `DROP TABLE _kbi_filt_<…>;` SQL + verbatim Kinetica error string `SqlEngine: Object '<view>' not found (S/SDc:1513)` + retry POST URL excerpt.
       - `### Max-1-Retry Cap (Pitfall 3)` — verbatim 2× DROP SQL + Network tab sequence (POST materialize 1 → GET sql 400 → POST materialize 2 → GET sql falls-through to raw `FROM ki_home.v13_filter_fixture`) + confirmation "no infinite loop, no error toast".

    9. **`## Test Coverage Citation`** — two subsections:
       - `### Backend (supertest)` — table with file paths + counts:
         | File | Tests | Covers |
         |---|---|---|
         | `kinetica_bi/server/tests/routes.filter-materialize.spec.ts` | <count> | POST + DELETE under AUTH_MODE=password and AUTH_MODE=oidc with stubbed sessions; DDL SQL-string shape; view-name regex; empty-filters→DROP path; 403 on permission denial |
         | `kinetica_bi/server/tests/lib.viewNaming.spec.ts` | <count> | view-name builder + OIDC sanitization (VIEW-V13-03) |
         | `kinetica_bi/server/tests/lib.whereClause.spec.ts` | <count> | server-side WHERE builder (VIEW-V13-06) |
         Total server vitest count + supertest pass status.
         **DO NOT cite supertest mock coverage as a substitute for the OIDC S2.b live re-probe.** Be explicit: "These tests cover the auth-mode wiring at request-handler level under stubbed OIDC sessions; they do NOT replace a live DDL probe against deployed Kinetica with OIDC trust." (See Open Gaps section.)
       - `### Frontend (vitest)` — table with file paths + counts (Phase 14/15/16):
         | File | Covers |
         |---|---|
         | `kinetica_bi/src/store/filterViewStore.spec.ts` | VSTORE-V13-01..03 |
         | `kinetica_bi/src/api/client.spec.ts` (materializeFilter / dropFilterView blocks) | VSTORE-V13-04 |
         | `kinetica_bi/src/components/charts/WidgetRenderer.spec.tsx` (FROM-swap + materialize trigger) | FILT-V13-01..05 |
         | `kinetica_bi/src/components/charts/MapChartRenderer.spec.tsx` (LAYERS-swap + viewsKey + pure-consumer-lock 16-A..16-E) | MAP-V13-01..06 |
         | `kinetica_bi/src/lib/wmsUrlBuilder.spec.ts` | MAP-V13-03..06 |
         | `kinetica_bi/src/lib/viewExpiry.spec.ts` | LIFE-V13-01 |
         | `kinetica_bi/src/lib/kineticaErrors.spec.ts` (if exists) | LIFE-V13-02 |
         | `kinetica_bi/src/components/MapFilteringBadge.spec.tsx` | Phase 16 badge |
         | `kinetica_bi/src/App.spec.tsx` (logout reset hook) | LIFE-V13-03 |
         | `kinetica_bi/src/components/DashboardsPage.spec.tsx` (dashboard-switch cleanup) | LIFE-V13-04 |
         Total frontend vitest count: 332/332 expected (per Phase 16 close); confirm via final vitest run.

    10. **`## Open Gaps + Accepted Limitations`** — three required entries (mandatory exact headings):
        - `### V13-LIMIT-01 — Records-table-only on tableId X (accepted limitation)` — Phase 15 lock; Dashboard E observation; not a regression. Future scope only if users hit it routinely.
        - `### MAP-LIMIT-01 — Map-only on tableId X (accepted limitation)` — Phase 16 lock; Dashboard F observation; parallel to V13-LIMIT-01.
        - `### OIDC-S2B-DEFERRED — Live DDL re-probe deferred to production-deploy gate` — Per CONTEXT.md user-lock 5: skip the live re-probe in this verification cycle. Document the deferral with rationale (architectural separation: OIDC DDL is auth-deployment-readiness, not v1.3 feature scope; pattern matches v1.0/v1.1 deferred items like TD-V11-03). **Explicitly state: "Phase 13 supertest mock coverage is NOT cited as substitute for this live probe."** Tracking: this section + PROJECT.md backlog single-line entry (Task 6) + MILESTONES.md v1.3 known-gaps line (Task 6).

    11. **`## Tech Debt (Informational)`** — bullet list of carry-forward items that surfaced or remain post-Phase 17. Required minimum entries (per CONTEXT.md):
        - `bumpMaterializeVersion` action removal candidate — Phase 14 ships the action; no caller emerged across Phases 14/15/16. Removal is a separate cleanup commit (not Phase 17 scope).
        - `widget.config.layerName` legacy fallback removal candidate — Phase 16 left as-is.
        - Plus any UAT-surfaced minor items the operator flagged in Tasks 3/4 (a11y notes, console-warning observations, etc.) — optional.

    12. **`## Recommended Next Step`** — single line: "Run `/gsd:audit-milestone v1.3` to consume this report and evolve PROJECT.md / MILESTONES.md for v1.3 close."

    Authoring notes:
    - Do NOT touch any src/ file in this task.
    - Pull verbatim Network tab URL excerpts, dashboard IDs, verification SELECT outputs, and Console snippets from the operator's scratch note (Tasks 2/3/4).
    - The before/after screenshots already exist on disk from Task 3; reference them inline in the Walkthrough Log + Phase 16 Deferred Checks Closure sections (e.g., `![map-filter-before](./screenshots/map-filter-before.png)` and `![map-filter-after](./screenshots/map-filter-after.png)`).
    - Run `cd kinetica_bi && npm run vitest -- --run` and `cd kinetica_bi && npx tsc --noEmit` to confirm 332/332 + tsc clean still hold; cite the actual numbers in Test Coverage Citation. (If the count differs from 332, investigate — Phase 17 should leave coverage flat.)
    - Run `cd kinetica_bi/server && npm run vitest -- --run` to capture the server-side count.
  </action>
  <verify>
    <automated>test -f .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "^phase: 17-verification" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "^overall_status:" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "criterion_a:" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "## Walkthrough Log" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "## Per-Criterion Status" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "## Per-Flow + Edge-Case Status" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "## Phase 16 Deferred Checks Closure" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "## TTL Recovery" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "### Proactive (LIFE-V13-01)" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "### Reactive (LIFE-V13-02)" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "### Max-1-Retry Cap" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "## Test Coverage Citation" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "## Open Gaps + Accepted Limitations" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "V13-LIMIT-01" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "MAP-LIMIT-01" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "OIDC-S2B-DEFERRED" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "useFilterViewStore.getState().setView" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "DROP TABLE _kbi_filt_" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "SqlEngine: Object" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "S/SDc:1513" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "LAYERS=_kbi_filt_" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "LAYERS=ki_home.v13_filter_fixture" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "## Tech Debt" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; grep -q "## Recommended Next Step" .planning/phases/17-verification/17-VERIFICATION.md &amp;&amp; ! grep -q "Phase 13 supertest.*substitute" .planning/phases/17-verification/17-VERIFICATION.md</automated>
  </verify>
  <done>
    `17-VERIFICATION.md` exists with all 12 required sections (frontmatter, top-level header, Verdict, Walkthrough Log, Per-Criterion Status, Per-Flow + Edge-Case Status, Phase 16 Deferred Checks Closure, TTL Recovery with 3 verbatim subsections, Test Coverage Citation with backend + frontend tables, Open Gaps + Accepted Limitations with V13-LIMIT-01 + MAP-LIMIT-01 + OIDC-S2B-DEFERRED entries, Tech Debt Informational, Recommended Next Step).
    Frontmatter contains `phase`, `verified_at`, `overall_status`, `criteria_status` map (4 entries), `gaps` array, `human_verification` array, `env` map.
    The verbatim 3-arg `useFilterViewStore.getState().setView(...)` snippet is in the Proactive subsection.
    The verbatim `DROP TABLE _kbi_filt_<…>;` SQL is in the Reactive subsection.
    The verbatim `SqlEngine: Object '<view>' not found (S/SDc:1513)` error string is cited.
    The 3 WMS URL excerpts (filtered+_mv=N, same-name+_mv=N+1, cleared no-_mv) are present.
    The OIDC deferral is explicit and does NOT cite Phase 13 supertest mock coverage as a substitute.
    Vitest 332/332 + tsc clean confirmed via re-run; counts cited inline.
  </done>
</task>

<task type="auto">
  <name>Task 6: Update STATE/ROADMAP/REQUIREMENTS + final commit; surface OIDC-S2B-DEFERRED to PROJECT.md backlog + MILESTONES.md</name>
  <files>
    .planning/STATE.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/PROJECT.md
    .planning/MILESTONES.md
  </files>
  <read_first>
    - .planning/STATE.md (current frontmatter + Current Position + Decisions sections)
    - .planning/ROADMAP.md (Phase 17 entry under "v1.3 Unified Dashboard Filtering"; Phases table)
    - .planning/REQUIREMENTS.md (Traceability table — VERIFY-V13-01 + VERIFY-V13-02 rows; Verification section status checkboxes)
    - .planning/PROJECT.md (Active section / backlog block where Known Gaps / Deferred items live; surface OIDC-S2B-DEFERRED)
    - .planning/MILESTONES.md (v1.3 entry placeholder if any; or schema for adding v1.3 known-gaps line)
    - .planning/phases/17-verification/17-VERIFICATION.md (just-written; final source of truth for status values)
  </read_first>
  <action>
    Apply the following EXACT edits across the five files. Each edit must match the file's existing pattern verbatim — no formatting drift.

    --- 1. .planning/STATE.md ---

    - Update frontmatter:
      - `status: complete` (was `unknown`)
      - `stopped_at: Phase 17 complete; v1.3 milestone ready for /gsd:audit-milestone` (was `Phase 17 context gathered`)
      - `last_updated: <ISO timestamp at edit time>`
      - `progress.completed_phases: 5` (was `4`)
      - `progress.total_plans: 13` (was `12`)
      - `progress.completed_plans: 13` (was `12`)
    - Update body `## Current Position`:
      - `Phase: 17 (verification) — COMPLETE`
      - `Plan: 1 of 1 (1 complete)`
    - Append a new row to `## Performance Metrics > By Plan (v1.3)` table: `| Phase 17 P01 | <duration> | 6 tasks | <fileCount> files |` — duration in minutes, fileCount = sum of files modified across Tasks 1+5+6 (fixture + verification + STATE/ROADMAP/REQUIREMENTS/PROJECT/MILESTONES).
    - Append to `### Phase 16` (or add a new `### Phase 17` block under `## Decisions`) — record the Phase 17 lock decisions for traceability:
      - `[Phase 17]: Synthetic CREATE TABLE on Kinetica with 5–10 distinct values + 500–2000 CONUS-spread points + temporary lifecycle (operator drops post-VERIFICATION.md). Reference SQL committed at kinetica_bi/server/scripts/test-fixtures/v13-filter-fixture.sql.`
      - `[Phase 17]: Atomic single-plan verification (17-01-PLAN.md) per user-lock — mirrors Phase 16 atomic-plan choice.`
      - `[Phase 17]: TTL recovery verified via shortcut paths — DevTools Console mutation for proactive (LIFE-V13-01), manual Kinetica DROP for reactive (LIFE-V13-02), double-DROP for max-1-retry cap (Pitfall 3 lock).`
      - `[Phase 17]: OIDC S2.b live DDL re-probe explicitly DEFERRED — auth-deployment-readiness, not v1.3 feature scope; tracked in VERIFICATION.md / PROJECT.md / MILESTONES.md. NOT papered over with Phase 13 supertest mock coverage.`
      - `[Phase 17]: Phase 16 deferred 4 human-needed checks all closed in Phase 17 UAT (visual narrowing via before/after screenshots; network-tab WMS URLs; _mv cache-bust runtime; StrictMode v1.2 lifecycle).`
      - `[Phase 17]: bumpMaterializeVersion action removal flagged for tech-debt cleanup — no caller emerged across Phases 14/15/16; removal is separate cleanup commit, not Phase 17 scope.`
    - Update `## Blockers/Concerns`:
      - Strikethrough the SPIKE-V13-02 OIDC deferral mention OR move to a new "Resolved post-Phase 17" subsection: replace with `**OIDC S2.b live DDL re-probe** — DEFERRED to production-deploy gate per Phase 17 verification close. Tracked in 17-VERIFICATION.md Open Gaps + PROJECT.md backlog + MILESTONES.md v1.3 known-gaps. Pattern matches v1.0/v1.1 production-deploy gate items (e.g., TD-V11-03).`
    - Update `## Session Continuity`:
      - `Last session: <ISO timestamp>`
      - `Stopped at: Phase 17 complete; v1.3 milestone ready for /gsd:audit-milestone`
      - `Resume file: .planning/phases/17-verification/17-VERIFICATION.md`

    --- 2. .planning/ROADMAP.md ---

    - In `### 🚧 v1.3 Unified Dashboard Filtering (In Progress)` checklist, change Phase 17 entry from:
      `- [ ] **Phase 17: verification** — Test fixture + end-to-end verification (closes TD-V12-01 + TD-V12-04)`
      to:
      `- [x] **Phase 17: verification** — Test fixture + end-to-end verification (closes TD-V12-01 + TD-V12-04) (completed <YYYY-MM-DD>)`
    - Update the milestone heading line: change `🚧 **v1.3 Unified Dashboard Filtering** — Phases 13-17 (in progress)` to:
      `🚧 **v1.3 Unified Dashboard Filtering** — Phases 13-17 (verification complete; awaiting /gsd:audit-milestone for formal close)`
    - In `### Phase 17: verification` body, update **Plans**: from `**Plans**: TBD` to `**Plans**: 1 plan (atomic per user lock)` and append `- [x] 17-01-PLAN.md — Test fixture + UAT walkthrough + VERIFICATION.md authoring (VERIFY-V13-01, VERIFY-V13-02) — completed <YYYY-MM-DD>`
    - In `## Progress` table, change Phase 17 row: from `| 17. verification | v1.3 | 0/TBD | Not started | - |` to `| 17. verification | v1.3 | 1/1 | Complete | <YYYY-MM-DD> |`

    --- 3. .planning/REQUIREMENTS.md ---

    - In `### Verification` section, change both checkboxes:
      `- [ ] **VERIFY-V13-01**: ...` → `- [x] **VERIFY-V13-01**: ...`
      `- [ ] **VERIFY-V13-02**: ...` → `- [x] **VERIFY-V13-02**: ...`
    - In `## Traceability` table, update both rows:
      `| VERIFY-V13-01 | Phase 17 | Pending |` → `| VERIFY-V13-01 | Phase 17 | Complete |`
      `| VERIFY-V13-02 | Phase 17 | Pending |` → `| VERIFY-V13-02 | Phase 17 | Complete |`
    - Update the `**Coverage:**` block summary to reflect 33/33 Complete (no Pending rows). Update the Last-updated footer line to today's date.

    --- 4. .planning/PROJECT.md ---

    - Locate the v1.3 backlog / Known Gaps / Deferred Items block (the file structure is established by prior milestone closes — search for "Deferred" or "Known Gaps" or "Backlog" or "Active" header). Append a single-line entry under v1.3:
      `- **OIDC-S2B-DEFERRED** (v1.3 production-deploy gate): Live DDL re-probe (CREATE OR REPLACE MATERIALIZED VIEW + DROP via deployed AUTH_MODE=oidc against real Kinetica with OIDC trust) was explicitly skipped during Phase 17 verification. Architectural separation rationale: OIDC DDL is auth-deployment-readiness, not v1.3 feature scope. Tracked in 17-VERIFICATION.md Open Gaps. Pattern matches v1.0 TD-01 / v1.1 TD-V11-03 production-deploy gate items.`
    - If a v1.3 milestone status block exists, mark "verification complete; ready for /gsd:audit-milestone".

    --- 5. .planning/MILESTONES.md ---

    - Locate the v1.3 entry (or append a new one if not yet present — schema follows v1.2 entry structure: heading, status, audit, requirements counts, known gaps).
    - Add or update v1.3 known-gaps line:
      `- **Known gaps (carried to v1.3 production-deploy gate):** OIDC-S2B-DEFERRED — live DDL re-probe under AUTH_MODE=oidc against deployed Kinetica with OIDC trust; tracked in PROJECT.md backlog + 17-VERIFICATION.md Open Gaps.`
    - If the v1.3 entry has a "Status" line, set it to `verification complete; awaiting /gsd:audit-milestone formal close (target: passed)`.

    --- COMMIT ---

    Stage and commit using the gsd-tools wrapper (do NOT run raw git commands; the project commit protocol is gsd-managed):

    ```bash
    node "/Users/rydelpereira/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(17-01): complete v1.3 verification + close milestone-readiness" --files \
      kinetica_bi/server/scripts/test-fixtures/v13-filter-fixture.sql \
      .planning/phases/17-verification/screenshots/map-filter-before.png \
      .planning/phases/17-verification/screenshots/map-filter-after.png \
      .planning/phases/17-verification/17-VERIFICATION.md \
      .planning/STATE.md \
      .planning/ROADMAP.md \
      .planning/REQUIREMENTS.md \
      .planning/PROJECT.md \
      .planning/MILESTONES.md
    ```

    (The fixture SQL + screenshots are committed alongside the doc updates as a single atomic v1.3 verification close commit. If commit_docs is split-mode in this repo, follow the project's gsd-tools commit invocation pattern — but the trailing `--files` list above is the authoritative artifact set for this phase close.)
  </action>
  <verify>
    <automated>grep -q "^status: complete" .planning/STATE.md &amp;&amp; grep -q "completed_phases: 5" .planning/STATE.md &amp;&amp; grep -q "completed_plans: 13" .planning/STATE.md &amp;&amp; grep -q "Phase: 17 (verification) — COMPLETE" .planning/STATE.md &amp;&amp; grep -q "\[Phase 17\]:" .planning/STATE.md &amp;&amp; grep -q "\[x\] \*\*Phase 17: verification\*\*" .planning/ROADMAP.md &amp;&amp; grep -q "17. verification | v1.3 | 1/1 | Complete" .planning/ROADMAP.md &amp;&amp; grep -q "\[x\] \*\*VERIFY-V13-01\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -q "\[x\] \*\*VERIFY-V13-02\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -q "VERIFY-V13-01 | Phase 17 | Complete" .planning/REQUIREMENTS.md &amp;&amp; grep -q "VERIFY-V13-02 | Phase 17 | Complete" .planning/REQUIREMENTS.md &amp;&amp; grep -q "OIDC-S2B-DEFERRED" .planning/PROJECT.md &amp;&amp; grep -q "OIDC-S2B-DEFERRED\|Known gaps" .planning/MILESTONES.md &amp;&amp; (cd kinetica_bi &amp;&amp; npx tsc --noEmit) &amp;&amp; (cd kinetica_bi &amp;&amp; npm run vitest -- --run 2>&amp;1 | grep -E "Test Files.*passed|Tests.*passed")</automated>
  </verify>
  <done>
    All five doc files updated with the exact edits described:
    - STATE.md frontmatter shows status=complete, completed_phases=5, completed_plans=13; Phase 17 marked COMPLETE in Current Position; Phase 17 decisions block appended; Session Continuity updated; OIDC S2.b deferral entry preserved (now resolved as production-deploy gate item).
    - ROADMAP.md Phase 17 row marked `[x]` with completed date; v1.3 milestone heading reflects verification-complete status; Plans section lists 17-01-PLAN.md complete; Progress table row shows `1/1 Complete`.
    - REQUIREMENTS.md VERIFY-V13-01 + VERIFY-V13-02 checkboxes flipped to `[x]`; Traceability rows show `Complete`; Coverage block summary updated to 33/33 Complete.
    - PROJECT.md backlog has the OIDC-S2B-DEFERRED single-line entry.
    - MILESTONES.md v1.3 entry has the known-gaps line referencing OIDC-S2B-DEFERRED.
    - tsc --noEmit exits 0 (no regression from doc work).
    - vitest 332/332 still green (no regression).
    - All edits committed via gsd-tools as a single atomic docs commit (`docs(17-01): complete v1.3 verification + close milestone-readiness`).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 7: Operator drops the temporary fixture table from Kinetica (cleanup)</name>
  <files>Kinetica DB row only (no repo file). The reference SQL at kinetica_bi/server/scripts/test-fixtures/v13-filter-fixture.sql remains committed for reproducibility (committed in Task 6).</files>
  <action>Operator runs the verbatim cleanup SQL against deployed Kinetica with BI-user creds: `DROP TABLE IF EXISTS ki_home.v13_filter_fixture;`. Executor MUST present the <how-to-verify> block and PAUSE for the resume-signal `fixture-dropped`. This honors the temporary-fixture lifecycle locked in CONTEXT.md user-lock 2. Any active `_kbi_filt_<…>` materialized views auto-drop within ~5 minutes via the sliding TTL (LIFE-V13-05); no manual cleanup needed for those. After this task: Phase 17 is complete; recommended next gsd command is `/gsd:audit-milestone v1.3`.</action>
  <verify>
    <automated>echo 'CHECKPOINT — operator confirms fixture-dropped resume-signal; no automated verification possible (Kinetica DROP TABLE is operator-side against deployed instance).'</automated>
  </verify>
  <done>Operator typed `fixture-dropped`. `ki_home.v13_filter_fixture` no longer exists in deployed Kinetica (verified by operator via subsequent SELECT returning table-not-found OR Kinetica schema browser confirmation). Reference SQL remains committed in repo per Task 6 commit.</done>
  <what-built>
    Per CONTEXT.md user-lock 2: the `ki_home.v13_filter_fixture` table is TEMPORARY — created at start of UAT (Task 2), dropped after VERIFICATION.md is finalized. The reference SQL script in `kinetica_bi/server/scripts/test-fixtures/v13-filter-fixture.sql` is the persistent artifact (already committed in Task 6). The Kinetica DB row is ephemeral.
  </what-built>
  <how-to-verify>
    Operator runs the cleanup SQL against Kinetica using BI-user creds (Workbench / SQL tab / curl):

    ```sql
    DROP TABLE IF EXISTS ki_home.v13_filter_fixture;
    ```

    Verify: subsequent `SELECT COUNT(*) FROM ki_home.v13_filter_fixture;` returns a "table not found" error (or operator's Kinetica UI shows the table no longer in the schema browser).

    Operator may keep the dev servers (`npm run dev`) running or shut them down — no longer required for any verification step.

    Note on data left over: any `_kbi_filt_<…>` materialized views still alive at the time of cleanup will auto-drop within ~5 minutes via the sliding TTL (per LIFE-V13-05). No manual cleanup needed for those.
  </how-to-verify>
  <resume-signal>
    Type "fixture-dropped" when `ki_home.v13_filter_fixture` no longer exists in Kinetica. Phase 17 is then complete; the next gsd command should be `/gsd:audit-milestone v1.3` (or `/gsd:summarize-phase 17` if your workflow runs that first).
  </resume-signal>
  <acceptance_criteria>
    Operator confirms via resume signal "fixture-dropped" that:
    - `DROP TABLE IF EXISTS ki_home.v13_filter_fixture;` was run successfully against deployed Kinetica.
    - The table is no longer queryable / no longer present in the schema browser.
    - The reference SQL script at `kinetica_bi/server/scripts/test-fixtures/v13-filter-fixture.sql` remains committed in the repo for reproducibility (verified by Task 6 commit).
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all 7 tasks complete:

1. **Artifact existence checks** (run from repo root):
   - `test -f kinetica_bi/server/scripts/test-fixtures/v13-filter-fixture.sql`
   - `test -s .planning/phases/17-verification/screenshots/map-filter-before.png`
   - `test -s .planning/phases/17-verification/screenshots/map-filter-after.png`
   - `test -f .planning/phases/17-verification/17-VERIFICATION.md`

2. **VERIFICATION.md structural integrity** (all required sections + verbatim commands):
   - All 12 sections present (frontmatter, Verdict, Walkthrough Log, Per-Criterion Status, Per-Flow + Edge-Case Status, Phase 16 Deferred Checks Closure, TTL Recovery + 3 subsections, Test Coverage Citation + 2 subsections, Open Gaps + 3 entries, Tech Debt, Recommended Next Step).
   - Frontmatter has `phase`, `verified_at`, `overall_status`, `criteria_status` (4 entries), `gaps`, `human_verification`, `env`.
   - Verbatim 3-arg `useFilterViewStore.getState().setView(...)` Console snippet present.
   - Verbatim `DROP TABLE _kbi_filt_<…>;` SQL present.
   - Verbatim Kinetica error string `SqlEngine: Object '<view>' not found (S/SDc:1513)` present.
   - 3 WMS URL excerpts (filtered+_mv=N, same-name+_mv=N+1, cleared no-_mv) present.
   - OIDC-S2B-DEFERRED entry explicit; NO claim that Phase 13 supertest mocks substitute for the live probe.

3. **Doc-tree consistency** (STATE / ROADMAP / REQUIREMENTS / PROJECT / MILESTONES all aligned):
   - STATE.md frontmatter: status=complete, completed_phases=5, completed_plans=13.
   - ROADMAP.md Phase 17 marked `[x]`; Progress table row `Complete`.
   - REQUIREMENTS.md VERIFY-V13-01 + VERIFY-V13-02 = Complete in both checkboxes and Traceability table.
   - PROJECT.md backlog has OIDC-S2B-DEFERRED entry.
   - MILESTONES.md v1.3 entry has known-gaps line referencing OIDC-S2B-DEFERRED.

4. **No code regression**:
   - `cd kinetica_bi && npx tsc --noEmit` exits 0.
   - `cd kinetica_bi && npm run vitest -- --run` reports 332/332 pass (or whatever the post-Phase-16 baseline was; should not drop).
   - `cd kinetica_bi/server && npm run vitest -- --run` reports server-side count consistent with Phase 13/15 baseline (no regression from doc work).

5. **Single atomic commit** for the verification close (Task 6 commit) — verify via `git log --oneline -1` shows `docs(17-01): complete v1.3 verification + close milestone-readiness` (or equivalent gsd-tools-formatted message).

6. **Fixture cleanup confirmed** — operator dropped `ki_home.v13_filter_fixture` from Kinetica (Task 7).
</verification>

<success_criteria>
Phase 17 is complete and the v1.3 milestone is ready for `/gsd:audit-milestone` when:

1. **VERIFY-V13-01 satisfied**: Synthetic low-cardinality + spatially-spread fixture exists; reference SQL committed; operator confirmed it produced visually unambiguous filter narrowing in UAT.

2. **VERIFY-V13-02 satisfied**: 17-VERIFICATION.md authored with all 12 required sections; PASS / DEFERRED / FAIL recorded per success criterion (a/b/c/d); 4 user flows + 4 edge cases + 8 chart types covered; before/after screenshots embedded; TTL recovery verified across 3 exercises with verbatim commands; backend + frontend test coverage cited.

3. **TD-V12-01 closed**: Phase 16 deferred Truth 1 visual side closed via screenshots; Phase 16 deferred 4 human-needed checks all PASS in 17-VERIFICATION.md.

4. **TD-V12-04 closed**: Reproducible synthetic fixture with operator-controlled cardinality + CONUS spread; before/after screenshots prove unambiguous narrowing.

5. **OIDC S2.b honestly deferred**: Documented as DEFERRED in 17-VERIFICATION.md Open Gaps; surfaced in PROJECT.md backlog + MILESTONES.md v1.3 known-gaps; NO papering over with Phase 13 supertest mock claims.

6. **Gap-handling protocol respected**: If UAT surfaced real bugs, follow-up `17-NN-PLAN.md` plans were created and executed inline within Phase 17 (NOT deferred to v1.3.1; NOT routed via `/gsd:insert-phase`). If no real bugs surfaced, proceed straight to milestone-close.

7. **Code regression-free**: `tsc --noEmit` exits 0; `vitest` suite still 332/332 green (or current baseline); no src/ files modified in Phase 17 happy path.

8. **Doc-tree fully aligned**: STATE.md / ROADMAP.md / REQUIREMENTS.md / PROJECT.md / MILESTONES.md all reflect Phase 17 complete + v1.3 milestone-close-ready.

9. **Operator handoff**: Fixture table dropped from Kinetica (temporary lifecycle honored); reference SQL committed for reproducibility; next command for the user is `/gsd:audit-milestone v1.3`.
</success_criteria>

<output>
After completion, create `.planning/phases/17-verification/17-01-SUMMARY.md` capturing:

- Tasks completed (1–7) with commit hashes for Tasks 1 (fixture SQL) and 6 (consolidated doc commit).
- Total Phase 17 duration (from Task 1 commit to Task 6 commit).
- Files created (fixture SQL + 17-VERIFICATION.md + 2 screenshots).
- Files modified (STATE / ROADMAP / REQUIREMENTS / PROJECT / MILESTONES).
- Test counts at close: server supertest count, frontend vitest count (target: 332/332 + tsc clean — no regression).
- Outcome summary: VERIFY-V13-01 + VERIFY-V13-02 marked Complete; TD-V12-01 + TD-V12-04 closed; OIDC-S2B-DEFERRED tracked.
- Recommended next command: `/gsd:audit-milestone v1.3`.
- Any inline gap-fix plans (17-02-PLAN.md, etc.) that were created during Phase 17 — list with their resolutions; if none, state "No UAT-surfaced gaps; happy path."
- Any tech-debt items surfaced for v1.3 backlog (e.g., `bumpMaterializeVersion` removal, `widget.config.layerName` legacy fallback).
</output>
