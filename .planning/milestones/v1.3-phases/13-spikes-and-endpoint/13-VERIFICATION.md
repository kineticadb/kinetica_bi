---
phase: 13-spikes-and-endpoint
verified: 2026-05-06T18:43:28Z
status: passed
score: 11/11 must-haves verified
human_verification:
  - test: "SPIKE-V13-02 OIDC re-probe (S2.b)"
    expected: "CREATE OR REPLACE MATERIALIZED VIEW with `Authorization: Bearer <oidc_access_token>` returns `status: OK` against deployed Kinetica"
    why_human: "No OIDC access token was reachable in the spike environment. Live re-probe required against deployed Kinetica; explicitly tracked for Phase 15/17 re-verification per spike notes Caveats § 'OIDC mode unverified for DDL permission (S2.b)' and ROADMAP. Phase 13 unblocks downstream work via the supertest OIDC code-path coverage; the live cluster grant is verified later."
---

# Phase 13: spikes-and-endpoint Verification Report

**Phase Goal:** Architectural unknowns resolved and the server-side materialize primitive is live and tested.
**Verified:** 2026-05-06T18:43:28Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                            | Status     | Evidence                                                                                                                                                                                                                                                                                                                       |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | All four Phase-13 spikes (S1, S2, S3, S4) have verbatim probe outputs, definitive verdicts, and downstream consequence captured                                                  | ✓ VERIFIED | `13-SPIKE-NOTES.md` contains S1 (PASS), S2.a/c (PASS) + S2.b (N/A — DEFERRED with reason), S3 (RESOLVED, verbatim error string captured), S4 (BOTH FORMS WORK). Cross-spike summary table at lines 11-18. Each section has probe command + observed result + verdict + downstream consequence per the Phase-11 precedent format. |
| 2   | Plan 13-02 ships two pure utility modules — `viewNaming.ts` (`buildFilterViewName`, `sanitizeForViewName`) and `whereClause.ts` (`buildServerWhereClause`, `escapeKineticaStringLiteral`, `ActiveFilter`) — with zero non-stdlib imports | ✓ VERIFIED | `kinetica_bi/server/src/lib/viewNaming.ts` (61 lines) and `whereClause.ts` (92 lines) both have `grep -c "^import"` = 0. API surfaces match must_haves exactly. Tests pass: `lib.viewNaming.spec.ts` (15 tests) + `lib.whereClause.spec.ts` (15 tests) = 30/30 PASS.                                                              |
| 3   | View-name shape conforms to the locked format `_kbi_filt_u<userId>_d<dashId>_t<tableId>_s<sessionShort>` with proper sanitization (max 32 chars username, regex `[^a-zA-Z0-9_]→_`) | ✓ VERIFIED | `viewNaming.ts:38` uses regex `/[^a-zA-Z0-9_]/g`, `.slice(0, 32)`. Spec asserts `john.doe@kinetica.com → john_doe_kinetica_com`, `auth0\|abc123 → auth0_abc123def456`, 50-char input truncated to 32. `buildFilterViewName` line 60 returns the literal locked prefix `_kbi_filt_u${u}_d${dashboardId}_t${tableId}_s${s}`.        |
| 4   | POST `/api/filter/materialize` is wired inside `createApp()` body in `index.ts`, mounted under `/api/*` requireAuth gate, uses `requireConfig` + `asyncHandler`, no try/catch    | ✓ VERIFIED | `index.ts:667` registers `app.post("/api/filter/materialize", requireConfig, asyncHandler(...))` AFTER `app.use("/api", requireAuth)` at line 422. `sed -n '667,705p' \| grep -c "try {"` returns 0. Handler closes at line 704; before line 933 (createApp end), inside the function body.                                       |
| 5   | DELETE `/api/filter/materialize` is wired inside `createApp()` body, also under `/api/*` requireAuth, accepts `dashboardId` + `tableId` query params, returns `{ dropped: true }` | ✓ VERIFIED | `index.ts:706` registers `app.delete("/api/filter/materialize", requireConfig, asyncHandler(...))`. Validates `Number.isFinite(dashboardId)` and `Number.isFinite(tableId)` from `req.query`. Handler issues `DROP TABLE IF EXISTS ${viewName}` with `op: "MATERIALIZE"` and returns `{ dropped: true }` (line 727). No try/catch.       |
| 6   | POST issues the locked DDL string `CREATE OR REPLACE MATERIALIZED VIEW <name> AS (SELECT * FROM <schema>.<table> WHERE <clause>) USING TABLE PROPERTIES (TTL = 5)`, returns `{ viewName, expiresAt }` | ✓ VERIFIED | `index.ts:695` builds DDL with locked grammar (parenthesized SELECT subquery, `USING TABLE PROPERTIES (TTL = 5)`). `index.ts:702-703` computes `Date.now() + 5 * 60 * 1000` and returns `{ viewName, expiresAt }`. Supertest at lines 169-198 asserts the full DDL substring including `(SELECT * FROM ki_home.events WHERE zone = 'East Village')` and `USING TABLE PROPERTIES (TTL = 5)`. |
| 7   | Both routes invoke `kineticaSql` with `op: "MATERIALIZE"` (audit-tag); per-user creds path verbatim — no service-account fallback | ✓ VERIFIED | `grep -c 'op: "MATERIALIZE"' index.ts` = 3 (existing `/api/views/:id/materialize` + new POST + new DELETE). Spec at `routes.filter-materialize.spec.ts:251` asserts audit log entry `op: "MATERIALIZE"`; line 531 asserts the same for DELETE. SPIKE-V13-02 password-mode PASS confirmed per-user DDL works against deployed Kinetica.   |
| 8   | Server is stateless re views — no SQLite reads/writes for transient views; no in-memory map; route handlers contain zero try/catch (typed errors bubble through asyncHandler → errorMiddleware) | ✓ VERIFIED | The new POST/DELETE handlers (lines 667-728) have no SQLite calls (no `getView`/`createView`/`updateView*`/`db.prepare` references inside the handler bodies — all DB references in `index.ts` belong to the persisted-views CRUD endpoint at lines ~613-650 which is intentionally unchanged). Inline comment at lines 656-660 documents the stateless invariant. `grep -c "try {"` on lines 667-728 = 0. |
| 9   | OIDC username sanitization works end-to-end: a session with `credentialType: "oidc"` and username `john.doe@kinetica.com` produces a sanitized view name matching `_kbi_filt_u\w+_d\d+_t\d+_s\w{8}` | ✓ VERIFIED | `routes.filter-materialize.spec.ts:429-453` exercises an OIDC session via `createSession({ credentialType: "oidc", ... })` with username `john.doe@kinetica.com` and asserts `res.body.viewName.startsWith("_kbi_filt_ujohn_doe_kinetica_com_")`. Test passes. The general regex `/^_kbi_filt_u\w+_d\d+_t\d+_s\w{8}$/` is asserted at line 227.   |
| 10  | Test coverage exists for both utility modules and the endpoint: `lib.viewNaming.spec.ts`, `lib.whereClause.spec.ts`, `routes.filter-materialize.spec.ts` — all 53 NEW tests pass | ✓ VERIFIED | `cd kinetica_bi/server && npx vitest run tests/lib.viewNaming.spec.ts tests/lib.whereClause.spec.ts tests/routes.filter-materialize.spec.ts` reports `Test Files 3 passed (3); Tests 53 passed (53)` in 714ms. Breakdown: viewNaming 15 + whereClause 15 + filter-materialize 23 = 53.                                                  |
| 11  | `npx tsc --noEmit` is clean for `kinetica_bi/server` (no TypeScript errors introduced)                                                                                            | ✓ VERIFIED | `cd kinetica_bi/server && npx tsc --noEmit` exits 0 with no output (no errors). Confirmed against the new endpoints, lib modules, and tests at the head of the working tree.                                                                                                                                                  |

**Score:** 11/11 truths verified

### Required Artifacts

| Artifact                                                                  | Expected                                                                                                          | Status     | Details                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `.planning/phases/13-spikes-and-endpoint/13-SPIKE-NOTES.md`               | All four spike sections (S1, S2, S3, S4) with probe + verbatim result + verdict + downstream consequence          | ✓ VERIFIED | 226 lines; contains all six required headings (S1, S2, S3, S4, Open Question Resolutions, Caveats); zero unfilled `<...>` placeholders; S2.b documented as N/A — DEFERRED with explicit re-probe requirement.                                                                                                                                       |
| `kinetica_bi/server/src/lib/viewNaming.ts`                                | Pure module exporting `sanitizeForViewName`, `buildFilterViewName`, `FilterViewNameArgs` type — zero imports     | ✓ VERIFIED | 61 lines, 0 imports (`grep -c "^import"` = 0). Exports both functions and the type. Contains regex `/[^a-zA-Z0-9_]/g`, `.slice(0, 32)`, `.slice(0, 8)`, locked prefix `_kbi_filt_u`, and S4-outcome inline comment (lines 13-17).                                                                                                                  |
| `kinetica_bi/server/src/lib/whereClause.ts`                               | Pure module exporting `ActiveFilter` type, `escapeKineticaStringLiteral`, `buildServerWhereClause` — zero imports | ✓ VERIFIED | 92 lines, 0 imports. Contains the byte-for-byte v1.2 escape `return val.replace(/'/g, "''");`, empty-filter fallback `"1=1"`, ` AND ` join separator. Equality-only semantics matching v1.2 client-side.                                                                                                                                            |
| `kinetica_bi/server/tests/lib.viewNaming.spec.ts`                         | Unit tests for `sanitizeForViewName` + `buildFilterViewName`                                                      | ✓ VERIFIED | 115 lines; both `describe` blocks present; covers OIDC dot/at/pipe sanitization, 32-char truncation, deterministic composition, and unqualified S4 form. 15 tests pass.                                                                                                                                                                              |
| `kinetica_bi/server/tests/lib.whereClause.spec.ts`                        | Unit tests for `escapeKineticaStringLiteral` + `buildServerWhereClause`                                            | ✓ VERIFIED | 115 lines; both `describe` blocks present; covers `O'Brien` quote-escape, empty-filter `"1=1"` fallback, multi-filter ` AND ` join, all five dataType branches. 15 tests pass.                                                                                                                                                                       |
| `kinetica_bi/server/src/index.ts`                                         | POST + DELETE `/api/filter/materialize` handlers inside `createApp()` body; imports lib helpers; mounted under `/api/*` requireAuth | ✓ VERIFIED | Lines 16-17 import `buildFilterViewName` and `buildServerWhereClause + ActiveFilter`. Lines 667 (POST) and 706 (DELETE) register handlers. Line 422 mounts `requireAuth` on `/api`. Both handlers inside `createApp` body (lines 71-933). Three `op: "MATERIALIZE"` invocations (existing + new POST + new DELETE).                                                |
| `kinetica_bi/server/tests/routes.filter-materialize.spec.ts`              | Supertest coverage for POST + DELETE in both AUTH_MODE variants                                                    | ✓ VERIFIED | 628 lines; four `describe` blocks (`POST … password`, `POST … oidc`, `DELETE … password`, `DELETE … oidc`); 23 tests pass. Covers happy paths, 400/401/403/404, view-name regex `/^_kbi_filt_u\w+_d\d+_t\d+_s\w{8}$/`, single-quote escape `O''Brien`, audit-log `op: "MATERIALIZE"`, OIDC `Bearer` header, no-`code`-field 403 contract.            |

### Key Link Verification

| From                                                | To                                                          | Via                                                                                              | Status   | Details                                                                                                                                                                          |
| --------------------------------------------------- | ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 13-SPIKE-NOTES.md S2 finding                        | Plan 13-03 endpoint construction                            | DDL permission gate — pass=build per-user; fail=add 13-04 service-account                        | ✓ WIRED  | S2.a PASS → endpoint built per-user (no Plan 13-04 added). S2.b N/A documented as deferred re-probe; supertest OIDC tests cover the code-path so Phase 14 can ship on the contract. |
| 13-SPIKE-NOTES.md S4 finding                        | Plan 13-02 view-name builder                                | Schema-qualified vs unqualified return value                                                     | ✓ WIRED  | S4 BOTH WORK → builder returns UNQUALIFIED. Inline comment in `viewNaming.ts:13-17` documents the S4 outcome and the choice; `FilterViewNameArgs` type omits `schema?` field.    |
| 13-SPIKE-NOTES.md S3 finding                        | Phase 15 LIFE-V13-02 `isViewNotFoundError()`                | Verbatim error string                                                                            | ✓ WIRED  | S3 captured verbatim: `SqlEngine: Object '<view-name>' not found (S/SDc:1513)`. Phase 15 has the exact regex pattern in `13-SPIKE-NOTES.md:147-153` to build against.            |
| `kinetica_bi/server/src/lib/viewNaming.ts`         | `kinetica_bi/server/src/index.ts`                            | `import { buildFilterViewName } from "./lib/viewNaming";`                                        | ✓ WIRED  | `index.ts:16` imports; used at lines 687 (POST) and 715 (DELETE). Both call sites pass `username`, `sessionId`, `dashboardId`, `tableId` from authedReq + body/query.            |
| `kinetica_bi/server/src/lib/whereClause.ts`        | `kinetica_bi/server/src/index.ts`                            | `import { buildServerWhereClause, type ActiveFilter } from "./lib/whereClause";`                  | ✓ WIRED  | `index.ts:17` imports both. `ActiveFilter` types the request-body `filters` field at line 671; `buildServerWhereClause(filters)` invoked at line 694.                            |
| `kinetica_bi/server/src/index.ts` POST handler     | `kinetica_bi/server/src/kinetica.ts kineticaSql()`           | `kineticaSqlHelper(authedReq, ddl, { route: "POST /api/filter/materialize", op: "MATERIALIZE" })` | ✓ WIRED  | `index.ts:697-700`. Awaited; throws bubble through asyncHandler → errorMiddleware. No try/catch wrap.                                                                            |
| `kinetica_bi/server/src/index.ts` DELETE handler   | `kinetica_bi/server/src/kinetica.ts kineticaSql()`           | `kineticaSqlHelper(authedReq, "DROP TABLE IF EXISTS ${viewName}", { op: "MATERIALIZE" })`        | ✓ WIRED  | `index.ts:722-725`. Awaited; same op tag.                                                                                                                                        |
| `index.ts` errorMiddleware                          | POST `/api/filter/materialize` 403 response                  | `KineticaPermissionError` → asyncHandler → errorMiddleware → 403 + `{ error }` (no `code` field)  | ✓ WIRED  | Supertest at lines 334-353 asserts `expect(res.status).toBe(403); expect(res.body.code).toBeUndefined();`. Test passes.                                                          |

### Requirements Coverage

| Requirement       | Source Plan      | Description                                                                                              | Status        | Evidence                                                                                                                                                                                                                                                                                                                                              |
| ----------------- | ---------------- | -------------------------------------------------------------------------------------------------------- | ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SPIKE-V13-01      | 13-01            | WMS LAYERS=`<materialized_view_name>` resolves to valid tiles                                            | ✓ SATISFIED   | 13-SPIKE-NOTES.md S1 PASS (operator confirmed verbatim WMS GetMap probe returned PNG against `LAYERS=_kbi_filt_spike_test`).                                                                                                                                                                                                                          |
| SPIKE-V13-02      | 13-01            | Per-user creds carry CREATE/DROP MATERIALIZED VIEW permission                                            | ✓ SATISFIED (with deferred OIDC re-probe) | S2.a CREATE PASS + S2.c DROP PASS (password mode). S2.b OIDC explicitly N/A — deferred to Phase 15/17. Acceptable per phase verdict; supertest covers OIDC code path. Re-probe tracked in human_verification.                                                                                                                                  |
| SPIKE-V13-03      | 13-01            | Capture verbatim Kinetica error for query-of-dropped-view                                                | ✓ SATISFIED   | 13-SPIKE-NOTES.md S3 RESOLVED. Verbatim string captured: `SqlEngine: Object '_kbi_filt_spike_test' not found (S/SDc:1513)`. HTTP 400. Pattern table + recommended `isViewNotFoundError()` regex provided for Phase 15.                                                                                                                                |
| SPIKE-V13-04      | 13-01            | Schema qualification — qualified vs unqualified                                                          | ✓ SATISFIED   | 13-SPIKE-NOTES.md S4: BOTH FORMS WORK. Endpoint returns unqualified; documented in `viewNaming.ts` header comment.                                                                                                                                                                                                                                    |
| VIEW-V13-01       | 13-03            | POST issues `CREATE OR REPLACE MATERIALIZED VIEW … USING TABLE PROPERTIES (TTL = 5)`; returns `{viewName, expiresAt}` | ✓ SATISFIED   | `index.ts:695-703`. Supertest happy-path asserts on full DDL substring + `expiresAt > Date.now()`. Note: ROADMAP success-criterion #3 mentions POST-with-empty-filters issuing DROP, but the locked PLAN/REQUIREMENTS contract is paired endpoints (POST creates, DELETE drops); `VIEW-V13-02` codifies DELETE-for-drop. Implementation matches the locked PLAN. |
| VIEW-V13-02       | 13-03            | DELETE issues `DROP TABLE IF EXISTS <viewName>`; returns `{dropped: true}`                               | ✓ SATISFIED   | `index.ts:722-727`. Supertest at lines 492-528 asserts the DDL shape and `{ dropped: true }` response.                                                                                                                                                                                                                                                |
| VIEW-V13-03       | 13-02            | Deterministic view name `_kbi_filt_u<userId>_d<dashId>_t<tableId>_s<sessionShort>` with OIDC sanitization | ✓ SATISFIED   | `viewNaming.ts:57-61` builds the locked shape; spec asserts deterministic, sanitized, truncation-bounded output. **Documentation divergence**: REQUIREMENTS.md VIEW-V13-03 says "first 4 hex chars"; CONTEXT/PLAN/SPIKE-NOTES lock it as 8 (more entropy). Tests pin `\w{8}` and the locked decision supersedes the stale REQUIREMENTS wording. INFO-only.        |
| VIEW-V13-04       | 13-03            | Server is stateless re views — no SQLite, no in-memory map; Kinetica TTL=5 is sole cleanup                | ✓ SATISFIED   | New handlers (lines 667-728) contain zero SQLite calls and no try/catch. Inline comment lines 656-660 documents the invariant. `op: "MATERIALIZE"` audit tag is the only side-channel.                                                                                                                                                                |
| VIEW-V13-05       | 13-03            | KineticaPermissionError → 403; client toast "Filtering not enabled" (one-time per session)                | ✓ SATISFIED (server contract; client toast in Phase 14/15) | Supertest asserts 403 with `error` body and `code` undefined (matches existing errorMiddleware convention). **Documentation divergence**: REQUIREMENTS.md VIEW-V13-05 mentions `code: "DDL_DENIED"` but the locked PLAN must_haves and existing errorMiddleware convention is no `code` field on 403 (only on 401 REAUTH_REQUIRED). PLAN/test/impl agree. Client toast deferred to Phase 14/15. |
| VIEW-V13-06       | 13-02            | Server-side WHERE-clause builder ported from v1.2 with same SQL-safe escape semantics                     | ✓ SATISFIED   | `whereClause.ts:50-52` ports the v1.2 single-quote escape verbatim. `buildServerWhereClause` covers all five dataType branches per behavior list. 15 tests pin every case including `O'Brien → O''Brien`.                                                                                                                                            |
| VIEW-V13-07       | 13-03            | Supertest coverage for POST + DELETE asserting DDL string, view-name composition, both AUTH_MODE variants | ✓ SATISFIED   | 23 supertest cases across four `describe` blocks. AUTH_MODE password + oidc both stubbed; DDL string asserted at lines 169-198 (POST) and 509-529 (DELETE); view-name regex `/^_kbi_filt_u\w+_d\d+_t\d+_s\w{8}$/` asserted at 227-249.                                                                                                                |

All 11 requirement IDs from PLAN frontmatter are accounted for. No orphaned requirements (REQUIREMENTS.md traceability shows Phase 13: 11 requirements, all marked Complete).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |

None found in Phase 13's net-new files. Specifically:
- No TODO/FIXME/XXX/HACK markers in `viewNaming.ts`, `whereClause.ts`, the new handler block (index.ts:652-728), or `routes.filter-materialize.spec.ts`.
- No `console.log` in new code paths.
- No empty `return null` / `return {}` / `=> {}` placeholders.
- No try/catch wrapping in the new handlers (intentional — VIEW-V13-04 statelessness).

### Human Verification Required

#### 1. SPIKE-V13-02 OIDC Re-probe (S2.b)

**Test:** Re-run the S2.a CREATE OR REPLACE MATERIALIZED VIEW probe against deployed Kinetica using `Authorization: Bearer <oidc_access_token>` instead of HTTP Basic, once an OIDC access token becomes reachable (current spike environment lacks one).
**Expected:** Kinetica returns `{"status":"OK", ...}` with `info.X-Kinetica-Group: DDL` (same shape as the password-mode probe).
**Why human:** No OIDC token was reachable in the current spike environment. The supertest OIDC code-path coverage (`routes.filter-materialize.spec.ts:415-481, 593-628`) proves the endpoint correctly forwards `Authorization: Bearer <token>` to Kinetica when `credentialType === "oidc"`, but the live cluster's OIDC-grant mapping is unverified. Tracked in `13-SPIKE-NOTES.md` Caveats and `deferred-items.md`. Acceptable for this phase's verdict; mandatory for Phase 15/17 milestone close per ROADMAP.

### Gaps Summary

No gaps. Phase 13 achieves its goal: the four architectural spikes are resolved (S1/S3/S4 with PASS/RESOLVED verdicts; S2 PASS for password mode and explicitly deferred for OIDC), and the server-side `/api/filter/materialize` POST + DELETE primitive is wired into `createApp()` under `/api/*` requireAuth, composed from the pure utility modules `viewNaming.ts` and `whereClause.ts`, with 53 new tests passing and `tsc --noEmit` clean.

The 104 pre-existing test failures noted in `deferred-items.md` reproduce on commit `97e893e` (before Plan 13-02 began) and are not caused by Phase 13 changes — explicitly excluded from this verification per the prompt's known baseline.

Two minor documentation divergences (REQUIREMENTS.md says "4 hex chars" for sessionShort while CONTEXT/PLAN/impl/tests use 8; REQUIREMENTS.md says `code: "DDL_DENIED"` while PLAN/impl/test use no `code` field on 403) reflect locked design decisions made during planning that supersede the original REQUIREMENTS.md wording. These should be flagged for a REQUIREMENTS.md text-update in a future planning pass but do NOT constitute gaps — the implementation matches the locked PLAN must_haves and is consistent with existing server conventions.

The single human-verification item (OIDC live re-probe) is explicitly scoped to Phase 15/17 and acceptable for this phase's verdict per the prompt and the spike-notes Caveats section.

---

_Verified: 2026-05-06T18:43:28Z_
_Verifier: Claude (gsd-verifier)_
