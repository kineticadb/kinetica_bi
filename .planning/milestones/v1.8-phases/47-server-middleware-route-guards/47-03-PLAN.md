---
phase: 47-server-middleware-route-guards
plan: 03
type: execute
wave: 3
depends_on: ["47-01", "47-02"]
files_modified:
  - packages/server/src/index.ts
  - packages/server/src/db.ts
  - packages/server/tests/routes.guards.spec.ts
  - packages/server/tests/routes.management.spec.ts
autonomous: true
requirements:
  - GUARD-V18-02
  - GUARD-V18-03
  - GUARD-V18-04

must_haves:
  truths:
    - "Every config-mutation route (dashboards/widgets/tables/views/dynamic-views/layers CRUD + reorder + POST /api/views/:id/materialize) returns 403 PERMISSION_DENIED for a session lacking the mapped permission, and 200/201 for an admin session"
    - "Every analyst-passthrough route remains reachable with requireAuth ONLY — an analyst session (dashboards:view only) gets a non-403 response from POST /api/filter/materialize, POST /api/sql, POST /api/info/query, GET /api/wms, etc."
    - "An explicit ANALYST-PASSTHROUGH BOUNDARY comment block appears in index.ts at the passthrough route registrations, documenting POST /api/sql and the dynamic-view preview/drop distinction"
    - "known_users table exists; a successful password login and a successful OIDC callback each upsert (lower(username), first_seen, last_seen)"
    - "Seven management routes are registered and gated: GET /api/users (users:view), POST + DELETE /api/users/:username/roles (users:assign_roles), GET /api/roles (roles:view), POST /api/roles (roles:create_custom), PUT /api/roles/:id/permissions (roles:manage_permissions), DELETE /api/roles/:id (roles:delete_custom)"
    - "After guards land, the set of FAILING spec FILES in a full `npm run test:server` run is a SUBSET of the 14 known-flaky files (zero NEW failing files vs the TD-V16-TEST-ISOLATION baseline). The new routes.guards + routes.management specs pass deterministically as a targeted group (depends on the Plan 01 admin-session migration)."
  artifacts:
    - path: "packages/server/src/index.ts"
      provides: "guards on 22 mutation routes + passthrough comment + known_users upsert + 7 management routes"
      contains: "requirePermission"
    - path: "packages/server/src/db.ts"
      provides: "known_users table in SCHEMA_DDL"
      contains: "known_users"
    - path: "packages/server/tests/routes.guards.spec.ts"
      provides: "403-for-non-holder + passthrough-reachable assertions"
      contains: "PERMISSION_DENIED"
    - path: "packages/server/tests/routes.management.spec.ts"
      provides: "management-route gate + GET /api/users union assertions"
      contains: "/api/users"
  key_links:
    - from: "packages/server/src/index.ts mutation routes"
      to: "src/rbac.requirePermission + lib/permissions.PERMISSIONS"
      via: "...requirePermission(PERMISSIONS.X) spread into route args"
      pattern: "requirePermission\\(PERMISSIONS\\."
    - from: "POST /api/auth/login + GET /api/auth/oidc/callback"
      to: "known_users table"
      via: "INSERT ... ON CONFLICT(username) DO UPDATE SET last_seen"
      pattern: "known_users"
    - from: "GET /api/users handler"
      to: "known_users UNION user_roles"
      via: "SELECT username FROM known_users UNION SELECT DISTINCT username FROM user_roles"
      pattern: "UNION"
---

<objective>
Wire `requirePermission` onto all 22 config-mutation routes in `index.ts`, document the analyst-passthrough boundary with a prominent comment block, add the `known_users` table + login/OIDC-callback upserts, and register the 7 RBAC management routes with their gates. This closes GUARD-V18-02 (mutation gating), GUARD-V18-03 (passthrough boundary documented in code), and GUARD-V18-04 (management routes gated). The Plan 01 spec migration guarantees the existing suite still passes because every existing route test now authenticates as admin.

Purpose: This is the phase's authoritative-enforcement deliverable — after this, server-side gating is live and Phase 48 frontend gating becomes pure UX.

Output: gated mutation routes; the boundary comment block; known_users table + upserts; 7 management routes; two new spec files (guards + management) proving 403/passthrough/gate behavior.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<regression_gate>
<!-- CANONICAL set-based regression gate for Phase 47. The full-suite pass COUNT is
     NONDETERMINISTIC run-to-run (TD-V16-TEST-ISOLATION + OIDC timeout flake — measured
     634, ~622, 620 across three runs). Do NOT use a fixed pass count anywhere. Use the
     three set-based checks below. -->

KNOWN-FLAKY FILES (14) — a failure in ANY of these is NOT a Phase 47 regression:
  tests/auth.oidc.spec.ts, tests/auth.routes.spec.ts, tests/boot.hardening.spec.ts,
  tests/boot.wipe.spec.ts, tests/bootstrap.spec.ts, tests/db.smoke.spec.ts,
  tests/errorMiddleware.spec.ts, tests/kinetica.creds.routes.spec.ts, tests/oidc.module.spec.ts,
  tests/routes.discovery.spec.ts, tests/routes.materialize.spec.ts, tests/routes.sql.spec.ts,
  tests/routes.wms.spec.ts, tests/routes.info-query.spec.ts

GATE 1 — NO NEW FAILING FILES: full `npm run test:server` run — the set of failing spec
  FILES MUST be a SUBSET of the 14 known-flaky files. Any failing file OUTSIDE the list
  (especially routes.guards, routes.management, routes.rbac, or any route spec made to 403
  by the new guards) is a Phase 47 regression and FAILS the gate. Inspect the per-file FAIL
  list, NOT the aggregate pass count.

GATE 2 — TARGETED GROUP GREEN: the Phase 47 / RBAC group passes deterministically with 0
  failures:
  `npm run test:server -- lib.permissions db.rbacMigration routes.rbac routes.guards routes.management`
  (all are Phase 47 specs — none are in the known-flaky list, so all MUST be green).

GATE 3 — DETERMINISTIC-GREEN MIGRATED SPECS: each migrated spec file that is NOT in the
  14-file known-flaky list passes when run individually after guards land. Spot-check at
  minimum these 5: routes.quantile, routes.dynamic-view, routes.filter-materialize, layers,
  routes.dashboard-layers-patch — each via `npm run test:server -- <name>` → 0 failures.
  (These hit gated routes and authenticate as admin via the Plan 01 migration; if any now
  403s, the guard wiring or the migration is wrong.)
</regression_gate>

<context>
@.planning/phases/47-server-middleware-route-guards/47-CONTEXT.md
@.planning/phases/47-server-middleware-route-guards/47-RESEARCH.md

<interfaces>
<!-- Verified from live code (line numbers confirmed via grep). -->

requirePermission (from Plan 02, src/rbac.ts) — RETURNS AN ARRAY, MUST be spread:
```typescript
import { requirePermission } from "./rbac";
app.post("/api/dashboards", ...requirePermission(PERMISSIONS.DASHBOARDS_CREATE), handler);
// When requireConfig also needed, requireConfig goes FIRST (Pitfall 2):
app.post("/api/views/:id/materialize", requireConfig, ...requirePermission(PERMISSIONS.DASHBOARDS_EDIT), asyncHandler(...));
```

Existing index.ts anchors (verified line numbers):
- line 254: `const requireConfig = (req, res, next) => { ... }`  (keep first in chain)
- line 278: `app.post("/api/auth/login", async (req, res) => {` ; line 297: `const sid = createSession({ username, secret: password, kineticaUrl });`
- line 359: `app.get("/api/auth/oidc/callback", asyncHandler(...))` ; line 465: `const sid = createSession({ ... });`
- line 477: `app.use("/api", requireAuth);`  (global gate — DO NOT modify)
- line 321: REAUTH_REQUIRED 401 precedent

PERMISSIONS catalog (lib/permissions.ts, incl. datasets:manage from Plan 01):
DASHBOARDS_CREATE, DASHBOARDS_EDIT, DASHBOARDS_DELETE, WIDGETS_CONFIGURE, LAYERS_MANAGE,
DYNAMIC_VIEWS_MANAGE, DATASETS_MANAGE, USERS_VIEW, USERS_ASSIGN_ROLES, ROLES_VIEW,
ROLES_MANAGE_PERMISSIONS, ROLES_CREATE_CUSTOM, ROLES_DELETE_CUSTOM

DB tables already present (db.ts): roles(id,name,description,built_in,...), role_permissions(role_id,permission), user_roles(username,role_id), rbac_seed_history. db is the module singleton export.
</interfaces>

<route_gate_table>
<!-- VERBATIM from 47-RESEARCH.md route inventory. route | line | gate. requireConfig stays first where present. -->
GATE these (insert ...requirePermission(PERMISSIONS.X) — requireConfig FIRST if listed):
  POST   /api/dashboards                               line 484   DASHBOARDS_CREATE
  PATCH  /api/dashboards/:id                            line 491   DASHBOARDS_EDIT
  DELETE /api/dashboards/:id                            line 498   DASHBOARDS_DELETE
  POST   /api/dashboards/:id/widgets                    line 512   DASHBOARDS_EDIT
  PATCH  /api/widgets/:id                               line 528   WIDGETS_CONFIGURE
  DELETE /api/widgets/:id                               line 535   DASHBOARDS_EDIT
  POST   /api/dashboards/:id/tables                     line 549   DASHBOARDS_EDIT
  DELETE /api/dashboards/:dashboardId/tables/:tableId   line 559   DASHBOARDS_EDIT
  POST   /api/dashboards/:id/layers                     line 578   LAYERS_MANAGE
  PATCH  /api/dashboards/:id/layers/reorder             line 595   LAYERS_MANAGE
  PATCH  /api/dashboards/:id/layers/:layerId            line 611   LAYERS_MANAGE
  DELETE /api/dashboards/:id/layers/:layerId            line 644   LAYERS_MANAGE
  POST   /api/dashboards/:id/views                      line 658   DASHBOARDS_EDIT
  PATCH  /api/views/:id                                 line 674   DASHBOARDS_EDIT
  DELETE /api/views/:id                                 line 683   DASHBOARDS_EDIT
  POST   /api/views/:id/materialize  (requireConfig)    line 696   DASHBOARDS_EDIT   (viewer-path verified safe: no web caller)
  POST   /api/dashboards/:dashboardId/dynamic-views (requireConfig)  line 1047  DYNAMIC_VIEWS_MANAGE
  PUT    /api/dynamic-views/:id      (requireConfig)    line 1124  DYNAMIC_VIEWS_MANAGE
  DELETE /api/dynamic-view/:id       (requireConfig)    line 1531  DYNAMIC_VIEWS_MANAGE
  POST   /api/tables                                    line 1811  DATASETS_MANAGE
  PATCH  /api/tables/:id                                line 1819  DATASETS_MANAGE
  DELETE /api/tables/:id                                line 1826  DATASETS_MANAGE

PASSTHROUGH — leave requireAuth-only (DO NOT add requirePermission); covered by the boundary comment:
  GET    /api/dashboards (480), GET widgets (506), GET tables-assoc (543), GET layers (572),
  GET views (652), GET dynamic-views (1034), GET /api/tables (1800), GET /api/tables/:id (1804)
  POST   /api/filter/materialize (748), DELETE /api/filter/materialize (849)
  POST   /api/quantile (884), POST /api/top-values (938), POST /api/column-stats (988)
  POST   /api/dynamic-view/preview (1241), POST /api/dynamic-view/materialize (1351), POST /api/dynamic-view/:id/drop (1502)
  POST   /api/info/query (1600)
  GET    /api/kinetica/schemas (1835), GET /api/kinetica/schemas/:schema/tables (1845), GET .../columns (1856)
  GET    /api/wms (1894), GET /api/wms/capabilities (1911)
  POST   /api/sql (1931)
</route_gate_table>
</context>

<tasks>

<task type="auto">
  <name>Task 1: known_users table + login/OIDC-callback upserts</name>
  <files>packages/server/src/db.ts, packages/server/src/index.ts, packages/server/tests/db.rbacMigration.spec.ts</files>
  <read_first>
    - packages/server/src/db.ts SCHEMA_DDL block (lines 14-185) — pattern for CREATE TABLE IF NOT EXISTS; place known_users alongside roles/user_roles
    - packages/server/src/index.ts lines 278-305 (POST /api/auth/login, the createSession call at 297)
    - packages/server/src/index.ts lines 359-475 (OIDC callback, the createSession call at 465)
    - 47-RESEARCH.md § "known_users Upsert Integration Points" + Pitfall 4
    - packages/server/tests/db.rbacMigration.spec.ts (idempotent-migration assertion pattern — add a known_users existence check)
  </read_first>
  <action>
    1. In `packages/server/src/db.ts` SCHEMA_DDL, add (next to the RBAC tables, before the closing backtick):
    ```sql
    -- Phase 47 (v1.8): durable login history independent of session GC. Upserted on every
    -- successful login (password + OIDC). Username LOWERCASED (Phase 46 convention). Feeds
    -- GET /api/users union + Phase 49 last-seen. CREATE TABLE IF NOT EXISTS covers existing deployments.
    CREATE TABLE IF NOT EXISTS known_users (
      username TEXT PRIMARY KEY,
      first_seen TEXT NOT NULL DEFAULT (datetime('now')),
      last_seen  TEXT NOT NULL DEFAULT (datetime('now'))
    );
    ```
    No seedRbac change — known_users is runtime-populated.
    2. In `index.ts` POST /api/auth/login, AFTER the successful `const sid = createSession({...})` at line ~297 (only when session creation did not throw):
    ```typescript
    db.prepare(
      `INSERT INTO known_users (username, first_seen, last_seen)
       VALUES (lower(?), datetime('now'), datetime('now'))
       ON CONFLICT(username) DO UPDATE SET last_seen = datetime('now')`
    ).run(username);
    ```
    3. In the OIDC callback, AFTER `const sid = createSession({...})` at line ~465, add the SAME upsert using the OIDC-resolved `username`. Ensure `db` is imported in index.ts (it already imports from ./db — confirm and reuse).
    Pitfall 4: place the upsert only on the success path after createSession; do not let it run when login fails.
    4. Add a known_users existence assertion to db.rbacMigration.spec.ts (table exists after createDb on both fresh and existing schema).
  </action>
  <verify>
    <automated>npm run test:server -- db.rbacMigration auth.routes auth.cookie 2>&1 | grep -E "Tests|FAIL|known_users"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "CREATE TABLE IF NOT EXISTS known_users" packages/server/src/db.ts` succeeds
    - `grep -c "INSERT INTO known_users" packages/server/src/index.ts` returns >= 2 (login + OIDC callback)
    - `grep -q "ON CONFLICT(username) DO UPDATE SET last_seen" packages/server/src/index.ts` succeeds
    - `grep -q "lower(?)" packages/server/src/index.ts` succeeds (lowercasing in the upsert)
    - `npm run test:server -- db.rbacMigration` passes 0 failures (db.rbacMigration is a Phase 47 spec, not known-flaky). auth.routes/auth.cookie are in the targeted run for signal only — auth.routes is in the known-flaky list, so judge it by per-test behavior, not the file pass/fail.
    - `cd packages/server && npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>known_users table exists in SCHEMA_DDL; both login paths upsert lowercased username with last_seen refresh; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire requirePermission onto 22 mutation routes + add the passthrough boundary comment block</name>
  <files>packages/server/src/index.ts</files>
  <read_first>
    - packages/server/src/index.ts — read each route registration referenced in the route_gate_table BEFORE editing it (line numbers verified, but re-confirm exact signatures since Task 1 may have shifted lines)
    - packages/server/src/rbac.ts (requirePermission from Plan 02)
    - packages/server/src/lib/permissions.ts (PERMISSIONS keys)
    - 47-RESEARCH.md § "Route Inventory" (the verdict table) + § "Passthrough Comment Block Template" (use it verbatim) + Pitfalls 1 (spread) & 2 (requireConfig order)
    - 47-RESEARCH.md § "Viewer-Path Verification: POST /api/views/:id/materialize" (confirmed no web caller — safe to gate)
  </read_first>
  <action>
    1. Add `import { requirePermission } from "./rbac";` and ensure `PERMISSIONS` is imported from `./lib/permissions` in index.ts.
    2. For EACH of the 22 routes in the route_gate_table above, insert `...requirePermission(PERMISSIONS.X)` into the middleware chain:
       - Routes with NO requireConfig: `app.post("/api/dashboards", ...requirePermission(PERMISSIONS.DASHBOARDS_CREATE), (req,res)=>{...})`
       - Routes WITH requireConfig (materialize, dynamic-views POST/PUT/DELETE): keep requireConfig FIRST, then spread requirePermission: `app.post("/api/views/:id/materialize", requireConfig, ...requirePermission(PERMISSIONS.DASHBOARDS_EDIT), asyncHandler(...))` (Pitfall 2).
       - ALWAYS spread with `...` (Pitfall 1 — an un-spread array is silently ignored by Express and the guard never fires).
       - Use the exact permission from the table. Do NOT gate any route in the PASSTHROUGH list.
    3. Insert the ANALYST-PASSTHROUGH BOUNDARY comment block (verbatim from 47-RESEARCH.md § "Passthrough Comment Block Template") immediately above the first passthrough route registration cluster (the filter/materialize group around line 748 is the natural anchor, OR place it once prominently before the passthrough cluster). It MUST document POST /api/sql prominently and the preview/drop-vs-DELETE distinction. This is a ROADMAP SC5 deliverable, not a nicety.
    4. Document the dynamic-view preview decision in the comment block: PASSTHROUGH (reads only; designer surface but creates nothing) — matches the route_gate_table.
    DO NOT modify the global `app.use("/api", requireAuth)` at line 477 or the requireConfig definition.
  </action>
  <verify>
    <automated>npm run test:server 2>&1 | grep -E "FAIL" | grep "\.spec\.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "requirePermission(PERMISSIONS\." packages/server/src/index.ts` returns >= 22
    - `grep -c "\.\.\.requirePermission" packages/server/src/index.ts` returns >= 22 (every usage spread — Pitfall 1)
    - `grep -q "ANALYST-PASSTHROUGH BOUNDARY" packages/server/src/index.ts` succeeds (SC5 comment block present)
    - `grep -q "GUARD-V18-03" packages/server/src/index.ts` succeeds (boundary tagged to requirement)
    - `grep -q "POST /api/sql" packages/server/src/index.ts` within the comment block (prominent passthrough documentation)
    - `! grep -q "requirePermission" packages/server/src/index.ts` near line 748 filter/materialize registration — i.e. POST /api/filter/materialize is NOT gated (verify by inspecting the registration: `grep -n "api/filter/materialize" packages/server/src/index.ts` shows only requireConfig, no requirePermission)
    - `grep -A2 'app.post("/api/views/:id/materialize"' packages/server/src/index.ts | grep -q "requireConfig"` AND same block contains `requirePermission(PERMISSIONS.DASHBOARDS_EDIT)` with requireConfig appearing first
    - GATE 1 (see <regression_gate>): full `npm run test:server` run — set of failing spec FILES is a SUBSET of the 14 known-flaky files. Existing route specs authenticate as admin via the Plan 01 migration, so guards must NOT push any new file into the FAIL set (no 403 regressions). Inspect the per-file FAIL list, NOT the pass count.
    - GATE 3 (see <regression_gate>): routes.quantile, routes.dynamic-view, routes.filter-materialize, layers, routes.dashboard-layers-patch each pass individually (`npm run test:server -- <name>` → 0 failures) — proves the admin migration carries through the new gates.
    - `cd packages/server && npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>All 22 mutation routes spread requirePermission with the correct permission; requireConfig stays first where present; passthrough routes untouched; the boundary comment block is present and documents POST /api/sql + preview/drop; no new failing spec files vs the known-flaky baseline and the spot-checked deterministic-green specs pass individually.</done>
</task>

<task type="auto">
  <name>Task 3: Register 7 management routes (gated) + write guards & management spec coverage</name>
  <files>packages/server/src/index.ts, packages/server/tests/routes.management.spec.ts, packages/server/tests/routes.guards.spec.ts</files>
  <read_first>
    - packages/server/src/index.ts — where routes append before errorMiddleware (find the errorMiddleware registration; new routes go just above it)
    - packages/server/src/rbac.ts (requirePermission)
    - packages/server/src/db.ts (db singleton; roles, role_permissions, user_roles, known_users tables)
    - 47-RESEARCH.md § "NEW MANAGEMENT ROUTES" table + § "GET /api/users query" + Open Question 2 (response shapes) + Pitfall 6 (target = req.params.username, not caller)
    - packages/server/tests/routes.quantile.spec.ts (supertest pattern) + tests/helpers/db.ts createAdminSession
  </read_first>
  <action>
    1. Register these 7 routes just before errorMiddleware in index.ts, each gated (all need requirePermission only; no requireConfig — these are SQLite-only). Use Pitfall-6-safe targeting (target = `req.params.username.toLowerCase()`; actor = `req.user!.creds.username`). SAFE-V18-01 (last-admin) and SAFE-V18-02 (escalation) are Phases 49/50 — do NOT implement them here (basic validation only). Shapes (Open Question 2 recommendation — lock the Phase 49 contract now):
       - `GET /api/users` ...requirePermission(PERMISSIONS.USERS_VIEW) → `{ users: [{ username, roles: string[] }] }` via:
         `SELECT username FROM known_users UNION SELECT DISTINCT username FROM user_roles ORDER BY username`, then per-username resolve role names by joining user_roles→roles (or the json_group_array query in 47-RESEARCH.md § "GET /api/users query").
       - `POST /api/users/:username/roles` ...requirePermission(PERMISSIONS.USERS_ASSIGN_ROLES); body `{ roleName }` → look up role_id from roles by name, `INSERT OR IGNORE INTO user_roles (username, role_id) VALUES (lower(?), ?)`; 404 if role not found; 200 on success.
       - `DELETE /api/users/:username/roles/:roleName` ...requirePermission(PERMISSIONS.USERS_ASSIGN_ROLES) → delete the user_roles row by username+role_id; 200 (idempotent).
       - `GET /api/roles` ...requirePermission(PERMISSIONS.ROLES_VIEW) → `{ roles: [{ id, name, description, built_in, permissions: string[] }] }` (permissions from role_permissions).
       - `POST /api/roles` ...requirePermission(PERMISSIONS.ROLES_CREATE_CUSTOM); body `{ name, description?, permissions: string[] }` → INSERT into roles (built_in=0) + role_permissions rows; 201; 409 if name exists.
       - `PUT /api/roles/:id/permissions` ...requirePermission(PERMISSIONS.ROLES_MANAGE_PERMISSIONS); body `{ permissions: string[] }` → replace role_permissions for that role_id (DELETE then INSERT); 200; validate each permission string is in the PERMISSIONS catalog (reject unknown with 400).
       - `DELETE /api/roles/:id` ...requirePermission(PERMISSIONS.ROLES_DELETE_CUSTOM) → block deletion of built_in=1 roles (400); for custom roles, basic delete (last-holder check deferred to Phase 50); 200.
       Document in handler comments that SAFE-V18-01/02 guards are deferred to Phases 49/50.
    2. Write `packages/server/tests/routes.management.spec.ts`: for each route assert (a) admin session → success shape; (b) a non-holder session (seed user_roles so the username has analyst only, NOT bootstrap admin) → 403 PERMISSION_DENIED. Assert GET /api/users returns the UNION (seed a known_users row for a never-assigned user + a user_roles row for an assigned user; both appear). Assert POST /api/users/:username/roles targets req.params.username (Pitfall 6) — assign to "bob", verify bob (not the admin caller) gained the role.
    3. Write `packages/server/tests/routes.guards.spec.ts`: spot-check the Plan-02 factory ACROSS REAL routes — (a) a non-holder session → 403 on POST /api/dashboards, PATCH /api/widgets/:id, POST /api/tables; (b) an analyst session reaches passthrough routes without 403: assert POST /api/filter/materialize, POST /api/sql, POST /api/info/query, GET /api/wms do NOT return 403 for an analyst (they may return 4xx/5xx from Kinetica mocks, but never 403 PERMISSION_DENIED — assert `res.body.code !== "PERMISSION_DENIED"`). Build the analyst session by seeding user_roles with the analyst role for a non-admin username.
  </action>
  <verify>
    <automated>npm run test:server -- routes.management routes.guards --reporter=verbose 2>&1 | grep -E "Tests|FAIL|PERMISSION_DENIED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "api/users\|api/roles" packages/server/src/index.ts` shows the 7 management routes registered
    - `grep -q "requirePermission(PERMISSIONS.USERS_VIEW)" packages/server/src/index.ts` succeeds
    - `grep -q "requirePermission(PERMISSIONS.USERS_ASSIGN_ROLES)" packages/server/src/index.ts` succeeds
    - `grep -q "requirePermission(PERMISSIONS.ROLES_VIEW)" packages/server/src/index.ts` succeeds
    - `grep -q "requirePermission(PERMISSIONS.ROLES_CREATE_CUSTOM)" packages/server/src/index.ts` succeeds
    - `grep -q "requirePermission(PERMISSIONS.ROLES_MANAGE_PERMISSIONS)" packages/server/src/index.ts` succeeds
    - `grep -q "requirePermission(PERMISSIONS.ROLES_DELETE_CUSTOM)" packages/server/src/index.ts` succeeds
    - `grep -q "UNION" packages/server/src/index.ts` (GET /api/users union query)
    - `grep -q "req.params.username" packages/server/src/index.ts` (Pitfall-6-safe targeting)
    - `grep -q "PERMISSION_DENIED" packages/server/tests/routes.management.spec.ts` and `grep -q "PERMISSION_DENIED" packages/server/tests/routes.guards.spec.ts`
    - GATE 2 (see <regression_gate>): `npm run test:server -- routes.management routes.guards` passes (0 failures) — both are Phase 47 specs, not in the known-flaky list, so they MUST be deterministically green.
    - GATE 1 (see <regression_gate>): full `npm run test:server` run — set of failing spec FILES is a SUBSET of the 14 known-flaky files (no new failing files); `cd packages/server && npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>7 management routes registered with correct gates and conventional REST shapes; SAFE guards explicitly deferred; guards + management specs prove 403-for-non-holder, admin-success, analyst-passthrough-reachable, and the GET /api/users union; routes.guards + routes.management are deterministically green (GATE 2) and no new failing files appear in the full run (GATE 1).</done>
</task>

</tasks>

<verification>
- GATE 1 (<regression_gate>): full `npm run test:server` run — set of failing spec FILES is a SUBSET of the 14 known-flaky files (pre-v1.8 TD-V16-TEST-ISOLATION baseline; the new guards/management specs must NOT appear as failing files). Pass COUNT is NOT used as a gate.
- GATE 2 (<regression_gate>): `npm run test:server -- lib.permissions db.rbacMigration routes.rbac routes.guards routes.management` → 0 failures (all Phase 47 specs, deterministically green).
- GATE 3 (<regression_gate>): the 5 spot-checked deterministic-green migrated specs each pass individually.
- `cd packages/server && npx tsc --noEmit` clean.
- `grep -c "\.\.\.requirePermission(PERMISSIONS\." packages/server/src/index.ts` >= 29 (22 mutation + 7 management).
- ANALYST-PASSTHROUGH BOUNDARY comment block present (SC5); POST /api/sql + POST /api/filter/materialize NOT gated.
- known_users table + dual login upserts present.
</verification>

<success_criteria>
- GUARD-V18-02: all 22 config-mutation routes return 403 PERMISSION_DENIED for non-holders, success for admin.
- GUARD-V18-03: analyst-passthrough routes reachable with requireAuth only; boundary documented in code (SC5).
- GUARD-V18-04: 7 management routes gated on the correct catalog permissions.
- Server enforcement is now authoritative ahead of Phase 48 frontend gating; the set of failing spec files stays a subset of the 14 known-flaky files (GATE 1) and the Phase 47 spec group is deterministically green (GATE 2).
</success_criteria>

<output>
After completion, create `.planning/phases/47-server-middleware-route-guards/47-03-SUMMARY.md`
</output>
</output>
