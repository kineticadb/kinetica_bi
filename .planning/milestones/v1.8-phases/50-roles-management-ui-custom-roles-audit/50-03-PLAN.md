---
phase: 50-roles-management-ui-custom-roles-audit
plan: 03
type: execute
wave: 3
depends_on: ["50-02"]
files_modified:
  - packages/web/src/App.tsx
  - packages/web/src/App.spec.tsx
  - packages/web/src/components/UsersPage.tsx
  - packages/web/src/components/UsersPage.spec.tsx
autonomous: true
requirements: [ROLES-V18-01, SAFE-V18-02]
must_haves:
  truths:
    - "Selecting the Roles nav item renders the RolesPage; the active page reflects 'roles'"
    - "After an OIDC redirect that stored page 'roles', the app restores to the Roles page on return"
    - "On the Users page, a non-admin editor's role popover and bulk-assign dropdown do NOT list the admin role; an admin editor still sees it"
  artifacts:
    - path: "packages/web/src/App.tsx"
      provides: "'roles' Page-union value + render branch + OIDC ReturnTo case"
      contains: "page === \"roles\""
  key_links:
    - from: "packages/web/src/App.tsx"
      to: "packages/web/src/components/RolesPage.tsx"
      via: "page === 'roles' && <RolesPage />"
      pattern: "<RolesPage"
    - from: "packages/web/src/components/UsersPage.tsx"
      to: "useAuthStore.user.roles"
      via: "filter admin role from popover + bulk dropdown for non-admin editors"
      pattern: "roles.*includes\\(\"admin\"\\)"
---

<objective>
Wire RolesPage into the app shell exactly like Phase 49 wired UsersPage (Page union, render branch, OIDC ReturnTo restore), and apply the one small UsersPage retrofit: hide the admin role from the assign popover and bulk-assign dropdown for editors who do not themselves hold the admin role (SAFE-V18-02 UI mirror — the server already blocks the assignment in 50-01).

Purpose: Makes the Roles page reachable (ROLES-V18-01 end-to-end) and closes the last SAFE-V18-02 UI surface. This is glue + a small filter — no new business logic.
Output: App.tsx wiring + spec, UsersPage popover/bulk filter + spec update.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-roles-management-ui-custom-roles-audit/50-CONTEXT.md
@.planning/phases/50-roles-management-ui-custom-roles-audit/50-02-SUMMARY.md

<interfaces>
<!-- Verified from live code 2026-06-06. -->

From packages/web/src/App.tsx (Phase 49 precedent to mirror exactly):
```typescript
import { UsersPage } from "./components/UsersPage";                            // line 8 — add RolesPage import alongside
type Page = "dashboards" | "datasets" | "settings" | "users";                 // line 20 — add | "roles"
// OIDC ReturnTo restore guard (lines 167-172):
if (parsed.page === "dashboards" || parsed.page === "datasets" ||
    parsed.page === "settings" || parsed.page === "users") {                  // add || parsed.page === "roles"
  setPage(parsed.page);
}
// Render branch (line 253):
{page === "users" && <UsersPage />}                                           // add {page === "roles" && <RolesPage />}
```
The Sidebar already defines the "Roles" nav item (Sidebar.tsx:28, key "roles", gated on PERMISSIONS.ROLES_VIEW) and onSelect already does `setPage(key as Page)` — no Sidebar change needed; only the Page union + render branch + ReturnTo.

From packages/web/src/components/RolesPage.tsx (50-02 output): `export function RolesPage()` (named export).

From packages/web/src/components/UsersPage.tsx (retrofit targets):
```typescript
const [roles, setRoles] = useState<RoleDto[]>([]);                            // line 30
// Bulk dropdown options (lines 224-228): roles.map((r) => <option .../>)
// Popover checkboxes (line 343): roles.map((role) => { ... checkbox ... })
```
Editor-is-admin predicate (matches server guard 1): `useAuthStore((s) => s.user?.roles ?? []).includes("admin")`.

From packages/web/src/test/seedAuthStore.ts: seedAdminStore (roles ["admin"]) line 62; seedUserAdminStore (roles ["user_admin"]) line 79.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App.tsx Page union + render branch + OIDC ReturnTo + App.spec</name>
  <files>packages/web/src/App.tsx, packages/web/src/App.spec.tsx</files>
  <read_first>packages/web/src/App.tsx lines 1-35 (imports + Page type), 160-175 (ReturnTo restore), 245-256 (render branches), packages/web/src/App.spec.tsx (existing page-render / ReturnTo test idiom)</read_first>
  <action>
1. Add `import { RolesPage } from "./components/RolesPage";` alongside the UsersPage import (~line 8).
2. Extend the Page union (line 20): `type Page = "dashboards" | "datasets" | "settings" | "users" | "roles";`
3. In the OIDC ReturnTo restore guard (~lines 167-172), add `|| parsed.page === "roles"` to the allowed-page condition so a stored "roles" page restores.
4. Add the render branch after the users branch (~line 253): `{page === "roles" && <RolesPage />}`.
5. App.spec.tsx: add a test that selecting the Roles nav (or setting page to "roles") renders RolesPage; and a ReturnTo test that a stored `{page:"roles"}` restores to the Roles page on authenticated mount. Mock the RolesPage's client calls (or mock the RolesPage module) consistent with how the existing App.spec mocks UsersPage. Seed auth with seedAdminStore().
  </action>
  <verify>
    <automated>npm run test -- --run App.spec</automated>
    <automated>grep -q "page === \"roles\"" packages/web/src/App.tsx</automated>
  </verify>
  <done>"roles" in Page union, render branch, and ReturnTo guard; App.spec covers render + ReturnTo restore.</done>
</task>

<task type="auto">
  <name>Task 2: UsersPage admin-role filter for non-admin editors + spec</name>
  <files>packages/web/src/components/UsersPage.tsx, packages/web/src/components/UsersPage.spec.tsx</files>
  <read_first>packages/web/src/components/UsersPage.tsx lines 28-50 (state + useAuthStore usage), 218-232 (bulk dropdown), 340-365 (popover checkboxes), packages/web/src/components/UsersPage.spec.tsx (seed + render idiom)</read_first>
  <action>
1. Add editor-is-admin read near the existing hasPermission usage: `const editorIsAdmin = useAuthStore((s) => s.user?.roles ?? []).includes("admin");`
2. Derive the assignable role list: `const assignableRoles = editorIsAdmin ? roles : roles.filter((r) => r.name !== "admin");`
3. Use assignableRoles in BOTH the bulk dropdown options (~line 224) and the popover checkbox map (~line 343) instead of `roles`. Leave the chip display of already-assigned roles untouched (we only filter what can be NEWLY assigned). Guard the bulk default-selection effect (~line 57) so it picks from assignableRoles.
4. UsersPage.spec.tsx: add cases — with seedUserAdminStore() the popover and bulk dropdown render no "admin" option; with seedAdminStore() they do. Keep existing tests green.
  </action>
  <verify>
    <automated>npm run test -- --run UsersPage</automated>
    <automated>grep -q "editorIsAdmin" packages/web/src/components/UsersPage.tsx</automated>
  </verify>
  <done>Non-admin editors cannot pick the admin role in popover or bulk dropdown; admin editors can; spec covers both; existing UsersPage tests still pass.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run App.spec` and `npm run test -- --run UsersPage` green.
- Full frontend gate no worse than 1527/1528 baseline (1 known-red DashboardsPage).
- `grep -q "page === \"roles\"" packages/web/src/App.tsx`; `grep -q "editorIsAdmin" packages/web/src/components/UsersPage.tsx`.
- Frontend tsc clean (`npm run build` or the project's typecheck script).
</verification>

<success_criteria>
- ROLES-V18-01 end-to-end: Roles nav renders RolesPage; OIDC ReturnTo restores it (criterion 1 reachability).
- SAFE-V18-02 UI mirror complete: admin role hidden from non-admin editors' assign surfaces (criterion 5 Users-page half), with the server (50-01) as authority.
</success_criteria>

<output>
After completion, create `.planning/phases/50-roles-management-ui-custom-roles-audit/50-03-SUMMARY.md`
</output>
