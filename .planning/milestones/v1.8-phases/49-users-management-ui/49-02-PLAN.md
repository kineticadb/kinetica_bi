---
phase: 49-users-management-ui
plan: 02
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - packages/web/src/api/client.ts
  - packages/web/src/components/UsersPage.tsx
  - packages/web/src/lib/relativeTime.ts
  - packages/web/src/lib/relativeTime.spec.ts
  - packages/web/src/test/seedAuthStore.ts
  - packages/web/src/components/UsersPage.spec.tsx
  - packages/web/src/styles/global.css
autonomous: true
requirements: [USERS-V18-01, USERS-V18-02, USERS-V18-03]
must_haves:
  truths:
    - "A user_admin-seeded render of UsersPage shows an Edit roles button per non-bootstrap row and the bulk action bar; a users:view-only render shows neither — chips are read-only"
    - "Each explicitly-assigned role renders as a chip with a × control; an unassigned user (roles.length===0) renders a muted 'analyst (default)' chip with no ×"
    - "The bootstrap row (is_bootstrap:true) renders a lock indicator and an immutable admin chip with no × and no Edit roles button"
    - "Clicking × on a chip fires DELETE; a 400 last-admin response surfaces the verbatim server message as an error toast and the chip remains"
    - "Selecting multiple rows and applying a role via the bulk bar issues one assign per selected user and toasts an aggregate result"
    - "last_seen renders humanized ('5m ago', '3d ago'); null renders 'never'"
  artifacts:
    - path: "packages/web/src/components/UsersPage.tsx"
      provides: "Users table — rows, role chips, Edit-roles popover, bulk select bar, last-seen column"
      min_lines: 120
    - path: "packages/web/src/lib/relativeTime.ts"
      provides: "humanizeRelativeTime(isoString|null) helper"
      contains: "humanizeRelativeTime"
    - path: "packages/web/src/api/client.ts"
      provides: "listUsers / assignRole / revokeRole / listRoles fetch wrappers + UserRow type"
      contains: "is_bootstrap"
    - path: "packages/web/src/test/seedAuthStore.ts"
      provides: "seedUserAdminStore() helper"
      contains: "seedUserAdminStore"
  key_links:
    - from: "UsersPage chip ×"
      to: "DELETE /api/users/:username/roles/:roleName via revokeRole"
      via: "revokeRole(username, roleName)"
      pattern: "revokeRole"
    - from: "UsersPage Edit-roles popover checkbox"
      to: "POST/DELETE /api/users/:username/roles via assignRole/revokeRole"
      via: "checkbox toggle handler"
      pattern: "assignRole|revokeRole"
    - from: "UsersPage assign/revoke controls"
      to: "useAuthStore.hasPermission(PERMISSIONS.USERS_ASSIGN_ROLES)"
      via: "hide-don't-disable gate"
      pattern: "USERS_ASSIGN_ROLES"
---

<objective>
Build the Users page component: a table of all known users (username | role chips | last-seen | actions), per-row role chips with × to revoke, an Edit-roles checkbox popover (assign/revoke), bulk row-select + "Assign role to N selected", and a humanized last-seen column. The page consumes the extended GET /api/users shape from Plan 49-01.

Hide-don't-disable: assign/revoke controls (chip ×, Edit roles button, bulk bar) render ONLY when the session has `users:assign_roles`; a `users:view`-only session sees read-only chips. The bootstrap row is always locked (no ×, no Edit roles). The component is NOT wired into App.tsx here — that is Plan 49-03 (which imports this component).

Purpose: USERS-V18-01 (list + chips + last-seen), USERS-V18-02 (assign/revoke per user, multiple roles), USERS-V18-03 (bulk assign).
Output: UsersPage.tsx, client API wrappers, relativeTime helper, seedUserAdminStore test helper, spec, CSS.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-users-management-ui/49-RESEARCH.md
@.planning/phases/49-users-management-ui/49-CONTEXT.md

<read_first>
- 49-CONTEXT.md § Specific Ideas — the approved layout sketch (USERNAME | ROLES | LAST SEEN | actions; lock on bootstrap; default chip)
- 49-RESEARCH.md § "Code Examples" — humanizeRelativeTime helper, click-outside popover pattern, seedUserAdminStore helper
- packages/web/src/components/charts/DataFilterRenderer.tsx lines ~735–752 — click-outside popover (useRef + document.addEventListener("mousedown"))
- packages/web/src/components/DatasetsPage.tsx — closest page-shape precedent (useApiQuery or local useEffect+useState fetch, list/error/loading states)
- packages/web/src/test/seedAuthStore.ts — seedAdminStore/seedAnalystStore/seedDesignerStore (pattern to mirror)
- packages/web/src/api/client.ts — apiFetch wrapper, existing fetch-helper style (lines ~196–256), AuthUser type (~112)
- packages/web/src/store/toast.ts — showToast(message, kind?: "permission"|"info"|"error")
- packages/web/src/lib/permissions.ts — PERMISSIONS.USERS_VIEW, PERMISSIONS.USERS_ASSIGN_ROLES
- packages/web/src/styles/global.css — chip/badge precedents (.layer-row-badge), .layers-modal-* naming
</read_first>

<interfaces>
GET /api/users → `{ users: Array<{ username: string; roles: string[]; last_seen: string|null; is_bootstrap: boolean }> }`
GET /api/roles → `{ roles: Array<{ id: number; name: string; description: string; built_in: boolean; permissions: string[] }> }`
POST /api/users/:username/roles  body `{ roleName }` → 200 `{ ok, username, roleName }` | 404 | 400
DELETE /api/users/:username/roles/:roleName → 200 | 404 | 400 `{ error: "Cannot revoke: this is the last admin. ..." }`
useAuthStore: `const hasPermission = useAuthStore(s => s.hasPermission)`.
useToastStore: `useToastStore.getState().showToast(msg, "error"|"info")`.
apiFetch is the default-exported fetch wrapper in client.ts; existing helpers throw on non-ok and parse JSON — follow that style, BUT for assign/revoke return the parsed body (including error) on non-ok so the caller can surface the verbatim server message rather than a generic throw.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add client API wrappers, UserRow type, relativeTime helper, and seedUserAdminStore</name>
  <files>packages/web/src/api/client.ts, packages/web/src/lib/relativeTime.ts, packages/web/src/lib/relativeTime.spec.ts, packages/web/src/test/seedAuthStore.ts</files>
  <read_first>
    - client.ts existing fetch helpers (~196–256) for style; AuthUser type (~112)
    - seedAuthStore.ts existing three helpers
    - 49-RESEARCH.md humanizeRelativeTime + seedUserAdminStore code examples
  </read_first>
  <action>
    In `packages/web/src/api/client.ts` add (near the other DTO types and helpers):
    ```typescript
    export type RoleDto = { id: number; name: string; description: string; built_in: boolean; permissions: string[] };
    export type UserRow = { username: string; roles: string[]; last_seen: string | null; is_bootstrap: boolean };

    export const listUsers = async (signal?: AbortSignal): Promise<UserRow[]> => {
      const response = await apiFetch(`${API_BASE}/api/users`, { signal });
      if (!response.ok) throw new Error(`Failed to load users (${response.status})`);
      const json = await response.json();
      return json.users as UserRow[];
    };

    export const listRoles = async (signal?: AbortSignal): Promise<RoleDto[]> => {
      const response = await apiFetch(`${API_BASE}/api/roles`, { signal });
      if (!response.ok) throw new Error(`Failed to load roles (${response.status})`);
      const json = await response.json();
      return json.roles as RoleDto[];
    };

    // Returns { ok: true } on success, or { ok: false, error } carrying the verbatim
    // server message (e.g. the SAFE-V18-01 last-admin 400) so callers can surface it.
    export const assignRole = async (username: string, roleName: string): Promise<{ ok: boolean; error?: string }> => {
      const response = await apiFetch(`${API_BASE}/api/users/${encodeURIComponent(username)}/roles`, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ roleName }),
      });
      const json = await response.json().catch(() => ({}));
      return response.ok ? { ok: true } : { ok: false, error: json.error || `Failed (${response.status})` };
    };

    export const revokeRole = async (username: string, roleName: string): Promise<{ ok: boolean; error?: string }> => {
      const response = await apiFetch(
        `${API_BASE}/api/users/${encodeURIComponent(username)}/roles/${encodeURIComponent(roleName)}`,
        { method: "DELETE" }
      );
      const json = await response.json().catch(() => ({}));
      return response.ok ? { ok: true } : { ok: false, error: json.error || `Failed (${response.status})` };
    };
    ```
    Use the existing `apiFetch` reference exactly as the other helpers in the file do (same import/closure scope — do not re-import).

    Create `packages/web/src/lib/relativeTime.ts` with the exact humanizeRelativeTime helper from 49-RESEARCH.md § Code Examples (handles null → "never"; s/m/h/d ago buckets).
    Create `packages/web/src/lib/relativeTime.spec.ts` (CHECKER BLOCKER FIX — vitest exits non-zero on zero matching files): 4 unit tests for humanizeRelativeTime using bigNumberFormat.spec.ts as the format precedent — null input → "never"; ~5 minutes ago → matches /\d+m ago/; ~2 hours ago → matches /\d+h ago/; ~3 days ago → matches /\d+d ago/.

    In `packages/web/src/test/seedAuthStore.ts` add `seedUserAdminStore()` exactly per 49-RESEARCH.md § Code Examples — user_admin permissions: USERS_VIEW, USERS_ASSIGN_ROLES, ROLES_VIEW, ROLES_MANAGE_PERMISSIONS, ROLES_CREATE_CUSTOM, DASHBOARDS_VIEW. Use PERMISSIONS.* constants only (no raw strings) so a catalog rename is caught at compile time.
  </action>
  <verify>
    <automated>npm run test -- --run relativeTime</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export const listUsers" packages/web/src/api/client.ts` and `grep -q "export const revokeRole" packages/web/src/api/client.ts`
    - `grep -q "is_bootstrap" packages/web/src/api/client.ts` (UserRow type present)
    - `grep -q "humanizeRelativeTime" packages/web/src/lib/relativeTime.ts`
    - `test -f packages/web/src/lib/relativeTime.spec.ts` and `npm run test -- --run relativeTime` exits 0 with ≥4 tests
    - `grep -q "seedUserAdminStore" packages/web/src/test/seedAuthStore.ts` and `grep -q "USERS_ASSIGN_ROLES" packages/web/src/test/seedAuthStore.ts`
    - tsc clean (no type errors) — verified by the build in the verification block.
  </acceptance_criteria>
  <done>Client wrappers (listUsers/listRoles/assignRole/revokeRole) return parsed error bodies on failure; UserRow/RoleDto types exported; relativeTime helper + seedUserAdminStore in place.</done>
</task>

<task type="auto">
  <name>Task 2: Build UsersPage.tsx (table, chips, Edit-roles popover, bulk bar, last-seen) + CSS</name>
  <files>packages/web/src/components/UsersPage.tsx, packages/web/src/styles/global.css</files>
  <read_first>
    - 49-CONTEXT.md layout sketch + decisions (chip ×, default chip, bootstrap lock, bulk bar copy)
    - DataFilterRenderer.tsx popover pattern; DatasetsPage.tsx fetch/loading/error shape
    - global.css .layer-row-badge / .layers-modal-* for class-naming convention
  </read_first>
  <action>
    Create `packages/web/src/components/UsersPage.tsx`. Structure:

    State: `users: UserRow[]`, `roles: RoleDto[]`, `loading/error`, `openPopoverUser: string | null`, `selected: Set<string>` (bulk), `bulkRoleName: string`. Single `popoverRef = useRef<HTMLDivElement>(null)`.
    `const hasPermission = useAuthStore(s => s.hasPermission); const canAssign = hasPermission(PERMISSIONS.USERS_ASSIGN_ROLES);`

    Fetch: on mount, `useEffect` with an AbortController — call `listUsers(signal)` and `listRoles(signal)` (Promise.all); set state; abort on cleanup (DynamicViewsModal AbortController precedent). Expose a `refetch()` that re-runs listUsers and updates state — call it after every successful assign/revoke (Pitfall 7: re-fetch to keep chips fresh).

    Click-outside popover: replicate the DataFilterRenderer pattern — `useEffect` gated on `openPopoverUser`, `document.addEventListener("mousedown", ...)` closing when `popoverRef.current` does not contain the target. Only one popover open at a time (keyed by username).

    Table rendering (one row per user, sorted as returned):
    - Checkbox cell: render the row checkbox ONLY when `canAssign && !user.is_bootstrap`. Toggling updates `selected`.
    - Username cell: username + a lock icon (faLock) when `user.is_bootstrap`.
    - Roles cell:
      - If `user.is_bootstrap`: render an immutable `[admin]` chip (`.role-chip` no ×), tooltip "Bootstrap admin (APP_ADMIN_USERNAME) — always app admin." No Edit-roles button.
      - Else if `user.roles.length === 0`: render a muted default chip `.role-chip .role-chip--default` reading "analyst (default)", no ×, tooltip "No roles assigned — analyst by default."
      - Else: render each role as `.role-chip` with a × button when `canAssign`. The × calls `handleRevoke(user.username, roleName)`.
    - Last-seen cell: `humanizeRelativeTime(user.last_seen)`.
    - Actions cell: when `canAssign && !user.is_bootstrap`, an "Edit roles" button that sets `openPopoverUser = user.username`. The popover (wrapped in `popoverRef` div) lists ALL roles from `roles` as checkboxes; checked = role in `user.roles`. Toggling a checked→unchecked fires `handleRevoke`; unchecked→checked fires `handleAssign`. After the call, refetch. If a revoke returns `{ ok:false, error }`, render the verbatim error inline in the popover next to that checkbox (a small `.popover-error` span) AND keep the checkbox checked.

    handleAssign(username, roleName): `const r = await assignRole(...)`; if !r.ok showToast(r.error!, "error"); else refetch.
    handleRevoke(username, roleName): `const r = await revokeRole(...)`; if !r.ok showToast(r.error!, "error") (this surfaces the verbatim SAFE-V18-01 message for chip-× revokes); else refetch.

    Bulk bar (render only when `canAssign && selected.size > 0`): a role `<select>` bound to `bulkRoleName` (options from `roles`) + an "Assign role to N selected" button. On click: `Promise.allSettled` of `assignRole(username, bulkRoleName)` for each username in `selected` (Pitfall 8 — allSettled, not all). Count fulfilled with `r.value.ok` as success; collect failures as `<username — error>`. Toast aggregate: e.g. `"3 assigned, 1 failed: jchen — Role 'x' not found."` (omit the "failed" clause when zero failures). Clear `selected`, refetch.

    Empty/loading/error: loading → muted "Loading users…"; error → `.error` message; empty (only bootstrap) → still renders the bootstrap row (Claude's discretion for any extra empty hint).

    Read-only path (canAssign false): no checkboxes, no × on chips, no Edit-roles button, no bulk bar — chips render plain (USERS-V18 hide-don't-disable; users:view-only sees read-only chips).

    Import faLock, faXmark from "@fortawesome/free-solid-svg-icons" and FontAwesomeIcon (already used elsewhere — match existing import style). Use clsx for chip variant classes if convenient (already a dep).

    CSS in `packages/web/src/styles/global.css` (append): `.users-page` table layout; `.role-chip` (inline-flex pill, matching .layer-row-badge sizing); `.role-chip--default` (muted/outlined); `.role-chip__remove` (× button); `.users-bulk-bar`; `.users-popover` (absolute-positioned panel, z-index above table); `.popover-error` (small error text). Mirror existing `.layers-modal-*` / `.layer-row-badge` conventions — no new design system.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "USERS_ASSIGN_ROLES" packages/web/src/components/UsersPage.tsx` (assign gate present)
    - `grep -q "is_bootstrap" packages/web/src/components/UsersPage.tsx` (bootstrap lock branch)
    - `grep -q "humanizeRelativeTime" packages/web/src/components/UsersPage.tsx` (last-seen column)
    - `grep -q "allSettled" packages/web/src/components/UsersPage.tsx` (bulk aggregate)
    - `grep -q "role-chip" packages/web/src/styles/global.css`
    - `npm run build` (web tsc + build) passes clean.
  </acceptance_criteria>
  <done>UsersPage renders the table per the layout sketch; assign/revoke controls gate on users:assign_roles; bootstrap row locked; bulk assign uses allSettled with aggregate toast; last-seen humanized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: UsersPage.spec.tsx — gated render + chip/bootstrap/last-admin behaviors</name>
  <files>packages/web/src/components/UsersPage.spec.tsx</files>
  <read_first>
    - packages/web/src/test/setup.ts — vi.mock("zustand") reset shim + afterEach cleanup
    - existing gated-render spec using seedAuthStore (e.g. a Phase 48 DashboardsPage or Sidebar spec) for the RTL + seed-in-beforeEach + fetch-mock pattern
    - packages/web/src/api/client.ts — listUsers/listRoles/assignRole/revokeRole (vi.mock these)
  </read_first>
  <behavior>
    - Test 1 (USERS-V18-01 gating): seedUserAdminStore() → render → "Edit roles" buttons present for non-bootstrap rows AND bulk affordance available; seedAnalystStore() (users:view-only is NOT analyst — use a custom seed with only USERS_VIEW, OR assert via a viewer seed) → no "Edit roles" button, chips have no × control.
    - Test 2 (chips): a user with roles ["designer","user_admin"] shows two chips; an unassigned user (roles []) shows one muted "analyst (default)" chip with no ×.
    - Test 3 (bootstrap lock): is_bootstrap row shows a lock indicator + admin chip with no × and no "Edit roles" button.
    - Test 4 (last-admin verbatim toast): mock revokeRole to resolve `{ ok:false, error:"Cannot revoke: this is the last admin. At least one non-bootstrap user must hold the admin role." }`; click a chip × → assert showToast called with that exact string and kind "error".
    - Test 5 (last-seen): a user with last_seen null shows "never"; a recent last_seen shows an "m ago"/"h ago" style string.
  </behavior>
  <action>
    Create `packages/web/src/components/UsersPage.spec.tsx`. `vi.mock("../api/client", ...)` to stub listUsers/listRoles/assignRole/revokeRole with controllable resolved values per test. Mock useToastStore.getState().showToast with a vi.fn (or spy on the store) to assert calls.

    For the users:view-only render, add a local seed helper inside the spec (or extend seedAuthStore) that sets permissions to `[PERMISSIONS.USERS_VIEW]` only — this is the read-only viewer the gating must serve. Assert the absence of assign controls with queryByText/queryByRole returning null (`! present`).

    Use `seedUserAdminStore()` for the assign-capable render. Seed inside beforeEach AFTER the zustand reset shim (per seedAuthStore.ts module note). Use findBy*/waitFor for the async fetch resolution before asserting.

    For Test 4, render with seedUserAdminStore, wait for the chip, click the chip × (getByRole button within the admin chip), await the revoke, assert the toast spy received the verbatim string.
  </action>
  <verify>
    <automated>npm run test -- --run UsersPage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "seedUserAdminStore" packages/web/src/components/UsersPage.spec.tsx`
    - `grep -q "Cannot revoke: this is the last admin" packages/web/src/components/UsersPage.spec.tsx` (verbatim toast assertion)
    - `npm run test -- --run UsersPage` passes all 5 cases.
  </acceptance_criteria>
  <done>Spec proves gated rendering (user_admin vs view-only), chip variants, bootstrap lock, verbatim last-admin toast, and humanized last-seen.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run UsersPage relativeTime` passes.
- `npm run build` (web tsc + vite build) passes clean.
- Frontend full gate: 1513/1514 baseline preserved — do not fix or worsen the known-red TD-V17-DASHPAGE-SPEC (DashboardsPage button-order).
</verification>

<success_criteria>
- UsersPage lists users with role chips + humanized last-seen; assign/revoke controls gate on users:assign_roles (hide-don't-disable); bootstrap row locked.
- Chip × revokes; verbatim SAFE-V18-01 400 surfaces as an error toast (and inline in popover).
- Bulk assign issues per-user assigns with an aggregate toast.
- USERS-V18-01/02/03 satisfied (page-level); component ready for App.tsx wiring in 49-03.
</success_criteria>

<output>
After completion, create `.planning/phases/49-users-management-ui/49-02-SUMMARY.md`
</output>
