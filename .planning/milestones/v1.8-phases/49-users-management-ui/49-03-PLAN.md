---
phase: 49-users-management-ui
plan: 03
type: execute
wave: 3
depends_on: ["49-01", "49-02"]
files_modified:
  - packages/web/src/App.tsx
  - packages/web/src/App.spec.tsx
  - packages/web/src/styles/global.css
autonomous: true
requirements: [USERS-V18-04]
must_haves:
  truths:
    - "Selecting the 'User Management' sidebar item routes to the Users page (page === 'users' renders <UsersPage/>)"
    - "After OIDC re-auth, a stored ReturnTo of page:'users' restores the Users page instead of falling back to dashboards"
    - "A session with users:assign_roles and >=1 unassigned user sees the onboarding banner ('N user(s) ... on the default analyst role — Review in User Management'); clicking the link navigates to the Users page"
    - "A session WITHOUT users:assign_roles never fetches the count and never sees the banner"
    - "Dismissing the banner (×) hides it for the session; the banner does not appear when zero users are unassigned"
  artifacts:
    - path: "packages/web/src/App.tsx"
      provides: "Page union 'users' member, render branch, OIDC ReturnTo case, onboarding banner state+fetch+JSX"
      contains: "onboarding-banner"
  key_links:
    - from: "Sidebar 'users' key onSelect"
      to: "page === 'users' render branch → <UsersPage/>"
      via: "setPage(key as Page)"
      pattern: "page === \"users\""
    - from: "onboarding banner fetch effect"
      to: "GET /api/users (count roles.length===0)"
      via: "listUsers gated on hasPermission(USERS_ASSIGN_ROLES)"
      pattern: "USERS_ASSIGN_ROLES"
    - from: "banner link"
      to: "Users page"
      via: "setPage('users')"
      pattern: "setPage\\(\"users\"\\)"
---

<objective>
Wire the Users page into the app shell and add the USERS-V18-04 onboarding banner. Three touches in App.tsx (per research finding 3): (1) add `"users"` to the `type Page` union; (2) add the `page === "users"` render branch mounting `<UsersPage/>`; (3) extend the OIDC ReturnTo restore conditional to accept `parsed.page === "users"` (easy to miss — without it, re-authed Users-page users bounce to dashboards). Plus the onboarding banner: a session-dismissable bar in `<div className="main">` between Topbar and the page branches, shown to users with `users:assign_roles` while unassigned users exist.

Purpose: USERS-V18-04 (admin onboarding banner) + makes the Users page reachable. Depends on 49-02 (imports UsersPage) and 49-01 (banner reads the live GET /api/users; unassigned = roles.length===0).
Output: App.tsx integration, banner, spec, CSS.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-users-management-ui/49-RESEARCH.md
@.planning/phases/49-users-management-ui/49-CONTEXT.md

<read_first>
- packages/web/src/App.tsx — line 18 (type Page union, 3 members), lines ~155–166 (OIDC ReturnTo restore useEffect + parsed.page conditional), lines ~202–218 (app-shell: <div className="main"><Topbar/>{page branches}</div>), the status==="authenticated" effects nearby
- 49-RESEARCH.md § "Pattern 1/2/3" (Page union, banner mount, banner JSX), Pitfalls 3 (ReturnTo) and 6 (banner fetch must not block bootstrap)
- 49-RESEARCH.md § "App.tsx Banner Integration Point" code example (exact JSX)
- packages/web/src/lib/permissions.ts — PERMISSIONS.USERS_ASSIGN_ROLES
- packages/web/src/api/client.ts — listUsers (added in 49-02)
- packages/web/src/styles/global.css — .login-banner / role="status" precedent
</read_first>

<interfaces>
type Page (App.tsx:18) currently `"dashboards" | "datasets" | "settings"`.
Sidebar onSelect (App.tsx ~206) does `setPage(key as Page)`; the "users" nav key already exists (Phase 48, gated on USERS_VIEW).
OIDC ReturnTo restore (App.tsx ~158–166): `if (parsed.page === "dashboards" || parsed.page === "datasets" || parsed.page === "settings") setPage(parsed.page);`
useAuthStore hasPermission selector available; `status` already in scope from useAuthStore.
listUsers(signal?) → UserRow[] with `roles: string[]` (added 49-02); unassigned user = `roles.length === 0`. The bootstrap row (is_bootstrap:true, roles:[]) must NOT be counted as "unassigned" for the banner — exclude `is_bootstrap` rows from the count.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three App.tsx touches — Page union, render branch, OIDC ReturnTo restore</name>
  <files>packages/web/src/App.tsx</files>
  <read_first>
    - App.tsx line 18 (Page union), lines ~155–166 (ReturnTo conditional), lines ~210–217 (render branches inside .main)
  </read_first>
  <action>
    1. Import UsersPage at the top of App.tsx alongside the other page imports: `import UsersPage from "./components/UsersPage";`.
    2. Extend the union at line 18: `type Page = "dashboards" | "datasets" | "settings" | "users";`.
    3. In the OIDC ReturnTo restore useEffect (~line 158), add `|| parsed.page === "users"` to the conditional so a stored `{ page: "users" }` restores correctly (Pitfall 3):
       ```typescript
       if (
         parsed.page === "dashboards" ||
         parsed.page === "datasets" ||
         parsed.page === "settings" ||
         parsed.page === "users"
       ) {
         setPage(parsed.page);
       }
       ```
    4. Add the render branch inside `<div className="main">`, after the `page === "settings"` block:
       `{page === "users" && <UsersPage />}`
    Do not change the Sidebar onSelect cast — `setPage(key as Page)` already accepts "users" once the union includes it.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"settings" | "users"' packages/web/src/App.tsx` (union extended)
    - `grep -q 'parsed.page === "users"' packages/web/src/App.tsx` (ReturnTo case)
    - `grep -q 'page === "users" && <UsersPage' packages/web/src/App.tsx` (render branch)
    - `grep -q 'import UsersPage' packages/web/src/App.tsx`
    - `npm run build` passes clean.
  </acceptance_criteria>
  <done>Users page is reachable via the sidebar and survives OIDC re-auth ReturnTo restore.</done>
</task>

<task type="auto">
  <name>Task 2: Onboarding banner — state, lazy fetch effect, JSX, CSS</name>
  <files>packages/web/src/App.tsx, packages/web/src/styles/global.css</files>
  <read_first>
    - 49-RESEARCH.md § "Pattern 2" + "App.tsx Banner Integration Point" (exact JSX) + Pitfall 6 (separate effect, not the bootstrap chain)
    - 49-CONTEXT.md onboarding banner decision (copy, session-dismiss, who sees it)
  </read_first>
  <action>
    In App.tsx add two state values: `const [unassignedCount, setUnassignedCount] = useState<number | null>(null);` and `const [bannerDismissed, setBannerDismissed] = useState(false);`. Import `listUsers` from "./api/client" and `PERMISSIONS` from "./lib/permissions"; get `const hasPermission = useAuthStore(s => s.hasPermission);` (if not already selected in App.tsx, add it).

    Add a SEPARATE useEffect (NOT inside the auth bootstrap chain — Pitfall 6) gated on `status === "authenticated" && hasPermission(PERMISSIONS.USERS_ASSIGN_ROLES)`. Inside, fetch `listUsers(controller.signal)` once; compute `count = users.filter(u => !u.is_bootstrap && u.roles.length === 0).length` (exclude the bootstrap row — it is never "unassigned"); `setUnassignedCount(count)`. Wrap in try/catch and swallow AbortError. Cleanup aborts the controller. Deps: `[status, hasPermission]`. Users without users:assign_roles never enter this effect, so the count stays null and the banner never renders.

    Add the banner JSX inside `<div className="main">`, AFTER `<Topbar />` and BEFORE the page branches (matching 49-RESEARCH.md § "App.tsx Banner Integration Point"):
    ```tsx
    {unassignedCount !== null && unassignedCount > 0 && !bannerDismissed && (
      <div className="onboarding-banner" role="status">
        {unassignedCount} user{unassignedCount !== 1 ? "s are" : " is"} on the default analyst role —{" "}
        <button className="banner-link" onClick={() => setPage("users")}>
          Review in User Management
        </button>
        <button className="banner-dismiss" aria-label="Dismiss" onClick={() => setBannerDismissed(true)}>×</button>
      </div>
    )}
    ```
    Note the `> 0` guard: the banner auto-hides when all (non-bootstrap) users have explicit assignments. `bannerDismissed` is React state only — session-dismissable, reappears on reload/next login (49-RESEARCH.md Open Question 3 — no sessionStorage needed).

    CSS in global.css (append): `.onboarding-banner` styled like `.login-banner` (role="status" precedent) — a full-width info bar below the topbar; `.banner-link` (link-styled button); `.banner-dismiss` (× button, right-aligned). Mirror existing banner/notice styling — no new design system.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "onboarding-banner" packages/web/src/App.tsx` and `grep -q "onboarding-banner" packages/web/src/styles/global.css`
    - `grep -q "default analyst role" packages/web/src/App.tsx` (banner copy)
    - `grep -q "USERS_ASSIGN_ROLES" packages/web/src/App.tsx` (banner gated on assign permission)
    - `grep -q "u.is_bootstrap" packages/web/src/App.tsx` (bootstrap excluded from count) — or equivalent `!u.is_bootstrap` filter
    - `grep -q "setPage(\"users\")" packages/web/src/App.tsx` (banner link target)
    - `npm run build` passes clean.
  </acceptance_criteria>
  <done>Banner renders for users:assign_roles holders while non-bootstrap unassigned users exist, links to the Users page, is session-dismissable, and auto-hides at zero.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: App.spec.tsx — banner gating + dismiss + routing</name>
  <files>packages/web/src/App.spec.tsx</files>
  <read_first>
    - packages/web/src/test/setup.ts (zustand reset shim, afterEach cleanup)
    - existing App.spec.tsx if present (extend it); otherwise mirror a Phase 48 page spec for the App render + seed pattern
    - seedAuthStore.ts — seedUserAdminStore (49-02), seedAnalystStore
  </read_first>
  <behavior>
    - Test 1: seedUserAdminStore() + mock listUsers resolving 2 non-bootstrap users with roles:[] → banner renders with "2 users are on the default analyst role"; clicking "Review in User Management" routes to the Users page (UsersPage content visible).
    - Test 2: seedAnalystStore() (no users:assign_roles) → listUsers NOT called for the banner; banner not in document. — assert via `expect(listUsers).not.toHaveBeenCalled()` after rendering with the non-permitted store seed
    - Test 3: dismiss — with the banner shown, click the × → banner removed from document.
    - Test 4: zero unassigned (mock listUsers resolving only assigned users + a bootstrap row) → banner not shown (the bootstrap row's roles:[] must not count).
  </behavior>
  <action>
    Create or extend `packages/web/src/App.spec.tsx`. `vi.mock("./api/client")` to stub listUsers (and any other client calls App triggers — fetchMe/fetchAuthConfig/initWmsCapabilities; stub minimally so App reaches authenticated render). Also mock the UsersPage's own client calls if rendering the page in Test 1 (or assert a UsersPage landmark/heading).

    Seed the auth store in beforeEach AFTER the reset shim. For Test 1 use seedUserAdminStore and mock listUsers to resolve `[{username:"a",roles:[],last_seen:null,is_bootstrap:false},{username:"b",roles:[],last_seen:null,is_bootstrap:false}]`; await the banner via findByText(/2 users are on the default analyst role/); click the "Review in User Management" button; assert the Users page rendered (its heading/landmark).

    For Test 4 mock listUsers to resolve `[{username:"admin",roles:[],last_seen:null,is_bootstrap:true},{username:"c",roles:["designer"],last_seen:null,is_bootstrap:false}]` and assert the banner text is NOT present (bootstrap excluded; the only other user is assigned).

    Use waitFor/findBy for async fetch resolution.
  </action>
  <verify>
    <automated>npm run test -- --run App.spec</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "default analyst role" packages/web/src/App.spec.tsx` (banner text assertion)
    - `grep -q "seedUserAdminStore" packages/web/src/App.spec.tsx`
    - `npm run test -- --run App.spec` passes all 4 cases.
  </acceptance_criteria>
  <done>Spec proves banner gating (assign vs non-assign), dismiss, zero-unassigned hide (bootstrap excluded), and banner-link routing to the Users page.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run App.spec` passes.
- `npm run build` (web tsc + vite build) passes clean.
- Frontend full gate: 1513/1514 baseline preserved — do not fix or worsen TD-V17-DASHPAGE-SPEC.
</verification>

<success_criteria>
- "User Management" sidebar item routes to the Users page; OIDC ReturnTo restores it.
- Onboarding banner shows to users:assign_roles holders while non-bootstrap unassigned users exist, links to the page, is session-dismissable, auto-hides at zero.
- USERS-V18-04 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/49-users-management-ui/49-03-SUMMARY.md`
</output>
