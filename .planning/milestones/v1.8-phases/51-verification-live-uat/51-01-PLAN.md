---
phase: 51-verification-live-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/DashboardsPage.spec.tsx
  - .planning/phases/51-verification-live-uat/51-01-AUTOMATED-GATES.md
autonomous: true
requirements: [VERIFY-V18-01]
must_haves:
  truths:
    - "The known-red DashboardsPage button-order test now passes against the CURRENT toolbar order (Tables → Dynamic Views → Map Layers → Visualizations)"
    - "Frontend vitest is 100% green (0 failures) and frontend tsc --noEmit is clean"
    - "Server tsc --noEmit is clean and all 8 RBAC spec groups are deterministically green"
    - "Server vitest failing FILES are a subset of the 14 known-flaky TD-V16-TEST-ISOLATION list (no new failing files)"
    - "Both production builds (web + server) complete clean"
    - "All gate results are recorded verbatim in 51-01-AUTOMATED-GATES.md for the compiler in 51-03"
  artifacts:
    - path: ".planning/phases/51-verification-live-uat/51-01-AUTOMATED-GATES.md"
      provides: "Recorded pass/fail + counts for every automated gate, consumed by 51-03 VERIFICATION compile"
      contains: "frontend_vitest"
    - path: "packages/web/src/components/DashboardsPage.spec.tsx"
      provides: "Corrected button-order assertion matching real DOM order"
  key_links:
    - from: "DashboardsPage.spec.tsx button-order test"
      to: "DashboardsPage.tsx dashboard-toolbar render order"
      via: "getAllByRole('button') index comparison"
      pattern: "dvIdx.*tablesIdx|mapLayersIdx.*dvIdx"
---

<objective>
Fix the single known-red frontend test (TD-V17-DASHPAGE-SPEC) so the frontend suite reaches TRUE 100% green, then run and record EVERY automated gate that feeds the milestone-close VERIFICATION report. This is the autonomous, machine-verifiable half of Phase 51 — it runs to completion before any human walk-through.

Purpose: VERIFY-V18-01 requires "frontend vitest/tsc + server tsc green; server vitest no worse than pre-v1.8 baseline." This plan produces the evidence for that clause and clears the last red test blocking a clean milestone close.
Output: A corrected `DashboardsPage.spec.tsx` assertion + `51-01-AUTOMATED-GATES.md` capturing all gate results for the 51-03 compiler.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/51-verification-live-uat/51-CONTEXT.md

<read_first>
- `packages/web/src/components/DashboardsPage.tsx` lines 739-761 — the `dashboard-toolbar` block. CURRENT render order (verified during planning): **Tables (canEdit) → Dynamic Views (canDynamicViews) → Map Layers (canLayers) → Visualizations (canEdit)**. The "+ New Dashboard" button (line 151) is in the LIST view, not this toolbar.
- `packages/web/src/components/DashboardsPage.spec.tsx` lines 598-608 — the failing test "renders 'Dynamic Views' button in dashboard action bar after 'Map Layers'". It currently asserts `expect(dvIdx).toBeGreaterThan(mapLayersIdx)` — i.e. Dynamic Views AFTER Map Layers. This is STALE: the real DOM renders Dynamic Views (line 747) BEFORE Map Layers (line 752), so `dvIdx < mapLayersIdx`. That is why it is red.
</read_first>

<interfaces>
The failing test body (DashboardsPage.spec.tsx:598-608):
```typescript
it("renders 'Dynamic Views' button in dashboard action bar after 'Map Layers'", async () => {
  await openDashboard();
  const button = await screen.findByRole("button", { name: "Dynamic Views" });
  expect(button).toBeInTheDocument();
  // Button ordering: Map Layers → Dynamic Views.
  const buttons = screen.getAllByRole("button");
  const mapLayersIdx = buttons.findIndex((b) => b.textContent === "Map Layers");
  const dvIdx = buttons.findIndex((b) => b.textContent === "Dynamic Views");
  expect(mapLayersIdx).toBeGreaterThanOrEqual(0);
  expect(dvIdx).toBeGreaterThan(mapLayersIdx);   // <-- WRONG: real order is dv BEFORE mapLayers
});
```

The 8 RBAC server spec groups (confirmed present as files in packages/server/tests/):
lib.permissions.spec.ts, db.rbacMigration.spec.ts, db.rbacAudit.spec.ts, lib.rbacDb.spec.ts, boot.rbacAdminWarning.spec.ts, routes.rbac.spec.ts, routes.guards.spec.ts, routes.management.spec.ts

The 14 known-flaky server files (TD-V16-TEST-ISOLATION — EXCLUDED from the gate, may fail in the full run only):
auth.oidc.spec.ts, auth.routes.spec.ts, boot.hardening.spec.ts, boot.wipe.spec.ts, bootstrap.spec.ts, db.smoke.spec.ts, errorMiddleware.spec.ts, kinetica.creds.routes.spec.ts, oidc.module.spec.ts, routes.discovery.spec.ts, routes.materialize.spec.ts, routes.sql.spec.ts, routes.wms.spec.ts (13 named in the Phase 50 report; the gate accepts any subset of these — "14 known-flaky" is the historical ceiling).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the stale button-order assertion (TD-V17-DASHPAGE-SPEC)</name>
  <files>packages/web/src/components/DashboardsPage.spec.tsx</files>
  <action>
This is a SPEC fix, not a product change — do NOT touch DashboardsPage.tsx. The component's button order is correct and intentional; the test's expectation is stale.

CURRENT real toolbar order (DashboardsPage.tsx:739-761): Tables → Dynamic Views → Map Layers → Visualizations.

In the test at DashboardsPage.spec.tsx:598-608, update the assertion to match the real DOM order. Rename the `it(...)` title to describe the true order and assert it:
- Title: change to `"renders 'Dynamic Views' button in dashboard toolbar between 'Tables' and 'Map Layers'"`.
- Replace the ordering assertions so they prove the ACTUAL order. Compute `tablesIdx` (button whose textContent === "Tables"), `dvIdx`, and `mapLayersIdx`, then assert:
  - `expect(tablesIdx).toBeGreaterThanOrEqual(0)`
  - `expect(dvIdx).toBeGreaterThan(tablesIdx)`
  - `expect(mapLayersIdx).toBeGreaterThan(dvIdx)`
- Update the inline comment from "Button ordering: Map Layers → Dynamic Views." to "Button ordering: Tables → Dynamic Views → Map Layers (matches DashboardsPage.tsx dashboard-toolbar)."

Do NOT weaken the test into a presence-only check — keep it asserting relative order, just the CORRECT order. Leave every other test in the file untouched.
  </action>
  <verify>
    <automated>cd /Users/rydelpereira/Documents/projects/kinetica_bi && npm run test -- --run DashboardsPage 2>&1 | tail -15</automated>
  </verify>
  <done>The DashboardsPage spec file runs with 0 failures; the renamed button-order test passes asserting Tables → Dynamic Views → Map Layers.</done>
</task>

<task type="auto">
  <name>Task 2: Run and record all automated gates into 51-01-AUTOMATED-GATES.md</name>
  <files>.planning/phases/51-verification-live-uat/51-01-AUTOMATED-GATES.md</files>
  <action>
Run each gate from the repo root `/Users/rydelpereira/Documents/projects/kinetica_bi` and capture the headline result (pass/fail + counts + any failing file names) into a new markdown file. Run them in this order; record the tail summary line of each.

1. **Frontend vitest (full):** `npm run test -- --run` — expect 100% green now that Task 1 landed. Record passed/total. If ANY non-DashboardsPage failure appears, STOP and report it (it is a regression, not in scope to ignore).
2. **Frontend tsc:** `npm run -w packages/web exec tsc -- --noEmit` (or the web package's typecheck script if one exists — check packages/web/package.json `scripts` first; fall back to `npx -w packages/web tsc --noEmit`). Record clean/errors. NOTE: the Phase 50 report flagged pre-existing tsc errors in `DataFilterConfigPanel.spec.tsx` and `App.spec.tsx` as pre-existing debt — if those exact files error, record them as KNOWN pre-existing (not a new failure); any OTHER file erroring is a new failure to report.
3. **Server tsc:** `npm run build:server` already runs tsc; but for an isolated typecheck use the server package typecheck script if present, else `npx -w packages/server tsc --noEmit`. Record clean/errors.
4. **RBAC spec groups (deterministic gate):** run each of the 8 groups and record individual counts:
   `npm run test:server -- lib.permissions db.rbacMigration db.rbacAudit lib.rbacDb boot.rbacAdminWarning routes.rbac routes.guards routes.management 2>&1 | tail -30`
   All 8 MUST be green. Record per-file pass counts. boot.rbacAdminWarning doubles as the automated proxy for the OIDC bootstrap-warning check (a manual boot confirmation goes on the 51-02 UAT checklist).
5. **Server vitest (full, set-based gate):** `npm run test:server 2>&1 | tail -40`. Extract the list of FAILING files. The gate PASSES iff every failing file is in the 14 known-flaky TD-V16-TEST-ISOLATION list (see interfaces). Record: total passed, total failed, the failing-file list, and an explicit "failing files ⊆ known-flaky list: YES/NO" verdict. If NO, name the offending file(s).
6. **Builds:** `npm run build 2>&1 | tail -10` then `npm run build:server 2>&1 | tail -10`. Record clean/errors for each.

Write `.planning/phases/51-verification-live-uat/51-01-AUTOMATED-GATES.md` with frontmatter `plan: 51-01`, a run timestamp, and one section per gate using these EXACT heading anchors so 51-03 can grep them: `## frontend_vitest`, `## frontend_tsc`, `## server_tsc`, `## rbac_spec_groups`, `## server_vitest_setgate`, `## builds`. Each section: a one-line `result: PASS|FAIL` plus the captured counts/file-list as evidence. End with a `## gate_summary` table (gate | result | evidence).

If any gate FAILS in a way that is NOT a documented known-flaky/pre-existing item, do NOT paper over it — record result: FAIL and surface it in the plan return so the orchestrator can spin a 51.x fix before 51-02 proceeds.
  </action>
  <verify>
    <automated>test -f /Users/rydelpereira/Documents/projects/kinetica_bi/.planning/phases/51-verification-live-uat/51-01-AUTOMATED-GATES.md && grep -qE "## (frontend_vitest|frontend_tsc|server_tsc|rbac_spec_groups|server_vitest_setgate|builds|gate_summary)" /Users/rydelpereira/Documents/projects/kinetica_bi/.planning/phases/51-verification-live-uat/51-01-AUTOMATED-GATES.md && echo OK</automated>
  </verify>
  <done>51-01-AUTOMATED-GATES.md exists with all 6 gate sections + gate_summary; frontend vitest 100% green recorded; all 8 RBAC groups green recorded; server set-gate verdict (failing files ⊆ known-flaky) recorded; both builds recorded clean.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run` reports 0 failures (frontend TRUE 100% green).
- 51-01-AUTOMATED-GATES.md contains a PASS/FAIL line for each of the 6 gates plus the gate_summary table.
- The server set-based gate verdict explicitly states whether failing files are a subset of the 14 known-flaky list.
</verification>

<success_criteria>
- TD-V17-DASHPAGE-SPEC closed: the button-order test passes asserting the CURRENT order, no product code changed.
- Every automated gate VERIFY-V18-01 demands is run and recorded with evidence for the 51-03 compiler.
- Any new (non-known-flaky) failure is surfaced, not hidden.
</success_criteria>

<output>
After completion, create `.planning/phases/51-verification-live-uat/51-01-SUMMARY.md`.
</output>
