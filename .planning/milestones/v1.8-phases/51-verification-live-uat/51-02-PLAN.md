---
phase: 51-verification-live-uat
plan: 02
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - .planning/phases/51-verification-live-uat/51-UAT.md
autonomous: false
requirements: [VERIFY-V18-01]
user_setup:
  - service: kinetica
    why: "Live persona walk-through needs real Kinetica accounts in AUTH_MODE=password"
    dashboard_config:
      - task: "Create ~4 Kinetica test users (e.g. uat_useradmin, uat_designer, uat_analyst, uat_custom) before the walk-through"
        location: "Kinetica account administration"
      - task: "Ensure at least one dashboard with multiple widgets (incl. a map widget with a spatial target) exists for the analyst click-through exercise"
        location: "Kinetica BI app (as admin/designer)"
must_haves:
  truths:
    - "51-UAT.md exists as a persona-by-persona live checklist the operator can execute against the running app"
    - "The checklist embeds ALL queued human-verify items from the 48, 49, and 50 verification reports"
    - "The checklist embeds the live escalation/last-admin guard checks with the verbatim server messages"
    - "The checklist includes a precondition section naming the test users + dashboard the operator must provision"
    - "Role assignment to the fresh test users happens THROUGH THE APP as the first admin-persona steps (the assignment flow is itself under test)"
    - "The checklist includes a sqlite3 audit-row spot-check command against rbac_audit"
    - "After presenting the checklist, the operator's per-item attestations are recorded back into 51-UAT.md"
  artifacts:
    - path: ".planning/phases/51-verification-live-uat/51-UAT.md"
      provides: "The live guided UAT checklist + recorded operator attestations + gap table"
      contains: "preconditions"
  key_links:
    - from: "51-UAT.md analyst persona section"
      to: "core product value (click-through filtering / drill-down / map interaction)"
      via: "explicit analyst regression steps"
      pattern: "click-through|drill"
---

<objective>
Author the live, guided, persona-by-persona UAT checklist (`51-UAT.md`) for v1.8 RBAC, then run the human checkpoint: present it to the operator, collect per-item attestation, and record results. Every queued human-verify item from Phases 48/49/50, the live escalation/last-admin guards, and the audit-row spot-check are embedded so the operator walks one coherent script.

Purpose: VERIFY-V18-01's core clause — "Live operator walk-through as each role confirming gated UI + server 403s + analyst click-through exploration intact." The analyst walk doubles as the milestone's core-value regression.
Output: `51-UAT.md` with the full checklist AND the operator's recorded attestations + any gap rows.

NOTE: This plan is NOT autonomous. Task 2 is a blocking human checkpoint. Role assignment to the fresh test users is performed BY THE ADMIN PERSONA THROUGH THE APP as the opening steps — the assignment flow is part of what is under test.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/51-verification-live-uat/51-CONTEXT.md
@.planning/phases/51-verification-live-uat/51-01-AUTOMATED-GATES.md

<read_first>
- `.planning/phases/24-verification/24-01-UAT-NOTES.md` — the v1.4 UAT precedent format (per-step `name/status/evidence` YAML-ish list + a `gaps:` block). Mirror this structure but extend with persona sections.
- `.planning/phases/48-me-extension-frontend-store-ui-gating/48-VERIFICATION.md` § "Human Verification Required" (3 items) — embed verbatim.
- `.planning/phases/49-users-management-ui/49-VERIFICATION.md` § "Human Verification Required" (4 items) — embed verbatim.
- `.planning/phases/50-roles-management-ui-custom-roles-audit/50-VERIFICATION.md` § "Human Verification Required" (4 items, incl. OBS-01 audit log) — embed verbatim.
</read_first>

<interfaces>
Verbatim server messages the operator must see during the guard checks (from packages/server/src/index.ts):
- Guard 1 (assign admin role as non-admin) → HTTP 403: `Only admins can assign the admin role.`
- Guard 2 (edit admin role mappings as non-admin) → HTTP 403: `Only admins can modify the admin role.`
- Guard 3 (grant unheld permission to a custom role as non-admin) → HTTP 403: `Cannot grant permissions you do not hold: {perm}`
- Last-admin revoke (revoke admin from the last non-bootstrap admin) → HTTP 400: `Cannot revoke: this is the last admin. At least one non-bootstrap user must hold the admin role.`
- OIDC boot warning (manual boot check) server log contains: `AUTH_MODE=oidc but APP_ADMIN_USERNAME is the default 'admin'. ...` (event name `rbac_bootstrap_admin_warning`).

rbac_audit table columns: id, ts, actor, action, target, before_json, after_json.
SQLite DB path: `packages/server/data/kinetica.db` (DB_PATH=./data/kinetica.db relative to packages/server).
Audit spot-check command (run from repo root):
`sqlite3 packages/server/data/kinetica.db "SELECT ts, actor, action, target FROM rbac_audit ORDER BY id DESC LIMIT 10;"`

Run the app for UAT: `npm run dev` (web) + `npm run dev:server` (server) from repo root; AUTH_MODE=password (packages/server/.env already set).

The 4 built-in roles + their gating consequences (from CONTEXT + Phase 48-50):
- admin: all 16 permissions; sees everything.
- user_admin: users:view, users:assign_roles, roles:view, roles:manage_permissions, roles:create_custom, roles:delete_custom — Users+Roles pages; analyst-level on dashboards/widgets.
- designer: dashboards create/edit/delete, widgets:configure, layers:manage, dynamic_views:manage, datasets:manage — full design surfaces; NO Users/Roles nav.
- analyst (default for unassigned): view + interaction only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 51-UAT.md — the full persona-by-persona live checklist</name>
  <files>.planning/phases/51-verification-live-uat/51-UAT.md</files>
  <action>
Write `.planning/phases/51-verification-live-uat/51-UAT.md` mirroring the 24-01-UAT-NOTES.md precedent (frontmatter: `plan: 51-02`, `operator: RPereira@kinetica.com`, `auth_mode: password`, `started_on:` left blank for the operator). Each checklist item gets a `status: PENDING` field (operator/Task-2 flips to PASS/FAIL) and an `evidence:` field. Enumerate the FULL content below — do not leave scope to be invented at checkpoint time.

## Section 0 — Preconditions (operator provisions BEFORE the walk)
- [ ] AUTH_MODE=password confirmed in packages/server/.env
- [ ] App running: `npm run dev` + `npm run dev:server`
- [ ] ~4 Kinetica test users created: `uat_useradmin`, `uat_designer`, `uat_analyst`, `uat_custom` (all start UNASSIGNED → analyst default)
- [ ] At least one dashboard exists with multiple widgets, including a map widget with a configured spatial target (for the analyst click-through exercise)
- [ ] sqlite3 available (`which sqlite3`) for the audit spot-check
- Note: the bootstrap `admin` Kinetica account IS the admin persona. No role assignment needed for it.

## Section 1 — Admin persona (bootstrap `admin`)  [ROADMAP SC2]
Opening steps (role assignment THROUGH THE APP — itself under test):
- 1.1 Log in as `admin`. Confirm onboarding banner shows ("N users on default analyst role") since uat_* users are unassigned. IMMEDIATELY after a fresh login (no page refresh) — User Management + Roles nav visible, grid draggable (login-shape regression).
- 1.2 Go to User Management. Confirm all uat_* users + admin listed; admin row shows lock + immutable admin chip.
- 1.3 Assign roles via the app: uat_useradmin → user_admin; uat_designer → designer; uat_analyst → analyst (explicit); leave uat_custom unassigned for now. Confirm chips update live.
- 1.4 Bulk-assign exercise: select 2 users, assign a role in one action; confirm aggregate toast + chip updates (covers 49 human-verify #3).
- 1.5 Full dashboard access: create a dashboard, edit it, add a widget, delete a widget, delete the dashboard — all succeed.
- 1.6 Roles page: open it; assign/edit the admin role mappings is permitted (admin); built-in Save shows the "affects all users" confirm (covers 50 human-verify #3).
- 1.7 Create a custom role `uat_custom_role` with a deliberate subset = {dashboards:view, widgets:configure}. Confirm it appears in the matrix and becomes assignable.
- 1.8 Confirm banner clears once all non-bootstrap users have explicit assignments.

## Section 2 — Embedded Phase 48 human-verify items  [analyst gating visuals]
Embed verbatim from 48-VERIFICATION.md:
- 2.1 Live Analyst Walk-Through — no New Dashboard / Edit / Delete / Dynamic Views / Map Layers / Visualizations / Tables buttons; no widget gear/x; grid drag/resize inert. (Cross-ref: also exercised in Section 5.)
- 2.2 Mid-Session Permission Change Self-Healing — two sessions: as designer, have admin revoke dashboards:edit, then click an edit action → toast "You no longer have permission: dashboards:edit" + Edit buttons vanish without refresh.
- 2.3 Topbar user menu + Profile + Logout — log in as designer/user_admin: (a) Topbar shows username + derived initials and NO role chips; clicking the identity opens a menu with Profile + Log out (click-outside dismisses). (b) Click Profile → read-only Profile page shows username, auth mode (password sign-in / SSO), assigned roles as chips, and effective permissions grouped by category — correct for the persona. (c) Click Log out → lands on LoginPage with the session cleared; pressing the browser Back button does NOT resurrect the session (stays on LoginPage).
- 2.4 Light-mode theming pass — click the Topbar theme toggle to LIGHT mode, then walk Users, Roles, and Profile pages + the Topbar user menu. Confirm ALL surfaces render with the light theme (no dark panels), and ALL text + role chips are legible (Roles page not stuck dark; Edit-roles popover fully visible on the last row with no clipping/horizontal scrollbar; role chips readable green-on-light; Profile default chip legible). Toggle back to dark and confirm both themes read well. (Catches the Phase 50.3 bug class.) Also: trigger a login error (wrong password) in light mode — error banner text legible (dark red on tint).

## Section 3 — User admin persona (`uat_useradmin`)  [ROADMAP SC3 + embedded 49 items]
- 3.1 Log in as uat_useradmin. Confirm Users + Roles nav present; Dashboards is analyst-level (no create/edit affordances).
- 3.2 (49 human-verify #1) Users page visual layout: USERNAME | ROLE CHIPS | LAST SEEN | ACTIONS; chips are pills; bootstrap row locked; default chip muted.
- 3.3 (49 human-verify #2) Edit-roles popover click-outside dismiss closes the popover.
- 3.4 (49 human-verify #3) Live bulk-assign flow (if not already covered in 1.4 — re-run as user_admin).
- 3.5 (50 human-verify #1) Roles page two-pane layout: built-ins badged, custom roles with trash (disabled when holders>0), [+ New role]; right pane 16 checkboxes in 5 groups.
- 3.6 (50 human-verify #2) Dirty-guard: toggle a checkbox, switch roles → "Discard unsaved changes?" confirm.
- ESCALATION GUARDS (live, must show verbatim messages):
- 3.7 Attempt to assign the **admin** role to a user → admin must NOT appear in the assign popover/bulk dropdown for this non-admin editor (UX mirror). If forced via API, server returns 403 `Only admins can assign the admin role.`
- 3.8 Attempt to edit the **admin** role's mappings on the Roles page → admin role shown locked; server rejects with 403 `Only admins can modify the admin role.`
- 3.9 Attempt to grant a custom role a permission the user_admin does NOT hold → unheld permissions render disabled; server rejects with 403 `Cannot grant permissions you do not hold: {perm}`.

## Section 4 — Designer persona (`uat_designer`)  [ROADMAP SC4]
- 4.1 Log in as uat_designer. Confirm full design surfaces: create/edit dashboards + widgets, Dynamic Views, Map Layers, Visualizations, Tables buttons present; widget gear + drag/resize active.
- 4.2 Confirm NO "User Management" and NO "Roles" nav items.
- 4.3 Confirm cannot reach Roles/Users by any nav affordance.

## Section 5 — Analyst persona (`uat_analyst`)  [ROADMAP SC5 — CORE VALUE REGRESSION]
- 5.1 Log in as uat_analyst. Clean read-only app: no create/edit/delete affordances anywhere (re-confirms 2.1).
- 5.2 CLICK-THROUGH EXPLORATION (core product value — must be fully intact): open the dashboard, click a chart element → entire dashboard filters to that slice; confirm filter chip appears; dismiss the chip → filter clears.
- 5.3 Drill-down across multiple widgets works.
- 5.4 Map interaction: draw a spatial filter / click a map feature (info popup) → other widgets filter; confirm the analyst-passthrough routes (materialize, info/query, top-values, wms) respond (no 403 on interaction).
- 5.5 Attempt a gated mutation (e.g., via a stale edit URL or dev tools) → server returns 403 PERMISSION_DENIED.

## Section 6 — Custom role persona (`uat_custom`)  [ROADMAP SC6]
- 6.1 As admin (or user_admin), assign the `uat_custom_role` (dashboards:view + widgets:configure) to uat_custom.
- 6.2 Log in as uat_custom. Confirm: can SEE the dashboard list and open dashboards; CANNOT create/edit/delete dashboards; widget gear/config IS available (widgets:configure held) — verify ONLY the exact composed subset of surfaces appears, nothing more.

## Section 7 — Last-admin protection (live)  [ROADMAP SC3 safeguard]
- 7.1 Ensure exactly one non-bootstrap admin exists (assign admin to a uat user as bootstrap admin, then) attempt to revoke admin from that last non-bootstrap admin → server returns 400 `Cannot revoke: this is the last admin. At least one non-bootstrap user must hold the admin role.`; chip remains.

## Section 8 — Audit spot-check (50 human-verify #4)  [ROADMAP SC6 / AUDIT-V18-01]
- 8.1 After performing assigns/revokes/mapping edits above, inspect server stdout for an OBS-01 JSON line: `{"...","event":"rbac_audit","actor":"...","action":"role_assigned",...,"before_json":"...","after_json":"..."}`.
- 8.2 Run the rbac_audit spot-check from repo root and confirm rows for the actions performed:
  `sqlite3 packages/server/data/kinetica.db "SELECT ts, actor, action, target FROM rbac_audit ORDER BY id DESC LIMIT 10;"`
  Confirm actor = the persona who performed each action, action ∈ {role_assigned, role_revoked, role_permissions_updated, role_created, role_deleted}, before/after present.

## Section 9 — OIDC bootstrap-warning targeted check (no full OIDC persona pass)
- 9.1 Boot the server with AUTH_MODE=oidc and APP_ADMIN_USERNAME unset/default → confirm boot log contains the `rbac_bootstrap_admin_warning` line (verbatim text in interfaces). Restore AUTH_MODE=password afterward. (Automated proxy: boot.rbacAdminWarning spec already green in 51-01; this is the live confirmation.)
- 9.2 (49 human-verify #4 — OIDC ReturnTo) Fold in here if OIDC env is up: navigate to Users, trigger re-auth, confirm return to Users page. If OIDC env is not available, mark `status: SKIPPED` with note "OIDC test accounts not provisioned — covered by App.spec ReturnTo unit tests; deferred per CONTEXT".

## Section 10 — Gaps block
Add an empty `gaps:` YAML block (mirror 24-01 format: id, severity, title, description, resolution) for any bug found during the walk → routed to a 51.x inline plan-revision.

Each ROADMAP SC2-SC6 must be traceable to at least one section. Add a short "## Traceability" footer mapping SC2→§1, SC3→§3+§7, SC4→§4, SC5→§5, SC6→§6+§8.
  </action>
  <verify>
    <automated>test -f /Users/rydelpereira/Documents/projects/kinetica_bi/.planning/phases/51-verification-live-uat/51-UAT.md && grep -qi "preconditions" /Users/rydelpereira/Documents/projects/kinetica_bi/.planning/phases/51-verification-live-uat/51-UAT.md && grep -c "status:" /Users/rydelpereira/Documents/projects/kinetica_bi/.planning/phases/51-verification-live-uat/51-UAT.md</automated>
  </verify>
  <done>51-UAT.md exists with Sections 0-10: preconditions, all 6 personas, embedded 48/49/50 human-verify items, the 4 verbatim guard/last-admin checks, the sqlite3 audit spot-check, OIDC boot-warning check, gaps block, and SC2-SC6 traceability. Includes the §2.4 light-mode theming pass. Every check item carries `status: PENDING`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live UAT walk-through checkpoint — collect persona attestations</name>
  <files>.planning/phases/51-verification-live-uat/51-UAT.md</files>
  <action>BLOCKING HUMAN CHECKPOINT. Present the 51-UAT.md checklist to the operator, have them walk all sections live against the running app, then record each per-item attestation back into 51-UAT.md (flip status PENDING→PASS/FAIL/SKIPPED, fill evidence, populate the gaps block). Do not auto-attest — wait for the operator. See what-built / how-to-verify / resume-signal below.</action>
  <what-built>The complete 51-UAT.md live checklist (Sections 0-10) covering all 5 personas + last-admin + escalation guards + audit spot-check + OIDC boot warning, with every queued 48/49/50 human-verify item embedded. The app is built, all automated gates passed in 51-01.</what-built>
  <how-to-verify>
1. Provision the Section 0 preconditions: create the 4 Kinetica test users (uat_useradmin, uat_designer, uat_analyst, uat_custom), ensure a dashboard with widgets (incl. a map widget with a spatial target) exists, confirm AUTH_MODE=password.
2. Start the app: `npm run dev` and `npm run dev:server` from the repo root.
3. Walk the checklist top to bottom, section by section. Role assignment to the test users is done THROUGH THE APP as the admin persona (Section 1) — that flow is part of the test.
4. For the escalation guards (§3.7-3.9) and last-admin (§7), confirm you see the EXACT verbatim messages listed in the checklist.
5. Run the audit spot-check (§8.2) and confirm rbac_audit rows match your actions.
6. For each item, report PASS or FAIL (with what you saw). For any failure, describe it so it can be logged as a gap → 51.x inline fix + re-walk.
7. If OIDC env is unavailable, §9.2 may be SKIPPED with note.
  </how-to-verify>
  <resume-signal>Provide per-section attestations (e.g. "Sections 1-6 all PASS; §7 PASS; §8 audit rows confirmed; §9.1 warning seen, §9.2 SKIPPED") or describe any failures. Then this task records them into 51-UAT.md (flip status fields, fill evidence, populate gaps block). If any FAIL, halt and report so the orchestrator spins a 51.x revision before 51-03 compiles.</resume-signal>
  <verify>
    <automated>grep -qE "status: (PASS|FAIL|SKIPPED)" /Users/rydelpereira/Documents/projects/kinetica_bi/.planning/phases/51-verification-live-uat/51-UAT.md && ! grep -q "status: PENDING" /Users/rydelpereira/Documents/projects/kinetica_bi/.planning/phases/51-verification-live-uat/51-UAT.md && echo OK</automated>
  </verify>
  <done>Every 51-UAT.md item has a resolved status (no PENDING remaining); operator attestations and any gaps recorded. Any FAIL halts for a 51.x revision before 51-03.</done>
</task>

</tasks>

<verification>
- 51-UAT.md exists with all 10 sections and PENDING→PASS/FAIL/SKIPPED status fields populated after the checkpoint.
- Every queued human-verify item from 48 (3), 49 (4), and 50 (4) appears in the checklist.
- The 4 verbatim guard/last-admin messages are present in the checklist text.
- Operator attestations and any gaps are recorded back into 51-UAT.md.
</verification>

<success_criteria>
- The operator has walked all 5 personas + last-admin + escalation guards live against the running app.
- Analyst click-through exploration confirmed intact (core-value regression).
- Audit rows spot-checked via sqlite3; OIDC boot warning confirmed.
- All attestations recorded; any bug captured as a gap routed to a 51.x revision (re-walked before close).
</success_criteria>

<output>
After the checkpoint resolves, create `.planning/phases/51-verification-live-uat/51-02-SUMMARY.md` summarizing the attestation outcome and listing any gaps raised.
</output>
