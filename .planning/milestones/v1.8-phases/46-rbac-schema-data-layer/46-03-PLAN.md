---
phase: 46-rbac-schema-data-layer
plan: 03
type: execute
wave: 3
depends_on: ["46-01", "46-02"]
files_modified:
  - packages/server/src/lib/rbacDb.ts
  - packages/server/src/index.ts
  - packages/server/.env.example
  - packages/server/tests/lib.rbacDb.spec.ts
  - packages/server/tests/boot.rbacAdminWarning.spec.ts
autonomous: true
requirements: [SCHEMA-V18-02, SCHEMA-V18-03]
must_haves:
  truths:
    - "getEffectivePermissions('admin') returns all 15 permissions regardless of DB state (bootstrap short-circuit fires before any DB lookup)"
    - "A username with zero user_roles rows resolves to exactly the analyst role's current permission set (default-analyst fallback, no lockout)"
    - "A user with multiple roles resolves to the union of all their roles' permissions"
    - "Username matching is case-insensitive (bootstrap comparison and user_roles lookup)"
    - "When AUTH_MODE=oidc and APP_ADMIN_USERNAME is unset/default 'admin', the boot log contains a structured warning about the OIDC IdP-claim mismatch risk"
  artifacts:
    - path: "packages/server/src/lib/rbacDb.ts"
      provides: "getEffectivePermissions, getEffectiveRoles, getEffectiveRolesAndPermissions, APP_ADMIN_USERNAME resolution, isBootstrapAdmin"
      exports: ["getEffectivePermissions", "getEffectiveRoles", "getEffectiveRolesAndPermissions"]
    - path: "packages/server/src/index.ts"
      provides: "Structured OIDC bootstrap-admin boot warning inside the authMode==='oidc' block"
      contains: "rbac_bootstrap_admin_warning"
    - path: "packages/server/tests/lib.rbacDb.spec.ts"
      provides: "Covers admin short-circuit, analyst fallback, single role, multi-role union, custom role, case-insensitivity"
    - path: "packages/server/tests/boot.rbacAdminWarning.spec.ts"
      provides: "Standalone createApp() boot test asserting console.warn emits rbac_bootstrap_admin_warning in oidc+default-admin mode, and is SILENT when APP_ADMIN_USERNAME is set or AUTH_MODE=password (SCHEMA-V18-03 / ROADMAP success criterion 4)"
  key_links:
    - from: "packages/server/src/lib/rbacDb.ts getEffectivePermissions"
      to: "user_roles JOIN role_permissions (Plan 46-02 tables)"
      via: "synchronous better-sqlite3 prepared SELECT keyed on lower(username)"
      pattern: "role_permissions"
    - from: "packages/server/src/index.ts (authMode oidc block)"
      to: "APP_ADMIN_USERNAME env resolution"
      via: "boot warning when oidc + default 'admin'"
      pattern: "rbac_bootstrap_admin_warning"
    - from: "packages/server/tests/boot.rbacAdminWarning.spec.ts"
      to: "packages/server/src/index.ts createApp() oidc warning"
      via: "createApp() with stubbed oidc env + console.warn spy asserting event rbac_bootstrap_admin_warning"
      pattern: "rbac_bootstrap_admin_warning"
---

<objective>
Create `packages/server/src/lib/rbacDb.ts` — the synchronous permission-resolution data layer: `getEffectivePermissions(username)` with the bootstrap-admin short-circuit, analyst default fallback, multi-role union, and case-insensitive matching; plus `getEffectiveRoles` and `getEffectiveRolesAndPermissions` (consumed by Phase 47 middleware and Phase 48 /me). Emit a structured OIDC bootstrap-admin boot warning in index.ts when `AUTH_MODE=oidc` and `APP_ADMIN_USERNAME` is unset/default, and prove it with a standalone boot spec. Document `APP_ADMIN_USERNAME` in `.env.example`.

Purpose: This is the per-request authorization primitive (SCHEMA-V18-02) and the OIDC-lockout safeguard (SCHEMA-V18-03). Roles are NOT cached in sessions — resolution is a synchronous SQLite read on every call so role changes take effect immediately (PITFALLS Pitfall 4, 10). The admin short-circuit means the bootstrap account can never be locked out (Pitfall 5).
Output: rbacDb.ts data layer + OIDC boot warning + a standalone test that proves the warning fires + .env.example entry + unit spec covering all resolution paths.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-rbac-schema-data-layer/46-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Consumes from Plan 46-01 (on disk): -->
From packages/server/src/lib/permissions.ts:
```typescript
export const ALL_PERMISSIONS: readonly Permission[];   // 15 strings — admin short-circuit return value
export type Permission = string-literal-union;
```

<!-- Consumes Plan 46-02 tables (on disk after wave 2): -->
roles(id, name, description, built_in, ...), role_permissions(role_id, permission), user_roles(username LOWERCASED, role_id)

<!-- Existing code contracts (verified this session): -->
- index.ts: `const authMode = (process.env.AUTH_MODE || "password")` read ONCE at createApp() top (line ~124).
  The oidc_boot structured log is inside `if (authMode === "oidc") { ... }` at lines ~169-188.
  Style to mirror exactly:
    console.log(JSON.stringify({ ts: new Date().toISOString(), level: "info", event: "oidc_boot", message: "...", ... }));
- index.ts imports db from "./db" (the module-level singleton). rbacDb.ts helpers should accept an injectable db
  for testability OR import the singleton; PREFER an injectable-with-default signature (see action) so the spec
  can pass an in-memory createDb() instance.
- env.ts is the FIRST import in index.ts — APP_ADMIN_USERNAME is read via process.env at call time inside rbacDb.ts;
  do NOT add any import above `import "./env"` in index.ts.
- createApp() is exported from src/index.ts and can be invoked directly in tests (boot.hardening.spec.ts does this);
  the bootstrap IIFE that wraps it only runs when NODE_ENV !== "test", so importing index.ts in a test does NOT
  auto-boot — the test calls createApp() itself.

<!-- Test scaffolding contract (CRITICAL — verified this session): -->
- ~106 server tests are PRE-EXISTING RED (TD-V16-TEST-ISOLATION; the shared OIDC mock diverges with
  "Issuer is not a constructor"). The new boot warning test MUST NOT depend on those broken mocks passing.
- boot.hardening.spec.ts is the canonical pattern for booting createApp() in AUTH_MODE=oidc from a test:
  it defines its OWN openid-client mock via `vi.hoisted(() => {...})` + `vi.mock("openid-client", ...)` at the
  top of the file (self-contained — does not import the shared broken mock), stubs the oidc env vars with
  vi.stubEnv, spies on console, calls createApp(), and parses the JSON log lines via a findEvent() helper.
  Copy that scaffold VERBATIM into the new spec so it runs standalone and green.

<!-- Downstream consumers (do NOT build here): -->
<!--   Phase 47 rbac.ts requirePermission: const perms = getEffectivePermissions(username); if (!perms.has(perm)) 403 -->
<!--   Phase 48 /me handler: const { roles, permissions } = getEffectiveRolesAndPermissions(username) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getEffectivePermissions + getEffectiveRoles + getEffectiveRolesAndPermissions (rbacDb.ts)</name>
  <files>packages/server/src/lib/rbacDb.ts, packages/server/tests/lib.rbacDb.spec.ts</files>
  <read_first>
    - packages/server/src/lib/permissions.ts (Plan 46-01) — import ALL_PERMISSIONS for the admin short-circuit
    - packages/server/src/lib/dynamicViewSql.ts OR packages/server/src/lib/viewNaming.ts — existing pure-lib style (named exports, no side effects)
    - packages/server/src/db.ts — how the db singleton is exported (line ~212: `export const db = createDb(...)`); rbacDb.ts will default to importing it
    - .planning/research/ARCHITECTURE.md § Q1 (the getEffectivePermissions logic: admin short-circuit → user_roles join → analyst fallback → union) + § Anti-Pattern 4 (admin short-circuit lives HERE, not in requireAuth)
    - .planning/phases/46-rbac-schema-data-layer/46-CONTEXT.md § Bootstrap admin (single APP_ADMIN_USERNAME, default "admin", case-insensitive, short-circuit before DB lookup) + § Default role mappings
    - .planning/research/PITFALLS.md Pitfall 4, 8, 10 (no role caching; analyst fallback for zero rows; per-request read)
  </read_first>
  <behavior>
    - Test: getEffectivePermissions("admin", db) returns a Set of all 15 permissions even on an EMPTY db (no roles seeded) — short-circuit fires before any query.
    - Test: getEffectivePermissions("ADMIN", db) (uppercase) also returns all 15 — bootstrap comparison is case-insensitive.
    - Test: respecting APP_ADMIN_USERNAME: with process.env.APP_ADMIN_USERNAME="alice@corp.com", getEffectivePermissions("Alice@Corp.com", db) returns all 15; getEffectivePermissions("admin", db) does NOT short-circuit (must come from DB). Restore env after.
    - Test: a username with zero user_roles rows → returns exactly the analyst role's current permission set from the DB (after seedRbac: { "dashboards:view" }). Assert the user is NOT locked out (set is non-empty, equals analyst mapping).
    - Test: a user assigned a single role (designer) → returns exactly designer's 8 permissions.
    - Test: a user assigned TWO roles (designer + user_admin) → returns the UNION (designer 8 ∪ user_admin 6, deduped).
    - Test: a user assigned a custom role (built_in=0) with an arbitrary subset → returns exactly that subset.
    - Test: case-insensitive user_roles lookup — assign a role to "bob" (stored lowercased), then getEffectivePermissions("BOB", db) returns bob's permissions.
    - Test: getEffectiveRoles returns the role-name array (e.g. ["designer","user_admin"]); for admin short-circuit returns ["admin"]; for zero-rows returns ["analyst"].
    - Test: getEffectiveRolesAndPermissions returns { roles: string[], permissions: string[] } consistent with the two helpers above.
  </behavior>
  <action>
    Create packages/server/src/lib/rbacDb.ts. Imports:
      import type Database from "better-sqlite3";
      import { db as defaultDb } from "../db";
      import { ALL_PERMISSIONS } from "./permissions";

    Implement and export:

    1. `export const getAppAdminUsername = (): string => (process.env.APP_ADMIN_USERNAME || "admin").trim();`
       Read at call time (NOT module-level) so the spec can mutate process.env between cases. Add comment:
       "Read at call time, not cached — APP_ADMIN_USERNAME default 'admin' (Kinetica superuser). Case-insensitive compare in isBootstrapAdmin."

    2. `export const isBootstrapAdmin = (username: string): boolean =>`
       `  username.trim().toLowerCase() === getAppAdminUsername().toLowerCase();`

    3. `export const getEffectivePermissions = (username: string, conn: Database.Database = defaultDb): Set<string> => { ... }`
       Logic, in this exact order (ARCHITECTURE Q1 + Anti-Pattern 4):
         a. if (isBootstrapAdmin(username)) return new Set(ALL_PERMISSIONS);   // short-circuit BEFORE any DB read
         b. const uname = username.trim().toLowerCase();
         c. Query (single prepared JOIN):
              SELECT DISTINCT rp.permission AS permission
              FROM user_roles ur
              JOIN role_permissions rp ON rp.role_id = ur.role_id
              WHERE ur.username = ?
            with param uname.
         d. if rows.length === 0 → analyst fallback: query the analyst role's CURRENT mappings from the DB
              SELECT rp.permission FROM role_permissions rp
              JOIN roles r ON r.id = rp.role_id WHERE r.name = 'analyst'
            and return that Set. (Use the live DB value — NOT the code constant — so an operator who edits the
            analyst role mappings sees the change reflected for unassigned users. Comment this rationale.)
         e. else return new Set(rows.map(r => r.permission)).
       Add comment: "No caching — synchronous per-request read so role changes take effect immediately (PITFALLS P4/P10). Roles are NOT stored in the session row."

    4. `export const getEffectiveRoles = (username: string, conn = defaultDb): string[] => { ... }`
         a. if (isBootstrapAdmin(username)) return ["admin"];
         b. SELECT r.name FROM user_roles ur JOIN roles r ON r.id = ur.role_id WHERE ur.username = ? (lowercased)
         c. if empty → return ["analyst"]; else return the names.

    5. `export const getEffectiveRolesAndPermissions = (username: string, conn = defaultDb): { roles: string[]; permissions: string[] } => ({`
         `  roles: getEffectiveRoles(username, conn),`
         `  permissions: [...getEffectivePermissions(username, conn)],`
       `});`
       (Phase 48 /me consumes this shape.)

    Create packages/server/tests/lib.rbacDb.spec.ts (vitest). For setup use `createDb(":memory:")` (which now runs seedRbac, so the 4 built-in roles + default mappings already exist). Helper to assign a role: look up role id by name, INSERT INTO user_roles (username, role_id) with the LOWERCASED username. For the custom-role test: INSERT a roles row (built_in=0) + role_permissions subset, then assign it. Save/restore process.env.APP_ADMIN_USERNAME in the APP_ADMIN_USERNAME test (afterEach or try/finally). Implement every test in <behavior>.
  </action>
  <verify>
    <automated>npm run test:server -- lib.rbacDb 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'export const getEffectivePermissions' packages/server/src/lib/rbacDb.ts`
    - `grep -q 'export const getEffectiveRoles' packages/server/src/lib/rbacDb.ts` and `grep -q 'export const getEffectiveRolesAndPermissions' packages/server/src/lib/rbacDb.ts`
    - `grep -q 'isBootstrapAdmin' packages/server/src/lib/rbacDb.ts` and `grep -q 'new Set(ALL_PERMISSIONS)' packages/server/src/lib/rbacDb.ts`
    - `grep -q 'toLowerCase()' packages/server/src/lib/rbacDb.ts` (case-insensitive matching)
    - `grep -q "name = 'analyst'\|name = ?" packages/server/src/lib/rbacDb.ts` (analyst fallback queries the live DB, not a code constant — verify the analyst fallback path reads role_permissions, not ALL_PERMISSIONS or DEFAULT_ROLE_MAPPINGS)
    - `grep -q 'APP_ADMIN_USERNAME' packages/server/src/lib/rbacDb.ts`
    - The admin short-circuit appears BEFORE any prepared statement in getEffectivePermissions (manual read: `isBootstrapAdmin` return is the first statement)
    - `npm run test:server -- lib.rbacDb` reports admin-short-circuit (incl. empty-db + uppercase + APP_ADMIN_USERNAME override), analyst-fallback, single-role, multi-role-union, custom-role, and case-insensitive-lookup tests all passing
    - `npx tsc -p packages/server/tsconfig.json --noEmit` exits 0
  </acceptance_criteria>
  <done>rbacDb.ts exports getEffectivePermissions/getEffectiveRoles/getEffectiveRolesAndPermissions with admin short-circuit (before any DB read), case-insensitive matching, live-DB analyst fallback, and multi-role union; spec proves every resolution path; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: OIDC bootstrap-admin boot warning + standalone boot test + .env.example documentation</name>
  <files>packages/server/src/index.ts, packages/server/tests/boot.rbacAdminWarning.spec.ts, packages/server/.env.example</files>
  <read_first>
    - packages/server/src/index.ts lines 116-229 — the authMode read (line ~124) and the `if (authMode === "oidc") { ... oidc_boot log ... }` block (lines ~169-188). The new warning goes INSIDE this oidc block, immediately after the oidc_boot console.log.
    - packages/server/src/lib/rbacDb.ts (Task 1) — import getAppAdminUsername for the default-value check
    - packages/server/tests/boot.hardening.spec.ts — COPY its scaffold VERBATIM: the `vi.hoisted(() => {...})` openid-client mock object, the `vi.mock("openid-client", () => ({ Issuer: ..., custom: ..., errors: ... }))` block, the `beforeEach`/`afterEach` (resetOidcClientForTests, vi.unstubAllEnvs, vi.restoreAllMocks), and the `findEvent(spy, event)` JSON-log parser. This spec self-mocks openid-client so it does NOT depend on the ~106 pre-existing-red shared OIDC mocks ("Issuer is not a constructor"); the new test MUST be runnable standalone and green.
    - .planning/phases/46-rbac-schema-data-layer/46-CONTEXT.md § Bootstrap admin ("OIDC + default 'admin' → warn and boot, NOT fail-fast"; mirror oidc_boot JSON style)
    - .planning/research/PITFALLS.md Pitfall 6 (exact warning intent: OIDC usernames come from IdP claims; set APP_ADMIN_USERNAME to the IdP username, else roles can only be assigned by users already granted admin)
    - packages/server/.env.example — current entries + formatting (to append APP_ADMIN_USERNAME with a comment)
  </read_first>
  <action>
    In packages/server/src/index.ts:
      - Add `import { getAppAdminUsername } from "./lib/rbacDb";` alongside the other imports (NOT above `import "./env"` line 3 — env must stay first).
      - INSIDE the existing `if (authMode === "oidc") { ... }` block, immediately AFTER the `console.log(JSON.stringify({ ... event: "oidc_boot" ... }))` statement, add:

        ```typescript
        // v1.8 RBAC (SCHEMA-V18-03): warn-and-boot when the bootstrap admin is still the
        // default "admin" in OIDC mode. OIDC usernames come from the IdP claim (post-regex),
        // which is rarely literally "admin" — so no OIDC user would short-circuit to admin and
        // roles could only be assigned by users already granted admin. NON-FATAL (analyst default
        // means nobody is locked out). Operators search: jq 'select(.event=="rbac_bootstrap_admin_warning")'.
        const appAdminUsername = getAppAdminUsername();
        if (appAdminUsername.toLowerCase() === "admin") {
          console.warn(
            JSON.stringify({
              ts: new Date().toISOString(),
              level: "warn",
              event: "rbac_bootstrap_admin_warning",
              message:
                "AUTH_MODE=oidc but APP_ADMIN_USERNAME is the default 'admin'. OIDC usernames come from the IdP claim and are rarely literally 'admin'. Set APP_ADMIN_USERNAME to the intended admin's OIDC username (post-regex claim value), or roles can only be assigned by a user already granted the admin role.",
              app_admin_username: appAdminUsername,
              auth_mode: authMode,
            })
          );
        }
        ```

      Do NOT make this fatal — it is a console.warn only; boot continues. Do NOT add it outside the oidc block (password mode default "admin" is correct and must not warn).

    Create packages/server/tests/boot.rbacAdminWarning.spec.ts (vitest). This is the ROADMAP success-criterion-4 (SCHEMA-V18-03) automated proof — it exercises the rbac_bootstrap_admin_warning block by booting createApp() directly. Build it as a STANDALONE spec (do NOT rely on the broken shared OIDC mocks):
      - Copy the `vi.hoisted(() => {...})` openid-client mock + `vi.mock("openid-client", ...)` block from boot.hardening.spec.ts VERBATIM (so Issuer.discover etc. are stubbed and the createApp() oidc path reaches the warning without a real IdP).
      - Import: `import { createApp } from "../src/index";`, `import { resetOidcClientForTests } from "../src/oidc";`, and the `db` singleton; reuse the `findEvent(spy, event)` helper from boot.hardening.spec.ts.
      - beforeEach: resetOidcClientForTests(); reset the Issuer.discover mock; clear sessions table (mirror boot.hardening beforeEach). afterEach: vi.unstubAllEnvs(); vi.unstubAllGlobals(); vi.restoreAllMocks().
      - In each test, set the FULL valid OIDC env via vi.stubEnv so validateOidcEnv() passes and createApp() reaches the oidc block:
          AUTH_MODE=oidc, AUTH_OIDC_ISSUER_URL=https://idp.example.com, AUTH_OIDC_CLIENT_ID=kinetica-bi,
          AUTH_OIDC_CLIENT_SECRET=secret, AUTH_OIDC_REDIRECT_URI=https://bi.example.com/cb
        (match the exact var names validateOidcEnv requires — confirm against oidc env validation while reading).
      - Test 1 (the SC4 proof): with APP_ADMIN_USERNAME unset (delete it / do not stub it, so it resolves to default "admin"), spy on console.warn, `await createApp()`, then assert `findEvent(warnSpy, "rbac_bootstrap_admin_warning")` is defined, its level is "warn", auth_mode is "oidc", app_admin_username is "admin", and message matches /APP_ADMIN_USERNAME/.
      - Test 2 (no false-positive): set APP_ADMIN_USERNAME="alice@corp.com" (a non-default), spy on console.warn, `await createApp()`, assert `findEvent(warnSpy, "rbac_bootstrap_admin_warning")` is undefined (warning suppressed when a real admin username is configured).
      - Test 3 (mode gate): AUTH_MODE=password (stub KINETICA_URL as boot.hardening Test 2 does), APP_ADMIN_USERNAME unset, spy on console.warn, `await createApp()`, assert the warning event is undefined (password-mode default "admin" must NOT warn).
      - Use a case-insensitive uppercase variant in Test 1 OR a separate assertion if convenient (APP_ADMIN_USERNAME="ADMIN" still warns) — optional, only if it does not complicate the spec.

    In packages/server/.env.example, append (matching the file's existing comment+KEY=value style):
      ```
      # v1.8 RBAC bootstrap admin. This username always resolves to ALL permissions and can never be
      # locked out (short-circuit before any DB lookup). Case-insensitive. Default "admin" is the
      # Kinetica built-in superuser (password mode). In OIDC mode, set this to the intended admin's
      # OIDC username (the post-regex claim value) — otherwise no OIDC user gets bootstrap admin and
      # the server logs an rbac_bootstrap_admin_warning at boot.
      APP_ADMIN_USERNAME=admin
      ```

    If a README "Environment Variables" table exists at packages/server/README.md or root README.md, add an APP_ADMIN_USERNAME row matching the table format; if no such table exists, skip (the .env.example entry is the source of truth) and note the skip in the SUMMARY.
  </action>
  <verify>
    <automated>npm run test:server -- boot.rbacAdminWarning 2>&1 | tail -25; echo "---"; npx tsc -p packages/server/tsconfig.json --noEmit && echo TSC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'rbac_bootstrap_admin_warning' packages/server/src/index.ts`
    - `grep -q 'getAppAdminUsername' packages/server/src/index.ts` and `grep -q 'from "./lib/rbacDb"' packages/server/src/index.ts`
    - The warning is inside the oidc block: `awk '/if \(authMode === "oidc"\)/,/^  }/' packages/server/src/index.ts | grep -q rbac_bootstrap_admin_warning` (warning text appears within the oidc conditional)
    - `head -3 packages/server/src/index.ts | grep -q 'import "./env"'` (env import still first; rbacDb import did not displace it)
    - `grep -q 'vi.mock("openid-client"' packages/server/tests/boot.rbacAdminWarning.spec.ts` (self-mocks openid-client; does NOT depend on the broken shared OIDC mocks)
    - `grep -q 'rbac_bootstrap_admin_warning' packages/server/tests/boot.rbacAdminWarning.spec.ts` and `grep -q 'createApp()' packages/server/tests/boot.rbacAdminWarning.spec.ts`
    - `npm run test:server -- boot.rbacAdminWarning` runs STANDALONE and reports Test 1 (oidc+default-admin → warning emitted), Test 2 (oidc+custom admin → no warning), Test 3 (password mode → no warning) all passing. This is the SCHEMA-V18-03 / ROADMAP success-criterion-4 automated proof.
    - `grep -q 'APP_ADMIN_USERNAME=admin' packages/server/.env.example` and `grep -q 'rbac_bootstrap_admin_warning' packages/server/.env.example` (doc references the warning)
    - `npx tsc -p packages/server/tsconfig.json --noEmit` exits 0 (TSC_OK printed)
    - Server vitest no worse than baseline: `npm run test:server` total passing >= 582 (no new failures from the index.ts edit; the ~106 pre-existing-red OIDC-mock tests are unchanged)
  </acceptance_criteria>
  <done>index.ts emits a structured rbac_bootstrap_admin_warning (console.warn, non-fatal) only when AUTH_MODE=oidc and APP_ADMIN_USERNAME resolves to default "admin"; boot.rbacAdminWarning.spec.ts proves the warning fires (oidc+default), is suppressed (oidc+custom admin, and password mode) by booting createApp() with self-contained openid-client mocks runnable standalone; env import order preserved; .env.example documents APP_ADMIN_USERNAME; tsc clean; vitest no worse than baseline.</done>
</task>

</tasks>

<verification>
- `npm run test:server -- lib.rbacDb` — all resolution-path specs green (ROADMAP success criteria 2 + 3)
- `npm run test:server -- boot.rbacAdminWarning` — standalone boot spec green; PROVES the OIDC bootstrap-admin warning fires on oidc+default-admin and is suppressed otherwise (ROADMAP success criterion 4 / SCHEMA-V18-03 automated proof)
- `npx tsc -p packages/server/tsconfig.json --noEmit` — clean
- `grep` confirms admin short-circuit precedes any DB read; warning is inside the oidc block; env import order preserved
- Server vitest total >= 582 (no worse than pre-v1.8 baseline; the ~106 pre-existing-red shared-OIDC-mock tests are not touched)
</verification>

<success_criteria>
- getEffectivePermissions('admin') returns all 15 permissions on any DB state (short-circuit before DB lookup) — ROADMAP criterion 2
- Zero-user_roles username resolves to the analyst role's live DB permission set (no lockout) — ROADMAP criterion 3
- Multi-role union + case-insensitive matching working and unit-tested
- AUTH_MODE=oidc + default APP_ADMIN_USERNAME emits a structured boot warning (non-fatal), proven by a standalone boot.rbacAdminWarning spec that boots createApp() — ROADMAP criterion 4
- APP_ADMIN_USERNAME documented in .env.example
- Zero new npm dependencies; tsc clean; server vitest no worse than baseline (582/689)
</success_criteria>

<output>
After completion, create `.planning/phases/46-rbac-schema-data-layer/46-03-SUMMARY.md`
</output>
</content>
</invoke>
