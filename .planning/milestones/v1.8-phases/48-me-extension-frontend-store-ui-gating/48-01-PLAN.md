---
phase: 48-me-extension-frontend-store-ui-gating
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/lib/permissions.ts
  - packages/web/src/lib/permissions.spec.ts
  - packages/web/src/api/client.ts
  - packages/web/src/store/auth.ts
  - packages/web/src/store/auth.spec.ts
  - packages/web/src/test/seedAuthStore.ts
  - packages/server/src/index.ts
autonomous: true
requirements: [GATE-V18-01]
must_haves:
  truths:
    - "GET /api/auth/me returns roles: string[] and permissions: string[] alongside the existing user.username and authMode fields"
    - "useAuthStore.user carries roles + permissions arrays after bootstrap"
    - "useAuthStore.getState().hasPermission('dashboards:edit') returns true when that string is in user.permissions, false otherwise, and false when user is null"
    - "The frontend permissions mirror exposes all 16 server permission strings byte-for-byte"
  artifacts:
    - path: "packages/web/src/lib/permissions.ts"
      provides: "Client-side PERMISSIONS constant mirroring server catalog + Permission type"
      contains: "DASHBOARDS_EDIT"
    - path: "packages/web/src/lib/permissions.spec.ts"
      provides: "Byte-parity spec independently hardcoding all 16 permission strings"
      contains: "dashboards:edit"
    - path: "packages/web/src/store/auth.ts"
      provides: "hasPermission selector + setPermissions action on useAuthStore; widened user state"
      contains: "hasPermission"
    - path: "packages/web/src/test/seedAuthStore.ts"
      provides: "seedDesignerStore() + seedAnalystStore() test helpers consumed by Plans 03 + 04 spec migrations"
      contains: "seedDesignerStore"
    - path: "packages/server/src/index.ts"
      provides: "/me handler returning roles + permissions via getEffectiveRolesAndPermissions"
      contains: "getEffectiveRolesAndPermissions"
  key_links:
    - from: "packages/server/src/index.ts /api/auth/me handler"
      to: "packages/server/src/lib/rbacDb.ts getEffectiveRolesAndPermissions"
      via: "synchronous call with loaded.session.username"
      pattern: "getEffectiveRolesAndPermissions\\("
    - from: "packages/web/src/store/auth.ts hasPermission"
      to: "get().user.permissions"
      via: "Set lookup reading current state (no stale closure)"
      pattern: "get\\(\\)\\.user"
    - from: "packages/web/src/api/client.ts fetchMe"
      to: "AuthUser roles/permissions"
      via: "widened AuthUser type threaded through MeResponse"
      pattern: "roles"
---

<objective>
Lay the RBAC foundation for the frontend: widen the `/api/auth/me` server response with roles + permissions (the ONLY server change in Phase 48), create the client-side permissions mirror module, and extend `useAuthStore` with `hasPermission(perm)` + a `setPermissions` action. Every downstream gating plan (02, 03, 04) depends on these contracts existing.

Purpose: GATE-V18-01 — the permission data must flow from server → store → `hasPermission` before any UI can gate on it. The permissions mirror must exist before any JSX references `PERMISSIONS.*`.
Output: A working `hasPermission` selector seeded from a real `/me` response, plus a byte-parity-verified permissions mirror module.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/48-me-extension-frontend-store-ui-gating/48-RESEARCH.md

<interfaces>
<!-- Contracts the executor needs. Extracted from live source — do NOT re-explore. -->

Server catalog (packages/server/src/lib/permissions.ts) — 16 entries, SCREAMING_SNAKE_CASE keys, noun:verb string values:
```
DASHBOARDS_VIEW: "dashboards:view"            WIDGETS_CONFIGURE: "widgets:configure"
DASHBOARDS_CREATE: "dashboards:create"        LAYERS_MANAGE: "layers:manage"
DASHBOARDS_EDIT: "dashboards:edit"            DYNAMIC_VIEWS_MANAGE: "dynamic_views:manage"
DASHBOARDS_DELETE: "dashboards:delete"        DATA_FILTERS_CONFIGURE: "data_filters:configure"
USERS_VIEW: "users:view"                      USERS_ASSIGN_ROLES: "users:assign_roles"
ROLES_VIEW: "roles:view"                      ROLES_MANAGE_PERMISSIONS: "roles:manage_permissions"
ROLES_CREATE_CUSTOM: "roles:create_custom"    ROLES_DELETE_CUSTOM: "roles:delete_custom"
AUDIT_VIEW: "audit:view"                       DATASETS_MANAGE: "datasets:manage"
```

Server rbacDb export (packages/server/src/lib/rbacDb.ts:144):
```typescript
export const getEffectiveRolesAndPermissions = (
  username: string,
  conn: Database.Database = defaultDb
): { roles: string[]; permissions: string[] }
```

Current server /me handler (packages/server/src/index.ts:327):
```typescript
app.get("/api/auth/me", (req, res) => {
  const loaded = loadSessionForRequest(req);
  if (!loaded) {
    clearSessionCookie(res);
    return res.status(401).json({ error: "Not authenticated.", code: "REAUTH_REQUIRED" });
  }
  return res.json({ user: { username: loaded.session.username }, authMode });
});
```

Current client types (packages/web/src/api/client.ts):
```typescript
export type AuthUser = { username: string };                    // line 89
export type MeResponse = { user: AuthUser; authMode: AuthMode }; // line 105
// fetchMe (line 124-131) returns { user: json.user as AuthUser, authMode: json.authMode as AuthMode }
```

Current auth store (packages/web/src/store/auth.ts):
```typescript
type AuthState = {
  status; user: AuthUser | null; error; reason; authMode;
  bootstrap; login; logout; markUnauthenticated;
};
// bootstrap path: set({ status: "authenticated", user: me.user, authMode: me.authMode, ... })
```

auth.spec.ts mocks "../api/client" with { fetchAuthConfig, fetchMe, login, logout } — adding a new import (lib/permissions) to auth.ts is fine; it is a separate module not in that mock.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create client permissions mirror module + byte-parity spec</name>
  <files>packages/web/src/lib/permissions.ts, packages/web/src/lib/permissions.spec.ts</files>
  <read_first>
    - packages/server/src/lib/permissions.ts (source of truth — the 16 strings)
    - packages/web/src/lib/dynamicViewName.ts (byte-parity mirror precedent: "INTENTIONALLY INLINED — parity enforced by spec" comment style)
    - .planning/phases/48-me-extension-frontend-store-ui-gating/48-RESEARCH.md § Pattern 5
  </read_first>
  <behavior>
    - Test: PERMISSIONS has exactly 16 keys.
    - Test: each of the 16 string values matches the independently-hardcoded expected string (dashboards:view, dashboards:create, dashboards:edit, dashboards:delete, widgets:configure, layers:manage, dynamic_views:manage, data_filters:configure, users:view, users:assign_roles, roles:view, roles:manage_permissions, roles:create_custom, roles:delete_custom, audit:view, datasets:manage).
    - Test: no duplicate string values across the catalog.
  </behavior>
  <action>
    Create `packages/web/src/lib/permissions.ts` as a pure module (zero imports, zero side effects). Copy the PERMISSIONS constant VERBATIM from packages/server/src/lib/permissions.ts (all 16 entries, SCREAMING_SNAKE_CASE keys → noun:verb values), marked `as const`. Add `export type Permission = (typeof PERMISSIONS)[keyof typeof PERMISSIONS];`. Add a header comment: "Frontend mirror of packages/server/src/lib/permissions.ts. BYTE-PARITY: values must match server exactly. Parity enforced by spec (independently hardcodes all strings). Pure module — zero imports."

    Create `packages/web/src/lib/permissions.spec.ts`. The spec MUST hardcode all 16 expected strings as literals (NEVER derive them from the source under test — mirrors the Phase 46 catalog spec rule). Assert: (1) Object.values(PERMISSIONS).length === 16; (2) each PERMISSIONS.KEY equals its hardcoded literal; (3) new Set(Object.values(PERMISSIONS)).size === 16 (no duplicates).
  </action>
  <verify>
    <automated>cd packages/web && npm run test -- --run src/lib/permissions.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ':' packages/web/src/lib/permissions.ts` shows the 16 string entries present
    - `grep -q 'as const' packages/web/src/lib/permissions.ts`
    - `grep -q 'export type Permission' packages/web/src/lib/permissions.ts`
    - `! grep -q "import" packages/web/src/lib/permissions.ts` (pure module, zero imports)
    - `grep -q 'datasets:manage' packages/web/src/lib/permissions.spec.ts` AND `grep -q 'roles:delete_custom' packages/web/src/lib/permissions.spec.ts` (strings hardcoded independently in spec)
    - permissions.spec.ts passes
  </acceptance_criteria>
  <done>permissions.ts exports a 16-entry PERMISSIONS const + Permission type; spec passes with all 16 strings independently hardcoded.</done>
</task>

<task type="auto">
  <name>Task 2: Extend server /me handler with roles + permissions</name>
  <files>packages/server/src/index.ts</files>
  <read_first>
    - packages/server/src/index.ts (lines ~315-340 — /me handler + nearby lib imports)
    - packages/server/src/lib/rbacDb.ts (getEffectiveRolesAndPermissions signature, line 144)
    - .planning/phases/48-me-extension-frontend-store-ui-gating/48-RESEARCH.md § Code Examples /me Server Extension
  </read_first>
  <action>
    In `packages/server/src/index.ts`: import `getEffectiveRolesAndPermissions` from `./lib/rbacDb` (place it alongside the existing rbacDb/lib imports — do NOT add any import above the `./env` first-import per STATE.md TD-03). In the `app.get("/api/auth/me", ...)` handler (~line 327), after the `if (!loaded)` 401 guard, call:
    `const { roles, permissions } = getEffectiveRolesAndPermissions(loaded.session.username);`
    Change the success response to:
    `return res.json({ user: { username: loaded.session.username, roles, permissions }, authMode });`
    This is the ONLY server change in Phase 48. The bootstrap-admin short-circuit and analyst fallback are already handled inside getEffectiveRolesAndPermissions (Phase 46) — do not duplicate that logic here.
  </action>
  <verify>
    <automated>cd packages/server && npm run build:server 2>&1 | tail -5 || npx tsc --noEmit -p tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "getEffectiveRolesAndPermissions" packages/server/src/index.ts`
    - `grep -A3 'auth/me' packages/server/src/index.ts | grep -q 'roles'` (handler returns roles)
    - Server TypeScript compiles with no new errors
    - Existing server suite (set-based gate) shows no NEW failures beyond the 14 known-flaky files: `cd packages/server && npm run test:server -- auth 2>&1 | tail -15`
  </acceptance_criteria>
  <done>/api/auth/me returns { user: { username, roles, permissions }, authMode }; getEffectiveRolesAndPermissions wired; server compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Widen AuthUser/MeResponse and extend useAuthStore with hasPermission + setPermissions</name>
  <files>packages/web/src/api/client.ts, packages/web/src/store/auth.ts, packages/web/src/store/auth.spec.ts</files>
  <read_first>
    - packages/web/src/api/client.ts (AuthUser line 89, MeResponse line 105, fetchMe lines 124-131)
    - packages/web/src/store/auth.ts (full file — AuthState, bootstrap, login, markUnauthenticated)
    - packages/web/src/store/auth.spec.ts (existing mock setup + bootstrap tests — extend, do not rewrite)
    - .planning/phases/48-me-extension-frontend-store-ui-gating/48-RESEARCH.md § Pattern 1, Pitfall 3 (stale closure)
  </read_first>
  <behavior>
    - Test: hasPermission(p) returns true when p is in user.permissions.
    - Test: hasPermission(p) returns false when p is not in user.permissions.
    - Test: hasPermission(p) returns false when user is null (unauthenticated).
    - Test: hasPermission reads CURRENT state — after setState updates user.permissions, a subsequent hasPermission call reflects the new array (no stale closure).
    - Test: setPermissions(roles, permissions) updates user.roles + user.permissions in place (preserving username); calling it when user is null is a safe no-op (state unchanged).
    - Test (existing bootstrap tests still pass): widening AuthUser does not break the latest-write-wins authMode tests.
  </behavior>
  <action>
    In `packages/web/src/api/client.ts`:
    - Widen `export type AuthUser = { username: string; roles: string[]; permissions: string[] };` (line 89).
    - In `fetchMe` (line 131), coalesce defensively so an un-upgraded server cannot crash the consumer:
      `return { user: { username: json.user.username, roles: json.user.roles ?? [], permissions: json.user.permissions ?? [] }, authMode: json.authMode as AuthMode };`
    - `MeResponse` stays `{ user: AuthUser; authMode: AuthMode }` (now widened transitively).

    In `packages/web/src/store/auth.ts`:
    - Add to AuthState: `hasPermission: (perm: string) => boolean;` and `setPermissions: (roles: string[], permissions: string[]) => void;`.
    - Implement using `get` (add `get` to the `create<AuthState>((set, get) => ({...}))` signature):
      `hasPermission: (perm) => new Set(get().user?.permissions ?? []).has(perm),`  — MUST read get().user, never a closed-over array (Pitfall 3).
      `setPermissions: (roles, permissions) => set((s) => (s.user ? { user: { ...s.user, roles, permissions } } : s)),` — no-op when user is null.
    - Do NOT add anything to a reset chain — permissions vanish when markUnauthenticated sets user=null (research Anti-Patterns).

    In `packages/web/src/store/auth.spec.ts`: add a new `describe("hasPermission selector + setPermissions")` block covering the behaviors above. Use `useAuthStore.setState({ status: "authenticated", user: { username: "t", roles: ["designer"], permissions: ["dashboards:edit"] } })` to seed; assert getState().hasPermission(...). Reset with `useAuthStore.setState({ user: null })` between cases. Keep the existing bootstrap describe blocks intact.
  </action>
  <verify>
    <automated>cd packages/web && npm run test -- --run src/store/auth.spec.ts src/api/client.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'roles: string\[\]; permissions: string\[\]' packages/web/src/api/client.ts` (AuthUser widened)
    - `grep -q 'roles ?? \[\]' packages/web/src/api/client.ts` OR `grep -q 'roles: json.user.roles' packages/web/src/api/client.ts` (defensive coalesce in fetchMe)
    - `grep -q 'hasPermission:' packages/web/src/store/auth.ts` AND `grep -q 'get().user' packages/web/src/store/auth.ts` (reads current state, not closure)
    - `grep -q 'setPermissions:' packages/web/src/store/auth.ts`
    - `grep -q 'hasPermission' packages/web/src/store/auth.spec.ts` (new test block)
    - auth.spec.ts AND client.spec.ts both pass (existing bootstrap/latest-write-wins tests still green)
  </acceptance_criteria>
  <done>AuthUser carries roles+permissions; hasPermission + setPermissions implemented reading current state; new + existing store/client specs pass.</done>
</task>

<task type="auto">
  <name>Task 4: Create seedAuthStore test helper (consumed by Plans 03 + 04)</name>
  <files>packages/web/src/test/seedAuthStore.ts</files>
  <read_first>
    - packages/web/src/store/auth.ts (setState shape — status/user; from Task 3)
    - packages/web/src/lib/permissions.ts (PERMISSIONS — from Task 1)
    - packages/web/__mocks__/zustand.ts (reset shim — confirms why seeding must happen inside beforeEach)
    - .planning/phases/48-me-extension-frontend-store-ui-gating/48-RESEARCH.md § Test Store Seeding Pattern, Pitfall 1
  </read_first>
  <action>
    Create `packages/web/src/test/seedAuthStore.ts` exporting two helpers (imported by the wave-3 spec migrations in Plans 03 + 04). Import useAuthStore from "../store/auth" and PERMISSIONS from "../lib/permissions".
    - `export function seedDesignerStore(): void` — `useAuthStore.setState({ status: "authenticated", user: { username: "testdesigner", roles: ["designer"], permissions: [PERMISSIONS.DASHBOARDS_VIEW, PERMISSIONS.DASHBOARDS_CREATE, PERMISSIONS.DASHBOARDS_EDIT, PERMISSIONS.DASHBOARDS_DELETE, PERMISSIONS.WIDGETS_CONFIGURE, PERMISSIONS.LAYERS_MANAGE, PERMISSIONS.DYNAMIC_VIEWS_MANAGE, PERMISSIONS.DATA_FILTERS_CONFIGURE, PERMISSIONS.DATASETS_MANAGE] } });`
    - `export function seedAnalystStore(): void` — `useAuthStore.setState({ status: "authenticated", user: { username: "testanalyst", roles: ["analyst"], permissions: [PERMISSIONS.DASHBOARDS_VIEW] } });`
    - `export function seedAdminStore(): void` — seeds permissions = Object.values(PERMISSIONS) (all 16; includes USERS_VIEW + ROLES_VIEW for Sidebar tests), roles: ["admin"].
    Use PERMISSIONS.* constants only — NO raw permission strings. Note in a header comment that callers must invoke these INSIDE beforeEach (after the zustand reset shim wipes user→null).
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit 2>&1 | grep -i "seedAuthStore" || echo "seedAuthStore typechecks clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'export function seedDesignerStore' packages/web/src/test/seedAuthStore.ts`
    - `grep -q 'export function seedAnalystStore' packages/web/src/test/seedAuthStore.ts`
    - `grep -q 'export function seedAdminStore' packages/web/src/test/seedAuthStore.ts`
    - `grep -q 'PERMISSIONS' packages/web/src/test/seedAuthStore.ts` AND `! grep -q '"dashboards:edit"' packages/web/src/test/seedAuthStore.ts` (constants only, no raw strings)
    - File typechecks (no tsc error referencing seedAuthStore)
  </acceptance_criteria>
  <done>seedAuthStore.ts exports seedDesignerStore/seedAnalystStore/seedAdminStore using PERMISSIONS constants; ready for Plans 03 + 04 spec migrations.</done>
</task>

</tasks>

<verification>
- Server: `cd packages/server && npm run test:server -- auth` — /me-touching tests show no NEW failures beyond the 14 known-flaky set.
- Frontend: `cd packages/web && npm run test -- --run src/lib/permissions.spec.ts src/store/auth.spec.ts src/api/client.spec.ts` all green.
- TypeScript: both packages compile (`npm run build` web, `npm run build:server`).
- No reset-chain entry added for permissions (grep auth.ts shows no new reset call).
</verification>

<success_criteria>
- GATE-V18-01 satisfied: /me returns roles+permissions; useAuthStore.hasPermission works against current state; 16-string mirror byte-parity-verified.
- Foundation contracts (PERMISSIONS mirror, AuthUser shape, hasPermission, setPermissions, seedAuthStore helper) exist for Plans 02-04 to consume.
- Zero new test regressions (frontend baseline 1492/1493 preserved; the 1 known-red DashboardsPage button-order test untouched).
</success_criteria>

<output>
After completion, create `.planning/phases/48-me-extension-frontend-store-ui-gating/48-01-SUMMARY.md`
</output>
