---
phase: 48-me-extension-frontend-store-ui-gating
plan: 03
type: execute
wave: 3
depends_on: ["48-01"]
files_modified:
  - packages/web/src/components/DashboardsPage.tsx
  - packages/web/src/components/charts/WidgetRenderer.tsx
  - packages/web/src/components/DashboardsPage.spec.tsx
  - packages/web/src/components/charts/LegendRenderer.tsx
  - packages/web/src/components/charts/LegendRenderer.spec.tsx
autonomous: true
requirements: [GATE-V18-02, GATE-V18-03, GATE-V18-04]
must_haves:
  truths:
    - "An analyst-seeded store renders DashboardsPage list view with no '+ New Dashboard', no per-row 'Edit', no per-row 'Delete' button"
    - "An analyst-seeded store renders an opened dashboard with no 'Dynamic Views', no 'Map Layers', no 'Visualizations', and no 'Tables' toolbar button"
    - "An analyst-seeded store renders widget cards with no gear (configure) button and no × (remove) button, and the grid drag/resize is inert"
    - "A designer-seeded store renders ALL of the above buttons and the grid is draggable/resizable"
  artifacts:
    - path: "packages/web/src/components/DashboardsPage.tsx"
      provides: "Permission-gated action-bar buttons + grid affordances using PERMISSIONS.* via hasPermission"
      contains: "hasPermission"
    - path: "packages/web/src/components/charts/WidgetRenderer.tsx"
      provides: "onConfigureWidget passed through only when configure is permitted (Reconfigure CTA gated)"
      contains: "onConfigureWidget"
  key_links:
    - from: "packages/web/src/components/DashboardsPage.tsx grid dragConfig/resizeConfig"
      to: "hasPermission(PERMISSIONS.DASHBOARDS_EDIT)"
      via: "enabled: canEdit on dragConfig + resizeConfig"
      pattern: "enabled: canEdit"
    - from: "packages/web/src/components/DashboardsPage.tsx gear button"
      to: "hasPermission(PERMISSIONS.WIDGETS_CONFIGURE)"
      via: "conditional render canConfigure && <gear>"
      pattern: "canConfigure"
    - from: "packages/web/src/components/DashboardsPage.spec.tsx beforeEach blocks"
      to: "seedDesignerStore()"
      via: "store seeding so gated buttons remain visible in pre-existing tests"
      pattern: "seedDesignerStore"
---

<objective>
Gate the DashboardsPage action bar, dashboard toolbar, and widget grid affordances behind `hasPermission`, and CO-LOCATE the spec migration that keeps the ~15 existing DashboardsPage tests green (they click "Dynamic Views"/"Map Layers"/"Visualizations" and assert button presence — without seeding designer permissions they would vanish from the DOM). This is the frontend analog of Phase 47's createAdminSession rule.

Purpose: GATE-V18-02 (action-bar buttons), GATE-V18-03 (widget grid add/delete/drag/resize), GATE-V18-04 (gear hidden → ChartConfigPanel unreachable, satisfying read-only intent by unreachability per CONTEXT.md).
Output: Hide-don't-disable gating on every DashboardsPage affordance in the inventory, the gear/Reconfigure config affordance gated, plus a seedAuthStore test helper consumed by every affected beforeEach.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/48-me-extension-frontend-store-ui-gating/48-RESEARCH.md

<interfaces>
<!-- Element → Permission gating table (VERBATIM from research, line refs verified against live DashboardsPage.tsx). -->
<!-- Note: actual line numbers shifted slightly from research; locate by element text/className, not line number. -->

| Element (locate by) | Permission | Action |
|---------------------|------------|--------|
| `+ New Dashboard` button (list view, className btn-primary) | PERMISSIONS.DASHBOARDS_CREATE | Hide |
| Per-row `Edit` button (ds-row, ghost-sm) | PERMISSIONS.DASHBOARDS_EDIT | Hide |
| Per-row `Delete` button (ds-row, ghost-sm ghost-danger) | PERMISSIONS.DASHBOARDS_DELETE | Hide |
| DashboardDetail `Edit` button (onEdit) | PERMISSIONS.DASHBOARDS_EDIT | Hide |
| Toolbar `Tables` button (setShowTableModal) | PERMISSIONS.DASHBOARDS_EDIT | Hide (table association = edit-tier; server gates assoc routes on dashboards:edit — orchestrator-locked Q1) |
| Toolbar `Dynamic Views` button (setShowDynamicViewsModal) | PERMISSIONS.DYNAMIC_VIEWS_MANAGE | Hide |
| Toolbar `Map Layers` button (setShowLayersModal) | PERMISSIONS.LAYERS_MANAGE | Hide |
| Toolbar `Visualizations` button (setShowVizModal) | PERMISSIONS.DASHBOARDS_EDIT | Hide (add-widget = edit) |
| Grid `dragConfig.enabled` | PERMISSIONS.DASHBOARDS_EDIT | enabled: canEdit |
| Grid `resizeConfig.enabled` | PERMISSIONS.DASHBOARDS_EDIT | enabled: canEdit |
| Widget gear button (widget-configure, faGear, setConfiguringWidget) | PERMISSIONS.WIDGETS_CONFIGURE | Hide |
| Widget × button (widget-remove, faXmark, handleRemoveWidget) | PERMISSIONS.DASHBOARDS_EDIT | Hide |
| WidgetRenderer onConfigureWidget prop (LegendRenderer orphan Reconfigure CTA) | PERMISSIONS.WIDGETS_CONFIGURE | pass prop only when canConfigure |

react-grid-layout v2.2.2: `dragConfig?: Partial<DragConfig>` / `resizeConfig?: Partial<ResizeConfig>` where DragConfig.enabled: boolean and ResizeConfig.enabled: boolean (verified in node_modules/react-grid-layout/dist types). Setting `enabled: false` is the supported, first-class inert API — NO per-item isDraggable fallback needed. (Resolves research Open Question 2.)

Store selectors (from Plan 48-01): `useAuthStore((s) => s.hasPermission)` and `PERMISSIONS` from `../lib/permissions`.

Grid + gear/xmark live INLINE in DashboardsPage.tsx (the DashboardOpen component body, ~lines 880-945), NOT in WidgetRenderer. The gear at className "widget-configure", the × at "widget-remove".

LegendRenderer (packages/web/src/components/charts/LegendRenderer.tsx:114-117) renders an orphan "Reconfigure" button calling `onConfigureWidget?.(widget)`. WidgetRenderer (line 226) threads `onConfigureWidget` to LegendRenderer. Gating approach: in DashboardsPage, pass `onConfigureWidget` to WidgetRenderer ONLY when canConfigure (else undefined → button still renders but is a no-op; cleaner: also hide via canConfigure). Per CONTEXT.md GATE-V18-04, configure affordances hidden entirely — pass undefined when !canConfigure so the no-op Reconfigure cannot open the panel.

Affected spec describe blocks (DashboardsPage.spec.tsx, 732 lines) that click/assert gated buttons and currently do NOT seed useAuthStore:
- "Phase 30 — spatial chips in FilterBar" (beforeEach ~line 393)
- "Phase 34 — Dynamic Views action-bar button" (beforeEach ~line 576) — asserts "Dynamic Views", "Map Layers" presence/order
- "Phase 35 — useDynamicViewMaterializeChain" (beforeEach ~line 665)
- "DashboardsPage — LIFE-V13-04" (beforeEach ~line 136)
Each opens a dashboard and relies on toolbar buttons being present. The 1 KNOWN-RED test (TD-V17-DASHPAGE-SPEC button-order) must remain red exactly as-is — do not fix, do not worsen.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate DashboardsPage.spec beforeEach blocks to seed designer permissions</name>
  <files>packages/web/src/components/DashboardsPage.spec.tsx</files>
  <read_first>
    - packages/web/src/components/DashboardsPage.spec.tsx (all beforeEach blocks + the describe headers listed in interfaces)
    - packages/web/src/store/auth.ts (setState shape — status/user)
    - packages/web/src/lib/permissions.ts (PERMISSIONS — from Plan 48-01)
    - packages/web/__mocks__/zustand.ts (reset shim — resets user to null before each test, the root cause of collapse)
    - packages/web/src/test/seedAuthStore.ts (seedDesignerStore/seedAnalystStore — created in Plan 48-01)
    - .planning/phases/48-me-extension-frontend-store-ui-gating/48-RESEARCH.md § Pitfall 1, Test Store Seeding Pattern
  </read_first>
  <action>
    The seedDesignerStore/seedAnalystStore helpers were created in Plan 48-01 at `packages/web/src/test/seedAuthStore.ts`. This task only CONSUMES them.

    In `packages/web/src/components/DashboardsPage.spec.tsx`: import seedDesignerStore from "../test/seedAuthStore". Add `seedDesignerStore();` as the FIRST line inside each beforeEach for the describe blocks listed in interfaces (Phase 30, Phase 34, Phase 35, LIFE-V13-04, and any other block whose tests open a dashboard or assert toolbar/action-bar buttons). The Zustand reset shim wipes user → null before each test, so seeding must be inside beforeEach (after the reset). Do NOT modify the known-red button-order test's assertions.
  </action>
  <verify>
    <automated>cd packages/web && npm run test -- --run src/components/DashboardsPage.spec.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'from "../test/seedAuthStore"' packages/web/src/components/DashboardsPage.spec.tsx` (consumes the Plan 01 helper)
    - `grep -c 'seedDesignerStore()' packages/web/src/components/DashboardsPage.spec.tsx` returns >= 4 (one per affected beforeEach)
    - DashboardsPage.spec.tsx: only the 1 pre-existing known-red button-order failure remains (no NEW failures). Confirm failure count unchanged from baseline.
  </acceptance_criteria>
  <done>Every affected DashboardsPage.spec beforeEach seeds designer permissions via the Plan 01 helper; suite shows only the 1 pre-existing red test.</done>
</task>

<task type="auto">
  <name>Task 2: Gate DashboardsPage action-bar + toolbar buttons + verify ResponsiveGridLayout inert</name>
  <files>packages/web/src/components/DashboardsPage.tsx</files>
  <read_first>
    - packages/web/src/components/DashboardsPage.tsx (list view ~136-190, DashboardDetail ~192-230, DashboardOpen toolbar ~710-730, grid ~880-945)
    - packages/web/src/store/auth.ts (hasPermission selector)
    - packages/web/src/lib/permissions.ts (PERMISSIONS)
    - node_modules/react-grid-layout/dist/types-BiXsdXr7.d.ts (DragConfig.enabled / ResizeConfig.enabled — confirm enabled:boolean propagates to inert)
  </read_first>
  <action>
    In `packages/web/src/components/DashboardsPage.tsx`, import PERMISSIONS from "../lib/permissions" and useAuthStore from "../store/auth".

    In each relevant component body (DashboardList, DashboardDetail, DashboardOpen), derive booleans from a single store subscription:
    `const hasPermission = useAuthStore((s) => s.hasPermission);` then `const canCreate = hasPermission(PERMISSIONS.DASHBOARDS_CREATE);` etc. (Claude's discretion: inline calls vs derived booleans — prefer derived booleans for the DashboardOpen body which reads many.)

    Apply the gating table from interfaces using hide semantics (`{canX && <button .../>}`):
    - List view: `+ New Dashboard` → canCreate; per-row `Edit` → canEdit; per-row `Delete` → canDelete.
    - DashboardDetail: `Edit` (onEdit) → canEdit.
    - DashboardOpen toolbar: `Tables` → canEdit; `Dynamic Views` → canDynamicViews (DYNAMIC_VIEWS_MANAGE); `Map Layers` → canLayers (LAYERS_MANAGE); `Visualizations` → canEdit.
    - Grid: change `dragConfig={{ enabled: true, handle: ".widget-drag-handle" }}` → `dragConfig={{ enabled: canEdit, handle: ".widget-drag-handle" }}` and `resizeConfig={{ enabled: true }}` → `resizeConfig={{ enabled: canEdit }}`.

    VERIFY STEP (orchestrator-locked Q2): Confirm `dragConfig.enabled: false` actually disables drag in this react-grid-layout build. Check node_modules/react-grid-layout/dist types (DragConfig.enabled is a first-class boolean — confirmed). If for any reason it does NOT propagate to inert behavior, fall back to per-item `static: true` / `isDraggable: false` in the `layouts` array. Document which path was taken in the SUMMARY.

    Keep the "No visualizations yet. Click 'Visualizations' to add one." empty-state text as-is, but the empty-state hint only makes sense when canEdit — leave the text rendering unchanged (it's informational; analysts simply see an empty dashboard).
  </action>
  <verify>
    <automated>cd packages/web && npm run test -- --run src/components/DashboardsPage.spec.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'PERMISSIONS' packages/web/src/components/DashboardsPage.tsx` AND `grep -q 'hasPermission' packages/web/src/components/DashboardsPage.tsx`
    - `grep -q 'enabled: canEdit' packages/web/src/components/DashboardsPage.tsx` (grid drag/resize gated) — OR if fallback taken, `grep -q 'isDraggable: false\|static: true' packages/web/src/components/DashboardsPage.tsx`
    - `grep -q 'canDynamicViews\|DYNAMIC_VIEWS_MANAGE' packages/web/src/components/DashboardsPage.tsx` AND `grep -q 'LAYERS_MANAGE\|canLayers' packages/web/src/components/DashboardsPage.tsx`
    - `! grep -q 'dragConfig={{ enabled: true' packages/web/src/components/DashboardsPage.tsx` (the hardcoded true is gone)
    - DashboardsPage.spec.tsx: only the 1 pre-existing known-red failure (designer-seeded tests still see all buttons)
  </acceptance_criteria>
  <done>All action-bar + toolbar + grid affordances gated with hide-don't-disable; grid inert path verified; designer-seeded specs green except the 1 known-red.</done>
</task>

<task type="auto">
  <name>Task 3: Gate widget gear/× + Reconfigure CTA; add analyst-hidden + designer-visible component tests</name>
  <files>packages/web/src/components/DashboardsPage.tsx, packages/web/src/components/charts/WidgetRenderer.tsx, packages/web/src/components/charts/LegendRenderer.tsx, packages/web/src/components/DashboardsPage.spec.tsx, packages/web/src/components/charts/LegendRenderer.spec.tsx</files>
  <read_first>
    - packages/web/src/components/DashboardsPage.tsx (widget-configure gear ~919, widget-remove × ~926, WidgetRenderer onConfigureWidget pass ~937)
    - packages/web/src/components/charts/WidgetRenderer.tsx (onConfigureWidget prop line 60, 226, threaded to LegendRenderer line 251)
    - packages/web/src/components/charts/LegendRenderer.tsx (Reconfigure button lines 110-117)
    - packages/web/src/components/DashboardsPage.spec.tsx (Phase 34 describe — openDashboard helper pattern to reuse)
    - .planning/phases/48-me-extension-frontend-store-ui-gating/48-CONTEXT.md § Config-panel access (GATE-V18-04 unreachability interpretation)
  </read_first>
  <action>
    In `packages/web/src/components/DashboardsPage.tsx` (DashboardOpen widget map, ~lines 915-940):
    - Derive `const canConfigure = hasPermission(PERMISSIONS.WIDGETS_CONFIGURE);` (alongside canEdit already added in Task 2).
    - Gear button (widget-configure): wrap `{canConfigure && <button className="widget-configure" .../>}`.
    - × button (widget-remove): wrap `{canEdit && <button className="widget-remove" .../>}`.
    - WidgetRenderer call: pass `onConfigureWidget={canConfigure ? (target) => setConfiguringWidget(target) : undefined}` so the LegendRenderer orphan-Reconfigure CTA cannot open the config panel for non-configurers. (GATE-V18-04 satisfied by unreachability — document in SUMMARY: ChartConfigPanel needs NO read-only mode because it is only reachable when canConfigure.)
    - In WidgetRenderer.tsx / LegendRenderer.tsx: NO change required if `onConfigureWidget` is undefined the Reconfigure button's onClick (`onConfigureWidget?.(widget)`) is already a safe no-op. OPTIONAL polish: in LegendRenderer, render the Reconfigure button only when `onConfigureWidget` is defined (`{onConfigureWidget && <button .../>}`) so it disappears for analysts — preferred for clean hide semantics. If you change LegendRenderer, update LegendRenderer.spec.tsx accordingly (co-located).

    In `packages/web/src/components/DashboardsPage.spec.tsx`: add a new describe block "Phase 48 — permission gating (GATE-V18-02/03/04)" with two contexts:
    - With `seedAnalystStore()` in beforeEach: open a dashboard, assert `screen.queryByRole("button", { name: "Dynamic Views" })` is null; same for "Map Layers", "Visualizations", "Tables", "+ New Dashboard"; assert no `.widget-configure` and no `.widget-remove` elements (use container.querySelector). Use `! present` style assertions.
    - With `seedDesignerStore()` in beforeEach: assert all those buttons ARE present.
    Reuse the existing openDashboard() helper pattern (render DashboardsPage, click Open, await listWidgets). For widget-card tests, mock listWidgets to return at least one widget so a widget-card renders.
  

EXPLICIT ANALYST REGRESSION (checker warning 3, orchestrator-mandated): add one test in the GATE-V18 describe block — seedAnalystStore(), render DashboardsPage, open a dashboard, and positively assert an ungated interaction works (e.g. a FilterBar chip dismiss button is clickable / fires removeFilter without error). This makes "analyst click-through exploration remains fully functional" an explicit assertion, not just a baseline-count proxy.
</action>
  <verify>
    <automated>cd packages/web && npm run test -- --run src/components/DashboardsPage.spec.tsx src/components/charts/LegendRenderer.spec.tsx src/components/charts/WidgetRenderer.spec.tsx 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'canConfigure' packages/web/src/components/DashboardsPage.tsx` AND `grep -q 'WIDGETS_CONFIGURE' packages/web/src/components/DashboardsPage.tsx`
    - `grep -q 'canConfigure ? ' packages/web/src/components/DashboardsPage.tsx` OR `grep -q 'canConfigure && ' packages/web/src/components/DashboardsPage.tsx` (gear + onConfigureWidget gated)
    - `grep -q 'GATE-V18' packages/web/src/components/DashboardsPage.spec.tsx` (new gating describe block)
    - `grep -q 'seedAnalystStore' packages/web/src/components/DashboardsPage.spec.tsx` (analyst-hidden assertions)
    - DashboardsPage.spec, LegendRenderer.spec, WidgetRenderer.spec: only the 1 pre-existing known-red DashboardsPage failure; no new regressions
  </acceptance_criteria>
  <done>Gear/×/Reconfigure config affordances gated (GATE-V18-03/04 by hide + unreachability); analyst-hidden + designer-visible component tests pass.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npm run test -- --run src/components/DashboardsPage.spec.tsx src/components/charts/LegendRenderer.spec.tsx src/components/charts/WidgetRenderer.spec.tsx` — only the 1 known-red DashboardsPage button-order failure (TD-V17-DASHPAGE-SPEC) remains.
- Full frontend suite: `cd packages/web && npm run test -- --run 2>&1 | tail -15` — baseline 1492/1493 preserved (no new failures, the 1 known-red unchanged).
- TypeScript compiles: `cd packages/web && npm run build 2>&1 | tail -5`.
</verification>

<success_criteria>
- GATE-V18-02: New/Edit/Delete/Map Layers/Dynamic Views (+ Tables/Visualizations) hidden for analyst, visible for designer.
- GATE-V18-03: add-widget (Visualizations) + widget × hidden, grid drag/resize inert without dashboards:edit.
- GATE-V18-04: gear + Reconfigure CTA hidden without widgets:configure → ChartConfigPanel unreachable (read-only intent satisfied by unreachability; documented).
- Spec migration co-located: every DashboardsPage test that asserts gated buttons seeds permissions; baseline preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/48-me-extension-frontend-store-ui-gating/48-03-SUMMARY.md`
</output>
