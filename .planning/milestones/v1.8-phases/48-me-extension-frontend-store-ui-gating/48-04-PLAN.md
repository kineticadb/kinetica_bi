---
phase: 48-me-extension-frontend-store-ui-gating
plan: 04
type: execute
wave: 3
depends_on: ["48-01"]
files_modified:
  - packages/web/src/components/Sidebar.tsx
  - packages/web/src/components/Sidebar.spec.tsx
  - packages/web/src/components/Topbar.tsx
  - packages/web/src/store/user.ts
files_deleted:
  - packages/web/src/store/user.ts
autonomous: true
requirements: [GATE-V18-05]
must_haves:
  truths:
    - "An analyst-seeded store renders the Sidebar with no 'User Management' and no 'Roles' nav item; the three existing items (Dashboards, Datasets, Settings) remain"
    - "A store seeded with users:view + roles:view renders 'User Management' and 'Roles' nav items"
    - "The Topbar shows the real authenticated username from useAuthStore plus role chips (e.g. 'analyst'); initials derive from the username"
    - "The vestigial useUserStore (hardcoded 'Data Engineer'/'Admin') no longer exists and has no consumers"
  artifacts:
    - path: "packages/web/src/components/Sidebar.tsx"
      provides: "Permission-gated 'User Management' (users:view) + 'Roles' (roles:view) nav items"
      contains: "users:view"
    - path: "packages/web/src/components/Topbar.tsx"
      provides: "Real identity from useAuthStore: username + role chips + derived initials"
      contains: "useAuthStore"
  key_links:
    - from: "packages/web/src/components/Sidebar.tsx nav rendering"
      to: "hasPermission(PERMISSIONS.USERS_VIEW) / hasPermission(PERMISSIONS.ROLES_VIEW)"
      via: "filter nav items by permission before render"
      pattern: "USERS_VIEW"
    - from: "packages/web/src/components/Topbar.tsx"
      to: "useAuthStore((s) => s.user)"
      via: "real username + roles replacing useUserStore"
      pattern: "useAuthStore"
    - from: "packages/web/src/components/Topbar.tsx"
      to: "deleted useUserStore"
      via: "import removed; store/user.ts deleted"
      pattern: "useUserStore"
---

<objective>
Gate the Sidebar nav items "User Management" (`users:view`) and "Roles" (`roles:view`), and replace the Topbar's vestigial hardcoded identity (`useUserStore` = "Data Engineer"/"Admin") with the real authenticated username + role chips from `useAuthStore`. Delete `store/user.ts` entirely (single consumer = Topbar, verified).

Purpose: GATE-V18-05 (Sidebar nav gating) + the CONTEXT.md "Topbar identity" decision (real username + role chips, delete dead code).
Output: Two new gated nav items, a Topbar driven by real identity, and zero remaining references to useUserStore.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/48-me-extension-frontend-store-ui-gating/48-RESEARCH.md

<interfaces>
<!-- Contracts extracted from live source — do NOT re-explore. -->

Current Sidebar nav array (packages/web/src/components/Sidebar.tsx:17-21) — 3 ungated items:
```typescript
const nav: NavItem[] = [
  { label: "Dashboards", key: "dashboards", icon: faTableColumns },
  { label: "Datasets", key: "datasets", icon: faDatabase },
  { label: "Settings", key: "settings", icon: faGear },
];
type NavItem = { label: string; key: string; icon: IconDefinition };
```
Sidebar is a presentational component receiving { activeKey, onSelect, collapsed, onToggleCollapse } props. Nav items render via `nav.map(...)` to <button> with `name={item.label}`.

NEW nav items to add (gated, hidden when permission absent):
- { label: "User Management", key: "users", icon: <pick a FA icon e.g. faUsers>, permission: PERMISSIONS.USERS_VIEW }
- { label: "Roles", key: "roles", icon: <pick a FA icon e.g. faUserShield>, permission: PERMISSIONS.ROLES_VIEW }
Existing 3 items have NO permission (all roles have dashboards:view; Datasets/Settings ungated in catalog) — keep ungated.

Sidebar.spec.tsx (verified): asserts each of the 3 items present via individual getByRole("button", { name: "..." }) — does NOT assert a total count. So existing tests stay green as long as the 3 items still render. New gated items are hidden by default (test store has no permissions unless seeded).

Topbar.tsx CURRENT (full):
```typescript
import { useUserStore } from "../store/user";          // line 3 — DELETE
const user = useUserStore((state) => state.user);       // line 7 — REPLACE
// renders <div className="avatar">{user.initials}</div>
```
useUserStore consumers (verified, exactly 2): store/user.ts (definition) + Topbar.tsx (lines 3, 7). NO spec imports useUserStore. App.spec.tsx mocks Topbar entirely (vi.mock("./components/Topbar")) so Topbar internals are not under test there. NO Topbar.spec.tsx exists.

useAuthStore.user shape (after Plan 48-01): `{ username: string; roles: string[]; permissions: string[] } | null`.
PERMISSIONS from "../lib/permissions".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate Sidebar nav items on users:view / roles:view + extend Sidebar.spec</name>
  <files>packages/web/src/components/Sidebar.tsx, packages/web/src/components/Sidebar.spec.tsx</files>
  <read_first>
    - packages/web/src/components/Sidebar.tsx (full file — nav array, NavItem type, nav.map render)
    - packages/web/src/components/Sidebar.spec.tsx (existing tests + render harness; how it passes props)
    - packages/web/src/store/auth.ts (hasPermission selector)
    - packages/web/src/lib/permissions.ts (PERMISSIONS)
    - packages/web/src/test/seedAuthStore.ts (seedAnalystStore/seedAdminStore — created in Plan 48-01)
  </read_first>
  <action>
    In `packages/web/src/components/Sidebar.tsx`:
    - Import useAuthStore from "../store/auth" and PERMISSIONS from "../lib/permissions"; import two FontAwesome icons for the new items (e.g. faUsers, faUserShield from @fortawesome/free-solid-svg-icons).
    - Widen NavItem: `type NavItem = { label: string; key: string; icon: IconDefinition; permission?: string };`.
    - Add the two new items to the `nav` array with their permission fields: User Management (key "users", PERMISSIONS.USERS_VIEW), Roles (key "roles", PERMISSIONS.ROLES_VIEW).
    - In the component, subscribe: `const hasPermission = useAuthStore((s) => s.hasPermission);`. Compute `const visibleNav = nav.filter((item) => !item.permission || hasPermission(item.permission));` and map over `visibleNav` instead of `nav`. (Ungated items always pass; gated items only when permission held.)

    In `packages/web/src/components/Sidebar.spec.tsx`:
    - Existing tests assert the 3 base items by name — they remain valid (base items have no permission so always render). Confirm they still pass.
    - Add a describe "Phase 48 — gated nav items (GATE-V18-05)":
      - Test (analyst): seed useAuthStore with user permissions = [DASHBOARDS_VIEW] (or call seedAnalystStore), render Sidebar, assert `screen.queryByRole("button", { name: "User Management" })` is null AND `queryByRole("button", { name: "Roles" })` is null, AND the 3 base items still present.
      - Test (admin/user_admin): seed useAuthStore.setState with permissions including USERS_VIEW + ROLES_VIEW, render, assert both "User Management" and "Roles" buttons present.
    - Import seedAnalystStore + seedAdminStore from "../test/seedAuthStore" (Plan 48-01). seedAdminStore seeds all 16 permissions incl. USERS_VIEW + ROLES_VIEW. Account for the zustand reset shim — seed inside beforeEach/per-test (after the shim resets user→null).
  </action>
  <verify>
    <automated>cd packages/web && npm run test -- --run src/components/Sidebar.spec.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'USERS_VIEW' packages/web/src/components/Sidebar.tsx` AND `grep -q 'ROLES_VIEW' packages/web/src/components/Sidebar.tsx`
    - `grep -q 'User Management' packages/web/src/components/Sidebar.tsx` AND `grep -q '"Roles"\|label: "Roles"' packages/web/src/components/Sidebar.tsx`
    - `grep -q 'hasPermission' packages/web/src/components/Sidebar.tsx` AND `grep -q 'filter(' packages/web/src/components/Sidebar.tsx` (permission-filtered nav)
    - `grep -q 'GATE-V18-05\|User Management' packages/web/src/components/Sidebar.spec.tsx` (new gated test block)
    - Sidebar.spec.tsx fully passes (all existing + new tests green)
  </acceptance_criteria>
  <done>Sidebar shows User Management + Roles only with users:view / roles:view; base 3 items unchanged; Sidebar.spec green with analyst-hidden + admin-visible tests.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Topbar identity with useAuthStore; delete useUserStore</name>
  <files>packages/web/src/components/Topbar.tsx, packages/web/src/store/user.ts (DELETE)</files>
  <read_first>
    - packages/web/src/components/Topbar.tsx (full file — useUserStore import line 3, usage line 7, avatar render)
    - packages/web/src/store/user.ts (vestigial store to delete)
    - packages/web/src/store/auth.ts (user shape: { username, roles, permissions } | null)
    - packages/web/src/App.spec.tsx (confirms Topbar is mocked there — Topbar change cannot break App.spec)
    - .planning/phases/48-me-extension-frontend-store-ui-gating/48-RESEARCH.md § Pattern 6 (Topbar Replacement), Pitfall 4 (deletion safety)
  </read_first>
  <action>
    In `packages/web/src/components/Topbar.tsx`:
    - Remove `import { useUserStore } from "../store/user";`.
    - Add `import { useAuthStore } from "../store/auth";`.
    - Replace `const user = useUserStore((state) => state.user);` with `const user = useAuthStore((s) => s.user);`.
    - Derive initials: `const initials = user ? user.username.slice(0, 2).toUpperCase() : "?";`.
    - Render the avatar with `{initials}` instead of `{user.initials}`.
    - Add the identity block per research Pattern 6: when `user` is present, render the username and role chips:
      `{user && (<div className="user-identity"><span className="username">{user.username}</span><div className="role-chips">{user.roles.map((r) => (<span key={r} className="role-chip">{r.replace("_", " ")}</span>))}</div></div>)}`
      Place it adjacent to the avatar inside `.top-actions`. (Analyst-default users show a single "analyst" chip — server /me resolves the effective role.)
    - Guard against an empty roles array defensively (map over [] renders nothing — fine).

    DELETE `packages/web/src/store/user.ts` entirely (e.g. `git rm` or remove the file). It has exactly two references (its own definition + Topbar) — Topbar is migrated above, so deletion is now safe (Pitfall 4).
  </action>
  <verify>
    <automated>cd packages/web && npm run test -- --run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `! test -f packages/web/src/store/user.ts` (file deleted)
    - `! grep -rq 'useUserStore' packages/web/src/` (zero references remain anywhere)
    - `grep -q 'useAuthStore' packages/web/src/components/Topbar.tsx` AND `grep -q 'user.username' packages/web/src/components/Topbar.tsx`
    - `grep -q 'role-chip' packages/web/src/components/Topbar.tsx` (role chips rendered)
    - `cd packages/web && npm run build 2>&1 | tail -3` compiles with no error (no dangling import to deleted store)
    - Full frontend suite: only the 1 pre-existing known-red DashboardsPage failure; no new regressions (App.spec mocks Topbar — unaffected)
  </acceptance_criteria>
  <done>Topbar shows real username + role chips + derived initials from useAuthStore; store/user.ts deleted; zero useUserStore references; suite baseline preserved.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npm run test -- --run src/components/Sidebar.spec.tsx` green.
- `! grep -rq 'useUserStore' packages/web/src/` (dead code gone).
- Full frontend suite: `cd packages/web && npm run test -- --run 2>&1 | tail -15` — baseline 1492/1493 preserved.
- `cd packages/web && npm run build` compiles.
</verification>

<success_criteria>
- GATE-V18-05: User Management + Roles nav items gated on users:view / roles:view; hidden for analyst, visible for user_admin/admin.
- Topbar identity decision satisfied: real username + role chips + derived initials; vestigial useUserStore deleted with no dead code.
- Zero new test regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/48-me-extension-frontend-store-ui-gating/48-04-SUMMARY.md`
</output>
