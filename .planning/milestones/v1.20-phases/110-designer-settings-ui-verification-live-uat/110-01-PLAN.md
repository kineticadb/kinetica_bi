---
phase: 110-designer-settings-ui-verification-live-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/DashboardSettingsModal.tsx
  - packages/web/src/components/DashboardSettingsModal.spec.tsx
  - packages/web/src/components/DashboardsPage.tsx
  - packages/web/src/components/DashboardsPage.spec.tsx
autonomous: true
requirements: [FSET-V120-01]

must_haves:
  truths:
    - "A user with DASHBOARDS_EDIT sees a 'Settings' button in the dashboard toolbar; a viewer without it does not."
    - "Clicking Settings opens a modal whose only control is a two-option segmented toggle (Top bar | Right panel) reflecting the dashboard's current filter_display_mode."
    - "Choosing the other option immediately PATCHes filter_display_mode (no Save/Cancel) and the dashboard surface switches top-bar<->panel LIVE with no page reload."
    - "A topbar/unset dashboard's toolbar and grid DOM are unchanged apart from the new canEdit-gated Settings button (byte-identical rendering path)."
  artifacts:
    - path: "packages/web/src/components/DashboardSettingsModal.tsx"
      provides: "Dashboard-settings modal with the filter-display-mode segmented toggle (save-on-change)"
      min_lines: 40
    - path: "packages/web/src/components/DashboardsPage.tsx"
      provides: "canEdit-gated Settings toolbar button + modal wiring + live state-lift of the updated dashboard"
      contains: "DashboardSettingsModal"
  key_links:
    - from: "DashboardsPage toolbar Settings button"
      to: "showSettingsModal state / DashboardSettingsModal"
      via: "onClick setShowSettingsModal(true)"
      pattern: "setShowSettingsModal\\(true\\)"
    - from: "DashboardSettingsModal segmented toggle"
      to: "api client updateDashboard({ filter_display_mode })"
      via: "onModeChange handler → updateDashboard → onDashboardUpdated → view.dashboard re-render → isPanelMode recompute"
      pattern: "updateDashboard\\(.*filter_display_mode"
---

<objective>
Add the designer-facing UI to choose a dashboard's filter display mode (top bar vs right panel), the FINAL feature of v1.20. A canEdit-gated "Settings" button on the dashboard toolbar opens a new small dashboard-settings modal whose first (and only) occupant is a two-option segmented toggle. Changing the toggle saves immediately via the Phase-106 `updateDashboard({ filter_display_mode })` and updates the in-memory dashboard so the Phase-107 `isPanelMode` XOR switch re-renders LIVE — no reload.

Purpose: closes FSET-V120-01. Persistence (Phase 106) and the panel/top-bar XOR rendering (Phase 107) already exist; this plan only adds the control that flips the persisted value and lifts it back into state.
Output: `DashboardSettingsModal.tsx` (+ spec) and the toolbar-button / modal / state-lift wiring in `DashboardsPage.tsx` (+ spec additions).
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-designer-settings-ui-verification-live-uat/110-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs; extracted from the codebase. Use directly — no exploration required. -->

From packages/web/src/api/client.ts (Phase 106 — already shipped):
```typescript
export type DashboardDto = {
  id: number;
  name: string;
  description?: string;
  filter_display_mode: "topbar" | "panel"; // always concrete on the wire (server coalesces NULL -> 'topbar')
  created_at: string;
  updated_at: string;
};
// PATCH /api/dashboards/:id — filter_display_mode is in the validated allow-list. Returns the updated DashboardDto.
export const updateDashboard = (id: number, attrs: Partial<Pick<DashboardDto, "name" | "description" | "filter_display_mode">>): Promise<DashboardDto>;
```

From packages/web/src/components/DashboardsPage.tsx:
- `const canEdit = hasPermission(PERMISSIONS.DASHBOARDS_EDIT);` (both at page level ~line 94 and inside DashboardOpen ~line 401).
- `DashboardOpen` is `({ dashboard, onBack }: { dashboard: DashboardDto; onBack: () => void })` (~line 393). `dashboard` is a PROP, not local state — the live update must be LIFTED to the parent.
- The parent renders it in the `view.mode === "open"` branch (~line 117-124): `<DashboardOpen dashboard={view.dashboard} onBack={() => setView({ mode: "list" })} />`. `setView` (~line 106) also has access to `setDashboards` (the list). Pattern for lifting an updated dashboard already exists in the `edit` branch (~line 153-156): `setDashboards((prev) => prev.map((d) => (d.id === updated.id ? updated : d)))`.
- Toolbar (~line 1098-1125): `<div className="dashboard-toolbar">` with `{canEdit && <button className="btn-primary btn-sm" onClick={() => setShow...Modal(true)}>Tables</button>}` ... then `<button className="ghost-sm" onClick={onBack}>Back</button>`. Modal-open state pattern: `const [showTableModal, setShowTableModal] = useState(false);` (~line 413-416).
- `const isPanelMode = dashboard.filter_display_mode === "panel";` (~line 619) — the Phase-107 XOR switch. It recomputes on every render, so a new `dashboard` prop with a changed mode flips the surface with no extra work.
- `useApiQuery(...[dashboard.id])` (~line 406-408): widget/table/view queries key off `dashboard.id` ONLY — updating the dashboard OBJECT (same id) does NOT refetch, so the live switch has no reload/flicker.

From packages/web/src/components/DashboardsPage.tsx TablePickerModal (~line 1539-1577) — modal chrome to mirror (all classes already in global.css):
```tsx
<div className="modal-overlay" onClick={onClose}>
  <div className="modal-content" onClick={(e) => e.stopPropagation()}>
    <div className="modal-header">
      <span className="modal-title">Manage Dashboard Tables</span>
      <button className="ghost-sm" onClick={onClose}>Close</button>
    </div>
    <div className="modal-body"> ... </div>
  </div>
</div>
```

From packages/web/src/components/charts/RadioGroupRenderer.tsx (~line 117-128) — the EXISTING segmented control markup (classes `.radiogroup--buttons`, `.radiogroup-button`, `.radiogroup-button--selected` all already in global.css:1225-1262; REUSE verbatim — do NOT invent new toggle classes):
```tsx
<div className="radiogroup--buttons">
  {options.map((opt) => (
    <button
      key={opt.value}
      type="button"
      className={`radiogroup-button${isSelected ? " radiogroup-button--selected" : ""}`}
      onClick={() => onChange(opt.value)}
    >{opt.label}</button>
  ))}
</div>
```

Form-field wrapper (global.css): `<div className="ds-field"><span className="ds-field-label">Filter display</span> ...control... </div>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create DashboardSettingsModal with the filter-display-mode segmented toggle (save-on-change)</name>
  <read_first>
    - packages/web/src/components/DashboardsPage.tsx lines 1539-1620 (TablePickerModal chrome to mirror) and the `<interfaces>` block above.
    - packages/web/src/components/charts/RadioGroupRenderer.tsx lines 100-140 (segmented-control markup + classes to reuse).
    - packages/web/src/styles/global.css lines 825-880 (.modal-overlay/.modal-content/.modal-header/.modal-title/.modal-body) and 1024-1062 (.ds-field/.ds-field-label) and 1221-1275 (.radiogroup--buttons segmented control).
    - CLAUDE.md UI conventions (reuse classes, tokens-only, no invented classNames).
  </read_first>
  <files>packages/web/src/components/DashboardSettingsModal.tsx, packages/web/src/components/DashboardSettingsModal.spec.tsx</files>
  <behavior>
    - Renders modal chrome (.modal-overlay + .modal-content + .modal-header with .modal-title "Dashboard Settings" + a ghost-sm Close button + .modal-body) mirroring TablePickerModal; clicking the overlay or Close calls `onClose`; clicking inside .modal-content does not.
    - Body contains ONE `.ds-field` labeled "Filter display" wrapping a `.radiogroup--buttons` segmented control with exactly two `.radiogroup-button` buttons: "Top bar" (value "topbar") and "Right panel" (value "panel").
    - The button matching the current `mode` prop carries `radiogroup-button--selected`; the other does not.
    - Clicking the NON-selected button calls `onModeChange(newMode)` exactly once with the other value; clicking the ALREADY-selected button is a no-op (does not call onModeChange). There is NO Save/Cancel footer (save-on-change).
    - While a mode change is in flight the buttons are disabled (prevent double-PATCH); on `onModeChange` rejection the modal surfaces the error via the shared toast store (does not crash / does not close).
  </behavior>
  <action>
    Create `DashboardSettingsModal.tsx` exporting `DashboardSettingsModal`. Props:
    `{ mode: "topbar" | "panel"; onModeChange: (mode: "topbar" | "panel") => Promise<void> | void; onClose: () => void }`.
    (Keep the API PATCH itself OUT of this component — the parent owns `updateDashboard` + the live state-lift; this component is a pure controlled toggle so it stays trivially testable. Pass `mode` from the parent's live dashboard so the selected segment reflects the current value.)
    Mirror the TablePickerModal chrome exactly (overlay+content+header+title+Close+body, all existing classes). Title: "Dashboard Settings". In the body render the single `.ds-field` ("Filter display") + the two-button `.radiogroup--buttons` segmented control (reuse `.radiogroup-button`/`--selected` per the RadioGroupRenderer pattern — do NOT add any new CSS class). Track an in-flight boolean; disable both buttons while awaiting `onModeChange`; wrap the call in try/catch and on failure call `useToastStore.getState().showToast(msg, "error")` (match the app's toast usage) and keep the modal open. Do NOT touch global.css (no new classes needed — verify by grep before finishing).
    Write `DashboardSettingsModal.spec.tsx` covering every bullet in <behavior> (render both segments + selected state for each mode; clicking the other segment fires onModeChange once with the right value; clicking the selected one is a no-op; overlay/Close call onClose; in-flight disables; rejected onModeChange keeps the modal open).
  </action>
  <acceptance_criteria>
    - `grep -n "radiogroup--buttons" packages/web/src/components/DashboardSettingsModal.tsx` matches (reuses the existing segmented control).
    - `grep -nE "modal-overlay|modal-content|modal-header|modal-title|modal-body" packages/web/src/components/DashboardSettingsModal.tsx` shows the mirrored chrome classes.
    - `git diff --name-only -- packages/web/src/styles/global.css` is EMPTY for this task (no new CSS classes introduced).
    - `cd packages/web && npx vitest run src/components/DashboardSettingsModal.spec.tsx` passes 100%.
    - `cd packages/web && npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/DashboardSettingsModal.spec.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>DashboardSettingsModal renders the two-option segmented toggle reflecting the current mode, fires onModeChange only on an actual change (no footer), disables while in flight, toasts on failure; spec green; zero new CSS classes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the canEdit-gated Settings button + modal + LIVE state-lift into DashboardsPage</name>
  <read_first>
    - packages/web/src/components/DashboardsPage.tsx lines 88-160 (parent view branches, `setView`, `setDashboards`, the edit-branch `onSaved` lift pattern), 393-440 (DashboardOpen props + queries keyed on dashboard.id), 413-416 (modal-open state pattern), 1098-1125 (toolbar), 1396-1448 (modal render block), and 619 (`isPanelMode`).
    - packages/web/src/components/DashboardsPage.spec.tsx lines 746-830 (Phase 34 toolbar-button test pattern) and 833-912 (Phase 48 permission-gating analyst-hidden vs designer-visible pattern) — mirror these for the Settings button.
    - Task 1's DashboardSettingsModal props.
  </read_first>
  <files>packages/web/src/components/DashboardsPage.tsx, packages/web/src/components/DashboardsPage.spec.tsx</files>
  <behavior>
    - Toolbar renders a `canEdit`-gated `<button className="btn-primary btn-sm">Settings</button>` placed AFTER "Visualizations" and BEFORE the "Back" ghost button; a user WITHOUT DASHBOARDS_EDIT (analyst) does NOT see it.
    - Clicking Settings opens DashboardSettingsModal with the current `dashboard.filter_display_mode`; Close/overlay closes it.
    - Toggling to the other mode calls `updateDashboard(dashboard.id, { filter_display_mode })` exactly once with the chosen value, then lifts the returned dashboard so the surface switches LIVE: `isPanelMode` recomputes and top-bar<->panel swap without a widget refetch/reload.
    - A topbar/unset dashboard's existing toolbar buttons + grid path are unchanged aside from the added Settings button (existing DashboardsPage.spec assertions stay green).
  </behavior>
  <action>
    In `DashboardsPage.tsx`:
    1. Add `showSettingsModal` state alongside the other `show*Modal` flags (~line 413-416).
    2. Add an optional `onDashboardUpdated?: (d: DashboardDto) => void` prop to `DashboardOpen` (~line 393-399); in the parent `view.mode === "open"` branch (~line 117-124) pass `onDashboardUpdated={(updated) => { setView({ mode: "open", dashboard: updated }); setDashboards((prev) => prev.map((d) => (d.id === updated.id ? updated : d))); }}` — reusing the existing edit-branch lift pattern. (Confirm the `view` union's "open" member carries `dashboard`; it does — line 120 reads `view.dashboard`.)
    3. Add the toolbar button after the Visualizations button (~line 1121): `{canEdit && (<button className="btn-primary btn-sm" onClick={() => setShowSettingsModal(true)}>Settings</button>)}`.
    4. In the modal render block (~line 1396+) add `{showSettingsModal && (<DashboardSettingsModal mode={dashboard.filter_display_mode} onModeChange={handleChangeDisplayMode} onClose={() => setShowSettingsModal(false)} />)}`.
    5. Add `const handleChangeDisplayMode = async (mode: "topbar" | "panel") => { const updated = await updateDashboard(dashboard.id, { filter_display_mode: mode }); onDashboardUpdated?.(updated); };` (let the error propagate so the modal's catch toasts). `updateDashboard` is already imported (line 8).
    Do NOT change the grid block, the `!isPanelMode` top-bar block, or any other toolbar button — topbar/unset must stay byte-identical apart from the new gated button.
    In `DashboardsPage.spec.tsx` add a describe block (mirror Phase 34 + Phase 48 patterns): Settings button visible for designer, hidden for analyst; clicking opens the modal; toggling the segment calls `updateDashboard` with `{ filter_display_mode: "panel" }` and the surface flips to panel mode live (assert the panel appears / top-bar bar disappears, or that the mocked updateDashboard was called once with the right args + onDashboardUpdated re-render occurs); a topbar dashboard's other toolbar buttons remain present.
  </action>
  <acceptance_criteria>
    - `grep -n "Settings" packages/web/src/components/DashboardsPage.tsx` shows the `btn-primary btn-sm` gated button; `grep -n "onDashboardUpdated" packages/web/src/components/DashboardsPage.tsx` shows the lift prop wired in both DashboardOpen and the parent open-branch.
    - `grep -nE "updateDashboard\\(dashboard.id, \\{ filter_display_mode" packages/web/src/components/DashboardsPage.tsx` matches the live PATCH call.
    - `cd packages/web && npx vitest run src/components/DashboardsPage.spec.tsx` passes 100% (new + all pre-existing assertions, including the Phase 48 analyst-hidden set).
    - `cd packages/web && npx tsc --noEmit` clean; `npx vitest run src/styles/theme-guard.spec.ts` green.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/DashboardsPage.spec.tsx src/styles/theme-guard.spec.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Settings button appears only for canEdit users; opening it and toggling the segment PATCHes filter_display_mode and flips the panel/top-bar surface live with no reload; topbar/unset toolbar otherwise unchanged; specs + theme-guard green.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx tsc --noEmit` clean.
- `cd packages/web && npx vitest run` 100% (0 failures; the modal + DashboardsPage specs green, no regressions).
- `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` green (no raw hex / no invented classes — this plan adds NO new CSS classes).
- `git diff --stat -- packages/web/src/styles/global.css` shows no change (segmented control + modal + field classes all reused).
</verification>

<success_criteria>
- A designer with DASHBOARDS_EDIT can open a Settings modal and pick Top bar / Right panel; the choice PATCHes immediately and the dashboard switches surface live with no reload.
- A viewer without DASHBOARDS_EDIT sees no Settings button and simply gets the designer's chosen mode.
- No new RBAC permission; no server change; no new npm package; no new CSS class.
- topbar/unset dashboards render byte-identically apart from the new gated button.
</success_criteria>

<output>
After completion, create `.planning/phases/110-designer-settings-ui-verification-live-uat/110-01-SUMMARY.md`.
</output>
