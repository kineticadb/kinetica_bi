---
phase: 109.2-wire-custom-panel-charts-filter-scope
plan: 02
type: execute
wave: 2
depends_on:
  - "109.2-01"
files_modified:
  - packages/web/src/components/charts/CalendarRenderer.tsx
  - packages/web/src/components/charts/CalendarConfigPanel.tsx
  - packages/web/src/components/WidgetCard.tsx
  - packages/web/src/components/charts/CalendarRenderer.spec.tsx
  - packages/web/src/components/charts/CalendarConfigPanel.spec.tsx
  - packages/web/src/components/WidgetCard.spec.tsx
autonomous: true
requirements:
  - FSCOPE-V120-05
user_setup: []

must_haves:
  truths:
    - "CalendarRenderer resolves its FROM target from filterCombinationStore.vizToHash (mirroring TimelineRenderer for the table path and AggregatedWidgetRenderer's dv-branch for the dv path) — so it applies its configured filter scope on the live read path."
    - "respondToFilters is fully gone: not in CalendarConfig type, not in DEFAULT_CALENDAR_CONFIG, not in the CalendarConfigPanel checkbox, not in CalendarRenderer's gating."
    - "A legacy calendar saved with respondToFilters:false still ignores all filters (migrated via coalesce to an empty allow-list → NOFILTER → base table)."
    - "The on-card WidgetFilterBadge reflects the TRUE post-migration scope for a legacy calendar (0-of-N when an active filter is from an excluded source), not a stale accept-all read."
    - "bar/pie/table + all other widget badge/render behavior unchanged; sole-materialize-trigger invariant holds."
  artifacts:
    - path: "packages/web/src/components/charts/CalendarRenderer.tsx"
      provides: "vizToHash-based FROM resolution; no respondToFilters; no filterViewStore READ"
      contains: "vizToHash"
    - path: "packages/web/src/components/charts/CalendarConfigPanel.tsx"
      provides: "respondToFilters removed from type/default/checkbox; FilterSelectionPanel value coalesced"
    - path: "packages/web/src/components/WidgetCard.tsx"
      provides: "calendar-only coalesce applied to WidgetFilterBadge cfg prop"
      contains: "coalesceCalendarFilterSelection"
  key_links:
    - from: "packages/web/src/components/charts/CalendarRenderer.tsx"
      to: "filterCombinationStore.vizToHash"
      via: "primitive vizKey selectors (viewName/expiresAt/materializing) + dv-branch getState"
      pattern: "vizToHash\\[vizKey\\]"
    - from: "packages/web/src/components/WidgetCard.tsx"
      to: "coalesceCalendarFilterSelection"
      via: "cfg = w.type === 'calendar' ? coalesceCalendarFilterSelection(...) : ..."
      pattern: "coalesceCalendarFilterSelection"
---

<objective>
Complete Calendar's migration to the v1.18 combination engine: swap CalendarRenderer's dead `filterViewStore`/`respondToFilters` read path to the `filterCombinationStore.vizToHash` pattern its siblings (Timeline/NumericLine, and AggregatedWidgetRenderer for dv) already use, retire `respondToFilters` end-to-end, and coalesce the on-card badge so it reflects true post-migration scope.

Purpose: With Plan 01 now binding calendar's `vizToHash`, Calendar can finally read the combination view and honor its filter scope (Success Criterion 1). Retiring `respondToFilters` removes provably-dead code (its `respondToFilters:true` path has been non-functional since Phase 91) and unifies calendar with `filterSelection`. The badge coalesce prevents the known "badge reads a legacy source → goes stale" bug class.
Output: CalendarRenderer read-path swap + respondToFilters deletion; CalendarConfigPanel type/default/checkbox deletion + display coalesce; WidgetCard badge coalesce; three updated specs.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109.2-wire-custom-panel-charts-into-filter-scope-engine-and-reverse-map/109.2-RESEARCH.md

<interfaces>
Pattern to mirror — TimelineRenderer.tsx:224-239 (table path, vizToHash consumer):
```typescript
const vizKey = `w:${widget.id}`;
const fvViewName = useFilterCombinationStore((s) => {
  const h = s.vizToHash[vizKey];
  return h && !h.endsWith(`:${NOFILTER_SENTINEL}`) ? (s.registry[h]?.viewName ?? "") : "";
});
const fvExpiresAt = useFilterCombinationStore((s) => {
  const h = s.vizToHash[vizKey];
  return h && !h.endsWith(`:${NOFILTER_SENTINEL}`) ? (s.registry[h]?.expiresAt ?? 0) : 0;
});
const fvMaterializing = useFilterCombinationStore((s) => {
  const h = s.vizToHash[vizKey];
  return h && !h.endsWith(`:${NOFILTER_SENTINEL}`) ? (s.registry[h]?.materializing ?? false) : false;
});
```
Pattern to mirror — WidgetRenderer.tsx AggregatedWidgetRenderer dv-branch:585-602 (dv path, prefer combo view, fall back to raw dv view):
```typescript
const dvComboHash = useFilterCombinationStore.getState().vizToHash[vizKey];
const dvComboEntry =
  dvComboHash && !dvComboHash.endsWith(`:${NOFILTER_SENTINEL}`)
    ? useFilterCombinationStore.getState().registry[dvComboHash]
    : undefined;
const dvSource = dvComboEntry?.viewName || dvViewName; // dvViewName = raw dv view (unchanged read)
```
Helper created in Plan 01 (packages/web/src/lib/coalesceCalendarFilterSelection.ts):
```typescript
export function coalesceCalendarFilterSelection(cfg: Record<string, unknown>): FilterSelectionConfig | undefined
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swap CalendarRenderer to vizToHash read path + delete respondToFilters gating + dead filterViewStore reads; update CalendarRenderer.spec</name>
  <files>packages/web/src/components/charts/CalendarRenderer.tsx, packages/web/src/components/charts/CalendarRenderer.spec.tsx</files>
  <read_first>
    - packages/web/src/components/charts/CalendarRenderer.tsx (respondToFilters local line 129; filterViewStore READ selectors lines 190-213; neutralize vars 215-226; the if(respondToFilters){...}else{...} FROM branch 285-329; fetch dep array 381-406; handleCellClick markMaterializing/markDvMaterializing WRITES 534-558)
    - packages/web/src/components/charts/TimelineRenderer.tsx:224-239 (table-path vizToHash selectors to mirror) + its import of useFilterCombinationStore + NOFILTER_SENTINEL
    - packages/web/src/components/charts/WidgetRenderer.tsx:585-602 (AggregatedWidgetRenderer dv-branch to mirror for the dv path)
    - .planning/phases/109.2-wire-custom-panel-charts-into-filter-scope-engine-and-reverse-map/109.2-RESEARCH.md § "Pattern 3" (exact delete/replace list) + § "Pitfall 1" (why the legacy path is dead — do NOT try to revive it)
    - packages/web/src/components/charts/CalendarRenderer.spec.tsx (filterViewStore mock 86-99; respondToFilters tests: Test 3/3a/4a/4a-on/5/9/19b/19c/37; the "no flicker" test ~1148)
  </read_first>
  <behavior>
    - Table-bound calendar: FROM target = combo view name from `vizToHash["w:<id>"]` (registry viewName) when present and not NOFILTER; otherwise base `schema.table`. Suspend while that combo entry is materializing; proactively clear on expiry.
    - Dv-bound calendar: FROM target = dv-combo view (from vizToHash) when present, else raw dvViewName; dv must be materialized (existing dvStatus gates unchanged).
    - A legacy respondToFilters:false calendar (Plan 01 coalesce → empty allow-list → NOFILTER, orchestrator sets vizToHash undefined) reads the base table = unchanged ignore-all behavior.
    - Cell-click drill still dispatches filters (setBulkFilters / addDvFilter) exactly as today; the markMaterializing/markDvMaterializing WRITE calls in handleCellClick are KEPT (Timeline-parity; harmless now).
    - No `respondToFilters` reference and no `useFilterViewStore` READ remain in the renderer.
  </behavior>
  <action>
1. Add imports: `import { useFilterCombinationStore } from "../../store/filterCombinationStore";` and `import { NOFILTER_SENTINEL } from "../../lib/stableComboHash";` (mirror TimelineRenderer).

2. Define `const vizKey = \`w:${widget.id}\`;` and replace the three table-path selectors (current lines 190-194 reading `useFilterViewStore((s) => s.views[tableId]...)`) with the vizToHash-based `fvViewName`/`fvExpiresAt`/`fvMaterializing` selectors from the <interfaces> TimelineRenderer block (verbatim, scoped to vizKey).

3. Delete the `respondToFilters` local (line 129), the dv-path `dvFilterEntry`/`dvFilterViewName`/`dvFilterMaterializing` filterViewStore READ selectors (lines 209-213), and the entire "neutralize when OFF" block `dataFilterVersion`/`dataFvViewName`/`dataFvExpiresAt`/`dataFvMaterializing`/`dataDvFilterViewName`/`dataDvFilterMaterializing` (lines 215-226).

4. Replace the whole `if (respondToFilters) { ... } else { ... }` FROM-resolution branch (lines 285-329) with a single unconditional resolution:
   - Table path (`dynamicViewId === undefined`): if `fvMaterializing` return; if `fvViewName && fvExpiresAt > 0 && Date.now() >= fvExpiresAt` then clear the combo entry (`const h = useFilterCombinationStore.getState().vizToHash[vizKey]; if (h) useFilterCombinationStore.getState().clearEntry(h);`) and return; then `fromTarget = fvViewName ? fvViewName : (effectiveSchema ? \`${effectiveSchema}.${effectiveTable}\` : effectiveTable);` (mirror TimelineRenderer's table-path resolution exactly).
   - Dv path (`dynamicViewId !== undefined`): keep the existing `dvStatus !== "materialized"` gate; resolve via the AggregatedWidgetRenderer dv-branch (WidgetRenderer.tsx:585-602) — prefer dv-combo view from vizToHash, fall back to raw `dvViewName`; `fromTarget = dvSource;` (guard: if no dvSource, keep existing not-materialized behavior).

5. Update the fetch effect dep array (lines 381-406): remove `respondToFilters`, the `dataFv*`/`dataDvFilter*` entries; add `fvViewName`, `fvExpiresAt`, `fvMaterializing`, and `vizKey`. Keep `dvViewName`, `dvStatus`, `customWhere`, `customMetricsConfigVersion`, `metricId`, and the config deps.

6. KEEP the `useFilterViewStore.getState().markMaterializing(...)` / `markDvMaterializing(...)` WRITE calls in `handleCellClick` (lines 534-558) exactly as-is (Timeline-parity per RESEARCH § Pattern 3). If nothing else references `useFilterViewStore` after removing the READ selectors, keep the import only if the getState() writes still need it (they do) — verify the import is still used solely by those two write calls.

7. Update `packages/web/src/components/charts/CalendarRenderer.spec.tsx`:
   - Add a `filterCombinationStore` selector-aware mock (mirror TimelineRenderer.spec.tsx's `mockVizToHash`/`mockRegistry` mock + `getState`), seeded per-test.
   - Replace the respondToFilters-based FROM tests (Test 3, 3a, 4a, 4a-on, 5, 9, 19c, 37) with vizToHash-based equivalents: (a) no combo binding → FROM = base table; (b) combo binding present (vizToHash set to a hash whose registry viewName = "kv_c123") → FROM = that view name; (c) legacy `respondToFilters:false` widget with no vizToHash binding → FROM = base table (ignore-all preserved); (d) materializing combo entry → runSql suspended; (e) dv path: dv-combo view preferred, else raw dvViewName.
   - Keep the cell-click drill tests (Test 19b and the drill/toggle tests) passing (handleCellClick unchanged).
   - Remove Test 19c ("respondToFilters in dep array") — the field no longer exists.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/CalendarRenderer.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "respondToFilters" packages/web/src/components/charts/CalendarRenderer.tsx` === 0.
    - `grep -n "useFilterViewStore((s)" packages/web/src/components/charts/CalendarRenderer.tsx` returns NO matches (no filterViewStore READ selectors remain); the ONLY remaining `useFilterViewStore` references are the `.getState().markMaterializing`/`markDvMaterializing` WRITE calls in handleCellClick.
    - `grep -n "vizToHash\[vizKey\]" packages/web/src/components/charts/CalendarRenderer.tsx` matches (combo-store read path present).
    - `npx vitest run src/components/charts/CalendarRenderer.spec.tsx` green, including a test proving a combo-bound calendar's SQL FROM is the combo view name AND a legacy respondToFilters:false (no binding) calendar's FROM is the base table.
    - `cd packages/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>CalendarRenderer reads the combination view via vizToHash, applies configured scope, has zero respondToFilters and zero filterViewStore READ references; spec proves combo-bound vs base-table FROM resolution and legacy ignore-all preservation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove respondToFilters from CalendarConfig type/default/checkbox + coalesce config-panel display + coalesce WidgetCard badge; update both specs</name>
  <files>packages/web/src/components/charts/CalendarConfigPanel.tsx, packages/web/src/components/WidgetCard.tsx, packages/web/src/components/charts/CalendarConfigPanel.spec.tsx, packages/web/src/components/WidgetCard.spec.tsx</files>
  <read_first>
    - packages/web/src/components/charts/CalendarConfigPanel.tsx (CalendarConfig type field line 68; DEFAULT_CALENDAR_CONFIG line 85; respondToFilters local line 154; checkbox block lines 662-672; FilterSelectionPanel value= line 727)
    - packages/web/src/components/WidgetCard.tsx (WidgetFilterBadge cfg prop lines 141-150; existing imports)
    - packages/web/src/lib/coalesceCalendarFilterSelection.ts (helper from Plan 01)
    - .planning/phases/109.2-wire-custom-panel-charts-into-filter-scope-engine-and-reverse-map/109.2-RESEARCH.md § "Pattern 2" call site 4 (badge — required) + call site 5 (config-panel display — recommended, in-scope per Open Q1) + § "Pitfall 4" (badge stale-read bug class)
    - packages/web/src/components/charts/CalendarConfigPanel.spec.tsx (respondToFilters tests 12-16, lines ~308-350)
    - packages/web/src/components/WidgetCard.spec.tsx (existing badge test setup)
  </read_first>
  <behavior>
    - CalendarConfig has no `respondToFilters` field; DEFAULT_CALENDAR_CONFIG has no `respondToFilters`; the "Respond to dashboard filters" checkbox is gone from the panel.
    - Opening a legacy calendar's config (respondToFilters:false, no filterSelection) shows the FilterSelectionPanel reflecting the coalesced resolved state (customize ON, all sources unchecked) rather than a misleading "accept all" — because value is `coalesceCalendarFilterSelection(cfg)`.
    - On-card WidgetFilterBadge for a legacy respondToFilters:false calendar with an active filter from an excluded source renders the "0 of N filters" indicator (badge cfg is coalesced to the empty allow-list), instead of rendering nothing.
    - bar/pie/table (and all non-calendar) badge cfg reads are unchanged (raw filterSelection).
  </behavior>
  <action>
1. In `CalendarConfigPanel.tsx`:
   - Remove the `respondToFilters?: boolean;` field from the `CalendarConfig` type (line 68).
   - Remove `respondToFilters: false,` from `DEFAULT_CALENDAR_CONFIG` (line 85).
   - Remove the `const respondToFilters = cfg.respondToFilters ?? false;` local (line 154).
   - Delete the entire "Respond to dashboard filters" checkbox `<label className="config-toggle">…</label>` block (lines 662-672).
   - Add `import { coalesceCalendarFilterSelection } from "../../lib/coalesceCalendarFilterSelection";` and change the FilterSelectionPanel `value` prop (line 727) from `cfg.filterSelection as FilterSelectionConfig | undefined` to `coalesceCalendarFilterSelection(cfg as Record<string, unknown>)`. (Display-only read; `onChange`/`patch` still writes a concrete filterSelection — cannot corrupt persisted config.)

2. In `WidgetCard.tsx`:
   - Add `import { coalesceCalendarFilterSelection } from "../lib/coalesceCalendarFilterSelection";`.
   - Change the `WidgetFilterBadge` `cfg=` prop (lines 143) so calendar widgets use the coalesce:
     `cfg={w.type === "calendar" ? coalesceCalendarFilterSelection((w.config as Record<string, unknown> | undefined) ?? {}) : ((w.config as Record<string, unknown> | undefined)?.filterSelection as FilterSelectionConfig | undefined)}`

3. Update `CalendarConfigPanel.spec.tsx`: remove Tests 12-16 (the respondToFilters checkbox/default tests, lines ~308-350). Add a test asserting the FilterSelectionPanel receives the coalesced value for a legacy `{ respondToFilters: false }` calendar (resolved allow-list empty) — e.g. assert the panel renders in "customize/allowlist" state, or (simpler) unit-assert via the panel's rendered controls. Keep all other tests green.

4. Update `WidgetCard.spec.tsx`: add a test that a calendar widget with `config: { tableId, respondToFilters: false }` and an active filter from an EXCLUDED source renders the WidgetFilterBadge "N of M" / restricted indicator (proving the coalesced cfg → empty allow-list → not-accept-all). Ensure non-calendar widget badge behavior is unchanged.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/CalendarConfigPanel.spec.tsx src/components/WidgetCard.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "respondToFilters" packages/web/src/components/charts/CalendarConfigPanel.tsx` === 0.
    - `grep -n "coalesceCalendarFilterSelection" packages/web/src/components/charts/CalendarConfigPanel.tsx` matches (FilterSelectionPanel value coalesced).
    - `grep -n "coalesceCalendarFilterSelection" packages/web/src/components/WidgetCard.tsx` matches AND the match is gated on `w.type === "calendar"` (calendar-only branch; non-calendar still reads raw filterSelection).
    - `npx vitest run src/components/charts/CalendarConfigPanel.spec.tsx src/components/WidgetCard.spec.tsx` green, including the new legacy-calendar badge test.
    - `cd packages/web && npx tsc --noEmit` exits 0 (removing the type field surfaces no other references — respondToFilters existed only in CalendarRenderer + CalendarConfigPanel).
  </acceptance_criteria>
  <done>respondToFilters is gone from the config type/default/checkbox; the config panel displays the coalesced scope; the on-card badge reflects true post-migration scope for legacy calendars; both specs green.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx tsc --noEmit` clean.
- `cd packages/web && npx vitest run` (default PARALLEL) 100% — full suite green (specs timer-clean).
- `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` green (no raw hex / invented classes introduced; this plan added no new CSS classes).
- Sole-materialize-trigger invariant: `grep -rE "materializeFilter|dropCombinationView" packages/web/src/components/` finds only pre-existing authorized call sites (CalendarRenderer imports neither).
- `grep -rn "respondToFilters" packages/web/src/` returns NO matches (fully retired across src, including specs).
</verification>

<success_criteria>
- Calendar honors its configured filter scope on the live read path (combo-bound vizToHash), verified by spec; legacy respondToFilters:false calendars still ignore all filters (empty allow-list migration).
- respondToFilters is fully removed from config type, default, config panel, and renderer.
- On-card badge reflects true post-migration scope for legacy calendars (no stale accept-all).
- bar/pie/table + all other widget behavior unchanged; full web suite + tsc + theme-guard green.
</success_criteria>

<output>
After completion, create `.planning/phases/109.2-wire-custom-panel-charts-into-filter-scope-engine-and-reverse-map/109.2-02-SUMMARY.md`
</output>
