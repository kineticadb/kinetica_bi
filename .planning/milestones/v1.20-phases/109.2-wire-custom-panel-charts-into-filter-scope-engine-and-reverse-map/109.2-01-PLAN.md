---
phase: 109.2-wire-custom-panel-charts-filter-scope
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/lib/coalesceCalendarFilterSelection.ts
  - packages/web/src/lib/coalesceCalendarFilterSelection.spec.ts
  - packages/web/src/hooks/useCombinationOrchestrator.ts
  - packages/web/src/lib/useReverseFilterMap.ts
  - packages/web/src/hooks/useCombinationOrchestrator.spec.ts
  - packages/web/src/lib/useReverseFilterMap.spec.tsx
autonomous: true
requirements:
  - FSCOPE-V120-05
user_setup: []

must_haves:
  truths:
    - "The orchestrator mints a scoped vizToHash binding for calendar/timeline/numericline widgets from their filterSelection (they are no longer skipped by NON_TRIGGER_TYPES)."
    - "The reverse-map (enumerateVizDescriptors) enumerates calendar/timeline/numericline so the panel's applies-to count + highlight include them."
    - "A legacy calendar persisted with respondToFilters:false resolves (via coalesce) to an EMPTY allow-list = respond-to-none = NOFILTER = unfiltered base, unchanged behavior."
    - "A brand-new calendar with no respondToFilters key AND no filterSelection resolves to accept-all (undefined), matching every other chart type."
    - "bar/pie/table + existing combination/dv/badge behavior is unchanged; sole-materialize-trigger invariant holds."
  artifacts:
    - path: "packages/web/src/lib/coalesceCalendarFilterSelection.ts"
      provides: "Pure key-presence migration coalesce for calendar filterSelection"
      contains: "hasOwnProperty"
    - path: "packages/web/src/hooks/useCombinationOrchestrator.ts"
      provides: "NON_TRIGGER_TYPES without calendar/timeline/numericline + calendar coalesce at 2 read sites"
    - path: "packages/web/src/lib/useReverseFilterMap.ts"
      provides: "NON_TRIGGER_TYPES without calendar/timeline/numericline + calendar coalesce in enumerateVizDescriptors"
  key_links:
    - from: "packages/web/src/hooks/useCombinationOrchestrator.ts"
      to: "coalesceCalendarFilterSelection"
      via: "w.type === 'calendar' ? coalesceCalendarFilterSelection(w.config) : ..."
      pattern: "coalesceCalendarFilterSelection"
    - from: "packages/web/src/lib/useReverseFilterMap.ts"
      to: "coalesceCalendarFilterSelection"
      via: "cfg: w.type === 'calendar' ? coalesceCalendarFilterSelection(cfg) : ..."
      pattern: "coalesceCalendarFilterSelection"
---

<objective>
Fix the filter-engine root cause: `NON_TRIGGER_TYPES` (in `useCombinationOrchestrator.ts` AND its hand-mirrored twin in `useReverseFilterMap.ts`) lists `"calendar"`, `"timeline"`, `"numericline"`, so the orchestrator never mints a scoped `vizToHash` binding for these three widget types and the reverse-map never enumerates them. Remove the three strings from BOTH sets (they MUST land together — a reverse-map/orchestrator mismatch is the exact reported bug), and add a pure `coalesceCalendarFilterSelection` helper threaded through the calendar read sites so a legacy `respondToFilters:false` calendar migrates to "respond to none" (empty allow-list) with unchanged behavior.

Purpose: Timeline/NumericLine are already `vizToHash` consumers (zero renderer changes needed) — they "just work" the moment the orchestrator populates their binding. This plan is the engine-side half; Plan 02 (wave 2) does Calendar's renderer read-path swap + respondToFilters retirement + badge, all of which depend on the coalesce helper and the orchestrator binding created here.
Output: New pure helper + 2 one-line `Set` edits + 3 coalesce call sites + updated engine specs.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109.2-wire-custom-panel-charts-into-filter-scope-engine-and-reverse-map/109.2-RESEARCH.md

<interfaces>
From packages/web/src/types/filterSelection.ts:
```typescript
export type FilterSelectionConfig = {
  sourceMode: "all" | "allowlist";
  allowedSourceWidgetIds: (number | string)[];
};
export const DEFAULT_FILTER_SELECTION: FilterSelectionConfig = { sourceMode: "all", allowedSourceWidgetIds: [] };
```

Orchestrator table loop (useCombinationOrchestrator.ts ~line 213), current raw read to replace:
```typescript
const cfg = w.config.filterSelection as FilterSelectionConfig | undefined;
```
Orchestrator dv loop (~line 293), current read (dvScopeDisabled short-circuits FIRST):
```typescript
const cfg = dvScopeDisabled ? undefined : (w.config.filterSelection as FilterSelectionConfig | undefined);
```
Reverse-map enumerateVizDescriptors widget loop (useReverseFilterMap.ts ~line 80), current read:
```typescript
cfg: cfg.filterSelection as FilterSelectionConfig | undefined,
```

`FILTER_PRODUCING_TYPES` (components/charts/filterSourceTypes.ts) ALREADY includes calendar/timeline/numericline — it is a SEPARATE, orthogonal set governing "is a filter SOURCE". Do NOT touch it. Editing `NON_TRIGGER_TYPES` cannot make these widgets "become sources" (bar/pie/table are in BOTH sets already with zero conflict).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create coalesceCalendarFilterSelection pure helper + unit spec</name>
  <files>packages/web/src/lib/coalesceCalendarFilterSelection.ts, packages/web/src/lib/coalesceCalendarFilterSelection.spec.ts</files>
  <read_first>
    - .planning/phases/109.2-wire-custom-panel-charts-into-filter-scope-engine-and-reverse-map/109.2-RESEARCH.md § "Pattern 2" (exact function body) + § "Pitfall 3" (key-presence discriminator rationale)
    - packages/web/src/types/filterSelection.ts (FilterSelectionConfig / DEFAULT_FILTER_SELECTION)
    - packages/web/src/lib/resolveFilterSet.ts (style reference — small pure lib, same shape)
  </read_first>
  <behavior>
    - filterSelection present (any value, e.g. {sourceMode:"allowlist",allowedSourceWidgetIds:[7]}) → returned as-is (explicit scope wins).
    - filterSelection absent, respondToFilters KEY present === true → undefined (accept-all / default scope).
    - filterSelection absent, respondToFilters KEY present === false → { sourceMode:"allowlist", allowedSourceWidgetIds: [] } (respond to none).
    - filterSelection absent, respondToFilters KEY absent entirely (brand-new post-migration widget OR pre-Phase-68 widget) → undefined (accept-all).
    - Discriminator is KEY PRESENCE via Object.prototype.hasOwnProperty.call, NOT value (a `false` value and an absent key must resolve differently).
  </behavior>
  <action>
Create `packages/web/src/lib/coalesceCalendarFilterSelection.ts` with EXACTLY this body (verbatim from RESEARCH § Pattern 2):

```typescript
import type { FilterSelectionConfig } from "../types/filterSelection";

// Legacy calendar migration (Phase 109.2 — FSCOPE-V120-05).
// - cfg.filterSelection present (any value) -> use it as-is (explicit scope always wins).
// - cfg.filterSelection absent AND "respondToFilters" key present in the persisted config ->
//     respondToFilters === true  -> undefined (accept-all / default scope)
//     respondToFilters === false -> { sourceMode: "allowlist", allowedSourceWidgetIds: [] } (respond to none)
// - cfg.filterSelection absent AND "respondToFilters" key absent (brand-new post-migration
//   calendar, or a pre-Phase-68 calendar) -> undefined (accept-all), matching every other
//   chart type's DEFAULT_FILTER_SELECTION convention.
export function coalesceCalendarFilterSelection(
  cfg: Record<string, unknown>,
): FilterSelectionConfig | undefined {
  if (cfg.filterSelection !== undefined) {
    return cfg.filterSelection as FilterSelectionConfig;
  }
  if (Object.prototype.hasOwnProperty.call(cfg, "respondToFilters")) {
    return cfg.respondToFilters === true
      ? undefined
      : { sourceMode: "allowlist", allowedSourceWidgetIds: [] };
  }
  return undefined;
}
```

Create `packages/web/src/lib/coalesceCalendarFilterSelection.spec.ts` covering all four cases in <behavior>, including the DISTINCT pair that is the regression guard for Pitfall 3:
  - `{ tableId: 1, respondToFilters: false }` → `{ sourceMode: "allowlist", allowedSourceWidgetIds: [] }`
  - `{ tableId: 1 }` (no respondToFilters key) → `undefined`
Also assert: `{ filterSelection: {sourceMode:"allowlist",allowedSourceWidgetIds:[7]} }` → returned unchanged; `{ respondToFilters: true }` → `undefined`.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/lib/coalesceCalendarFilterSelection.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "hasOwnProperty" packages/web/src/lib/coalesceCalendarFilterSelection.ts` matches (key-presence discriminator, not value).
    - `npx vitest run src/lib/coalesceCalendarFilterSelection.spec.ts` green, including BOTH the `respondToFilters:false → empty allowlist` and the `no-key → undefined` cases as separate assertions.
    - `cd packages/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Pure helper exists with key-presence semantics; all four migration cases proven by isolated unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove 3 types from BOTH NON_TRIGGER_TYPES sets + thread coalesce (orchestrator ×2, reverse-map ×1) + update engine specs</name>
  <files>packages/web/src/hooks/useCombinationOrchestrator.ts, packages/web/src/lib/useReverseFilterMap.ts, packages/web/src/hooks/useCombinationOrchestrator.spec.ts, packages/web/src/lib/useReverseFilterMap.spec.tsx</files>
  <read_first>
    - packages/web/src/hooks/useCombinationOrchestrator.ts (NON_TRIGGER_TYPES lines 70-81; table loop ~208-241; dv loop ~283-312)
    - packages/web/src/lib/useReverseFilterMap.ts (NON_TRIGGER_TYPES lines 32-41; enumerateVizDescriptors widget loop ~71-85)
    - .planning/phases/109.2-wire-custom-panel-charts-into-filter-scope-engine-and-reverse-map/109.2-RESEARCH.md § "Pattern 1" (exact diff + comment) + § "Pattern 2" call-site list + § "Pitfall 2" (both edits MUST be one atomic change) + § "Code Examples" (SC7/SC7b test shape)
    - packages/web/src/hooks/useCombinationOrchestrator.spec.ts (existing SC6 "records" test ~1140-1157 to mirror; makeWidget/bumpFilterVersion/advanceDebounce helpers; stableComboHash import)
    - packages/web/src/lib/useReverseFilterMap.spec.tsx (existing enumeration tests to mirror — INCLUDING the "excludes NON_TRIGGER_TYPES widgets ... calendar ..." test ~line 81-96 that this task must UPDATE, not delete)
    - packages/web/src/components/charts/filterSourceTypes.ts (confirm FILTER_PRODUCING_TYPES is the orthogonal source set — do NOT edit)
  </read_first>
  <behavior>
    - Orchestrator: a table-bound calendar/timeline/numericline widget with an active filter and default (accept-all) scope produces exactly ONE materialize POST and `vizToHash["w:<id>"]` === `stableComboHash("table", tableId, [filter])`.
    - Orchestrator: a legacy calendar (respondToFilters:false, no filterSelection) with an active filter resolves to NOFILTER → `vizToHash["w:<id>"]` is undefined and NO materialize POST fires (empty allow-list = respond to none).
    - Reverse-map: enumerateVizDescriptors emits a widget descriptor for calendar/timeline/numericline (previously skipped); a legacy respondToFilters:false calendar's descriptor carries the empty-allowlist cfg (so its applies-to set is empty), while a default-scope one carries accept-all.
    - bar/pie/table/records + dv-bound + dvScopeDisabled behavior UNCHANGED.
  </behavior>
  <action>
1. In BOTH `packages/web/src/hooks/useCombinationOrchestrator.ts` (lines 70-81) and `packages/web/src/lib/useReverseFilterMap.ts` (lines 32-41): remove the three strings `"timeline"`, `"numericline"`, `"calendar"` from the `NON_TRIGGER_TYPES` `Set` literal. Keep `"map"`, `"info-card"`, `"legend"`, `"datafilter"`, `"radiogroup"`. Add a one-line comment referencing Phase 109.2 / FSCOPE-V120-05 noting these three are combo-store CONSUMERS needing a scoped vizToHash binding and that this is orthogonal to `FILTER_PRODUCING_TYPES`. Keep the two sets as independently-edited literals (do NOT extract a shared module this phase — RESEARCH § Alternatives).

2. In `useCombinationOrchestrator.ts`, add `import { coalesceCalendarFilterSelection } from "../lib/coalesceCalendarFilterSelection";`. Apply at the TWO calendar read sites:
   - Table loop (~line 213): replace
     `const cfg = w.config.filterSelection as FilterSelectionConfig | undefined;`
     with
     `const cfg = w.type === "calendar" ? coalesceCalendarFilterSelection(w.config) : (w.config.filterSelection as FilterSelectionConfig | undefined);`
   - Dv loop (~line 293): keep the `dvScopeDisabled` short-circuit FIRST, then apply calendar coalesce only when NOT disabled:
     `const cfg = dvScopeDisabled ? undefined : (w.type === "calendar" ? coalesceCalendarFilterSelection(w.config) : (w.config.filterSelection as FilterSelectionConfig | undefined));`

3. In `useReverseFilterMap.ts`, add `import { coalesceCalendarFilterSelection } from "./coalesceCalendarFilterSelection";`. In `enumerateVizDescriptors`'s chart/table widget loop (~line 80), replace
   `cfg: cfg.filterSelection as FilterSelectionConfig | undefined,`
   with
   `cfg: w.type === "calendar" ? coalesceCalendarFilterSelection(cfg) : (cfg.filterSelection as FilterSelectionConfig | undefined),`
   (`cfg` is the local `w.config` Record already in scope.) Leave the map-layer loop untouched (layers have no respondToFilters).

4. Update `useCombinationOrchestrator.spec.ts`: add tests mirroring the existing "records" SC6 shape (RESEARCH § Code Examples SC7 / SC7b):
   - calendar (and timeline, and numericline) table-bound widget + active filter → ONE materializeFilter call; `vizToHash["w:<id>"]` === `stableComboHash("table", TABLE_A, [FILTER_A])`.
   - legacy `respondToFilters:false` calendar + active filter → `vizToHash["w:<id>"]` undefined AND materializeFilter NOT called.
   Confirm all pre-existing tests still pass (do not delete unrelated tests).

5. Update `useReverseFilterMap.spec.tsx`: add a test asserting enumerateVizDescriptors now emits descriptors for calendar/timeline/numericline widget types, and that a legacy `respondToFilters:false` calendar's descriptor `cfg` is the empty-allowlist config (so applies-to resolves to zero widgets for filters from other sources) while a default calendar's descriptor `cfg` is undefined (accept-all).

6. BLAST-RADIUS — UPDATE (do NOT delete) the EXISTING test in `useReverseFilterMap.spec.tsx` (~line 81-96): `"excludes NON_TRIGGER_TYPES widgets (e.g. map, info-card, legend, calendar) from vizKind:'widget'"`. It builds a `type:"calendar"` widget and asserts `vizs).toHaveLength(0)`; after removing `"calendar"` from NON_TRIGGER_TYPES that calendar widget IS enumerated, so the assertion breaks. Remove `calendar` from the excluded-widget list so only `map`/`info-card`/`legend` remain excluded (adjust the test name/comment accordingly), and add/adjust an assertion that a calendar widget IS now enumerated as `vizKind:'widget'` — do NOT just bump the expected length blindly. Keep the intent intact: map/info-card/legend still excluded; calendar/timeline/numericline now included.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/hooks/useCombinationOrchestrator.spec.ts src/lib/useReverseFilterMap.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '"(timeline|numericline|calendar)"' packages/web/src/hooks/useCombinationOrchestrator.ts packages/web/src/lib/useReverseFilterMap.ts` returns NO matches inside the NON_TRIGGER_TYPES set (the strings are gone from both files' set literals).
    - `grep -c "coalesceCalendarFilterSelection" packages/web/src/hooks/useCombinationOrchestrator.ts` >= 3 (import + 2 call sites); `grep -c "coalesceCalendarFilterSelection" packages/web/src/lib/useReverseFilterMap.ts` >= 2 (import + 1 call site).
    - `git diff --stat packages/web/src/components/charts/filterSourceTypes.ts` shows NO change (orthogonal source set untouched).
    - The pre-existing `"excludes NON_TRIGGER_TYPES widgets ..."` test in `useReverseFilterMap.spec.tsx` (~line 81) was UPDATED (not deleted): its excluded set no longer contains `calendar` (only map/info-card/legend), and it now asserts a calendar widget IS enumerated as `vizKind:'widget'`.
    - `npx vitest run src/hooks/useCombinationOrchestrator.spec.ts src/lib/useReverseFilterMap.spec.tsx` green, including the new calendar/timeline/numericline binding tests AND the legacy-respondToFilters:false → NOFILTER/undefined test.
    - TimelineRenderer.spec.tsx / NumericLineRenderer.spec.tsx require ZERO edits and remain green (they mock filterCombinationStore directly): `npx vitest run src/components/charts/TimelineRenderer.spec.tsx src/components/charts/NumericLineRenderer.spec.tsx` green with no diff to those two spec files.
    - `cd packages/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Both NON_TRIGGER_TYPES sets no longer skip the 3 types; calendar coalesce threads through both engine files; new + existing engine specs green (including the updated blast-radius exclusion test); timeline/numericline renderers + specs untouched and green.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx tsc --noEmit` clean.
- `cd packages/web && npx vitest run src/lib/coalesceCalendarFilterSelection.spec.ts src/hooks/useCombinationOrchestrator.spec.ts src/lib/useReverseFilterMap.spec.tsx src/components/charts/TimelineRenderer.spec.tsx src/components/charts/NumericLineRenderer.spec.tsx` all green.
- Sole-materialize-trigger invariant unchanged: `grep -rE "materializeFilter|dropCombinationView" packages/web/src/components/` finds only pre-existing authorized call sites (this plan added none in components/).
- `FILTER_PRODUCING_TYPES` (filterSourceTypes.ts) unchanged.
</verification>

<success_criteria>
- calendar/timeline/numericline are removed from NON_TRIGGER_TYPES in BOTH files (atomic, one plan).
- coalesceCalendarFilterSelection exists (key-presence semantics) and is imported+applied at all 3 engine call sites.
- Orchestrator mints vizToHash for these 3 types; legacy respondToFilters:false calendar → NOFILTER (respond-to-none); brand-new calendar → accept-all.
- Reverse-map enumerates the 3 types with the coalesced calendar cfg.
- Timeline/NumericLine renderers + their specs need no changes and stay green.
</success_criteria>

<output>
After completion, create `.planning/phases/109.2-wire-custom-panel-charts-into-filter-scope-engine-and-reverse-map/109.2-01-SUMMARY.md`
</output>
