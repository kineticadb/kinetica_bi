---
phase: 12-dashboard-layers-panel
plan: 07
type: execute
wave: 4
status: superseded
superseded_at: "2026-05-06"
superseded_reason: "Filtering will be reworked outside WMS QUERY; the QUERY-based filter-param fix this plan was scoped to deliver is no longer the approach. Spike scaffolding from commit 44e625f reverted in commit 8e60d2c. GAP-12-C3 in 12-VERIFICATION.md also marked superseded. No further action against this plan."
depends_on:
  - 12-05
  - 12-06
files_modified:
  - kinetica_bi/server/src/wmsFilterSpike.ts
  - kinetica_bi/server/package.json
  - .gitignore
  - .planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md
  - kinetica_bi/src/lib/wmsUrlBuilder.ts
  - kinetica_bi/src/lib/wmsUrlBuilder.spec.ts
  - .planning/phases/12-dashboard-layers-panel/12-VERIFICATION.md
autonomous: false
gap_closure: true
requirements:
  - LAYER-10-renderer-n-stack
  - LAYER-13-integration-verify
must_haves:
  truths:
    - "A standalone Kinetica WMS filter probe script exists at kinetica_bi/server/src/wmsFilterSpike.ts and runs via `npm run wms-filter-spike` from kinetica_bi/server, mirroring the Phase 11 wmsSpike.ts pattern"
    - "12-07-SPIKE-NOTES.md exists at .planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md and documents at least 4 GetMap probes (one per candidate param: QUERY, CQL_FILTER, FILTER, WHERE) against a small low-cardinality Kinetica table (NOT demo.nyctaxi), each entry recording HTTP status, response Content-Type, response byte length, and a one-line note on whether the tile output differed from the no-filter baseline"
    - "12-07-SPIKE-NOTES.md ends with a `CONCLUSION:` line stating either (a) which param name + value format produced a verifiable byte-level tile difference vs. baseline (and the wmsUrlBuilder.ts FILTER_PARAM constant to use), or (b) `CONCLUSION: BLOCKED — <reason>` if no probe produced a difference"
    - "If the spike CONCLUSION is non-blocked, kinetica_bi/src/lib/wmsUrlBuilder.ts uses the param name from the CONCLUSION line as the FILTER_PARAM constant value (replacing the current hardcoded `\"QUERY\"`); a corresponding constant rename + value-format adjustment is applied if the CONCLUSION specifies one"
    - "kinetica_bi/src/lib/wmsUrlBuilder.spec.ts asserts the new FILTER_PARAM constant is emitted in buildWmsParams output when whereClause is non-empty (and is NOT emitted when whereClause is empty/whitespace per existing AP-3 lock)"
    - "The full vitest suite in kinetica_bi (frontend + server) exits 0 after wmsUrlBuilder.ts changes — no regressions in the 43+ existing wmsUrlBuilder tests"
    - "12-VERIFICATION.md frontmatter `criteria_status.criterion_3` is updated to `PASS` (or to `BLOCKED` with a documented reason if Wave 1 concluded BLOCKED), and `overall_status` is recomputed accordingly"
    - "12-VERIFICATION.md gains a `## GAP-12-C3 Closed` (or `## GAP-12-C3 Re-Verified — Still Open`) section documenting the re-verification walkthrough: the table used (with cardinality), the column filtered, the filterVersion bump observed, the before/after tile difference observed, and a Network-tab confirmation of the new param name in the WMS GetMap request"
    - "GAP-12-C3 closure does NOT touch any UI files (UI is already GREEN per 12-VERIFICATION.md); the only source-code edit is kinetica_bi/src/lib/wmsUrlBuilder.ts and its spec"
    - "Plans 12-01 through 12-06 are NOT modified by this gap-closure plan"
  artifacts:
    - path: "kinetica_bi/server/src/wmsFilterSpike.ts"
      provides: "Standalone CLI probe script — issues 4+ GetMap requests (QUERY, CQL_FILTER, FILTER, WHERE variants) against a low-cardinality Kinetica table; logs status, content-type, byte length, and byte-equality vs. baseline for each variant"
      contains: "REQUEST=GetMap"
    - path: ".planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md"
      provides: "Probe results table + CONCLUSION line locking the param name + value format for Wave 2"
      contains: "CONCLUSION:"
    - path: "kinetica_bi/src/lib/wmsUrlBuilder.ts"
      provides: "FILTER_PARAM constant updated per SPIKE-NOTES CONCLUSION; AP-3 + M-02 locks preserved"
      contains: "FILTER_PARAM"
    - path: "kinetica_bi/src/lib/wmsUrlBuilder.spec.ts"
      provides: "Test asserting the corrected FILTER_PARAM is emitted on non-empty whereClause; existing 43+ tests unchanged"
      contains: "FILTER_PARAM"
    - path: ".planning/phases/12-dashboard-layers-panel/12-VERIFICATION.md"
      provides: "Frontmatter criterion_3 flipped from RED to PASS (or documented as still BLOCKED with reason); appended `## GAP-12-C3 Closed` section"
      contains: "GAP-12-C3 Closed"
  key_links:
    - from: "kinetica_bi/server/src/wmsFilterSpike.ts"
      to: ".planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md"
      via: "Spike runner stdout — operator records HTTP status + byte length per probe in the notes file; CONCLUSION line locks the param choice"
      pattern: "wmsFilterSpike.*12-07-SPIKE-NOTES"
    - from: ".planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md"
      to: "kinetica_bi/src/lib/wmsUrlBuilder.ts"
      via: "CONCLUSION: line dictates the FILTER_PARAM constant value — Task 2 reads the conclusion and applies it verbatim"
      pattern: "CONCLUSION.*FILTER_PARAM"
    - from: "kinetica_bi/src/lib/wmsUrlBuilder.ts"
      to: "kinetica_bi/src/components/charts/MapChartRenderer.tsx (NOT modified — consumer)"
      via: "Existing import; renderer's per-layer source.updateParams() call now emits the corrected param name automatically"
      pattern: "buildWmsParams.*updateParams"
    - from: "kinetica_bi/src/lib/wmsUrlBuilder.ts"
      to: ".planning/phases/12-dashboard-layers-panel/12-VERIFICATION.md"
      via: "Re-verification walkthrough — operator observes the new param in Network tab and a tile difference in the OL canvas; result recorded in the `## GAP-12-C3 Closed` section"
      pattern: "criterion_3:.*PASS"
---

<objective>
Close GAP-12-C3 from `12-VERIFICATION.md` — the per-layer WMS filter param does not narrow rendered tiles. Renderer wiring is correct; the bug is on the WMS param-name / value-format side. Phase 11's spike accepted `QUERY` without error but never validated that filtered tile output differs from unfiltered (Caveats § Filter Param). Phase 12-06 verification confirmed the issue still exists end-to-end. This plan resolves it.

Purpose: Produce a verifiable, reproducible answer to "which Kinetica WMS filter param actually narrows tile content for this deployed instance" by running a server-side spike against a *low-cardinality* table (not demo.nyctaxi, which masked the effect at viewport zoom in Phase 11). Apply the answer to `wmsUrlBuilder.ts`, then re-verify Criterion 3 against the running app and flip its status in `12-VERIFICATION.md`.

Output:
1. A reusable CLI probe script `kinetica_bi/server/src/wmsFilterSpike.ts` (mirrors `wmsSpike.ts` from Phase 11).
2. `12-07-SPIKE-NOTES.md` documenting probes for QUERY / CQL_FILTER / FILTER / WHERE with HTTP status + Content-Type + byte length + byte-equality-vs-baseline, ending with a `CONCLUSION:` line that either locks a param + value format or documents a blocker.
3. A surgical edit to `kinetica_bi/src/lib/wmsUrlBuilder.ts` (a single constant + possibly a small value-format helper) that uses the answer from the CONCLUSION line; a matching spec assertion.
4. An updated `12-VERIFICATION.md` with `criterion_3: PASS` (or `BLOCKED` with a reason) and an appended `## GAP-12-C3 Closed` (or `## GAP-12-C3 Re-Verified — Still Open`) section.

Scope guardrails:
- This plan touches `wmsUrlBuilder.ts` (one constant) and its spec only — NOT `MapChartRenderer.tsx`, NOT any component, NOT any UI.
- Plans 12-01 through 12-06 are NOT modified.
- The spike target table is selected in Wave 1 by the operator (must be a Kinetica table with a low-cardinality column AND clear spatial clustering, e.g., a small status/category column on a regional table). NOT `demo.nyctaxi` (Phase 11 caveat: dense data masks filter effect at viewport zoom).
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-dashboard-layers-panel/12-CONTEXT.md
@.planning/phases/12-dashboard-layers-panel/12-RESEARCH.md
@.planning/phases/12-dashboard-layers-panel/12-VERIFICATION.md
@.planning/phases/11-map-chart/11-SPIKE-NOTES.md
@.planning/phases/12-dashboard-layers-panel/12-05-PLAN.md
@.planning/phases/12-dashboard-layers-panel/12-06-PLAN.md
@kinetica_bi/server/src/wmsSpike.ts
@kinetica_bi/src/lib/wmsUrlBuilder.ts
@kinetica_bi/src/lib/wmsUrlBuilder.spec.ts
@kinetica_bi/src/components/charts/MapChartRenderer.tsx

<interfaces>
<!-- The single relevant existing contract for Wave 2 -->

From kinetica_bi/src/lib/wmsUrlBuilder.ts (CURRENT — to be revised in Task 2):
```typescript
// Lines 60-67 — the constants block; the FILTER_PARAM is the lever Task 2 will flip:
const X_COLUMN_PARAM = "X_ATTR";
const Y_COLUMN_PARAM = "Y_ATTR";
const GEOMETRY_COLUMN_PARAM = "GEO_ATTR";
const FILTER_PARAM = "QUERY";       // ← Task 2 replaces this string per SPIKE-NOTES CONCLUSION
const SRS_VALUE = "EPSG:3857";

// Lines 180-185 — the only place FILTER_PARAM is read:
if (whereClause && whereClause.trim().length > 0) {
  params[FILTER_PARAM] = whereClause; // QUERY=<whereClause>
}
```

If the SPIKE-NOTES CONCLUSION dictates a non-bare-WHERE-clause value format (e.g. URL-encoded, full SELECT, base64), Task 2 wraps the assignment in a small format helper. Otherwise the change is a single string-constant edit.

From kinetica_bi/server/src/wmsSpike.ts (REFERENCE PATTERN — mirror this shape for wmsFilterSpike.ts):
```typescript
import dotenv from "dotenv";
dotenv.config();
const KINETICA_URL = process.env.KINETICA_URL?.replace(/\/$/, "");
const KINETICA_USERNAME = process.env.KINETICA_USERNAME;
const KINETICA_PASSWORD = process.env.KINETICA_PASSWORD;
const basicAuth = "Basic " + Buffer.from(`${KINETICA_USERNAME}:${KINETICA_PASSWORD}`).toString("base64");
async function rawFetch(url: string, options: RequestInit = {}) {
  return fetch(url, { ...options, headers: { Authorization: basicAuth, ...(options.headers ?? {}) } });
}
```
</interfaces>

<gap_summary>
GAP-12-C3 — Per-layer WMS filter param does not narrow rendered tiles.

- Renderer wiring CONFIRMED CORRECT (M-02 lock intact, _v cache-bust intact, updateParams fires per layer with the correct top-level layer.table_id-derived WHERE clause).
- WMS GetMap requests fire with `&QUERY=<sql>` — confirmed in DevTools Network tab during 12-06 verification.
- Kinetica renders byte-identical tiles whether or not the QUERY param is present (carry-over from Phase 11 caveat).

Suspected root causes (per 12-VERIFICATION.md):
1. Wrong param name — Kinetica may expect `CQL_FILTER`, `FILTER`, `WHERE`, or a proprietary param.
2. Wrong value format — bare WHERE-clause body vs. full SELECT vs. URL-encoded vs. base64.
3. Phase 11 spike used a dense NYC taxi dataset that masked the effect at viewport zoom — the spike must use a *low-cardinality* table where filtering will produce a visibly different tile.
</gap_summary>

<verbatim_strings>
<!-- The single piece of UI text touched by this plan — copied verbatim from 12-VERIFICATION.md GAP-12-C3 entry -->

GAP id (frontmatter + section heading anchor): `GAP-12-C3`
</verbatim_strings>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 (Wave 1 — Kinetica WMS filter spike): probe 4+ filter param variants against a low-cardinality table; produce 12-07-SPIKE-NOTES.md with a CONCLUSION line</name>
  <files>kinetica_bi/server/src/wmsFilterSpike.ts, kinetica_bi/server/package.json, .gitignore, .planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md</files>
  <read_first>
    - kinetica_bi/server/src/wmsSpike.ts (FULL FILE — Phase 11 spike runner; mirror its dotenv loading, basicAuth construction, rawFetch helper, output-file pattern, and gitignore convention; the new spike runner is its sibling)
    - kinetica_bi/server/package.json (find the existing `scripts` block — locate the `wms-spike` entry to mirror its shape for the new `wms-filter-spike` script)
    - .planning/phases/11-map-chart/11-SPIKE-NOTES.md (Caveats § "Filter Param (QUERY) Effectiveness" — the seven param variants Phase 11 already tried and the three suspected reasons; do NOT re-do those probes blindly)
    - .planning/phases/12-dashboard-layers-panel/12-VERIFICATION.md (full GAP-12-C3 entry — Suspected root causes 1, 2, 3; Missing items 1, 2, 5 — these are the artefact contract for the spike notes)
    - .gitignore (repo-root gitignore — current ignored files; line ~8 already contains the Phase 11 precedent `kinetica_bi/server/src/wmsCapabilities.xml`; mirror that path style for the new spike's PNG dumps)
  </read_first>
  <action>
    This is a CHECKPOINT task — the operator (human) drives Wave 1. Claude's role is twofold: (a) scaffold the spike runner + npm script + gitignore line in advance so the operator can immediately run it, and (b) present the run protocol below and pause for the resume signal once the operator has produced 12-07-SPIKE-NOTES.md with a CONCLUSION line.

    **Why a checkpoint and not autonomous:** the acceptance gate (12-07-SPIKE-NOTES.md exists with at least 4 probe rows and a single `CONCLUSION:` line) requires the operator to (i) export Kinetica env vars, (ii) run `npm run wms-filter-spike` against a deployed Kinetica instance, (iii) eyeball the PNG diffs, (iv) hand-author the SPIKE-NOTES file. None of those four steps are autonomous-executable.

    Claude DOES perform (autonomous portion — runs before pausing for the resume signal):

    1. **Create `kinetica_bi/server/src/wmsFilterSpike.ts`** — a sibling to `wmsSpike.ts`. Mirror its shape:

       a. `import dotenv from "dotenv"; dotenv.config();`
       b. Read `KINETICA_URL`, `KINETICA_USERNAME`, `KINETICA_PASSWORD` from env; exit 1 if any are missing (same error message format as `wmsSpike.ts`).
       c. Build `basicAuth` Base64 header exactly as `wmsSpike.ts` does.
       d. Implement an `async function probeGetMap(label: string, qs: string): Promise<{ status: number; contentType: string; bytes: number; sha1: string }>` that:
          - Issues `fetch(\`${KINETICA_URL}/wms?${qs}\`, { headers: { Authorization: basicAuth } })`
          - Reads `response.arrayBuffer()` for byte-accurate length
          - Computes a SHA-1 (or simple content fingerprint — Node's `crypto.createHash('sha1').update(Buffer.from(buf)).digest('hex')`) so byte-equivalence vs. baseline is trivially detectable
          - Returns `{ status, contentType: response.headers.get('content-type') ?? '', bytes: buf.byteLength, sha1 }`
          - Writes the PNG (or whatever response) to `kinetica_bi/server/src/wmsFilterSpike-<label>.png` so the operator can manually eyeball tile differences after the run

       e. The script accepts target-table parameters via env vars (so it does NOT bake in a specific table name and stays reusable):
          - `SPIKE_TABLE` — fully-qualified `schema.tableName` (REQUIRED; exit 1 with a usage hint if missing — the hint must say "set SPIKE_TABLE to a SMALL Kinetica table with a low-cardinality column and clear spatial clustering; do NOT use demo.nyctaxi")
          - `SPIKE_X_ATTR` (lon column), `SPIKE_Y_ATTR` (lat column) — REQUIRED for lat/lon mode probes
          - `SPIKE_FILTER_COLUMN` — the low-cardinality column to filter on (REQUIRED)
          - `SPIKE_FILTER_VALUE` — a literal value present in `SPIKE_FILTER_COLUMN` (REQUIRED; quoted by the script for SQL safety)
          - `SPIKE_BBOX` — `minLon,minLat,maxLon,maxLat` for the GetMap viewport (REQUIRED; must be tight on the spatial clustering of the filtered subset so the visual difference is obvious; provide a reasonable default for testing if absent — `-180,-90,180,90` covers the world but the operator should override)

       f. Build a `baseline` query string with NO filter (`STYLES=raster`, `LAYERS=${SPIKE_TABLE}`, `X_ATTR=${SPIKE_X_ATTR}`, `Y_ATTR=${SPIKE_Y_ATTR}`, `SRS=EPSG:3857`, `FORMAT=image/png`, `WIDTH=512`, `HEIGHT=512`, `BBOX=${SPIKE_BBOX}`, `VERSION=1.1.1`, `SERVICE=WMS`, `REQUEST=GetMap`, `TRANSPARENT=true`).

       g. For each candidate filter param name, build a corresponding query string that appends the filter clause AND probe it. Use `<col> = '<val>'` as the bare value format on the first pass (the format the renderer currently sends); if all four return identical sha1 to baseline, the script also runs a second pass with each param using `SELECT * FROM <table> WHERE <col> = '<val>'` (full-SELECT format) — a total of up to 8 probes:

          Candidate param names (probe in this order):
          - `QUERY` (current production value — establish parity with renderer behaviour)
          - `CQL_FILTER` (GeoServer-standard; Kinetica may share it)
          - `FILTER` (generic; tested in Phase 11 spike but only against demo.nyctaxi)
          - `WHERE` (Kinetica SQL-flavored guess)

          Value format pass A (bare WHERE-clause body): `<param>=${encodeURIComponent(`${SPIKE_FILTER_COLUMN} = '${SPIKE_FILTER_VALUE}'`)}`
          Value format pass B (full SELECT, only run if all pass-A probes match baseline sha1): `<param>=${encodeURIComponent(`SELECT * FROM ${SPIKE_TABLE} WHERE ${SPIKE_FILTER_COLUMN} = '${SPIKE_FILTER_VALUE}'`)}`

       h. Print a results table to stdout — fixed-width columns: `label | status | content-type | bytes | sha1 (first 12) | matches-baseline?`. Example output line: `QUERY-pass-A      | 200    | image/png      |  4321 | abc123def456 | YES (BUG)`.

       i. End the script with two summary lines:
          - `[spike] Tile responses written to: kinetica_bi/server/src/wmsFilterSpike-*.png`
          - `[spike] Operator: open .planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md and record the table above + the CONCLUSION line.`

       j. Add a top-of-file doc-comment matching `wmsSpike.ts`'s style: PURPOSE, USAGE (`cd kinetica_bi/server && npm run wms-filter-spike`), OUTPUT (the PNG dumps + stdout), and `NOT PART OF THE EXPRESS APP`.

    2. **Update `kinetica_bi/server/package.json`** — add `wms-filter-spike` to the `scripts` block, mirroring the existing `wms-spike` entry's shape (likely `"wms-filter-spike": "tsx src/wmsFilterSpike.ts"` or whatever runner the existing `wms-spike` uses; preserve identical syntax).

    3. **Update the repo-root `.gitignore`** — append the line `kinetica_bi/server/src/wmsFilterSpike-*.png` so the spike's binary PNG dumps stay out of git. The repo-root `.gitignore` is the canonical one (it already contains the Phase 11 precedent `kinetica_bi/server/src/wmsCapabilities.xml` on line ~8). Use that exact path style — the new pattern goes adjacent to or below the wmsCapabilities.xml entry. Do NOT create or modify any other gitignore file (e.g., do NOT create `kinetica_bi/.gitignore`; the repo uses a single root gitignore).

    4. **Confirm tsc compiles cleanly** — run `cd kinetica_bi/server && npx tsc --noEmit` and confirm exit 0 before pausing for the resume signal. If tsc fails, fix the script and re-run before pausing.

    Then PAUSE for the operator. The operator performs:

    5. **Run the spike (operator step):**

       ```bash
       cd kinetica_bi/server
       SPIKE_TABLE=<schema.smallTable> \
         SPIKE_X_ATTR=<lon_col> SPIKE_Y_ATTR=<lat_col> \
         SPIKE_FILTER_COLUMN=<lowCardinalityCol> SPIKE_FILTER_VALUE=<aValue> \
         SPIKE_BBOX=<minLon>,<minLat>,<maxLon>,<maxLat> \
         npm run wms-filter-spike
       ```

       Choose the table per the Phase 11 caveat: NOT demo.nyctaxi. Pick something with under ~100k rows, a column with 5-50 distinct values, and clear spatial clustering of those values within the BBOX. If a suitable table is not available on the deployed Kinetica, document that as a blocker (see step 7 alt path).

    6. **Eyeball the dumped PNGs** — open `wmsFilterSpike-baseline.png`, `wmsFilterSpike-QUERY-pass-A.png`, `wmsFilterSpike-CQL_FILTER-pass-A.png`, `wmsFilterSpike-FILTER-pass-A.png`, `wmsFilterSpike-WHERE-pass-A.png` in any image viewer. Compare visually. Whichever PNG visibly differs from baseline (fewer points, narrower coverage) AND has a different sha1 in the stdout table is the working param. If no pass-A probe differs, repeat the comparison for pass-B PNGs. If still no difference, the spike is BLOCKED.

    7. **Write `.planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md`** with the following structure (operator records actual values; do NOT pre-fill placeholder values — they must come from the run):

       ```markdown
       # Phase 12 — WMS Filter Param Spike Notes

       **Spike date:** <YYYY-MM-DD from the run>
       **Deployed Kinetica:** <KINETICA_URL with credentials redacted>
       **Spike runner:** kinetica_bi/server/src/wmsFilterSpike.ts
       **Closes gap:** GAP-12-C3 from .planning/phases/12-dashboard-layers-panel/12-VERIFICATION.md

       ## Target table

       | Property | Value |
       |---|---|
       | Table | `<schema.tableName>` |
       | Approximate row count | <N> |
       | Filter column | `<columnName>` |
       | Filter column distinct value count | <N> |
       | Filter value tested | `<value>` |
       | BBOX (minLon,minLat,maxLon,maxLat) | `<bbox>` |
       | Reason this table was chosen (NOT demo.nyctaxi) | <one sentence> |

       ## Probes — pass A (bare `<col> = '<val>'` WHERE-clause body)

       | Param | HTTP status | Content-Type | Bytes | sha1 (first 12) | Matches baseline? | Visible tile difference? |
       |---|---|---|---|---|---|---|
       | (baseline, no filter) | <s> | <ct> | <b> | <h> | — | — |
       | QUERY | <s> | <ct> | <b> | <h> | <YES/NO> | <YES/NO> |
       | CQL_FILTER | <s> | <ct> | <b> | <h> | <YES/NO> | <YES/NO> |
       | FILTER | <s> | <ct> | <b> | <h> | <YES/NO> | <YES/NO> |
       | WHERE | <s> | <ct> | <b> | <h> | <YES/NO> | <YES/NO> |

       ## Probes — pass B (full `SELECT * FROM <t> WHERE <col> = '<val>'`)

       <Only fill in if pass A had no winners. Same table shape.>

       ## Open observations

       - <Anything noted during the spike — e.g., one param returned XML error, one returned 200 but identical sha1, etc.>

       ## CONCLUSION:

       <ONE of the following two formats:>

       <Format 1 — found a working param:>
       CONCLUSION: param=<NAME>, value-format=<bare-where|full-select|url-encoded|other:<describe>>; wmsUrlBuilder.ts FILTER_PARAM constant should be set to "<NAME>" and the assignment should pass <bare-where|full-select> to params[<NAME>]. Verified by sha1 difference and visible PNG difference vs. baseline.

       <Format 2 — no probe produced a difference:>
       CONCLUSION: BLOCKED — <one-sentence reason: e.g. "all 8 probes returned baseline-matching sha1; suspect Kinetica deployment does not honour any standard WMS filter param and requires a view-based layer (out of scope for v1.2)" OR "no low-cardinality table available on the deployed Kinetica to run the spike against; deferred until a suitable test fixture exists">. Recommended next step: <e.g. "open Kinetica admin ticket asking which filter param the deployed version supports" / "create a test view layer in Kinetica and probe that" / "defer C3 to v1.3 with a backlog entry">.
       ```

    The CONCLUSION line is the load-bearing artefact for Wave 2. It MUST start with the literal token `CONCLUSION:` at column 1 of its line so Task 2's grep checks find it deterministically.

    8. **Do NOT modify any other planning artefact.** Plans 12-01 through 12-06 are immutable. The only files this task creates / modifies are: `wmsFilterSpike.ts`, `package.json` scripts, `.gitignore`, and `12-07-SPIKE-NOTES.md`.
  </action>
  <verify>
    <automated>cd kinetica_bi && test -f server/src/wmsFilterSpike.ts && grep -q 'wms-filter-spike' server/package.json && test -f ../.planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md && grep -q '^CONCLUSION:' ../.planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md && cd server && npx tsc --noEmit</automated>
  </verify>
  <what-built>
    Claude scaffolds the reusable Wave-1 plumbing: a sibling spike runner (`kinetica_bi/server/src/wmsFilterSpike.ts`) modelled on `wmsSpike.ts`, a new `wms-filter-spike` npm script in `kinetica_bi/server/package.json`, and a single line in the repo-root `.gitignore` keeping the spike's PNG dumps out of git. `npx tsc --noEmit` is green before the pause. The autonomous-executable side of Wave 1 is complete; what remains (running the script against a live Kinetica, eyeballing PNG diffs, hand-authoring 12-07-SPIKE-NOTES.md with a load-bearing CONCLUSION line) is operator work.
  </what-built>
  <how-to-verify>
    Operator runs steps 5, 6, and 7 from `<action>` above. Specifically:

    1. Export Kinetica env vars (`KINETICA_URL`, `KINETICA_USERNAME`, `KINETICA_PASSWORD`) plus the SPIKE_* table-binding vars described in step 5.
    2. Run `cd kinetica_bi/server && npm run wms-filter-spike`. Confirm the stdout results table shows up to 8 probe rows and that PNG dumps land at `kinetica_bi/server/src/wmsFilterSpike-*.png`.
    3. Eyeball the PNG dumps in any image viewer. Compare each filtered probe's tile to the baseline.
    4. Hand-author `.planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md` per the template in step 7. Make sure the CONCLUSION line starts at column 1 with the literal token `CONCLUSION:` and follows one of the two valid formats (`CONCLUSION: param=...` for the success path, `CONCLUSION: BLOCKED — ...` for the blocked path).
    5. Confirm the file ends with at least 4 populated probe rows and exactly one CONCLUSION line.
  </how-to-verify>
  <resume-signal>Type "spike-complete" once `.planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md` is saved with at least 4 populated probe rows and a single `CONCLUSION:` line (either `CONCLUSION: param=...` success path or `CONCLUSION: BLOCKED — ...` blocked path). If you hit a blocker before producing the notes (e.g., no suitable table on the deployed Kinetica), describe the blocker and we'll either pivot to a different test fixture or write a `CONCLUSION: BLOCKED —` note documenting the blocker.</resume-signal>
  <acceptance_criteria>
    - `test -f kinetica_bi/server/src/wmsFilterSpike.ts` succeeds (exit 0)
    - `grep -nE 'KINETICA_URL|basicAuth|REQUEST=GetMap' kinetica_bi/server/src/wmsFilterSpike.ts` returns >= 3 matches (mirrors wmsSpike.ts pattern)
    - `grep -nE 'QUERY|CQL_FILTER|FILTER|WHERE' kinetica_bi/server/src/wmsFilterSpike.ts` returns >= 4 distinct param-name matches (all four candidates probed)
    - `grep -n 'demo.nyctaxi' kinetica_bi/server/src/wmsFilterSpike.ts` returns 0 matches in active code paths (only allowed in a usage-hint string forbidding it; if grep returns >0 matches, ALL of them must be inside a string literal that says "do NOT use demo.nyctaxi" or equivalent)
    - `grep -n '"wms-filter-spike"' kinetica_bi/server/package.json` returns >= 1 match
    - `grep -nE 'wmsFilterSpike-\*\.png|wmsFilterSpike-' .gitignore` returns >= 1 match (PNG dumps gitignored at repo root)
    - `cd kinetica_bi/server && npx tsc --noEmit` exits 0 (script compiles cleanly)
    - `test -f .planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md` succeeds
    - `grep -cE '^\| (QUERY|CQL_FILTER|FILTER|WHERE)' .planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md` returns >= 4 (all four candidate params represented as table rows)
    - `grep -cE '^\| HTTP status|http status|Content-Type|sha1' .planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md` returns >= 3 (probe-result columns documented per the table schema in step 7)
    - `grep -nE '^CONCLUSION:' .planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md` returns exactly 1 match (the load-bearing CONCLUSION line for Wave 2)
    - The CONCLUSION line either starts with `CONCLUSION: param=` (success path) OR `CONCLUSION: BLOCKED —` (blocked path) — both are valid spike outcomes per the spike-task caveat
    - `grep -n 'Plans 12-01 through 12-06\|12-01-PLAN\|12-02-PLAN\|12-03-PLAN\|12-04-PLAN\|12-05-PLAN\|12-06-PLAN' .planning/phases/12-dashboard-layers-panel/12-01-PLAN.md` produces the same byte content as before this task started (i.e. plans 01-06 untouched — see also `git diff --stat .planning/phases/12-dashboard-layers-panel/12-01-PLAN.md` returns empty)
    - `git status --porcelain .planning/phases/12-dashboard-layers-panel/12-0{1,2,3,4,5,6}-PLAN.md` returns empty (none of the prior plans modified)
  </acceptance_criteria>
  <done>Spike runner exists at kinetica_bi/server/src/wmsFilterSpike.ts with the npm script registered; PNG dumps gitignored at repo-root `.gitignore`; 12-07-SPIKE-NOTES.md exists, contains a 4+ row probe-results table, and ends with a single `CONCLUSION:` line that either locks the param + value format (success path) or documents a specific blocker (blocked path). No prior plan files modified.</done>
</task>

<task type="auto">
  <name>Task 2 (Wave 2 — wmsUrlBuilder fix): apply the SPIKE-NOTES CONCLUSION to wmsUrlBuilder.ts and update its spec</name>
  <files>kinetica_bi/src/lib/wmsUrlBuilder.ts, kinetica_bi/src/lib/wmsUrlBuilder.spec.ts</files>
  <read_first>
    - .planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md (FULL FILE — the CONCLUSION line dictates EXACTLY what change to apply; nothing else; do not improvise)
    - kinetica_bi/src/lib/wmsUrlBuilder.ts (FULL FILE — current implementation; identify the FILTER_PARAM constant at ~line 66 and the single read site at ~line 184)
    - kinetica_bi/src/lib/wmsUrlBuilder.spec.ts (FULL FILE — 43 existing tests; identify the test(s) that assert the QUERY param name; those need updating)
    - kinetica_bi/src/components/charts/MapChartRenderer.tsx (READ ONLY — confirm the renderer is a pure consumer of buildWmsParams and does NOT itself reference the FILTER_PARAM string; this verifies the fix is local to wmsUrlBuilder.ts)
  </read_first>
  <action>
    **Branch on the CONCLUSION line.** Open `.planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md` and find the line starting with `CONCLUSION:`. The exact text on that line dictates the entire change set for this task.

    **If the CONCLUSION starts with `CONCLUSION: BLOCKED —`:**

    1. STOP. Do not modify `wmsUrlBuilder.ts` or its spec.
    2. Skip directly to Task 3, but in Task 3, instead of flipping `criterion_3` to PASS, flip it to `BLOCKED` and copy the blocker reason from the CONCLUSION line into the new `## GAP-12-C3 Re-Verified — Still Open` section.
    3. The acceptance criteria below for "FILTER_PARAM updated" do not apply in the BLOCKED path; only the BLOCKED-path acceptance criteria (last bullet group) apply.

    **If the CONCLUSION starts with `CONCLUSION: param=<NAME>, value-format=<FORMAT>`:**

    1. **Read the constant name and value format from the CONCLUSION line** — these are load-bearing values you cannot pre-fill. Examples (illustrative only — use the actual value from the CONCLUSION):
       - `CONCLUSION: param=CQL_FILTER, value-format=bare-where; ...` → constant value `"CQL_FILTER"`, value format unchanged from current bare-clause assignment.
       - `CONCLUSION: param=QUERY, value-format=full-select; ...` → constant value `"QUERY"` (unchanged), but the value passed in becomes `\`SELECT * FROM ${tableRef} WHERE ${whereClause}\`` instead of the bare clause.
       - `CONCLUSION: param=FILTER, value-format=url-encoded; ...` → constant value `"FILTER"`, value passed in is `encodeURIComponent(whereClause)`.

    2. **Edit `kinetica_bi/src/lib/wmsUrlBuilder.ts`:**

       a. **Replace the FILTER_PARAM constant value** at line ~66:
          ```typescript
          const FILTER_PARAM = "<NAME from CONCLUSION>";   // ← was "QUERY"
          ```
          Add an inline comment on the line above the constant referencing the source-of-truth: `// Locked by .planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md CONCLUSION (2026-05-06).`

       b. **Update the SPIKE-LOCKED CONSTANTS doc-block (lines ~54-67)** — add a one-line note under the QUERY entry pointing at 12-07-SPIKE-NOTES.md. Example:
          ```
          // QUERY: superseded by <NAME> per 12-07-SPIKE-NOTES.md (Phase 11's QUERY was accepted but never narrowed tiles).
          ```

       c. **If value-format != bare-where**, modify the assignment block at lines ~180-185. Replace the bare assignment with the format-specific transformation. The exact transformation depends on the CONCLUSION's `value-format=`:
          - `bare-where`: NO change — `params[FILTER_PARAM] = whereClause;` stays.
          - `full-select`: `params[FILTER_PARAM] = \`SELECT * FROM ${config.tableRef ?? config.layerName ?? ''} WHERE ${whereClause}\`;` (only if both tableRef-precedence is acceptable; if neither is set, fall back to omitting the param — preserve the existing AP-3 lock).
          - `url-encoded`: `params[FILTER_PARAM] = encodeURIComponent(whereClause);`
          - `other:<describe>`: implement per the CONCLUSION's free-form description.

       d. **Preserve all existing locks verbatim** in the source: PITFALL M-02 cache-buster comment, AP-3 doc-block, M-04 GEO_ATTR branch, M-03 SRS lock, etc. Do NOT touch any param other than FILTER_PARAM.

    3. **Update `kinetica_bi/src/lib/wmsUrlBuilder.spec.ts`:**

       a. Find every existing test that asserts the param key `"QUERY"` (likely 1-3 tests; grep `\"QUERY\"` in the spec).
       b. Replace `"QUERY"` with `"<NAME from CONCLUSION>"` in those assertions.
       c. Add ONE new test (placed adjacent to the modified test, in the same describe block) that verifies the SPIKE-NOTES locking. Suggested shape:
          ```typescript
          it("emits the spike-locked filter param name (12-07-SPIKE-NOTES.md CONCLUSION)", () => {
            const params = buildWmsParams(
              { tableId: 1, spatialMode: "latlon", latColumn: "lat", lonColumn: "lon", renderMode: "raster" },
              7,
              "vendor_id = 'CMT'",
            );
            // Locked param name from .planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md
            expect(params["<NAME>"]).toBe(<expected formatted value per CONCLUSION>);
            // Old QUERY param MUST NOT be emitted (regression guard)
            expect(params["QUERY"]).toBeUndefined();
          });
          ```
          Adjust the `<expected formatted value>` line based on `value-format`:
          - bare-where → `expect(params["<NAME>"]).toBe("vendor_id = 'CMT'")`
          - full-select → `expect(params["<NAME>"]).toMatch(/^SELECT \* FROM .* WHERE vendor_id = 'CMT'$/)`
          - url-encoded → `expect(params["<NAME>"]).toBe(encodeURIComponent("vendor_id = 'CMT'"))`
          - other → match the CONCLUSION's described format

       d. The "QUERY MUST NOT be emitted" regression guard is critical. Even if the CONCLUSION param is, by chance, still QUERY (i.e. the spike found that QUERY DOES work but only with a different value format), keep this assertion as the spec for the new value-format expectation.

       e. Keep all other 43+ tests unchanged. The "do not emit QUERY when whereClause is empty" / AP-3 tests must continue to pass — verify by running the spec.

    4. **Do not edit any other source file.** `MapChartRenderer.tsx` is a pure consumer of `buildWmsParams` — its `source.updateParams(...)` call automatically picks up the new param name on the next render. Verify (do not modify) by:
       - `grep -nE 'QUERY|CQL_FILTER|FILTER_PARAM|WHERE\b' kinetica_bi/src/components/charts/MapChartRenderer.tsx` returns 0 matches for filter-param strings (the renderer never references the constant directly; it only calls `buildWmsParams`).

    5. **Run the spec to confirm zero regressions:**
       ```bash
       cd kinetica_bi && npx vitest run src/lib/wmsUrlBuilder.spec.ts
       ```
       All 43+ existing tests + the 1 new test must pass.

    6. **Run the full frontend type-check:**
       ```bash
       cd kinetica_bi && npx tsc --noEmit
       ```
       Must exit 0.
  </action>
  <verify>
    <automated>cd kinetica_bi && npx vitest run src/lib/wmsUrlBuilder.spec.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    <!-- Pre-flight: confirm the CONCLUSION exists and was read -->
    - `grep -E '^CONCLUSION:' .planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md` returns exactly 1 line (Task 1 produced the input this task consumes)

    <!-- Success-path criteria (apply when CONCLUSION starts with `CONCLUSION: param=`) -->
    - **If** the CONCLUSION line starts with `CONCLUSION: param=` (success path), THEN ALL of the following apply:
      - `grep -E '^const FILTER_PARAM = ' kinetica_bi/src/lib/wmsUrlBuilder.ts` returns exactly 1 line
      - The string-literal value of that FILTER_PARAM constant matches the param name from the CONCLUSION line; specifically, `node -e "const f=require('fs').readFileSync('kinetica_bi/src/lib/wmsUrlBuilder.ts','utf8'); const m=f.match(/^const FILTER_PARAM = \"([^\"]+)\"/m); console.log(m?m[1]:'MISSING')"` outputs the same param name as appears after `param=` in the CONCLUSION line
      - The FILTER_PARAM constant value is NOT `"QUERY"` UNLESS the CONCLUSION explicitly says `param=QUERY` (a successful CONCLUSION may rename the constant or keep QUERY but with a different value format)
      - `grep -nE '12-07-SPIKE-NOTES' kinetica_bi/src/lib/wmsUrlBuilder.ts` returns >= 1 match (source-of-truth comment links back to the spike notes)
      - `grep -nE '"QUERY"' kinetica_bi/src/lib/wmsUrlBuilder.spec.ts | grep -v 'toBeUndefined\|MUST NOT be emitted\|regression guard\|was "QUERY"'` produces no remaining QUERY assertion lines that pass when the param is now NAME != QUERY (i.e. the old positive QUERY assertions are removed; the only QUERY references in the spec are negative/regression-guard assertions)
      - `grep -nE 'spike-locked filter param name|12-07-SPIKE-NOTES' kinetica_bi/src/lib/wmsUrlBuilder.spec.ts` returns >= 1 match (new locking test added)
      - `cd kinetica_bi && npx vitest run src/lib/wmsUrlBuilder.spec.ts` exits 0 (all 43+ existing tests + 1 new test pass)
      - `cd kinetica_bi && npx tsc --noEmit` exits 0
      - `grep -nE 'QUERY|CQL_FILTER|FILTER_PARAM' kinetica_bi/src/components/charts/MapChartRenderer.tsx` returns 0 matches for filter-param string literals (the renderer is a pure consumer; not modified)

    <!-- Blocked-path criteria (apply when CONCLUSION starts with `CONCLUSION: BLOCKED —`) -->
    - **If** the CONCLUSION line starts with `CONCLUSION: BLOCKED —` (blocked path), THEN ALL of the following apply:
      - `git diff --stat kinetica_bi/src/lib/wmsUrlBuilder.ts kinetica_bi/src/lib/wmsUrlBuilder.spec.ts` returns empty (NO source changes; this task is a no-op in the blocked path)
      - The Wave 3 task uses the BLOCKED branch (criterion_3 → BLOCKED, not PASS)

    <!-- Universal: no out-of-scope source edits in either path -->
    - `git status --porcelain kinetica_bi/src/components/charts/` returns empty (no Phase 12-05 component edits)
    - `git status --porcelain .planning/phases/12-dashboard-layers-panel/12-0{1,2,3,4,5,6}-PLAN.md` returns empty (prior plans untouched)
  </acceptance_criteria>
  <done>wmsUrlBuilder.ts FILTER_PARAM constant is updated to the value the SPIKE-NOTES CONCLUSION locks (or the source is left unchanged if the CONCLUSION is BLOCKED); the spec covers the new locking with a regression guard against the old QUERY param; existing 43+ tests still pass; tsc green; no out-of-scope source files modified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (Wave 3 — re-verify Criterion 3): walk the running app, confirm filtered tiles differ, append `## GAP-12-C3 Closed` to 12-VERIFICATION.md, flip criterion_3 in frontmatter</name>
  <files>.planning/phases/12-dashboard-layers-panel/12-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md (the CONCLUSION line — informs whether Wave 3 is the success path or the BLOCKED path)
    - .planning/phases/12-dashboard-layers-panel/12-VERIFICATION.md (FULL FILE — current frontmatter shape, GAP-12-C3 entry to mirror in style, "Open Gap" section heading conventions)
    - kinetica_bi/src/lib/wmsUrlBuilder.ts (post-Task-2 — confirm the FILTER_PARAM constant matches the CONCLUSION before walking the UI)
    - kinetica_bi/src/components/charts/MapChartRenderer.tsx (READ ONLY — confirm renderer still uses buildWmsParams)
  </read_first>
  <action>
    This is a CHECKPOINT task — Claude does not execute it autonomously. Claude's role: present the verification protocol from `<how-to-verify>` to the user, ensure the dev environment can start, then STOP. The user runs the protocol manually and reports outcome via the resume signal.

    Concrete steps Claude DOES perform:
    1. Read `.planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md` and surface the `CONCLUSION:` line to the user — this tells the user (and Claude) whether Wave 3 is the success path or the BLOCKED path.
    2. Print the relevant branch of `<how-to-verify>` (success path OR blocked path — NOT both) to the user.
    3. Confirm both dev commands are available (`cd kinetica_bi/server && npm run dev` for backend on :4000; `cd kinetica_bi && npm run dev` for frontend on :5173) — only relevant on the success path.
    4. Block on the resume signal.
    5. After "approved": apply the user's reported edits to `.planning/phases/12-dashboard-layers-panel/12-VERIFICATION.md` (frontmatter flip + appended section) per the path the user took. Claude does the file edits; the user provides the values (sha1s, table name, walkthrough observations).
    6. If the user reports unexpected behaviour (e.g. param emits but tiles still identical), do NOT edit 12-VERIFICATION.md; instead, surface the discrepancy and recommend re-opening Wave 1 with a different table or value format.
  </action>
  <verify>
    <automated>cd kinetica_bi && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <what-built>
    Wave 1 produced 12-07-SPIKE-NOTES.md locking the working WMS filter param name + value format. Wave 2 applied that to wmsUrlBuilder.ts. The renderer (MapChartRenderer.tsx) is unchanged — it consumes the updated builder automatically. The full vitest suite passes. This checkpoint is the human walkthrough that proves Criterion 3 of 12-VERIFICATION.md now passes (or, if the spike was BLOCKED, that documents the still-open state with the locked reason).
  </what-built>
  <how-to-verify>
    **Choose the path based on `12-07-SPIKE-NOTES.md` CONCLUSION line.**

    ===== Success path (CONCLUSION starts with `CONCLUSION: param=`) =====

    1. Start backend and frontend dev servers:
       ```bash
       cd kinetica_bi/server && npm run dev   # backend on :4000
       cd kinetica_bi && npm run dev          # frontend on :5173
       ```

    2. Open http://localhost:5173 in a browser. Authenticate (password mode or OIDC per current AUTH_MODE).

    3. Open the same dashboard used for 12-06 verification (or a fresh one with a Map widget on a low-cardinality table — the same table the spike used in Wave 1 is ideal).

    4. Add a chart (bar or pie) for the SAME low-cardinality column the spike filtered on. Confirm the map widget shows tiles for the unfiltered table.

    5. Open DevTools → Network tab → filter to `/api/wms`. Note the GetMap request URL — confirm:
       - The `_v=<filterVersion>` cache-buster is present (M-02 lock — should still be there)
       - The filter param uses the **NEW** name from the SPIKE-NOTES CONCLUSION (e.g. `&CQL_FILTER=...` instead of `&QUERY=...`)
       - The value format matches the CONCLUSION's `value-format=` (bare-where / full-select / url-encoded / other)

    6. Click a chart element to add a drill-down filter. The Phase 10 filter chip should appear; the map widget should refetch tiles.

    7. **The acceptance test:** Visually compare the map BEFORE and AFTER the filter:
       - The filtered tiles MUST show fewer points / narrower spatial coverage / different color distribution.
       - If the tiles are byte-identical to the unfiltered ones, Wave 2's fix did not actually narrow the rendered output. Stop and reopen Wave 1 with a different table or different value format.

    8. (Optional but recommended) Compare the new GetMap request's response PNG checksum against the no-filter baseline using DevTools Network tab → response → "Save as" → run `sha1sum <file>` in a terminal. The two must differ.

    9. Open `.planning/phases/12-dashboard-layers-panel/12-VERIFICATION.md` and apply the following edits:

       a. **Frontmatter:** flip `criterion_3: RED` → `criterion_3: PASS`. Recompute `overall_status`: if all 6 criteria are now PASS, set `overall_status: passed`; otherwise leave `tech_debt`.

       b. **Frontmatter `gaps:` block:** mark the GAP-12-C3 entry as closed by adding a `closed_at: "2026-MM-DD"` field and a `closed_by: <verifier-email>` field directly under the existing `recommended_action:` field. Do NOT delete the gap entry — it remains as historical record. Example diff:
          ```yaml
          gaps:
            - id: GAP-12-C3
              criterion: 3
              title: "Per-layer WMS filter param does not narrow rendered tiles"
              ...
              recommended_action: >
                Run /gsd:plan-phase 12 --gaps to generate 12-07-PLAN.md ...
              closed_at: "2026-MM-DD"
              closed_by: RPereira@kinetica.com
              closed_by_plan: 12-07
          ```

       c. **Append a new section** to the bottom of the file (after "Tech Debt" section), heading exactly: `## GAP-12-C3 Closed`. Use the template below — fill in actual values from the walkthrough:
          ```markdown
          ## GAP-12-C3 Closed

          **Closed:** <YYYY-MM-DD>
          **Closed by:** <verifier email>
          **Closed by plan:** 12-07
          **Spike notes:** .planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md
          **Source change:** kinetica_bi/src/lib/wmsUrlBuilder.ts FILTER_PARAM <was "QUERY", now "<NAME>">; value format: <bare-where | full-select | url-encoded | other>

          ### Re-verification walkthrough

          | Property | Value |
          |---|---|
          | Table walked | `<schema.tableName>` |
          | Approximate row count | <N> |
          | Filter column | `<columnName>` |
          | Filter column distinct value count | <N> |
          | Filter chip applied | `<col> = '<value>'` |
          | filterVersion before / after | <N1> / <N2> |
          | Network tab confirmation | `&<NAME>=<encoded value>` observed in WMS GetMap request URL |
          | Tile output before vs. after | <one-sentence description: "Pre-filter tiles showed points distributed across the BBOX; post-filter tiles showed only points where col = 'value', ~<N>% of original coverage."> |
          | Response PNG sha1 before / after | <sha1-A first 12> / <sha1-B first 12> (DIFFERENT — confirms server-side filtering) |

          ### Locks preserved

          - PITFALL M-01 (map dispose): unchanged — same `setTarget(undefined); dispose()` cleanup
          - PITFALL M-02 (updateParams not rebuild): unchanged — `_v=filterVersion` still emitted; per-layer `source.updateParams()` still the only mutation path
          - PITFALL M-03 (EPSG:3857): unchanged
          - AP-3 (no QUERY when whereClause empty): unchanged — buildWmsParams still gates emission on `whereClause.trim().length > 0`
          ```

       d. Save the file.

    ===== Blocked path (CONCLUSION starts with `CONCLUSION: BLOCKED —`) =====

    1. Skip steps 1-8 above (no source change to verify).

    2. Open `.planning/phases/12-dashboard-layers-panel/12-VERIFICATION.md`:

       a. **Frontmatter:** change `criterion_3: RED` → `criterion_3: BLOCKED`. Leave `overall_status` as `tech_debt`.

       b. **Frontmatter `gaps:` block:** add `re_verified_at: "2026-MM-DD"`, `re_verified_by: <email>`, `re_verified_by_plan: 12-07`, and `still_open_reason:` (one-sentence summary of the SPIKE-NOTES blocked reason) under the existing GAP-12-C3 entry. Do NOT add `closed_at`.

       c. **Append a new section** to the bottom of the file, heading exactly: `## GAP-12-C3 Re-Verified — Still Open`. Template:
          ```markdown
          ## GAP-12-C3 Re-Verified — Still Open

          **Re-verified:** <YYYY-MM-DD>
          **Re-verified by:** <verifier email>
          **Re-verified by plan:** 12-07
          **Spike notes:** .planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md (CONCLUSION: BLOCKED)
          **Reason still open:** <verbatim copy of the BLOCKED reason from the SPIKE-NOTES CONCLUSION line>
          **Recommended next step:** <verbatim copy of the SPIKE-NOTES "Recommended next step" if present>

          The renderer (MapChartRenderer.tsx) and per-layer filter subscription wiring (M-02 lock) remain correct. The bug is on the deployed Kinetica WMS side — no client-side fix is possible without further information from the deployment team. Track for v1.3 if not resolved upstream.
          ```

       d. Save the file.

    3. Stop. Do not run any further verification.

    ===== Resume signal =====

    After applying the above edits, type "approved" to mark the gap-closure plan as complete. If the walkthrough produced unexpected results (e.g. the new param emits but tiles STILL look identical, or DevTools shows a different param than the SPIKE-NOTES locked), describe the specific discrepancy and we'll re-open Wave 1.
  </how-to-verify>
  <resume-signal>Type "approved" once 12-VERIFICATION.md is saved with criterion_3 flipped (PASS or BLOCKED) and the appropriate `## GAP-12-C3 Closed` or `## GAP-12-C3 Re-Verified — Still Open` section is appended; or describe specific issues found.</resume-signal>
  <acceptance_criteria>
    <!-- Universal: SPIKE-NOTES exists and was the input -->
    - `grep -E '^CONCLUSION:' .planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md` returns exactly 1 line

    <!-- Success path: CONCLUSION started with `param=` -->
    - **If** the SPIKE-NOTES CONCLUSION starts with `CONCLUSION: param=`, THEN:
      - `grep -E '^\s*criterion_3:\s*PASS\s*$' .planning/phases/12-dashboard-layers-panel/12-VERIFICATION.md` returns 1 match in the frontmatter
      - `grep -nE '^## GAP-12-C3 Closed\s*$' .planning/phases/12-dashboard-layers-panel/12-VERIFICATION.md` returns exactly 1 match
      - `grep -nE 'closed_at:|closed_by:|closed_by_plan:' .planning/phases/12-dashboard-layers-panel/12-VERIFICATION.md` returns >= 3 matches (gap entry annotated)
      - `grep -nE 'sha1|Response PNG sha1|Tile output before' .planning/phases/12-dashboard-layers-panel/12-VERIFICATION.md` returns >= 1 match (walkthrough evidence recorded)
      - `grep -nE 'Network tab|GetMap request URL' .planning/phases/12-dashboard-layers-panel/12-VERIFICATION.md` returns >= 1 match (Network tab observation recorded)

    <!-- Blocked path: CONCLUSION started with `BLOCKED —` -->
    - **If** the SPIKE-NOTES CONCLUSION starts with `CONCLUSION: BLOCKED —`, THEN:
      - `grep -E '^\s*criterion_3:\s*BLOCKED\s*$' .planning/phases/12-dashboard-layers-panel/12-VERIFICATION.md` returns 1 match in the frontmatter
      - `grep -nE '^## GAP-12-C3 Re-Verified — Still Open\s*$' .planning/phases/12-dashboard-layers-panel/12-VERIFICATION.md` returns exactly 1 match
      - `grep -nE 're_verified_at:|re_verified_by:|still_open_reason:' .planning/phases/12-dashboard-layers-panel/12-VERIFICATION.md` returns >= 3 matches

    <!-- Universal: no off-scope edits -->
    - `git status --porcelain .planning/phases/12-dashboard-layers-panel/12-0{1,2,3,4,5,6}-PLAN.md` returns empty (prior plans untouched)
    - `git status --porcelain kinetica_bi/src/components/charts/MapChartRenderer.tsx kinetica_bi/src/components/charts/MapChartRenderer.spec.tsx kinetica_bi/src/components/charts/MapConfigPanel.tsx kinetica_bi/src/components/charts/MapConfigPanel.spec.tsx kinetica_bi/src/components/charts/WidgetRenderer.tsx kinetica_bi/src/components/LayersModal.tsx 2>/dev/null` returns empty (no UI / renderer / modal edits)
    - `cd kinetica_bi && npx vitest run` exits 0 (full frontend suite still green)
  </acceptance_criteria>
  <done>12-VERIFICATION.md frontmatter has `criterion_3: PASS` and an appended `## GAP-12-C3 Closed` section with walkthrough evidence (success path), OR `criterion_3: BLOCKED` and an appended `## GAP-12-C3 Re-Verified — Still Open` section copying the SPIKE-NOTES reason (blocked path). No off-scope source files modified. Full vitest suite green. The gap-closure cycle is complete.</done>
</task>

</tasks>

<verification>
- [ ] `test -f kinetica_bi/server/src/wmsFilterSpike.ts` succeeds (Wave 1 artefact)
- [ ] `grep -q '"wms-filter-spike"' kinetica_bi/server/package.json` succeeds (npm script registered)
- [ ] `test -f .planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md` succeeds
- [ ] `grep -E '^CONCLUSION:' .planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md` returns exactly 1 line
- [ ] `grep -cE '^\| (QUERY|CQL_FILTER|FILTER|WHERE)' .planning/phases/12-dashboard-layers-panel/12-07-SPIKE-NOTES.md` returns >= 4 (4+ probes documented)
- [ ] `cd kinetica_bi && npx vitest run src/lib/wmsUrlBuilder.spec.ts` exits 0 (Wave 2 + new locking test pass; 43+ tests preserved)
- [ ] `cd kinetica_bi && npx tsc --noEmit` exits 0 (no type regressions)
- [ ] `grep -E 'criterion_3:\s*(PASS|BLOCKED)' .planning/phases/12-dashboard-layers-panel/12-VERIFICATION.md` returns 1 match (criterion_3 flipped from RED — Wave 3 outcome recorded)
- [ ] `grep -nE '^## GAP-12-C3 (Closed|Re-Verified — Still Open)' .planning/phases/12-dashboard-layers-panel/12-VERIFICATION.md` returns exactly 1 match
- [ ] `git status --porcelain .planning/phases/12-dashboard-layers-panel/12-0{1,2,3,4,5,6}-PLAN.md` returns empty (no prior-plan modifications)
- [ ] `git status --porcelain kinetica_bi/src/components/` returns empty (no UI / renderer source files modified)
</verification>

<success_criteria>
1. Wave 1 produces a reproducible CLI spike runner (`wmsFilterSpike.ts`) and a `12-07-SPIKE-NOTES.md` file documenting 4+ filter-param probes against a low-cardinality Kinetica table (NOT demo.nyctaxi), ending with a single `CONCLUSION:` line that locks either a working param + value format or a specific blocker reason.
2. Wave 2 applies the SPIKE-NOTES CONCLUSION verbatim to `wmsUrlBuilder.ts` — a single constant edit (and possibly a small value-format helper); spec covers the new param with a regression guard against the old QUERY name; 43+ existing tests still pass; tsc green. If the CONCLUSION was BLOCKED, Wave 2 is a no-op.
3. Wave 3 flips `criterion_3` in `12-VERIFICATION.md` from RED to PASS (success path) or to BLOCKED (blocked path), and appends a fully-evidenced `## GAP-12-C3 Closed` (or `## GAP-12-C3 Re-Verified — Still Open`) section.
4. Plans 12-01 through 12-06 are NOT modified.
5. No UI / renderer / config-panel / modal source files are modified — the entire fix is local to `wmsUrlBuilder.ts` (per Wave 2 acceptance criteria).
6. Full vitest suite remains green throughout (no regressions).
</success_criteria>

<output>
After completion, create `.planning/phases/12-dashboard-layers-panel/12-07-SUMMARY.md` documenting:
- Spike methodology (target table chosen, BBOX, filter column + value, why-not-demo.nyctaxi reasoning)
- Probe results table (paste the relevant section from 12-07-SPIKE-NOTES.md)
- The CONCLUSION verbatim
- Wave 2 source edits (FILTER_PARAM old vs. new value, value-format change if any, spec test added)
- Wave 3 walkthrough outcome (criterion_3 status before / after; sha1 evidence of tile difference if success path; blocker reason if blocked path)
- Locks preserved confirmation (M-01, M-02, M-03, AP-3)
- Whether the closure flips Phase 12 `overall_status` from `tech_debt` → `passed`, or leaves it at `tech_debt` (e.g. if Wave 3 was BLOCKED)
- Cross-phase note: confirm whether Phase 11's identical Caveat (`11-SPIKE-NOTES.md` § "Filter Param (QUERY) Effectiveness") is now also resolved in spirit, even though `11-SPIKE-NOTES.md` itself is not edited (historical artefact)
</output>
</content>
</invoke>