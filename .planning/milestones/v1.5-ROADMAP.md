# Roadmap: Kinetica BI

## Milestones

- ✅ **v1.0 Authentication & Per-User Access** — Phases 1-3 (shipped 2026-04-29) — see `milestones/v1.0-ROADMAP.md`
- ✅ **v1.1 OIDC SSO Support** — Phases 4-8 (shipped 2026-05-02) — see `milestones/v1.1-ROADMAP.md`
- ✅ **v1.2 Interactive Dashboards** — Phases 9-12 (shipped 2026-05-06) — see `milestones/v1.2-ROADMAP.md`
- ✅ **v1.3 Unified Dashboard Filtering** — Phases 13-17 (shipped 2026-05-07) — see `milestones/v1.3-ROADMAP.md`
- ✅ **v1.4 Map Info Popup** — Phases 18-24 (shipped 2026-05-11) — see `milestones/v1.4-ROADMAP.md`
- 🚧 **v1.5 Spatial filtering on map** — Phases 25-31 (in progress)

## Phases

<details>
<summary>✅ v1.0 Authentication & Per-User Access (Phases 1-3) — SHIPPED 2026-04-29</summary>

- [x] Phase 1: Encrypted server-side session store (4/4 plans) — completed 2026-04-28
- [x] Phase 2: Per-user credential passthrough on every Kinetica call (6/6 plans) — completed 2026-04-28
- [x] Phase 3: Auth failure UX + admin credential removal (6/6 plans) — completed 2026-04-28

Full details: `milestones/v1.0-ROADMAP.md`. Milestone summary: `MILESTONES.md`. Audit: `milestones/v1.0-MILESTONE-AUDIT.md` (status: tech_debt; 19/19 requirements; 5 deferred items).

</details>

<details>
<summary>✅ v1.1 OIDC SSO Support (Phases 4-8) — SHIPPED 2026-05-02</summary>

- [x] Phase 4: Schema + SessionStore Foundation (3/3 plans) — completed 2026-04-30
- [x] Phase 5: OIDC Module + Routes (4/4 plans) — completed 2026-05-01
- [x] Phase 6: requireAuth + Helper Credential Branch (4/4 plans) — completed 2026-05-01
- [x] Phase 7: Frontend AUTH_MODE Awareness (5/5 plans) — completed 2026-05-01
- [x] Phase 8: Boot Wipe + Hardening + Runbook (3/3 plans) — completed 2026-05-01

Full details: `milestones/v1.1-ROADMAP.md`. Milestone summary: `MILESTONES.md`. Audit: `milestones/v1.1-MILESTONE-AUDIT.md` (status: passed; 21/21 requirements; 4 informational tech-debt items).

</details>

<details>
<summary>✅ v1.2 Interactive Dashboards (Phases 9-12) — SHIPPED 2026-05-06</summary>

- [x] Phase 9: Filter Foundation (3/3 plans) — completed 2026-05-04
- [x] Phase 10: Existing-Chart Drill-Down (4/4 plans) — completed 2026-05-05
- [x] Phase 11: Map Chart (10/10 plans) — completed 2026-05-05
- [x] Phase 12: Dashboard Layers Panel (6/7 plans; plan 12-07 superseded) — completed 2026-05-06

Full details: `milestones/v1.2-ROADMAP.md`. Milestone summary: `MILESTONES.md` v1.2 entry. No formal `/gsd:audit-milestone` was run — pragmatic close with 12/12 FILT/DRILL/MAP requirements `[x]` and 4 known gaps (TD-V12-01..04) carried to v1.3 backlog. Top carry-forward: TD-V12-01 (WMS-QUERY tile filter does not narrow tiles — superseded; rework filtering wholesale in v1.3).

</details>

<details>
<summary>✅ v1.3 Unified Dashboard Filtering (Phases 13-17) — SHIPPED 2026-05-07</summary>

- [x] Phase 13: spikes-and-endpoint — Operator spike validation + net-new materialize endpoint (completed 2026-05-06)
- [x] Phase 14: filter-view-store — `useFilterViewStore` Zustand slice + client API helpers (completed 2026-05-06)
- [x] Phase 15: chart-filtering — FROM-swap in all chart renderers + dead-code deletion + lifecycle reset wiring (completed 2026-05-07)
- [x] Phase 16: map-filtering — LAYERS-swap in map renderer + wmsUrlBuilder cleanup + `_mv` cache-buster (completed 2026-05-07)
- [x] Phase 17: verification — E2E verification + 3 gap-closure cycles (closes TD-V12-01; TD-V12-04 partial) (completed 2026-05-07)

Full details: `milestones/v1.3-ROADMAP.md`. Milestone summary: `MILESTONES.md` v1.3 entry. No formal `/gsd:audit-milestone` was run — pragmatic close with 33/34 requirements `[x]` (1 partial: VERIFY-V13-01) and 3 known gaps (TD-V12-04, TD-V11-04, TD-V13-01) carried to v1.4.

</details>

<details>
<summary>✅ v1.4 Map Info Popup (Phases 18-24) — SHIPPED 2026-05-11</summary>

- [x] Phase 18: spatial-spike-and-endpoint (3/3 plans) — completed 2026-05-08
- [x] Phase 19: config-schema (2/2 plans) — completed 2026-05-08
- [x] Phase 20: info-selection-store (2/2 plans) — completed 2026-05-08
- [x] Phase 21: map-click-popup (3/3 plans) — completed 2026-05-08
- [x] Phase 22: config-ui (3/3 plans) — completed 2026-05-08
- [x] Phase 23: info-card (4/4 plans) — completed 2026-05-09
- [x] Phase 24: verification (6/6 plans, includes 3 gap-closure plans) — completed 2026-05-11

Full details: `milestones/v1.4-ROADMAP.md`. Milestone summary: `MILESTONES.md` v1.4 entry. No formal `/gsd:audit-milestone` was run — pragmatic close with `overall_status: passed` via live operator attestation; 19/20 requirements `Complete` (1 `Deferred`: SPATIAL-V14-03 via TD-V14-WKB-SPIKE inherited from Phase 18). Four UAT-discovered gaps (GAP-24-01-A, -01-B, -02-A, -06-A) all closed inline with code fixes + regression specs + live re-walk.

</details>

---

### 🚧 v1.5 Spatial filtering on map (Phases 25-31)

**Milestone Goal:** Users draw bbox / lasso / circle shapes directly on map widgets to filter the dashboard to that spatial region; multiple shapes OR within and AND with column filters via the v1.3 materialize pipeline.

- [x] **Phase 25: spatial-predicate-spike** — Operator-driven spike confirms Kinetica spatial predicates + commits findings; P1 gate for all SQL builder code (completed 2026-05-12)
- [x] **Phase 26: server-spatial-where** — Pure `spatialWhereClause.ts` module + `composeWhereClause` + `POST /api/filter/materialize` body extension + supertest coverage (completed 2026-05-12)
- [x] **Phase 27: spatial-filter-store** — `useSpatialFilterStore` Zustand slice + `spatialFilterVersion` counter + 5th-store lifecycle reset block (completed 2026-05-12)
- [x] **Phase 28: spatial-target-config** — `spatialTargets.ts` helper + `widget.config.spatialTargets` persistence + `MapConfigPanel` spatial targets editor UI (completed 2026-05-12)
- [x] **Phase 29: draw-and-shape** — `MapDrawToolbar` + OL Draw interactions (bbox/lasso/circle) + Effect 6 mode-guard + VectorLayer shape overlay + cross-map rendering + measurements (completed 2026-05-12)
- [x] **Phase 30: materialize-and-chips** — `AggregatedWidgetRenderer` spatial trigger extension + `materializeFilter` client helper extension + `FilterBar` spatial chips (completed 2026-05-13)
- [x] **Phase 31: verification** — End-to-end operator UAT covering all draw types, both spatial modes, both auth modes, chip removal, lifecycle reset, cross-phase integration (completed 2026-05-14)

---

## Phase Details

### Phase 13: spikes-and-endpoint
**Goal**: Architectural unknowns resolved and the server-side materialize primitive is live and tested
**Depends on**: Nothing (first v1.3 phase)
**Requirements**: SPIKE-V13-01, SPIKE-V13-02, SPIKE-V13-03, SPIKE-V13-04, VIEW-V13-01, VIEW-V13-02, VIEW-V13-03, VIEW-V13-04, VIEW-V13-05, VIEW-V13-06, VIEW-V13-07
**Success Criteria** (what must be TRUE):
  1. Operator has recorded spike findings for S1 (WMS LAYERS=view), S2 (DDL permission), S3 (error message format for expired view), and S4 (schema qualification) — each with a concrete pass/fail and its downstream consequence for the build
  2. `POST /api/filter/materialize` with non-empty filters issues `CREATE OR REPLACE MATERIALIZED VIEW _kbi_filt_u<...>_d<...>_t<...>_s<...> AS (SELECT * FROM <table> WHERE <clause>) USING TABLE PROPERTIES (TTL = 5)` via `kineticaSql` and returns `{ viewName, expiresAt }`
  3. `POST /api/filter/materialize` with empty filters issues `DROP TABLE IF EXISTS <viewName>` and returns `{ dropped: true }`; client can call it fire-and-forget on clear/logout/switch
  4. OIDC usernames with special characters (dots, at-signs, hyphens) are sanitized to Kinetica-safe alphanumeric+underscore before interpolation into the view name
  5. Supertest coverage passes for both `AUTH_MODE=password` and `AUTH_MODE=oidc` session contexts, asserts on DDL SQL string shape, view-name format, and 403 response when DDL permission is denied
**Plans**: 3 plans
- [x] 13-01-spike-runner-PLAN.md — Operator runs S1-S4 spikes against deployed Kinetica; writes 13-SPIKE-NOTES.md (SPIKE-V13-01..04) — completed 2026-05-06
- [x] 13-02-view-utils-PLAN.md — Pure utility modules: lib/viewNaming.ts (view-name builder + OIDC sanitization) + lib/whereClause.ts (server-side WHERE builder ported from v1.2 client) (VIEW-V13-03, VIEW-V13-06) — completed 2026-05-06
- [x] 13-03-endpoint-PLAN.md — Wire POST + DELETE /api/filter/materialize routes in index.ts + supertest coverage (password + OIDC modes) (VIEW-V13-01, VIEW-V13-02, VIEW-V13-04, VIEW-V13-05, VIEW-V13-07) — completed 2026-05-06

### Phase 14: filter-view-store
**Goal**: The client has a stable view-name store and API helpers that all widget renderers can consume
**Depends on**: Phase 13
**Requirements**: VSTORE-V13-01, VSTORE-V13-02, VSTORE-V13-03, VSTORE-V13-04
**Success Criteria** (what must be TRUE):
  1. `useFilterViewStore` Zustand slice exists with `views`, `expiresAt`, `materializeVersion`, and `materializing` fields per tableId; store resets cleanly between vitest runs via the existing Zustand reset shim (no state bleed across tests)
  2. `materializeFilter(args, signal?)` and `dropFilterView(args, signal?)` client functions follow the existing `apiFetch` + `throwForStatus` pattern and accept an AbortSignal so rapid filter changes can cancel in-flight materialize calls
  3. `AggregatedWidgetRenderer` is the sole trigger for materialize calls — a dedicated `materializeAbortRef` (separate from the chart-query AbortController) aborts any in-flight materialize before firing a new one on `filterVersion` change (NOTE: per CONTEXT.md user-lock + RESEARCH.md, renderer wiring is migrated to Phase 15 — Phase 14 ships dormant store + helper plumbing only)
  4. `useFilterStore` (existing v1.2 slice) is byte-for-byte unchanged — chip state, `filterVersion`, 10-cap, dedupe/replace, and `addFilter`/`removeFilter`/`clearFilters`/`reset` all behave identically to v1.2
**Plans**: 3 plans
- [ ] 14-01-PLAN.md — Create `useFilterViewStore` Zustand slice + spec (VSTORE-V13-01, VSTORE-V13-02 store-side portion)
- [ ] 14-02-PLAN.md — Add `materializeFilter` + `dropFilterView` helpers to `client.ts` + `client.spec.ts` (VSTORE-V13-04)
- [ ] 14-03-PLAN.md — Verification gate: confirm `useFilterStore` byte-for-byte unchanged + dormant-plumbing scope honored (VSTORE-V13-03)

### Phase 15: chart-filtering
**Goal**: Every chart and records-table widget queries filtered data via `FROM <view>` swap when filters are active, dead WHERE-injection code is gone, and all lifecycle reset hooks are wired
**Depends on**: Phase 14
**Requirements**: FILT-V13-01, FILT-V13-02, FILT-V13-03, FILT-V13-04, FILT-V13-05, LIFE-V13-01, LIFE-V13-02, LIFE-V13-03, LIFE-V13-04, LIFE-V13-05
**Success Criteria** (what must be TRUE):
  1. User clicks a bar/line/pie/scatter/table chart element → debounce fires → all widgets on the same table re-render using `FROM <view_name>` (not `FROM <raw_table>` with a WHERE clause); a per-widget "filtering..." badge appears during the materialize phase and disappears once chart data loads; existing chart data remains visible underneath during both phases
  2. `RecordsTableRenderer` (the second SQL consumer that bypasses `AggregatedWidgetRenderer`) also uses `FROM <view_name>` when a filter is active — row count in the records table narrows to match the filtered dataset
  3. When no filters are active (`useFilterStore.filters[tableId]` is empty), widgets query the raw table with zero materialize overhead; cold dashboard loads incur no additional round-trips
  4. After 5+ minutes idle with an active filter, the next chart interaction triggers silent re-materialize with no error state shown to the user (proactive `expiresAt` check clears stale view; reactive "not found" catch re-materializes then retries)
  5. User clicks logout or switches dashboards → `useFilterViewStore.reset()` fires alongside the existing `useFilterStore.reset()`, and a fire-and-forget `DROP TABLE IF EXISTS` is sent for each active view
  6. `tsc --noEmit` passes with zero errors after deletion of `injectWhereClause`, `buildWhereClause`, `escapeKineticaStringLiteral`, and `buildEqualityFilter` from `filterStore.ts`; `filterStore.spec.ts` test blocks for those functions are removed; full vitest suite is green
**Plans**: 5 plans
- [ ] 15-01-PLAN.md — DashboardContext provider + hook + DashboardOpen mount (FILT-V13-01 foundation)
- [ ] 15-02-PLAN.md — AggregatedWidgetRenderer materialize trigger + FROM-swap + FilteringBadge + atomic dead-code deletion + MapChartRenderer migration + FilterViewEntry.dashboardId extension (FILT-V13-01, FILT-V13-03, FILT-V13-04, FILT-V13-05)
- [ ] 15-03-PLAN.md — RecordsTableRenderer FROM-swap consumer (FILT-V13-02)
- [ ] 15-04-PLAN.md — TTL recovery: proactive expiresAt + reactive isViewNotFoundError max-1-retry (LIFE-V13-01, LIFE-V13-02)
- [ ] 15-05-PLAN.md — Lifecycle reset extensions: App.tsx logout + DashboardsPage dashboard-switch (LIFE-V13-03, LIFE-V13-04, LIFE-V13-05)

### Phase 16: map-filtering
**Goal**: Map tiles narrow to filtered data when a filter is active (closes TD-V12-01); `QUERY` param is gone from all WMS requests; `_mv` cache-buster ensures OL re-fetches on every materialize
**Depends on**: Phase 14
**Requirements**: MAP-V13-01, MAP-V13-02, MAP-V13-03, MAP-V13-04, MAP-V13-05, MAP-V13-06
**Success Criteria** (what must be TRUE):
  1. User applies a filter by clicking a chart → map tiles visibly narrow to only the matching spatial points/shapes (gated on SPIKE-V13-01 passing; if S1 failed, this criterion is deferred and replaced with a documented fallback note)
  2. User clears all filters → map tiles immediately revert to full unfiltered dataset (`LAYERS=<raw_table>`) on the next WMS GetMap request; no stale filtered tiles remain
  3. WMS GetMap requests in the Network tab contain `LAYERS=_kbi_filt_<...>` (view name) when a filter is active and `LAYERS=<schema.table>` when no filter is active; `QUERY` and `FILTER_PARAM` params are absent from all WMS requests
  4. Changing a filter to a different value on the same table (same view name, new view content after `CREATE OR REPLACE`) triggers a new WMS tile fetch — confirmed by `_mv` (materialize version) changing in the request URL even when `LAYERS` value stays the same
  5. v1.2 map lifecycle fixes are preserved verbatim: no double OL Map construction in StrictMode dev builds, no blank-tile flash on filter clear, ResizeObserver `updateSize` still fires on grid-cell mount
**Plans**: 1 plan (atomic per user-locked decision; 5 internal tasks across 4 waves)
- [ ] 16-01-PLAN.md — wmsUrlBuilder.ts signature change (drops whereClause, adds materializeVersion) + MapChartRenderer.tsx Effects 2+3 LAYERS-swap + viewsKey selector + isViewExpired helper + MapFilteringBadge component + DashboardsPage wiring (MAP-V13-01..06)

### Phase 17: verification
**Goal**: End-to-end verification documented and both v1.2 gaps (TD-V12-01 and TD-V12-04) are demonstrably closed
**Depends on**: Phase 15, Phase 16
**Requirements**: VERIFY-V13-01, VERIFY-V13-02
**Success Criteria** (what must be TRUE):
  1. A low-cardinality / spatially-spread test dataset exists (synthetic table or curated slice of `demo.nyctaxi`) where applying a filter visibly reduces the number of map tiles and chart data points at normal dashboard zoom — filter effect is unambiguous to the naked eye
  2. `VERIFICATION.md` documents: (a) filter click → chart data narrows → map tiles narrow → Clear All → both revert; (b) TTL expiry recovery verified (proactive and reactive paths); (c) supertest backend DDL assertions passing for both `AUTH_MODE=password` and `AUTH_MODE=oidc`; (d) vitest frontend coverage for FROM-swap logic and store behavior; all with explicit PASS / DEFERRED / FAIL status per criterion
**Plans**: 1 plan (atomic per user-locked decision; verification-only, no signature breaking change to demand multi-plan separation)
- [ ] 17-01-PLAN.md — Test fixture (synthetic CREATE TABLE on Kinetica) + comprehensive UAT walkthrough (4 user flows × 4 edge cases × 8 chart types) + TTL recovery verification (proactive DevTools + reactive Kinetica DROP + max-1-retry cap) + 17-VERIFICATION.md authoring + STATE/ROADMAP/REQUIREMENTS update + milestone-close handoff to /gsd:audit-milestone (VERIFY-V13-01, VERIFY-V13-02)


---

### Phase 25: spatial-predicate-spike
**Goal**: Operator-confirmed proof that the deployed Kinetica instance accepts spatial predicates for the two relevant modes (latlon + WKT) with drawn-shape WKT literals; spike findings committed and a go/no-go decision recorded — this gates all SQL builder code in Phase 26
**Depends on**: Nothing (first v1.5 phase; P1 gate for WHERE builder work)
**Requirements**: SPIKE-V15-01, SPIKE-V15-02
**Success Criteria** (what must be TRUE):
  1. Operator has executed SQL probes for STXY_WITHIN/STXY_CONTAINS (latlon mode) and ST_WITHIN/ST_INTERSECTS (WKT mode) against the deployed Kinetica instance with representative WKT literals — at minimum a 4-corner bbox, 64-vertex circle polygon, and 150-vertex lasso polygon — each probe recorded as PASS or FAIL with the exact SQL that was executed
  2. Spike findings, the chosen predicate names (e.g., STXY_WITHIN vs STXY_CONTAINS), and any Kinetica-version-specific caveats are committed to `phases/25-spike/SPIKE-NOTES.md` alongside the runner script so a future re-run is one command
  3. A clear decision record is present in SPIKE-NOTES.md: PASS → `buildSpatialOrBlock` in Phase 26 is authorized to use the confirmed predicate names; FAIL → escalation path (alternative predicate or defer) documented with operator sign-off
**Plans**: 1 plan
- [x] 25-01-spatial-predicate-spike-PLAN.md — Spike runner script (kinetica_bi/server/src/spatialPredicateSpike.ts, mirrors wkbSpike.ts at commit d458408 with production-parity 7-field /execute/sql payload) + npm-script wire-up + operator UAT run of 12-probe matrix (2 modes × 2 candidate predicates × 3 shapes; STXY_WITHIN/STXY_CONTAINS for latlon against demo.nyctaxi; ST_WITHIN/ST_INTERSECTS for WKT against ki_home.us_states.WKT) + 25-SPIKE-NOTES.md decision record with locked predicate names per mode OR NONE_ESCALATE → BLOCK_V15 escalation (SPIKE-V15-01, SPIKE-V15-02)

### Phase 26: server-spatial-where
**Goal**: The server can compose a combined spatial + column WHERE clause and the materialize endpoint accepts drawn shapes, backed by full supertest coverage — all SQL builder code is authorized only after Phase 25 PASS
**Depends on**: Phase 25 (PASS outcome required)
**Requirements**: WHERE-V15-01, WHERE-V15-02, WHERE-V15-03, WHERE-V15-04
**Success Criteria** (what must be TRUE):
  1. `buildSpatialOrBlock(shapes, target)` produces a correctly parenthesised OR chain — `(STXY_WITHIN(lon, lat, ST_GEOMFROMTEXT('...')) = 1 OR STXY_WITHIN(lon, lat, ST_GEOMFROMTEXT('...')) = 1)` for latlon mode and `(ST_INTERSECTS(geom_col, ST_GEOMFROMTEXT('...')) = 1 OR ...)` for WKT mode — confirmed by unit tests that assert the exact paren structure for 1-shape and 3-shape inputs (V15-P-07 mitigation)
  2. `composeWhereClause(columnClause, spatialClause)` correctly handles all four cases: both clauses → `(spatial) AND (col)`; spatial only → spatial clause; column only → column clause; neither → `1=1`
  3. `POST /api/filter/materialize` accepts `{ filters, spatialFilters, spatialTarget }` and builds a combined WHERE clause in the DDL; sending only `filters` (no spatialFilters) is backward-compatible with v1.3 callers
  4. Supertest coverage asserts OR-clause parenthesis correctness for 2-shape input, exercises column-only / spatial-only / combined / 0-shape pass-through, and covers both `AUTH_MODE=password` and `AUTH_MODE=oidc` sessions; WKB-mode target returns HTTP 501
**Plans**: 3 plans
- [ ] 26-01-spatial-where-builder-PLAN.md — Pure `spatialWhereClause.ts` module exporting `buildSpatialOrBlock` + `composeWhereClause` + types + `SpatialFilterWkbDeferredError`, with unit tests asserting V15-P-07 paren invariant + 4-case composition (WHERE-V15-01, WHERE-V15-02)
- [ ] 26-02-materialize-endpoint-PLAN.md — Extend `POST /api/filter/materialize` body with `{ spatialFilters?, spatialTarget? }`, 5-step validation chain with WKB→501 early-return, replace `buildServerWhereClause` call with `composeWhereClause` (WHERE-V15-03)
- [ ] 26-03-supertest-coverage-PLAN.md — New sibling spec `routes.filter-materialize-spatial.spec.ts` mirroring v1.3 supertest auth fixtures; 26 tests (13 × 2 AUTH_MODE blocks) covering 1/2/3-shape paren correctness, 4-case composition, pair-completeness 400s, tableId mismatch 400, WKB→501 with zero kineticaSql calls (WHERE-V15-04)

### Phase 27: spatial-filter-store
**Goal**: The frontend has a session-only Zustand slice that holds committed drawn shapes, a version counter consumed by the materialize trigger, and is wired into the 5-store lifecycle reset block — ships dormant (no OL or FilterBar consumer until Phases 29-30)
**Depends on**: Nothing (pure Zustand; no OL dependency; can land in parallel with Phase 26)
**Requirements**: STORE-V15-01, STORE-V15-02, STORE-V15-03, STORE-V15-04
**Success Criteria** (what must be TRUE):
  1. `useSpatialFilterStore` exists with shape `{ shapes: Shape[], spatialFilterVersion: number }` and actions `addShape`, `removeShape(id)`, `clearAll()`, `reset()`; `spatialFilterVersion` increments on every mutation (add / remove / clearAll) and resets to 0 on `reset()`
  2. Auto-generated shape labels follow `{Type} {N}` (e.g., "Bbox 1", "Circle 2", "Lasso 3") with N as sequential per-session draw order; N resets after dashboard-switch or logout
  3. `reset()` is wired as the 5th call in the existing lifecycle reset block at both `App.tsx` UNAUTHORIZED handler and `DashboardsPage.tsx` DashboardOpen cleanup — vitest spec confirms all 5 stores reset in order
  4. Store passes vitest coverage: no state bleeding across test runs via the existing Zustand reset shim; `removeShape` with a non-existent ID is a no-op; `clearAll` when `shapes.length === 0` does not increment `spatialFilterVersion`
**Plans**: 2 plans
- [ ] 27-01-spatial-filter-store-PLAN.md — Create `useSpatialFilterStore` Zustand slice + sibling spec + REQUIREMENTS.md path correction (STORE-V15-01, STORE-V15-02, STORE-V15-03)
- [ ] 27-02-lifecycle-reset-wiring-PLAN.md — Wire 5th-store reset at App.tsx UNAUTHORIZED + DashboardsPage.tsx DashboardOpen cleanup + extend specs to assert all 5 stores reset (STORE-V15-04)

### Phase 28: spatial-target-config
**Goal**: Map widget configs can persist explicit spatial filter target entries (table + spatial mode + columns), a helper library validates eligibility, and the MapConfigPanel UI exposes an add/remove editor — independent of the draw UX and can land before or after Phase 29
**Depends on**: Phase 27 (SpatialTarget type; shares eligibility predicate logic)
**Requirements**: TARGET-V15-01, TARGET-V15-02, TARGET-V15-03
**Success Criteria** (what must be TRUE):
  1. `widget.config.spatialTargets` is persisted via the existing `PATCH /api/widgets/:id` endpoint; loading a map widget that was saved with `spatialTargets` correctly restores the array; legacy v1.4 widgets without `spatialTargets` default to `[]` (spatial filtering inert — no data migration needed)
  2. `isSpatialTargetEligible(target)` returns `false` for WKB-mode targets and for incomplete latlon/WKT targets (missing column fields); returns `true` only for fully-configured latlon or WKT targets
  3. `MapConfigPanel` "Spatial filter targets" section lets the operator add rows (table picker from associatedTables + spatial mode picker + column picker), remove rows, and see an inline warning on WKB-mode rows ("WKB spatial mode not yet supported — deferred"); changes persist via the existing 300ms-debounced auto-save without requiring a new server endpoint
**Plans**: 2 plans
- [ ] 28-01-spatial-targets-helper-PLAN.md — Create `kinetica_bi/src/lib/spatialTargets.ts` (SpatialMode + SpatialTarget types byte-parity with server lines 54-81 + getSpatialTargets + isSpatialTargetEligible helpers) + sibling spec + extend `MapWidgetConfig` in `wmsUrlBuilder.ts` with `spatialTargets?: SpatialTarget[]` (TARGET-V15-01 type half + TARGET-V15-02)
- [ ] 28-02-map-config-panel-section-PLAN.md — Extend `ConfigPanelProps` with optional `tables?` + thread through `ChartConfigPanel` Custom slot; add "Spatial filter targets" section to `MapConfigPanel.tsx` (always-visible header + `+` add affordance + per-row table/mode/column pickers + WKB warning + incomplete indicator); extend `MapConfigPanel.spec.tsx` with 16 new tests (TARGET-V15-01 persistence + TARGET-V15-03 UI)

### Phase 29: draw-and-shape
**Goal**: Users can draw bbox, lasso, and circle shapes on any map widget using a per-map toolbar; shapes appear as a Vector overlay on every map in the dashboard; live and persistent measurement labels are shown; shape removal via click+Delete is wired — this is the most complex phase and V15-P-01 mode-guard must be its first code change
**Depends on**: Phase 27 (store — read shapes for overlay; write on draw-end)
**Requirements**: DRAW-V15-01, DRAW-V15-02, DRAW-V15-03, DRAW-V15-04, DRAW-V15-05, DRAW-V15-06, SHAPE-V15-01, SHAPE-V15-02, SHAPE-V15-03, SHAPE-V15-04
**Success Criteria** (what must be TRUE):
  1. The Effect 6 mode-guard (`if (drawMode !== 'pan' && drawMode !== 'info') return`) is the first change in this phase — confirmed in the plan as the initial task before any Draw interaction code is introduced; the info popup does not fire when the user is in bbox/lasso/circle mode (V15-P-01 mitigation)
  2. The `MapDrawToolbar` renders 5 mutually-exclusive mode buttons (Pan / Info / Bbox / Lasso / Circle) plus a trash "Clear all" icon (visible only when `shapes.length > 0`); clicking a draw-mode button changes the cursor to crosshair and the previously active mode shows a depressed/highlighted style; Info mode auto-restores after a draw commits or ESC is pressed
  3. Drawing a bbox (click-drag), lasso (freehand), or circle (click-drag) produces a shape committed to `useSpatialFilterStore` with the correct WKT in EPSG:4326 coordinates (never raw EPSG:3857); near-zero shapes (< 10 px² screen area) are rejected with a toast; lasso polygons are simplified via Douglas-Peucker before store commit (V15-P-03 mitigation); pressing ESC mid-draw aborts without committing
  4. Every committed shape renders as a semi-transparent filled polygon on the VectorLayer of every map widget in the dashboard simultaneously (cross-map shape visibility); a persistent measurement label (e.g., "2.5 km", "5km × 3km", "12.4 km²") appears anchored to the shape's centroid via `ol/Overlay` and follows pan/zoom automatically
  5. A live measurement tooltip follows the cursor during active drawing for all three draw types — measurements use `ol/sphere.getDistance` / `getArea` (never raw EPSG:3857 values — V15-P-04 mitigation); clicking a committed shape and pressing Delete removes it from the store; removal is mode-gated to draw modes only (not Info mode)
**Plans**: 5 plans
- [ ] 29-01-mode-guard-and-foundation-PLAN.md — V15-P-01 Effect 6 mode-guard (FIRST code change), DrawMode state + drawModeRef + previousModeRef + cursor useEffect in MapChartRenderer, lib/shapeDraw.ts foundation (DrawMode + formatDistance + formatArea), Font Awesome deps install (DRAW-V15-02, DRAW-V15-03)
- [ ] 29-02-map-draw-toolbar-PLAN.md — Standalone MapDrawToolbar.tsx React overlay component with 5 mode buttons + conditional Trash + Font Awesome icons + sibling spec + CSS classes appended to global.css (DRAW-V15-01)
- [ ] 29-03-vector-layer-and-shape-sync-PLAN.md — VectorLayer/VectorSource setup in Effect 1, Effect 7 atomic store→features sync + per-shape persistent measurement overlays, MapDrawToolbar JSX mount in MapChartRenderer, .shape-measurement-pill CSS class (SHAPE-V15-01, SHAPE-V15-02, SHAPE-V15-03)
- [ ] 29-04-draw-interaction-and-pipeline-PLAN.md — lib/shapeDraw helpers (buildDrawInteraction + computeMeasurement + isDegenerateExtent), Effect 8 OL Draw interaction mount per mode + drawstart live tooltip + drawend pipeline (validate→simplify lasso→ol/sphere measure→WKT EPSG:4326→addShape→remove sketch feature→auto-restore mode) + ESC keydown abort (DRAW-V15-04, DRAW-V15-05, DRAW-V15-06)
- [ ] 29-05-selection-and-delete-PLAN.md — selectedShapeId useState + selectedShapeIdRef mirror, selection-click useEffect (mode-gated to draw modes; uses forEachFeatureAtPixel hit-test on vectorLayer only), Delete/Backspace keydown handler, selection-clear-on-mode-switch effect, selection-clear-on-shape-removed effect, VectorLayer style function updated to render selected shape with 4px outline + white halo (SHAPE-V15-04)

### Phase 30: materialize-and-chips
**Goal**: `AggregatedWidgetRenderer` fires the existing materialize pipeline whenever shapes change (spatial OR column), the `materializeFilter` client helper carries spatial args to the server, and `FilterBar` shows spatial chips mixed with column chips — dashboard tile filtering becomes live end-to-end
**Depends on**: Phase 26 (server WHERE builder), Phase 27 (store), Phase 28 (eligibility helpers)
**Requirements**: MAT-V15-01, MAT-V15-02, MAT-V15-03, CHIP-V15-01, CHIP-V15-02
**Success Criteria** (what must be TRUE):
  1. Drawing a shape on any map triggers `AggregatedWidgetRenderer`'s Effect 1 (via `spatialFilterVersion` dep) → 300ms debounce → `POST /api/filter/materialize` with `spatialFilters` + `spatialTarget` → DDL with combined WHERE clause → view name returned → `_mv` cache-buster increments → WMS tiles re-render with spatially-filtered data; the existing single-materialize-trigger invariant (no trigger in `MapChartRenderer`) is preserved
  2. `FilterBar` shows spatial chips in the same row as column-filter chips; each chip displays `{Type} {N}` with the measurement as secondary text (e.g., "Bbox 1 (5km × 3km)", "Circle 1 (2.5 km)", "Lasso 1 (12.4 km²)"); clicking chip × removes the shape, triggers re-materialize, and hides the chip
  3. The per-map "Clear all spatial filters" toolbar button (DRAW-V15-01) calls `clearAll()` which is dashboard-scoped — all shapes are removed, all chips disappear, and (if no column filters remain) the materialized view is dropped and tiles revert to unfiltered
  4. WKB-mode spatial targets are silently skipped at materialize time (`isSpatialTargetEligible` gate); the server returns HTTP 501 as a defense-in-depth guard if a WKB target is sent (mirrors v1.4 info-query WKB deferral)
**Plans**: 3 plans
- [ ] 30-01-foundation-PLAN.md — aggregateSpatialTargetsByTable helper + DashboardContext widgets extension + MaterializeFilterArgs payload extension (MAT-V15-02)
- [ ] 30-02-materialize-trigger-PLAN.md — AggregatedWidgetRenderer 5th dep + targetsByTable memo + combined payload + extended DROP guard; MapChartRenderer untouched (MAT-V15-01, MAT-V15-03)
- [ ] 30-03-filterbar-chips-PLAN.md — FilterBar spatial chips inside targeted table rows + per-table Clear all global shape nuke (CHIP-V15-01, CHIP-V15-02)

### Phase 31: verification
**Goal**: End-to-end operator UAT confirms spatial filtering works across all draw types, spatial modes, auth modes, chip removal paths, and lifecycle reset scenarios; the v1.3 materialize pipeline integration is verified with spatial shapes active
**Depends on**: Phase 29, Phase 30
**Requirements**: VERIFY-V15-01
**Success Criteria** (what must be TRUE):
  1. Operator completes 5 draw flows: (a) draw 1 bbox shape → tiles narrow; (b) draw 3 shapes of mixed types → tiles show OR-combined spatial filter; (c) remove one via chip × → filter updates; (d) remove one via click-shape+Delete → filter updates; (e) "Clear all" toolbar → all shapes gone, tiles unfiltered; all flows verified in both latlon and WKT spatial modes
  2. Cross-map shape visibility confirmed: a shape drawn on Map A appears on Map B simultaneously; chip label shows correct measurement for each shape type
  3. Lifecycle reset verified: logout clears all shapes (FilterBar spatial chips disappear, store is empty); dashboard-switch clears all shapes; Info popup still functions normally inside a spatially-filtered view (spatial filter + info click compose correctly)
  4. Both auth modes (password + OIDC) exercise the full draw → materialize → filtered tiles path; frontend vitest green; tsc clean; backend supertest green for spatial WHERE composition (at minimum OR-clause paren correctness for multi-shape inputs)
**Plans**: TBD

---

## Progress

| Phase | Milestone | Plans Complete | Status | Completed |
|-------|-----------|----------------|--------|-----------|
| 1. Encrypted session store | v1.0 | 4/4 | Complete | 2026-04-28 |
| 2. Per-user credential passthrough | v1.0 | 6/6 | Complete | 2026-04-28 |
| 3. Auth failure UX + admin removal | v1.0 | 6/6 | Complete | 2026-04-28 |
| 4. Schema + SessionStore Foundation | v1.1 | 3/3 | Complete | 2026-04-30 |
| 5. OIDC Module + Routes | v1.1 | 4/4 | Complete | 2026-05-01 |
| 6. requireAuth + Helper Credential Branch | v1.1 | 4/4 | Complete | 2026-05-01 |
| 7. Frontend AUTH_MODE Awareness | v1.1 | 5/5 | Complete | 2026-05-01 |
| 8. Boot Wipe + Hardening + Runbook | v1.1 | 3/3 | Complete | 2026-05-01 |
| 9. Filter Foundation | v1.2 | 3/3 | Complete | 2026-05-04 |
| 10. Existing-Chart Drill-Down | v1.2 | 4/4 | Complete | 2026-05-05 |
| 11. Map Chart | v1.2 | 10/10 | Complete | 2026-05-05 |
| 12. Dashboard Layers Panel | v1.2 | 6/7 | Complete | 2026-05-06 |
| 13. spikes-and-endpoint | v1.3 | 3/3 | Complete | 2026-05-06 |
| 14. filter-view-store | v1.3 | 3/3 | Complete | 2026-05-06 |
| 15. chart-filtering | v1.3 | 5/5 | Complete | 2026-05-07 |
| 16. map-filtering | v1.3 | 1/1 | Complete | 2026-05-07 |
| 17. verification | v1.3 | 1/1 + 3 gap-closure | Complete | 2026-05-07 |
| 18. spatial-spike-and-endpoint | v1.4 | 3/3 | Complete | 2026-05-08 |
| 19. config-schema | v1.4 | 2/2 | Complete | 2026-05-08 |
| 20. info-selection-store | v1.4 | 2/2 | Complete | 2026-05-08 |
| 21. map-click-popup | v1.4 | 3/3 | Complete | 2026-05-08 |
| 22. config-ui | v1.4 | 3/3 | Complete | 2026-05-08 |
| 23. info-card | v1.4 | 4/4 | Complete | 2026-05-09 |
| 24. verification | v1.4 | 6/6 (incl. 3 gap-closure) | Complete | 2026-05-11 |
| 25. spatial-predicate-spike | 1/1 | Complete    | 2026-05-12 | - |
| 26. server-spatial-where | 3/3 | Complete    | 2026-05-12 | - |
| 27. spatial-filter-store | 2/2 | Complete    | 2026-05-12 | - |
| 28. spatial-target-config | 2/2 | Complete    | 2026-05-12 | - |
| 29. draw-and-shape | 5/5 | Complete    | 2026-05-12 | - |
| 30. materialize-and-chips | 3/3 | Complete    | 2026-05-13 | - |
| 31. verification | v1.5 | Complete    | 2026-05-14 | - |
