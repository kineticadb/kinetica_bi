---
phase: 69-verification-live-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/69-verification-live-uat/69-01-AUTOMATED-GATES.md
autonomous: true
requirements: [VERIFY-V113-01, CALUX-V113-03]

must_haves:
  truths:
    - "Frontend vitest is 100% green run FROM packages/web (cd packages/web && npx vitest run — NEVER --root from repo root), >= 2373 tests / >= 104 files, 0 failures"
    - "web tsc --noEmit -p tsconfig.json clean (exit 0) AND server tsc --noEmit -p tsconfig.json clean (exit 0) as SEPARATE gates"
    - "server vitest failing FILE set is IDENTICAL to the Phase 64 baseline {auth.oidc, auth.routes, boot.hardening, boot.wipe, bootstrap, db.smoke, oidc.module, routes.wms} (v1.13 is frontend-only → server source untouched → no new regressions)"
    - "The locked invariants are RE-ASSERTED via the existing CI gates: theme-guard.spec.ts (no raw hex in CalendarRenderer.tsx / CalendarConfigPanel.tsx — both NON-allowlisted in src/styles/theme-guard.spec.ts) AND CalendarRenderer.spec.tsx Test 0 (source imports no materializeFilter/dropFilterView/fromSwap) — both are part of the frontend vitest suite, so a green suite proves them"
    - "The targeted calendar spec group (calendarBin, buildCalendarSql, calendarColorScale, calendarGapFill, calendarLayout, calendarBuckets, CalendarConfigPanel, CalendarRenderer) all pass — proving the v1.13 chain under test incl. the 68.2 per-group gap-fill regression suites"
    - "The live Kinetica DATE_TRUNC('week') spike is RE-ATTEMPTED best-effort; if auth-blocked (REAUTH_REQUIRED / .env security-prohibited) it is recorded NOT-RUN and never blocks the close (inferWeekAnchorDow makes the anchor moot)"
  artifacts:
    - path: ".planning/phases/69-verification-live-uat/69-01-AUTOMATED-GATES.md"
      provides: "SC1 automated-gate evidence record consumed by 69-02 §0 + 69-03"
      contains: "overall_verdict"
  key_links:
    - from: "69-01-AUTOMATED-GATES.md"
      to: "Phase 64 baseline failing-file set (TD-V16-TEST-ISOLATION)"
      via: "set-IDENTICAL assertion table (server vitest)"
      pattern: "auth.oidc|auth.routes|boot.hardening|boot.wipe|bootstrap|db.smoke|oidc.module|routes.wms"
---

<objective>
Run the v1.13 SC1 automated gates fresh against HEAD, re-assert the locked invariants via the existing CI gates, best-effort re-attempt the live Kinetica `DATE_TRUNC('week')` spike, and record everything in `69-01-AUTOMATED-GATES.md` — the evidence file the operator cites in §0 of the UAT (69-02) and that 69-03 compiles into the verification record.

Purpose: SC1 of the v1.13 milestone gate is a set of deterministic automated gates (frontend vitest 100% from packages/web, web + server tsc clean, server vitest set-based ≡ Phase 64 baseline). v1.13 is FRONTEND-ONLY — no server source changed — so the server gate should match the Phase 64 baseline trivially; verify and record. The locked invariants (theme-guard no-raw-hex + static no-materialize/no-fromSwap grep) are already baked into the frontend vitest suite, so a green suite proves them — cite them explicitly. Mirrors 64-01-AUTOMATED-GATES.md.

Output: `.planning/phases/69-verification-live-uat/69-01-AUTOMATED-GATES.md` with `overall_verdict: ALL PASS` (or a clearly-recorded failure if a deterministic gate is red).

NO feature code. This plan only RUNS gates + a best-effort spike and RECORDS results. If a deterministic gate is red, record it as-is and surface it — do NOT fix code here (gap fixes go to a separate 69.x decimal phase). The spike NEVER blocks: auth-blocked → record NOT-RUN.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- FORMAT PRECEDENT — reuse this structure exactly (frontmatter overall_verdict, Gate Results table, per-gate detail, server set-IDENTICAL table, targeted-spec subsections, closing verdict block) -->
@.planning/phases/64-verification-live-uat/64-01-AUTOMATED-GATES.md

<!-- THE SPIKE TO RE-ATTEMPT (best-effort) — the exact probe queries + the auth-blocked NOT-RUN fallback -->
@.planning/phases/68.2-calendar-week-anchor-spike-per-group-date-range-gap-fill/68.2-02-SPIKE.md

<gates_reality>
<!-- Locked test-gate policy for SC1 (ROADMAP §Phase 69 SC1 + v1.13 locked invariants line 77). -->
- Frontend vitest is DETERMINISTIC → must be 100% green. Run FROM `packages/web` (`cd packages/web && npx vitest run`). DO NOT use `--root` from repo root — that doubles src/src paths and breaks cwd-relative specs (TEST CWD PITFALL). Baseline per 69-CONTEXT = 2373 passing across 104 files; count must be >= 2373 with 0 failures.
- Server vitest is FLAKY → SET-BASED gate ONLY, and v1.13 is FRONTEND-ONLY so the failing FILE set must be IDENTICAL to the Phase 64 baseline {auth.oidc, auth.routes, boot.hardening, boot.wipe, bootstrap, db.smoke, oidc.module, routes.wms}. NEVER assert a fixed pass-count. Any failing file OUTSIDE that set = a new server regression = gate RED.
- web tsc AND server tsc are SEPARATE gates: `cd packages/web && npx tsc --noEmit -p tsconfig.json` and `cd packages/server && npx tsc --noEmit -p tsconfig.json`, each must exit 0.
- v1.13 is FRONTEND-ONLY: server source untouched. `git diff --name-only -- packages/server` is context (expect empty); flag anything unexpected.
</gates_reality>

<locked_invariants_are_in_the_suite>
<!-- These two locked invariants are NOT separate scripts — they are spec files inside the frontend vitest suite. A green suite RE-ASSERTS them. Cite them explicitly in the record. -->
- THEME-GUARD: `packages/web/src/styles/theme-guard.spec.ts` recursively scans `src/components/**/*.tsx` for raw hex. `CalendarRenderer.tsx` and `CalendarConfigPanel.tsx` are NOT on the allowlist (the allowlist is the color-authoring files: TimelineRenderer, NumericLineRenderer, ChartConfigPanel, MapChartRenderer, TimelineConfigPanel, NumericLineConfigPanel, …) — so any raw hex in the calendar files fails this gate. A passing theme-guard.spec.ts proves "no raw hex in CalendarRenderer/CalendarConfigPanel".
- STATIC NO-MATERIALIZE / NO-FROMSWAP GREP: `packages/web/src/components/charts/CalendarRenderer.spec.tsx` "Test 0 (static invariant): source does NOT import materializeFilter|dropFilterView|fromSwap" reads the renderer source with readFileSync and asserts the import lines do NOT match `/materializeFilter|dropFilterView|fromSwap/`. A passing CalendarRenderer.spec.tsx proves the sole-materialize-trigger + no-fromSwap invariants.
- BOTH run inside `cd packages/web && npx vitest run` — confirm both files are GREEN in the full run and record them as the invariant re-assertion.
</locked_invariants_are_in_the_suite>

<targeted_calendar_specs>
<!-- The v1.13 calendar spec files to record as a targeted green group (proves the chain under test, incl. the 68.2 per-group gap-fill regression). Run FROM packages/web. -->
- src/lib/calendarBin.spec.ts            (computeCellBounds half-open buckets + valid-combo/DATE_TRUNC-unit/LIMIT constants)
- src/lib/buildCalendarSql.spec.ts       (two-level DATE_TRUNC builder, FROM resolved before string) — run if present
- src/lib/calendarColorScale.spec.ts     (computeDomain + 5-bucket quantize + palette resolver)
- src/lib/calendarGapFill.spec.ts        (per-group gap-fill: in-range grey, out-of-range blank; format-agnostic bucket-key lookup — 4f4ef7c)
- src/lib/calendarLayout.spec.ts         (block layout + WEEK_START + week×hour punchcard — 0a9d9f8)
- src/lib/calendarBuckets.spec.ts        (enumerateGroupBuckets — all 8 combos incl. leap; inferWeekAnchorDow anchor-agnostic — 90c8f3b)
- src/components/charts/CalendarConfigPanel.spec.tsx  (dependent domain/subdomain, cap, Display section toggles — 344c274)
- src/components/charts/CalendarRenderer.spec.tsx     (Test 0 static invariant + fetch/FROM-precedence + drill + reactive color domain)
</targeted_calendar_specs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the SC1 gates + targeted calendar spec group, best-effort re-attempt the week-anchor spike, capture all raw output</name>
  <read_first>
    - .planning/phases/64-verification-live-uat/64-01-AUTOMATED-GATES.md (the exact record format to mirror — Gate Results table, per-gate detail, server set-IDENTICAL table; the Phase 64 baseline failing-file set)
    - .planning/phases/68.2-calendar-week-anchor-spike-per-group-date-range-gap-fill/68.2-02-SPIKE.md (the spike Query A + the REAUTH_REQUIRED auth barrier + the .env-prohibited constraint + the NOT-RUN fallback)
  </read_first>
  <files>(no source files — this task runs commands and captures output for Task 2)</files>
  <action>
    Capture HEAD commit: `git rev-parse --short HEAD`.

    Run each gate and capture the exact tail/summary output (tests, files, failures, exit code):

    1. Frontend vitest (DETERMINISTIC, 100% bar — RUN FROM packages/web, NOT --root):
       `cd packages/web && npx vitest run`
       Record: tests passed/total, files passed/total, failures. MUST be 0 failures and >= 2373 tests / >= 104 files.
       In the SAME run output, locate and confirm GREEN: `src/styles/theme-guard.spec.ts` (no-raw-hex invariant) AND `src/components/charts/CalendarRenderer.spec.tsx` (Test 0 static no-materialize/no-fromSwap). These are the locked-invariant re-assertions.

    2. Web tsc (separate gate):
       `cd packages/web && npx tsc --noEmit -p tsconfig.json` — record exit code (must be 0, no output).

    3. Server tsc (separate gate):
       `cd packages/server && npx tsc --noEmit -p tsconfig.json` — record exit code (must be 0, no output).

    4. Server vitest (SET-BASED gate — failing-file set must EQUAL the Phase 64 baseline):
       `cd packages/server && npx vitest run`
       Record the list of FAILING test FILES (NOT the pass-count). Assert the set is IDENTICAL to the Phase 64 baseline {auth.oidc, auth.routes, boot.hardening, boot.wipe, bootstrap, db.smoke, oidc.module, routes.wms}. Any file OUTSIDE that set = new server regression = RED (should not happen — v1.13 is frontend-only).

    5. Targeted v1.13 CALENDAR spec group (proves the calendar chain under test — run FROM packages/web; only list files that exist):
       `cd packages/web && npx vitest run src/lib/calendarBin.spec.ts src/lib/calendarColorScale.spec.ts src/lib/calendarGapFill.spec.ts src/lib/calendarLayout.spec.ts src/lib/calendarBuckets.spec.ts src/components/charts/CalendarConfigPanel.spec.tsx src/components/charts/CalendarRenderer.spec.tsx`
       (also include `src/lib/buildCalendarSql.spec.ts` if `ls` shows it exists). Record tests/files passed; all must be green.

    6. Source-tree clean guard (v1.13 frontend-only, all phases committed):
       `git status --porcelain -- packages/server packages/web` and `git diff --name-only -- packages/server packages/web`.
       Expect a clean working tree for source (this phase produces DOCS only). Record verbatim; flag any unexpected uncommitted source diff. Confirm `git diff --name-only -- packages/server` is EMPTY (server source untouched in v1.13).

    7. BEST-EFFORT week-anchor spike re-attempt (NEVER blocks):
       a. `curl -s http://localhost:4000/api/health` — record whether the server is up.
       b. `curl -s -X POST http://localhost:4000/api/sql -H "Content-Type: application/json" -d '{"sql": "SELECT DATE_TRUNC('"'"'week'"'"', TIMESTAMP '"'"'2024-01-01 00:00:00'"'"') AS wk"}'` — record the raw response.
       Interpretation: result `2024-01-01` → Monday-ISO (WEEK_START=1 confirmed); result `2023-12-31` → Sunday; `REAUTH_REQUIRED` / any auth error → NOT-RUN.
       DO NOT read, print, echo, or commit `packages/server/.env`. If a session is required and cannot be obtained without .env credentials, record NOT-RUN with the exact barrier (mirrors Phase 65-02 / 68.2-02). Note that `inferWeekAnchorDow` makes the anchor empirically moot regardless → CALUX-V113-03 is functionally complete either way.

    Do NOT modify any source. If a deterministic gate (frontend vitest, web tsc, server tsc) is RED, or a server file OUTSIDE the baseline set fails, record the failure verbatim — do not attempt a fix.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run 2>&1 | tail -5 ; npx tsc --noEmit -p tsconfig.json ; echo "web tsc exit=$?" ; cd ../server && npx tsc --noEmit -p tsconfig.json ; echo "server tsc exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Frontend vitest output captured (run from packages/web): 0 failures, >= 2373 tests, >= 104 files
    - theme-guard.spec.ts AND CalendarRenderer.spec.tsx (Test 0) confirmed GREEN in the full run and noted as the locked-invariant re-assertion
    - web tsc exit 0; server tsc exit 0 (both captured, each via `-p tsconfig.json` from its own package dir)
    - Server vitest failing-file list captured and confirmed IDENTICAL to the Phase 64 baseline set {auth.oidc, auth.routes, boot.hardening, boot.wipe, bootstrap, db.smoke, oidc.module, routes.wms}
    - Targeted v1.13 calendar spec group captured all-green (every existing calendar spec file)
    - `git diff --name-only -- packages/server` captured and EMPTY (server source untouched); `git status --porcelain` for web+server captured (expected clean)
    - Week-anchor spike re-attempted: response captured verbatim → recorded RAN(anchor=…) or NOT-RUN(barrier=REAUTH_REQUIRED); `.env` NEVER read
  </acceptance_criteria>
  <done>Raw output for all gates + the targeted calendar group + the clean-tree guard + the best-effort spike attempt is captured and ready to transcribe into the record.</done>
</task>

<task type="auto">
  <name>Task 2: Author 69-01-AUTOMATED-GATES.md mirroring the 64-01 format</name>
  <read_first>
    - .planning/phases/64-verification-live-uat/64-01-AUTOMATED-GATES.md (clone its frontmatter + section structure exactly)
  </read_first>
  <files>.planning/phases/69-verification-live-uat/69-01-AUTOMATED-GATES.md</files>
  <action>
    Write `69-01-AUTOMATED-GATES.md` cloning the 64-01 structure:

    Frontmatter:
    ```
    ---
    automated_gates_ref: "69-01"
    phase: 69-verification-live-uat
    recorded_on: "<ISO timestamp>"
    commit: "<short HEAD>"
    overall_verdict: ALL PASS   # or a recorded failure if a deterministic gate is red
    week_anchor_spike: <RAN(anchor=Monday|Sunday) | NOT-RUN(REAUTH_REQUIRED)>
    ---
    ```

    Body sections (reuse 64-01 headings):
    - Title "# Phase 69: SC1 Automated Gate Results" + Recorded/HEAD/Operator (RPereira@kinetica.com)/Purpose line ("Evidence record for SC1 of the v1.13 milestone-gate verification. Consumed by 69-02 §0 (UAT preconditions) and 69-03 (compiled verification).").
    - Gate Results summary table: rows for `frontend_vitest`, `web_tsc`, `server_tsc`, `server_vitest_setgate`, `locked_invariants_reassert` (theme-guard + static-grep), `targeted_v113_calendar_specs`, `source_tree_clean_guard`, `week_anchor_spike` — each with Command / Result / Numbers-or-Notes columns, transcribed from Task 1.
    - Gate Detail subsections (one per gate) with the exact command, observed output, exit code, verdict.
    - Server vitest SET-BASED subsection: a "Failing test FILES" table with an "In Phase 64 baseline / TD-V16-TEST-ISOLATION?" YES/NO column for each failing file, plus an explicit "IDENTICAL to Phase 64 baseline" check sentence. State plainly that v1.13 is frontend-only so the set must match the Phase 64 baseline exactly; the exact failing test-COUNT is irrelevant per the set-based policy.
    - Locked Invariants Re-Assertion subsection: state that theme-guard.spec.ts (no raw hex in CalendarRenderer.tsx / CalendarConfigPanel.tsx — both NON-allowlisted) and CalendarRenderer.spec.tsx Test 0 (no materializeFilter/dropFilterView/fromSwap import) are BOTH part of the green frontend suite, so the suite re-asserts both locked invariants. List both file results.
    - Targeted v1.13 calendar specs subsection: a per-file pass table for the calendar spec files; call out the 68.2 regression coverage (per-group gap-fill in calendarGapFill.spec.ts, week×hour punchcard in calendarLayout.spec.ts, anchor-agnostic inferWeekAnchorDow in calendarBuckets.spec.ts).
    - Source-tree clean guard note: v1.13 is frontend-only; `git diff --name-only -- packages/server` is EMPTY (zero server source diff); this verification phase produces DOCS only.
    - Week-Anchor Spike subsection: record RAN (anchor + interpretation + any code-change needed) or NOT-RUN (barrier = REAUTH_REQUIRED, .env security-prohibited as in Phase 65-02 / 68.2-02). Explicitly state: `inferWeekAnchorDow` makes the anchor empirically moot → CALUX-V113-03 is functionally complete regardless of the spike outcome; the spike does NOT block the milestone close.
    - Final `## overall_verdict: ALL PASS` section enumerating the gate outcomes (mirror 64-01's closing block). The spike is recorded but is NOT a pass/fail gate — note this.

    The doc must be self-describing — 69-03 reads only this file for SC1 evidence.
  </action>
  <verify>
    <automated>test -f .planning/phases/69-verification-live-uat/69-01-AUTOMATED-GATES.md && grep -q "overall_verdict" .planning/phases/69-verification-live-uat/69-01-AUTOMATED-GATES.md && grep -qiE "Phase 64 baseline|TD-V16-TEST-ISOLATION" .planning/phases/69-verification-live-uat/69-01-AUTOMATED-GATES.md && grep -qiE "theme-guard|theme guard" .planning/phases/69-verification-live-uat/69-01-AUTOMATED-GATES.md && grep -qiE "materializeFilter|fromSwap|static.{0,12}invariant" .planning/phases/69-verification-live-uat/69-01-AUTOMATED-GATES.md && grep -qiE "week.?anchor|DATE_TRUNC|NOT-RUN|RAN" .planning/phases/69-verification-live-uat/69-01-AUTOMATED-GATES.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists with frontmatter containing `overall_verdict`, `commit`, `recorded_on`, `week_anchor_spike`
    - Contains all gate rows + a server-vitest set-IDENTICAL table referencing the Phase 64 baseline files
    - Contains the Locked Invariants Re-Assertion subsection (theme-guard + static no-materialize/no-fromSwap, both confirmed green in the suite)
    - Contains the targeted v1.13 calendar spec group results + the source-tree clean guard note (server diff empty)
    - Contains the week-anchor spike record (RAN or NOT-RUN) with the explicit "inferWeekAnchorDow makes the anchor moot; does NOT block close" note
    - `overall_verdict` reflects the actual gate outcomes (ALL PASS only if every deterministic gate is green; the spike is NOT a pass/fail gate)
  </acceptance_criteria>
  <done>69-01-AUTOMATED-GATES.md exists, fully transcribed from Task 1, ready for the operator (69-02 §0) and the compiler (69-03).</done>
</task>

</tasks>

<verification>
- Frontend vitest 100% green from packages/web (>= 2373 / >= 104 files, 0 failures) — recorded
- web tsc clean AND server tsc clean (separate gates) — recorded
- Server vitest failing-file set IDENTICAL to the Phase 64 baseline (no new server regressions — v1.13 frontend-only) — recorded with the set table
- Locked invariants re-asserted via the green suite (theme-guard no-raw-hex + CalendarRenderer Test 0 static no-materialize/no-fromSwap) — recorded
- Targeted v1.13 calendar spec group all green (incl. 68.2 per-group gap-fill / punchcard / anchor-agnostic regression suites) — recorded
- Week-anchor spike re-attempted best-effort; NOT-RUN fallback recorded if auth-blocked; never blocks the close — recorded
- 69-01-AUTOMATED-GATES.md committed with an honest overall_verdict
</verification>

<success_criteria>
SC1 automated evidence is captured in `69-01-AUTOMATED-GATES.md` with `overall_verdict` reflecting the true gate state, citable by 69-02 §0 and 69-03. The locked invariants are re-asserted, the server gate matches the Phase 64 baseline, and the week-anchor spike is recorded best-effort (never blocking). No source code touched.
</success_criteria>

<output>
After completion, create `.planning/phases/69-verification-live-uat/69-01-SUMMARY.md`.
</output>
