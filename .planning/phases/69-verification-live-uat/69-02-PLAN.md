---
phase: 69-verification-live-uat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/69-verification-live-uat/69-UAT.md
autonomous: true
requirements: [VERIFY-V113-01, CALUX-V113-03]

must_haves:
  truths:
    - "69-UAT.md exists in the 64-UAT.md format: §0 preconditions, per-item operator PASS/FAIL fenced attestation blocks (status: PENDING), a gaps YAML block, an attestation summary (overall_result), traceability tables"
    - "The UAT covers ALL 8 domain×subdomain combos as discrete attestable items: year×month, year×week, year×day, month×day, month×week, week×day, week×hour, day×hour — each asserting per-group gap-fill (in-range grey, out-of-range blank), week combos column-clean (anchor-aligned), week×hour punchcard (7 day-rows × 24 hour-cols), year×day horizontal-scroll reveals data"
    - "The UAT covers BOTH bindings (table-bound AND dv-bound) and BOTH layout modes (Wrap default AND Continuous strip h-scroll)"
    - "The UAT covers on-widget controls: toggle 'Show domain/subdomain controls' ON → 2 viewer dropdowns appear, live combo switch (re-fetch + dependent gating + 8 valid combos), selection is VIEW-LOCAL (resets on reload, does NOT persist to saved config); default OFF → no controls, static grid"
    - "The UAT covers drill+chip+WMS: table-bound cell click filters bar/pie/records on the same table; removable chip shows a HUMAN-READABLE date range (e.g. 'Mar 2 – Mar 8, 2026', NOT raw ISO); chip clears to unfiltered; dv-bound drill is dv-isolated (same-dv updates, source-table + other-dv unaffected); a WMS map on the same table/dv updates tiles after a cell click; empty/grey cells are non-interactive (no drill)"
    - "The UAT covers 'Respond to dashboard filters' ON → CAL-V113-05 filter-aware re-fetch (another widget's filter narrows the calendar); default OFF keeps the full grid"
    - "A blocking checkpoint:human-verify (in 69-03) gates the walk; the operator runs it against live deployed Kinetica (password mode) and attests each item — this plan SHIPS the doc in status 'pending attestation'"
  artifacts:
    - path: ".planning/phases/69-verification-live-uat/69-UAT.md"
      provides: "Operator-executed full-matrix live walk-through doc covering the v1.13 calendar end-to-end"
      contains: "overall_result"
  key_links:
    - from: "69-UAT.md §0"
      to: "69-01-AUTOMATED-GATES.md"
      via: "automated_gates_ref + a P-precondition citing overall_verdict"
      pattern: "69-01-AUTOMATED-GATES"
---

<objective>
Author `69-UAT.md` — the self-contained, operator-executed FULL-MATRIX live walk-through proving the v1.13 Calendar Heatmap end-to-end against deployed Kinetica — then ship it in status "pending attestation" (the operator runs it and attests at the 69-03 blocking checkpoint).

Purpose: the live review iteration during v1.13 (checkbox layout → week×day phantom columns → all-grey format → anchor-agnostic weeks → week×hour punchcard) found a high bug density across combos. The operator decision (69-CONTEXT) is to be THOROUGH: one formal walk confirming all 8 domain×subdomain combos, both bindings, both layout modes, on-widget controls, and drill/chip/WMS are now correct. This is the v1.13 milestone gate. Mirrors 64-UAT.md / 61-UAT.md.

Output: `.planning/phases/69-verification-live-uat/69-UAT.md` shipped in status "pending attestation" (all `status:` PENDING). Operator attestation happens at the 69-03 checkpoint → `overall_result: passed` (or `gaps_found` → a 69.x decimal gap-closure phase). `failed` is not an acceptable close state.

NO feature code. This plan AUTHORS the doc (autonomous) and STOPS — the actual attestation is the 69-03 checkpoint. If a gap is found at the walk, it routes to a separate 69.x repro-test-driven plan — do NOT fix code in this plan.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/69-verification-live-uat/69-CONTEXT.md

<!-- FORMAT PRECEDENT — clone this doc's structure (frontmatter, §0 preconditions, per-item PASS/FAIL fenced blocks, gaps YAML, attestation summary, traceability tables) -->
@.planning/phases/64-verification-live-uat/64-UAT.md

<!-- THE EVIDENCE THIS UAT CITES IN §0 (may not exist yet if 69-01 runs in parallel — reference by path; operator confirms at the checkpoint) -->
@.planning/phases/69-verification-live-uat/69-01-AUTOMATED-GATES.md

<!-- WHAT THE WALK EXERCISES — the shipped calendar surface (read for accurate UI/behavior phrasing) -->
@packages/web/src/components/charts/CalendarRenderer.tsx
@packages/web/src/components/charts/CalendarConfigPanel.tsx

<the_matrix_being_proven>
<!-- The full UAT matrix (69-CONTEXT §Live UAT — FULL MATRIX). The walk must confirm all of this is now correct in one pass after the live-review fixes (344c274 / 4f4ef7c / 90c8f3b / 0a9d9f8). -->
8 DOMAIN×SUBDOMAIN COMBOS (each a discrete attestable item):
  year×month, year×week, year×day, month×day, month×week, week×day, week×hour, day×hour.
  Each must show: per-group gap-fill (in-range buckets with no data = GREY tiles; out-of-range slots = BLANK no-tile);
  week-domain groups stay COLUMN-CLEAN (anchor-aligned, no phantom month-shaped block); a week×day group is a single 7-row column;
  week×hour renders a PUNCHCARD (7 day-rows × 24 hour-cols); year×day is a ~52-week-wide strip where HORIZONTAL SCROLL reveals data.
BINDINGS: table-bound AND dv-bound.
LAYOUT MODES: Wrap (default — mini-calendar blocks flowing left→right) AND Continuous strip (single horizontal h-scroll line).
ON-WIDGET CONTROLS (CALUX-V113-02): config toggle "Show domain/subdomain controls" default OFF (static grid, no controls);
  ON → 2 viewer dropdowns on the widget top bar, switch combos LIVE (re-fetch + dependent gating to the 8 valid combos), VIEW-LOCAL (resets on reload, never persists to saved config).
DRILL + CHIP + WMS (the original SC2-4):
  table-bound cell click → bar/pie/records on the SAME table filter to the slice; removable chip with a HUMAN-READABLE date range (e.g. "Mar 2 – Mar 8, 2026", NOT raw ISO); chip clears to unfiltered;
  dv-bound → dv-isolated (same-dv widgets update; source-table + other-dv unaffected);
  WMS map on the same table/dv updates tiles after a cell click;
  empty/grey cells are NON-INTERACTIVE (click does nothing).
RESPOND-TO-FILTERS (CAL-V113-05): "Respond to dashboard filters" ON → another widget's filter narrows the calendar (filter-aware re-fetch); default OFF → full grid regardless of dashboard filters.
WEEK-ANCHOR (CALUX-V113-03 close-out): the week anchor is now INFERRED from the data (inferWeekAnchorDow) — the calendar is anchor-agnostic, so week combos are column-clean independent of the live spike. The UAT confirms column-cleanliness visually; the live spike itself is best-effort in 69-01.
</the_matrix_being_proven>

<chat_fixes_to_confirm>
<!-- These v1.13 fixes were made directly during live review (committed, unit-tested). The UAT confirms each is now correct in the running app — verify-in-place, no backfilled plans. Each gets a UAT checklist line. -->
- 344c274 fix(68.1): inline calendar config toggles via config-toggle layout → confirmed by the §controls + Display-section checks
- 4f4ef7c fix(68.2): format-agnostic bucket-key lookup in gapFillCalendar (all-grey fix) → confirmed by per-group gap-fill showing real data (not all-grey)
- 90c8f3b fix(68.2): anchor-agnostic week handling (infer Kinetica week anchor from data) → confirmed by week combos being column-clean
- 0a9d9f8 fix(68.2): week×hour 7-day × 24-hour punchcard → confirmed by the week×hour combo item
</chat_fixes_to_confirm>

<personas_and_env>
- OPERATOR: RPereira@kinetica.com (admin/designer) authors the fixtures + drives the entire walk. Single-persona (this is a data-viz/drill behavior, no RBAC dimension).
- Environment: live deployed Kinetica, PASSWORD mode (same as v1.9–v1.12 closes). Requires: a dashboard with a TABLE-bound calendar over a real timestamp column + a bar/pie/records widget on that same table + a WMS map widget on the same table; a DYNAMIC VIEW with a dv-bound calendar + ≥1 other same-dv widget + a source-table widget + ideally a second dv widget (for other-dv isolation); a second widget capable of applying a filter to the calendar's table (for the respond-to-filters check).
</personas_and_env>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 69-UAT.md — §0 preconditions + §1 the 8-combo matrix + §2 layout modes + §3 on-widget controls + §4 drill/chip/WMS + §5 respond-to-filters + gaps + attestation + traceability</name>
  <read_first>
    - .planning/phases/64-verification-live-uat/64-UAT.md (clone frontmatter, §0 PASS/FAIL fenced-block format, gaps YAML, attestation summary, traceability tables)
    - .planning/phases/69-verification-live-uat/69-CONTEXT.md §"Live UAT — FULL MATRIX" + §"Chat-fixes" + §"Deferred → v2" (the exact matrix the walk must cover + the fixes to confirm)
    - packages/web/src/components/charts/CalendarRenderer.tsx + CalendarConfigPanel.tsx (so the UAT phrasing matches the real UI: the Display section, the layout-mode option, the "Show domain/subdomain controls" toggle, the "Respond to dashboard filters" toggle, the chip text, the top control bar)
  </read_first>
  <files>.planning/phases/69-verification-live-uat/69-UAT.md</files>
  <action>
    Write `69-UAT.md` cloning the 64-UAT.md structure. Each check item is a fenced block with `id`/`check`/`status: PENDING`/`evidence:` (operator fills status+evidence at the 69-03 checkpoint).

    Frontmatter:
    ```
    ---
    plan: 69-02
    operator: RPereira@kinetica.com
    started_on: <date>
    automated_gates_ref: .planning/phases/69-verification-live-uat/69-01-AUTOMATED-GATES.md
    automated_gates_verdict: <transcribe overall_verdict + commit from 69-01-AUTOMATED-GATES.md if it exists at author time; else "pending — confirm at checkpoint">
    ---
    ```

    Header: Title "# 69 UAT — Live v1.13 Calendar Heatmap Full-Matrix Walk-Through". Purpose (self-contained operator verification of the v1.13 calendar end-to-end against the running app). A "WHY FULL MATRIX" callout (live review found high bug density across combos — this one pass confirms all 8 combos / both bindings / both layouts / controls / drill+chip+WMS are now correct). Outcome routing: any FAIL halts the UAT → a 69.x repro-test-driven gap plan (failing RED repro → fix → re-walk the affected item before 69-03 compiles); trivial fixes may ride inline; this is a milestone gate — gaps are NOT accepted as tech debt. A "How to fill this in" line (set status PASS/FAIL + one line of evidence).

    ## Section 0 — Preconditions (fenced PASS/FAIL block each, status PENDING):
    - P1: app running (web + server) against deployed Kinetica in PASSWORD mode (`npm run dev` web + `npm run dev:server`).
    - P2: a dashboard exists with these authored fixtures (record names in the blanks): (a) a TABLE-bound calendar over a real timestamp column + metric; (b) a bar OR pie OR records widget on that SAME table (for the drill-target check); (c) a WMS MAP widget on the same table; (d) a DYNAMIC VIEW with a dv-bound calendar + ≥1 other same-dv widget; (e) a SOURCE-TABLE widget on the dv's underlying table; (f) ideally a SECOND dv widget (other-dv isolation); (g) a second widget able to filter the calendar's table (respond-to-filters check). Record dashboard / table / dv name(s) / timestamp column / widget labels.
    - P3: 69-01 automated gates recorded ALL PASS (cite 69-01-AUTOMATED-GATES.md overall_verdict + commit + the week-anchor-spike disposition). Record-only — no manual rerun.

    ## Section 1 — All 8 Domain×Subdomain Combos Render Correctly [ROADMAP SC2; CALUX-V113-01/03] (OPERATOR):
    One fenced item PER COMBO (1.1 … 1.8): year×month, year×week, year×day, month×day, month×week, week×day, week×hour, day×hour. For EACH combo, the operator switches the calendar to that combo and attests:
      - the grid renders the correct shape (domain groups present, correct subdomain cells);
      - per-group gap-fill: in-range buckets with no data are GREY; out-of-range slots are BLANK (no tile) — NOT a global axis, NOT all-grey;
      - week-domain combos (year×week, month×week, week×day, week×hour) are COLUMN-CLEAN (anchor-aligned; no phantom month-shaped block);
      - 1.6 week×day specifically: a single 7-row column per week (not a month block);
      - 1.7 week×hour specifically: a PUNCHCARD — 7 day-rows × 24 hour-cols;
      - 1.3 year×day specifically: a ~52-week-wide strip — HORIZONTAL SCROLL reveals the populated columns (data is present, just off-screen by default — this is expected; auto-scroll is a v2 deferral).
    Add a note tying these to the chat-fixes: 4f4ef7c (real data shows, not all-grey), 90c8f3b (column-clean weeks), 0a9d9f8 (week×hour punchcard).

    ## Section 2 — Layout Modes [CALUX-V113-01] (OPERATOR):
    - 2.1 WRAP (default): the calendar renders wrapped, compact mini-calendar domain-group blocks flowing left→right.
    - 2.2 CONTINUOUS STRIP: switch the layout mode (Display section) to Continuous strip — the calendar renders a single horizontal strip with horizontal scroll; cells/drill/gap-fill still correct in strip mode.

    ## Section 3 — On-Widget Domain/Subdomain Controls [CALUX-V113-02] (OPERATOR):
    - 3.1 DEFAULT OFF: with "Show domain/subdomain controls" OFF, the widget shows NO viewer dropdowns — a static grid only.
    - 3.2 TOGGLE ON → 2 DROPDOWNS: enable the toggle (Display section, info-icon hover present) → 2 viewer-facing domain/subdomain dropdowns appear on the widget top control bar, initialized from the operator's configured values.
    - 3.3 LIVE COMBO SWITCH: change the viewer domain/subdomain → the calendar RE-FETCHES and re-renders; the subdomain dropdown is DEPENDENT-GATED to only the valid options for the chosen domain (the 8 valid combos / VALID_DOMAIN_SUBDOMAIN); an invalid combo is not selectable.
    - 3.4 VIEW-LOCAL: the viewer selection does NOT persist to the saved widget config — reload the dashboard and confirm the calendar resets to the operator's configured combo. (Confirms 344c274 inline-toggle layout too.)

    ## Section 4 — Cell Drill + Chip + WMS Propagation [ROADMAP SC2/SC3/SC4; CALDR-V113-01/02/03] (OPERATOR):
    - 4.1 TABLE-BOUND DRILL FILTERS THE DASHBOARD: click a non-empty cell on the table-bound calendar → the bar/pie/records widget on the SAME table filters to that time slice LIVE (no manual refresh).
    - 4.2 HUMAN-READABLE CHIP: a removable filter chip appears showing a HUMAN-READABLE date range (e.g. "Mar 2 – Mar 8, 2026"), NOT a raw ISO string.
    - 4.3 CHIP CLEARS: remove the chip → the dashboard reverts to unfiltered (the same-table widgets show full data again).
    - 4.4 EMPTY/GREY CELLS NON-INTERACTIVE: click a grey/empty cell → nothing happens (no drill, no chip).
    - 4.5 DV-BOUND DRILL IS DV-ISOLATED: click a cell on the dv-bound calendar → same-dv widgets update; the SOURCE-TABLE widget and any OTHER-dv widget stay UNAFFECTED (dv-isolated scope; the v1.12 killed-bug pattern stays fixed).
    - 4.6 WMS MAP UPDATES: a WMS map widget on the same table/dv updates its TILES after a cell click (verified in-phase per the v1.12 Phase 63.1 lesson — the calendar's filter reaches the MapChartRenderer WMS read-path).

    ## Section 5 — Respond-to-Dashboard-Filters [CAL-V113-05] (OPERATOR):
    - 5.1 RESPOND ON: with the calendar's "Respond to dashboard filters" toggle ON, apply a filter from another widget on the calendar's table → the calendar RE-FETCHES and narrows to the filtered data (filter-aware consumer; the color scale rescales to the narrowed data).
    - 5.2 RESPOND OFF (default): with the toggle OFF, the same external filter leaves the calendar showing the FULL grid (does not narrow). Note the v2 deferral "ignore own filter but respond to others" is OUT of scope (needs a second materialized view).

    ## Section 6 — Automated Gates + Invariants Reference [ROADMAP SC1]:
    - 6.1 SC1 covered by 69-01-AUTOMATED-GATES.md (cite §0 P3): frontend vitest 100% from packages/web, web + server tsc clean, server set-gate ≡ Phase 64 baseline, locked invariants re-asserted (theme-guard no-raw-hex + CalendarRenderer static no-materialize/no-fromSwap), week-anchor spike disposition. Record-only.

    ## Section 7 — Gaps Block: a `gaps: []` YAML block with the commented per-defect template (id GAP-69-NN, severity, in_scope: v1.13, sections, title, resolution → routed to 69.x).

    ## Attestation Summary: `overall_result:` (passed|gaps_found), sections_passed/failed/skipped, operator_notes, attested_by, attested_on — all PENDING until the 69-03 checkpoint (this doc ships in status "pending attestation").

    ## Traceability: two tables — (a) ROADMAP SC → sections (SC1 → §0 P3 + §6; SC2 → §1 + §2 + §4.1-4.4; SC3 → §4.5; SC4 → §4.6 + §4.2), (b) Requirement ID → sections (VERIFY-V113-01 → all sections; CALUX-V113-01 → §1 + §2; CALUX-V113-02 → §3; CALUX-V113-03 → §1 (column-clean weeks + per-group gap-fill) + §6 (anchor-spike disposition); CAL-V113-05 → §5; CALDR-V113-01 → §4.1-4.4; CALDR-V113-02 → §4.5; CALDR-V113-03 → §4.6). Add a "Chat-fixes confirmed live" mini-table mapping 344c274/4f4ef7c/90c8f3b/0a9d9f8 → the section that confirms each.

    Leave all `status:` as PENDING and `evidence:`/attestation fields blank for the operator to fill at the 69-03 checkpoint. The doc ships now in "pending attestation".
  </action>
  <verify>
    <automated>test -f .planning/phases/69-verification-live-uat/69-UAT.md && grep -qE "^## Section 1" .planning/phases/69-verification-live-uat/69-UAT.md && grep -qE "^## Section 4" .planning/phases/69-verification-live-uat/69-UAT.md && grep -qiE "year.?month|year×month" .planning/phases/69-verification-live-uat/69-UAT.md && grep -qiE "week.?hour|week×hour|punchcard" .planning/phases/69-verification-live-uat/69-UAT.md && grep -qiE "dv-isolated|dv-bound|source-table" .planning/phases/69-verification-live-uat/69-UAT.md && grep -qiE "WMS|map.{0,8}tiles" .planning/phases/69-verification-live-uat/69-UAT.md && grep -qiE "human-readable|Mar 2" .planning/phases/69-verification-live-uat/69-UAT.md && grep -qiE "view-local|resets on reload|does not persist" .planning/phases/69-verification-live-uat/69-UAT.md && grep -qiE "respond to (dashboard )?filters" .planning/phases/69-verification-live-uat/69-UAT.md && grep -qi "overall_result" .planning/phases/69-verification-live-uat/69-UAT.md && grep -q "69-01-AUTOMATED-GATES" .planning/phases/69-verification-live-uat/69-UAT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 69-UAT.md exists with frontmatter (operator, automated_gates_ref, automated_gates_verdict)
    - §0 (P1-P3) + §1 (8 discrete combo items 1.1-1.8) + §2 (Wrap + Continuous strip) + §3 (controls OFF/ON/live-switch/view-local) + §4 (drill/chip-human-readable/clear/grey-non-interactive/dv-isolated/WMS) + §5 (respond ON/OFF) + §6 (gates ref) + §7 gaps + attestation + traceability all present
    - §1 enumerates all 8 combos with per-group gap-fill (grey in-range / blank out-of-range), column-clean weeks, week×day single column, week×hour punchcard, year×day horizontal-scroll
    - §4.2 explicitly requires a HUMAN-READABLE chip (not raw ISO); §4.4 grey cells non-interactive; §4.5 dv-isolated; §4.6 WMS tiles update
    - Both bindings (table + dv) and both layout modes (Wrap + Continuous strip) attestable; on-widget controls view-local; respond-to-filters ON/OFF
    - §0 P3 cites 69-01-AUTOMATED-GATES.md; traceability maps VERIFY-V113-01 + CALUX-V113-03 (and the other v1.13 calendar reqs exercised live); chat-fixes mini-table present
    - All status fields PENDING (operator fills at the 69-03 checkpoint); doc ships in "pending attestation"
  </acceptance_criteria>
  <done>69-UAT.md is a complete, self-contained full-matrix walk-through doc in status "pending attestation", ready for the operator to execute against the live app at the 69-03 checkpoint.</done>
</task>

</tasks>

<verification>
- 69-UAT.md authored in the 64-UAT.md format, covering the FULL matrix: all 8 combos (per-group gap-fill, column-clean weeks, week×day single column, week×hour punchcard, year×day h-scroll), both bindings, both layout modes, on-widget view-local controls, drill+human-readable-chip+clear+grey-non-interactive, dv-isolated drill, WMS tile propagation, respond-to-filters ON/OFF
- The live-review chat-fixes (344c274 / 4f4ef7c / 90c8f3b / 0a9d9f8) are each confirmed by a UAT item (mini-table in traceability)
- §0 P3 cites the 69-01 gate record; doc ships in status "pending attestation"; operator attestation occurs at the 69-03 blocking checkpoint
</verification>

<success_criteria>
`69-UAT.md` is complete, self-contained, and in status "pending attestation" — ready for the operator to walk against live deployed Kinetica. Every matrix dimension from 69-CONTEXT (8 combos, both bindings, both layouts, on-widget controls, drill/chip/WMS, respond-to-filters) is authored as an attestable item mapped to the ROADMAP SCs and requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/69-verification-live-uat/69-02-SUMMARY.md`.
</output>
