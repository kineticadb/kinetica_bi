---
phase: 109.2-wire-custom-panel-charts-filter-scope
verified: 2026-07-12T20:05:55Z
status: passed
score: 5/5 must-haves verified (both plans)
---

# Phase 109.2: Wire Custom-Panel Charts into Filter-Scope Engine and Reverse-Map Verification Report

**Phase Goal:** Calendar/Timeline/Numeric-Line ACTUALLY honor per-visualization filter scope (not just the config UI) + appear correctly in the panel's applies-to count + highlight; retire the redundant calendar respondToFilters. Root cause was NON_TRIGGER_TYPES excluding them from the orchestrator binding + reverse-map enumeration.
**Verified:** 2026-07-12T20:05:55Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Orchestrator mints scoped `vizToHash` binding for calendar/timeline/numericline (no longer skipped by `NON_TRIGGER_TYPES`) | VERIFIED | `useCombinationOrchestrator.ts:72-87` — `NON_TRIGGER_TYPES` set contains only `map, info-card, legend, datafilter, radiogroup`; comment documents the 3-type removal. Coalesce threaded at table loop (L219) and dv loop (L299, after `dvScopeDisabled` short-circuit). |
| 2 | Reverse-map (`enumerateVizDescriptors`) enumerates calendar/timeline/numericline so panel applies-to count/highlight include them | VERIFIED | `useReverseFilterMap.ts:33-43` — identical `NON_TRIGGER_TYPES` set (mirrored, 3 types removed); coalesce threaded at L82. Spec `useReverseFilterMap.spec.tsx:81-137` proves calendar/timeline/numericline now enumerated as `vizKind:'widget'`, while map/info-card/legend remain excluded. |
| 3 | Legacy calendar `respondToFilters:false` resolves via coalesce to EMPTY allow-list (respond to none), unchanged base-table behavior | VERIFIED | `coalesceCalendarFilterSelection.ts` uses `Object.prototype.hasOwnProperty.call(cfg, "respondToFilters")` (key-presence, not value) → `false` case returns `{sourceMode:"allowlist", allowedSourceWidgetIds:[]}`. `useCombinationOrchestrator.spec.ts` SC7b proves `vizToHash` stays `undefined` + no materialize POST for this case. `CalendarRenderer.spec.tsx` Test 4a proves SQL FROM = base table. |
| 4 | Brand-new calendar (no `respondToFilters` key, no `filterSelection`) resolves to accept-all (undefined) | VERIFIED | `coalesceCalendarFilterSelection.ts` L21-22 returns `undefined` when key absent; `coalesceCalendarFilterSelection.spec.ts` covers this case distinctly from the `false`-value case (the Pitfall-3 regression guard). |
| 5 | bar/pie/table + existing combination/dv/badge behavior unchanged; sole-materialize-trigger invariant holds | VERIFIED | `FILTER_PRODUCING_TYPES` (filterSourceTypes.ts) untouched (confirmed no diff in phase commits). `grep -rn "materializeFilter\|dropCombinationView" src/components/` shows only pre-existing authorized sites (`DashboardsPage.tsx`, `WidgetRenderer.tsx`); `CalendarRenderer.tsx` explicitly does NOT import either (comment-documented at L31/488). Full suite green. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/web/src/lib/coalesceCalendarFilterSelection.ts` | Pure key-presence migration coalesce | VERIFIED | Exists, uses `hasOwnProperty`, matches RESEARCH Pattern 2 verbatim. |
| `packages/web/src/hooks/useCombinationOrchestrator.ts` | `NON_TRIGGER_TYPES` w/o 3 types + coalesce at 2 sites | VERIFIED | Set edited; `coalesceCalendarFilterSelection` imported + used at L219 (table) and L299 (dv, after `dvScopeDisabled`). |
| `packages/web/src/lib/useReverseFilterMap.ts` | `NON_TRIGGER_TYPES` w/o 3 types + coalesce in enumerateVizDescriptors | VERIFIED | Set edited (mirrored); coalesce at L82. |
| `packages/web/src/components/charts/CalendarRenderer.tsx` | vizToHash-based FROM resolution; no respondToFilters; no filterViewStore READ | VERIFIED | `grep -c "respondToFilters"` = 0. `grep "useFilterViewStore((s)"` = 0 matches (no READ selectors); only `.getState().markMaterializing/markDvMaterializing` WRITE calls remain (Timeline-parity, intentional). `vizToHash[vizKey]` read path present at table (L194-209, 285-293) and dv (L298-305) branches, mirroring TimelineRenderer/AggregatedWidgetRenderer exactly. |
| `packages/web/src/components/charts/CalendarConfigPanel.tsx` | respondToFilters removed from type/default/checkbox; FilterSelectionPanel coalesced | VERIFIED | `grep -c "respondToFilters"` = 0 in type (L58-76) and default (L78-90); no checkbox block found; `value={coalesceCalendarFilterSelection(cfg as Record<string, unknown>)}` at L713. |
| `packages/web/src/components/WidgetCard.tsx` | calendar-only coalesce applied to WidgetFilterBadge cfg prop | VERIFIED | L144-148: `cfg={w.type === "calendar" ? coalesceCalendarFilterSelection(...) : (raw filterSelection)}` — gated correctly, non-calendar unaffected. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `useCombinationOrchestrator.ts` | `coalesceCalendarFilterSelection` | `w.type === 'calendar' ? coalesceCalendarFilterSelection(w.config) : ...` | WIRED | Matches exactly at both table (L219) and dv (L299) loops. |
| `useReverseFilterMap.ts` | `coalesceCalendarFilterSelection` | `cfg: w.type === 'calendar' ? coalesceCalendarFilterSelection(cfg) : ...` | WIRED | Matches exactly at L82. |
| `CalendarRenderer.tsx` | `filterCombinationStore.vizToHash` | primitive vizKey selectors + dv-branch getState | WIRED | `vizToHash[vizKey]` present at all documented sites (L197,202,207,285,298); mirrors TimelineRenderer/WidgetRenderer patterns verbatim. |
| `WidgetCard.tsx` | `coalesceCalendarFilterSelection` | `cfg = w.type === 'calendar' ? coalesceCalendarFilterSelection(...) : ...` | WIRED | Matches exactly at L144-148. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|--------------|--------|----------|
| FSCOPE-V120-05 | 109.2-01, 109.2-02 | Calendar/Timeline/Numeric-Line honor filter scope on read path + applies-to panel; respondToFilters retired | SATISFIED | REQUIREMENTS.md marks `[x]` with both plans noted COMPLETE; status column reads "Complete". All engine + renderer wiring confirmed above. |

No orphaned requirements found for this phase (only FSCOPE-V120-05 mapped, both plans claim it).

### Anti-Patterns Found

None blocking. A pre-existing, unrelated comment string "FALLBACK PLACEHOLDER FIX" at `useCombinationOrchestrator.ts:418` (Phase 96-01, untouched by this phase) matched the TODO/PLACEHOLDER grep but is not a stub — it's a historical fix description, not new to 109.2.

### Human Verification Required

None required for pass — the RESEARCH doc anticipated a live-UAT walk-through in Phase 110 to visually confirm the badge/highlight behavior in the running app; that is explicitly deferred to Phase 110 per the SUMMARY's "Next Phase Readiness" note, not a gap in this phase.

### Gates Executed

- `cd packages/web && npx tsc --noEmit` — clean, 0 errors.
- `npx vitest run src/lib/coalesceCalendarFilterSelection.spec.ts src/hooks/useCombinationOrchestrator.spec.ts src/lib/useReverseFilterMap.spec.tsx src/components/charts/TimelineRenderer.spec.tsx src/components/charts/NumericLineRenderer.spec.tsx src/components/charts/CalendarRenderer.spec.tsx src/components/charts/CalendarConfigPanel.spec.tsx src/components/WidgetCard.spec.tsx` — 8 files, 235/235 tests passed.
- `npx vitest run` (full suite, default parallel) — 151/151 test files passed, 3353/3353 tests passed. 10 unhandled-rejection "errors" logged, all originating from `InfoCardRenderer.spec.tsx`/`InfoPopup.spec.tsx` (`ReauthRequiredError` 401 from `columnDisplayConfigStore.loadConfig`) — this is the documented pre-existing parallel-isolation flakiness (matches SUMMARY 109.2-02's "Issues Encountered" note); it did not fail any test (all tests still reported passed) and does not touch any 109.2-modified file. Per verification instructions, this pre-existing infra flakiness on unrelated files does not fail the phase.
- `npx vitest run src/styles/theme-guard.spec.ts` — green, 146/146 tests passed.
- `TimelineRenderer.tsx` / `NumericLineRenderer.tsx` confirmed UNCHANGED during this phase (no commits touching them since before Phase 109.2; `FILTER_PRODUCING_TYPES` in `filterSourceTypes.ts` also confirmed untouched).
- Sole-materialize-trigger invariant: `grep -rE "materializeFilter|dropCombinationView" packages/web/src/components/` — only pre-existing authorized sites; `CalendarRenderer.tsx` explicitly imports neither.
- All 4 phase commits (`a54ca69`, `6586778`, `3659856`, `0107e99`) confirmed present in git history.

### Gaps Summary

No gaps found. Both plans (109.2-01 engine-side, 109.2-02 calendar renderer migration) fully match their PLAN must_haves against the live codebase: `NON_TRIGGER_TYPES` correctly edited in both files atomically, `coalesceCalendarFilterSelection` correctly implements key-presence (not value) discrimination and is threaded through all 5 documented call sites (orchestrator ×2, reverse-map ×1, WidgetCard badge, CalendarConfigPanel display), `CalendarRenderer.tsx` fully swapped to the `vizToHash` read path with zero `respondToFilters`/`filterViewStore`-READ references remaining, and `FILTER_PRODUCING_TYPES`/Timeline/NumericLine renderers confirmed untouched. All test gates pass; the only test-run anomaly is the pre-existing, well-documented `InfoCardRenderer`/`InfoPopup` unhandled-rejection noise unrelated to this phase's files.

---

_Verified: 2026-07-12T20:05:55Z_
_Verifier: Claude (gsd-verifier)_
