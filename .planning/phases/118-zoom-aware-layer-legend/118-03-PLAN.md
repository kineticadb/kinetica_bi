---
phase: 118-zoom-aware-layer-legend
plan: 03
type: execute
wave: 3
depends_on: ["118-01", "118-02"]
files_modified:
  - packages/web/src/components/charts/MapChartRenderer.tsx
  - packages/web/src/components/charts/MapChartRenderer.spec.tsx
  - packages/web/src/components/charts/LegendRenderer.tsx
  - packages/web/src/components/charts/LegendRenderer.spec.tsx
autonomous: false
requirements: [ZLGND-V123-01, ZLGND-V123-02, ZLGND-V123-03, ZLGND-V123-04, ZLGND-V123-06, ZLGND-V123-07]

must_haves:
  truths:
    - "The in-map legend reads its zoom from mapCurrentViewStore keyed by its OWN widget id, reactively — not from an imperative mapRef read."
    - "The standalone Legend widget reads its zoom from mapCurrentViewStore keyed by its sourceMapWidgetId."
    - "When the bound map is not mounted, the standalone Legend renders exactly as it does today — no chip, no dimming, and NOT the orphan UI."
    - "Zooming the live map updates the panel with no reload and no panel re-open."
    - "Zoom-inactive, eye-off and active read as three different things in BOTH dark and light themes."
    - "An info click on a layer at a fractional zoom just below its minZoom now returns records."
  artifacts:
    - path: "packages/web/src/components/charts/MapChartRenderer.tsx"
      provides: "currentZoomForLegend reactive selector threaded into resolveLegendLayers + its useMemo deps"
      contains: "currentZoomForLegend"
    - path: "packages/web/src/components/charts/LegendRenderer.tsx"
      provides: "sourceMapWidgetId-keyed zoom selector threaded into resolveLegendLayers"
      contains: "useMapCurrentViewStore"
  key_links:
    - from: "packages/web/src/components/charts/MapChartRenderer.tsx"
      to: "packages/web/src/store/mapCurrentViewStore.ts"
      via: "useMapCurrentViewStore((s) => s.views[widget.id]?.zoom)"
      pattern: "s.views\\[widget.id\\]\\?.zoom"
    - from: "packages/web/src/components/charts/LegendRenderer.tsx"
      to: "packages/web/src/store/mapCurrentViewStore.ts"
      via: "useMapCurrentViewStore((s) => s.views[sourceMapWidgetId]?.zoom)"
      pattern: "s.views\\[sourceMapWidgetId\\]\\?.zoom"
    - from: "both consumers"
      to: "packages/web/src/lib/resolveLegendLayers.ts"
      via: "third argument to resolveLegendLayers"
      pattern: "resolveLegendLayers\\([^)]*,[^)]*,"
---

<objective>
Wire the live zoom into both legend consumers and verify the finished feature — automated where that
is honest, and by the operator's own eyes where it is not.

Purpose: `ZLGND-V123-04` (live update, no reload) and `ZLGND-V123-06` (standalone Legend mirrors its
bound map, degrades to today's appearance when that map's zoom is unavailable). Plan 118-02 built the
derivation and the rendering; nothing consumes them yet. This plan connects them and closes the phase.

Output: two one-line reactive selectors + two call-site arguments + dependency-array entries; a
`mapCurrentViewStore` mock added to `LegendRenderer.spec.tsx` (it has none today); and a blocking
operator UAT covering both themes, live zooming, and the deliberate info-click behaviour change from
plan 118-01.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/118-zoom-aware-layer-legend/118-CONTEXT.md
@.planning/phases/118-zoom-aware-layer-legend/118-RESEARCH.md
@.planning/phases/118-zoom-aware-layer-legend/118-01-SUMMARY.md
@.planning/phases/118-zoom-aware-layer-legend/118-02-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase. Use these directly — no exploration needed. -->

`packages/web/src/store/mapCurrentViewStore.ts` — read-only for this phase, do NOT modify:
```typescript
export type MapCurrentView = {
  center: [number, number]; // EPSG:3857
  zoom: number;             // fractional OL zoom, UNROUNDED (111-CONTEXT.md lock)
};
type State = {
  views: Record<number, MapCurrentView | undefined>; // keyed by widget.id
  publish: (widgetId: number, view: MapCurrentView) => void;
  clear: (widgetId: number) => void;
  reset: () => void;
};
export const useMapCurrentViewStore = create<State>(...);
```
`views[id]` is `undefined` — never stale — in EVERY unavailable scenario: never mounted (key absent),
mounted-then-unmounted (`MapChartRenderer.tsx:2316` `clear()` `delete`s the key on cleanup), and
dashboard-switch / logout (`App.tsx:298`, `DashboardsPage.tsx:727` call `reset()`).

The established scoped-selector pattern to copy — `packages/web/src/components/charts/MapConfigPanel.tsx:160-162`:
```typescript
const currentView = useMapCurrentViewStore((s) =>
  widgetId === undefined ? undefined : s.views[widgetId]
);
```

`packages/web/src/lib/resolveLegendLayers.ts` after plan 118-02:
```typescript
export function resolveLegendLayers(
  storeLayers: DashboardLayerDto[],
  includedLayerIds: number[] | undefined,
  zoom?: number,
): ResolvedLegendLayer[];
// ResolvedLegendLayer gains: zoomRange?: { minZoom?: number; maxZoom?: number }; zoomActive?: boolean;
```

Consumer 1 — `packages/web/src/components/charts/MapChartRenderer.tsx`:
- `useMapCurrentViewStore` is ALREADY imported (line 103) but used only imperatively (`.getState().publish`
  line 2305, `.getState().clear` line 2316). No import change is needed — add a reactive selector.
- The single `resolveLegendLayers(` call is at ~line 702, inside the `resolvedLegendLayers` useMemo
  whose dep array (~line 781) is
  `[legendKey, includedLayerIdsForLegend, dynamicViewsKey, filterVersion, shapesKey]`.
- Effect 9c (lines 2297-2318) publishes `{center, zoom}` for `widget.id` on mount AND on every
  `moveend`, always-on and ungated.

Consumer 2 — `packages/web/src/components/charts/LegendRenderer.tsx`:
- `sourceMapWidgetId` is resolved at line 44. `isOrphan` (line 73) is the 3-case check.
- The single `resolveLegendLayers(effectiveLayers, includedLayerIds)` call is at line 92, inside a
  useMemo with deps `[effectiveLayers, includedLayerIds, isOrphan, dynamicViewVersion]` (line 106).
- This file has NO `mapCurrentViewStore` import today.

Test harness — `packages/web/src/components/charts/MapChartRenderer.spec.tsx`:
- `_currentViewState` mock (lines 537-544) with a mutable `.views` record and `vi.fn` publish/clear.
- `vi.mock("../../store/mapCurrentViewStore", ...)` (lines 546-552) exposes
  `hook = (selector) => selector(_currentViewState)` — **a plain function with NO subscription**.
  A store write therefore does NOT trigger a React re-render in this harness. See Task 1's honesty note.
- `capturedMoveendHandlers` / `fireAllMoveend()` (lines 138-139); `lastMockView.getZoom` is a
  `vi.fn(() => 10)` mutable via `.mockReturnValue()`.
- Existing legend describe block: `"LayersLegendPanel mount (Phase 41)"` at line 5019, with
  `makeLegendLayer(id, cb_config)` and a beforeEach that resets `_layersState.layers` only.
- `makeWidget()` produces widget id **10**; `makeWidget({ legendPanelEnabled: true })` is required or
  the in-map panel does not render at all (`DEFAULT_LEGEND_PANEL_ENABLED = false`,
  `lib/legendPanelConfig.ts:9`).

Test harness — `packages/web/src/components/charts/LegendRenderer.spec.tsx`:
- Mocks `../LayersLegendPanel` and captures props into `lastLayersLegendPanelProps` (lines 8-20) —
  assert on `lastLayersLegendPanelProps!.layers[0]`, no DOM digging needed.
- `_storeState.layers` is the mocked dashboardLayersStore; `makeWidget`/`makeLayer`/`renderWithContext`
  helpers exist (lines 39-91).
- The simpler mock shape to add, copied from `MapConfigPanel.spec.tsx:44-50` — read-only, no
  publish/clear needed since LegendRenderer never writes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the in-map legend to its own widget's live zoom</name>

  <read_first>
    - `packages/web/src/components/charts/MapChartRenderer.tsx` lines 698-782 (the `resolvedLegendLayers` useMemo and its dep array) and lines 2297-2318 (Effect 9c, the publish side) — the file being modified
    - `packages/web/src/components/charts/MapConfigPanel.tsx` lines 155-165 — the scoped-selector pattern being mirrored (source of truth)
    - `packages/web/src/components/charts/MapChartRenderer.spec.tsx` lines 533-552 and 5016-5045 — the store mock (note: NON-reactive) and the legend describe block being extended
  </read_first>

  <files>
    packages/web/src/components/charts/MapChartRenderer.tsx,
    packages/web/src/components/charts/MapChartRenderer.spec.tsx
  </files>

  <behavior>
    ZLM1: widget id 10, `legendPanelEnabled: true`, one layer configured `{minZoom:3, maxZoom:10}`.
          After mount (Effect 9c publishes the mock's zoom 10) the layer block does NOT carry
          `layers-legend-panel-layer-block--zoom-inactive` and a chip reading `zoom 3–10` is present.
          Then `lastMockView.getZoom.mockReturnValue(10.5)`, `fireAllMoveend()`, `rerender(...)` →
          the SAME block now DOES carry `--zoom-inactive`, still with no reload and the panel never
          re-opened.
    ZLM2 (structural): `MapChartRenderer.tsx` source contains a reactive
          `useMapCurrentViewStore((s) => ...)` selector AND contains no `getView().getZoom()` read
          inside the `resolvedLegendLayers` useMemo body — the anti-pattern that would freeze the
          legend's zoom at whatever it was on some arbitrary render.
  </behavior>

  <action>
**HONESTY NOTE — read before writing ZLM1.** The spec's `mapCurrentViewStore` mock is
`(selector) => selector(_currentViewState)`: a plain function with no zustand subscription. A store
write in this harness does NOT schedule a React re-render. ZLM1 therefore drives the re-read with an
explicit `rerender(...)` after `fireAllMoveend()`. That proves the **data path** end to end (moveend →
publish → store → selector → resolveLegendLayers → DOM) and proves the value is not frozen at
mount — but it does NOT prove zustand auto-re-renders on the store write. Do not claim it does. The
auto-re-render half of `ZLGND-V123-04` is covered structurally by ZLM2 and for real by UAT check
**UAT-118-C** in Task 3. Write this reasoning into a comment above ZLM1 so nobody later "strengthens"
the test into a false claim.

**1a — the selector.** In `MapChartRenderer.tsx`, near the other legend-related derivations
(immediately before the `resolvedLegendLayers` useMemo, ~line 697), add:

```typescript
  // Phase 118 (ZLGND-V123-04): the in-map legend's live zoom. This widget already PUBLISHES
  // its view into mapCurrentViewStore on mount + every moveend (Effect 9c, ~line 2300); this
  // reads it back so the legend can mark which layers OL is actually drawing.
  // A primitive `number | undefined` selector, matching the MapConfigPanel.tsx:160 precedent and
  // this file's convention of primitive useMemo deps (legendKey / filterVersion / shapesKey).
  // Cheap: the store only changes on moveend, never continuously during a drag.
  // NEVER replace this with mapRef.current.getView().getZoom() — a ref read is not reactive and
  // would freeze the legend at whatever zoom happened to be current on some arbitrary render.
  const currentZoomForLegend = useMapCurrentViewStore(
    (s) => s.views[widget.id]?.zoom,
  );
```

**1b — the call site.** Pass it as the third argument:
```typescript
    const base = resolveLegendLayers(
      effectiveLayers,
      includedLayerIdsForLegend,
      currentZoomForLegend,
    );
```

**1c — the dependency array.** Append `currentZoomForLegend` to the useMemo's dep array (~line 781),
making it `[legendKey, includedLayerIdsForLegend, dynamicViewsKey, filterVersion, shapesKey, currentZoomForLegend]`,
and extend the explanatory comment block directly above it with one line naming it as the
zoom-activity re-render trigger. This is not optional bookkeeping — without it the memo would not
recompute on zoom and `ZLGND-V123-04` would silently fail.

Note the brief mount window where `views[widget.id]` is still `undefined` (before Effect 9c's initial
publish): the legend renders with no zoom indication for that first paint, which is precisely the
defined degradation (identical to today's appearance), not a bug. Do not add a loading state.

Change nothing else in this file.

**1d — tests.** Add a NEW describe block in `MapChartRenderer.spec.tsx`, placed immediately after the
existing `"LayersLegendPanel mount (Phase 41)"` block, titled
`"LayersLegendPanel zoom activity (Phase 118 / ZLGND-V123-04)"`. Its beforeEach must reset
`_layersState.layers = []`, `lastMockView = null`, `capturedMoveendHandlers = []`,
`_currentViewState.views = {}` and `_currentViewState.publish.mockClear()` (mirroring the
Phase 104/111 describe blocks at lines 6330-6340 — the Phase 41 block does NOT reset these).

Test titles `ZLM1:` and `ZLM2:` (verified: `ZLM1:` occurs **0** times anywhere in `packages/web/src`).
Build the layer with the existing `makeLayer({ id: 1, table_id: 11, config: { renderMode: "raster", visible: true, minZoom: 3, maxZoom: 10 } })` shape and render
`<MapChartRenderer widget={makeWidget({ legendPanelEnabled: true })} tables={defaultTables} />`.
Query with `document.querySelector(".layers-legend-panel-layer-block")`.

For ZLM2, read the source with `fs.readFileSync` (the `LegendRenderer.spec.tsx:284` "Test 11" CSS
test is the in-repo precedent for a source-text assertion) and assert:
`/useMapCurrentViewStore\(\s*\(s\)\s*=>/.test(src)` is true, and that the substring
`"getView().getZoom()"` does not appear between the `const resolvedLegendLayers = useMemo` marker and
the `}, [legendKey` dep-array marker.
  </action>

  <acceptance_criteria>
    1. `cd packages/web && npx vitest run src/components/charts/MapChartRenderer.spec.tsx` →
       **224 passed (224)**, 0 failed. (Confirmed baseline measured this session: **220**; plan 118-01
       Task 3 takes it to **222**; this task adds ZLM1+ZLM2 = **224**.)
    2. `grep -c "currentZoomForLegend" packages/web/src/components/charts/MapChartRenderer.tsx` = **3**
       (declaration + call-site argument + dep-array entry). (Confirmed current count: **0**.)
    3. `grep -cF "s.views[widget.id]?.zoom" packages/web/src/components/charts/MapChartRenderer.tsx` = **1**.
       (Confirmed current count: **0**.)
    4. `grep -c "ZLM1:\|ZLM2:" packages/web/src/components/charts/MapChartRenderer.spec.tsx` = **2**.
       (Confirmed current repo-wide count of `ZLM1:` = **0**.)
    5. `grep -c "mockReturnValue(10.5)" packages/web/src/components/charts/MapChartRenderer.spec.tsx` = **1**.
       (Confirmed current count: **0**.)
    6. **MUTATION PROBE (required, record the transcript in the SUMMARY):** remove
       `currentZoomForLegend` from the useMemo dependency array only (leave the selector and the
       call-site argument in place). Re-run the spec and confirm **ZLM1 reddens** — it is the dep-array
       entry, not merely the argument, that makes the panel update on zoom. Revert and confirm 224/224.
    7. `cd packages/web && npx tsc --noEmit` → exit 0, clean.
  </acceptance_criteria>

  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/components/charts/MapChartRenderer.spec.tsx</automated>
  </verify>

  <done>
    The in-map legend derives zoom activity from `mapCurrentViewStore` keyed by its own widget id,
    with the value in the memo's deps; the spec is 224/224 (was 222 after plan 01); the dep-array
    mutation probe reddened ZLM1 and was reverted; ZLM1's comment states exactly what it does and does
    not prove.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the standalone Legend widget and prove its unavailable-zoom degradation</name>

  <read_first>
    - `packages/web/src/components/charts/LegendRenderer.tsx` (all 139 lines) — the file being modified; note its documented subscription discipline (PITFALL S-02, primitive selectors) and its 3-case orphan contract
    - `packages/web/src/components/charts/MapConfigPanel.spec.tsx` lines 40-55 — the read-only `mapCurrentViewStore` mock shape being copied (source of truth being mirrored)
    - `packages/web/src/components/charts/LegendRenderer.spec.tsx` lines 1-95 — the existing mock/fixture harness being extended; it has NO mapCurrentViewStore mock today
  </read_first>

  <files>
    packages/web/src/components/charts/LegendRenderer.tsx,
    packages/web/src/components/charts/LegendRenderer.spec.tsx
  </files>

  <behavior>
    ZLR1 (bound map mounted): legend widget bound to map widget id 7; `views[7] = { center:[0,0], zoom: 10.5 }`;
         one store layer configured `{minZoom:3, maxZoom:10}` → `lastLayersLegendPanelProps.layers[0].zoomActive === false`
         and `.zoomRange` deep-equals `{ minZoom: 3, maxZoom: 10 }`.
    ZLR1b (same, in range): `views[7] = { ..., zoom: 2.9 }` → `zoomActive === true`. Proves the
         standalone widget gets the SAME fractional-boundary answer as the map (ZLGND-V123-05/06).
    ZLR2 (bound map NOT mounted): `views` is `{}` (the real, verified shape — never a stale entry);
         same layer → `zoomActive === undefined` (NOT `false`) while `zoomRange` is still defined.
    ZLR3 (the fourth state is not the orphan state): in the ZLR2 scenario the component renders the
         mocked `LayersLegendPanel` (`data-testid="mocked-layers-legend-panel"` present) and does NOT
         render the orphan UI (`.legend-widget-orphan` absent, "Reconfigure" button absent). The
         binding is valid; only the live zoom is missing.
  </behavior>

  <action>
**2a — the selector.** In `LegendRenderer.tsx`, add
`import { useMapCurrentViewStore } from "../../store/mapCurrentViewStore";` with the other store
imports, and immediately after the `sourceMapWidgetId` const (line 44-46) add:

```typescript
  // Phase 118 (ZLGND-V123-06): the BOUND map's live zoom, read from the store the map publishes
  // into (keyed by widget.id). Primitive `number | undefined` selector per this file's documented
  // subscription discipline — never a whole-object selector, which would re-render this tile on
  // unrelated store writes.
  //
  // `undefined` here is the FOURTH state and it needs no UI of its own: the binding is valid
  // (isOrphan stays false) but that map is not currently mounted. mapCurrentViewStore has no code
  // path that leaves a stale entry — clear() deletes on unmount, reset() wipes on dashboard-switch
  // and logout — so undefined always means "genuinely unknown", never "possibly outdated".
  // Passing it straight through to resolveLegendLayers makes this degrade through the SAME path as
  // "no range configured": zoomActive stays undefined and the panel renders exactly as it does
  // today. Do NOT add a "zoom unavailable" banner or fold this into the orphan UI — the locked
  // decision is explicitly to show nothing new.
  const currentZoom = useMapCurrentViewStore((s) =>
    sourceMapWidgetId === undefined ? undefined : s.views[sourceMapWidgetId]?.zoom,
  );
```

**2b — the call site + deps.** Change line 92 to
`const base = resolveLegendLayers(effectiveLayers, includedLayerIds, currentZoom);` and append
`currentZoom` to that useMemo's dep array (line 106), extending the comment above it to name it as
the zoom-activity trigger.

Do NOT touch `isOrphan`, the orphan JSX, or any existing dep. Never write `?? 0`, `?? false`, or any
other coercion of the undefined zoom.

**2c — the mock.** Add to `LegendRenderer.spec.tsx`, alongside the existing `vi.mock` calls and
BEFORE the `import LegendRenderer` line (mocks must be registered first):

```typescript
// Phase 118: read-only mapCurrentViewStore mock (LegendRenderer never writes to it).
// Mirrors MapConfigPanel.spec.tsx:44-50. `views` is mutated per-test to simulate the bound map
// being mounted (entry present) or not mounted (key absent — the real store's only "unavailable"
// shape, since clear()/reset() delete rather than sentinel).
const _currentViewState = {
  views: {} as Record<number, { center: [number, number]; zoom: number } | undefined>,
};
vi.mock("../../store/mapCurrentViewStore", () => ({
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  useMapCurrentViewStore: (selector: (s: any) => any) => selector(_currentViewState),
}));
```
Add `_currentViewState.views = {};` to the existing `beforeEach` (line 85) so the mounted/unmounted
state cannot bleed between tests.

**2d — tests.** Append `ZLR1:`, `ZLR1b:`, `ZLR2:`, `ZLR3:` to the existing describe block (verified:
`ZLR1:` occurs **0** times anywhere in `packages/web/src`). Use the existing
`renderWithContext(legendWidget, [legendWidget, mapWidget])` helper with
`makeWidget({ id: 100, type: "legend", config: { sourceMapWidgetId: 7 } })` and
`makeWidget({ id: 7, type: "map", config: {} })`, and
`_storeState.layers = [makeLayer(1, { config: { minZoom: 3, maxZoom: 10 } })]`.
Assert on `lastLayersLegendPanelProps!.layers[0]`.
  </action>

  <acceptance_criteria>
    1. `cd packages/web && npx vitest run src/components/charts/LegendRenderer.spec.tsx` →
       **16 passed (16)**, 0 failed. (Confirmed baseline measured this session: **12 passed (12)**.
       +4 delta = ZLR1, ZLR1b, ZLR2, ZLR3.)
    2. `grep -c "mapCurrentViewStore" packages/web/src/components/charts/LegendRenderer.spec.tsx` ≥ **1**.
       (Confirmed current count: **0** — RESEARCH flagged this as a genuine harness gap, not an
       existing mock to extend.)
    3. `grep -c "mapCurrentViewStore" packages/web/src/components/charts/LegendRenderer.tsx` = **1**
       (the import). (Confirmed current count: **0**.)
    4. `grep -c "ZLR1:\|ZLR1b:\|ZLR2:\|ZLR3:" packages/web/src/components/charts/LegendRenderer.spec.tsx` = **4**.
       (Confirmed current repo-wide count of `ZLR1:` = **0**.)
    5. `grep -cE "\?\?\s*0\b|\?\?\s*false" packages/web/src/components/charts/LegendRenderer.tsx` = **0** —
       the unknown zoom is never coerced. (Confirmed current count: **0**; the mutation probe is what
       gives this teeth.)
    6. `grep -c "legend-widget-orphan" packages/web/src/components/charts/LegendRenderer.tsx` = **2**
       — unchanged; no new orphan branch was added. (Confirmed current count: **2**.)
    7. **MUTATION PROBE (required, record the transcript in the SUMMARY):** change the selector to
       coerce, i.e. `... : (s.views[sourceMapWidgetId]?.zoom ?? 0)`. Re-run the spec and confirm
       **ZLR2 reddens** (the layer would report `zoomActive: false` instead of `undefined` — the
       "confidently wrong panel" failure this requirement exists to prevent). Revert and confirm 16/16.
    8. `cd packages/web && npx tsc --noEmit` → exit 0, clean.
  </acceptance_criteria>

  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/components/charts/LegendRenderer.spec.tsx</automated>
  </verify>

  <done>
    The standalone Legend reads its bound map's live zoom by `sourceMapWidgetId`, passes it through
    unchanged, and renders today's appearance (not the orphan UI) when that map is not mounted; the
    spec is 16/16 (was 12) and has a `mapCurrentViewStore` mock for the first time; the coercion
    mutation probe reddened ZLR2 and was reverted.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator UAT — both themes, live zoom, and the info-click behaviour change</name>

  <read_first>
    - `.planning/phases/118-zoom-aware-layer-legend/118-02-SUMMARY.md` — the chip copy strings AS SHIPPED; quote those exact strings in the UAT script rather than the ones planned here, if they differ
    - `.planning/phases/118-zoom-aware-layer-legend/118-01-SUMMARY.md` — the info-click behaviour change, for check UAT-118-G
    - `packages/web/src/styles/global.css` — the three opacity values (0.45 / 0.55 / 0.65) whose distinguishability is what UAT-118-D and UAT-118-E actually test
  </read_first>

  <files>
    (no code files — verification only; the artifact is
    `.planning/phases/118-zoom-aware-layer-legend/118-UAT.md` recording the operator's verdicts)
  </files>

  <action>
    1. Run the full gate set in `<acceptance_criteria>` below and paste each command's literal output
       into the checkpoint message. Do NOT present the UAT until every gate is green — an operator
       should never be asked to eyeball a build whose own tests are failing.
    2. Re-read `118-02-SUMMARY.md` for the chip copy AS SHIPPED and substitute those exact strings
       into UAT-118-B / UAT-118-D if they differ from `zoom 3–10` / `zoom 12–18`.
    3. Present the `<how-to-verify>` script verbatim, checks A through H, and stop. This is blocking:
       do not proceed, do not mark the phase complete, and do not self-assess any of these checks.
    4. When the operator replies, write their verbatim per-check verdicts into
       `.planning/phases/118-zoom-aware-layer-legend/118-UAT.md` and into `118-03-SUMMARY.md`.
  </action>

  <what-built>
    - `lib/zoomRangeBounds.ts` — one shared predicate; both `applyZoomRangeToLayer` and the info-click
      gate now derive from it (the gate previously used a different, wrong formula).
    - `resolveLegendLayers` — three-state `zoomActive` + raw `zoomRange` per layer.
    - `LayersLegendPanel` — `--zoom-inactive` modifier (opacity 0.65 + a `var(--warning)` left rule)
      and a configured-range chip reusing `.layers-legend-panel-mode-chip`.
    - Both consumers (in-map legend and the standalone Legend widget) read live zoom from
      `mapCurrentViewStore`.
    Automated gates: web `tsc` clean; `npx vitest run` 100% (baseline 3990 / 175 files, plus this
    phase's additions); `theme-guard` 150/150; `packages/server` untouched.

    **Why this checkpoint is blocking rather than automated.** Three of this phase's requirements are
    not honestly machine-checkable in this repository:
    (1) `theme-guard.spec.ts` ALLOWLISTS `src/styles/global.css` from its hex scan — for allowlisted
        files it asserts hex IS present, never that it is absent. A raw colour literal there passes
        every gate. The audit in plan 118-02 proved no hex was ADDED; it cannot prove the tokens
        chosen are legible in both themes. That is exactly how Phase 114's `.onboarding-banner`
        light-mode defect shipped.
    (2) "Zoom-inactive, eye-off and active read as three DIFFERENT things" is a perceptual claim.
        `0.45` vs `0.55` vs `0.65` are numerically distinct; whether a human can tell them apart is
        not something a grep can answer.
    (3) The spec's `mapCurrentViewStore` mock has no zustand subscription, so no automated test in
        this harness can prove the panel re-renders on a real store write without a manual rerender.
  </what-built>

  <how-to-verify>
Run against the live Kinetica instance. Record PASS / FAIL / NOT-EXERCISED per check, verbatim.

**Setup (once).** Open a dashboard with a map widget that has at least FOUR layers, and configure
them via the Layers panel so that:
  - **L1** — no zoom range at all (leave minZoom/maxZoom unset). This is the regression canary.
  - **L2** — zoom range 3-10.
  - **L3** — zoom range 12-18.
  - **L4** — zoom range 3-10, and toggle its eye OFF.
Turn ON the map widget's "Layers panel" option so the in-map legend renders. Zoom the map to
roughly zoom 8.

- **UAT-118-A (ZLGND-V123-07 — the regression canary, check this FIRST).**
  Look at **L1**. It must look EXACTLY as it did before this phase: normal brightness, no coloured
  left rule, and **no chip of any kind** next to its name. Most layers in real dashboards have no
  configured range, so a regression here is the most visible possible failure.
  → PASS only if L1 is indistinguishable from its previous appearance.

- **UAT-118-B (ZLGND-V123-01/03 — active vs inactive, and the range chip).**
  At zoom ~8: **L2** should look ACTIVE (normal brightness) and carry a chip reading `zoom 3–10`.
  **L3** should look ZOOM-INACTIVE (faded, with a coloured left rule) and carry a chip reading
  `zoom 12–18`.
  → PASS if you can tell at a glance which of L2/L3 the map is actually drawing, and the chip tells
    you what zoom would bring L3 back.

- **UAT-118-C (ZLGND-V123-04 — live update, no reload, no re-open).**
  WITHOUT reloading the page and WITHOUT collapsing/re-opening the panel, scroll-zoom the map in to
  about zoom 14. L2 and L3 must SWAP states (L2 becomes faded, L3 becomes normal) as you zoom.
  Then zoom back out to ~8 and confirm they swap back.
  → PASS if the panel tracks the map. → FAIL if you have to reload or re-open the panel to see it
    change, or if it only updates after some other interaction.
  Also note whether the swap happens smoothly at the end of each zoom gesture (expected — the store
  publishes on `moveend`, not continuously) or feels laggy/stuck.

- **UAT-118-D (ZLGND-V123-02 — zoom-inactive vs eye-off must NOT look the same). DARK THEME.**
  With the app in dark theme, at zoom ~8, compare three rows side by side:
  **L2** (active), **L3** (zoom-inactive), **L4** (eye-off).
  → PASS only if all THREE read as three different things without you having to think about it.
  → FAIL if L3 and L4 are hard to tell apart — that would replace one ambiguity with another, which
    is the exact failure this phase exists to prevent.
  Also confirm L3's left rule colour is clearly visible against the panel background, and that its
  chip text (`zoom 12–18`) is still fully legible despite the row being faded.

- **UAT-118-E (ZLGND-V123-02 + criterion 7 — SAME CHECK, LIGHT THEME).**
  Switch the app to light theme and repeat UAT-118-D exactly. This is a SEPARATE check, not a
  formality: the amber `--warning` token has different values per theme (`#f59e0b` dark /
  `#d97706` light) and no automated gate in this repository can see a colour problem in `global.css`.
  → PASS only if the three states are still clearly distinct AND the left rule and chip text are
    legible on the light background.
  → If the left rule looks washed out or the faded row is unreadable in light mode, FAIL and say so —
    that is a real defect, not a nitpick.

- **UAT-118-F (ZLGND-V123-06 — the standalone Legend widget).**
  Add a standalone **Legend** widget to the dashboard and bind it to the map from the setup. It must
  show the SAME indication as the in-map panel (L3 faded with its chip at zoom ~8, flipping when you
  zoom), updating live as you zoom the map.
  Then remove the MAP widget from the dashboard (or open a dashboard where the legend's bound map is
  not present) while leaving the Legend widget bound to it. The Legend must fall back to plain,
  today's appearance — no fading, **no chips at all** — and must NOT show the "Source map widget not
  found. Reconfigure the legend." orphan message, because the binding is still valid.
  → FAIL if it shows every zoom-limited layer as faded (that is the "confidently wrong" failure), or
    if it shows the orphan/reconfigure UI.

- **UAT-118-G (the DELIBERATE info-click behaviour change — read this before testing).**
  This phase intentionally changed info-click behaviour and it is not a side effect. Previously the
  info-click gate used raw inclusive bounds while OpenLayers used a translated bound, so with a layer
  configured `minZoom: 3`, the map DREW the layer at fractional zoom 2.9 but clicking it returned
  nothing. Both now use the same rule.
  To test: set a layer's range to something like 3-10 and zoom to just under the lower bound (scroll
  slowly so you land on a fractional zoom like 2.9 — the layer is still visibly drawn on the map).
  Click on a feature of that layer.
  → PASS if the info popup now returns records for a layer you can see.
  → Also confirm the gate still WORKS: zoom well below the range (say zoom 1.5) until the layer is no
    longer drawn, click where it was, and confirm you get nothing from it.
  → FAIL if clicking a clearly-visible layer still returns nothing, or if a layer that is NOT drawn
    now returns records.

- **UAT-118-H (the operator's original complaint).**
  Ignoring every check above: zoom around the map for a minute the way you normally would.
  → Is the question *"which layer is responsible for what I'm looking at?"* now answerable at a
    glance? That is the feature's actual success test.
  → Also confirm the reading of "should be clear" as *faded, still listed* (rather than *removed from
    the panel*) matches what you wanted. If you would rather inactive rows disappear entirely, say so
    now — it is a small change, but it is a different decision from the one locked in CONTEXT.md.
  </how-to-verify>

  <acceptance_criteria>
    Before presenting this checkpoint, confirm and paste the output of the full gate set:
    1. `cd packages/web && npx tsc --noEmit` → exit 0.
    2. `cd packages/web && npx vitest run` → 100% passing, 0 failed. Record the file/test totals.
       (Baseline before this phase, confirmed: **175 files / 3990 tests**. Expected after all three
       plans: **176 files** — `zoomRangeBounds.spec.ts` is new — and **3990 + 13 + 7 + 7 + 4 + 4 = 4025**
       tests. If the totals differ, reconcile them explicitly in the SUMMARY rather than restating
       whatever the run printed.) Note the known v1.22 finding that this suite is non-deterministic
       under parallel load: if a file reddens, re-run it in isolation, and record both outcomes rather
       than re-running until it is green.
    3. `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` → **150 passed (150)**.
    4. `git status --porcelain packages/server` → empty output.
    5. `grep -rn "isLayerActiveAtZoom\|toOlZoomBounds" packages/web/src --include=*.ts --include=*.tsx | grep -v spec | grep -v "lib/zoomRangeBounds.ts" | wc -l` = **2** —
       exactly two production consumers outside the defining module (MapChartRenderer's two uses count
       as its one import line plus one call each; reconcile the exact number against what plans 01 and
       02 actually wrote and state it). The point of this criterion is to prove no THIRD
       implementation of the range comparison was introduced anywhere.
    Then present the UAT. The phase is NOT complete until the operator has answered every check
    A through H.
  </acceptance_criteria>

  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run && npx vitest run src/styles/theme-guard.spec.ts && git status --porcelain packages/server</automated>
  </verify>

  <done>
    All automated gates are green with their output recorded, and the operator has returned a
    PASS / FAIL / NOT-EXERCISED verdict for every one of UAT-118-A … UAT-118-H, captured verbatim in
    `118-UAT.md`. Any FAIL is carried as an open gap, not smoothed over.
  </done>

  <resume-signal>
    Reply with a PASS / FAIL / NOT-EXERCISED verdict for each of UAT-118-A … UAT-118-H. For any FAIL,
    describe what you saw. Type "approved" only if all eight are PASS.
  </resume-signal>
</task>

</tasks>

<verification>
- `cd packages/web && npx tsc --noEmit` → exit 0.
- `cd packages/web && npx vitest run` → 100%, 0 failed; totals recorded and reconciled against the 175/3990 baseline.
- `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` → 150 passed (150).
- `git status --porcelain packages/server` → empty.
- Both mutation probes ran (dep-array removal → ZLM1; `?? 0` coercion → ZLR2), reddened the NAMED test, and were reverted.
- Operator UAT answered for all eight checks A-H.
</verification>

<success_criteria>
- Both legend consumers derive zoom activity from `mapCurrentViewStore` via a reactive primitive
  selector — neither reads a ref imperatively, and both have the value in their memo dependencies.
- The standalone Legend widget's "bound map not mounted" case produces today's appearance and does
  NOT enter the orphan UI; a coercion of the unknown zoom reddens a named test.
- The operator has confirmed, by eye and in BOTH themes, that active / zoom-inactive / eye-off read
  as three different things and that the range chip is legible — the part of criterion 7 that no gate
  in this repository can check.
- The operator has confirmed the info-click change behaves as intended in both directions (a visible
  layer at fractional zoom now responds; a non-drawn layer still does not).
</success_criteria>

<output>
After completion, create `.planning/phases/118-zoom-aware-layer-legend/118-03-SUMMARY.md`.
Record in it: both mutation-probe transcripts, the full-suite totals with the baseline reconciliation,
the operator's verbatim verdict for each of UAT-118-A … UAT-118-H, and — if UAT-118-H surfaced a
preference for removing inactive rows rather than fading them — log it as a deferred item rather than
acting on it, since fading is the locked CONTEXT.md decision.
</output>
