---
phase: 24-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/24-verification/24-01-UAT-NOTES.md
autonomous: false
requirements:
  - VERIFY-V14-01

must_haves:
  truths:
    - "Operator clicked an info-enabled map widget in lat/lon spatial mode and a popup opened at the click position showing the nearest records from the first eligible layer (POPUP-V14-01 visual proof for lat/lon mode)"
    - "Operator clicked an info-enabled map widget in WKT spatial mode and a popup opened with records ranked by STXY_DISTANCE in degrees (POPUP-V14-01 visual proof for WKT mode)"
    - "Operator confirmed that clicking an info-enabled map widget backed by a Kinetica GEOMETRY column (containing WKT content) issues a request with spatialMode='wkb' and the server responds with the live ST_DISTANCE / ST_GEOMFROMTEXT SQL path (post-88c6a86 revision); operator records the observed HTTP status + response shape"
    - "Operator switched layers in the popup dropdown and saw the record list reset to page 1 with the new layer's first 50 records (POPUP-V14-02)"
    - "Operator clicked Next/Back in the single-record nav and saw 'Record N of M(+)' update; clicking Next at the last loaded record with hasMore=true auto-fired the next-page fetch (Session Fix #2 single-record nav)"
    - "Operator opened a layer with info_template configured and saw HTML template-rendered records; switched to a layer without info_template and saw plain key-value fallback in the same session without page refresh (POPUP-V14-04 — both modes visible)"
    - "Operator changed infoPopupWidthPx and infoPopupHeightPx in MapConfigPanel's INFO POPUP section, re-clicked the map, and saw the popup render at the configured dimensions; popup size is bounded 200–1200 px (Session Fix #2 resize config)"
    - "Operator clicked near each of the 4 map widget corners and observed the popup anchor flipping to stay inside the map widget bounds (top-right → top-left → bottom-right → bottom-left fallback; Session Fix #3 edge-aware positioning)"
    - "Operator clicked an info-enabled map widget and confirmed the close-X does NOT overlap the layer dropdown (Session Fix #2 close-button overlap fix — .info-selection-header reserves 32px on the right)"
    - "Operator confirmed the Info Card on the same dashboard renders the same selected record(s) for the same layer as the popup, in both template and key-value modes, without a page refresh (VERIFY-V14-01 criterion 2)"
    - "Operator typed 'approved' (or recorded a specific gap with file references) at the resume signal — UAT outcomes captured in 24-01-UAT-NOTES.md for 24-03 to consume"
  artifacts:
    - path: ".planning/phases/24-verification/24-01-UAT-NOTES.md"
      provides: "Per-step PASS/FAIL/DEFERRED outcomes for each UAT step in this plan; consumed by 24-03 to author 24-VERIFICATION.md"
      contains: "step:"
  key_links:
    - from: "Operator UAT outcomes"
      to: "24-03 24-VERIFICATION.md authoring plan"
      via: "24-01-UAT-NOTES.md structured notes"
      pattern: "step:.*status:"
---

<objective>
Operator-driven UAT covering all three spatial modes (lat/lon, WKT, Kinetica-GEOMETRY/WKB-route), popup behaviour, Info Card parity, single-record nav, popup resize config, edge-aware positioning, and the close-X overlap fix. This is a checkpoint plan — the executor sets up state and prompts the operator; the operator performs the click flows and records outcomes.

Purpose: Cover VERIFY-V14-01 success criteria 1 + 2 with concrete walkthrough steps. Address Session Fix items #2 (single-record nav, popup resize, close-X overlap) and #3 (edge-aware positioning) plus the live Kinetica-geometry SQL revision from Session Fix #1.

Output:
  - .planning/phases/24-verification/24-01-UAT-NOTES.md with one row per UAT step, status (PASS / FAIL / DEFERRED), evidence (network tab assertions, screenshot paths, or operator notes).

No production code is modified in this plan. Bugs discovered during UAT are NOT fixed inline — they are recorded as gaps and routed to a follow-on gap-closure cycle (mirrors v1.3 Phase 17's 17-02/17-03/17-04 cadence).
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.3-phases/17-verification/17-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator UAT — lat/lon + WKT spatial modes (popup open, layer switch, pagination, template vs key-value)</name>
  <files>.planning/phases/24-verification/24-01-UAT-NOTES.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (POPUP-V14-01..06 + SPATIAL-V14-01..02 + Session Fix #1 bbox projection)
    - kinetica_bi/src/components/charts/MapChartRenderer.tsx (click handler + EPSG:3857→4326 transform from Session Fix #1)
    - kinetica_bi/src/components/InfoSelectionView.tsx (popup body — dropdown, list, Load more)
    - kinetica_bi/server/src/spatialQuery.ts (buildLatLonQuery uses GEODIST in meters; buildWktQuery uses STXY_DISTANCE in degrees)
  </read_first>
  <action>
    Executor pre-flight automation (run BEFORE pausing for operator):
      1. Confirm backend env is loadable: `test -f kinetica_bi/server/.env`. If missing, STOP and request operator to provide .env before proceeding.
      2. Start backend from a fresh shell with the env-load workaround: `cd kinetica_bi/server && set -a && source .env && set +a && npm run dev` (per MEMORY.md "Backend dev .env load order"). Run in background; confirm Express :4000 boots cleanly by checking for "listening on 4000" or equivalent.
      3. Start frontend: `cd kinetica_bi && npm run dev`. Run in background; confirm Vite :5173 ready.
      4. Create the UAT notes scaffold at `.planning/phases/24-verification/24-01-UAT-NOTES.md` with this YAML structure ready for the operator to fill in (do NOT pre-populate `status:` values — leave as `pending`):

         ```yaml
         ---
         plan: 24-01
         operator: rpereira@kinetica.com
         started_on: <today YYYY-MM-DD>
         ---
         # Task 1 — lat/lon + WKT spatial modes
         - step: 1.1
           name: "lat/lon mode — basic popup open + 50-record page"
           status: pending
           evidence: ""
         - step: 1.2
           name: "WKT mode — basic popup open + 50-record page"
           status: pending
           evidence: ""
         - step: 1.3
           name: "layer switch resets to page 1"
           status: pending
           evidence: ""
         - step: 1.4
           name: "single-record nav — Back / Next / Record N of M(+) + auto-fetch on Next at last loaded"
           status: pending
           evidence: ""
         - step: 1.5
           name: "template vs key-value in the same session"
           status: pending
           evidence: ""
         - step: 1.6
           name: "Info Card parity with popup"
           status: pending
           evidence: ""
         ```

      5. PAUSE for the operator. Display the UAT step instructions from <how-to-verify> verbatim in the chat. Operator performs steps; operator (or executor on operator's behalf) updates 24-01-UAT-NOTES.md `status:` and `evidence:` fields per step.

    Executor is forbidden from modifying any file under `kinetica_bi/src/` or `kinetica_bi/server/src/` in this plan — any bug discovered is captured as a gap in 24-01-UAT-NOTES.md only, NOT fixed inline (mirrors Phase 17's 17-02/17-03 gap-closure cadence).
  </action>
  <what-built>
    - lat/lon spatial-mode click → POST /api/info/query with spatialMode='latlon' → GEODIST-ordered records → popup opens
    - WKT spatial-mode click → POST /api/info/query with spatialMode='wkt' → STXY_DISTANCE-ordered records → popup opens
    - In-popup layer dropdown switching with page reset
    - "Load more" pagination (50 per page) — Next at last loaded record + hasMore=true auto-fires the next page (Session Fix #2)
    - Template rendering vs key-value fallback in the same popup session
    - Session Fix #1 bbox projection: click bbox transforms EPSG:3857 → EPSG:4326 client-side BEFORE being included in the POST payload (no million-meter radius bug)
  </what-built>
  <how-to-verify>
    Operator UAT steps — record PASS / FAIL / DEFERRED + evidence in .planning/phases/24-verification/24-01-UAT-NOTES.md after each.

    STEP 1.1 (lat/lon mode — basic popup open + 50-record page):
      - Open a dashboard with a map widget whose included layer has spatialMode='latlon' and info_enabled=1.
      - Open DevTools → Network tab, filter on `/api/info/query`.
      - Click on a dense point cluster.
      - EXPECT: popup opens at the click position; "Record 1 of <M>(+)" header visible; ≤50 records in the layer entry; Network tab shows POST /api/info/query with `spatialMode: "latlon"` and the mapBbox numbers in EPSG:4326 range (lon −180..180, lat −90..90 — NOT EPSG:3857 meter values in the millions). HTTP 200.
      - Record: PASS / FAIL (+ what the bbox values were if FAIL). This validates Session Fix #1 bbox projection.

    STEP 1.2 (WKT mode — basic popup open + 50-record page):
      - Switch the dashboard (or open another) with a map widget whose included layer has spatialMode='wkt' and info_enabled=1.
      - Click on a dense point cluster.
      - EXPECT: popup opens; Network tab shows POST /api/info/query with `spatialMode: "wkt"`; HTTP 200; rows ordered by ascending STXY_DISTANCE (closest record visible first).
      - Record: PASS / FAIL.

    STEP 1.3 (layer switch resets to page 1):
      - With at least 2 eligible layers visible, click on the map to open the popup.
      - In the popup's layer dropdown, switch to a different eligible layer.
      - EXPECT: a NEW POST /api/info/query fires with the same click coords but a different layerId/tableId; record counter resets to "Record 1 of <M>(+)"; popup body updates with the new layer's first 50 records.
      - Record: PASS / FAIL.

    STEP 1.4 (single-record nav — Back / Next / Record N of M(+)):
      - In the open popup, click Next several times. Then click Back.
      - EXPECT: "Record N of M(+)" updates; Back/Next button-disabled state is correct at boundaries.
      - Click Next when at the last loaded record AND hasMore=true (typically record 50 of 50+).
      - EXPECT: a NEW POST /api/info/query auto-fires with page=1 (or page=N+1) returning the next 50 rows; the (+) in the counter resolves to a concrete number when hasMore=false.
      - Record: PASS / FAIL. (Closes Session Fix #2 single-record nav.)

    STEP 1.5 (template vs key-value in the same session):
      - With the popup open, switch the layer dropdown to a layer with info_template SET (HTML template).
      - EXPECT: each record renders as the configured HTML template.
      - Switch the dropdown to a layer with info_template NULL.
      - EXPECT: same popup, body switches to plain key-value table of selected columns. No page refresh occurred between the two states.
      - Record: PASS / FAIL. (Closes POPUP-V14-04.)

    STEP 1.6 (Info Card parity with popup):
      - Add an Info Card widget to the same dashboard, configured for one of the layers exercised above.
      - Without refreshing, click the map again to populate the popup.
      - EXPECT: Info Card body renders the same record content as the popup body for that layer (same template output OR same key-value fallback). Switching layers in the popup vs. Info Card layer-config target shows consistent behaviour.
      - Record: PASS / FAIL. (Closes VERIFY-V14-01 criterion 2 visually.)

    Type "approved" if all steps PASS, or describe each FAIL with the affected file path / network detail / screenshot in 24-01-UAT-NOTES.md so 24-03 can transcribe them with diagnoses.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/24-verification/24-01-UAT-NOTES.md && grep -c "^- step: 1\." .planning/phases/24-verification/24-01-UAT-NOTES.md | awk '{ if ($1 == 6) exit 0; else exit 1 }' && ! grep -E "^  status: pending$" .planning/phases/24-verification/24-01-UAT-NOTES.md | grep -q "step: 1\."</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/24-verification/24-01-UAT-NOTES.md exists and contains exactly 6 step rows for Task 1 (1.1 through 1.6), each with `status:` set to PASS / FAIL / DEFERRED (no remaining `pending` for Task 1 steps).
    - For STEP 1.1, the recorded `mapBbox` evidence is in EPSG:4326 range (lon −180..180, lat −90..90), NOT EPSG:3857 meter values — operator notes this verbatim in the `evidence:` field.
    - For STEP 1.4, the recorded evidence references either "Next at last loaded record auto-fires page+1" or describes a FAIL with what happened.
    - Operator typed "approved" at the resume signal OR each FAIL is captured with a concrete reproduction note that 24-03 can include in 24-VERIFICATION.md.
  </acceptance_criteria>
  <done>24-01-UAT-NOTES.md has 6 Task 1 step rows with non-pending status values; operator-approval or FAIL-diagnoses captured.</done>
  <resume-signal>Type "approved" or describe each failed step with affected file/network evidence so 24-03 can transcribe diagnoses verbatim.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator UAT — Kinetica-GEOMETRY (WKB-route) live SQL, popup resize, edge-aware positioning, close-X overlap</name>
  <files>.planning/phases/24-verification/24-01-UAT-NOTES.md</files>
  <read_first>
    - .planning/phases/24-verification/24-01-UAT-NOTES.md (Task 1 outcomes — context for Task 2)
    - .claude/projects/-Users-rydelpereira-Documents-projects-codex-kinetica-bi/memory/project_wkb_tech_debt.md (TD-V14-WKB-SPIKE current state — endpoint behaviour for spatialMode='wkb')
    - kinetica_bi/server/src/spatialQuery.ts (current buildWkbQuery implementation per Session Fix #1 revision: ST_DISTANCE(col, ST_GEOMFROMTEXT('POINT(x y)')))
    - kinetica_bi/src/components/MapConfigPanel.tsx (INFO POPUP section — infoPopupWidthPx / infoPopupHeightPx inputs)
    - kinetica_bi/src/lib/pickPopupAnchor.ts (Session Fix #3 — 4-corner anchor flip helper)
    - kinetica_bi/src/components/InfoPopup.tsx (popup chrome — .info-popup container and .info-selection-header right-padding for close-X)
  </read_first>
  <action>
    Executor pre-flight automation (run BEFORE pausing for operator):
      1. Confirm backend + frontend are still running from Task 1. If not, restart per Task 1 pre-flight steps.
      2. Append a Task 2 step block to 24-01-UAT-NOTES.md (do NOT overwrite Task 1 rows):

         ```yaml
         # Task 2 — Kinetica-GEOMETRY + popup resize + edge-aware + close-X overlap
         - step: 2.1
           name: "Kinetica-GEOMETRY column — live SQL path (ST_DISTANCE + ST_GEOMFROMTEXT)"
           status: pending
           evidence: ""
         - step: 2.2
           name: "popup resize config — 200×200 min and 1200×1200 max"
           status: pending
           evidence: ""
         - step: 2.3
           name: "edge-aware positioning — 4-corner anchor flip"
           status: pending
           evidence: ""
         - step: 2.4
           name: "close-X does not overlap layer dropdown"
           status: pending
           evidence: ""
         ```

      3. PAUSE for the operator. Display the UAT step instructions from <how-to-verify> verbatim. Operator performs steps; operator (or executor on operator's behalf) updates `status:` and `evidence:` fields per step.

    Executor is forbidden from modifying any file under `kinetica_bi/src/` or `kinetica_bi/server/src/` in this plan.
  </action>
  <what-built>
    - WKB-route SQL revised twice (Session Fix #1 → STXY_DISTANCE; then re-revised to ST_DISTANCE(col, ST_GEOMFROMTEXT('POINT(x y)')) because Kinetica geometry columns rejected STXY_DISTANCE with "function: 'stxy_distance' has invalid argument list: geometry,decimal8,decimal8,..."). The TD-V14-WKB-SPIKE memory file records this closure for WKT-content-in-Kinetica-geometry case (deferred only for true WKB-binary).
    - infoPopupWidthPx / infoPopupHeightPx widget config (200–1200 px, default 360×400) wired into MapConfigPanel INFO POPUP section (Session Fix #2 resize).
    - pickPopupAnchor pure helper flips OL Overlay anchor among 4 corners (top-right → top-left → bottom-right → bottom-left) keeping popup inside map widget (Session Fix #3 edge-aware positioning).
    - .info-selection-header reserves 32px on the right so the close-X does not overlap the layer dropdown (Session Fix #2 close-X overlap).
  </what-built>
  <how-to-verify>
    Operator UAT steps — record PASS / FAIL / DEFERRED + evidence in 24-01-UAT-NOTES.md.

    STEP 2.1 (Kinetica-GEOMETRY column — live SQL path):
      - Open a dashboard whose map widget includes a layer backed by a Kinetica GEOMETRY column (WKT content stored in a Kinetica geometry-typed column — NOT a text WKT column and NOT a true WKB-binary column).
      - Operator confirms which dashboard / layer / table / column this is in 24-01-UAT-NOTES.md by name.
      - Click on a dense point cluster.
      - EXPECT: Network tab shows POST /api/info/query with `spatialMode: "wkb"` (the route used for Kinetica-GEOMETRY-typed columns). HTTP status 200 with rows AND server logs show the executed SQL contained `ST_DISTANCE(<col>, ST_GEOMFROMTEXT('POINT(<x> <y>)'))` — NOT STXY_DISTANCE (which previously errored with "invalid argument list: geometry,decimal8,decimal8").
      - If operator does NOT have a Kinetica-GEOMETRY-backed layer reachable: record this as DEFERRED with rationale; this is acceptable per TD-V14-WKB-SPIKE which already defers the TRUE WKB-binary path. The Kinetica-GEOMETRY case is the additional sub-case Session Fix #1 closed.
      - Record: PASS / FAIL / DEFERRED.

    STEP 2.2 (popup resize config — 200×200 minimum and 1200×1200 maximum):
      - Open MapConfigPanel for an info-enabled map widget. Scroll to the INFO POPUP section.
      - Change `infoPopupWidthPx` from 360 to 200 (minimum). Change `infoPopupHeightPx` to 200. Save.
      - Click the map → popup opens. EXPECT: popup renders ~200×200 px.
      - Re-open config, change to 1200 / 1200. Save. Click map.
      - EXPECT: popup renders ~1200×1200 px (or clamped to widget bounds if widget is smaller).
      - Attempt to set 100 (below min). EXPECT: input clamps to 200 OR shows inline validation. Attempt 1500 (above max). EXPECT: clamps to 1200.
      - Record: PASS / FAIL. NOTE: this affects ONLY the popup. Info Card sizes via dashboard grid — confirm Info Card on the same dashboard is unaffected by these resize inputs.

    STEP 2.3 (edge-aware positioning — 4-corner anchor flip):
      - With a map widget visible at a typical dashboard size (e.g. 6×6 grid cells), click near each of the 4 corners inside the widget.
        - Center-ish click: EXPECT popup anchored top-right of cursor (default).
        - Click in top-right corner area: EXPECT popup flips to top-left of cursor (so it stays inside widget bounds).
        - Click in bottom-left corner area: EXPECT popup flips to bottom-right of cursor.
        - Click in bottom-right corner area: EXPECT popup flips to bottom-left of cursor.
        - If all 4 fail-cases (popup would clip), EXPECT fallback top-right.
      - Operator verifies popup is always fully inside the map widget bounds. Optional: capture a screenshot of one flipped-corner state.
      - Record: PASS / FAIL.

    STEP 2.4 (close-X does not overlap layer dropdown):
      - Open the popup (any layer). Inspect the .info-selection-header band.
      - EXPECT: the close-X is in the top-right corner; the layer dropdown sits to its left with at least 32px clearance from the X (.info-selection-header right-padding). No visual overlap; clicking the dropdown does not accidentally hit the X.
      - Record: PASS / FAIL. Optional screenshot.

    Type "approved" if all steps PASS, or describe each FAIL/DEFERRED with file/CSS/network evidence in 24-01-UAT-NOTES.md.
  </how-to-verify>
  <verify>
    <automated>grep -c "^- step: 2\." .planning/phases/24-verification/24-01-UAT-NOTES.md | awk '{ if ($1 == 4) exit 0; else exit 1 }' && ! awk '/^- step: 2\./,/^- step:|^$/' .planning/phases/24-verification/24-01-UAT-NOTES.md | grep -q "status: pending"</automated>
  </verify>
  <acceptance_criteria>
    - 24-01-UAT-NOTES.md contains step rows 2.1, 2.2, 2.3, 2.4 each with `status:` set to PASS / FAIL / DEFERRED.
    - For STEP 2.1, the recorded evidence either (a) cites the live SQL `ST_DISTANCE(...ST_GEOMFROMTEXT...)` route + HTTP 200, OR (b) is marked DEFERRED with the rationale "no Kinetica-GEOMETRY-backed layer reachable".
    - For STEP 2.2, the recorded evidence cites the min/max clamp behaviour (200/1200) AND notes that Info Card sizing is unaffected.
    - For STEP 2.3, the recorded evidence enumerates outcomes for at least 3 of the 4 corner cases.
    - For STEP 2.4, the recorded evidence states "no overlap" OR cites the overlap as a FAIL with screenshot path.
    - Operator typed "approved" OR each FAIL/DEFERRED captured in 24-01-UAT-NOTES.md with a reproduction note.
  </acceptance_criteria>
  <done>24-01-UAT-NOTES.md has 4 Task 2 step rows with non-pending status values; operator-approval or FAIL-diagnoses captured for all four.</done>
  <resume-signal>Type "approved" or describe each failed/deferred step with file/CSS/network/screenshot evidence.</resume-signal>
</task>

</tasks>

<verification>
After both checkpoint tasks complete:
  - File .planning/phases/24-verification/24-01-UAT-NOTES.md exists with 10 step rows (1.1–1.6 + 2.1–2.4), each with `status:` set.
  - `grep -c "^- step: " .planning/phases/24-verification/24-01-UAT-NOTES.md` returns 10.
  - No row has `status: pending` remaining.
  - Operator-typed "approved" lines or per-step gap descriptions exist for both tasks.
</verification>

<success_criteria>
- VERIFY-V14-01 success criteria 1 (popup open + layer switch + pagination + template rendering across lat/lon, WKT, Kinetica-GEOMETRY/WKB-route) covered by Task 1 STEP 1.1–1.5 + Task 2 STEP 2.1.
- VERIFY-V14-01 success criterion 2 (Info Card receives same selection as popup, template + key-value modes, no refresh) covered by Task 1 STEP 1.6.
- All four Session Fixes addressed in this plan recorded with status: Fix #1 bbox projection (STEP 1.1), Fix #1 Kinetica-GEOMETRY SQL (STEP 2.1), Fix #2 single-record nav (STEP 1.4), Fix #2 popup resize (STEP 2.2), Fix #2 close-X overlap (STEP 2.4), Fix #3 edge-aware positioning (STEP 2.3).
</success_criteria>

<output>
After both tasks complete, create `.planning/phases/24-verification/24-01-SUMMARY.md` linking to 24-01-UAT-NOTES.md and summarizing PASS / FAIL / DEFERRED counts per VERIFY-V14-01 criterion.
</output>
