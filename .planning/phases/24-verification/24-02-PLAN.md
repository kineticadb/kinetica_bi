---
phase: 24-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/24-verification/24-02-UAT-NOTES.md
autonomous: false
requirements:
  - VERIFY-V14-01

must_haves:
  truths:
    - "Operator exercised the full map-click → popup → Info Card flow under AUTH_MODE=password and recorded HTTP status + auth-mode evidence (VERIFY-V14-01 criterion 1 — password auth mode)"
    - "Operator exercised the full map-click → popup → Info Card flow under AUTH_MODE=oidc and recorded HTTP status + bearer-token-auth evidence (VERIFY-V14-01 criterion 1 — OIDC auth mode)"
    - "Operator confirmed that disabling info_enabled on one layer removes ONLY that layer from the popup dropdown on the next click (per-layer kill switch, POPUP-V14-04/CONFIG-V14-03 closure)"
    - "Operator confirmed that disabling infoEnabled on the map widget config produces ZERO POST /api/info/query requests on click — Network tab confirms no request fired (per-widget kill switch, POPUP-V14-06 closure)"
    - "Operator switched dashboards while a popup was open and confirmed the popup does NOT reopen with stale records in the new dashboard — useInfoSelectionStore + useLastInfoClickContextStore both reset in the four-store reset block (STORE-V14-03, VERIFY-V14-01 criterion 3 dashboard-switch side)"
    - "Operator logged out while a popup / Info Card had content and confirmed re-login starts with empty info state — no stale records from the prior session (STORE-V14-04, VERIFY-V14-01 criterion 3 logout side)"
    - "Operator applied a v1.3 filter (chart drill-down) to a table that has a map widget with an info-enabled layer, then clicked the map; the POST /api/info/query payload includes the filter-view name from useFilterViewStore.views[layer.table_id] AND the server executes FROM <viewName> instead of FROM <raw_table> — Session Fix #1 filter-view alignment is live"
    - "If a v1.3 filter-narrowing fixture (ki_home.v13_filter_fixture per TD-V12-04) is available, operator materializes it once and walks a single click-with-active-filter UAT step against it; otherwise records TD-V12-04 as still-DEFERRED in 24-02-UAT-NOTES.md with rationale"
    - "Operator typed 'approved' (or recorded a specific gap with file references) at the resume signal — outcomes captured in 24-02-UAT-NOTES.md for 24-03 to consume"
  artifacts:
    - path: ".planning/phases/24-verification/24-02-UAT-NOTES.md"
      provides: "Per-step PASS/FAIL/DEFERRED outcomes for auth modes + kill switches + lifecycle resets + filter-view popup + TD-V12-04 status; consumed by 24-03 to author 24-VERIFICATION.md"
      contains: "step:"
  key_links:
    - from: "Operator UAT outcomes"
      to: "24-03 24-VERIFICATION.md authoring plan"
      via: "24-02-UAT-NOTES.md structured notes"
      pattern: "step:.*status:"
    - from: "STEP 2.1 + 2.2 dashboard-switch + logout reset"
      to: "useInfoSelectionStore.reset() + useLastInfoClickContextStore.reset() four-store reset block"
      via: "App.tsx UNAUTHORIZED + DashboardsPage DashboardOpen cleanup"
      pattern: "useInfoSelectionStore.*reset"
---

<objective>
Operator-driven UAT covering both auth modes (password + OIDC), per-layer + per-widget kill switches, lifecycle resets (dashboard-switch + logout), the live filter-view-aligned popup (Session Fix #1 viewName routing), and an optional TD-V12-04 resolution attempt. This is a checkpoint plan — the executor sets up state and prompts the operator; the operator performs the flows and records outcomes.

Purpose: Cover VERIFY-V14-01 success criteria 3 + 4 with concrete walkthrough steps; also resolve OR explicitly defer TD-V12-04 per the ROADMAP notes for Phase 24.

Output:
  - .planning/phases/24-verification/24-02-UAT-NOTES.md with one row per UAT step, status (PASS / FAIL / DEFERRED), evidence (network tab, server logs, screenshots, or operator notes).

No production code is modified in this plan. Bugs discovered during UAT are NOT fixed inline — they are recorded as gaps for follow-on gap-closure cycles.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.3-phases/17-verification/17-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator UAT — both auth modes (password + OIDC) + per-layer kill switch + per-widget kill switch</name>
  <files>.planning/phases/24-verification/24-02-UAT-NOTES.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (POPUP-V14-06 per-widget kill switch + CONFIG-V14-03 per-layer kill switch; VERIFY-V14-01 criteria 1+4)
    - kinetica_bi/server/src/index.ts (POST /api/info/query route — confirm requireAuth + per-user-creds path mirrors v1.0/v1.1 kineticaSql auth branching)
    - kinetica_bi/src/components/charts/MapChartRenderer.tsx (Effect 6 — kill-switch-gated singleclick listener; getInfoEnabled gate; per-layer info_enabled filter in fan-out)
    - kinetica_bi/server/.env (AUTH_MODE switch — operator toggles between password and oidc)
  </read_first>
  <action>
    Executor pre-flight automation (run BEFORE pausing for operator):
      1. Verify backend `.env` is reachable: `test -f kinetica_bi/server/.env`. Confirm operator has both `password`-mode and `oidc`-mode env-var sets available (the operator may keep two .env templates; surface this to the operator at pause time).
      2. Create the UAT notes scaffold at `.planning/phases/24-verification/24-02-UAT-NOTES.md` with this YAML structure ready for the operator to fill in (status fields start as `pending`):

         ```yaml
         ---
         plan: 24-02
         operator: rpereira@kinetica.com
         started_on: <today YYYY-MM-DD>
         ---
         # Task 1 — auth modes + kill switches
         - step: 1.1
           name: "AUTH_MODE=password — full info-popup flow + audit-log auth_mode evidence"
           status: pending
           evidence: ""
         - step: 1.2
           name: "AUTH_MODE=oidc — full info-popup flow + Bearer-token evidence"
           status: pending
           evidence: ""
         - step: 1.3
           name: "per-layer kill switch — disabled layer drops from dropdown"
           status: pending
           evidence: ""
         - step: 1.4
           name: "per-widget kill switch — zero POST /api/info/query requests"
           status: pending
           evidence: ""
         ```

      3. PAUSE for the operator. Display the UAT step instructions from <how-to-verify> verbatim. Operator restarts the backend between STEP 1.1 (password) and STEP 1.2 (oidc) per the .env-load workaround in MEMORY.md.

    Executor is forbidden from modifying any file under `kinetica_bi/src/` or `kinetica_bi/server/src/` in this plan.
  </action>
  <what-built>
    - POST /api/info/query is mounted under requireAuth — per-user Kinetica credentials flow through (Basic for password mode, Bearer for OIDC mode) per v1.0/v1.1 kineticaSql auth branching.
    - Per-widget kill switch: `infoEnabled: false` on widget.config disables the OL singleclick listener entirely — no request fires, no popup opens (POPUP-V14-06).
    - Per-layer kill switch: `info_enabled = 0` on a dashboard_layers row removes that layer from the popup dropdown AND from the click fan-out (so its POST /api/info/query is never sent). Operator can toggle via the layer config panel Info Popup tab (CONFIG-V14-03).
  </what-built>
  <how-to-verify>
    Operator UAT steps — record PASS / FAIL / DEFERRED + evidence in .planning/phases/24-verification/24-02-UAT-NOTES.md after each.

    STEP 1.1 (AUTH_MODE=password — full info-popup flow):
      - Stop backend if running. Edit `kinetica_bi/server/.env` to `AUTH_MODE=password`. Start backend with the env-load workaround: `cd kinetica_bi/server && set -a && source .env && set +a && npm run dev` (per MEMORY.md).
      - Start frontend if not running. Log in via the password LoginPage branch with valid Kinetica credentials.
      - Open a dashboard with an info-enabled map widget. Click the map.
      - EXPECT: popup opens with records. Network tab POST /api/info/query returns HTTP 200. The session cookie is the v1.0 BI session cookie; per-user Kinetica creds flow through as Basic auth (verified by operator opening backend audit-log line — JSON one-liner contains `auth_mode: "password"` and the operator's username).
      - Record: PASS / FAIL + audit-log auth_mode value.

    STEP 1.2 (AUTH_MODE=oidc — full info-popup flow):
      - Stop backend. Edit `kinetica_bi/server/.env` to `AUTH_MODE=oidc` (with valid OIDC env vars). Restart backend with the env-load workaround.
      - Operator opens the app, completes the OIDC Authorization Code flow against the deployed IdP.
      - Open a dashboard with an info-enabled map widget. Click the map.
      - EXPECT: popup opens with records. Network tab POST /api/info/query returns HTTP 200. Backend audit-log line contains `auth_mode: "oidc"` and the OIDC-resolved username. Kinetica receives a Bearer token (not Basic) — confirmed by server logs or a one-time `console.log` of the Authorization header prefix.
      - Record: PASS / FAIL + audit-log auth_mode value. If OIDC env vars are not configured in this session: record DEFERRED with rationale. (Note: v1.3 Phase 17 verified live OIDC for materialize endpoint; SAME per-user-creds pattern applies here.)

    STEP 1.3 (per-layer kill switch — disabled layer drops from dropdown):
      - With a map widget that has ≥2 included layers, both info_enabled=1, click the map and confirm both layers appear in the dropdown.
      - Open the layer config for Layer A. Toggle info_enabled OFF. Save (auto-saves on debounce).
      - Click the map again.
      - EXPECT: popup opens with Layer B only. Layer A is NOT in the dropdown. Network tab confirms POST /api/info/query fired ONLY for Layer B (no request with Layer A's layerId). The fan-out skipped Layer A entirely.
      - Toggle Layer A info_enabled back ON. Click again — Layer A re-appears in the dropdown.
      - Record: PASS / FAIL.

    STEP 1.4 (per-widget kill switch — zero requests):
      - Open the MapConfigPanel INFO POPUP section for the map widget. Toggle `infoEnabled` OFF. Save.
      - Click the map repeatedly in multiple locations.
      - EXPECT: NO popup appears. Network tab shows ZERO POST /api/info/query requests fired across all clicks. The OL singleclick listener is not registered (or is registered but short-circuited at the kill-switch gate).
      - Toggle `infoEnabled` back ON. Click again — popup opens, request fires normally.
      - Record: PASS / FAIL. Critical: this MUST be zero requests, not "ignored response" — the kill switch suppresses the request entirely.

    Type "approved" if all steps PASS, or describe each FAIL/DEFERRED with the affected file path / network evidence / audit-log line in 24-02-UAT-NOTES.md.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/24-verification/24-02-UAT-NOTES.md && grep -c "^- step: 1\." .planning/phases/24-verification/24-02-UAT-NOTES.md | awk '{ if ($1 == 4) exit 0; else exit 1 }' && ! awk '/^- step: 1\./,/^- step: 2\.|^$/' .planning/phases/24-verification/24-02-UAT-NOTES.md | grep -q "status: pending"</automated>
  </verify>
  <acceptance_criteria>
    - 24-02-UAT-NOTES.md contains step rows 1.1, 1.2, 1.3, 1.4 each with `status:` set to PASS / FAIL / DEFERRED.
    - For STEP 1.1, the recorded evidence cites `auth_mode: "password"` in the backend audit-log line.
    - For STEP 1.2, the recorded evidence either (a) cites `auth_mode: "oidc"` in the audit-log line AND a Bearer-token Authorization header, OR (b) is DEFERRED with the rationale "OIDC env vars not configured in this session" + a note that v1.3 Phase 17 verified the same per-user-creds pattern under OIDC.
    - For STEP 1.3, the recorded evidence states "Layer A NOT in dropdown after toggle" AND "Network tab shows zero requests for Layer A's layerId".
    - For STEP 1.4, the recorded evidence states "zero POST /api/info/query requests across N clicks" — the kill switch is request-suppression, not response-discarding.
    - Operator typed "approved" OR each FAIL/DEFERRED captured with a concrete reproduction note.
  </acceptance_criteria>
  <done>24-02-UAT-NOTES.md has 4 Task 1 step rows with non-pending status values; operator-approval or FAIL-diagnoses captured.</done>
  <resume-signal>Type "approved" or describe each failed/deferred step with file/network/audit-log evidence.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator UAT — dashboard-switch + logout reset, filter-view-aligned popup (viewName routing), TD-V12-04 resolution attempt</name>
  <files>.planning/phases/24-verification/24-02-UAT-NOTES.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (STORE-V14-03 dashboard-switch reset + STORE-V14-04 logout reset + VERIFY-V14-01 criterion 3)
    - kinetica_bi/src/App.tsx (UNAUTHORIZED effect — four-store reset block: filterViewStore → filterStore → infoSelectionStore → lastInfoClickContextStore)
    - kinetica_bi/src/components/DashboardsPage.tsx (DashboardOpen cleanup effect — same four-store reset block)
    - kinetica_bi/src/store/infoSelectionStore.ts (reset() semantics)
    - kinetica_bi/src/store/lastInfoClickContextStore.ts (reset() semantics — added Phase 23)
    - kinetica_bi/src/components/charts/MapChartRenderer.tsx (Session Fix #1 viewName inclusion: passes useFilterViewStore.views[layer.table_id].viewName in POST payload when present)
    - kinetica_bi/server/src/index.ts (POST /api/info/query — server uses FROM <viewName> when payload includes a non-empty viewName)
    - kinetica_bi/server/scripts/test-fixtures/v13-filter-fixture.sql (reference SQL for TD-V12-04 fixture)
  </read_first>
  <action>
    Executor pre-flight automation (run BEFORE pausing for operator):
      1. Append a Task 2 step block to 24-02-UAT-NOTES.md (do NOT overwrite Task 1 rows):

         ```yaml
         # Task 2 — lifecycle resets + viewName routing + TD-V12-04
         - step: 2.1
           name: "dashboard-switch reset — stores cleared, no stale popup"
           status: pending
           evidence: ""
         - step: 2.2
           name: "logout reset — no stale records after re-login"
           status: pending
           evidence: ""
         - step: 2.3
           name: "filter-view-aligned popup — viewName routing in POST payload"
           status: pending
           evidence: ""
         - step: 2.4
           name: "TD-V12-04 fixture resolution attempt"
           status: pending
           evidence: ""
         ```

      2. PAUSE for the operator. Display the UAT step instructions from <how-to-verify> verbatim. Operator decides at STEP 2.4 whether to materialize the v1.3 fixture or carry TD-V12-04 forward as still-DEFERRED.

    Executor is forbidden from modifying any file under `kinetica_bi/src/` or `kinetica_bi/server/src/` in this plan.
  </action>
  <what-built>
    - Four-store reset block at App.tsx UNAUTHORIZED + DashboardsPage DashboardOpen cleanup: filterViewStore → filterStore → infoSelectionStore → lastInfoClickContextStore. Both lifecycle paths clear all info-popup state — no stale records leak across dashboard switches or logouts (Phase 20 + Phase 23).
    - Session Fix #1 viewName alignment: when useFilterViewStore.views[layer.table_id] is set (i.e. a v1.3 filter view is materialized for that table), MapChartRenderer includes `viewName` in the POST /api/info/query payload; server then issues `FROM <viewName>` instead of `FROM <raw_table>`, so the popup records match the filter view the WMS layer is actually showing.
    - TD-V12-04 reference fixture (`kinetica_bi/server/scripts/test-fixtures/v13-filter-fixture.sql`): committed during Phase 17 but never materialized during UAT. Operator may attempt to materialize and walk a single filtering-narrowed click step against it.
  </what-built>
  <how-to-verify>
    Operator UAT steps — record PASS / FAIL / DEFERRED + evidence in 24-02-UAT-NOTES.md.

    STEP 2.1 (dashboard-switch reset):
      - In Dashboard A, click an info-enabled map widget so a popup opens with records. (Optionally also open the dashboard's Info Card so the store has selections.)
      - Switch to Dashboard B using the existing dashboard switcher.
      - EXPECT: popup is closed in Dashboard A. In Dashboard B, no popup auto-opens and no Info Card is pre-populated with Dashboard A's records. Click anywhere on Dashboard B's map (if any) to confirm a fresh fetch occurs against Dashboard B's layers.
      - EXPECT in DevTools React/Zustand devtools (or by inspecting the rendered state): useInfoSelectionStore.activeLayerId is null AND useInfoSelectionStore.state is {} after the switch; useLastInfoClickContextStore.context is null.
      - Record: PASS / FAIL.

    STEP 2.2 (logout reset):
      - In any dashboard, click the map to open a popup with content. Confirm Info Card on the dashboard (if configured) also has content.
      - Click Logout in the topbar.
      - Confirm the LoginPage appears. Log back in.
      - Re-open the SAME dashboard.
      - EXPECT: popup is NOT open; Info Card displays the empty-state placeholder ("Click a point on the map to see details"); no stale records from the prior session.
      - Record: PASS / FAIL.

    STEP 2.3 (filter-view-aligned popup — viewName routing):
      - Open a dashboard with: (a) a chart widget on table T1 that supports drill-down AND (b) a map widget that includes a layer also backed by table T1 with info_enabled=1.
      - With no filter active, click the map → popup opens with records from the raw table.
      - Apply a v1.3 filter by clicking a chart element on the chart widget. Wait ~300ms for the materialize to land (you should see the filtering badge briefly, then the chart + map re-render against the view).
      - Click the map again at the same approximate location.
      - EXPECT: Network tab POST /api/info/query payload includes a `viewName` field with the materialized view name (matches the `LAYERS=_kbi_filt_...` value visible in the WMS request URLs in the same Network tab). Server response rows are a SUBSET of the unfiltered-click rows — only records that match the active filter chip are present in the popup.
      - Clear the filter (Clear All). Click the map again.
      - EXPECT: POST /api/info/query no longer includes `viewName` (or includes it as empty/null). Records revert to the unfiltered set.
      - Record: PASS / FAIL.

    STEP 2.4 (optional — TD-V12-04 fixture resolution attempt):
      - Operator decision: (a) materialize the fixture (`kinetica_bi/server/scripts/test-fixtures/v13-filter-fixture.sql`) on their Kinetica instance and walk STEP 2.3 ONE MORE TIME against the fixture (low-cardinality, CONUS-spread points → visible narrowing); document outcome PASS, OR (b) record TD-V12-04 still-DEFERRED in 24-02-UAT-NOTES.md with the same rationale as 17-VERIFICATION.md (operator using a denser dataset; fixture not materialized).
      - If chosen (a): take a before/after screenshot of the map widget + popup (filter applied vs. cleared) and save under `.planning/phases/24-verification/screenshots/`. Record in 24-02-UAT-NOTES.md the path + outcome.
      - Record: PASS / DEFERRED.

    Type "approved" if all steps PASS / acceptable-DEFER, or describe each FAIL with file/network evidence.
  </how-to-verify>
  <verify>
    <automated>grep -c "^- step: 2\." .planning/phases/24-verification/24-02-UAT-NOTES.md | awk '{ if ($1 == 4) exit 0; else exit 1 }' && ! awk '/^- step: 2\./,/^- step:|^$/' .planning/phases/24-verification/24-02-UAT-NOTES.md | grep -q "status: pending"</automated>
  </verify>
  <acceptance_criteria>
    - 24-02-UAT-NOTES.md contains step rows 2.1, 2.2, 2.3, 2.4 each with `status:` set.
    - For STEP 2.1, the recorded evidence references `useInfoSelectionStore.state === {}` and `activeLayerId === null` after the dashboard switch.
    - For STEP 2.2, the recorded evidence states the Info Card empty-state placeholder text was visible on re-login OR specifies the exact divergent behaviour.
    - For STEP 2.3, the recorded evidence cites the `viewName` field present in the POST payload while filter active AND absent/empty when no filter active AND that records changed accordingly.
    - For STEP 2.4, the status is either PASS with a screenshot path under `.planning/phases/24-verification/screenshots/`, OR DEFERRED with a rationale carrying TD-V12-04 forward (acceptable per ROADMAP "should be resolved during this phase if a test fixture is available").
    - Operator typed "approved" OR each FAIL captured with a concrete reproduction note.
  </acceptance_criteria>
  <done>24-02-UAT-NOTES.md has 4 Task 2 step rows with non-pending status values; operator-approval or FAIL-diagnoses captured for all four.</done>
  <resume-signal>Type "approved" or describe each failed/deferred step with file/network/screenshot evidence so 24-03 can transcribe outcomes.</resume-signal>
</task>

</tasks>

<verification>
After both checkpoint tasks complete:
  - File .planning/phases/24-verification/24-02-UAT-NOTES.md exists with 8 step rows (1.1–1.4 + 2.1–2.4), each with `status:` set.
  - `grep -c "^- step: " .planning/phases/24-verification/24-02-UAT-NOTES.md` returns 8.
  - No row has `status: pending` remaining.
  - Operator-typed "approved" lines or per-step gap descriptions exist for both tasks.
</verification>

<success_criteria>
- VERIFY-V14-01 criterion 1 (both auth modes exercised end-to-end) covered by Task 1 STEP 1.1–1.2.
- VERIFY-V14-01 criterion 3 (dashboard-switch + logout clear info selection) covered by Task 2 STEP 2.1–2.2.
- VERIFY-V14-01 criterion 4 (per-layer + per-widget kill switches) covered by Task 1 STEP 1.3–1.4.
- Session Fix #1 viewName / filter-view alignment recorded in STEP 2.3.
- TD-V12-04 either resolved (STEP 2.4 PASS with fixture + screenshots) or explicitly carried-forward (STEP 2.4 DEFERRED with rationale) — matches ROADMAP "should be resolved during this phase if a test fixture is available".
</success_criteria>

<output>
After both tasks complete, create `.planning/phases/24-verification/24-02-SUMMARY.md` linking to 24-02-UAT-NOTES.md and summarizing PASS / FAIL / DEFERRED counts per VERIFY-V14-01 criterion + TD-V12-04 status.
</output>
