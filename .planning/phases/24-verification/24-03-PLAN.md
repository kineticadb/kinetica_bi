---
phase: 24-verification
plan: 03
type: execute
wave: 2
depends_on:
  - 24-01
  - 24-02
files_modified:
  - .planning/phases/24-verification/24-VERIFICATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - VERIFY-V14-01

must_haves:
  truths:
    - "24-VERIFICATION.md exists at .planning/phases/24-verification/24-VERIFICATION.md and mirrors the 17-VERIFICATION.md structure: top-level YAML frontmatter (phase, overall_status, verified_on, operator, criteria_status map of 1..5, gaps array, environment block) + Summary + Criteria Results + Carried Tech Debt + Sign-off"
    - "criteria_status map in frontmatter contains exactly 5 keys (criterion_1, criterion_2, criterion_3, criterion_4, criterion_5) — one per ROADMAP Phase 24 success criterion — each set to passed | tech_debt | deferred | failed based on the actual UAT outcomes recorded in 24-01-UAT-NOTES.md + 24-02-UAT-NOTES.md"
    - "Each Criteria Result section cites the specific UAT step IDs (1.1, 1.2 ... 2.4 across both UAT plans) that produced the verdict — no synthesis without traceback to a concrete step"
    - "All seven post-phase-23 session fixes are explicitly enumerated in 24-VERIFICATION.md with their UAT step ID and outcome (bbox projection STEP 24-01/1.1; viewName filter-view STEP 24-02/2.3; Kinetica-GEOMETRY SQL STEP 24-01/2.1; single-record nav STEP 24-01/1.4; popup resize STEP 24-01/2.2; edge-aware positioning STEP 24-01/2.3; close-X overlap STEP 24-01/2.4)"
    - "TD-V12-04 status is recorded: either CLOSED with screenshot evidence path OR still-DEFERRED with rationale carried forward from STEP 24-02/2.4"
    - "SPATIAL-V14-03 (WKB) and TD-V14-WKB-SPIKE are explicitly carried forward as DEFERRED in the gaps array — Phase 24 does NOT close TD-V14-WKB-SPIKE; it inherits Phase 18's deferral verbatim and records that the Kinetica-GEOMETRY case (Session Fix #1) is the narrower closure"
    - "STATE.md is updated: position reflects Phase 24 complete; v1.4 milestone-close readiness noted; resume pointer updated; carried-tech-debt list reflects the final TD-V12-04 + TD-V14-WKB-SPIKE state from this verification"
    - "ROADMAP.md is updated: Phase 24 marked [x] with completion date; v1.4 progress count finalized in the Progress table"
    - "REQUIREMENTS.md traceability table: VERIFY-V14-01 row status set to Complete (or Tech-Debt if any criterion landed tech_debt); 'Last updated' footer bumped to phase-close date"
    - "Full vitest suite is re-run as a regression check: 496/496 frontend green and tsc --noEmit clean — proves no doc-only work introduced a regression. Result recorded in 24-VERIFICATION.md test_coverage block"
    - "Git working tree after this plan: only the 4 files in files_modified are dirty (plus the existing 2 UAT-NOTES.md from Waves 1); no stray src/ edits"
  artifacts:
    - path: ".planning/phases/24-verification/24-VERIFICATION.md"
      provides: "v1.4 Phase 24 end-to-end verification report consumed by /gsd:audit-milestone (or pragmatic-close); mirrors 17-VERIFICATION.md structure"
      contains: "overall_status:"
    - path: ".planning/STATE.md"
      provides: "Phase 24 marked complete; v1.4 milestone-close prep noted; carried-tech-debt list final"
      contains: "Phase: 24"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 24 status [x] with completion date; Progress table row updated"
      contains: "[x] **Phase 24: verification**"
    - path: ".planning/REQUIREMENTS.md"
      provides: "VERIFY-V14-01 row status set to Complete in traceability table"
      contains: "VERIFY-V14-01 | Phase 24 |"
  key_links:
    - from: "24-VERIFICATION.md frontmatter criteria_status"
      to: "ROADMAP Phase 24 success criteria 1..5"
      via: "structured YAML map; one key per ROADMAP criterion"
      pattern: "criteria_status:"
    - from: "24-VERIFICATION.md Criteria Results sections"
      to: "24-01-UAT-NOTES.md + 24-02-UAT-NOTES.md step rows"
      via: "explicit step-ID citation in each criterion result"
      pattern: "STEP 24-0[12]/[12]\\.[1-6]"
    - from: "24-VERIFICATION.md Carried Tech Debt section"
      to: "TD-V14-WKB-SPIKE + TD-V12-04 entries"
      via: "explicit deferral with rationale; matches Phase 17 'Carried Tech Debt' structure"
      pattern: "TD-V14-WKB-SPIKE|TD-V12-04"
    - from: "REQUIREMENTS.md traceability row VERIFY-V14-01"
      to: "status column set to Complete or Tech-Debt"
      via: "in-place table edit"
      pattern: "VERIFY-V14-01.*Phase 24"
---

<objective>
Synthesize the operator UAT outcomes from 24-01-UAT-NOTES.md + 24-02-UAT-NOTES.md into 24-VERIFICATION.md, mirroring the 17-VERIFICATION.md structure. Then update STATE.md, ROADMAP.md, and REQUIREMENTS.md for v1.4 milestone-close readiness.

Purpose: Produce the deliverable artifact (24-VERIFICATION.md) that the v1.4 milestone close consumes — equivalent role to 17-VERIFICATION.md for v1.3. ZERO production src/ code is written in this plan. All UAT walkthroughs already happened in Waves 1 plans (24-01 + 24-02); this plan is pure documentation synthesis + state propagation.

Output:
  - .planning/phases/24-verification/24-VERIFICATION.md (the deliverable, structured to mirror 17-VERIFICATION.md)
  - Updated .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.3-phases/17-verification/17-VERIFICATION.md
@.planning/phases/24-verification/24-01-UAT-NOTES.md
@.planning/phases/24-verification/24-02-UAT-NOTES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 24-VERIFICATION.md mirroring 17-VERIFICATION.md structure</name>
  <files>.planning/phases/24-verification/24-VERIFICATION.md</files>
  <read_first>
    - .planning/milestones/v1.3-phases/17-verification/17-VERIFICATION.md (FORMAT TEMPLATE — frontmatter shape, section ordering, table styles, sign-off pattern)
    - .planning/phases/24-verification/24-01-UAT-NOTES.md (operator outcomes for spatial modes + popup features + 7 session fixes — STEPS 1.1–1.6 + 2.1–2.4)
    - .planning/phases/24-verification/24-02-UAT-NOTES.md (operator outcomes for auth modes + kill switches + lifecycle resets + viewName routing + TD-V12-04 — STEPS 1.1–1.4 + 2.1–2.4)
    - .planning/ROADMAP.md (Phase 24 section — 5 success criteria verbatim)
    - .planning/REQUIREMENTS.md (VERIFY-V14-01 wording for traceability)
  </read_first>
  <action>
    Create `.planning/phases/24-verification/24-VERIFICATION.md` with the EXACT section structure from 17-VERIFICATION.md, scaled to Phase 24's 5 criteria:

    1. Frontmatter (YAML):
       - phase: 24-verification
       - overall_status: passed | tech_debt | deferred | failed (derive from UAT outcomes: passed if all 5 criteria PASS; tech_debt if any criterion has acceptable known-gap deferral like TD-V12-04 or OIDC-not-configured-this-session; failed if any blocker FAIL)
       - verified_on: <today's ISO date — read from the system clock; format YYYY-MM-DD>
       - operator: rpereira@kinetica.com
       - criteria_status:
           criterion_1: <status>  # popup E2E across lat/lon, WKT, Kinetica-GEOMETRY-WKB-route — from 24-01/1.1 + 24-01/1.2 + 24-01/1.3 + 24-01/2.1 + 24-02/1.1 + 24-02/1.2
           criterion_2: <status>  # Info Card receives same selection — from 24-01/1.5 + 24-01/1.6
           criterion_3: <status>  # dashboard-switch + logout clear info selection — from 24-02/2.1 + 24-02/2.2
           criterion_4: <status>  # per-layer + per-widget kill switches — from 24-02/1.3 + 24-02/1.4
           criterion_5: <status>  # this document committed with PASS/DEFERRED/FAIL per criterion matching 17-VERIFICATION.md format — set to passed at file-write time
       - gaps: array of objects with id, title, severity, deferred_to (e.g. v2 or future-milestone), note
           - At minimum include:
             - TD-V14-WKB-SPIKE (carried — true WKB-binary case still deferred; Kinetica-GEOMETRY sub-case closed via Session Fix #1 — record both facts)
             - TD-V12-04 (CLOSED if STEP 24-02/2.4 passed with fixture screenshots, OR still-DEFERRED with rationale)
             - Any FAIL or DEFERRED outcomes from UAT notes that don't fit the above
       - session_fixes_verified: structured map of the 7 session fixes → UAT step IDs that verified each
       - test_coverage: object with frontend_vitest count + tsc result (run npm test + npx tsc --noEmit in kinetica_bi/ as the final regression check; record actual numbers)
       - environment: dev_server_frontend, dev_server_backend, browser, auth_modes_exercised, verified_date

    2. Body sections (mirror 17-VERIFICATION.md ordering):
       - # Phase 24 Verification — v1.4 Map Info Popup
       - ## Summary (2–3 paragraphs: scope of UAT, blanket attestation, headline outcome)
       - ## Criteria Results — one subsection per criterion (1, 2, 3, 4, 5) with PASS/DEFERRED/FAIL header + tables citing the UAT step IDs and evidence
         - For criterion 1, include a "Spatial modes verified" table (lat/lon | WKT | Kinetica-GEOMETRY-WKB-route) + a "Popup features verified" table (open, layer switch, pagination, template rendering, single-record nav)
         - For criterion 4, include both per-layer and per-widget kill-switch evidence rows
       - ## Session Fixes Verified — table mapping each of the 7 session-fix items (bbox projection, viewName filter-view alignment, Kinetica-GEOMETRY SQL, single-record nav + Back/Next + total + resizable popup, edge-aware positioning, close-X overlap fix) to its UAT step ID and outcome
       - ## Auth Modes Exercised — section confirming both AUTH_MODE=password (STEP 24-02/1.1) and AUTH_MODE=oidc (STEP 24-02/1.2) covered, with audit-log auth_mode evidence
       - ## Carried Tech Debt — TD-V14-WKB-SPIKE (with the Phase 18 NONE_ESCALATE → TECH_DEBT decision carried verbatim PLUS Session Fix #1's Kinetica-GEOMETRY narrowing) + TD-V12-04 (CLOSED or still-DEFERRED per STEP 24-02/2.4)
       - ## What This Verification Did NOT Cover (e.g. true WKB-binary if no column reachable; any UAT step recorded as DEFERRED)
       - ## Sign-off (operator name, date, status)

    Rules:
    - Mirror 17-VERIFICATION.md's voice (matter-of-fact; cite specific commits, file paths, error strings; tables not prose where tables fit).
    - Every "PASS" claim must cite a STEP ID from one of the UAT notes files.
    - Every "DEFERRED" must include a rationale matching the operator's note in the UAT file (do not invent rationales).
    - Every "FAIL" must include a concrete reproduction note + the affected file(s).
    - Do NOT mention or use emojis.
    - File length: aim for ~250–350 lines (17-VERIFICATION.md is 300 lines as reference).
  </action>
  <verify>
    <automated>
      test -f .planning/phases/24-verification/24-VERIFICATION.md \
        && grep -q "^phase: 24-verification$" .planning/phases/24-verification/24-VERIFICATION.md \
        && grep -q "^overall_status:" .planning/phases/24-verification/24-VERIFICATION.md \
        && grep -q "^criteria_status:" .planning/phases/24-verification/24-VERIFICATION.md \
        && grep -q "criterion_1:" .planning/phases/24-verification/24-VERIFICATION.md \
        && grep -q "criterion_5:" .planning/phases/24-verification/24-VERIFICATION.md \
        && grep -q "^operator: rpereira@kinetica.com$" .planning/phases/24-verification/24-VERIFICATION.md \
        && grep -q "TD-V14-WKB-SPIKE" .planning/phases/24-verification/24-VERIFICATION.md \
        && grep -q "TD-V12-04" .planning/phases/24-verification/24-VERIFICATION.md \
        && grep -q "AUTH_MODE=password\|auth_mode.*password" .planning/phases/24-verification/24-VERIFICATION.md \
        && grep -q "AUTH_MODE=oidc\|auth_mode.*oidc" .planning/phases/24-verification/24-VERIFICATION.md \
        && grep -q "## Sign-off" .planning/phases/24-verification/24-VERIFICATION.md \
        && grep -cE "STEP 24-0[12]/" .planning/phases/24-verification/24-VERIFICATION.md | awk '{ if ($1 >= 10) exit 0; else exit 1 }'
    </automated>
  </verify>
  <acceptance_criteria>
    - 24-VERIFICATION.md exists at exactly .planning/phases/24-verification/24-VERIFICATION.md
    - Frontmatter contains all required keys: phase, overall_status, verified_on, operator, criteria_status (with criterion_1..criterion_5), gaps, test_coverage, environment
    - At least 10 STEP-ID citations of the form "STEP 24-01/X.Y" or "STEP 24-02/X.Y" appear in the body (one per UAT step covered)
    - "TD-V14-WKB-SPIKE" mentioned in the Carried Tech Debt section
    - "TD-V12-04" mentioned in the Carried Tech Debt section (either CLOSED or still-DEFERRED)
    - Both `AUTH_MODE=password` and `AUTH_MODE=oidc` exercise outcomes appear in the Auth Modes Exercised section
    - The 7 session fixes are listed in a Session Fixes Verified table — `grep -c "^| .*Fix" 24-VERIFICATION.md` returns at least 7, OR the table has 7 fix rows by visual count
    - No emojis in the file (`grep -P "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" 24-VERIFICATION.md` returns empty / exit 1)
  </acceptance_criteria>
  <done>24-VERIFICATION.md committed at the locked path, mirroring 17-VERIFICATION.md structure, with full UAT traceback. Frontmatter validates: phase, overall_status, criteria_status (5 keys), gaps, test_coverage, environment all present.</done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md, ROADMAP.md, REQUIREMENTS.md; run final regression check</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/24-verification/24-VERIFICATION.md</files>
  <read_first>
    - .planning/STATE.md (current frontmatter + "Current Position" + "Decisions" + "Blockers/Concerns" + "Session Continuity")
    - .planning/ROADMAP.md (Phase 24 row in "Phases" section + "Progress" table at bottom)
    - .planning/REQUIREMENTS.md (Traceability table — VERIFY-V14-01 row; "Last updated" footer)
    - .planning/phases/24-verification/24-VERIFICATION.md (just-written; the overall_status frontmatter field drives STATE / ROADMAP / REQUIREMENTS status values)
  </read_first>
  <action>
    Final state propagation. Run in this order:

    1. Run the frontend regression check ONCE (single source of truth for the test_coverage block in 24-VERIFICATION.md and the gating signal here):
       - `cd kinetica_bi && npm test -- --run 2>&1 | tail -5` — record the X/Y green count
       - `cd kinetica_bi && npx tsc --noEmit 2>&1` — record exit code (must be 0)
       If either fails, STOP and route the failure to a gap-closure plan rather than continuing — a regression here means doc work or git operations changed something. (Expected: 496/496 green; tsc clean — matches Phase 23 close baseline.)
       After running, update the `test_coverage` block in 24-VERIFICATION.md to reflect the actual numbers (do not rely on the placeholder values written in Task 1 — overwrite with measured truth).

    2. Update `.planning/STATE.md`:
       - Frontmatter: `status: complete` (or `tech_debt` matching overall_status from 24-VERIFICATION.md); `stopped_at: "Completed 24-03-verification-synthesis-PLAN.md (Phase 24 complete — v1.4 ready for milestone close)"`; `last_updated` to current ISO timestamp; `progress.completed_phases` increment; `progress.completed_plans` increment by the count of plans completed (Phase 24 has 3 plans — 24-01 + 24-02 + 24-03).
       - "Current Position" section: `Phase: 24 (verification) — COMPLETE`; `Plan: 3 of 3 (all complete)`.
       - "Performance Metrics" table: append a v1.4 milestone row capturing phases / plans / timeline / final audit status (e.g. "passed" or "tech_debt").
       - "Decisions" section: append a new "## Phase 24-verification" subsection summarising 1–3 sentences of UAT key findings (e.g. OIDC verified, Kinetica-GEOMETRY SQL closure, TD-V12-04 status).
       - "Blockers/Concerns" section: mark TD-V12-04 final status (resolved or still-carried); leave TD-V14-WKB-SPIKE as still-pending (Phase 24 did NOT re-attempt the WKB spike).
       - "Session Continuity": update timestamp + "Stopped at" to reflect Phase 24 close.

    3. Update `.planning/ROADMAP.md`:
       - In the "### v1.4 Map Info Popup" phase list, change `- [ ] **Phase 24: verification**` to `- [x] **Phase 24: verification**` with `(completed YYYY-MM-DD)` appended (today's date).
       - Mark v1.4 milestone status: the top-of-file `🚧 **v1.4 Map Info Popup** — Phases 18-24 (active)` line — change `🚧 active` to `✅ ... — SHIPPED YYYY-MM-DD` ONLY IF overall_status is passed; otherwise leave as active and note in the line that close-out is pragmatic-tech-debt (mirror v1.3 pragmatic-close language).
       - "Progress" table at the bottom: Phase 24 row → `1/1+...` style status update matching the actual plans-completed count (3/3 if all three plans landed); set Status column to "Complete" and Completed column to today's date.

    4. Update `.planning/REQUIREMENTS.md`:
       - Traceability table row `VERIFY-V14-01 | Phase 24 | Pending` → change Status column to `Complete` (if all criteria PASS) OR `Tech-Debt` (if any criterion landed tech_debt) matching 24-VERIFICATION.md overall_status.
       - "Coverage" block: bump "v1.4 requirements: 25 total → Mapped to phases: 25 → Unmapped: 0" — should already be at 25/25; verify and leave unchanged if so.
       - Footer "Last updated: YYYY-MM-DD — ..." line: update date to today; replace the "Phase 23 complete" trailer with "Phase 24 complete: VERIFY-V14-01 closed (or tech-debt-carried); v1.4 milestone ready for close."

    5. Do NOT commit. The orchestrator handles git commits via gsd-tools.
  </action>
  <verify>
    <automated>
      cd kinetica_bi && npm test -- --run 2>&1 | tail -3 | grep -E "passed|✓" \
        && cd kinetica_bi && npx tsc --noEmit \
        && cd - \
        && grep -q "Phase: 24" .planning/STATE.md \
        && grep -q "Completed 24-03" .planning/STATE.md \
        && grep -q "^\\- \\[x\\] \\*\\*Phase 24: verification\\*\\*" .planning/ROADMAP.md \
        && grep -E "^\\| VERIFY-V14-01 \\| Phase 24 \\| (Complete|Tech-Debt)" .planning/REQUIREMENTS.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `kinetica_bi/` vitest exit code 0 with at least the Phase-23-baseline count (≥496 passing); tsc --noEmit exits 0
    - STATE.md frontmatter: `stopped_at` references "24-03"; "Current Position" shows "Phase: 24 ... COMPLETE"
    - ROADMAP.md: Phase 24 line shows `[x]` with completion date; Progress table updated
    - REQUIREMENTS.md: VERIFY-V14-01 traceability row Status column is `Complete` or `Tech-Debt` (NOT `Pending`)
    - 24-VERIFICATION.md `test_coverage` block contains the ACTUAL measured frontend_vitest count from Task 2 step 1 (not a placeholder)
    - Git working tree shows ONLY the 4 files listed in files_modified as dirty (no stray src/ edits beyond the 2 UAT-NOTES.md files from Waves 1)
  </acceptance_criteria>
  <done>STATE/ROADMAP/REQUIREMENTS reflect Phase 24 complete; frontend regression passes; 24-VERIFICATION.md test_coverage block contains measured truth.</done>
</task>

</tasks>

<verification>
After both tasks complete:
  - `cat .planning/phases/24-verification/24-VERIFICATION.md | head -50` shows valid YAML frontmatter with all required keys
  - `grep -q "^\\- \\[x\\] \\*\\*Phase 24: verification\\*\\*" .planning/ROADMAP.md` exits 0
  - `grep -E "^\\| VERIFY-V14-01 \\| Phase 24 \\| (Complete|Tech-Debt)" .planning/REQUIREMENTS.md` exits 0
  - `cd kinetica_bi && npx tsc --noEmit` exits 0
  - `cd kinetica_bi && npm test -- --run` exits 0
</verification>

<success_criteria>
- VERIFY-V14-01 success criterion 5 (24-VERIFICATION.md committed with PASS/DEFERRED/FAIL status per criterion matching 17-VERIFICATION.md format) achieved by Task 1 completion.
- v1.4 milestone is ready for `/gsd:audit-milestone` (or pragmatic close): all 5 ROADMAP success criteria have explicit status in 24-VERIFICATION.md; STATE/ROADMAP/REQUIREMENTS updated; carried tech debt enumerated.
- Zero src/ regression — full vitest + tsc clean per Task 2 step 1.
</success_criteria>

<output>
After both tasks complete, create `.planning/phases/24-verification/24-03-SUMMARY.md` linking to 24-VERIFICATION.md and stating the final overall_status, the criterion_1..5 statuses, and the carried-tech-debt list.
</output>
