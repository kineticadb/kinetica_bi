---
phase: 60.2-radio-dashboard-control-multi-target-options
plan: "02"
subsystem: frontend/config-ui
tags: [radio-group, multi-target, actions-array, config-panel, tdd, back-compat]

requires:
  - phase: 60.2-01
    provides: RadioOption.actions[], getOptionActions normalizer, validateRadioOption(option, widgetTypeFor), applyWidgetActions plural dispatch
  - phase: 60.1-radio-config-ux-structured-layer-target-editor-reuse-cbconfigform
    provides: RadioLayerConfigEditor two-pane layout, cbValidMap per-option validity plumbing

provides:
  - OptionRow authors a per-option TARGET LIST over option.actions[] with add/remove
  - TargetEditor sub-component: one action's picker + editor + Advanced JSON; reused per target
  - Single-target-clean: no list chrome (TARGET N header, per-target Remove) when actions.length === 1
  - Save writes actions[] and clears legacy action field on every write (normalize-everywhere)
  - handleAddOption seeds actions:[{...}] not legacy action:{...}
  - cbValidMap keyed by "optionIdx:targetIdx" for per-target classbreak validity
  - validateRadioOption(option, widgetTypeFor) using resolver signature from Plan 01
  - 37 spec tests green covering multi-target authoring, legacy back-compat, isValid fold

affects:
  - packages/web/src/components/charts/RadioGroupConfigPanel.tsx
  - packages/web/src/components/charts/RadioGroupConfigPanel.spec.tsx

tech-stack:
  added: []
  patterns:
    - TargetEditor sub-component pattern (one action's full editor: picker + Capture + layer two-pane OR widget/dv JSON)
    - getOptionActions back-compat normalizer on every OptionRow read (legacy action → 1-element list)
    - commitActions write-back helper: clears action field on every write (normalize-everywhere)
    - cbValidMap keyed by optionIdx:targetIdx for independent per-target classbreak validity
    - single-target-clean gate: totalTargets > 1 before rendering list chrome

key-files:
  created: []
  modified:
    - packages/web/src/components/charts/RadioGroupConfigPanel.tsx
    - packages/web/src/components/charts/RadioGroupConfigPanel.spec.tsx

key-decisions:
  - "TargetEditor sub-component extracts all per-target editor markup (picker/Capture/layer two-pane/widget-dv JSON) so it can repeat per action"
  - "Single-target-clean: showListChrome = totalTargets > 1 — common single-target case looks identical to Phase 60.1"
  - "Legacy action:{} fixtures remain in spec as back-compat tests; new-shape fixtures use actions:[{...}]"
  - "Per-target testid radiogroup-target-{optionIdx}-{targetIdx} enables precise assertion"

patterns-established:
  - "commitActions(next): every write calls onChange({ ...option, actions: next, action: undefined }) — legacy field always cleared"
  - "getOptionActions(option) at top of OptionRow normalizes both legacy and new shapes before any reads"

requirements-completed: [RADIOMULTI-V111-01]

duration: 30min
completed: "2026-06-13"
---

# Phase 60.2 Plan 02: Per-Option Target List Over actions[] Summary

**RadioGroupConfigPanel OptionRow refactored to author N targets per option via actions[] with add/remove; single-target stays clean; 8 failing legacy-assertion tests fixed + 8 new multi-target tests added; 37/37 green**

## Performance

- **Duration:** ~30 min
- **Started:** 2026-06-13T00:43:00Z
- **Completed:** 2026-06-13T00:54:00Z
- **Tasks:** 2 (Tasks 1+2 committed as d5d01a4 by prior executor; Task 3 spec fixes + additions committed as 23fa2f6 in this run; Task 4 gate verified)
- **Files modified:** 2

## Accomplishments

- Task 1 (panel refactor) was already committed (d5d01a4) — per-option target list, TargetEditor sub-component, add/remove, single-target-clean, cbValidMap keyed by optionIdx:targetIdx, validateRadioOption(option, widgetTypeFor), handleAddOption seeds actions:[], all verified.
- Fixed 8 failing tests: updated assertions from `option.action.configPatch` to `option.actions[0].configPatch` and fixtures from `action:{...}` to `actions:[{...}]` where the test asserts onChange payload shape.
- Added 8 new multi-target tests in `describe("per-option target list")`: single-target-clean (no chrome), +Add target grows actions, per-target Remove shrinks actions, TARGET N chrome with 2 targets, layer-within-multi renders radio-layer-form-*, legacy {action:{}} back-compat load + re-saves as actions[] with action:undefined, isValid(true) / isValid(false) for multi-target options.
- Full gate: 37/37 spec green; web tsc exit 0; no server diff.

## Task Commits

1. **Task 1+2: Refactor OptionRow + panel save shape (prior executor)** - `d5d01a4` (feat)
2. **Task 3: Panel specs — fix 8 failing + add 8 multi-target tests** - `23fa2f6` (test)

## Files Created/Modified

- `packages/web/src/components/charts/RadioGroupConfigPanel.tsx` — per-option target list over actions[], TargetEditor, add/remove targets, single-target-clean, cbValidMap by optionIdx:targetIdx (committed d5d01a4)
- `packages/web/src/components/charts/RadioGroupConfigPanel.spec.tsx` — 8 failing tests fixed to actions[] assertions; 8 new multi-target tests in per-option target list describe block

## Decisions Made

- Kept legacy `action:{}` fixtures in the "structured layer editor" tests so they test the back-compat path (getOptionActions normalizes them), while updating only the assertion side to read `actions[0]`.
- Added a dedicated legacy back-compat test that explicitly uses `action:{}` shape and asserts re-save produces `actions:[]` with `action:undefined`.
- The "layer target within multi-target" test uses `getAllByRole` instead of `getByRole` for the JSON textarea because a 2-target option (widget + layer) renders TWO textareas (one for widget, one inside Advanced disclosure for layer).

## Deviations from Plan

None — plan executed exactly as written (the spec work was the exact scope described in Task 3).

## Issues Encountered

- The full vitest suite (run from `cd packages/web`) shows 2–4 failures in `actionEngine.canary.spec.tsx` when run in full-suite mode. These are pre-existing test-ordering/isolation failures: the canary passes 9/9 in isolation and when run alongside the RadioGroup spec. Git history confirms these failures existed before this plan (d5d01a4 showed 8/9 canary failures in the prior full-suite run). No fix applied — out of scope.

## Next Phase Readiness

- Phase 60.2 multi-target authoring (Plans 01+02) is complete. The config panel authors options with N targets; the renderer dispatches them via applyWidgetActions; the lib model is validated.
- Phase 61 (verification UAT) can proceed.

---
*Phase: 60.2-radio-dashboard-control-multi-target-options*
*Completed: 2026-06-13*
