---
phase: 61-verification-live-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/61-verification-live-uat/61-01-AUTOMATED-GATES.md
autonomous: true
requirements: [VERIFY-V111-01]

must_haves:
  truths:
    - "Frontend vitest is 100% green (run from packages/web), >= 1935 baseline, 0 failures"
    - "web tsc --noEmit clean (exit 0) AND server tsc --noEmit clean (exit 0) as SEPARATE gates"
    - "server vitest failing FILE set is a subset of the TD-V16-TEST-ISOLATION known-flaky set (NOT a fixed pass-count)"
    - "The v1.11 engine + radio targeted specs all pass when run from packages/web"
    - "Zero packages/server source diff across v1.11 (Phases 58-60 frontend-only)"
  artifacts:
    - path: ".planning/phases/61-verification-live-uat/61-01-AUTOMATED-GATES.md"
      provides: "SC3/SC4 automated-gate evidence record consumed by 61-02 §0 + 61-03"
      contains: "overall_verdict"
  key_links:
    - from: "61-01-AUTOMATED-GATES.md"
      to: "TD-V16-TEST-ISOLATION known-flaky set"
      via: "set-subset assertion table (server vitest)"
      pattern: "auth.oidc|auth.routes|boot.hardening|boot.wipe|bootstrap|db.smoke|oidc.module|routes.wms"
---

<objective>
Run the v1.11 SC3/SC4 automated gates fresh against HEAD and record the results in `61-01-AUTOMATED-GATES.md` — the evidence file the operator cites in §0 of the UAT (61-02) and that 61-03 compiles into the verification record.

Purpose: SC3/SC4 of the milestone gate are deterministic automated gates. Recording them once, authoritatively, frees the live walk-through from re-running anything and gives 61-03 a citable artifact. This mirrors 57-01-AUTOMATED-GATES.md.

Output: `.planning/phases/61-verification-live-uat/61-01-AUTOMATED-GATES.md` with `overall_verdict: ALL PASS` (or a clearly-recorded failure if a deterministic gate is red).

NO feature code. This plan only RUNS gates and RECORDS results. If a deterministic gate is red, record it as-is and surface it — do NOT fix code here (gap fixes go to a separate 61.x decimal phase).
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-verification-live-uat/61-CONTEXT.md

<!-- FORMAT PRECEDENT — reuse this structure exactly -->
@.planning/phases/57-verification-live-uat/57-01-AUTOMATED-GATES.md

<!-- WHAT SHIPPED — current baselines + the known-flaky server set -->
@.planning/phases/60-radio-renderer-wiring-persistence-mcp-seam-doc/60-VERIFICATION.md
@.planning/phases/58-action-engine-contract-allow-list-canary/58-VERIFICATION.md

<gates_reality>
<!-- Locked test-gate policy for SC3/SC4 (ROADMAP §Phase 61 SC3/SC4 + 61-CONTEXT §4). -->
- Frontend vitest is DETERMINISTIC → must be 100% green. Run from `packages/web`. Baseline at v1.11 close = 1935 (60-VERIFICATION); count must be >= 1935 with 0 failures.
- Server vitest is FLAKY → SET-BASED gate ONLY. Pass iff the failing FILE set is a subset of the TD-V16-TEST-ISOLATION known-flaky set: {auth.oidc, auth.routes, boot.hardening, boot.wipe, bootstrap, db.smoke, oidc.module, routes.wms}. NEVER assert a fixed pass-count for the server suite.
- web tsc AND server tsc are SEPARATE gates: `npx tsc --noEmit -p packages/web` and `npx tsc --noEmit -p packages/server`, each must exit 0.
- v1.11 has been frontend-only (Phases 58-60 zero server diffs) — server gates should be UNCHANGED/green. Flag any server diff.
</gates_reality>

<targeted_specs>
<!-- The v1.11 engine + radio specs to record as a targeted green group (proves the chain under test). All live in packages/web. -->
- src/lib/widgetAction.spec.ts
- src/lib/actionAllowList.spec.ts
- src/lib/applyWidgetAction.spec.ts
- src/lib/actionEngineDecoupling.spec.ts
- src/store/widgetActionStore.spec.ts
- src/components/charts/actionEngine.canary.spec.tsx
- src/lib/radioGroupConfig.spec.ts
- src/lib/radioGroupCapture.spec.ts
- src/components/charts/RadioGroupConfigPanel.spec.tsx
- src/components/charts/RadioGroupRenderer.spec.tsx
</targeted_specs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the four SC3/SC4 gates + the targeted v1.11 spec group and capture raw output</name>
  <read_first>
    - .planning/phases/57-verification-live-uat/57-01-AUTOMATED-GATES.md (the exact record format to mirror)
    - .planning/phases/58-action-engine-contract-allow-list-canary/58-VERIFICATION.md (known-flaky set + targeted-spec precedent)
  </read_first>
  <files>(no source files — this task runs commands and captures output for Task 2)</files>
  <action>
    Capture HEAD commit: `git rev-parse --short HEAD`.

    Run each gate and capture the exact tail/summary output (tests, files, failures, exit code):

    1. Frontend vitest (DETERMINISTIC, 100% bar):
       `cd packages/web && npx vitest run`
       Record: tests passed/total, files passed/total, failures. MUST be 0 failures and >= 1935 tests.

    2. Web tsc (separate gate):
       `npx tsc --noEmit -p packages/web` — record exit code (must be 0, no output).

    3. Server tsc (separate gate):
       `npx tsc --noEmit -p packages/server` — record exit code (must be 0, no output).

    4. Server vitest (SET-BASED gate — failing-file set only):
       `cd packages/server && npx vitest run`
       Record the list of FAILING test FILES (not the pass-count). Then assert each failing file is in the TD-V16-TEST-ISOLATION known-flaky set {auth.oidc, auth.routes, boot.hardening, boot.wipe, bootstrap, db.smoke, oidc.module, routes.wms}.

    5. Targeted v1.11 spec group (proves the chain under test):
       `cd packages/web && npx vitest run src/lib/widgetAction.spec.ts src/lib/actionAllowList.spec.ts src/lib/applyWidgetAction.spec.ts src/lib/actionEngineDecoupling.spec.ts src/store/widgetActionStore.spec.ts src/components/charts/actionEngine.canary.spec.tsx src/lib/radioGroupConfig.spec.ts src/lib/radioGroupCapture.spec.ts src/components/charts/RadioGroupConfigPanel.spec.tsx src/components/charts/RadioGroupRenderer.spec.tsx`
       Record tests/files passed; all must be green.

    6. Server-diff guard (v1.11 should be frontend-only):
       `git diff --name-only -- packages/server` against the v1.11 start — for the record, run `git diff --name-only -- packages/server` on HEAD working tree (expect empty); also note that Phase 58/58.1/59/60 SUMMARYs all report zero server diff.

    Do NOT modify any source. If a deterministic gate (frontend vitest, web tsc, server tsc) is RED, record the failure verbatim — do not attempt a fix.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run 2>&1 | tail -5 ; npx tsc --noEmit -p packages/web ; echo "web tsc exit=$?" ; npx tsc --noEmit -p packages/server ; echo "server tsc exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Frontend vitest output captured: 0 failures, >= 1935 tests
    - web tsc exit 0; server tsc exit 0 (both captured)
    - Server vitest failing-file list captured and each file confirmed ⊆ TD-V16-TEST-ISOLATION set
    - Targeted v1.11 spec group captured all-green
    - `git diff --name-only -- packages/server` captured (expected empty)
  </acceptance_criteria>
  <done>Raw output for all five gates + the server-diff guard is captured and ready to transcribe into the record.</done>
</task>

<task type="auto">
  <name>Task 2: Author 61-01-AUTOMATED-GATES.md mirroring the 57-01 format</name>
  <read_first>
    - .planning/phases/57-verification-live-uat/57-01-AUTOMATED-GATES.md (clone its frontmatter + section structure)
  </read_first>
  <files>.planning/phases/61-verification-live-uat/61-01-AUTOMATED-GATES.md</files>
  <action>
    Write `61-01-AUTOMATED-GATES.md` cloning the 57-01 structure:

    Frontmatter:
    ```
    ---
    automated_gates_ref: "61-01"
    phase: 61-verification-live-uat
    recorded_on: "<ISO timestamp>"
    commit: "<short HEAD>"
    overall_verdict: ALL PASS   # or a recorded failure if a deterministic gate is red
    ---
    ```

    Body sections (reuse 57-01 headings):
    - Title + Recorded/HEAD/Operator (RPereira@kinetica.com)/Purpose line ("Evidence record for SC3/SC4 of the v1.11 milestone-gate verification. Consumed by 61-02 §0 (UAT preconditions) and 61-03 (compiled verification).")
    - Gate Results summary table: rows for frontend_vitest, web_tsc, server_tsc, server_vitest_setgate, targeted_v111_specs — each with Command / Result / Numbers-or-Notes columns, transcribed from Task 1.
    - Gate Detail subsections (one per gate) with the exact command, observed output, exit code, verdict.
    - Server vitest SET-BASED subsection: a "Failing test FILES" table with an "In TD-V16-TEST-ISOLATION known-flaky list?" YES/NO column for each failing file, plus an explicit subset-check sentence. State plainly that the exact failing test-COUNT is irrelevant per the set-based policy — only the file set matters.
    - Targeted v1.11 specs subsection: per-file pass note for the 10 engine/radio specs.
    - A "Server diff guard" note: v1.11 frontend-only; `git diff --name-only -- packages/server` empty; cite the four SUMMARYs reporting zero server diff.
    - Final `## overall_verdict: ALL PASS` section enumerating the gate outcomes (mirror 57-01's closing block).

    The doc must be self-describing — 61-03 reads only this file for SC3/SC4 evidence.
  </action>
  <verify>
    <automated>test -f .planning/phases/61-verification-live-uat/61-01-AUTOMATED-GATES.md && grep -q "overall_verdict" .planning/phases/61-verification-live-uat/61-01-AUTOMATED-GATES.md && grep -qiE "TD-V16-TEST-ISOLATION" .planning/phases/61-verification-live-uat/61-01-AUTOMATED-GATES.md && grep -qE "frontend_vitest|frontend vitest" .planning/phases/61-verification-live-uat/61-01-AUTOMATED-GATES.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists with frontmatter containing `overall_verdict`, `commit`, `recorded_on`
    - Contains all five gate rows + a server-vitest set-subset table referencing the TD-V16-TEST-ISOLATION known-flaky files
    - Contains the targeted v1.11 spec group result and the server-diff guard note
    - `overall_verdict` reflects the actual gate outcomes (ALL PASS only if every deterministic gate is green)
  </acceptance_criteria>
  <done>61-01-AUTOMATED-GATES.md exists, fully transcribed from Task 1, ready for the operator (61-02 §0) and the compiler (61-03).</done>
</task>

</tasks>

<verification>
- Frontend vitest 100% green from packages/web (>= 1935, 0 failures) — recorded
- web tsc clean AND server tsc clean (separate gates) — recorded
- Server vitest failing-file set ⊆ TD-V16-TEST-ISOLATION — recorded with the subset table
- Targeted v1.11 engine/radio spec group all green — recorded
- 61-01-AUTOMATED-GATES.md committed with an honest overall_verdict
</verification>

<success_criteria>
SC3/SC4 automated evidence is captured in `61-01-AUTOMATED-GATES.md` with `overall_verdict` reflecting the true gate state, citable by 61-02 §0 and 61-03. No source code touched.
</success_criteria>

<output>
After completion, create `.planning/phases/61-verification-live-uat/61-01-SUMMARY.md`.
</output>
