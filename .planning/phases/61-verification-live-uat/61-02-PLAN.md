---
phase: 61-verification-live-uat
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .planning/phases/61-verification-live-uat/61-UAT.md
autonomous: false
requirements: [VERIFY-V111-01]

must_haves:
  truths:
    - "61-UAT.md exists in the 57-UAT.md format: §0 preconditions, per-section operator PASS/FAIL attestation blocks, a gaps YAML block, an attestation summary, traceability"
    - "§1 attests a radio option switches a map layer class-break RENDER MODE live (no remount) AND a widget.config-field option live; reload RESETS the radio to its configured DEFAULT (TRANSIENT — reload-resets-to-default, NOT live-selection-survives-reload)"
    - "§2 attests switch-replace isolation (renderMode+cb_config option → renderMode-only option reverts cb_config to baseline), an out-of-allow-list patch rejected at save (operator-visible), and during dispatch NO filter chips + NO materialize (filterVersion unchanged)"
    - "§3 attests the headline viewer payoff: a non-bypass ANALYST clicks the radio → target switches LIVE, NO permission error, nothing persists to the shared dashboard, reload resets to default; orphan target surfaces warning/typed-no-op"
    - "A blocking checkpoint:human-verify gates the walk; the operator runs it against live deployed Kinetica (password mode) with designer + non-admin-analyst logins and attests each section"
  artifacts:
    - path: ".planning/phases/61-verification-live-uat/61-UAT.md"
      provides: "Operator-executed live walk-through doc covering the 4 corrected SCs"
      contains: "overall_result"
  key_links:
    - from: "61-UAT.md §0"
      to: "61-01-AUTOMATED-GATES.md"
      via: "automated_gates_ref + P-precondition citing overall_verdict"
      pattern: "61-01-AUTOMATED-GATES"
---

<objective>
Author `61-UAT.md` — the self-contained, operator-executed live walk-through proving the v1.11 programmable-widget chain end-to-end against deployed Kinetica — then gate it behind a BLOCKING human checkpoint where the operator runs the walk and attests each section.

Purpose: SC1, SC2, and the §3 headline viewer payoff are behaviors only a live walk can prove (engine wires through real WMS render + real RBAC). This is the v1.11 milestone gate. Mirrors v1.9 Phase 54 and v1.10 Phase 57.

Output: `.planning/phases/61-verification-live-uat/61-UAT.md` with `overall_result: passed` (or `gaps_found` → a 61.x decimal gap-closure phase). `failed` is not an acceptable close state.

NO feature code. The walk exercises the running app. If a gap is found, it routes to a separate 61.x repro-test-driven plan — do NOT fix code in this plan.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/61-verification-live-uat/61-CONTEXT.md

<!-- FORMAT PRECEDENT — clone this doc's structure (frontmatter, §0 preconditions, per-item PASS/FAIL blocks, gaps YAML, attestation summary, traceability) -->
@.planning/phases/57-verification-live-uat/57-UAT.md

<!-- THE EVIDENCE THIS UAT CITES IN §0 -->
@.planning/phases/61-verification-live-uat/61-01-AUTOMATED-GATES.md

<!-- WHAT THE WALK EXERCISES (transient model, switch-replace, default-on-open, decoupling, orphan warning) -->
@.planning/phases/60-radio-renderer-wiring-persistence-mcp-seam-doc/60-VERIFICATION.md
@.planning/phases/59-radio-group-widget-registry-def-config-panel/59-VERIFICATION.md

<locked_transient_model>
<!-- LOCKED in 61-CONTEXT §"Reload behavior — TRANSIENT". The ROADMAP SC1/SC4 "persists across reload" wording is STALE and corrected. -->
A viewer's runtime radio selection is SESSION-ONLY / TRANSIENT (overlay store, NO PATCH). On reload (or dashboard-switch) the radio re-applies its DESIGNER-configured `defaultOptionId` (or neutral/first option if none). What persists is the authored radio config + the default — NOT the viewer's live click.
The operator attests **reload-resets-to-configured-default**. Do NOT write a UAT item expecting the live selection to survive reload — that would be a FALSE FAIL against the locked design.
</locked_transient_model>

<personas>
<!-- 61-CONTEXT §Personas + §Specifics. Operator has separate non-admin logins (v1.10 setup). -->
- DESIGNER (or admin/bypass — RPereira@kinetica.com): authors the radio + a map widget with a class-break-capable layer via the Phase 59 config panel; exercises authoring + interaction (§1, §2).
- VIEWER = a non-bypass ANALYST-role login: clicks the radio to prove the headline payoff (§3) — live switch, no permission error, no shared-dashboard mutation, reload resets to default.
- Environment: live deployed Kinetica, PASSWORD mode (same as v1.9/v1.10 closes). Requires a real class-break-capable map layer.
</personas>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 61-UAT.md (§0 preconditions + §1-§3 walk sections + gaps block + attestation + traceability)</name>
  <read_first>
    - .planning/phases/57-verification-live-uat/57-UAT.md (clone frontmatter, §0 PASS/FAIL block format, gaps YAML, attestation summary, traceability tables)
    - .planning/phases/61-verification-live-uat/61-CONTEXT.md §"Walk-through scenario coverage" (the §1-§4 → SC map)
    - .planning/phases/60-radio-renderer-wiring-persistence-mcp-seam-doc/60-VERIFICATION.md (transient/switch-replace/default-on-open behaviors being walked)
  </read_first>
  <files>.planning/phases/61-verification-live-uat/61-UAT.md</files>
  <action>
    Write `61-UAT.md` cloning the 57-UAT.md structure. Each check item is a fenced block with `id`/`check`/`status: PENDING`/`evidence:` (operator fills status+evidence at the checkpoint).

    Frontmatter:
    ```
    ---
    plan: 61-02
    operator: RPereira@kinetica.com
    started_on: <date>
    automated_gates_ref: .planning/phases/61-verification-live-uat/61-01-AUTOMATED-GATES.md
    automated_gates_verdict: <transcribe overall_verdict + commit from 61-01-AUTOMATED-GATES.md>
    ---
    ```

    Header: Purpose (self-contained operator verification of the v1.11 programmable-widget chain against the running app), Pre-reading: None, Outcome routing: any FAIL halts the UAT → a 61.x repro-test-driven gap plan (failing RED repro → fix → re-walk the affected section before 61-03 compiles); trivial fixes may ride inline; this is a milestone gate — gaps are NOT accepted as tech debt.

    ## Section 0 — Preconditions (PASS/FAIL each):
    - P1: app running (web + server) against deployed Kinetica in PASSWORD mode.
    - P2: a non-bypass ANALYST-role login is ready for §3 (record lowercased username placeholder); confirm it is NOT admin/designer (no bypass).
    - P3: a dashboard exists with (a) a MAP widget bound to a real CLASS-BREAK-CAPABLE layer, and (b) authored via the Phase 59 panel: a RADIO-GROUP widget with >= 2 options — one option patching the layer's `renderMode` (class-break render mode) [+ ideally a second option that ALSO sets `cb_config` for the §2 switch-replace test], and a separate option patching a `widget.config` field of another widget. A `defaultOptionId` is configured. Record the dashboard/layer/option names used.
    - P4: 61-01 automated gates recorded ALL PASS (cite 61-01-AUTOMATED-GATES.md overall_verdict + commit). Record-only — no manual rerun.

    ## Section 1 — Live config switch [ROADMAP SC1] (DESIGNER):
    - 1.1 RENDER-MODE LIVE SWITCH: as designer, click the radio option that patches the layer's class-break render mode. The MAP updates LIVE — the class-break rendering changes in place — with NO remount/reload (map does not blank-and-rebuild; tiles re-request with the new STYLES). Confirm no manual refresh needed.
    - 1.2 widget.config-FIELD LIVE SWITCH: click the option that patches a `widget.config` field of another widget. That widget reflects the new config value LIVE.
    - 1.3 RELOAD RESETS TO DEFAULT (TRANSIENT — the corrected SC1/SC4 behavior): after selecting a non-default option, RELOAD the dashboard. The radio re-applies its configured `defaultOptionId` — the control shows the DEFAULT option active and the target reflects the default, NOT the option clicked before reload. ATTEST reload-resets-to-configured-default. (Explicit note in the check text: the live click is transient by design — expecting it to survive reload would be a false fail.)

    ## Section 2 — Switch-replace + isolation [ROADMAP SC2] (DESIGNER):
    - 2.1 SWITCH-REPLACE ISOLATION: select an option that sets BOTH renderMode + cb_config; observe the map. Then select an option that sets ONLY renderMode. The layer's cb_config must REVERT to its saved baseline (the prior option's cb_config does NOT linger) while renderMode reflects the new option. (If P3 didn't author a cb_config-setting option, note it and exercise the closest two-field vs one-field pair available.)
    - 2.2 OUT-OF-ALLOW-LIST REJECTED AT SAVE (operator-visible): in the Phase 59 config panel, hand-edit an option's JSON to include an unknown/meta key (e.g. a non-allow-listed field or `id`/`__proto__`). Attempt to SAVE. Save is rejected with an operator-visible validation message; the bad binding cannot persist.
    - 2.3 NO FILTER / NO MATERIALIZE DURING DISPATCH: while clicking radio options (§1/§2 actions), confirm NO filter chips appear in the filter bar and NO materialize fires — `filterVersion` is unchanged, no dynamic-view materialize/drop network calls, the dashboard's filtered data is untouched. (The engine is decoupled from the filter/materialize system — sole-materialize-trigger intact.)

    ## Section 3 — Viewer-safe / transient [ROADMAP SC1 payoff] (VIEWER = non-bypass ANALYST):
    - 3.1 VIEWER LIVE SWITCH, NO PERMISSION ERROR: log in as the non-bypass analyst (P2). Open the dashboard. Click the radio render-mode option. The map switches LIVE with NO permission error (no 403, no toast denial) — because the change is a transient session overlay with NO PATCH.
    - 3.2 NO SHARED-DASHBOARD MUTATION: the viewer's selection does NOT persist to the shared dashboard. Confirm via a SECOND session (designer/admin) that the dashboard's saved state is unchanged — the designer does not see the viewer's pick. (And reload in the viewer session resets to the configured default per §1.3.)
    - 3.3 ORPHAN TARGET SAFETY: exercise an orphan target — either author a radio option bound to a target then delete that target (config side surfaces the orphan warning from Phase 59), OR click an option whose target was deleted (runtime: typed no-op + toast, no crash). Attest the warning/typed-no-op surfaces and nothing corrupts.

    ## Section 4 — Automated Gates Reference [ROADMAP SC3/SC4]:
    - 4.1 SC3/SC4 covered by 61-01-AUTOMATED-GATES.md (cite §0 P4). List the gate outcomes (frontend vitest 100%, web+server tsc clean, server set-gate ⊆ known-flaky, targeted v1.11 specs green). Record-only.

    ## Section 5 — Gaps Block: a `gaps: []` YAML block with the commented per-defect template (id GAP-61-NN, severity, in_scope: v1.11, sections, title, resolution → routed to 61.x).

    ## Attestation Summary: `overall_result:` (passed|gaps_found), sections_passed/failed/skipped, operator_notes, attested_by, attested_on — all PENDING until the checkpoint.

    ## Traceability: two tables — (a) ROADMAP SC → sections (SC1→§1+§3, SC2→§2, SC3+SC4→§0 P4+§4), (b) Requirement ID → sections (VERIFY-V111-01 → §0-§5 all).

    Leave all `status:` as PENDING and `evidence:`/attestation fields blank for the operator to fill at the checkpoint.
  </action>
  <verify>
    <automated>test -f .planning/phases/61-verification-live-uat/61-UAT.md && grep -qE "^## Section 1" .planning/phases/61-verification-live-uat/61-UAT.md && grep -qE "^## Section 3" .planning/phases/61-verification-live-uat/61-UAT.md && grep -qi "transient\|reset.*default\|default.*reload" .planning/phases/61-verification-live-uat/61-UAT.md && grep -qi "overall_result" .planning/phases/61-verification-live-uat/61-UAT.md && grep -q "61-01-AUTOMATED-GATES" .planning/phases/61-verification-live-uat/61-UAT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 61-UAT.md exists with frontmatter (operator, automated_gates_ref, automated_gates_verdict)
    - §0 (P1-P4) + §1 (1.1-1.3) + §2 (2.1-2.3) + §3 (3.1-3.3) + §4 (4.1) + §5 gaps + attestation + traceability all present
    - §1.3 text explicitly frames reload as RESET-TO-DEFAULT (transient), not survives-reload
    - §3 covers viewer live-switch + no-permission-error + no-shared-mutation + orphan safety
    - §0 P4 cites 61-01-AUTOMATED-GATES.md; traceability maps VERIFY-V111-01
    - All status fields PENDING (operator fills at checkpoint)
  </acceptance_criteria>
  <done>61-UAT.md is a complete, self-contained walk-through doc ready for the operator to execute against the live app.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runs the live walk against deployed Kinetica and attests each section</name>
  <files>.planning/phases/61-verification-live-uat/61-UAT.md</files>
  <action>Operator executes the authored 61-UAT.md against the deployed app (PASSWORD mode), switching between the designer/admin (RPereira@kinetica.com) and the non-bypass analyst logins; fills each check's status/evidence and the Attestation Summary. Exact steps are in how-to-verify below.</action>
  <what-built>
    A complete, self-contained UAT walk-through doc (`61-UAT.md`) covering the corrected 4 SCs of the v1.11 programmable-widget chain, plus the SC3/SC4 automated-gate record (`61-01-AUTOMATED-GATES.md`, overall_verdict recorded). The app is the existing deployed build — no v1.11 feature code changed in Phase 61.
  </what-built>
  <how-to-verify>
    1. Confirm §0 preconditions P1-P4 (app running against deployed Kinetica in PASSWORD mode; a non-bypass ANALYST login ready; a dashboard with a class-break-capable map layer + an authored radio group with a render-mode option, a widget.config-field option, a configured default; 61-01 gates recorded ALL PASS). Mark each PASS/FAIL with evidence.
    2. Execute §1 as designer: click the render-mode option → map switches class-break rendering LIVE (no remount). Click the widget.config-field option → that widget updates live. Reload → radio RESETS to the configured default (transient — do NOT expect the live click to survive). Attest 1.1-1.3.
    3. Execute §2 as designer: switch from a renderMode+cb_config option to a renderMode-only option → cb_config reverts to baseline (2.1). Hand-edit an option's JSON to an out-of-allow-list/meta key → save is rejected, operator-visible (2.2). While dispatching, confirm NO filter chips + NO materialize (filterVersion unchanged) (2.3).
    4. Execute §3 as the non-bypass ANALYST viewer: click the radio render-mode option → map switches LIVE with NO permission error (3.1); confirm nothing persists to the shared dashboard via a second designer/admin session + reload resets to default (3.2); exercise an orphan target → warning (config) / typed no-op + toast (runtime), no crash (3.3).
    5. §4.1: confirm SC3/SC4 are covered by the 61-01 record (record-only, no rerun).
    6. Fill every `status:`/`evidence:` block, the gaps YAML, and the Attestation Summary (`overall_result`, sections passed/failed, attested_by, attested_on) in 61-UAT.md.

    Routing: any FAIL halts the UAT and routes to a 61.x repro-test-driven gap plan (failing RED repro → fix → re-walk the affected section before 61-03 compiles). Trivial fixes may ride inline at executor discretion. Gaps are NOT accepted as tech debt — this is the v1.11 milestone gate.
  </how-to-verify>
  <verify>Operator attestation recorded in 61-UAT.md Attestation Summary (overall_result set).</verify>
  <done>61-UAT.md fully attested; overall_result is passed (all sections PASS) or gaps_found (→ 61.x).</done>
  <resume-signal>Type "approved" (sets overall_result: passed) or describe the gaps found (sets overall_result: gaps_found → spin a 61.x plan).</resume-signal>
</task>

</tasks>

<verification>
- 61-UAT.md authored in the 57-UAT.md format, covering the corrected 4 SCs (SC1 incl. the transient reload-resets-to-default reframe; the §3 viewer-safe payoff)
- Operator executed the walk against live deployed Kinetica (password mode) with designer + non-admin-analyst logins
- Every section attested PASS/FAIL with evidence; Attestation Summary completed with overall_result
- Any FAIL routed to a 61.x gap-closure plan before 61-03 compiles
</verification>

<success_criteria>
`61-UAT.md` is complete and operator-attested with `overall_result: passed` (or `gaps_found` → 61.x). The headline §3 viewer payoff (live switch, no permission friction, no shared mutation, reset-to-default) is attested.
</success_criteria>

<output>
After completion, create `.planning/phases/61-verification-live-uat/61-02-SUMMARY.md`.
</output>
