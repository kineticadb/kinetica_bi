---
phase: 93.5-spatial-filters-in-the-combination-model
verified: 2026-06-28T19:45:00Z
status: passed
score: 5/5 success criteria verified
re_verification: false
---

# Phase 93.5: Spatial Filters in the Combination Model — Verification Report

**Phase Goal:** Spatial (map-draw) filters flow through the same one-view-per-unique-combination model as column filters — each visualization's resolved set + dedup hash incorporate the spatial shapes it accepts (per its source allow-list's spatial-draws entry), the combination orchestrator OWNS spatial materialization (no per-renderer trigger), and a table shared between a chart and a map no longer drops spatial. Default accept-all keeps spatial applied everywhere.
**Verified:** 2026-06-28T19:45:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC1 | Chart + map sharing a table with accept-all + active spatial draw materializes ONE combo view including the spatial predicate; neither dropped nor double-materialized | VERIFIED | SC1 spec (line 826 of orchestrator.spec.ts) asserts 1 POST carrying `spatialFilters`, `spatialTarget`, and spatial-extended `combinationKey`; chart `w:11` and layer `l:20` share the same hash. |
| SC2 | Single spatial draw → exactly one POST per affected table carrying spatialFilters + spatialTarget | VERIFIED | SC2 spec (line 872) asserts 1 POST with `spatialFilters` present when no column filter; spatial-only hash does not contain "NOFILTER". |
| SC3 | Viz excluding spatial draws reads column-only view; sibling accepting spatial reads spatial view (different hashes) | VERIFIED | SC3 spec (line 905) asserts `hashA` (accept-all) has a spatial hash, `hashB` (empty allowlist) resolves to NOFILTER (undefined in vizToHash); they differ. |
| SC4 | Two vizs accepting same columns + same shapes share ONE combo view; removing a shape re-derives hash + ref-counts/DROPs correctly | VERIFIED | SC4 spec (line 951) asserts 1 POST (shared hash, refCount 2); shape removal bumps spatialFilterVersion; old spatial hash entry released and `dropCombinationView` called. |
| SC5 (unit coverage) | resolveSpatialShapes has accept-all/include/exclude/empty specs; stableComboHash has determinism + order-independence coverage; vitest 100%; tsc clean; theme-guard green | VERIFIED | 30 specs in stableComboHash.spec.ts + resolveSpatialShapes.spec.ts all pass; full suite 127/127 files pass; tsc clean; theme-guard 130/130 pass. |

**Score:** 5/5 success criteria verified

---

## Required Artifacts

| Artifact | Status | Details |
|----------|--------|---------|
| `packages/web/src/lib/stableComboHash.ts` | VERIFIED | Optional `shapes?: Pick<Shape,"wkt">[]` 4th param present (line 23); `";s:"` delimiter (line 56-57); two `localeCompare` usages (column sort + wkt sort); `slice()` before sort prevents mutation. |
| `packages/web/src/lib/resolveSpatialShapes.ts` | VERIFIED | Exports `resolveSpatialShapes`; imports `SPATIAL_DRAWS_SENTINEL`; `includes(SPATIAL_DRAWS_SENTINEL)` check present; `slice()` used in both accept paths. |
| `packages/web/src/lib/resolveSpatialShapes.spec.ts` | VERIFIED | 8 `it(` blocks (6 required cases + 2 no-mutation cases); all 30 Plan 01 specs pass. |
| `packages/web/src/lib/stableComboHash.spec.ts` | VERIFIED | Spatial extension describe block with no-break, determinism, order-independence, distinctness, wkt-only identity, and no-mutation specs. |
| `packages/web/src/hooks/useCombinationOrchestrator.ts` | VERIFIED | Imports `resolveSpatialShapes`, `aggregateSpatialTargetsByTable`, `useSpatialFilterStore`; `spatialFilterVersion` primitive selector; dep array includes `spatialFilterVersion` (line 490); shapes read imperatively (line 154); `resolveSpatialShapes` called in widget loop (line 173) AND layer loop (line 211); `shapesForHash` Pitfall-1 guard (lines 178, 214); `spatialFilters` + `spatialTarget` in materialize call (lines 363-368); `"records"` in NON_TRIGGER_TYPES (line 70); `"table"` deliberately absent from NON_TRIGGER_TYPES; fallback hash column-only (line 255, acceptedShapes: [] line 291). |
| `packages/web/src/components/DashboardsPage.tsx` | VERIFIED | No import or call of `useMapOnlySpatialMaterialize`; `useCombinationOrchestrator` call intact (line 447). |
| `packages/web/src/hooks/useCombinationOrchestrator.spec.ts` | VERIFIED | Phase 93.5 describe block (line 804) with SC1–SC6 (7 scenarios) + SC6b + SOLE-TRIGGER GATE = 9 new tests; all 24 orchestrator specs pass. |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `resolveSpatialShapes.ts` | `SPATIAL_DRAWS_SENTINEL` (filterSourceTypes.ts) | `import` + `allowedSourceWidgetIds.includes(SPATIAL_DRAWS_SENTINEL)` | WIRED | Line 3 import, line 21 includes check. |
| `stableComboHash.ts` | `Shape.wkt` | `sort shapes by wkt, append ;s: segments` | WIRED | `localeCompare` on `wkt` (line 41); `s.wkt` appended (line 42). |
| `useCombinationOrchestrator.ts` | `useSpatialFilterStore.getState().shapes` | Imperative read inside setTimeout callback | WIRED | Line 154 inside the 300ms timer callback. |
| `useCombinationOrchestrator.ts` | `spatialFilterVersion` | Primitive selector in Effect dep array | WIRED | Selector (line 96); dep array (line 490). |
| `useCombinationOrchestrator.ts` | `stableComboHash` 4th param | `stableComboHash("table", tableId, resolved, shapesForHash)` | WIRED | Lines 179, 215. |
| `useCombinationOrchestrator.ts` | `materializeFilter` spatial args | `spatialFilters + spatialTarget` when `acceptedShapes.length > 0 && spatialTarget` | WIRED | Lines 363–368. |

---

## Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| SPATIAL-V118-01 | 93.5-01-PLAN, 93.5-02-PLAN | Spatial filters participate in the per-combination view model; orchestrator owns spatial materialization; spatial neither dropped nor double-materialized; accept-all keeps spatial applied everywhere | SATISFIED | All 5 SC verified in code + specs; 24 orchestrator specs green; REQUIREMENTS.md shows "Complete". |

---

## Specific Verification Checklist (per prompt requirements)

### 1. stableComboHash: optional shapes 4th param

- **Byte-identical when shapes empty/absent:** `stableComboHash(t, id, filters)` === `stableComboHash(t, id, filters, [])` === `stableComboHash(t, id, filters, undefined)` — verified in "empty shapes array produces byte-identical output to 3-arg call" spec.
- **Phase-88 specs still green:** All 10 pre-existing specs in the first `describe("stableComboHash")` block pass (no edits to their assertions).
- **Order-independence (WKT-sort):** "produces the same hash regardless of shape array order" spec verified; `.slice().sort((a, b) => a.wkt.localeCompare(b.wkt))` in implementation.
- **WKT-only identity:** spec "hashes identically when shapes have same wkt but different id/label/addedAt" passes; signature uses `Pick<Shape, "wkt">`.
- **No-mutation:** `Object.freeze` test passes; implementation uses `.slice()` before sort.

### 2. resolveSpatialShapes: all-or-nothing via SPATIAL_DRAWS_SENTINEL

- **Accept-all default (cfg undefined):** Test 1 passes.
- **sourceMode "all":** Test 2 passes.
- **SPATIAL_DRAWS_SENTINEL present:** Test 3 passes.
- **Sentinel absent:** Tests 4 + 5 return `[]`.
- **Empty shapes passthrough:** Test 6 returns `[]`.
- **No mutation:** Two no-mutation specs verify `slice()` in both accept paths.

### 3. Orchestrator wiring

- **`spatialFilterVersion` in Effect dep array:** CONFIRMED — line 490: `[filterVersion, spatialFilterVersion, dashboardId, widgetsKey, layersKey, ceiling]`.
- **Shapes read imperatively:** CONFIRMED — line 154 inside `setTimeout` callback, NOT in subscription.
- **`acceptedShapes` folded into combo hash:** CONFIRMED — `shapesForHash` used as 4th arg to `stableComboHash`.
- **`spatialFilters` + `spatialTarget` in materialize ONLY when acceptedShapes non-empty AND eligible target exists:** CONFIRMED — `const spatialArgs = (acceptedShapes.length > 0 && spatialTarget) ? {...} : {}` (lines 363–368); wkb exclusion handled by `aggregateSpatialTargetsByTable` which calls `isSpatialTargetEligible` (pre-existing, reused as-is).
- **Fallback hash stays column-only:** CONFIRMED — fallback `stableComboHash("table", tableId, allFilters)` (no shapes arg) at line 255; HashEntry `acceptedShapes: [], spatialTarget: undefined` at line 291.
- **dv path does NOT fold spatial:** CONFIRMED — widget loop skips `tableId === undefined` (dv-bound); layer loop skips `dynamic_view_id !== null` — spatial code path only reached for table-bound viz.
- **No re-render loop:** `spatialFilterVersion` bumps ONLY on `addShape`/`removeShape`/`clearAll` (NOT on `setEntry`); SC4 test verifies orchestrator re-fires on shape removal without looping.

### 4. useMapOnlySpatialMaterialize fully removed

- **File deleted:** `test -f packages/web/src/hooks/useMapOnlySpatialMaterialize.ts` → false. CONFIRMED.
- **Spec deleted:** `test -f packages/web/src/hooks/useMapOnlySpatialMaterialize.spec.ts` → false. CONFIRMED.
- **Import removed from DashboardsPage:** No match in grep. CONFIRMED.
- **Call site removed from DashboardsPage:** No match in grep. CONFIRMED.
- **No remaining references:** Only match is a string literal inside the SOLE-TRIGGER GATE test (`path.join(hooksDir, "useMapOnlySpatialMaterialize.ts")`) which asserts the file does NOT exist — correct.
- **Map-only tables covered by orchestrator layer enumeration:** Orchestrator Phase 92 layer loop enumerates ALL table-bound layers including map-only; SC1 verifies a layer `l:20` on TABLE_A (map-only table) gets the spatial combo view.

### 5. Sole-materialize-trigger invariant

- **Grep gate present in spec:** SOLE-TRIGGER GATE test at line 1098 in orchestrator.spec.ts; asserts (a) `useMapOnlySpatialMaterialize.ts` absent, (b) orchestrator source contains `spatialFilters` AND `combinationKey: hash`, (c) no other hook in `hooks/` calls `materializeFilter` with `spatialFilters`.
- **Gate PASSES:** All 24 orchestrator specs including SOLE-TRIGGER GATE pass.
- **`"records"` in NON_TRIGGER_TYPES:** CONFIRMED — line 70.
- **`"table"` NOT excluded:** CONFIRMED — not present in NON_TRIGGER_TYPES set (widget.type "table" = TableRenderer inside AggregatedWidgetRenderer, correctly reads combo store).
- **Records legacy island intact:** `RecordsTableRenderer` in WidgetRenderer.tsx not modified in this phase; its self-contained materialize path untouched.

---

## Test Gates

| Gate | Result |
|------|--------|
| `cd packages/web && npx tsc --noEmit` | CLEAN (no output) |
| `npx vitest run src/lib/stableComboHash.spec.ts src/lib/resolveSpatialShapes.spec.ts` | 30/30 pass |
| `npx vitest run src/hooks/useCombinationOrchestrator.spec.ts` | 24/24 pass |
| `npx vitest run src/styles/theme-guard.spec.ts` | 130/130 pass |
| `npx vitest run` (full suite) | 127/127 test files pass; 2935 tests pass; 9 unhandled-rejection errors are the known TD-V16-TEST-ISOLATION set (InfoCardRenderer.spec.tsx + InfoPopup.spec.tsx cross-mode contamination) |
| `git diff --name-only packages/server` across phase commits | EMPTY — zero server files |
| `git diff --name-only` grep `package.json` across phase commits | EMPTY — no new deps |

---

## Anti-Patterns Found

None found in phase-modified files. The string "placeholder" appears only as an HTML input attribute and a code comment about view creation — both innocuous.

---

## Human Verification Required

None — all success criteria are verifiable programmatically via specs and grep. The network-tab behavioral claims (SC1 "spatial is not dropped", SC2 "matches v1.5/v1.17") are fully covered by unit specs that mock `materializeFilter` and assert the exact args including `spatialFilters`, `spatialTarget`, and `combinationKey`.

---

## Gaps Summary

No gaps. All 5 ROADMAP success criteria are satisfied by the actual codebase:

- Plan 01 (pure functions): `stableComboHash` and `resolveSpatialShapes` are substantive, wired, and fully tested.
- Plan 02 (orchestrator wiring): All 7 required changes are present — imperative shapes read, `resolveSpatialShapes` per viz, Pitfall-1 no-target guard, `spatialFilters` in materialize, `spatialFilterVersion` dep, `"records"` in NON_TRIGGER_TYPES, `useMapOnlySpatialMaterialize` fully deleted.
- Phase is FRONTEND-ONLY: confirmed zero server diff, zero package.json diff.
- ROADMAP plan checkboxes remain `[ ]` (executor did not update them), but REQUIREMENTS.md correctly marks SPATIAL-V118-01 as "Complete". This is a planning-doc cosmetic issue, not a code gap.

---

_Verified: 2026-06-28T19:45:00Z_
_Verifier: Claude (gsd-verifier)_
