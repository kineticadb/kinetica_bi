---
phase: 70-numeric-other-bucket
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/lib/wmsUrlBuilder.ts
  - packages/web/src/lib/wmsUrlBuilder.spec.ts
  - packages/web/src/components/charts/CbConfigForm.tsx
  - packages/web/src/components/charts/CbConfigForm.spec.tsx
autonomous: true
requirements: [CBOTHER-V114-01, CBOTHER-V114-02, CBOTHER-V114-03]

must_haves:
  truths:
    - "Operator sees an 'Include <other> bucket' toggle on a NUMERIC class-break config (today it only appears for categorical)."
    - "A numeric config whose breaks[] ends with a {value:'<other>'} row emits CB_VALS=...,<other> (literal token, not a min:max range)."
    - "POINTCOLORS (and any optional advanced arrays) stay positionally aligned with CB_VALS — the <other> break contributes its color in the same trailing position."
    - "Creating a new numeric config / switching a column to numeric defaults includeOtherBucket=true and appends one <other> row."
    - "An already-saved numeric config with NO <other> row and NO includeOtherBucket flag emits IDENTICAL WMS params as before (no <other> token injected) — the preservation invariant."
    - "The numeric <other> row renders as the read-only cb-other-chip (no min/max inputs) and keeps its color swatch + advanced panel; numeric min<max validation does NOT fire on the <other> row."
  artifacts:
    - path: "packages/web/src/lib/wmsUrlBuilder.ts"
      provides: "Numeric CB_VALS branch emits literal <other> for the <other> break"
      contains: "b.value === \"<other>\""
    - path: "packages/web/src/lib/wmsUrlBuilder.spec.ts"
      provides: "Numeric <other> emission test + preservation regression test"
      contains: "<other>"
    - path: "packages/web/src/components/charts/CbConfigForm.tsx"
      provides: "Toggle surfaced for numeric, type-change default-on, validation whitelist for <other>"
    - path: "packages/web/src/components/charts/CbConfigForm.spec.tsx"
      provides: "Numeric toggle render + default-on-type-change + chip render tests"
  key_links:
    - from: "CbConfigForm.tsx onToggleOtherBucket / type-change handler"
      to: "cbConfig.breaks[] (appends {value:'<other>'} row)"
      via: "patchCb"
      pattern: "value: \"<other>\""
    - from: "wmsUrlBuilder.ts numeric CB_VALS branch"
      to: "params.CB_VALS"
      via: "breaks.map ternary on b.value === '<other>'"
      pattern: "CB_VALS"
---

<objective>
Extend the class-break `<other>` catch-all bucket (shipped for CATEGORICAL columns in v1.7 Phase 39) to NUMERIC class-break columns. The numeric `CB_VALS` builder must emit the literal `<other>` token after the numeric ranges (`1:3,3:5,<other>`), the form must surface the toggle for numeric (default-ON for new/edited configs), and the numeric `<other>` row must render exactly like the categorical one (read-only chip, per-break color/style, whitelisted in validation).

Purpose: Lets operators express a catch-all bucket on numeric class-break maps so out-of-range/uncovered values render with a distinct color instead of disappearing — matching the categorical capability.

Output: Modified `wmsUrlBuilder.ts` (numeric branch), modified `CbConfigForm.tsx` (toggle gate + default-on + validation whitelist), and extended spec files including a regression test that LOCKS the preservation invariant.

Scope guardrails (NON-NEGOTIABLE — flag any deviation):
- FRONTEND-ONLY (`packages/web`). Zero server diff expected — `cb_config` already round-trips through the existing PATCH route. Flag any server change as a deviation.
- Do NOT touch the materialize path — `AggregatedWidgetRenderer` remains the SOLE materialize trigger. This phase touches only cb_config form state + WMS param-string building.
- Do NOT inject `<other>` in `coalesceCbConfig` or the builder. The builder only emits `<other>` when an `<other>` break actually exists in `breaks[]`. (Preservation invariant, CBOTHER-V114-02.)
- NO raw hex anywhere new — the `<other>` row color comes from `PALETTE_COLORS[index % len]` via the existing `createDefaultBreak` idiom. `theme-guard.spec.ts` must stay green.
- This is a numeric MIRROR of the categorical `<other>` (v1.7 Phase 39). Follow the categorical shape exactly.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/70-numeric-other-bucket/70-CONTEXT.md

# Canonical refs (read these — patterns must be replicated exactly)
@packages/web/src/lib/cbConfig.ts
@packages/web/src/lib/wmsUrlBuilder.ts
@packages/web/src/components/charts/CbConfigForm.tsx

<interfaces>
<!-- Key contracts the executor needs. Extracted from packages/web/src/lib/cbConfig.ts. -->
<!-- Use these directly — no codebase exploration needed. -->

CbBreak (cbConfig.ts:21-51):
```typescript
export type CbBreak = {
  value: string | number;   // categorical break value; literal "<other>" is the sink-bucket sentinel
  min?: number;             // numeric lower bound (INCLUSIVE) — used only when valsType="numeric"
  max?: number;             // numeric upper bound (EXCLUSIVE)
  color: string;            // 8-char AARRGGBB
  label?: string;
  pointSize?: number;
  pointShape?: string;
  shapeLineWidth?: number;
  shapeLineColor?: string;
  shapeFillColor?: string;
};
```

CbConfig (cbConfig.ts:53-68):
```typescript
export type CbConfig = {
  attr: string;
  valsType: "numeric" | "categorical";
  breaks: CbBreak[];
  includeOtherBucket?: boolean;   // form-UI flag; builder does NOT auto-inject
};
```

createDefaultBreak (cbConfig.ts:137) returns a CbBreak with color = PALETTE_COLORS[index % len]
and all 5 advanced fields populated. PALETTE_COLORS is the theme-safe palette (cbConfig.ts:122).

EXISTING categorical reference behavior in wmsUrlBuilder.ts:392-395 (the numeric vs categorical
ternary). Categorical emits `String(b.value)` verbatim — this is what numeric <other> must mirror.

EXISTING categorical reference in CbConfigForm.tsx:
  - type-change auto-append (lines 228-240): `if (typeChanged && newValsType === "categorical")` sets
    nextIncludeOther=true and appends one {value:"<other>"} row.
  - onToggleOtherBucket (lines 256-271): appends/filters the {value:"<other>"} row.
  - toggle UI + NULL hint (lines 660-673) live INSIDE the `{cbConfig.valsType === "categorical" && (...)}`
    block that opens at line 639.
  - numeric validation (lines 511-526): numeric branch requires finite min<max on EVERY row — does NOT
    currently whitelist <other>. The categorical branch already does (`if (b.value === "<other>") return true;`).
  - <other> chip render (lines 842-848) precedes the numeric min/max branch and is already valsType-agnostic.

STALE TEST TO RETIRE (CbConfigForm.spec.tsx:564-573):
  - `it("in numeric mode, [✓] Include <other> bucket checkbox is NOT rendered", ...)` asserts
    `screen.queryByLabelText("Include <other> bucket")).toBeNull()` for `valsType:"numeric"`.
  - Task 2 makes the toggle RENDER in numeric mode → this test will FAIL the full-suite gate.
    Task 2's spec edits MUST delete/invert it (see Task 2 action step 4).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit literal &lt;other&gt; in the numeric CB_VALS branch + lock the preservation invariant</name>
  <files>packages/web/src/lib/wmsUrlBuilder.ts, packages/web/src/lib/wmsUrlBuilder.spec.ts</files>
  <read_first>
    - packages/web/src/lib/wmsUrlBuilder.ts (the classbreak branch, lines 372-414 — the numeric vs categorical CB_VALS ternary at 392-395 is the exact edit site)
    - packages/web/src/lib/wmsUrlBuilder.spec.ts (existing classbreak tests ~lines 423-755; the categorical-<other> test at lines 681-692 is the reference to mirror)
    - packages/web/src/lib/cbConfig.ts (CbBreak / CbConfig shape — confirm <other> break carries no min/max)
    - packages/web/src/components/charts/CbConfigForm.tsx (lines 842-848 — confirm <other> is identified by `b.value === "<other>"`, the same check to use in the builder)
  </read_first>
  <behavior>
    - Numeric config with breaks [{min:1,max:3},{min:3,max:5},{value:"<other>"}] → CB_VALS === "1:3,3:5,<other>".
    - The <other> break's color is still emitted positionally in POINTCOLORS (N ranges + 1 = N+1 colors; the <other> color is last).
    - PRESERVATION REGRESSION: a numeric config with NO <other> break (e.g. breaks [{min:0,max:10},{min:10,max:25},{min:25,max:50}]) emits CB_VALS === "0:10,10:25,25:50" with NO "<other>" substring — byte-identical to pre-change output. This test must FAIL if anyone later auto-injects <other>.
    - The existing categorical <other> test (CB_VALS === "cash,credit,<other>") must remain green (no regression to the categorical path).
  </behavior>
  <action>
    In `wmsUrlBuilder.ts`, modify ONLY the numeric arm of the CB_VALS ternary at lines 392-395. Currently:
    ```ts
    params.CB_VALS =
      cb.valsType === "numeric"
        ? cb.breaks.map((b: CbBreak) => `${b.min ?? 0}:${b.max ?? 0}`).join(",")
        : cb.breaks.map((b: CbBreak) => String(b.value)).join(",");
    ```
    Change the numeric `.map` so the `<other>` break emits the literal token instead of a `min:max` range, mirroring the categorical identification check `b.value === "<other>"`:
    ```ts
    params.CB_VALS =
      cb.valsType === "numeric"
        ? cb.breaks
            .map((b: CbBreak) => (b.value === "<other>" ? "<other>" : `${b.min ?? 0}:${b.max ?? 0}`))
            .join(",")
        : cb.breaks.map((b: CbBreak) => String(b.value)).join(",");
    ```
    Do NOT pre-encode `<other>` — emit the raw `<other>` string; URLSearchParams handles `%3Cother%3E` downstream.
    Do NOT touch POINTCOLORS / POINTSIZES / POINTSHAPES / SHAPE* lines (396-413) — they already map every break positionally, so the `<other>` break's color/style lands in the correct trailing slot with zero special-casing (matches categorical).
    Do NOT modify `coalesceCbConfig`, `EMPTY_CB_CONFIG`, or `isCbConfigConfigured` — the builder must emit `<other>` ONLY when an `<other>` break exists in `breaks[]`.

    In `wmsUrlBuilder.spec.ts`, add to the existing cb_raster describe block (near the categorical-<other> test at lines 681-692):
    1. A numeric-<other>-emission test: build a config `{ attr:"fare", valsType:"numeric", breaks:[{min:1,max:3,color:"FF000000"},{min:3,max:5,color:"FF111111"},{value:"<other>",color:"FF7788AA"}] }` and assert `params.CB_VALS === "1:3,3:5,<other>"` AND `params.POINTCOLORS.split(",").length === 3` (positional alignment).
    2. A PRESERVATION regression test (label it explicitly, e.g. "numeric config WITHOUT an <other> break emits NO <other> token — preservation invariant CBOTHER-V114-02"): build `{ attr:"fare", valsType:"numeric", breaks:[{min:0,max:10,...},{min:10,max:25,...},{min:25,max:50,...}] }` and assert `params.CB_VALS === "0:10,10:25,25:50"` AND `expect(params.CB_VALS).not.toContain("<other>")`.
    Reuse the file's existing `makeConfig` / cb_config-JSON test helpers (see lines 629-735) — match their layerJsonFields wiring.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/lib/wmsUrlBuilder.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'b.value === "<other>"' packages/web/src/lib/wmsUrlBuilder.ts` returns a hit inside the numeric CB_VALS branch (~line 392-395).
    - `grep -c '<other>' packages/web/src/lib/wmsUrlBuilder.spec.ts` ≥ 3 (existing categorical test + new numeric-emit + preservation test).
    - `grep -n 'not.toContain("<other>")' packages/web/src/lib/wmsUrlBuilder.spec.ts` returns a hit (the preservation regression assertion).
    - `cd packages/web && npx vitest run src/lib/wmsUrlBuilder.spec.ts` exits 0; the new numeric-<other>, preservation, AND pre-existing categorical-<other> tests all pass.
    - `git diff --stat packages/server` shows NO server files changed.
  </acceptance_criteria>
  <done>Numeric CB_VALS emits `1:3,3:5,<other>` for a config containing an `<other>` break; a numeric config without one emits no `<other>` token (regression-locked); categorical path unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface the &lt;other&gt; toggle for numeric mode + default-on on type-change + whitelist &lt;other&gt; in numeric validation</name>
  <files>packages/web/src/components/charts/CbConfigForm.tsx, packages/web/src/components/charts/CbConfigForm.spec.tsx</files>
  <read_first>
    - packages/web/src/components/charts/CbConfigForm.tsx (lines 218-271 column-change + onToggleOtherBucket handlers; lines 505-546 validation effect; lines 638-675 the categorical-gated toggle+hint block; lines 842-848 the <other> chip render)
    - packages/web/src/components/charts/CbConfigForm.spec.tsx (existing categorical <other> toggle tests ~lines 551+; THE STALE NUMERIC TEST at lines 564-573 that must be retired; numeric-column tests ~lines 155-309 for the makeCbConfig / render harness pattern)
    - packages/web/src/lib/cbConfig.ts (createDefaultBreak, PALETTE_COLORS — the <other> row color source)
    - packages/web/src/styles/global.css (around line 2202, `.cb-other-chip` — confirm style exists; do NOT add new hex)
  </read_first>
  <behavior>
    - In numeric mode, the "Include <other> bucket" checkbox renders (today it only renders for categorical).
    - In categorical mode, EXACTLY ONE "Include <other> bucket" checkbox renders (the relocation must not leave a duplicate).
    - Toggling it ON in numeric mode appends one {value:"<other>"} row (reusing onToggleOtherBucket); toggling OFF removes it.
    - Switching a column TO numeric (typeChanged && newValsType==="numeric") sets includeOtherBucket=true and appends one <other> row — mirroring the categorical rule. A new numeric config is default-ON.
    - The numeric <other> row renders as the read-only cb-other-chip (no Min/Max inputs) and still shows its color swatch + advanced panel.
    - Numeric validation does NOT raise "Min must be less than Max" / empty-range errors on the <other> row; a numeric config with valid ranges + one <other> row is VALID (isValid(true)).
    - When the toggle is OFF (numeric), the "NULL values will not appear in the map." hint shows (it currently shows only for categorical).
    - PRESERVATION: loading an existing numeric config with no <other> row and no includeOtherBucket flag does NOT flip the flag or inject a row at render/parse time (default-on applies only at creation/column-change).
  </behavior>
  <action>
    All production edits in `CbConfigForm.tsx`. Mirror the categorical idiom exactly — do not invent new patterns.

    1. Type-change default-on (lines 228-240): broaden the categorical-only auto-append to also cover numeric. Change the guard from `if (typeChanged && newValsType === "categorical")` to fire for BOTH valsTypes (`if (typeChanged)`), keeping the existing `nextIncludeOther = true` + `hasOther` guard + append of `{ ...createDefaultBreak(newValsType, nextBreaks.length), value: "<other>" }`. Pass `newValsType` (not hardcoded "categorical") into createDefaultBreak so the row is materialized cleanly (min/max are unused for the chip row but this is cleaner per CONTEXT discretion note).

    2. Surface the toggle + NULL hint for numeric. The toggle block (lines 660-673) currently lives INSIDE the `{cbConfig.valsType === "categorical" && (<div className="cb-categorical-section">...)}` fragment opened at line 639. Move ONLY the `<other>` toggle `<label className="cb-other-toggle">...</label>` AND the `{!cbConfig.includeOtherBucket && (...NULL hint...)}` OUT of the categorical-only gate so they render in BOTH modes. Keep the categorical-only cardinality-probe hints (lines 642-657) inside the categorical gate — only the toggle + NULL hint move out. Simplest implementation: render the toggle+hint in a shared block gated on `cbConfig.attr !== ""` (so it appears once a column is chosen, for either valsType), and leave the cardinality hints in the categorical-only block. CRITICAL: this is a MOVE, not a copy — after the edit the toggle JSX must appear EXACTLY ONCE in the file (delete the original occurrence inside the categorical block). A duplicate toggle in categorical mode is a regression that Task 2's spec test (categorical single-render) will catch.

    3. Whitelist `<other>` in numeric validation (lines 511-526). Inside the `if (cbConfig.valsType === "numeric") { ... }` arm of the `allValuesPresent` `.every` callback, add a leading short-circuit: `if (b.value === "<other>") return true;` BEFORE the finite-min<max checks — exactly as the categorical arm does at line 524. This stops the <other> row (which has no min/max) from failing numeric validation.

    Do NOT change `onToggleOtherBucket` (lines 256-271) — reuse it as-is for numeric (it already builds the row and filters on OFF). Do NOT change the chip render (lines 842-848) — it's already valsType-agnostic and precedes the numeric min/max branch. Do NOT add any hex — the `<other>` row color comes from PALETTE_COLORS via createDefaultBreak.

    In `CbConfigForm.spec.tsx`, mirror the existing categorical <other> tests (~lines 551+) using the same makeCbConfig / render harness:
    1. "in NUMERIC mode, the Include <other> bucket checkbox renders" — render with `valsType:"numeric"`, assert the `aria-label="Include <other> bucket"` checkbox is present (`screen.getByLabelText("Include <other> bucket")` succeeds).
    2. "switching a column to numeric defaults includeOtherBucket=true and appends one <other> row" — drive the column-change path to numeric, assert emitted cb_config has `includeOtherBucket === true` and exactly one `breaks.some(b => b.value === "<other>")`.
    3. "the numeric <other> row renders as a read-only chip (no min/max inputs)" — render a numeric config whose last break is `{value:"<other>"}`, assert the `cb-other-chip` testid is present for that row and NO Min/Max number input exists within it.
    4. RETIRE THE STALE TEST + ADD CARDINALITY GUARD: Delete (or invert) the stale test at `CbConfigForm.spec.tsx:564` — `it("in numeric mode, [✓] Include <other> bucket checkbox is NOT rendered", ...)` which asserts `screen.queryByLabelText("Include <other> bucket")).toBeNull()`. Task 2 makes the toggle RENDER in numeric, so this assertion now contradicts the new behavior and will fail the suite. Replace it with: (a) a positive numeric assertion that the toggle IS rendered (this can be the same as new test #1 — if so, just delete the stale `it(...)` block entirely so there is exactly one numeric-toggle-render test, no contradiction); and (b) a CATEGORICAL single-render regression: render a categorical config and assert `screen.getAllByLabelText("Include <other> bucket").length === 1` (the relocation left no duplicate).
    5. "a numeric config with valid ranges + one <other> row is valid" — pass an `isValid` spy, assert it's called with `true` for breaks `[{min:0,max:10},{min:10,max:20},{value:"<other>"}]`.
    Match the existing makeCbConfig helper signature (it includes `includeOtherBucket`).
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/CbConfigForm.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'if (typeChanged)' packages/web/src/components/charts/CbConfigForm.tsx` shows the broadened guard (no longer `&& newValsType === "categorical"` on the <other>-append branch).
    - In CbConfigForm.tsx, the `aria-label="Include <other> bucket"` checkbox is NO LONGER nested solely inside the `cbConfig.valsType === "categorical"` block — verify by inspection that the toggle JSX is reachable when valsType==="numeric".
    - The `aria-label="Include <other> bucket"` toggle JSX appears EXACTLY ONCE in CbConfigForm.tsx (the relocation is a move, not a copy) — `grep -c 'Include <other> bucket' packages/web/src/components/charts/CbConfigForm.tsx` returns 1.
    - `grep -n 'b.value === "<other>") return true' packages/web/src/components/charts/CbConfigForm.tsx` returns a hit INSIDE the numeric validation arm (in addition to the pre-existing categorical one).
    - `grep -n 'is NOT rendered' packages/web/src/components/charts/CbConfigForm.spec.tsx` returns NO hit for the numeric-toggle case (the stale test at line 564 has been deleted/inverted).
    - The spec contains a categorical single-render assertion: `grep -n 'getAllByLabelText("Include <other> bucket")' packages/web/src/components/charts/CbConfigForm.spec.tsx` returns a hit asserting `.length === 1`.
    - `cd packages/web && npx vitest run src/components/charts/CbConfigForm.spec.ts` exits 0; the new numeric tests, the categorical single-render guard, AND all pre-existing categorical <other> tests pass; the stale "is NOT rendered" test is gone.
    - `git diff packages/web/src/styles/global.css` is empty (no new CSS/hex added).
    - `git diff --stat packages/server` shows NO server files changed.
  </acceptance_criteria>
  <done>Numeric mode shows the `<other>` toggle (default-ON for new/column-changed configs) with exactly one toggle rendered in each mode, the `<other>` row renders as a read-only chip with its own color, numeric validation whitelists it, the stale "NOT rendered" test is retired, and existing categorical behavior is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite + tsc + theme-guard gates</name>
  <files>(no production edits — gate verification only; fix only regressions surfaced)</files>
  <read_first>
    - packages/web/src/lib/wmsUrlBuilder.ts (your Task 1 edit)
    - packages/web/src/components/charts/CbConfigForm.tsx (your Task 2 edit)
  </read_first>
  <action>
    Run the full phase gates from `packages/web` and confirm green. These are SEPARATE gates — all must pass:
    1. Frontend vitest full run: `cd packages/web && npx vitest run` → 100% pass.
    2. theme-guard: ensure `src/**/theme-guard.spec.ts` is included in the above run and green (the `<other>` color reuses PALETTE_COLORS — no raw hex introduced).
    3. Web tsc: `cd packages/web && npx tsc --noEmit` → clean.
    4. Server tsc (separate gate, must stay clean since this is frontend-only): `cd packages/server && npx tsc --noEmit` → clean.
    If any pre-existing test is unrelated to this change, do NOT fix it here — note it in the SUMMARY. Only fix regressions your Task 1/2 edits introduced (note: the stale "is NOT rendered" test was already retired in Task 2 — if it resurfaces as a failure here, Task 2's spec edit was incomplete). Confirm zero server source diff (`git diff --stat packages/server`).
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/web && npx vitest run` exits 0 (frontend 100%, including theme-guard.spec.ts).
    - `cd packages/web && npx tsc --noEmit` exits 0.
    - `cd packages/server && npx tsc --noEmit` exits 0.
    - `git diff --stat packages/server` shows no production source changes (frontend-only invariant holds).
  </acceptance_criteria>
  <done>Frontend vitest 100%, web tsc clean, server tsc clean, theme-guard green, no server diff.</done>
</task>

</tasks>

<verification>
- Numeric CB_VALS emits the literal `<other>` token only when an `<other>` break exists (`1:3,3:5,<other>`); POINTCOLORS stays positionally aligned (N+1 entries).
- Preservation invariant locked by regression test: numeric config without an `<other>` break emits byte-identical CB_VALS and no `<other>` substring.
- Numeric mode surfaces the `<other>` toggle; new/column-changed numeric configs default includeOtherBucket=true with one appended `<other>` row; existing saved configs are untouched until re-saved.
- Exactly one `<other>` toggle renders per mode (the categorical→shared relocation left no duplicate); the stale "is NOT rendered" numeric test is retired.
- Numeric `<other>` row renders as a read-only chip with its own palette color; numeric validation whitelists it.
- Gates: frontend vitest 100%, web + server tsc clean (separate), theme-guard green, zero server diff.
</verification>

<success_criteria>
- CBOTHER-V114-01: numeric class-break `<other>` catch-all emitted into CB_VALS as the literal `<other>` after the ranges. ✓ (Task 1)
- CBOTHER-V114-02: toggle defaults ON for new/re-saved numeric configs; already-saved numeric layer with no flag renders exactly as today (regression-locked). ✓ (Tasks 1 + 2)
- CBOTHER-V114-03: numeric `<other>` row presented consistently with categorical (read-only chip, assignable per-break color/style). ✓ (Task 2)
- All test/tsc/theme gates green; frontend-only (no server diff). ✓ (Task 3)
</success_criteria>

<output>
After completion, create `.planning/phases/70-numeric-other-bucket/70-01-SUMMARY.md`
</output>
</content>
</invoke>
