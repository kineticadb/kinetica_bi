---
phase: 60.1-radio-config-ux-structured-layer-target-editor-reuse-cbconfigform
plan: "03"
type: execute
wave: 2
depends_on: ["60.1-01"]
files_modified:
  - packages/web/src/components/charts/KineticaWmsLayerForm.tsx
  - packages/web/src/components/charts/RadioLayerConfigEditor.tsx
  - packages/web/src/components/charts/RadioGroupConfigPanel.tsx
  - packages/web/src/components/charts/RadioGroupConfigPanel.spec.tsx
  - packages/web/src/styles/global.css
autonomous: true
requirements: [RADIOUX-V111-01]
must_haves:
  truths:
    - "When a radio option targets a map layer, the panel renders a SIDE-BY-SIDE editor: LEFT = the radio option config; RIGHT = the FULL KineticaWmsLayerForm (RENDER MODE + all mode/style params + opacity + INFO POPUP), seeded from the option's snapshot. DATA SOURCE + SPATIAL MODE sections are ABSENT. No raw JSON is needed to author any layer appearance (SC1)."
    - "KineticaWmsLayerForm gains an additive hideSpatialMode?: boolean (default false) that omits the SPATIAL MODE radios + spatial-column pickers; existing callers (LayersModal, MapConfigPanel) are byte-unaffected (SC1)."
    - "Editing render mode / a style param / the INFO POPUP toggle updates the option's full-config snapshot (config blob + top-level cb_config/track_config + top-level info_*); data-binding/spatial keys never appear in the snapshot — bridged via the Plan 01 snapshotToLayerForm/layerFormToSnapshot adapter (SC2)."
    - "The narrow first-cut RadioLayerConfigEditor (render-mode <select> + standalone CbConfigForm) is REPLACED by the full-form side-by-side editor; raw JSON remains behind a collapsible Advanced fallback for layer targets; widget/dynamic-view targets are UNCHANGED; the config modal widens ONLY when a layer-target option is being edited (SC1/SC4)."
  artifacts:
    - path: "packages/web/src/components/charts/KineticaWmsLayerForm.tsx"
      provides: "Additive hideSpatialMode prop gating the SPATIAL MODE + spatial-column block"
      contains: "hideSpatialMode"
    - path: "packages/web/src/components/charts/RadioLayerConfigEditor.tsx"
      provides: "Full-form side-by-side editor embedding KineticaWmsLayerForm (hideSpatialMode, data-source suppressed, info wired) via snapshotToLayerForm/layerFormToSnapshot"
      contains: "KineticaWmsLayerForm"
    - path: "packages/web/src/components/charts/RadioGroupConfigPanel.tsx"
      provides: "Two-pane layout for layer-target option editing + widen-modal CSS class toggle; widget/dv unchanged; Advanced JSON kept"
      contains: "RadioLayerConfigEditor"
    - path: "packages/web/src/styles/global.css"
      provides: "Widened modal-config + two-pane layout styles, scoped to layer-target editing"
      contains: "radiogroup-layer-editor"
  key_links:
    - from: "RadioLayerConfigEditor.tsx"
      to: "components/charts/KineticaWmsLayerForm.tsx (hideSpatialMode, no layer/onDataSourceChange, info props)"
      via: "full form embed seeded by snapshotToLayerForm; writes via layerFormToSnapshot"
      pattern: "KineticaWmsLayerForm|hideSpatialMode"
    - from: "RadioLayerConfigEditor.tsx"
      to: "lib/radioGroupLayerPatch.ts snapshotToLayerForm/layerFormToSnapshot"
      via: "snapshot → form config+info on read; form config + info patch → snapshot on change"
      pattern: "snapshotToLayerForm|layerFormToSnapshot"
    - from: "RadioGroupConfigPanel.tsx OptionRow (kind === 'layer')"
      to: "the widened two-pane modal CSS class"
      via: "panel toggles a class on its root / modal-config when a layer option is edited"
      pattern: "radiogroup-layer-editor|wide"
---

<objective>
Replace the narrow first-cut layer editor with the FULL-FORM SIDE-BY-SIDE editor the operator approved: when a radio option targets a map layer, the config modal shows the radio option config on the left and the ENTIRE `KineticaWmsLayerForm` (render mode + all style params + opacity + INFO POPUP) on the right, with DATA SOURCE auto-hidden and SPATIAL MODE suppressed via a new additive `hideSpatialMode` prop. The form is seeded from the option's full-config snapshot and every edit captures back a full snapshot via the Plan 01 adapter. Raw JSON stays as a collapsible Advanced fallback; widget/dynamic-view target authoring is unchanged; the modal widens only for layer-target editing.

Purpose: A designer configures a radio option's layer appearance exactly as in Map Layers (SC1). Edits capture a full-config snapshot that overlays the live layer (SC2 — bridged by Plan 01, dispatched by Plan 02). The strict allow-list path (widget/dv) is untouched (SC4).
Output: additive `hideSpatialMode` on `KineticaWmsLayerForm`; rewritten `RadioLayerConfigEditor.tsx` (full form); modified `RadioGroupConfigPanel.tsx` (two-pane + widen + Advanced); widened-modal CSS; extended panel spec. NO server diff.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60.1-radio-config-ux-structured-layer-target-editor-reuse-cbconfigform/60.1-CONTEXT.md
@.planning/phases/60.1-radio-config-ux-structured-layer-target-editor-reuse-cbconfigform/60.1-01-SUMMARY.md

<interfaces>
<!-- Extracted from codebase — use directly. -->

From lib/radioGroupLayerPatch.ts (Plan 01):
  export function snapshotToLayerForm(snapshot): { config: Record<string,unknown>; info: { info_enabled?:number; info_columns?:string|null; info_template?:string|null } };
  export function layerFormToSnapshot(formConfig: Record<string,unknown>, infoPatch?): Record<string,unknown>;  // strips data-binding/meta, folds info_*

KineticaWmsLayerForm props (KineticaWmsLayerForm.tsx 63-89) — the FULL form to embed:
  config: Record<string, unknown>;                 // ONE blob: renderMode + style + cb_config + track_config
  onChange: (config) => void;
  columns?: { name: string; type: string }[];
  isValid?: (valid: boolean) => void;              // classbreak zero-break → false
  infoEnabled?: number; infoColumns?: string|null; infoTemplate?: string|null;
  onChangeInfoConfig?: (patch: { info_enabled?:number; info_columns?:string|null; info_template?:string|null }) => void;
  tableMissing?: boolean;
  // DATA SOURCE auto-hides when layer/onDataSourceChange are ABSENT (renderDataSourcePicker returns null
  //   at line 468: `if (!layer || !onDataSourceChange) return null;`) — so DO NOT pass layer/onDataSourceChange.
  layer?: DashboardLayerDto; associatedTables?: TableDto[]; dynamicViews?: DynamicViewRow[];
  onDataSourceChange?: (patch) => void;
  // NEW (this plan): hideSpatialMode?: boolean — additive, default false.

SPATIAL MODE block to gate (KineticaWmsLayerForm.tsx): the <div className="config-group config-spatial-mode"
  role="radiogroup" aria-labelledby="map-spatial-mode-label"> ... </div> spanning ~lines 546-708 (the SPATIAL
  MODE radios + per-mode latlon/wkt/wkb/track column pickers). The TRACK STYLE block (~755) and CLASSBREAK
  CbConfigForm (~1379) are gated on spatialMode/effectiveRenderMode; with no spatialMode set they behave as
  raster — fine. RENDER MODE (~710-750), raster/heatmap params, ZOOM RANGE (~1472), INFO POPUP (~1510-1596)
  must all STILL render under hideSpatialMode.

LayersModal full-form embed precedent (LayersModal.tsx 548-588) — the exact lift/split pattern to mirror:
  config={{ ...layer.config, cb_config: layer.cb_config, track_config: layer.track_config }}
  onChange={(nextConfig) => { const { cb_config, track_config, ...rest } = nextConfig; onPatch(id, { config: rest, cb_config, track_config }); }}
  infoEnabled / infoColumns / infoTemplate + onChangeInfoConfig={(patch) => onPatch(id, patch)}
  (radio editor differs: NO layer/onDataSourceChange; hideSpatialMode; captures a SNAPSHOT not an onPatch.)

CURRENT RadioLayerConfigEditor props (to REPLACE the body; KEEP the prop surface the panel passes):
  { configPatch, columns, schema, tableName, tableRef, autoSuggestDisabledReason, onChange, onCbValid, idx }
  onChange emits a patch; the panel MERGES it onto option.action.configPatch.

CURRENT RadioGroupConfigPanel.tsx (already read) — OptionRow renders <RadioLayerConfigEditor .../> for
  kind==="layer" (lines 416-437) with a MERGE onChange { ...option.action.configPatch, ...nextPatch }; the
  Advanced <details> JSON block (440-478) for layer targets; widget/dv JSON textarea direct (479-506).
  Main panel destructures tables (533) + fetches dynamicViews (547-556) + cbValidMap + allValid useMemo (574).

global.css: .modal-config { max-width: 520px; } (line ~919). .config-panel-body { max-height:55vh; overflow-y:auto }.
  No :has() usage exists yet in the codebase — prefer an explicit class toggle over :has() for browser safety.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add additive hideSpatialMode prop to KineticaWmsLayerForm (gates SPATIAL MODE block; existing callers unaffected)</name>
  <files>packages/web/src/components/charts/KineticaWmsLayerForm.tsx</files>
  <read_first>
    - packages/web/src/components/charts/KineticaWmsLayerForm.tsx (props 63-89; SPATIAL MODE block ~546-708; RENDER MODE ~710; INFO POPUP ~1510; data-source picker ~467-537)
    - packages/web/src/components/LayersModal.tsx (548-588 — existing caller; confirm it does NOT pass hideSpatialMode so default false keeps it rendering SPATIAL MODE)
    - packages/web/src/components/charts/KineticaWmsLayerForm.spec.tsx (existing render harness + assertions to keep green; the SPATIAL MODE label assertion if any)
  </read_first>
  <action>
    Edit packages/web/src/components/charts/KineticaWmsLayerForm.tsx:
    A. Add to KineticaWmsLayerFormProps (after onDataSourceChange):
         /** When true, omit the SPATIAL MODE radios + spatial-column pickers (Phase 60.1 radio editor).
          *  Additive — default false; existing callers (LayersModal, MapConfigPanel) are unaffected. */
         hideSpatialMode?: boolean;
    B. Destructure it in the component signature with default false: `hideSpatialMode = false,`.
    C. Wrap the SPATIAL MODE <div className="config-group config-spatial-mode" ...> ... </div> block (the
       whole spatial-mode radiogroup spanning the SPATIAL MODE label through the per-mode latlon/wkt/wkb/track
       column pickers, ~lines 546-708) in `{!hideSpatialMode && ( ... )}`. Do NOT touch RENDER MODE, TRACK
       STYLE, raster/heatmap params, CbConfigForm, ZOOM RANGE, or INFO POPUP — all still render.
    D. Add a one-line JSDoc note at the top of the prop interface block documenting hideSpatialMode.
    Run from packages/web: full vitest (existing form spec must stay green — SPATIAL MODE still renders by
    default) + tsc. Commit:
    `feat(60.1-03): KineticaWmsLayerForm hideSpatialMode prop (additive, default false — gates SPATIAL MODE block)`.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/KineticaWmsLayerForm.spec.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "hideSpatialMode\?: boolean" packages/web/src/components/charts/KineticaWmsLayerForm.tsx` matches (prop declared).
    - `grep -E "hideSpatialMode = false" packages/web/src/components/charts/KineticaWmsLayerForm.tsx` matches (default false — additive).
    - `grep -E "!hideSpatialMode && \(" packages/web/src/components/charts/KineticaWmsLayerForm.tsx` matches (SPATIAL MODE block gated).
    - `grep -c "config-spatial-mode" packages/web/src/components/charts/KineticaWmsLayerForm.tsx` shows the spatial-mode block still present (wrapped, not deleted).
    - `git diff packages/web/src/components/LayersModal.tsx` is EMPTY (existing caller untouched — default false keeps SPATIAL MODE).
    - `cd packages/web && npx vitest run src/components/charts/KineticaWmsLayerForm.spec.tsx` exits 0 (SPATIAL MODE still renders by default).
    - `cd packages/web && npx tsc --noEmit -p tsconfig.json` exits 0.
    - A git commit exists with subject starting `feat(60.1-03): KineticaWmsLayerForm hideSpatialMode`.
  </acceptance_criteria>
  <done>KineticaWmsLayerForm has an additive hideSpatialMode prop (default false) that gates the SPATIAL MODE block; existing callers unaffected; form spec green; web tsc clean; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace RadioLayerConfigEditor body with the FULL KineticaWmsLayerForm (hideSpatialMode, data-source suppressed, info wired) via the snapshot adapter</name>
  <files>packages/web/src/components/charts/RadioLayerConfigEditor.tsx</files>
  <read_first>
    - packages/web/src/components/charts/RadioLayerConfigEditor.tsx (FULL — the narrow body to REPLACE; KEEP the prop surface the panel passes: configPatch/columns/schema/tableName/tableRef/autoSuggestDisabledReason/onChange/onCbValid/idx)
    - packages/web/src/components/charts/KineticaWmsLayerForm.tsx (props 63-89 + hideSpatialMode from Task 1; the config blob + onChange + info props contract)
    - packages/web/src/components/LayersModal.tsx (548-588 — the cb_config/track_config lift/split + info wiring precedent to mirror)
    - packages/web/src/lib/radioGroupLayerPatch.ts (Plan 01 — snapshotToLayerForm / layerFormToSnapshot)
    - .planning/phases/60.1-.../60.1-CONTEXT.md (lines 34-47 — full-form reuse + snapshot rules; the infinite-render pitfall warning)
  </read_first>
  <action>
    REWRITE packages/web/src/components/charts/RadioLayerConfigEditor.tsx. Remove the render-mode <select> +
    standalone CbConfigForm. Keep the SAME prop signature (configPatch, columns, schema, tableName, tableRef,
    autoSuggestDisabledReason, onChange, onCbValid, idx). New body:

      import KineticaWmsLayerForm from "./KineticaWmsLayerForm";
      import { snapshotToLayerForm, layerFormToSnapshot } from "../../lib/radioGroupLayerPatch";

      // 1. Seed the full form from the option's snapshot:
      const { config: formConfig, info } = snapshotToLayerForm(configPatch);
      //    snapshotToLayerForm already lifts cb_config/track_config INTO formConfig and surfaces info separately.

      // 2. Render the FULL form. Wrap in a div with data-testid `radio-layer-form-${idx}` + className
      //    "radiogroup-layer-form". Pass:
      <KineticaWmsLayerForm
        config={formConfig}
        columns={columns}
        onChange={(nextFormConfig) => onChange(layerFormToSnapshot(nextFormConfig, info))}
        isValid={onCbValid}
        infoEnabled={(info.info_enabled as number | undefined) ?? 1}
        infoColumns={(info.info_columns as string | null | undefined) ?? null}
        infoTemplate={(info.info_template as string | null | undefined) ?? null}
        onChangeInfoConfig={(patch) => onChange(layerFormToSnapshot(formConfig, { ...info, ...patch }))}
        hideSpatialMode={true}
        // DATA SOURCE auto-hides: deliberately DO NOT pass layer / onDataSourceChange / associatedTables.
        // schema/tableName/tableRef for CbConfigForm auto-suggest ride through config (KineticaWmsLayerForm
        // reads tableRef off config.tableRef ~line 1385; set config.tableRef if needed) — pass tableRef by
        // injecting it into formConfig if the form requires it; otherwise the cbAutoSuggestTarget resolves
        // from columns. If tableRef is needed by CbConfigForm, set formConfig.tableRef = tableRef before render.
      />

    CRITICAL — avoid the infinite-render pitfall (CONTEXT test-gate note + the first-cut deviation): the
    form's onChange / onChangeInfoConfig / isValid are event-driven (fired from user interaction or the form's
    own useEffect), NOT synchronously in render. Do NOT call onChange/onCbValid in the editor's render body.
    Compute formConfig + info once per render from props (pure). The form owns its effects. Mirror LayersModal,
    which embeds the same form with no loop.

    CONTEXT (LOCKED): the snapshot must NEVER carry data-binding/spatial keys — layerFormToSnapshot (Plan 01)
    already strips them, so even though the full form CAN edit spatial columns, hideSpatialMode hides those
    inputs AND the adapter strips any that slip through. Route EVERY write through layerFormToSnapshot.

    Update the file header comment to describe the full-form side-by-side approach (replaces the narrow doc).
    Follow theme tokens (config-group-label green accent, var(--border-color), no hardcoded colors) per
    [[ui-consistency-conventions]]. Run from packages/web: tsc (Task 4 adds asserting tests). Commit:
    `feat(60.1-03): RadioLayerConfigEditor — embed full KineticaWmsLayerForm (hideSpatialMode, info wired) via snapshot adapter`.
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep "KineticaWmsLayerForm" packages/web/src/components/charts/RadioLayerConfigEditor.tsx` matches (full form embedded).
    - `grep -E "hideSpatialMode=\{true\}|hideSpatialMode" packages/web/src/components/charts/RadioLayerConfigEditor.tsx` matches.
    - `grep -E "snapshotToLayerForm|layerFormToSnapshot" packages/web/src/components/charts/RadioLayerConfigEditor.tsx` matches BOTH adapter functions.
    - `grep -E "onChangeInfoConfig|infoEnabled|infoColumns|infoTemplate" packages/web/src/components/charts/RadioLayerConfigEditor.tsx` matches (INFO POPUP wired).
    - `grep -E "onDataSourceChange|associatedTables|layer=\{" packages/web/src/components/charts/RadioLayerConfigEditor.tsx` returns NOTHING (DATA SOURCE deliberately suppressed by NOT passing those).
    - `grep -E "radio-layer-form" packages/web/src/components/charts/RadioLayerConfigEditor.tsx` matches (data-testid present for the form wrapper).
    - The narrow content is gone: `grep -E "radio-layer-rendermode|<CbConfigForm" packages/web/src/components/charts/RadioLayerConfigEditor.tsx` returns NOTHING (replaced by the full form which embeds CbConfigForm internally).
    - `cd packages/web && npx tsc --noEmit -p tsconfig.json` exits 0.
    - A git commit exists with subject starting `feat(60.1-03): RadioLayerConfigEditor`.
  </acceptance_criteria>
  <done>RadioLayerConfigEditor embeds the full KineticaWmsLayerForm with hideSpatialMode, data-source suppressed, info-popup wired, seeded via snapshotToLayerForm and writing via layerFormToSnapshot; narrow select+standalone-CbConfigForm removed; web tsc clean; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Two-pane side-by-side layout in RadioGroupConfigPanel + widen modal for layer-target editing only; keep Advanced JSON + widget/dv unchanged; add CSS</name>
  <files>packages/web/src/components/charts/RadioGroupConfigPanel.tsx, packages/web/src/styles/global.css</files>
  <read_first>
    - packages/web/src/components/charts/RadioGroupConfigPanel.tsx (FULL — OptionRow render 270-521; the kind==="layer" RadioLayerConfigEditor mount 416-437; Advanced <details> 440-478; widget/dv textarea 479-506; main panel root <div className="config-group"> 637)
    - packages/web/src/components/DashboardsPage.tsx (1170-1192 — the .modal-content.modal-config shell wrapping ChartConfigPanel; the panel cannot reach it directly — use a class the panel sets that CSS keys off, see action)
    - packages/web/src/styles/global.css (~919 .modal-config max-width:520px; .config-panel-body; .config-group)
    - packages/web/src/components/LayersModal.tsx (350-490 — layers-modal-body / layers-modal-left / layers-modal-right split layout precedent + its CSS class names)
    - .planning/memory/ui-consistency-conventions.md (theme tokens; operator catches deviations)
  </read_first>
  <action>
    A. OptionRow two-pane layout (kind === "layer" ONLY): wrap the existing left content (label, target
       picker, Capture button, validation reasons, Advanced JSON <details>) and the RadioLayerConfigEditor in
       a side-by-side container ONLY when kind === "layer":
         <div className="radiogroup-layer-editor">
           <div className="radiogroup-layer-editor-left"> {label, target, capture, reasons, Advanced JSON} </div>
           <div className="radiogroup-layer-editor-right"> <RadioLayerConfigEditor .../> </div>
         </div>
       For kind === "widget" || "dynamicView": render the CURRENT single-column layout EXACTLY as today
       (no two-pane wrapper, JSON textarea direct, no RadioLayerConfigEditor). Keep the MERGE onChange
       ({ ...option.action.configPatch, ...nextPatch }) for the editor, the Advanced <details> for layer
       targets, the cbValid fold, and all existing validation/orphan logic intact. The RadioLayerConfigEditor
       moves into the RIGHT pane but its props are UNCHANGED.

    B. Widen the modal ONLY for layer-target editing. The panel root cannot reach .modal-config, so signal via
       a class the modal CSS targets. Implement: the main panel computes whether ANY option's target is a
       layer (`const hasLayerOption = cfg.options.some(o => o.action.target.kind === "layer");`) and adds a
       marker class to its OWN root div: `className={`config-group${hasLayerOption ? " radiogroup-has-layer-editor" : ""}`}`.
       Then in global.css (Task 3C) widen the enclosing .modal-config WHEN it contains that marker, using a
       descendant/ancestor selector that does NOT rely on :has() (none exists in the codebase). Concretely:
       add the marker class to the panel root AND set the wide layout entirely WITHIN the panel subtree
       (the two-pane .radiogroup-layer-editor + a min-width on .radiogroup-layer-editor-right), and OVERRIDE
       .modal-config max-width by scoping a rule `.modal-config:has(.radiogroup-has-layer-editor)` IF the
       project's browser target supports :has() — OTHERWISE (preferred, no :has() in codebase) widen via a
       wrapper: have RadioGroupConfigPanel render its root with the marker class AND rely on the two-pane
       grid to expand within the existing modal, bumping .modal-config min/max-width through a plain
       descendant selector is NOT possible (parent styling from a child) — so use the documented fallback:
       add `.modal-config { max-width: min(960px, 92vw); }` GATED behind the marker by emitting the marker on
       a parent the panel controls is impossible; INSTEAD widen unconditionally-safe: scope the wider width to
       `.modal-config:has(.radiogroup-has-layer-editor)` and ALSO add a non-:has() safety net
       `.radiogroup-has-layer-editor { width: 100%; }` so the two-pane grid fills available space. Pick ONE
       working mechanism and verify it compiles; the REQUIREMENT is: widget/dv-only radio config keeps the
       520px width, layer-target radio config widens. (Claude's discretion on the exact selector; document the
       choice in a CSS comment. :has() is supported in all evergreen browsers as of 2024 — acceptable here.)

    C. Run from packages/web: tsc (Task 4 adds asserting tests + full suite). Commit:
    `feat(60.1-03): two-pane side-by-side layer editor + widen modal for layer targets (widget/dv unchanged)`.

    Add the CSS in packages/web/src/styles/global.css (after .modal-config ~919):
      /* Phase 60.1: radio config widens to a two-pane layout ONLY when a layer-target option is edited. */
      .modal-config:has(.radiogroup-has-layer-editor) { max-width: min(960px, 92vw); }
      .radiogroup-layer-editor { display: grid; grid-template-columns: minmax(0, 1fr) minmax(0, 1.4fr); gap: 16px; align-items: start; }
      .radiogroup-layer-editor-right { border-left: 1px solid var(--border); padding-left: 16px; min-width: 0; }
      @media (max-width: 720px) { .radiogroup-layer-editor { grid-template-columns: 1fr; } .radiogroup-layer-editor-right { border-left: none; padding-left: 0; } }
    (Use theme tokens — var(--border) — no hardcoded colors. If :has() is rejected by the project's CSS
    lint/build, fall back to the marker-class width on .radiogroup-has-layer-editor + a wider modal default
    only when that class is present via a documented alternative; verify the build passes either way.)
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "radiogroup-layer-editor" packages/web/src/components/charts/RadioGroupConfigPanel.tsx` matches (two-pane wrapper for layer targets).
    - `grep -E "radiogroup-has-layer-editor|hasLayerOption" packages/web/src/components/charts/RadioGroupConfigPanel.tsx` matches (marker class / layer-option detection for the widen toggle).
    - `grep -E "kind === \"layer\"" packages/web/src/components/charts/RadioGroupConfigPanel.tsx` matches the conditional guarding the two-pane layout (widget/dv keep single column).
    - `grep "RadioLayerConfigEditor" packages/web/src/components/charts/RadioGroupConfigPanel.tsx` matches AND it sits inside the right pane.
    - `grep "<details" packages/web/src/components/charts/RadioGroupConfigPanel.tsx` STILL matches (Advanced JSON fallback kept for layer targets).
    - global.css contains `.radiogroup-layer-editor` and a widened .modal-config rule scoped to the marker class (`grep -E "radiogroup-layer-editor|radiogroup-has-layer-editor" packages/web/src/styles/global.css` matches) and uses var(--border) (no hardcoded hex in the new block).
    - `cd packages/web && npx tsc --noEmit -p tsconfig.json` exits 0.
    - A git commit exists with subject starting `feat(60.1-03): two-pane`.
  </acceptance_criteria>
  <done>Layer-target options render side-by-side (radio config left, full form right); the modal widens only for layer-target editing; Advanced JSON fallback + widget/dv single-column authoring unchanged; CSS uses theme tokens; web tsc clean; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Rewrite RadioGroupConfigPanel.spec.tsx layer-editor tests for the full-form side-by-side; full suite 100% green + web tsc clean</name>
  <files>packages/web/src/components/charts/RadioGroupConfigPanel.spec.tsx</files>
  <read_first>
    - packages/web/src/components/charts/RadioGroupConfigPanel.spec.tsx (FULL — the existing "structured layer editor (60.1)" describe block from the first cut: it asserts radio-layer-rendermode / radio-layer-cbform testids + the CbConfigForm mock; THESE ASSERTIONS NO LONGER HOLD — the editor now embeds the full form. Rewrite them. KEEP the unrelated existing tests: target picker, capture, widget/dv unchanged, isValid plumbing harness, the CbConfigForm-mock-via-useEffect pattern to avoid the infinite-render loop)
    - packages/web/src/components/charts/RadioLayerConfigEditor.tsx (Task 2 — data-testid radio-layer-form-${idx}; full form embed)
    - packages/web/src/components/charts/KineticaWmsLayerForm.tsx (a stable selector to assert the form rendered — e.g. the RENDER MODE label "RENDER MODE" / id="map-render-mode-label"; the INFO POPUP label id="map-info-popup-label"; the SPATIAL MODE label id="map-spatial-mode-label" which must be ABSENT under hideSpatialMode; the DATA SOURCE label "DATA SOURCE" absent without layer/onDataSourceChange)
    - packages/web/src/lib/radioGroupLayerPatch.ts (snapshotToLayerForm/layerFormToSnapshot — for asserting the emitted snapshot shape)
  </read_first>
  <action>
    Rewrite the "structured layer editor (60.1)" describe block in RadioGroupConfigPanel.spec.tsx to match the
    full-form side-by-side editor. Decide on mocking: KineticaWmsLayerForm is heavy (CodeMirror, stores). Mock
    it with an async `vi.mock("./KineticaWmsLayerForm", ...)` factory that renders stable testable markers and
    fires callbacks via useEffect (NOT synchronously in render — mirrors the first-cut fix that resolved the
    infinite-render loop). The mock should render, e.g.:
      - a region with text "RENDER MODE" and id="map-render-mode-label" (so the test can assert the form shows)
      - a button/control that calls props.onChange({ ...props.config, renderMode: "classbreak" }) on click
      - a control that calls props.onChangeInfoConfig({ info_enabled: 0 }) on click
      - NO "SPATIAL MODE" and NO "DATA SOURCE" markers (proving suppression) — i.e. the mock asserts those are
        not passed: it can render a "SPATIAL MODE" marker IFF props.hideSpatialMode !== true, and a
        "DATA SOURCE" marker IFF props.layer && props.onDataSourceChange. The test then asserts those markers
        are ABSENT.
    Tests (new/rewritten):
      1. "layer target renders the full form side-by-side": render one layer-target option (table-bound, with
         a `tables` prop so columns resolve). Assert getByTestId(`radio-layer-form-0`) exists; assert the
         RENDER MODE marker present; assert SPATIAL MODE marker ABSENT (hideSpatialMode=true) and DATA SOURCE
         marker ABSENT (no layer/onDataSourceChange); assert the two-pane wrapper `.radiogroup-layer-editor`
         is in the DOM (document.querySelector).
      2. "editing render mode updates the snapshot (no data-binding keys)": click the mock's render-mode
         control; assert the panel's outer onChange received an option whose action.configPatch has
         renderMode (and is a FLAT snapshot — cb_config/track_config/info_* at top level, style keys flat),
         and contains NO table_id/dynamic_view_id/spatialMode/latColumn key.
      3. "editing INFO POPUP updates the snapshot's top-level info_enabled": click the mock's info control;
         assert the emitted configPatch has top-level info_enabled === 0 (the snapshot carries info_* at top
         level, surfaced through layerFormToSnapshot).
      4. "Advanced JSON still present for layer targets": assert the <details.radio-advanced-json> + the raw
         JSON textarea round-trip still work (reuse the first-cut Test 4 pattern — valid JSON updates patch,
         invalid shows json-error and does NOT call onChange).
      5. "non-surfaced key survives a structured write (MERGE)": option configPatch already has
         { renderMode:"classbreak", track_config:"keepme" }; drive a render-mode change via the mock; assert
         the emitted configPatch STILL contains track_config (or that the snapshot preserves it — given the
         editor seeds from snapshotToLayerForm which lifts track_config into the form config, and
         layerFormToSnapshot writes it back top-level, the round-trip preserves it).
      6. "widget + dynamicView targets unchanged": assert NO radio-layer-form testid, JSON textarea rendered
         directly (no Advanced disclosure, no two-pane wrapper) — reuse the first-cut Test 7/7b.
    Remove the now-invalid radio-layer-rendermode / radio-layer-cbform assertions (those testids no longer
    exist). Keep cleanup() between multi-render tests + widgetActionStore reset if used. Run the FULL frontend
    suite from packages/web (cwd matters): `cd packages/web && npx vitest run`. MUST be 100% green. Then tsc.
    Commit:
    `test(60.1-03): full-form side-by-side layer editor panel specs (RENDER MODE present, SPATIAL/DATA SOURCE absent, snapshot writes, Advanced JSON, widget/dv unchanged)`.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "radio-layer-form" packages/web/src/components/charts/RadioGroupConfigPanel.spec.tsx` matches (asserts the full-form wrapper).
    - The spec asserts SPATIAL MODE + DATA SOURCE are ABSENT in the layer editor (`grep -iE "SPATIAL MODE|DATA SOURCE" packages/web/src/components/charts/RadioGroupConfigPanel.spec.tsx` matches absence assertions) and RENDER MODE present.
    - The spec asserts an edit produces a FLAT snapshot with NO data-binding key (`grep -E "table_id|dynamic_view_id|spatialMode" packages/web/src/components/charts/RadioGroupConfigPanel.spec.tsx` matches an assertion that these are absent from the emitted configPatch).
    - The spec asserts an INFO POPUP edit sets top-level info_enabled in the snapshot (`grep "info_enabled" packages/web/src/components/charts/RadioGroupConfigPanel.spec.tsx` matches).
    - The spec asserts Advanced JSON present + round-trips for layer targets, and widget/dv targets render the JSON textarea directly (unchanged).
    - The obsolete narrow assertions are gone: `grep -E "radio-layer-rendermode|radio-layer-cbform" packages/web/src/components/charts/RadioGroupConfigPanel.spec.tsx` returns NOTHING.
    - `cd packages/web && npx vitest run` exits 0 with 0 failures (run from packages/web — NOT --root from repo root).
    - `cd packages/web && npx tsc --noEmit -p tsconfig.json` exits 0.
    - `git diff --name-only` shows NO files under packages/server (frontend-only).
    - A git commit exists with subject starting `test(60.1-03):`.
  </acceptance_criteria>
  <done>Panel spec proves: layer target → full KineticaWmsLayerForm side-by-side (RENDER MODE present, SPATIAL MODE + DATA SOURCE absent); render-mode + info-popup edits write a flat full-config snapshot with no data-binding keys; non-surfaced keys survive; Advanced JSON kept; widget/dv unchanged. Full frontend vitest 100% green from packages/web; web tsc clean; zero server diff.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx vitest run` exits 0, 0 failures. MUST be run from packages/web (static-assertion specs use process.cwd()).
- `cd packages/web && npx tsc --noEmit -p tsconfig.json` exits 0.
- `git diff --name-only` shows NO packages/server files — frontend-only.
- Layer target → full KineticaWmsLayerForm side-by-side (RENDER MODE + style + INFO POPUP); SPATIAL MODE + DATA SOURCE absent; modal widens for layer targets only.
- Edits capture a full-config snapshot with no data-binding/spatial keys; Advanced JSON fallback kept; widget/dv unchanged; LayersModal/MapConfigPanel callers of KineticaWmsLayerForm unaffected.
</verification>

<success_criteria>
- SC1: layer-target option renders the full KineticaWmsLayerForm side-by-side (render mode + all mode/style params + opacity + INFO POPUP), seeded from the snapshot; DATA SOURCE + SPATIAL MODE absent; no raw JSON needed to author layer appearance.
- SC2: edits capture a full-config snapshot (config blob + top-level cb_config/track_config + top-level info_*) via the Plan 01 adapter; data-binding/spatial keys never in the snapshot.
- SC4: narrow first-cut editor replaced; Advanced JSON collapsible fallback kept; widget/dynamic-view targets unchanged; modal widens only for layer-target editing; frontend vitest 100%; web tsc clean; frontend-only.
</success_criteria>

<output>
After completion, create `.planning/phases/60.1-radio-config-ux-structured-layer-target-editor-reuse-cbconfigform/60.1-03-SUMMARY.md`
</output>
