---
phase: 60.1-radio-config-ux-structured-layer-target-editor-reuse-cbconfigform
plan: "02"
type: execute
wave: 2
depends_on: ["60.1-01"]
files_modified:
  - packages/web/src/lib/applyWidgetAction.ts
  - packages/web/src/lib/applyWidgetAction.spec.ts
  - packages/web/src/lib/radioGroupConfig.ts
  - packages/web/src/lib/radioGroupConfig.spec.ts
  - packages/web/src/components/charts/MapChartRenderer.spec.tsx
autonomous: true
requirements: [RADIOUX-V111-01]
must_haves:
  truths:
    - "applyWidgetAction routes a LAYER snapshot patch through validateLayerSnapshot (denylist) — a snapshot carrying render/style/cb_config/track_config/info_* is ACCEPTED and overlaid; a snapshot carrying table_id/dynamic_view_id/spatialMode is REJECTED with a typed { status:'rejected' } result; widget + dynamicView dispatch still use validateActionPatch unchanged (SC2)."
    - "A layer snapshot overlay is stored DTO-shaped { config:{renderMode,...style}, cb_config, track_config, info_enabled, info_columns, info_template } so MapChartRenderer.effectiveLayers deep-merges it correctly with no engine-merge change (SC2/SC3)."
    - "validateRadioOption validates a LAYER-target option via validateLayerSnapshot (snapshot mode) so a full-appearance option saves; widget/dv options still validate via validateActionPatch (SC2)."
    - "An overlaid top-level info_enabled:0 drops the layer from MapChartRenderer.eligibleLayers LIVE (read-live), and an overlaid info_columns reaches the info path — proven by a mounted-renderer test driving widgetActionStore (SC3, GAP-61 read-live class)."
  artifacts:
    - path: "packages/web/src/lib/applyWidgetAction.ts"
      provides: "Layer-snapshot validation + DTO-shaped overlay split routed through validateLayerSnapshot; widget/dv path unchanged"
      contains: "validateLayerSnapshot"
    - path: "packages/web/src/components/charts/MapChartRenderer.spec.tsx"
      provides: "Live-read test: overlaid info_enabled:0 removes layer from eligibleLayers via widgetActionStore"
      contains: "info_enabled"
  key_links:
    - from: "lib/applyWidgetAction.ts (target.kind === 'layer')"
      to: "lib/actionAllowList.ts validateLayerSnapshot"
      via: "snapshot patches validated by the denylist; build DTO-shaped overlay (top-level cb_config/track_config/info_*, nested config)"
      pattern: "validateLayerSnapshot"
    - from: "lib/radioGroupConfig.ts validateRadioOption (layer target)"
      to: "lib/actionAllowList.ts validateLayerSnapshot"
      via: "layer-target option validity uses the snapshot denylist; widget/dv keep validateActionPatch"
      pattern: "validateLayerSnapshot"
    - from: "components/charts/MapChartRenderer.tsx effectiveLayers → eligibleLayers (info_enabled !== 0)"
      to: "store/widgetActionStore layerOverrides"
      via: "overlaid info_enabled:0 drops layer from eligibleLayers live"
      pattern: "eligibleLayers|info_enabled"
---

<objective>
Route the layer-appearance SNAPSHOT through the new denylist at both the dispatch boundary (`applyWidgetAction`) and the save-time validity check (`radioGroupConfig.validateRadioOption`), and prove with a mounted-renderer test that an overlaid top-level `info_enabled`/`info_columns` takes effect LIVE on the map. The strict `validateActionPatch`/`LAYER_ALLOW_LIST` stays the contract for widget + dynamic-view targets and the future AI/MCP path.

Purpose: A full-config snapshot must (a) save (validateRadioOption accepts it) and (b) dispatch + overlay correctly (applyWidgetAction accepts it and stores a DTO-shaped overlay so effectiveLayers merges it). The info_* read-live test closes the GAP-61 read-once-at-mount risk for the newly-overlaid info-popup fields.
Output: modified `applyWidgetAction.ts` (+ spec), `radioGroupConfig.ts` (+ spec), and an extended `MapChartRenderer.spec.tsx` live-read test. NO server diff.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60.1-radio-config-ux-structured-layer-target-editor-reuse-cbconfigform/60.1-CONTEXT.md
@.planning/phases/60.1-radio-config-ux-structured-layer-target-editor-reuse-cbconfigform/60.1-01-SUMMARY.md

<interfaces>
<!-- Extracted from codebase — use directly. -->

From lib/actionAllowList.ts (Plan 01 + existing):
  export function validateLayerSnapshot(patch): {valid:true}|{valid:false;reasons:string[]};   // NEW (Plan 01)
  export const DATA_BINDING_KEYS: readonly string[];                                            // NEW (Plan 01)
  export function validateActionPatch(kind, widgetType, configPatch): ...;                      // UNCHANGED (widget/dv/AI)
  export function splitLayerPatch(configPatch): { config?; topLevel };                          // UNCHANGED (5-key strict split)

From lib/applyWidgetAction.ts (CURRENT — to modify; full file already read):
  - target.kind === "layer" path (lines 124-128) resolves the layer, then Step 2 validates via
    validateActionPatch(target.kind, widgetType, configPatch) (line 137), then Step 3 builds the overlay
    via splitLayerPatch(configPatch) (line 162) into { ...split.topLevel, config: split.config }.
  - widget / dynamicView paths use validateActionPatch + store configPatch directly. KEEP THOSE.

  THE CHANGE (layer path only): for a layer target, replace validateActionPatch with validateLayerSnapshot,
  and build the overlay with a SNAPSHOT-AWARE split (splitLayerPatch only knows the 5 strict keys; a full
  snapshot has arbitrary style keys). New split rule (consistent with effectiveLayers + radioGroupLayerPatch):
    top-level overlay fields: cb_config, track_config, info_enabled, info_columns, info_template
    everything else (renderMode + all style keys)  →  nested under `config`
  Produce: { config: {<style>}, cb_config?, track_config?, info_enabled?, info_columns?, info_template? }

From store/widgetActionStore.ts:
  - contributions[controlId] = { widget:{}, layer:{}, dynamicView:{} }; setControlContribution(controlId, contrib)
  - deriveOverlays builds layerOverrides[layerId] from contributions; { config, ...topLevel } shape.
  - MapChartRenderer reads layerOverrides and merges via effectiveLayers (config deep-merge + top-level spread).

From components/charts/MapChartRenderer.tsx (already read):
  - effectiveLayers (~482-498): const { config: cfgPatch, ...topLevel } = ov; merged = { ...l, ...topLevel, config: {...l.config, ...cfgPatch} }.
  - includedLayers (~529) derives from effectiveLayers.
  - eligibleLayers (~678-680): includedLayers.filter(layer => layer.info_enabled !== 0).
  → an overlaid TOP-LEVEL info_enabled:0 makes effectiveLayers carry info_enabled:0 → eligibleLayers drops it. LIVE.

From components/charts/MapChartRenderer.spec.tsx (already read):
  - Line 22-23: uses the REAL widgetActionStore (NOT mocked) for GAP-61 overlay tests.
  - "POPUP-V14 — info popup integration (Phase 21)" describe (~1768) with a latlon info_enabled:1 layer helper
    (~1818) + the P5 "info_enabled=0 layer excluded → not queried" test (~2027). MIRROR P5's assertions but
    drive info_enabled:0 via a widgetActionStore layer overlay instead of a baseline layer field.

From lib/radioGroupConfig.ts (already read):
  - validateRadioOption(option, widgetType?) → calls validateActionPatch(kind, widgetType, configPatch) for ALL kinds.
  - isRadioGroupConfigValid(config, widgetTypeFor) loops options → validateRadioOption.
  THE CHANGE: in validateRadioOption, when option.action.target.kind === "layer", validate via
  validateLayerSnapshot(configPatch) instead of validateActionPatch; keep the empty-patch rule; widget/dv unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route layer dispatch through validateLayerSnapshot + snapshot-aware DTO overlay split in applyWidgetAction (widget/dv unchanged)</name>
  <files>packages/web/src/lib/applyWidgetAction.ts, packages/web/src/lib/applyWidgetAction.spec.ts</files>
  <read_first>
    - packages/web/src/lib/applyWidgetAction.ts (FULL — Step 1 resolve, Step 2 validate ~137, Step 3 splitLayerPatch ~162, Step 5 setControlContribution; keep idempotency + switch-replace + TRANSIENT-ONLY + no-filter invariant comments)
    - packages/web/src/lib/applyWidgetAction.spec.ts (existing layer/widget/dv dispatch + rejection + idempotency tests — KEEP all; mirror their harness for the new cases)
    - packages/web/src/lib/actionAllowList.ts (validateLayerSnapshot + DATA_BINDING_KEYS from Plan 01; validateActionPatch + splitLayerPatch unchanged)
    - packages/web/src/store/widgetActionStore.ts (deriveOverlays + the DTO-shaped layer overlay { config, ...topLevel } it expects)
    - packages/web/src/components/charts/MapChartRenderer.tsx (~482-498 — the effectiveLayers merge the overlay must match)
  </read_first>
  <behavior>
    applyWidgetAction, target.kind === "layer":
      - Test: a full snapshot { renderMode:"classbreak", colormap:"viridis", cb_config:"{}", info_enabled:0 }
        dispatched against a present layer → result.status === "applied"; the stored contribution's layer
        overlay is DTO-shaped: overlay.config has renderMode+colormap, overlay.cb_config==="{}", overlay.info_enabled===0
        (top-level), and overlay has NO top-level renderMode/colormap (those are nested in config).
      - Test: a snapshot carrying a data-binding key { renderMode:"raster", table_id: 9 } → result.status === "rejected",
        reasons mentions table_id; NO contribution written (store unchanged).
      - Test: a snapshot { spatialMode:"latlon" } → rejected (spatial key blocked).
      - Test: idempotency — dispatching the SAME snapshot twice writes once (second call returns "applied" with no store change), reusing the existing fingerprint guard.
    widget / dynamicView dispatch (regression — UNCHANGED):
      - Test: widget map { show_popup:true } still validates via validateActionPatch + applies (existing behavior).
      - Test: a widget patch with an out-of-list key still rejects via validateActionPatch (existing strict path intact).
  </behavior>
  <action>
    Modify packages/web/src/lib/applyWidgetAction.ts. In the layer branch ONLY:

    A. Step 2 validation — route layer through the denylist, keep widget/dv on the strict path:
       Replace the single `validateActionPatch(target.kind, widgetType, configPatch)` call so that:
         const validation = target.kind === "layer"
           ? validateLayerSnapshot(configPatch)
           : validateActionPatch(target.kind, widgetType, configPatch);
       Import validateLayerSnapshot from "./actionAllowList" alongside the existing imports. Keep the
       existing rejected-result handling (toast + console.warn + { status:"rejected", target, reasons }).

    B. Step 3 overlay build — snapshot-aware split for layer (splitLayerPatch only knows the 5 strict keys;
       a snapshot has arbitrary style keys, so do NOT use splitLayerPatch for the snapshot). Add a local
       helper (or inline) that mirrors effectiveLayers + radioGroupLayerPatch:
         const LAYER_TOP_LEVEL = ["cb_config","track_config","info_enabled","info_columns","info_template"];
         function splitLayerSnapshot(patch) {
           const config = {}; const topLevel = {};
           for (const k of Object.keys(patch)) {
             if (LAYER_TOP_LEVEL.includes(k)) topLevel[k] = patch[k];
             else config[k] = patch[k];
           }
           return { config: Object.keys(config).length ? config : undefined, topLevel };
         }
       Then build newTargetPatch (DTO-shaped, same as today's structure):
         const split = splitLayerSnapshot(configPatch);
         newTargetPatch = { ...split.topLevel, ...(split.config ? { config: split.config } : {}) };
       (This replaces the splitLayerPatch call in the layer branch. Leave widget/dynamicView's
       `newTargetPatch = configPatch` exactly as-is.)
       NOTE: post-validation the snapshot has NO data-binding/meta keys (validateLayerSnapshot rejected them),
       so config holds only render/style keys. This DTO shape is exactly what effectiveLayers deep-merges.

    Keep splitLayerPatch imported ONLY if still used elsewhere; if the layer branch no longer calls it and
    nothing else does, remove the now-unused import to keep tsc clean (verify with tsc). Preserve the
    TRANSIENT-ONLY, ACTION-ENGINE-NO-FILTER, switch-replace, and idempotency-fingerprint logic verbatim.

    Add the <behavior> tests to packages/web/src/lib/applyWidgetAction.spec.ts in a new describe block
    "applyWidgetAction — layer snapshot (60.1)". Use the existing harness (reset widgetActionStore between
    tests; a layers lookup with one layer). KEEP all existing tests. Run from packages/web (see <verify>).
    Commit: `feat(60.1-02): route layer dispatch through validateLayerSnapshot + snapshot-aware DTO overlay (widget/dv unchanged)`.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/lib/applyWidgetAction.spec.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep "validateLayerSnapshot" packages/web/src/lib/applyWidgetAction.ts` matches (imported + used in the layer branch).
    - `grep -E "target.kind === \"layer\"" packages/web/src/lib/applyWidgetAction.ts` matches the conditional that selects validateLayerSnapshot.
    - `grep -E "info_enabled|cb_config|track_config" packages/web/src/lib/applyWidgetAction.ts` matches the top-level field list of the snapshot split.
    - applyWidgetAction.spec.ts contains a "layer snapshot" describe block asserting: a full snapshot applies with a DTO-shaped overlay (nested config + top-level cb_config/info_enabled), a data-binding-key snapshot is rejected, and widget/dv strict-path tests are unchanged.
    - `grep "validateActionPatch" packages/web/src/lib/applyWidgetAction.ts` STILL matches (widget/dv path preserved).
    - `cd packages/web && npx vitest run src/lib/applyWidgetAction.spec.ts` exits 0.
    - `cd packages/web && npx tsc --noEmit -p tsconfig.json` exits 0.
    - A git commit exists with subject starting `feat(60.1-02): route layer dispatch`.
  </acceptance_criteria>
  <done>Layer dispatch validates via validateLayerSnapshot + stores a DTO-shaped overlay via the snapshot-aware split; widget/dv strict path unchanged; idempotency/switch-replace/no-filter invariants preserved; spec green; web tsc clean; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: validateRadioOption uses validateLayerSnapshot for LAYER targets (widget/dv keep validateActionPatch)</name>
  <files>packages/web/src/lib/radioGroupConfig.ts, packages/web/src/lib/radioGroupConfig.spec.ts</files>
  <read_first>
    - packages/web/src/lib/radioGroupConfig.ts (FULL — validateRadioOption ~91 + isRadioGroupConfigValid ~133; the empty-patch rule + delegate-to-validateActionPatch logic)
    - packages/web/src/lib/radioGroupConfig.spec.ts (existing validateRadioOption/isRadioGroupConfigValid tests — KEEP all; mirror harness)
    - packages/web/src/lib/actionAllowList.ts (validateLayerSnapshot from Plan 01)
  </read_first>
  <behavior>
    validateRadioOption:
      - Test: a LAYER-target option whose configPatch is a full snapshot { renderMode:"classbreak", colormap:"viridis", cb_config:"{}", info_enabled:1 } → { valid:true } (snapshot accepted via denylist; previously validateActionPatch would have rejected colormap/info_enabled as out-of-list).
      - Test: a LAYER-target option whose configPatch carries table_id (data-binding) → { valid:false }, reasons mentions table_id.
      - Test: a LAYER-target option with EMPTY configPatch {} → { valid:false } (empty-patch rule still applies — keep it).
      - Test: a WIDGET-target option { kind:"widget", configPatch:{ show_popup:true } } with widgetType "map" → { valid:true } via validateActionPatch (UNCHANGED).
      - Test: a WIDGET-target option with an out-of-list key → { valid:false } via validateActionPatch (strict path UNCHANGED).
    isRadioGroupConfigValid:
      - Test: a config whose single layer option is a valid full snapshot → true (regression that the loop picks up the new layer-validation path).
  </behavior>
  <action>
    Modify packages/web/src/lib/radioGroupConfig.ts validateRadioOption. Keep Rule 1 (empty configPatch
    not saveable). For Rule 2, branch on target kind:
      import { validateActionPatch, validateLayerSnapshot } from "./actionAllowList";
      ...
      const patchResult = option.action.target.kind === "layer"
        ? validateLayerSnapshot(option.action.configPatch)
        : validateActionPatch(option.action.target.kind, widgetType, option.action.configPatch);
      if (!patchResult.valid) reasons.push(...patchResult.reasons);
    Leave isRadioGroupConfigValid's loop as-is (it calls validateRadioOption, which now routes layer targets
    correctly). Update the module JSDoc to note: layer targets validate via the snapshot denylist; widget/dv
    via the strict allow-list.
    Add the <behavior> tests to packages/web/src/lib/radioGroupConfig.spec.ts (new describe "validateRadioOption — layer snapshot (60.1)"). KEEP existing tests. Run from packages/web. Commit:
    `feat(60.1-02): validateRadioOption uses validateLayerSnapshot for layer targets (widget/dv strict path kept)`.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/lib/radioGroupConfig.spec.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep "validateLayerSnapshot" packages/web/src/lib/radioGroupConfig.ts` matches (imported + used for layer targets).
    - `grep -E "target.kind === \"layer\"" packages/web/src/lib/radioGroupConfig.ts` matches the branch selecting validateLayerSnapshot.
    - `grep "validateActionPatch" packages/web/src/lib/radioGroupConfig.ts` STILL matches (widget/dv branch).
    - radioGroupConfig.spec.ts asserts: a full-snapshot layer option is valid; a data-binding-key layer option is invalid; an empty layer patch is invalid; widget strict path unchanged.
    - `cd packages/web && npx vitest run src/lib/radioGroupConfig.spec.ts` exits 0.
    - `cd packages/web && npx tsc --noEmit -p tsconfig.json` exits 0.
    - A git commit exists with subject starting `feat(60.1-02): validateRadioOption`.
  </acceptance_criteria>
  <done>Layer-target options validate via validateLayerSnapshot; empty-patch rule kept; widget/dv keep validateActionPatch; spec green; web tsc clean; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MapChartRenderer live-read test — overlaid top-level info_enabled:0 drops the layer from eligibleLayers (read-live)</name>
  <files>packages/web/src/components/charts/MapChartRenderer.spec.tsx</files>
  <read_first>
    - packages/web/src/components/charts/MapChartRenderer.spec.tsx (FULL of the relevant regions — the real-widgetActionStore note ~22-23; the GAP-61-01 legend overlay test; the "POPUP-V14 — info popup integration" describe ~1768; the info-enabled layer helper ~1818; the P5 "info_enabled=0 layer excluded → not queried" test ~2027 — mirror P5's assertion that the layer is NOT queried by the info path)
    - packages/web/src/components/charts/MapChartRenderer.tsx (~482-498 effectiveLayers; ~678-680 eligibleLayers info_enabled !== 0; the info-query loop that iterates eligibleLayers ~1618)
    - packages/web/src/store/widgetActionStore.ts (setControlContribution + deriveOverlays — how to drive a layer overlay { config?, ...topLevel } from a test, exactly as the GAP-61-01 legend test does)
  </read_first>
  <behavior>
    New test in the POPUP-V14 / info-popup describe (or a new "60.1 — overlaid info_enabled read-live" describe):
      - "overlaid info_enabled:0 drops the layer from eligibleLayers (read-live)": render MapChartRenderer with
        ONE latlon layer baseline info_enabled:1 (reuse the existing eligible-layer helper). Trigger an info
        query (mirror the existing P5/P4 harness that triggers the info-query loop). With NO overlay, the
        layer IS queried (eligible). THEN, via the REAL widgetActionStore (setControlContribution with a layer
        contribution { info_enabled: 0 } at top level — DTO-shaped, mirroring the GAP-61-01 legend test), the
        layer must DROP from the info path: assert the info query for that layer is NOT fired after the overlay
        (mirror P5's "not queried" assertion). This proves effectiveLayers→eligibleLayers re-derive picks up
        the overlaid top-level info_enabled LIVE without remount.
      - OPTIONAL second assertion (if cheap with the existing harness): an overlaid info_columns reaches the
        info-query column set (assert the query payload uses the overlaid columns). Only add if it does not
        require new mock scaffolding; otherwise the info_enabled drop is the required proof.
    Use cleanup() + widgetActionStore.getState().reset() between renders so overlays do not bleed across tests.
  </behavior>
  <action>
    Extend packages/web/src/components/charts/MapChartRenderer.spec.tsx. Mirror the EXISTING GAP-61-01 overlay
    pattern (real widgetActionStore, setControlContribution to drive layerOverrides) and the EXISTING P5
    info-eligibility harness (how it asserts a layer is/ isn't queried by the info path). The overlay
    contribution for a layer is DTO-shaped — top-level info_enabled:0 (NOT nested in config), because
    info_enabled is a top-level DashboardLayerDto field and effectiveLayers spreads top-level fields. Reset
    the store between renders. Do NOT introduce a new global mock; reuse the spec's existing OL / fetch mocks.
    Run the FULL frontend suite from packages/web (cwd matters — static-assertion specs use process.cwd()):
    `cd packages/web && npx vitest run`. MUST be 100% green. Then tsc. Commit:
    `test(60.1-02): MapChartRenderer live-read — overlaid info_enabled:0 drops layer from eligibleLayers`.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - MapChartRenderer.spec.tsx contains a test referencing an OVERLAID info_enabled driven via widgetActionStore (`grep -E "info_enabled.*0|setControlContribution" packages/web/src/components/charts/MapChartRenderer.spec.tsx` matches in the new test) AND asserts the layer drops from the info/eligible path (mirrors P5's "not queried" assertion).
    - The test drives the overlay via the REAL widgetActionStore (NOT a vi.mock of it) — `grep -E "useWidgetActionStore|widgetActionStore" packages/web/src/components/charts/MapChartRenderer.spec.tsx` matches and the new test calls setControlContribution / reset.
    - `cd packages/web && npx vitest run` exits 0 with 0 failures (run from packages/web — NOT --root from repo root).
    - `cd packages/web && npx tsc --noEmit -p tsconfig.json` exits 0.
    - `git diff --name-only` shows NO files under packages/server (frontend-only).
    - A git commit exists with subject starting `test(60.1-02): MapChartRenderer live-read`.
  </acceptance_criteria>
  <done>A mounted-renderer test proves an overlaid top-level info_enabled:0 drops the layer from eligibleLayers live (no remount), driven via the real widgetActionStore; full frontend vitest 100% green from packages/web; web tsc clean; zero server diff.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx vitest run` exits 0, 0 failures. MUST be run from packages/web (static-assertion specs use process.cwd()).
- `cd packages/web && npx tsc --noEmit -p tsconfig.json` exits 0.
- `git diff --name-only` shows NO packages/server files — frontend-only.
- Layer dispatch + save-time validity both route through validateLayerSnapshot; widget/dv keep validateActionPatch.
- Overlaid top-level info_enabled:0 drops the layer from eligibleLayers live.
</verification>

<success_criteria>
- SC2: applyWidgetAction + validateRadioOption route LAYER snapshots through the denylist (accept render/style/cb_config/track_config/info_*; reject data-binding/spatial/meta); widget/dv + AI-path strict allow-list UNCHANGED.
- SC2/SC3: a layer snapshot overlay is stored DTO-shaped so effectiveLayers deep-merges it with no engine-merge change.
- SC3: overlaid render/style AND info_* take effect LIVE — proven by the eligibleLayers info_enabled:0 read-live test (GAP-61 read-once-at-mount risk closed for the snapshot path).
- Frontend vitest 100% from packages/web; web tsc clean; frontend-only.
</success_criteria>

<output>
After completion, create `.planning/phases/60.1-radio-config-ux-structured-layer-target-editor-reuse-cbconfigform/60.1-02-SUMMARY.md`
</output>
