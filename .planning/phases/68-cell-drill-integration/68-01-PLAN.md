---
phase: 68-cell-drill-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/lib/columnTypes.ts
  - packages/web/src/lib/columnTypes.spec.ts
autonomous: true
requirements: [CALDR-V113-01]
must_haves:
  truths:
    - "A datetime BETWEEN chip reads as a human-readable date range, not raw ISO strings"
    - "A week-cell chip shows BOTH start and end dates (e.g. 'Mar 2 – Mar 8, 2026')"
    - "The chip displays the inclusive human end, never '…23:59:59.999Z'"
  artifacts:
    - path: "packages/web/src/lib/columnTypes.ts"
      provides: "buildChipText datetime-between human-readable range formatting"
      contains: "operator === \"between\""
    - path: "packages/web/src/lib/columnTypes.spec.ts"
      provides: "Spec asserting datetime-between chip text is human-readable (no ISO T/Z)"
  key_links:
    - from: "buildChipText"
      to: "datetime between branch"
      via: "dataType === 'datetime' formatting"
      pattern: "between"
---

<objective>
Enhance `buildChipText`'s `between` branch (columnTypes.ts) so that a datetime BETWEEN
filter renders as a human-readable date range instead of raw ISO strings. This is the
chip half of CALDR-V113-01 and benefits ALL datetime-between chips (the timeline drag
filter too) — single source of truth for between-chip copy.

Purpose: When a calendar cell drill writes a `between` datetime filter (Plan 68-02), the
removable chip must read like "ts between Mar 2 and Mar 8, 2026", NOT
"ts between 2026-03-02T00:00:00.000Z and 2026-03-08T23:59:59.999Z".

Output: Updated `buildChipText` datetime-between formatting + spec coverage.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-cell-drill-integration/68-CONTEXT.md

<interfaces>
<!-- Current buildChipText signature + the between branch to enhance (columnTypes.ts §119-152) -->

```typescript
export function buildChipText(
  column: string,
  value: unknown,
  dataType: DrillDownDataType,   // "string" | "number" | "datetime" | "boolean" | "null"
  operator: "eq" | "in" | "between" | "isNull" = "eq",
): string

// Current between branch (§135-139) — renders RAW values:
//   return `${column} between ${String(lo)} and ${String(hi)}`;
// For datetime this produces ISO strings. ENHANCE the datetime case only.
```

The drill (Plan 68-02) writes value as `[cellStartIso, cellEndIso]` where
`cellEndIso = nextBucketStart − 1ms` (e.g. "2026-03-08T23:59:59.999Z"). The chip must
show the INCLUSIVE human end (e.g. "Mar 8, 2026"), derived from the stored bounds.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Human-readable datetime-between chip range</name>
  <files>packages/web/src/lib/columnTypes.ts, packages/web/src/lib/columnTypes.spec.ts</files>
  <read_first>
    - packages/web/src/lib/columnTypes.ts §110-152 (buildChipText — the between branch §135-139 + the eq datetime case §145-147 to mirror its `value instanceof Date ? toISOString() : String(value)` parsing)
    - packages/web/src/lib/columnTypes.spec.ts (existing buildChipText test structure — mirror describe/it layout)
    - packages/web/src/components/charts/CalendarRenderer.tsx §368 (Phase 67 tooltip smart-format via formatTimelineTick — match its month/day/hour phrasing for consistency)
    - .planning/phases/68-cell-drill-integration/68-CONTEXT.md §"Chip label" (locked format examples)
  </read_first>
  <behavior>
    - Given operator="between", dataType="datetime", value=["2026-03-02T00:00:00.000Z","2026-03-08T23:59:59.999Z"]:
      chip text contains "Mar 2" AND "Mar 8" AND "2026" and contains NO "T" / "Z" / "000" substrings (human range).
    - Given a single-day range (start "2026-03-03T00:00:00.000Z", end "2026-03-03T23:59:59.999Z"):
      reads "Mar 3, 2026" style (start==humanEnd → collapse to a single date, no "–").
    - Given an hour range (end ms = start + 1h − 1ms): shows the hour, e.g. "Mar 3, 2026 14:00".
    - Given operator="between" with dataType="number" value=[1,10]: UNCHANGED — still "col between 1 and 10".
    - Given operator="eq" datetime: UNCHANGED — still "col = '…'".
    - The inclusive human end is derived by adding 1ms to the stored end (recovering nextBucketStart) then formatting the bucket's last unit human-readably — NOT printing the raw ".999Z" value.
  </behavior>
  <action>
    Enhance ONLY the datetime arm of the `between` branch in buildChipText.

    1. Add a small local helper (in columnTypes.ts) `formatDatetimeRange(loIso, hiIso): string`:
       - Parse lo/hi via `new Date(iso).getTime()` (UTC; mirror the eq datetime parse).
       - Recover the inclusive human end: `humanEndMs = hiMs + 1` (undoes the −1ms), so the
         end formats as the bucket's natural boundary (e.g. ".999Z" end of Mar 8 → Mar 8, not Mar 9).
         Format the human end from `new Date(humanEndMs - 1)` day/hour OR equivalently format the
         day/hour that CONTAINS hiMs — pick whichever yields the inclusive last day/hour.
       - Format each endpoint with UTC getters (getUTCFullYear/Month/Date/Hours) — NEVER local getters.
       - Month names: ["Jan","Feb","Mar","Apr","May","Jun","Jul","Aug","Sep","Oct","Nov","Dec"].
       - If lo and the inclusive human end fall on the same day AND both are at 00:00 / full-day
         bounds → return a single date "Mon D, YYYY" (no en-dash).
       - If the range spans sub-day (hour granularity, hiMs−loMs < 24h) → include "HH:00".
       - Otherwise return "Mon D – Mon D, YYYY" (drop the year on the start if same year; keep it once at the end).
       - Use an en-dash "–" (U+2013) as the range separator, consistent with the operator's spec example.
    2. In the `between` branch: when `dataType === "datetime"`, return
       `${column} between ${formatDatetimeRange(String(lo), String(hi))}`. Keep the existing
       numeric/string path verbatim for all other dataTypes.
    3. Pure string logic only — NO new imports, NO React, NO theme. NO raw hex (n/a here).
    4. Write/extend columnTypes.spec.ts with the behavior cases above (RED first, then GREEN).
       Assert with `expect(text).not.toMatch(/[TZ]/)` for the datetime range and explicit substring
       checks for the month abbreviations + year.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/lib/columnTypes.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - columnTypes.spec.ts asserts a datetime between chip returns a human range with NO "T" and NO "Z" characters
    - columnTypes.spec.ts asserts a week-range chip contains both "Mar 2" and "Mar 8" and "2026"
    - columnTypes.spec.ts asserts a single-day range collapses to one date with no "–"
    - columnTypes.spec.ts asserts numeric between ("col between 1 and 10") is unchanged
    - columnTypes.ts between branch matches /dataType === "datetime"/ inside the between handling
    - `npx vitest run src/lib/columnTypes.spec.ts` from packages/web passes
  </acceptance_criteria>
  <done>
    buildChipText renders datetime BETWEEN filters as human-readable inclusive ranges (no ISO
    T/Z, no ".999"); non-datetime between and all eq paths unchanged; spec green.
  </done>
</task>

</tasks>

<verification>
- `cd packages/web && npx vitest run src/lib/columnTypes.spec.ts` passes
- `cd packages/web && npx tsc --noEmit` clean
- No raw hex introduced (theme-guard unaffected — pure string file)
</verification>

<success_criteria>
A datetime BETWEEN chip (from a calendar cell drill or timeline drag) reads as a
human-readable date range showing inclusive start AND end, satisfying the chip-copy half
of CALDR-V113-01. No regression to numeric/string between or eq chip text.
</success_criteria>

<output>
After completion, create `.planning/phases/68-cell-drill-integration/68-01-SUMMARY.md`
</output>
