---
phase: 68-cell-drill-integration
plan: 04
type: execute
wave: 3
depends_on: ["68-02", "68-03"]
files_modified:
  - packages/web/src/components/charts/MapChartRenderer.spec.tsx
  - packages/web/src/components/charts/CalendarRenderer.spec.tsx
files_modified_conditional:
  - packages/web/src/components/charts/MapChartRenderer.tsx
autonomous: true
requirements: [CALDR-V113-03]
must_haves:
  truths:
    - "A WMS map widget on the same TABLE updates its tiles after a calendar cell click (buildWmsParams picks up filters[tableId])"
    - "A WMS map widget on the same DV updates its tiles after a calendar cell click (buildWmsParams picks up dvViews[dvId])"
    - "CalendarRenderer never imports materializeFilter/dropFilterView (sole-materialize-trigger preserved)"
    - "A removable chip appears on drill and clears back to unfiltered on dismiss"
  artifacts:
    - path: "packages/web/src/components/charts/MapChartRenderer.spec.tsx"
      provides: "WMS propagation spec for calendar-driven filter on table AND dv"
      contains: "buildWmsParams"
    - path: "packages/web/src/components/charts/CalendarRenderer.spec.tsx"
      provides: "Static-grep re-assertion of no-materialize-import + chip lifecycle"
  key_links:
    - from: "calendar cell drill (filters[tableId] / dvFilters[dvId] → materialized view)"
      to: "MapChartRenderer buildWmsParams"
      via: "views[tableId] / dvViews[dvId] FROM-swap on the WMS read-path"
      pattern: "buildWmsParams"
---

<objective>
Verify (in-phase, per the v1.12 Phase 63.1 lesson) that a calendar cell drill propagates to a
WMS map widget on the SAME scope — both table-bound (map reads filters[tableId]→views[tableId])
and dv-bound (map reads dvViews[dvId]) — by asserting buildWmsParams + its dep keys pick up the
filter. Also re-assert the sole-materialize-trigger static-grep invariant and the chip lifecycle.

Purpose: WMS is a SEPARATE read-path (MEMORY "Map WMS is a separate read-path") — a drill that
filters charts/tables can silently skip the map. The calendar drill writes the SAME stores the
existing drill-down writes, and MapChartRenderer already FROM-swaps on views[tableId] (table) and
dvViews[dvId] (dv, Phase 63.1). This plan PROVES the propagation with automated specs; if a gap
is found, MapChartRenderer.tsx is wired (conditional — only if the assertion fails).

Output: WMS propagation specs (table + dv), no-materialize-import grep test, chip-lifecycle spec.

Sequenced after 68-02 (drill) and 68-03 (toggle) so the full drill behavior exists to verify.
This is the ONLY plan in the phase touching MapChartRenderer / its spec (no file collisions).
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-cell-drill-integration/68-CONTEXT.md

<interfaces>
<!-- MapChartRenderer WMS read-path (MapChartRenderer.tsx — the 2 buildWmsParams sites ~1238 + ~1486) -->

```typescript
// Table path FROM-swap (≈§1226-1232):
const entry = useFilterViewStore.getState().views[tableId];        // ← calendar table drill lands here (via markMaterializing → setView)
const viewName = !expired ? entry?.viewName : undefined;
const wmsConfigInput = { ...cfg, tableId, tableRef: viewName || rawTableRef };

// DV path FROM-swap (≈§1217-1223, Phase 63.1):
const dvFilter = useFilterViewStore.getState().dvViews[layer.dynamic_view_id];  // ← calendar dv drill lands here (via markDvMaterializing → setDvView)
if (dvFilter?.materializing === false && dvFilter.viewName) resolvedDvEntry = { status: "materialized", viewName: dvFilter.viewName };

const wmsParams = buildWmsParams(wmsConfigInput, materializeVersion, resolvedDvEntry, resolvedDvVersion, { cb_config, track_config });
```

The calendar drill (Plan 68-02) writes filters[tableId]+markMaterializing (table) /
dvFilters[dvId]+markDvMaterializing (dv) — the SAME stores. After materialization sets
views[tableId].viewName / dvViews[dvId].viewName, buildWmsParams should swap FROM to that
filtered view. The spec asserts buildWmsParams receives the filtered viewName.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WMS propagation spec — calendar filter on table AND dv</name>
  <files>packages/web/src/components/charts/MapChartRenderer.spec.tsx</files>
  <read_first>
    - packages/web/src/components/charts/MapChartRenderer.tsx §1195-1244 (table FROM-swap via views[tableId] + dv FROM-swap via dvViews[dvId] + buildWmsParams call) and the second buildWmsParams site ~§1486 (mirror)
    - packages/web/src/components/charts/MapChartRenderer.spec.tsx (existing harness — how it seeds filterViewStore + asserts buildWmsParams / LAYERS params; mirror it)
    - packages/web/src/lib/wmsUrlBuilder.ts (buildWmsParams signature + how tableRef/viewName flows into the WMS LAYERS/_mv params)
    - packages/web/src/store/filterViewStore.ts §52-160 (setView/markMaterializing + setDvView/markDvMaterializing — to seed a materialized filtered view)
    - .planning/memory MEMORY.md "Map WMS is a separate read-path" (the 2 buildWmsParams sites + dep keys)
    - .planning/phases/68-cell-drill-integration/68-CONTEXT.md §"WMS map propagation"
  </read_first>
  <action>
    Add two spec cases to MapChartRenderer.spec.tsx asserting the WMS read-path reflects a
    calendar-style filter on the same scope. (You do NOT need to render CalendarRenderer — seed
    the filter stores exactly as a calendar drill would, then assert the map's FROM-swap.)

    1. TABLE case: seed useFilterViewStore with a materialized filtered view for tableId
       (markMaterializing then setView with a viewName, materializing:false). Render
       MapChartRenderer with a layer on that same tableId. Assert buildWmsParams is called with
       `tableRef` === the filtered viewName (NOT the raw schema.table) — i.e. the map FROM-swapped
       to the calendar's filtered view. Spy on buildWmsParams (vi.mock the wmsUrlBuilder module) or
       assert the emitted WMS LAYERS / _mv param reflects the viewName.
    2. DV case: seed useFilterViewStore.dvViews[dvId] with a materialized dv-filter
       (setDvView, materializing:false, non-empty viewName). Render MapChartRenderer with a layer
       whose dynamic_view_id === dvId. Assert buildWmsParams receives resolvedDvEntry with that
       filtered dv viewName (Phase 63.1 path) — the map reflects the calendar's dv drill.
    3. If EITHER assertion fails because the read-path does not actually pick up the store write,
       wire the missing FROM-swap in MapChartRenderer.tsx (add MapChartRenderer.tsx to
       files_modified and document the gap in the SUMMARY). EXPECTATION: no wiring needed — the
       calendar writes the same stores the existing drill writes and 63.1 already wired the dv path;
       this task primarily PROVES it. Do not refactor; minimal change only if a real gap exists.
    4. NO raw hex; no new deps.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/MapChartRenderer.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - MapChartRenderer.spec.tsx asserts buildWmsParams picks up the calendar's between filter for a TABLE-bound layer (tableRef === filtered viewName, not raw schema.table)
    - MapChartRenderer.spec.tsx asserts buildWmsParams picks up the calendar's between filter for a DV-bound layer (resolvedDvEntry uses the filtered dv viewName)
    - Both new spec cases reference buildWmsParams and a calendar-style filtered viewName
    - `cd packages/web && npx vitest run src/components/charts/MapChartRenderer.spec.tsx` passes
  </acceptance_criteria>
  <done>
    Automated specs prove a calendar cell drill propagates to a WMS map widget on the same table
    AND the same dv (CALDR-V113-03); any propagation gap is wired and noted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sole-materialize-trigger static grep + chip lifecycle re-assertion</name>
  <files>packages/web/src/components/charts/CalendarRenderer.spec.tsx</files>
  <read_first>
    - packages/web/src/components/charts/CalendarRenderer.tsx (current state after 68-02/68-03 — confirm no materializeFilter/dropFilterView/fromSwap imports)
    - packages/web/src/components/charts/CalendarRenderer.spec.tsx (existing specs incl. the 68-02 drill cases — add the grep + chip-lifecycle assertions)
    - packages/web/src/components/charts/WidgetRenderer.tsx / Phase 67 plan 03 — the precedent static-grep assertion style (read the source file text and assert it does NOT match the import regex)
    - packages/web/src/store/filterStore.ts (the chip-removal / clearFilters / removeDvFilter actions — chip X clears the calendar's filter)
    - .planning/phases/68-cell-drill-integration/68-CONTEXT.md §"Sole-materialize-trigger invariant" + §"Chip label"
  </read_first>
  <action>
    1. Add a static-source-grep test (mirror the Phase 67 plan-03 precedent): read
       CalendarRenderer.tsx via fs.readFileSync and assert it does NOT match
       /import[\s\S]*?(materializeFilter|dropFilterView|fromSwap)/ — the sole-materialize-trigger
       invariant (re-asserted this phase per STATE.md / ROADMAP Phase 68 criterion 4). Confirm
       AggregatedWidgetRenderer remains the materialize trigger (the calendar only writes stores).
    2. Add a chip-lifecycle assertion: after a calendar drill (seed via the 68-02 path or a
       fireEvent.click), the active between filter exists in the store (chip present); after the
       removal action (chip X → clearFilters/removeDvFilter or setBulkFilters([])), the timeCol
       between filter is gone and the calendar returns to unfiltered (store slice empty for timeCol).
       Tie this to the reset-on-clear behavior; lifecycle reset on dashboard-switch/logout is the
       existing store-cleanup chain (no calendar-specific code — assert the filter is gone after clear).
    3. NO production code changes expected in this task; spec-only. (If the grep test FAILS,
       that is a real invariant break — fix the offending import in CalendarRenderer.tsx and note it.)
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/CalendarRenderer.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - CalendarRenderer.spec.tsx contains a static-grep test asserting CalendarRenderer.tsx does NOT match /import.*(materializeFilter|dropFilterView|fromSwap)/
    - The grep test passes (CalendarRenderer.tsx has no such import)
    - CalendarRenderer.spec.tsx asserts the between filter clears from the store on chip/clear, returning to unfiltered
    - `cd packages/web && npx vitest run src/components/charts/CalendarRenderer.spec.tsx` passes
  </acceptance_criteria>
  <done>
    A static grep re-asserts CalendarRenderer never imports materializeFilter/dropFilterView, and
    the chip add→dismiss→unfiltered lifecycle is covered by spec; both green.
  </done>
</task>

</tasks>

<verification>
- `cd packages/web && npx vitest run src/components/charts/MapChartRenderer.spec.tsx src/components/charts/CalendarRenderer.spec.tsx` passes
- `cd packages/web && npx tsc --noEmit` clean
- `cd packages/web && npx vitest run` 100% green (full frontend gate — no regressions)
- `grep -E "import.*(materializeFilter|dropFilterView|fromSwap)" packages/web/src/components/charts/CalendarRenderer.tsx` returns NOTHING
</verification>

<success_criteria>
CALDR-V113-03 is satisfied in-phase: automated specs prove a calendar cell drill propagates to
a WMS map on the same table AND the same dv, the sole-materialize-trigger invariant is statically
re-asserted, and the chip add→dismiss→unfiltered lifecycle is covered — not deferred to live UAT.
</success_criteria>

<output>
After completion, create `.planning/phases/68-cell-drill-integration/68-04-SUMMARY.md`
</output>
