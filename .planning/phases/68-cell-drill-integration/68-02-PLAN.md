---
phase: 68-cell-drill-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/charts/CalendarRenderer.tsx
  - packages/web/src/components/charts/CalendarRenderer.spec.tsx
autonomous: true
requirements: [CALDR-V113-01, CALDR-V113-02]
must_haves:
  truths:
    - "Clicking a non-empty calendar cell on a table-bound calendar applies a BETWEEN datetime filter to filters[tableId]"
    - "Clicking a non-empty cell on a dv-bound calendar applies the filter to dvFilters[dvId] and leaves filters[tableId] untouched"
    - "Clicking an empty/grey cell does nothing"
    - "Re-clicking the active cell toggles the calendar's timeCol filter off"
    - "The active cell is outlined while its filter is live (reactive, theme token)"
    - "A toast appears on drill with the slice label"
  artifacts:
    - path: "packages/web/src/components/charts/CalendarRenderer.tsx"
      provides: "Cell-click drill dispatch (table+dv BETWEEN routing) + reactive selected-cell highlight + toast"
      contains: "computeCellBounds"
    - path: "packages/web/src/components/charts/CalendarRenderer.spec.tsx"
      provides: "Specs for table drill, dv-isolation, empty-cell guard, toggle-off, highlight"
  key_links:
    - from: "CalendarRenderer cell onClick"
      to: "useFilterStore setBulkFilters / addDvFilter"
      via: "table-bound vs dv-bound routing on dynamicViewId"
      pattern: "setBulkFilters|addDvFilter"
    - from: "CalendarRenderer cell onClick"
      to: "useFilterViewStore markMaterializing / markDvMaterializing"
      via: "sole-trigger handoff (store write only, no materialize)"
      pattern: "markMaterializing|markDvMaterializing"
    - from: "store between filter bounds"
      to: "selected-cell outline"
      via: "reactive memo matching cell bounds (mirror appliedBand)"
      pattern: "operator.*between|appliedCell"
---

<objective>
Add the cell-click drill to CalendarRenderer: clicking a populated cell computes its
BETWEEN bounds via `computeCellBounds` and dispatches a datetime range filter — routed to
`filters[tableId]` (table-bound) or `dvFilters[dvId]` (dv-bound, dv-isolated) — plus a
reactive selected-cell outline and an info toast. This is the core of CALDR-V113-01 and -02.

Purpose: Make the calendar a click-through drill control. The handler MIRRORS the
dispatchDrillDown dv-routing pattern and TimelineRenderer's BETWEEN table-path, but is the
calendar's OWN dispatch (dispatchDrillDown is eq-only and would mis-dedupe BETWEEN tuples).

Output: Interactive CalendarRenderer with table+dv BETWEEN drill, toggle-off re-click,
reactive highlight, toast, pointer cursor on populated cells — and updated spec.

CRITICAL invariant: CalendarRenderer still NEVER imports materializeFilter/dropFilterView.
The drill only WRITES the filter stores; AggregatedWidgetRenderer Effect 1 materializes.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-cell-drill-integration/68-CONTEXT.md

<interfaces>
<!-- Store actions + types the handler uses (extracted from the codebase) -->

```typescript
// filterStore.ts
export type ActiveFilter = {
  column: string;
  value: string | number | boolean | null | (string | number)[] | [number, number] | [string, string];
  dataType: DrillDownDataType;          // use "datetime"
  operator?: "eq" | "in" | "between" | "isNull";
  sourceWidgetId?: number;
  addedAt?: number;
};
setBulkFilters(tableId: number, filters: ActiveFilter[]): void;   // table path — OVERWRITES same-col
addDvFilter(dynamicViewId: number, filter: ActiveFilter): void;   // dv path — writes dvFilters[dvId]

// filterViewStore.ts
markMaterializing(tableId: number, dashboardId: number): void;
markDvMaterializing(dynamicViewId: number, dashboardId: number): void;

// calendarBin.ts
computeCellBounds(dateIso: string, subdomainUnit: CalendarSubdomain): [string, string];
// → [cellStart, cellEnd] where cellEnd = nextBucketStart − 1ms (inclusive BETWEEN upper)

// useToastStore — showToast(text, "info")
// DashboardContext — const { dashboardId } = useDashboardContext();  (NOT currently imported in CalendarRenderer — ADD it; see TimelineRenderer §45,§121)
// columnTypes.ts — buildChipText(column, value, "datetime", "between")  // human range from Plan 68-01
```

TEMPLATE — dispatchDrillDown dv-routing (WidgetRenderer §124-150) and TimelineRenderer
BETWEEN table-path `commitFilter` (§311-323) and `appliedBand` reactive memo (§173-180).
MIRROR the routing/derivation; do NOT call dispatchDrillDown (eq-only).

Cell shape in the grid (CalendarRenderer §347-369): each cell has `cell.value`,
`cell.domainKey`, `cell.subdomainKey` (subdomainKey is the cell's ISO start — the
computeCellBounds input). Empty cells already have `pointerEvents:none` (§361).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cell-click BETWEEN drill dispatch (table + dv routing) + toast</name>
  <files>packages/web/src/components/charts/CalendarRenderer.tsx, packages/web/src/components/charts/CalendarRenderer.spec.tsx</files>
  <read_first>
    - packages/web/src/components/charts/CalendarRenderer.tsx (FULL — the file being modified; note §86-103 config reads, §345-384 cell render where onClick attaches, §350-364 empty-cell branch)
    - packages/web/src/components/charts/WidgetRenderer.tsx §104-150 (dispatchDrillDown dv branch — the dv-isolation routing template: dvFilters[dvId] + addDvFilter + markDvMaterializing; table path §152-186: filters[tableId] + setBulkFilters/addFilter + markMaterializing)
    - packages/web/src/components/charts/TimelineRenderer.tsx §45,§121 (useDashboardContext import + dashboardId), §305-323 (commitFilter — setBulkFilters + markMaterializing table BETWEEN drill)
    - packages/web/src/lib/calendarBin.ts §101-153 (computeCellBounds)
    - packages/web/src/store/filterStore.ts §17-65,§114-180 (ActiveFilter, setBulkFilters, addDvFilter)
    - packages/web/src/store/filterViewStore.ts §52-160 (markMaterializing, markDvMaterializing)
    - packages/web/src/components/charts/CalendarRenderer.spec.tsx (existing Phase 67 spec — mirror its render harness incl. DashboardContext wrapping; preserve all existing assertions)
    - .planning/phases/68-cell-drill-integration/68-CONTEXT.md §"Cell-click dispatch" + §"Re-click & cross-cell behavior" + §"Sole-materialize-trigger invariant"
  </read_first>
  <behavior>
    - Table-bound (dynamicViewId undefined), click populated cell: calls
      setBulkFilters(tableId, [{ column: timeCol, value: [cellStart, cellEnd], operator: "between", dataType: "datetime", sourceWidgetId: widget.id }])
      AND markMaterializing(tableId, dashboardId). Does NOT call addDvFilter / markDvMaterializing.
    - DV-bound (dynamicViewId set), click populated cell: calls
      addDvFilter(dynamicViewId, { ...same filter }) AND markDvMaterializing(dynamicViewId, dashboardId).
      Does NOT call setBulkFilters; filters[tableId] is untouched after the click (dv-isolation, CALDR-V113-02).
    - Click empty/grey cell (cell.value === null): handler returns early — no store writes at all.
    - cellStart/cellEnd come from computeCellBounds(cell.subdomainKey, subdomain) (cellEnd = nextBucketStart − 1ms).
    - On a NEW drill (column not already filtered with these exact bounds): showToast(label, "info") fires once.
  </behavior>
  <action>
    1. Import: useDashboardContext (from "../DashboardContext"), computeCellBounds (from
       "../../lib/calendarBin"), useToastStore, buildChipText (from "../../lib/columnTypes").
       DO NOT import materializeFilter/dropFilterView/fromSwap (invariant).
    2. Read `const { dashboardId } = useDashboardContext();` near the other hooks (after the
       early empty-state returns follow the existing eslint-disable rules-of-hooks pattern).
    3. Write a `handleCellClick(cell)` (inline or small local fn — Claude's discretion):
       - Guard: `if (cell.value === null) return;`
       - `const [cellStart, cellEnd] = computeCellBounds(cell.subdomainKey, subdomain);`
       - Build the ActiveFilter:
         `{ column: timeCol, value: [cellStart, cellEnd] as [string,string], operator: "between", dataType: "datetime", sourceWidgetId: widget.id, addedAt: Date.now() }`
       - TOGGLE-OFF: if this cell is the currently-active cell (see Task 2 `appliedCell` memo —
         compare the cell's [cellStart,cellEnd] to the active bounds), clear the calendar's
         timeCol filter instead of re-applying. Table path: setBulkFilters(tableId, []) (clears
         the calendar's bulk set) + markMaterializing(tableId, dashboardId). DV path: clear via
         the dv store's clear-dv-filter action (use the existing remove/clear dv action; if only
         addDvFilter exists, re-dispatch an empty set per the dv store API — read filterStore.ts
         to find the correct dv-clear action) + markDvMaterializing. Keep the chip-X as the other clear path.
       - REPLACE / NEW: table-bound → setBulkFilters(tableId, [filter]) + markMaterializing(tableId, dashboardId).
         dv-bound → addDvFilter(dynamicViewId, filter) + markDvMaterializing(dynamicViewId, dashboardId).
       - Toast: on a NEW drill (not toggle-off), `useToastStore.getState().showToast(buildChipText(timeCol, [cellStart, cellEnd], "datetime", "between"), "info")`.
    4. Attach `onClick={() => handleCellClick(cell)}` and `style={{ cursor: "pointer" }}` to the
       POPULATED-cell `<rect>` only (§371-382). Leave empty cells with pointerEvents:none unchanged.
    5. NO raw hex anywhere (theme-guard). Pointer cursor is fine (not a color).
    6. Spec (RED→GREEN): mock the stores (or assert via store getState after a simulated click).
       Use fireEvent.click on the populated rect. Wrap render in the existing DashboardContext
       harness from the Phase 67 spec. Add the behavior assertions listed above.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/CalendarRenderer.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - CalendarRenderer.spec.tsx asserts a table-bound populated-cell click calls setBulkFilters with operator "between" and value [cellStart, cellEnd]
    - CalendarRenderer.spec.tsx asserts a table-bound click calls markMaterializing and does NOT call addDvFilter
    - CalendarRenderer.spec.tsx asserts a dv-bound click calls addDvFilter and does NOT call setBulkFilters
    - CalendarRenderer.spec.tsx asserts a dv-bound click calls markDvMaterializing(dynamicViewId, dashboardId) and does NOT call markMaterializing
    - CalendarRenderer.spec.tsx asserts filters[tableId] is untouched (empty) after a dv-bound drill
    - CalendarRenderer.spec.tsx asserts clicking a cell with value===null does NOT dispatch any store write
    - CalendarRenderer.spec.tsx asserts re-clicking the active cell clears the calendar's timeCol filter (toggle off)
    - CalendarRenderer.spec.tsx asserts a toast fires on a new drill
    - CalendarRenderer.tsx contains "computeCellBounds" and matches /operator: "between"/
    - CalendarRenderer.tsx does NOT match /import.*(materializeFilter|dropFilterView|fromSwap)/
  </acceptance_criteria>
  <done>
    Populated-cell clicks dispatch a BETWEEN datetime filter with correct table/dv routing,
    empty cells are inert, re-click toggles off, a toast fires, dv-isolation holds; spec green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reactive selected-cell highlight</name>
  <files>packages/web/src/components/charts/CalendarRenderer.tsx, packages/web/src/components/charts/CalendarRenderer.spec.tsx</files>
  <read_first>
    - packages/web/src/components/charts/CalendarRenderer.tsx (the cell render §345-384 + the store selectors §127-152 — add a tableFilters / dvFilters selector for the active-filter memo)
    - packages/web/src/components/charts/TimelineRenderer.tsx §155 (tableFilters selector), §173-180 (appliedBand reactive memo — the EXACT pattern to mirror for the active between filter)
    - packages/web/src/lib/calendarBin.ts §101-153 (computeCellBounds — to match a cell's bounds against the active filter bounds)
    - packages/web/src/lib/chartColors.ts / useChartAxisColors (theme tokens — pick/extend an outline color token; NO raw hex)
    - .planning/phases/68-cell-drill-integration/68-CONTEXT.md §"Selected-cell feedback"
  </read_first>
  <behavior>
    - When the store holds a `between` filter on timeCol (table path: filters[tableId]; dv path: dvFilters[dvId]) whose [lo,hi] equals a cell's [cellStart,cellEnd], that cell's rect renders with a visible outline (stroke) using a theme token.
    - No active between filter → no cell is outlined.
    - Clearing the filter (chip X or toggle-off) removes the outline reactively (memo recomputes).
    - The matched cell is exposed for Task 1's toggle-off check (shared `appliedCell` value).
  </behavior>
  <action>
    1. Add a scoped selector for the active filter list: table-bound → useFilterStore((s) => s.filters[tableId] ?? []); dv-bound → useFilterStore((s) => s.dvFilters[dynamicViewId] ?? []). Choose by dynamicViewId (follow the existing eslint-disable rules-of-hooks selector style).
    2. Add `const appliedCell = useMemo(...)` mirroring TimelineRenderer.appliedBand:
       find the filter where `column === timeCol && operator === "between" && Array.isArray(value) && value.length === 2`; capture `[lo, hi] = value as [string,string]`. Store these bounds (a `[string,string] | null`). Deps: [the selected filter list, timeCol].
    3. In the populated-cell render, compute the cell's bounds (computeCellBounds(cell.subdomainKey, subdomain)) and set `stroke` + `strokeWidth` on the rect when the cell's [start,end] === appliedCell bounds. Use a theme token for the stroke (e.g. an accent/axis token from useChartAxisColors or a CSS var via a derived concrete string) — NO raw hex.
    4. Share `appliedCell` with Task 1's handler for the toggle-off equality test (same bounds source of truth).
    5. Spec: assert that with a matching between filter pre-seeded in the store, the matching rect has a stroke attribute; with no filter, no rect has a (non-default) stroke; after clearing, the stroke is gone.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/CalendarRenderer.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - CalendarRenderer.spec.tsx asserts the cell whose bounds match an active between filter renders with a stroke (outline)
    - CalendarRenderer.spec.tsx asserts no cell is outlined when no between filter is active
    - CalendarRenderer.tsx contains a useMemo deriving the active between bounds (mirrors appliedBand) referencing operator "between"
    - CalendarRenderer.tsx outline stroke uses a theme token / CSS var, NOT a raw hex (theme-guard passes)
    - `cd packages/web && npx vitest run src/components/charts/CalendarRenderer.spec.tsx` passes
  </acceptance_criteria>
  <done>
    The active cell is outlined via a reactive memo over the store's between filter (table or
    dv slice), using a theme token; the outline clears when the filter clears; spec green.
  </done>
</task>

</tasks>

<verification>
- `cd packages/web && npx vitest run src/components/charts/CalendarRenderer.spec.tsx` passes
- `cd packages/web && npx vitest run src/lib/theme-guard.spec.ts` passes (CalendarRenderer in scope — no raw hex)
- `cd packages/web && npx tsc --noEmit` clean
- Phase 67's 5 success criteria specs remain green (no regression to fetch/gap-fill/color/legend/re-fetch)
- `grep -E "import.*(materializeFilter|dropFilterView|fromSwap)" packages/web/src/components/charts/CalendarRenderer.tsx` returns NOTHING
</verification>

<success_criteria>
Clicking a populated calendar cell drills the dashboard with a BETWEEN datetime filter
(CALDR-V113-01), correctly routed to the table or the dv slice (CALDR-V113-02, dv-isolated),
empty cells inert, re-click toggles off, the active cell is outlined and a toast fires —
with the sole-materialize-trigger invariant preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/68-cell-drill-integration/68-02-SUMMARY.md`
</output>
