---
phase: 68-cell-drill-integration
plan: 03
type: execute
wave: 2
depends_on: ["68-02"]
files_modified:
  - packages/web/src/components/charts/CalendarConfigPanel.tsx
  - packages/web/src/components/charts/CalendarConfigPanel.spec.tsx
  - packages/web/src/components/charts/CalendarRenderer.tsx
  - packages/web/src/components/charts/CalendarRenderer.spec.tsx
autonomous: true
requirements: [CALDR-V113-01]
must_haves:
  truths:
    - "A 'Respond to dashboard filters' checkbox appears in CalendarConfigPanel"
    - "respondToFilters defaults to false (OFF) on new calendar widgets"
    - "With respondToFilters OFF, the calendar reads the UNFILTERED source (ignores fvViewName / dvFilterViewName)"
    - "With respondToFilters ON, the calendar uses the full Phase 67 FROM precedence (filter-aware)"
    - "Cell clicks still drive filters into the stores regardless of the toggle"
  artifacts:
    - path: "packages/web/src/components/charts/CalendarConfigPanel.tsx"
      provides: "respondToFilters CalendarConfig field + 'Respond to dashboard filters' checkbox (default false)"
      contains: "Respond to dashboard filters"
    - path: "packages/web/src/components/charts/CalendarRenderer.tsx"
      provides: "FROM resolution gated on respondToFilters"
  key_links:
    - from: "respondToFilters config flag"
      to: "CalendarRenderer FROM resolution"
      via: "OFF short-circuits to unfiltered source before SQL build"
      pattern: "respondToFilters"
---

<objective>
Add the "Respond to dashboard filters" config toggle (default OFF) and gate CalendarRenderer's
FROM resolution on it. OFF (default): the calendar ALWAYS reads the unfiltered source — a
stable full-grid drill control that ignores ALL filters but whose clicks STILL drive filters
into other widgets. ON: the existing Phase 67 filter-aware behavior (full FROM precedence).

Purpose: Operator decision (68-CONTEXT scope expansion) — the calendar is primarily a drill
CONTROL, so filter-awareness is opt-in. This edits two ALREADY-VERIFIED files
(CalendarConfigPanel from Phase 66, CalendarRenderer from Phase 67) and CHANGES the renderer's
default behavior, so the Phase 67 spec expectations MUST be updated (default now OFF → base table).

Output: respondToFilters field + checkbox + gated FROM resolution + updated specs.

Sequenced AFTER Plan 68-02 because both edit CalendarRenderer.tsx — never two plans on that
file in the same wave (STATE.md parallel-executor lesson).
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-cell-drill-integration/68-CONTEXT.md

<interfaces>
<!-- CalendarConfig type (CalendarConfigPanel.tsx §43-62) — ADD respondToFilters -->

```typescript
export type CalendarConfig = {
  tableId?: number;
  tableRef?: string;
  dynamicViewId?: number;
  timeCol: string;
  metricColumn: string;
  aggregation: TimelineAggregation;
  domain: CalendarDomain;
  subdomain: CalendarSubdomain;
  colorTheme: string;
  respondToFilters?: boolean;     // ADD — default false (OFF)
};
export const DEFAULT_CALENDAR_CONFIG: CalendarConfig = { /* ...; respondToFilters: false */ };
```

<!-- CalendarRenderer FROM resolution today (Phase 67, §164-189) -->
```typescript
// CURRENT (always filter-aware):
const fromView = dynamicViewId === undefined ? fvViewName : (dvFilterViewName ?? dvViewName);
const fromTarget = fromView ? fromView
  : effectiveSchema ? `${effectiveSchema}.${effectiveTable}` : effectiveTable;
// GATE on respondToFilters:
//   OFF → ignore fvViewName & dvFilterViewName entirely:
//     table-bound → `${effectiveSchema}.${effectiveTable}` (base table)
//     dv-bound    → dvViewName (raw dv view, NOT dvFilterViewName)
//   ON  → keep the existing precedence above.
```
The re-fetch dep array stays (clicks still update the store) — only the RESOLVED FROM changes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: respondToFilters field + 'Respond to dashboard filters' checkbox</name>
  <files>packages/web/src/components/charts/CalendarConfigPanel.tsx, packages/web/src/components/charts/CalendarConfigPanel.spec.tsx</files>
  <read_first>
    - packages/web/src/components/charts/CalendarConfigPanel.tsx (FULL — verified in Phase 66; §43-62 CalendarConfig + DEFAULT_CALENDAR_CONFIG, §351-528 render, §132-134 patch helper)
    - packages/web/src/components/charts/CalendarConfigPanel.spec.tsx (existing Phase 66 spec — preserve all assertions; mirror the test harness)
    - .planning/memory MEMORY.md "UI consistency conventions" (accent checkboxes, theme tokens, light-mode overrides — operator catches deviations)
    - .planning/phases/68-cell-drill-integration/68-CONTEXT.md §"Respond to dashboard filters toggle" (label string LOCKED, default OFF)
  </read_first>
  <behavior>
    - CalendarConfig gains `respondToFilters?: boolean`; DEFAULT_CALENDAR_CONFIG sets it false.
    - The panel renders a checkbox labeled EXACTLY "Respond to dashboard filters".
    - Toggling the checkbox calls onChange with respondToFilters flipped (patch helper).
    - Checkbox reflects the current config value; defaults to unchecked when undefined/false.
    - No raw hex (theme tokens / existing checkbox styling classes only).
  </behavior>
  <action>
    1. Add `respondToFilters?: boolean;` to the CalendarConfig type (§43-53) and
       `respondToFilters: false` to DEFAULT_CALENDAR_CONFIG (§55-62).
    2. Read `const respondToFilters = cfg.respondToFilters ?? false;` alongside the other cfg reads (§111-120).
    3. Render a checkbox field inside the configured block (after the data source is picked —
       reuse the existing `ds-field` markup / project checkbox convention; accent checkbox per
       MEMORY UI conventions). Label text EXACTLY "Respond to dashboard filters".
       onChange → `patch({ respondToFilters: e.target.checked })`. aria-label or htmlFor wired.
    4. NO raw hex (theme-guard scans this file).
    5. Spec: assert the panel renders text "Respond to dashboard filters"; assert default is
       unchecked; assert toggling calls onChange with respondToFilters true. Add to the existing
       Phase 66 spec without breaking its assertions.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/CalendarConfigPanel.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - CalendarConfigPanel.tsx contains the exact string "Respond to dashboard filters"
    - CalendarConfigPanel.tsx DEFAULT_CALENDAR_CONFIG matches /respondToFilters: false/
    - CalendarConfig type contains respondToFilters
    - CalendarConfigPanel.spec.tsx asserts the checkbox label "Respond to dashboard filters" renders
    - CalendarConfigPanel.spec.tsx asserts toggling calls onChange with respondToFilters true
    - theme-guard passes with CalendarConfigPanel.tsx in scope (no raw hex)
  </acceptance_criteria>
  <done>
    The config panel exposes a default-OFF "Respond to dashboard filters" checkbox persisted on
    CalendarConfig; Phase 66 spec assertions still pass; new assertions green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate CalendarRenderer FROM resolution on respondToFilters</name>
  <files>packages/web/src/components/charts/CalendarRenderer.tsx, packages/web/src/components/charts/CalendarRenderer.spec.tsx</files>
  <read_first>
    - packages/web/src/components/charts/CalendarRenderer.tsx (FULL — §93-103 cfg reads, §162-252 the useEffect FROM resolution + dep array; this file was edited by Plan 68-02 — read its current state)
    - packages/web/src/components/charts/CalendarRenderer.spec.tsx (Phase 67 + Plan 68-02 specs — the re-fetch/FROM expectations that change with default OFF)
    - packages/web/src/components/charts/CalendarConfigPanel.tsx §43-62 (CalendarConfig respondToFilters — added in Task 1)
    - .planning/phases/68-cell-drill-integration/68-CONTEXT.md §"Respond to dashboard filters toggle" semantics + §"Scope note for planner" (default behavior changes; update CalendarRenderer.spec.tsx)
  </read_first>
  <behavior>
    - respondToFilters OFF (default): table-bound → fromTarget = `${effectiveSchema}.${effectiveTable}` (or effectiveTable when no schema), IGNORING fvViewName; dv-bound → fromTarget = dvViewName (raw dv view), IGNORING dvFilterViewName. The calendar always fetches the unfiltered grid.
    - respondToFilters ON: existing Phase 67 precedence — table fvViewName || base; dv (dvFilterViewName ?? dvViewName).
    - Either way, the re-fetch dep array stays intact (filterVersion etc.) so cell clicks/store writes still re-render where applicable; OFF just resolves FROM to the unfiltered source.
    - The materializing/expiry suspend gates that only matter for filtered views are skipped/benign when OFF (do not block the base-table fetch when OFF).
  </behavior>
  <action>
    1. Read `const respondToFilters = cfg.respondToFilters ?? false;` from config.
    2. In the useEffect, gate the `fromView` resolution:
       - OFF: `const fromView = undefined;` (forces fromTarget to the base/raw-dv path) for table;
         for dv-bound OFF, set fromTarget = dvViewName directly (raw dv view), NOT dvFilterViewName.
         Concretely: when OFF, table-bound fromTarget = effectiveSchema ? `${effectiveSchema}.${effectiveTable}` : effectiveTable; dv-bound fromTarget = dvViewName.
       - ON: keep the existing `dynamicViewId === undefined ? fvViewName : (dvFilterViewName ?? dvViewName)` precedence and downstream fromTarget logic verbatim.
       - Guard the OFF dv path: dv-bound still requires dvStatus === "materialized" + dvViewName present before fetching (the raw dv view must exist); keep that gate.
       - When OFF, do not early-return on fvMaterializing (the base table fetch does not depend on the filtered view materializing).
    3. Add `respondToFilters` to the useEffect dep array (so toggling re-resolves FROM).
    4. NO new imports beyond what 68-02 added; NO materializeFilter/dropFilterView/fromSwap; NO raw hex.
    5. Spec (RED→GREEN) — UPDATE Phase 67 expectations: default (respondToFilters absent/false) →
       assert buildCalendarSql / runSql is called with the BASE table FROM (e.g. SQL contains the
       schema.table, NOT the fvViewName) even when an fvViewName is present in the store. With
       respondToFilters true + fvViewName present → assert the SQL uses fvViewName (filter-aware).
       For dv-bound: OFF → SQL uses dvViewName; ON → SQL uses dvFilterViewName when present.
       Mock runSql and inspect the SQL string, or assert the resolved fromTarget.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/CalendarRenderer.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - CalendarRenderer.tsx contains respondToFilters and gates the FROM resolution on it
    - CalendarRenderer.spec.tsx asserts: respondToFilters false → SQL/fromTarget uses the BASE table even when fvViewName is set in the store
    - CalendarRenderer.spec.tsx asserts: respondToFilters true → SQL uses fvViewName (filter-aware, Phase 67 behavior)
    - CalendarRenderer.spec.tsx asserts: dv-bound respondToFilters false → SQL uses dvViewName (raw), not dvFilterViewName
    - CalendarRenderer.tsx does NOT match /import.*(materializeFilter|dropFilterView|fromSwap)/
    - CalendarRenderer.spec.tsx asserts that with respondToFilters false, a cell click still calls setBulkFilters (onClick is not gated on the toggle)
    - Phase 67's gap-fill / color-domain / legend / tooltip specs still pass (no regression beyond the intentional default-FROM change)
  </acceptance_criteria>
  <done>
    CalendarRenderer reads the unfiltered source by default (respondToFilters OFF) and the
    filter-aware view only when ON; the dep array re-resolves on toggle; specs updated for the
    new default and green; sole-materialize invariant + theme-guard intact.
  </done>
</task>

</tasks>

<verification>
- `cd packages/web && npx vitest run src/components/charts/CalendarConfigPanel.spec.tsx src/components/charts/CalendarRenderer.spec.tsx` passes
- `cd packages/web && npx vitest run src/lib/theme-guard.spec.ts` passes (both edited files in scope)
- `cd packages/web && npx tsc --noEmit` clean
- `grep -E "import.*(materializeFilter|dropFilterView|fromSwap)" packages/web/src/components/charts/CalendarRenderer.tsx` returns NOTHING
- Phase 67's 5/5 criteria specs green except the intentional default-FROM expectation update
</verification>

<success_criteria>
Operators can toggle "Respond to dashboard filters" (default OFF); OFF keeps the calendar a
stable full-grid drill control reading the unfiltered source, ON restores Phase 67
filter-awareness — cell clicks drive filters either way. No regression to the other Phase 67
criteria; both previously-verified files updated with specs kept green.
</success_criteria>

<output>
After completion, create `.planning/phases/68-cell-drill-integration/68-03-SUMMARY.md`
</output>
