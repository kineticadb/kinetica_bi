---
phase: 76-column-formatting-editor-ui
plan: 02
type: execute
wave: 2
depends_on: ["76-01"]
files_modified:
  - packages/web/src/components/DatasetsPage.tsx
  - packages/web/src/components/DatasetsPage.spec.tsx
autonomous: true
requirements: [COLEDIT-V115-01]

must_haves:
  truths:
    - "The TableDetail (view) screen shows a 'Format columns' ghost-sm button alongside the Back action"
    - "Clicking 'Format columns' opens the ColumnFormatEditorModal for that table; closing it returns to TableDetail"
    - "The button + modal mount live entirely in the Tables area (DatasetsPage) — no new route"
  artifacts:
    - path: "packages/web/src/components/DatasetsPage.tsx"
      provides: "Format columns entry-point button + conditional ColumnFormatEditorModal mount on TableDetail"
      contains: "ColumnFormatEditorModal"
    - path: "packages/web/src/components/DatasetsPage.spec.tsx"
      provides: "Integration test: button renders on TableDetail, opens the editor (mocked), passes table + onClose"
      min_lines: 40
  key_links:
    - from: "DatasetsPage.tsx (TableDetail)"
      to: "ColumnFormatEditorModal"
      via: "conditional render gated by showFormatEditor state, props {table, onClose}"
      pattern: "<ColumnFormatEditorModal"
    - from: "Format columns button"
      to: "showFormatEditor state"
      via: "onClick setShowFormatEditor(true)"
      pattern: "Format columns"
---

<objective>
Wire the Plan 76-01 `ColumnFormatEditorModal` into the Tables area: add a "Format columns" `ghost-sm` button to the `TableDetail` (view-mode) screen in `DatasetsPage.tsx` that opens the editor for that table. This is the operator's entry point into the editor (COLEDIT-V115-01: "reached from the Tables area").

Purpose: Closes the COLEDIT-V115-01 entry-point requirement — the editor must be reachable from the Tables area, no new route.
Output: `DatasetsPage.tsx` gains a button + conditional modal mount; an integration test proves the button opens the editor with the right props.

CRITICAL: FRONTEND-ONLY. Do NOT touch `packages/server`. If any server diff appears, STOP — it is a defect.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-column-formatting-editor-ui/76-CONTEXT.md

# Entry host — TableDetail is at lines 123-179; it already iterates Object.entries(table.columns):
@packages/web/src/components/DatasetsPage.tsx

# The component being mounted (created by Plan 76-01) — props are { table: TableDto; onClose: () => void }:
# packages/web/src/components/ColumnFormatEditorModal.tsx

# Mount + open/close + test-mock pattern to mirror (conditional render of a modal from a page,
# vi.mock("./SomeModal") capturing props):
@packages/web/src/components/DashboardsPage.tsx  (lines 837 button, 1164-1168 conditional <DynamicViewsModal> mount)
@packages/web/src/components/DashboardsPage.spec.tsx  (lines 89-92 vi.mock DynamicViewsModal; line 791 "opens modal with props" assertion)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Format columns" button + ColumnFormatEditorModal mount to TableDetail + integration test</name>
  <files>packages/web/src/components/DatasetsPage.tsx, packages/web/src/components/DatasetsPage.spec.tsx</files>
  <read_first>
    - packages/web/src/components/DatasetsPage.tsx (the `TableDetail` component at :123-179 — its `actions` prop currently holds only the Back button at :131-135; the ChartCard wrapper; `import { ... TableDto } from "../api/client"` at top)
    - packages/web/src/components/DashboardsPage.tsx (:402 useState modal flag; :837 button that flips it; :1164-1168 conditional `<DynamicViewsModal ... onClose={() => setShow...(false)} />` mount) — mirror this open/close shape
    - packages/web/src/components/DashboardsPage.spec.tsx (:89-92 `vi.mock("./DynamicViewsModal", ...)` capturing props via a global; :791 the "clicking X opens modal with props" test) — mirror this test shape
    - packages/web/src/components/ColumnFormatEditorModal.tsx (the default export + its `{ table, onClose }` props, from Plan 76-01)
  </read_first>
  <action>
    Edit packages/web/src/components/DatasetsPage.tsx.

    1. Import the editor at the top of the file:
       `import ColumnFormatEditorModal from "./ColumnFormatEditorModal";`
       (Also import `useState` — already imported at line 1.)

    2. In the `TableDetail` component (currently a prop-only functional component at :123), add local state:
       `const [showFormatEditor, setShowFormatEditor] = useState(false);`

    3. In the ChartCard `actions` (currently just the Back button), ADD a "Format columns" button BEFORE Back, matching the existing `ghost-sm` style:
       `<button className="ghost-sm" onClick={() => setShowFormatEditor(true)}>Format columns</button>`
       Keep the existing `<button className="ghost-sm" onClick={onBack}>Back</button>`.

    4. After the ChartCard (still inside TableDetail's returned `.dashboard-list` wrapper, as a sibling of ChartCard), conditionally mount the modal:
       ```
       {showFormatEditor && (
         <ColumnFormatEditorModal
           table={table}
           onClose={() => setShowFormatEditor(false)}
         />
       )}
       ```
       (Mirror DashboardsPage.tsx:1164-1168 exactly — conditional && render with onClose flipping the flag false.)

    5. Do NOT change the list/edit/create modes, the columns table, or any other behavior. This is purely additive to TableDetail (view mode).

    THEME: the button reuses the existing `ghost-sm` class — no new styling, no raw hex.

    6. Create packages/web/src/components/DatasetsPage.spec.tsx (none exists today) with an integration test mirroring DashboardsPage.spec.tsx's modal pattern:
       - `vi.mock("./ColumnFormatEditorModal", ...)` returning a stub that records its props (e.g. to `globalThis.__lastCFEProps`) and renders a sentinel like `<div data-testid="cfe-modal-stub" />`.
       - Mock `../api/client` `listTables` (and any other client calls DatasetsPage makes on mount) so the page renders; render DatasetsPage and navigate to the view mode (click a table's "View" button) for a table with known columns.
       - Assert: a "Format columns" button is present on the detail screen.
       - Click it; assert the stub modal renders (the sentinel appears) and that it received `table` (the selected TableDto) + an `onClose` function prop.
       - Assert the modal is NOT rendered before the button is clicked.
       Mirror the render + mock + props-capture style of DashboardsPage.spec.tsx:89-131 and the assertion style at :791.
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest --run src/components/DatasetsPage.spec.tsx src/styles/theme-guard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep in DatasetsPage.tsx: `import ColumnFormatEditorModal from "./ColumnFormatEditorModal"` present.
    - grep: the exact string `Format columns` present as a button label.
    - grep: `<ColumnFormatEditorModal` mounted with `table={table}` and `onClose=`.
    - grep: `showFormatEditor` state flag + `setShowFormatEditor(true)` on the button click and `setShowFormatEditor(false)` in onClose.
    - DatasetsPage.spec.tsx exists; asserts the button renders on TableDetail, the modal is absent before click and present after, and the stub received `table` + `onClose` props.
    - grep: NO raw hex added to DatasetsPage.tsx — `grep -nE '#[0-9a-fA-F]{3,6}\b' src/components/DatasetsPage.tsx` returns nothing new (file currently has none).
    - `npx tsc --noEmit` clean; the spec + theme-guard pass; full `npx vitest --run` 100% from packages/web.
  </acceptance_criteria>
  <done>TableDetail shows a "Format columns" ghost-sm button that opens the ColumnFormatEditorModal for that table and closes back to the detail view; integration test proves the open flow + prop passing; tsc + theme-guard + full vitest green.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx vitest --run` is 100% green.
- `cd packages/web && npx tsc --noEmit` clean.
- theme-guard.spec.ts green (no new hex).
- `git diff --name-only` shows NO files under packages/server.
- The editor is reachable: View a table → "Format columns" → ColumnFormatEditorModal opens (COLEDIT-V115-01 entry point closed).
</verification>

<success_criteria>
- COLEDIT-V115-01 (entry point): the per-table Column Formatting editor is reachable from the Tables area via a "Format columns" button on TableDetail; clicking it opens the editor for that table.
- FRONTEND-ONLY; no server diff; no Phase 77 render-surface application.
</success_criteria>

<output>
After completion, create `.planning/phases/76-column-formatting-editor-ui/76-02-SUMMARY.md`.
</output>
