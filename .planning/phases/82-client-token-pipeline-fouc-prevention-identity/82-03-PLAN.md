---
phase: 82-client-token-pipeline-fouc-prevention-identity
plan: 03
type: execute
wave: 2
depends_on: ["82-01"]
files_modified:
  - packages/web/src/assets/logo-default.svg
  - packages/web/src/components/Sidebar.tsx
  - packages/web/src/components/Sidebar.spec.tsx
  - packages/web/src/components/LoginPage.tsx
autonomous: true
requirements: [BRANDUI-01]

must_haves:
  truths:
    - "The sidebar (expanded) renders the brand logo as an <img>; when no custom logoUrl is set it renders a bundled default logo asset"
    - "The login page (both OIDC and password branches) shows the brand appName, falling back to 'Kinetica BI' when none"
    - "No 'Kinetica BI' literal remains in Sidebar.tsx or LoginPage.tsx (it comes from the brand store / default fallback only)"
    - "The browser tab title reflects the app name (set by brandStore.bootstrap in 82-01; this plan keeps the static index.html <title> as the cold-cache fallback)"
    - "The Topbar is NOT given a logo this phase"
    - "The logo is always an <img> element, never inline SVG / dangerouslySetInnerHTML"
  artifacts:
    - path: "packages/web/src/assets/logo-default.svg"
      provides: "Bundled default Kinetica logo, imported as a Vite asset URL for the no-custom-logo fallback"
      contains: "<svg"
    - path: "packages/web/src/components/Sidebar.tsx"
      provides: "Logo <img> (custom logoUrl or default) + appName alt text, expanded state only"
      contains: "useBrandStore"
    - path: "packages/web/src/components/LoginPage.tsx"
      provides: "appName-driven brand text in both auth branches"
      contains: "useBrandStore"
  key_links:
    - from: "packages/web/src/components/Sidebar.tsx"
      to: "useBrandStore (logoUrl, appName)"
      via: "selector reads + <img src={logoUrl ?? DEFAULT_LOGO}>"
      pattern: "useBrandStore"
    - from: "packages/web/src/components/LoginPage.tsx"
      to: "useBrandStore (appName)"
      via: "appName ?? 'Kinetica BI' in both branches"
      pattern: "appName"
---

<objective>
Wire app identity to the brand store: replace every hardcoded "Kinetica BI" string in `Sidebar.tsx` and `LoginPage.tsx` with brand-store-driven values, render the logo as an `<img>` (custom `logoUrl` or a new bundled default asset) in the sidebar (expanded) and login page, create the default logo asset, and update `Sidebar.spec.tsx` (which asserts the literal "Kinetica BI" at 2 sites). `document.title` is already set by `brandStore.bootstrap()` (82-01); the static `index.html <title>` stays as the cold-cache fallback.

Purpose: BRANDUI-01 — app name + brand logo are used in the sidebar, login, and favicon (favicon handled in 82-01/82-02). Depends on 82-01 (the brand store + `logoUrl`/`appName` selectors must exist). Topbar is intentionally excluded this phase per locked CONTEXT decision.
Output: new `logo-default.svg`; edited `Sidebar.tsx`, `Sidebar.spec.tsx`, `LoginPage.tsx`.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-client-token-pipeline-fouc-prevention-identity/82-RESEARCH.md
@.planning/phases/82-client-token-pipeline-fouc-prevention-identity/82-CONTEXT.md
@.planning/phases/82-client-token-pipeline-fouc-prevention-identity/82-01-SUMMARY.md

<interfaces>
useBrandStore (from 82-01, packages/web/src/store/brandStore.ts):
- selector `useBrandStore((s) => s.appName)` -> string | null
- selector `useBrandStore((s) => s.logoUrl)` -> string | null
- test seeding: `useBrandStore.setState({ appName: "Acme", logoUrl: "/api/branding/logo?v=1" })`

Current "Kinetica BI" sites (CONFIRMED):
- Sidebar.tsx:45  ->  {!collapsed && <div className="logo">Kinetica BI</div>}
- LoginPage.tsx:27 (OIDC branch)  ->  <div className="login-brand">Kinetica BI</div>
- LoginPage.tsx:61 (password branch)  ->  <div className="login-brand">Kinetica BI</div>
- index.html:6  ->  <title>Kinetica BI</title>  (LEAVE AS-IS — cold-cache fallback; runtime overridden by brandStore.bootstrap)

Sidebar.spec.tsx literal assertions to update (CONFIRMED):
- line ~57: expect(screen.getByText("Kinetica BI")).toBeInTheDocument();  (in "renders the logo when expanded")
- line ~62: expect(screen.queryByText("Kinetica BI")).toBeNull();        (in "hides the logo when collapsed")
Spec seeds auth via seedAnalystStore/seedAdminStore from ../test/seedAuthStore and resets in beforeEach.

theme-guard: Sidebar.tsx + LoginPage.tsx are .tsx scanned for raw hex — introduce NO hex literals. logo-default.svg is NOT scanned (.svg, and lives in src/assets/ outside the components dir).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the bundled default logo asset</name>
  <files>packages/web/src/assets/logo-default.svg</files>
  <read_first>
    - 82-RESEARCH.md "Default logo asset" notes (lines 306-324, 665-672 — no src/assets dir exists; SVG imported as a Vite asset URL; format/dimensions are Claude's discretion)
    - 82-CONTEXT.md "Logo fallback + placement" (default bundled asset rendered when no custom logo; always <img>)
  </read_first>
  <action>
    Create directory `packages/web/src/assets/` and file `packages/web/src/assets/logo-default.svg`: a simple, self-contained Kinetica wordmark/mark SVG, roughly 40x24 (viewBox), single-color or use `currentColor` so it reads on the sidebar surface. Keep it small and dependency-free (no external refs, no embedded raster). This is the fallback shown when no custom logo is uploaded. It will be imported as a Vite asset URL: `import DEFAULT_LOGO from "../assets/logo-default.svg";` (returns a URL string). It is rendered via `<img src>` only — never inlined.
  </action>
  <verify>
    <automated>test -f packages/web/src/assets/logo-default.svg && head -1 packages/web/src/assets/logo-default.svg | grep -q "svg" && echo SVG_OK</automated>
  </verify>
  <acceptance_criteria>
    - `packages/web/src/assets/logo-default.svg` exists and contains `<svg`
    - file is self-contained (no `xlink:href` to external resources, no `<image href=`)
  </acceptance_criteria>
  <done>A bundled default logo SVG exists in src/assets/ ready to import as a Vite asset URL.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Sidebar logo + LoginPage appName to useBrandStore</name>
  <files>packages/web/src/components/Sidebar.tsx, packages/web/src/components/LoginPage.tsx</files>
  <read_first>
    - packages/web/src/components/Sidebar.tsx (FULL — line 45 logo div inside the `{!collapsed && ...}` of `.sidebar-header`; existing useAuthStore selector pattern)
    - packages/web/src/components/LoginPage.tsx (FULL — line 27 OIDC `.login-brand`, line 61 password `.login-brand`; existing useAuthStore selectors)
    - 82-RESEARCH.md "Identity Wiring Checklist" (lines 651-672) + "Logo rendering pattern for sidebar" (lines 306-322)
    - 82-01-SUMMARY.md (confirm the useBrandStore selector names: appName, logoUrl)
  </read_first>
  <action>
    1. `Sidebar.tsx`:
       - Add imports: `import { useBrandStore } from "../store/brandStore";` and `import DEFAULT_LOGO from "../assets/logo-default.svg";`
       - Inside the component, add selectors: `const logoUrl = useBrandStore((s) => s.logoUrl);` and `const appName = useBrandStore((s) => s.appName);`
       - Replace line 45 `{!collapsed && <div className="logo">Kinetica BI</div>}` with:
         ```tsx
         {!collapsed && (
           <div className="logo">
             <img src={logoUrl ?? DEFAULT_LOGO} alt={appName ?? "Kinetica BI"} className="logo-img" />
           </div>
         )}
         ```
       - Keep the existing `.logo` className so current sidebar styling still applies; add a `.logo-img` class for the image (CSS sizing can be added in global.css/sidebar styles if needed — a simple `max-height`/`width:auto` is fine; do NOT introduce hex literals).

    2. `LoginPage.tsx`:
       - Add import: `import { useBrandStore } from "../store/brandStore";`
       - Add selector near the other store reads: `const appName = useBrandStore((s) => s.appName);`
       - Replace BOTH `.login-brand` lines (27 OIDC, 61 password): `<div className="login-brand">Kinetica BI</div>` -> `<div className="login-brand">{appName ?? "Kinetica BI"}</div>`

    Logo is ALWAYS an `<img>` — never inline SVG / `dangerouslySetInnerHTML`. Do NOT add a Topbar logo (locked OUT). Do NOT change `document.title` here (brandStore.bootstrap from 82-01 owns it). Introduce no raw hex (theme-guard scans these .tsx files).
  </action>
  <verify>
    <automated>cd packages/web && grep -c "Kinetica BI" src/components/Sidebar.tsx; grep -c "useBrandStore" src/components/Sidebar.tsx; grep -c "Kinetica BI" src/components/Topbar.tsx; npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Kinetica BI" packages/web/src/components/Sidebar.tsx` == 0
    - `Sidebar.tsx` contains `useBrandStore`, `DEFAULT_LOGO`, and `<img` with `alt={appName`
    - `Sidebar.tsx` contains NO `dangerouslySetInnerHTML`
    - `LoginPage.tsx` contains `useBrandStore` and `{appName ?? "Kinetica BI"}` (2 occurrences of the fallback expression)
    - `grep -c "Kinetica BI" packages/web/src/components/Topbar.tsx` == 0 (Topbar still has no brand string — left unchanged)
    - `cd packages/web && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Sidebar renders a logo <img> (custom or default) with appName alt; LoginPage shows appName in both branches; no "Kinetica BI" literal in either component; Topbar untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Update Sidebar.spec.tsx for the logo <img></name>
  <files>packages/web/src/components/Sidebar.spec.tsx</files>
  <read_first>
    - packages/web/src/components/Sidebar.spec.tsx (FULL — the two tests at ~lines 55-63 asserting the "Kinetica BI" text; the beforeEach reset; the renderSidebar helper; the seedAuthStore import)
    - the edited Sidebar.tsx from Task 2 (the <img alt={appName ?? "Kinetica BI"}> + DEFAULT_LOGO fallback)
    - 82-RESEARCH.md "Sidebar.spec.tsx — required spec ripple" (lines 295-304)
  </read_first>
  <action>
    Edit `packages/web/src/components/Sidebar.spec.tsx`:
    - Add `import { useBrandStore } from "../store/brandStore";` at the top.
    - In `beforeEach`, after the existing auth reset, seed the brand store to a known state so the logo <img> has a deterministic alt:
      `useBrandStore.setState({ appName: "Kinetica BI", logoUrl: null });`
      (logoUrl null exercises the DEFAULT_LOGO fallback; alt stays "Kinetica BI" so role-name queries are stable.)
    - Rewrite the test "renders the logo when expanded" (was `getByText("Kinetica BI")`): assert the image is present:
      `expect(screen.getByRole("img", { name: "Kinetica BI" })).toBeInTheDocument();`
    - Rewrite the test "hides the logo when collapsed" (was `queryByText("Kinetica BI")` null): assert the image is absent:
      `expect(screen.queryByRole("img")).toBeNull();`
    - Optionally add one assertion that with a custom logoUrl seeded, the `<img>` src reflects it (`useBrandStore.setState({ logoUrl: "/api/branding/logo?v=9", appName: "Acme" })` -> `getByRole("img", { name: "Acme" })` has that src). Keep existing nav/collapse/footer/gated tests unchanged.

    Do NOT keep any `getByText("Kinetica BI")` / `queryByText("Kinetica BI")` assertion — those now fail because the text node is gone (replaced by an <img>).
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run --reporter=dot src/components/Sidebar.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `packages/web/src/components/Sidebar.spec.tsx` exits 0
    - `Sidebar.spec.tsx` contains `useBrandStore` and `getByRole("img"` 
    - `grep -c "getByText(\"Kinetica BI\")" packages/web/src/components/Sidebar.spec.tsx` == 0
    - `grep -c "queryByText(\"Kinetica BI\")" packages/web/src/components/Sidebar.spec.tsx` == 0
  </acceptance_criteria>
  <done>Sidebar.spec.tsx seeds the brand store and asserts the logo <img> presence/absence by role instead of the removed "Kinetica BI" text; spec green.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx vitest run` → 100% green (Sidebar.spec.tsx updated + passing; no other regressions)
- `cd packages/web && npx tsc --noEmit` → clean
- `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` → green (no raw hex in Sidebar.tsx/LoginPage.tsx; .svg not scanned)
- `grep -rc "Kinetica BI" packages/web/src/components/Sidebar.tsx packages/web/src/components/LoginPage.tsx packages/web/src/components/Topbar.tsx` → all 0
- Manual/UAT-only (Phase 84): real favicon render in the browser tab (not jsdom-testable).
</verification>

<success_criteria>
- BRANDUI-01: app name + logo flow from the brand store into sidebar + login; logo is an <img> (custom or bundled default); favicon (from 82-01/82-02) reflects the logo.
- Topbar excluded (locked); document.title owned by brandStore.bootstrap (82-01); static index.html <title> retained as cold-cache fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/82-client-token-pipeline-fouc-prevention-identity/82-03-SUMMARY.md` noting the default-logo asset path, the 3 edited identity sites, the Sidebar.spec update, and that Topbar was intentionally left unchanged.
</output>
