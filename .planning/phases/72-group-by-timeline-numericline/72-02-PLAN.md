---
phase: 72-group-by-timeline-numericline
plan: 02
type: execute
wave: 2
depends_on: ["72-01"]
files_modified:
  - packages/web/src/components/charts/TimelineConfigPanel.tsx
  - packages/web/src/components/charts/TimelineRenderer.tsx
  - packages/web/src/components/charts/TimelineConfigPanel.spec.tsx
  - packages/web/src/components/charts/TimelineRenderer.spec.tsx
autonomous: true
requirements: [GROUP-V114-01, GROUP-V114-03, GROUP-V114-04]
must_haves:
  truths:
    - "Operator can set an optional Group By dimension on the Timeline chart, and clearing it (None) returns to ungrouped multi-metric behavior"
    - "Selecting a Group By collapses the metrics builder to a single metric row (no Add metric button); clearing restores 1-4 metrics non-destructively"
    - "When grouped, the timeline renders one color-coded Line per group value on a single shared Y-axis, capped at MAX_SERIES (12) with a top-12-of-N affordance"
    - "When grouped, timeline drag-to-filter still dispatches a BETWEEN filter on timeCol"
    - "Ungrouped multi-metric behavior is unchanged (regression lock)"
  artifacts:
    - path: packages/web/src/components/charts/TimelineConfigPanel.tsx
      provides: "groupByColumn field + Group By picker + single-metric-when-grouped enforcement"
      contains: "groupByColumn"
    - path: packages/web/src/components/charts/TimelineRenderer.tsx
      provides: "grouped single-query pivot + N-series Line rendering + top-N cap affordance"
      contains: "groupByColumn"
  key_links:
    - from: packages/web/src/components/charts/TimelineRenderer.tsx
      to: packages/web/src/lib/buildTimelineSql.ts
      via: "buildTimelineSql({ ..., groupByColumn, seriesIn })"
      pattern: "groupByColumn"
    - from: packages/web/src/components/charts/TimelineRenderer.tsx
      to: packages/web/src/lib/groupedSeries.ts
      via: "selectTopSeries / pivotSeriesRows / MAX_SERIES"
      pattern: "from \"\\.\\./\\.\\./lib/groupedSeries\""
    - from: packages/web/src/components/charts/TimelineRenderer.tsx
      to: packages/web/src/store/filterStore.ts
      via: "setBulkFilters BETWEEN on timeCol (drag-to-filter preserved when grouped)"
      pattern: "operator: \"between\""
---

<objective>
Add the optional group-by dimension to the Timeline chart: a "Group By" picker in `TimelineConfigPanel` (with single-metric-when-grouped enforcement) and grouped N-series rendering in `TimelineRenderer` (single grouped query → top-N pivot → one Line per series, shared Y-axis, drag-to-filter preserved).

Purpose: Closes GROUP-V114-01 (Timeline picker), GROUP-V114-03 (UI-enforced single-metric mutual exclusion), and the Timeline half of GROUP-V114-04 (grouped SQL + N-series render + top-N cap).

Output: Updated TimelineConfigPanel + TimelineRenderer with grouped behavior, building on the Wave-1 builder/helper contract; updated specs locking grouped, mutual-exclusion, drag-when-grouped, and ungrouped-regression behavior.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-group-by-timeline-numericline/72-CONTEXT.md
@.planning/phases/72-group-by-timeline-numericline/72-01-SUMMARY.md

<interfaces>
<!-- Wave-1 contract (72-01) the renderer builds against. Use directly. -->

From packages/web/src/lib/buildTimelineSql.ts (post-72-01):
```typescript
export type BuildTimelineSqlArgs = {
  schema: string; table: string; timeCol: string;
  metric: TimelineMetric; interval: TimelineInterval; maxIntervals: number;
  groupByColumn?: string;        // grouped when non-empty
  seriesIn?: (string | number)[]; // top-N IN-filter for the main grouped query
};
```

From packages/web/src/lib/groupedSeries.ts (post-72-01):
```typescript
export const MAX_SERIES = 12;
export type SeriesTotalRow = { series: string; value: number };
export type GroupedRow = { bucket: string; series: string; value: number | null };
export function selectTopSeries(rows: SeriesTotalRow[], opts?: { max?: number }):
  { series: string[]; truncated: boolean; total: number };
export function pivotSeriesRows(rows: GroupedRow[], seriesValues: string[], opts?: { numericBuckets?: boolean }):
  Record<string, number | string | null>[];
```

From packages/web/src/components/charts/TimelineConfigPanel.tsx (current):
```typescript
export const MAX_METRICS = 4;
export const DEFAULT_COLOR_THEME = "Set2";
export type TimelineConfig = { tableId?, tableRef?, dynamicViewId?, timeCol, metrics, maxIntervals, showLegend, showTooltip, vertical, colorTheme, dateFormatOverride };
// drillDown-safe / categorical column filtering: isColumnDrillDownSafe + inferDataTypeFromColumn (../../lib/columnTypes)
```

Existing renderer grouped-by-column eligibility pattern (mirror): ChartConfigPanel uses `isColumnDrillDownSafe(c.type)` for its group-by picker (ChartConfigPanel.tsx:262-265).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add groupByColumn + Group By picker + single-metric-when-grouped to TimelineConfigPanel</name>
  <files>packages/web/src/components/charts/TimelineConfigPanel.tsx, packages/web/src/components/charts/TimelineConfigPanel.spec.tsx</files>
  <read_first>
    - packages/web/src/components/charts/TimelineConfigPanel.tsx (full file — 423 lines; note the Metrics builder + Add-metric button at lines ~271-355 and the handlers ~143-202)
    - packages/web/src/components/charts/TimelineConfigPanel.spec.tsx (existing assertions — may assert Add-metric always present; migrate any that break)
    - packages/web/src/lib/columnTypes.ts (isColumnDrillDownSafe at :76, inferDataTypeFromColumn at :85)
    - packages/web/src/components/charts/ChartConfigPanel.tsx:259-265 (group-by column eligibility precedent)
    - packages/web/src/phases/72-group-by-timeline-numericline/72-CONTEXT.md → <decisions> "Single metric when grouped" + "Config panel: Group By picker"
  </read_first>
  <behavior>
    - `groupByColumn?: string` added to TimelineConfig (default "").
    - A "Group By (optional)" single-select appears after the Time column field, with an explicit "None" option (value ""). Options = columns eligible for grouping (drill-down-safe; mirror ChartConfigPanel's `isColumnDrillDownSafe`), excluding the selected timeCol.
    - When groupByColumn is non-empty: the Metrics section shows EXACTLY ONE metric row, the "+ Add metric" button is HIDDEN (not just disabled), and the Remove button on the single row is hidden. The grouped state must be NON-DESTRUCTIVE: metrics[1..3] stay in config (not deleted) so clearing restores them.
    - When groupByColumn is cleared back to "": the full 1-4 multi-metric builder (incl. Add metric) is restored with the previously-configured metrics intact.
    - If grouping is enabled while 0 metrics configured, seed metrics[0] (so a single row renders).
    - Form validity when grouped: requires tableId + timeCol + groupByColumn-eligible + metrics[0] complete (column + aggregation).
  </behavior>
  <action>
    Add `groupByColumn?: string` to the `TimelineConfig` type. Read `const groupByColumn = cfg.groupByColumn ?? ""` and `const grouped = groupByColumn !== ""`.

    Build a `groupByColumns` useMemo: `Object.entries(columns).filter(([name, type]) => name !== timeCol && isColumnDrillDownSafe(type)).map(([name]) => name)` (categorical-friendly dimensions; mirror ChartConfigPanel). Render a new `ds-field` "Group By (optional)" select right after the Time column field — aria-label "Group by", first option `<option value="">None</option>`, then groupByColumns. onChange → `handleGroupByChange`.

    `handleGroupByChange(newCol)`: `patch({ groupByColumn: newCol })`. Do NOT mutate metrics array on enable (non-destructive) — but if enabling (`newCol !== ""`) and `metrics.length === 0`, seed one default metric (reuse handleAddMetric's color logic).

    In the Metrics render: when `grouped`, render only `metrics.slice(0,1)` (or seed if empty), hide the Remove button on that row, and HIDE the "+ Add metric" button (wrap it in `{!grouped && (...)}`). Update the METRICS header to read e.g. `METRIC (grouped by ${groupByColumn})` when grouped vs `METRICS (max ${MAX_METRICS})` when not. When NOT grouped, behavior is exactly as today.

    Update `formValid`: when grouped, require `metrics.length >= 1 && metrics[0].column !== "" && metrics[0].aggregation` and ignore metrics[1..]; when ungrouped, the existing rule.

    Handle the table-change reset: in `handleTableChange`, also clear `groupByColumn: ""` (old group col won't exist on the new schema). In `handleTimeColChange`, if the new timeCol equals the current groupByColumn, clear groupByColumn.

    STALE-TEST MIGRATION: grep TimelineConfigPanel.spec.tsx for assertions like "Add metric" always rendered or fixed metric-row counts. Migrate any that break under the grouped path (add the grouped context to them or split into grouped/ungrouped cases). Keep all ungrouped assertions passing unchanged. The FULL spec file must exit 0 (see Task 2 verify).
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/TimelineConfigPanel.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "groupByColumn" packages/web/src/components/charts/TimelineConfigPanel.tsx` ≥ 3 (type + read + picker).
    - `grep "aria-label=\"Group by\"" packages/web/src/components/charts/TimelineConfigPanel.tsx` matches; the picker has a `value=""` None option.
    - `grep "!grouped" packages/web/src/components/charts/TimelineConfigPanel.tsx` matches (Add-metric hidden when grouped).
    - No `metrics: metrics.filter` or `metrics: []` introduced in handleGroupByChange (non-destructive — clearing restores metrics[1..3]).
    - `npx vitest run src/components/charts/TimelineConfigPanel.spec.tsx` exits 0.
  </acceptance_criteria>
  <done>Group By picker present with None option; enabling collapses to single metric + hides Add-metric non-destructively; clearing restores 1-4; spec green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Grouped N-series rendering in TimelineRenderer (single query, top-N pivot, shared Y-axis, drag preserved)</name>
  <files>packages/web/src/components/charts/TimelineRenderer.tsx, packages/web/src/components/charts/TimelineRenderer.spec.tsx</files>
  <read_first>
    - packages/web/src/components/charts/TimelineRenderer.tsx (full file — 493 lines; pipeline lines 194-301, dep key line 295-301, drag/commitFilter 303-323, multi-axis render 420-465)
    - packages/web/src/components/charts/TimelineRenderer.spec.tsx (existing assertions — migrate any that assume per-metric-only render)
    - packages/web/src/lib/groupedSeries.ts (selectTopSeries / pivotSeriesRows / MAX_SERIES — post-72-01)
    - packages/web/src/lib/buildTimelineSql.ts (grouped signature — post-72-01)
    - packages/web/src/lib/cbColorThemes.ts (getCbColorTheme + themeColorsFor — cycle across series, NO raw hex)
    - packages/web/src/phases/72-group-by-timeline-numericline/72-CONTEXT.md → <decisions> "Renderer: series pivot + N-series rendering"
  </read_first>
  <behavior>
    - Ungrouped (no cfg.groupByColumn): pipeline UNCHANGED — N parallel per-metric queries, multi-axis render, exactly as today (regression lock).
    - Grouped (cfg.groupByColumn non-empty): single metric (metrics[0]). Pipeline:
      1. Range probe (unchanged) → pickInterval.
      2. Top-N pre-query: `SELECT <groupByCol> AS series, <agg(metrics[0])> AS value FROM <fromTarget> WHERE <timeCol> IS NOT NULL AND <groupByCol> IS NOT NULL GROUP BY series ORDER BY value DESC LIMIT <generous>` (build via a small inline query or buildTimelineSql with a series-only mode — see action). Feed its decoded rows to `selectTopSeries` → { series, truncated, total }.
      3. Main grouped query: `buildTimelineSql({ ..., groupByColumn, seriesIn: topSeries })` → decode → `pivotSeriesRows(rows, topSeries)` → Recharts rows keyed by series value.
    - Render: one `<Line dataKey={seriesValue}>` per series, ALL on a single shared Y-axis (yAxisId omitted/shared — NOT the alternating per-metric axes). Stroke colors cycle the colorTheme ramp via `themeColorsFor(getCbColorTheme(colorTheme), seriesCount)` — NO raw hex literals. Legend shows series values; tooltip shows series + bucket + value.
    - When `truncated`, render a small "Showing top {MAX_SERIES} of {total}" note (use a theme token / existing class, no raw hex).
    - Drag-to-filter: the existing onMouseDown/Move/Up + commitFilter(BETWEEN on timeCol) MUST still fire when grouped (band operates on the bucket axis, series-independent). Persistent appliedBand on the shared axis.
    - Effect dep key MUST include groupByColumn (add to the deps array at ~295-301) so toggling group-by re-fetches.
  </behavior>
  <action>
    Read `const groupByColumn = cfg.groupByColumn ?? ""` and `const grouped = groupByColumn !== ""`. When grouped, force `metrics` to `metrics.slice(0,1)`.

    In the fetch effect, branch on `grouped`:
    - Top-N pre-query: build it (either extend buildTimelineSql usage or a tiny local string using the same `aggExpr` shape — prefer reusing the builder; if the builder doesn't support a series-only/no-bucket mode, write a minimal inline `SELECT ${groupByColumn} AS series, ${agg} AS value FROM ${fromTarget} WHERE ${timeCol} IS NOT NULL AND ${groupByColumn} IS NOT NULL GROUP BY series ORDER BY value DESC LIMIT ${MAX_SERIES * 4}`). Decode → map to `{ series: String(r.series), value: Number(r.value) }` → `selectTopSeries(...)`.
    - Main query: `buildTimelineSql({ schema: querySchema, table: queryTable, timeCol, metric: metrics[0], interval: chosen, maxIntervals, groupByColumn, seriesIn: top.series })`. Decode → `pivotSeriesRows(rows.map(r => ({ bucket: String(r.bucket), series: String(r.series), value: <number|null> })), top.series)` → setData. Store `top.series` (ordered) + `{ truncated, total }` in state for render.
    - Keep the FROM-swap / filter-view / dv resolution logic exactly as today (the `EXTRACT(EPOCH FROM ...)` FROM-clobber gotcha note still applies — pass the view name as an unprefixed table; do NOT regex-swap).

    Render branch:
    - Grouped: a single `<YAxis type="number" />` (no alternating per-metric axes), and `top.series.map((sv, i) => <Line key={sv} dataKey={sv} stroke={toCssColor(seriesColors[i])} ... name={sv} connectNulls={false} isAnimationActive={false} />)` where `seriesColors = themeColorsFor(getCbColorTheme(colorTheme) ?? getCbColorTheme(DEFAULT_COLOR_THEME), top.series.length)`. Legend always shown when grouped (series matter). Render the truncated note when `truncated`.
    - Ungrouped: the existing multi-axis block, untouched.
    - Drag handlers + ReferenceArea (transient + appliedBand) stay outside the grouped/ungrouped branch so drag-to-filter works in both; for grouped, bind the band to the shared yAxisId.

    Add `groupByColumn` to the effect dep array (~line 295-301).

    Keep the SOLE-MATERIALIZE-TRIGGER invariant: do NOT import any materialize function. commitFilter stays setBulkFilters + markMaterializing only.

    STALE-TEST MIGRATION: grep TimelineRenderer.spec.tsx for assertions that lock per-metric line counts / multi-axis structure unconditionally. Wrap/split them into the ungrouped case and add grouped-case assertions. Full file must exit 0.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/TimelineRenderer.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "groupByColumn" packages/web/src/components/charts/TimelineRenderer.tsx` ≥ 3 (read + builder arg + dep key).
    - `grep -E "selectTopSeries|pivotSeriesRows" packages/web/src/components/charts/TimelineRenderer.tsx` matches both.
    - `grep "themeColorsFor" packages/web/src/components/charts/TimelineRenderer.tsx` matches AND `grep -nE "#[0-9a-fA-F]{6}" TimelineRenderer.tsx` introduces NO new raw-hex series colors (BAND_COLOR pre-exists; do not add others).
    - TimelineRenderer.tsx still does NOT import a materialize function: `grep -i "materialize" TimelineRenderer.tsx` shows only markMaterializing/comments, never an import of a materialize() call.
    - Spec includes a grouped test asserting one Line per series value + a top-N affordance + a drag-when-grouped BETWEEN dispatch; `npx vitest run src/components/charts/TimelineRenderer.spec.tsx` exits 0.
  </acceptance_criteria>
  <done>Grouped timeline renders one Line per top-N series on a shared Y-axis with theme-ramp colors + top-12-of-N note; drag-to-filter BETWEEN on timeCol preserved; ungrouped path unchanged; specs green.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx vitest run src/components/charts/TimelineConfigPanel.spec.tsx src/components/charts/TimelineRenderer.spec.tsx` exits 0.
- `cd packages/web && npx vitest run src/lib/chartTheme` theme-guard (or the theme-guard.spec.ts path) green — no raw hex introduced.
- `cd packages/web && npx tsc --noEmit` clean.
- No server diff (`git status packages/server` empty).
</verification>

<success_criteria>
- Operator sets/clears a Group By on the Timeline; clearing returns to ungrouped multi-metric (GROUP-V114-01).
- Group-by set ⇒ exactly one metric, Add-metric hidden, non-destructive on clear (GROUP-V114-03).
- Grouped SQL emits GROUP BY bucket, series; renderer plots one theme-colored Line per series on a shared Y-axis, capped at 12 with a top-12-of-N affordance; drag-to-filter BETWEEN on timeCol preserved (GROUP-V114-04 timeline half).
- Ungrouped behavior regression-locked; theme-guard green; web tsc clean.
</success_criteria>

<output>
After completion, create `.planning/phases/72-group-by-timeline-numericline/72-02-SUMMARY.md`
</output>
