---
phase: 72-group-by-timeline-numericline
plan: 03
type: execute
wave: 2
depends_on: ["72-01"]
files_modified:
  - packages/web/src/components/charts/NumericLineConfigPanel.tsx
  - packages/web/src/components/charts/NumericLineRenderer.tsx
  - packages/web/src/components/charts/NumericLineConfigPanel.spec.tsx
  - packages/web/src/components/charts/NumericLineRenderer.spec.tsx
autonomous: true
requirements: [GROUP-V114-02, GROUP-V114-03, GROUP-V114-04]
must_haves:
  truths:
    - "Operator can set an optional Group By dimension on the Numeric-Line chart, and clearing it (None) returns to ungrouped multi-metric behavior"
    - "Selecting a Group By collapses the metrics builder to a single metric row (no Add metric button); clearing restores 1-4 metrics non-destructively"
    - "When grouped, the numeric-line renders one color-coded Line per group value on a single shared Y-axis, capped at MAX_SERIES (12) with a top-12-of-N affordance"
    - "When grouped, numeric-line x-interaction (drag-to-filter BETWEEN on xField) is preserved"
    - "Ungrouped multi-metric behavior is unchanged (regression lock)"
  artifacts:
    - path: packages/web/src/components/charts/NumericLineConfigPanel.tsx
      provides: "groupByColumn field + Group By picker + single-metric-when-grouped enforcement"
      contains: "groupByColumn"
    - path: packages/web/src/components/charts/NumericLineRenderer.tsx
      provides: "grouped single-query pivot + N-series Line rendering + top-N cap affordance"
      contains: "groupByColumn"
  key_links:
    - from: packages/web/src/components/charts/NumericLineRenderer.tsx
      to: packages/web/src/lib/buildNumericLineSql.ts
      via: "buildNumericLineSql({ ..., groupByColumn, seriesIn })"
      pattern: "groupByColumn"
    - from: packages/web/src/components/charts/NumericLineRenderer.tsx
      to: packages/web/src/lib/groupedSeries.ts
      via: "selectTopSeries / pivotSeriesRows (numericBuckets) / MAX_SERIES"
      pattern: "from \"\\.\\./\\.\\./lib/groupedSeries\""
    - from: packages/web/src/components/charts/NumericLineRenderer.tsx
      to: packages/web/src/store/filterStore.ts
      via: "setBulkFilters BETWEEN on xField (drag-to-filter preserved when grouped)"
      pattern: "operator: \"between\""
---

<objective>
Add the optional group-by dimension to the Numeric-Line chart — symmetric with the Timeline (Plan 02): a "Group By" picker in `NumericLineConfigPanel` (single-metric-when-grouped) and grouped N-series rendering in `NumericLineRenderer` (single grouped query → top-N pivot with NUMERIC bucket sort → one Line per series, shared Y-axis, drag-to-filter preserved).

Purpose: Closes GROUP-V114-02 (Numeric-Line picker), GROUP-V114-03 (UI-enforced single-metric mutual exclusion, numeric-line half), and the Numeric-Line half of GROUP-V114-04 (grouped SQL + N-series render + top-N cap).

Output: Updated NumericLineConfigPanel + NumericLineRenderer building on the Wave-1 builder/helper contract; updated specs locking grouped, mutual-exclusion, drag-when-grouped, and ungrouped-regression behavior.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-group-by-timeline-numericline/72-CONTEXT.md
@.planning/phases/72-group-by-timeline-numericline/72-01-SUMMARY.md

<interfaces>
<!-- Wave-1 contract (72-01) the renderer builds against. Use directly. -->

From packages/web/src/lib/buildNumericLineSql.ts (post-72-01):
```typescript
export type BuildNumericLineSqlArgs = {
  schema: string; table: string; xField: string;
  binWidth: number; metric: NumericMetric; maxBuckets: number;
  groupByColumn?: string;        // grouped when non-empty
  seriesIn?: (string | number)[]; // top-N IN-filter for the main grouped query
};
```

From packages/web/src/lib/groupedSeries.ts (post-72-01):
```typescript
export const MAX_SERIES = 12;
export function selectTopSeries(rows, opts?): { series: string[]; truncated: boolean; total: number };
export function pivotSeriesRows(rows, seriesValues, opts?: { numericBuckets?: boolean }): Record<string, number|string|null>[];
// numeric-line MUST pass { numericBuckets: true } so buckets sort numerically (mirrors NumericLineRenderer's `.sort((a,b)=>Number(a)-Number(b))`)
```

From packages/web/src/components/charts/NumericLineConfigPanel.tsx (current):
```typescript
export type NumericLineConfig = { tableId?, tableRef?, dynamicViewId?, xField, metrics, maxBuckets, showLegend, showTooltip, vertical, colorTheme };
// imports MAX_METRICS, DEFAULT_COLOR_THEME from ./TimelineConfigPanel (read-only — Plan 02 does not change those exports)
// column filtering: inferDataTypeFromColumn + isColumnDrillDownSafe (../../lib/columnTypes)
```

NOTE: This plan does NOT modify TimelineConfigPanel.tsx or TimelineRenderer.tsx. It only re-uses the `MAX_METRICS` / `DEFAULT_COLOR_THEME` exports (unchanged by Plan 02), so Plans 02 and 03 are parallel-safe (no shared file writes).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add groupByColumn + Group By picker + single-metric-when-grouped to NumericLineConfigPanel</name>
  <files>packages/web/src/components/charts/NumericLineConfigPanel.tsx, packages/web/src/components/charts/NumericLineConfigPanel.spec.tsx</files>
  <read_first>
    - packages/web/src/components/charts/NumericLineConfigPanel.tsx (full file — 388 lines; Metrics builder + Add-metric ~248-332; handlers ~132-180)
    - packages/web/src/components/charts/NumericLineConfigPanel.spec.tsx (existing assertions — migrate any that assume Add-metric always present)
    - packages/web/src/components/charts/TimelineConfigPanel.tsx (Plan 02's symmetric implementation — mirror its Group By picker + grouped/single-metric logic for parity)
    - packages/web/src/lib/columnTypes.ts (isColumnDrillDownSafe :76, inferDataTypeFromColumn :85)
    - packages/web/src/phases/72-group-by-timeline-numericline/72-CONTEXT.md → <decisions> "Single metric when grouped" + "Config panel: Group By picker"
  </read_first>
  <behavior>
    - `groupByColumn?: string` added to NumericLineConfig (default "").
    - A "Group By (optional)" single-select after the X-axis column field, with an explicit "None" option (value ""). Options = drill-down-safe columns (mirror ChartConfigPanel / TimelineConfigPanel parity), excluding the selected xField.
    - When groupByColumn non-empty: Metrics shows EXACTLY ONE row, "+ Add metric" HIDDEN, Remove on the single row hidden, metrics[1..3] preserved (non-destructive).
    - Clearing to "": full 1-4 builder restored with prior metrics intact.
    - Enabling with 0 metrics seeds metrics[0].
    - Form validity grouped: tableId + xField + groupByColumn + metrics[0] complete.
  </behavior>
  <action>
    Mirror Plan 02's TimelineConfigPanel changes for parity. Add `groupByColumn?: string` to `NumericLineConfig`. Read `const groupByColumn = cfg.groupByColumn ?? ""`, `const grouped = groupByColumn !== ""`.

    `groupByColumns` useMemo: `Object.entries(columns).filter(([name, type]) => name !== xField && isColumnDrillDownSafe(type)).map(([name]) => name)`. Render a "Group By (optional)" `ds-field` select right after the X-axis column field, aria-label "Group by", first option `<option value="">None</option>`, then options; onChange → `handleGroupByChange`.

    `handleGroupByChange(newCol)`: `patch({ groupByColumn: newCol })`; if `newCol !== ""` and `metrics.length === 0`, seed one default metric (reuse handleAddMetric color logic). Do NOT delete metrics on enable.

    Metrics render: when `grouped`, render `metrics.slice(0,1)` (seed if empty), hide Remove on that row, wrap the "+ Add metric" button in `{!grouped && (...)}`. Update the METRICS header text to indicate grouped-by-single-metric when grouped.

    Update `formValid`: grouped → require metrics[0] complete + groupByColumn; ungrouped → existing rule.

    Reset hooks: in `handleTableChange` also clear `groupByColumn: ""`; if a new xField equals the current groupByColumn, clear groupByColumn.

    STALE-TEST MIGRATION: grep NumericLineConfigPanel.spec.tsx for Add-metric-always / fixed-row-count assertions and migrate them to the ungrouped case; add grouped-case assertions. Keep ungrouped assertions green. FULL spec file exits 0.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/NumericLineConfigPanel.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "groupByColumn" packages/web/src/components/charts/NumericLineConfigPanel.tsx` ≥ 3.
    - `grep "aria-label=\"Group by\"" packages/web/src/components/charts/NumericLineConfigPanel.tsx` matches; picker has a `value=""` None option.
    - `grep "!grouped" packages/web/src/components/charts/NumericLineConfigPanel.tsx` matches (Add-metric hidden when grouped).
    - No `metrics: metrics.filter` / `metrics: []` in handleGroupByChange (non-destructive).
    - `npx vitest run src/components/charts/NumericLineConfigPanel.spec.tsx` exits 0.
  </acceptance_criteria>
  <done>Group By picker with None option; enabling collapses to single metric + hides Add-metric non-destructively; clearing restores 1-4; spec green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Grouped N-series rendering in NumericLineRenderer (single query, numeric-bucket pivot, shared Y-axis, drag preserved)</name>
  <files>packages/web/src/components/charts/NumericLineRenderer.tsx, packages/web/src/components/charts/NumericLineRenderer.spec.tsx</files>
  <read_first>
    - packages/web/src/components/charts/NumericLineRenderer.tsx (full file — 451 lines; pipeline 177-270, dep key 265-270, drag/commitFilter 272-291, multi-axis render 380-423)
    - packages/web/src/components/charts/NumericLineRenderer.spec.tsx (existing assertions — migrate any per-metric-only render locks)
    - packages/web/src/components/charts/TimelineRenderer.tsx (Plan 02's symmetric grouped pipeline — mirror it; numeric differences below)
    - packages/web/src/lib/groupedSeries.ts (selectTopSeries / pivotSeriesRows(numericBuckets:true) / MAX_SERIES)
    - packages/web/src/lib/buildNumericLineSql.ts (grouped signature — post-72-01)
    - packages/web/src/lib/cbColorThemes.ts (getCbColorTheme + themeColorsFor — series ramp, NO raw hex)
    - packages/web/src/phases/72-group-by-timeline-numericline/72-CONTEXT.md → <decisions> "Renderer: series pivot + N-series rendering"
  </read_first>
  <behavior>
    - Ungrouped: pipeline UNCHANGED — N parallel per-metric queries, numeric bucket sort, multi-axis render (regression lock).
    - Grouped (cfg.groupByColumn non-empty): single metric (metrics[0]). Pipeline:
      1. Numeric range probe (unchanged) → pickNumericBinWidth.
      2. Top-N pre-query: `SELECT <groupByCol> AS series, <agg(metrics[0])> AS value FROM <fromTarget> WHERE <xField> IS NOT NULL AND <groupByCol> IS NOT NULL GROUP BY series ORDER BY value DESC LIMIT <MAX_SERIES*4>` → decode → selectTopSeries.
      3. Main grouped query: `buildNumericLineSql({ ..., groupByColumn, seriesIn: top.series })` → decode → `pivotSeriesRows(rows, top.series, { numericBuckets: true })` (numeric bucket sort) → setData.
    - Render: one `<Line dataKey={seriesValue}>` per series on a single shared Y-axis; colors cycle `themeColorsFor(getCbColorTheme(colorTheme), seriesCount)` — NO raw hex. Legend = series values; tooltip = series + bucket + value. "Showing top {MAX_SERIES} of {total}" note when truncated (theme token, no raw hex).
    - Drag-to-filter: existing onMouseUp commitFilter (BETWEEN, dataType "number", NUMERIC ordering of bounds) MUST still fire when grouped. appliedBand on the shared axis.
    - Effect dep key MUST include groupByColumn (add at ~265-270).
  </behavior>
  <action>
    Mirror Plan 02's TimelineRenderer grouped pipeline, adapted to numeric specifics:
    - Read `const groupByColumn = cfg.groupByColumn ?? ""`, `const grouped = groupByColumn !== ""`; when grouped force `metrics.slice(0,1)`.
    - Branch the fetch effect on `grouped`. Top-N pre-query: reuse buildNumericLineSql if it supports a series-only/no-bucket mode, else a minimal inline `SELECT ${groupByColumn} AS series, ${agg} AS value FROM ${fromTarget} WHERE ${xField} IS NOT NULL AND ${groupByColumn} IS NOT NULL GROUP BY series ORDER BY value DESC LIMIT ${MAX_SERIES * 4}`. Decode → selectTopSeries.
    - Main query: `buildNumericLineSql({ schema: querySchema, table: queryTable, xField, binWidth: width, metric: metrics[0], maxBuckets, groupByColumn, seriesIn: top.series })`. Decode → `pivotSeriesRows(..., top.series, { numericBuckets: true })` → setData; store top.series + { truncated, total } in state.
    - Keep filter-view / dv FROM resolution exactly as today.
    - Render grouped: single `<YAxis type="number" />`, `top.series.map((sv,i) => <Line key={sv} dataKey={sv} stroke={toCssColor(seriesColors[i])} name={sv} connectNulls={false} isAnimationActive={false} />)` with `seriesColors = themeColorsFor(getCbColorTheme(colorTheme) ?? getCbColorTheme(DEFAULT_COLOR_THEME), top.series.length)`. Legend always when grouped; truncated note when truncated.
    - Render ungrouped: existing multi-axis block untouched.
    - Drag handlers + ReferenceArea stay shared so drag-to-filter works in both; grouped band binds the shared yAxisId.
    - Add `groupByColumn` to the effect dep array.
    - SOLE-MATERIALIZE-TRIGGER invariant preserved — no materialize import; commitFilter = setBulkFilters + markMaterializing only.

    STALE-TEST MIGRATION: grep NumericLineRenderer.spec.tsx for per-metric line-count / multi-axis locks; split into ungrouped case + add grouped-case assertions. Full file exits 0.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/NumericLineRenderer.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "groupByColumn" packages/web/src/components/charts/NumericLineRenderer.tsx` ≥ 3.
    - `grep -E "selectTopSeries|pivotSeriesRows" packages/web/src/components/charts/NumericLineRenderer.tsx` matches both AND `grep "numericBuckets" NumericLineRenderer.tsx` matches (numeric bucket sort wired).
    - `grep "themeColorsFor" packages/web/src/components/charts/NumericLineRenderer.tsx` matches AND no NEW raw-hex series color introduced (BAND_COLOR pre-exists only).
    - `grep -i "materialize" NumericLineRenderer.tsx` shows only markMaterializing/comments — never a materialize() import (sole-trigger invariant).
    - Spec includes a grouped test asserting one Line per series + top-N affordance + drag-when-grouped BETWEEN dispatch; `npx vitest run src/components/charts/NumericLineRenderer.spec.tsx` exits 0.
  </acceptance_criteria>
  <done>Grouped numeric-line renders one Line per top-N series on a shared Y-axis with theme-ramp colors + top-12-of-N note, numeric bucket sort; drag-to-filter BETWEEN on xField preserved; ungrouped path unchanged; specs green.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx vitest run src/components/charts/NumericLineConfigPanel.spec.tsx src/components/charts/NumericLineRenderer.spec.tsx` exits 0.
- theme-guard.spec.ts green — no raw hex introduced.
- `cd packages/web && npx tsc --noEmit` clean.
- No server diff (`git status packages/server` empty).
</verification>

<success_criteria>
- Operator sets/clears a Group By on the Numeric-Line; clearing returns to ungrouped multi-metric (GROUP-V114-02).
- Group-by set ⇒ exactly one metric, Add-metric hidden, non-destructive on clear (GROUP-V114-03, numeric-line half).
- Grouped SQL emits GROUP BY bucket, series; renderer plots one theme-colored Line per series on a shared Y-axis (numeric bucket sort), capped at 12 with a top-12-of-N affordance; drag-to-filter BETWEEN on xField preserved (GROUP-V114-04 numeric-line half).
- Ungrouped behavior regression-locked; theme-guard green; web tsc clean.
</success_criteria>

<output>
After completion, create `.planning/phases/72-group-by-timeline-numericline/72-03-SUMMARY.md`
</output>
