---
phase: 72-group-by-timeline-numericline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/lib/buildTimelineSql.ts
  - packages/web/src/lib/buildNumericLineSql.ts
  - packages/web/src/lib/groupedSeries.ts
  - packages/web/src/lib/buildTimelineSql.spec.ts
  - packages/web/src/lib/buildNumericLineSql.spec.ts
  - packages/web/src/lib/groupedSeries.spec.ts
autonomous: true
requirements: [GROUP-V114-04]
must_haves:
  truths:
    - "buildTimelineSql with NO group-by arg emits byte-identical SQL to today (backward-compat lock)"
    - "buildNumericLineSql with NO group-by arg emits byte-identical SQL to today (backward-compat lock)"
    - "buildTimelineSql with a groupByCol emits `<groupByCol> AS series`, `GROUP BY bucket, series`, and `<groupByCol> IS NOT NULL`"
    - "buildNumericLineSql with a groupByCol emits the FLOOR bucket + `<groupByCol> AS series`, `GROUP BY bucket, series`, and `<groupByCol> IS NOT NULL`"
    - "groupedSeries provides a top-N series selector (default 12) ranked by total metric value descending and a bucket-keyed pivot helper"
  artifacts:
    - path: packages/web/src/lib/buildTimelineSql.ts
      provides: "optional group-by branch on the timeline SQL builder"
      contains: "groupByColumn"
    - path: packages/web/src/lib/buildNumericLineSql.ts
      provides: "optional group-by branch on the numeric-line SQL builder"
      contains: "groupByColumn"
    - path: packages/web/src/lib/groupedSeries.ts
      provides: "MAX_SERIES constant + selectTopSeries + pivotSeriesRows helpers"
      exports: ["MAX_SERIES", "selectTopSeries", "pivotSeriesRows"]
  key_links:
    - from: packages/web/src/lib/groupedSeries.ts
      to: "TimelineRenderer.tsx + NumericLineRenderer.tsx (Wave 2)"
      via: "MAX_SERIES / selectTopSeries / pivotSeriesRows imports"
      pattern: "from \"\\.\\./lib/groupedSeries\""
---

<objective>
Extend the two pure SQL builders (`buildTimelineSql`, `buildNumericLineSql`) with an OPTIONAL group-by column argument, and add a shared `groupedSeries.ts` helper module (top-N series selection + bucket-keyed pivot) that both Wave-2 renderers will consume.

Purpose: This is the interface-and-contract foundation for Phase 72. The grouped SQL shape and the `MAX_SERIES` / pivot helpers are defined here ONCE so the Timeline and Numeric-Line renderer plans (Wave 2) build against a fixed contract — no codebase scavenger-hunt, no duplicated top-N logic.

Output: Optional-arg SQL builders (ungrouped path byte-identical to today), a `groupedSeries.ts` helper, and spec coverage locking both backward-compat and grouped behavior.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-group-by-timeline-numericline/72-CONTEXT.md

<interfaces>
<!-- Current builder signatures + emitted shapes the executor extends. Use directly. -->

From packages/web/src/lib/buildTimelineSql.ts (current — DO NOT change ungrouped output):
```typescript
export type BuildTimelineSqlArgs = {
  schema: string; table: string; timeCol: string;
  metric: TimelineMetric; interval: TimelineInterval; maxIntervals: number;
};
// emits: SELECT <bucket> AS bucket, <agg> AS value FROM <fromTarget>
//        WHERE <timeCol> IS NOT NULL GROUP BY bucket ORDER BY bucket ASC LIMIT <maxIntervals>
// bucket = buildTimelineBucket(timeCol, interval); fromTarget = schema==="" ? table : `${schema}.${table}`
// aggExpr(metric): COUNT_DISTINCT -> COUNT(DISTINCT col); else `${agg}(${col})`
```

From packages/web/src/lib/buildNumericLineSql.ts (current — DO NOT change ungrouped output):
```typescript
export type BuildNumericLineSqlArgs = {
  schema: string; table: string; xField: string;
  binWidth: number; metric: NumericMetric; maxBuckets: number;
};
// emits: SELECT FLOOR(<xField> / <binWidth>) * <binWidth> AS bucket, <agg> AS value FROM <fromTarget>
//        WHERE <xField> IS NOT NULL GROUP BY bucket ORDER BY bucket ASC LIMIT <maxBuckets + 1>
```

NumericMetric is a re-export of TimelineMetric (numericBin.ts:15). TimelineMetric.column / .aggregation / .color / .label.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add optional group-by to buildTimelineSql + buildNumericLineSql (backward-compat locked)</name>
  <files>packages/web/src/lib/buildTimelineSql.ts, packages/web/src/lib/buildNumericLineSql.ts</files>
  <read_first>
    - packages/web/src/lib/buildTimelineSql.ts (full file — 67 lines)
    - packages/web/src/lib/buildNumericLineSql.ts (full file — 64 lines)
    - packages/web/src/lib/buildTimelineSql.spec.ts (existing assertions — these MUST stay green unchanged)
    - packages/web/src/lib/buildNumericLineSql.spec.ts (existing assertions)
    - packages/web/src/phases/72-group-by-timeline-numericline/72-CONTEXT.md → <decisions> "Group-by SQL shape" (the exact grouped shape)
  </read_first>
  <behavior>
    - Ungrouped (no groupByColumn passed, or passed as undefined/""): output is BYTE-IDENTICAL to today for both builders. Existing spec assertions stay green with zero edits.
    - buildTimelineSql grouped: `SELECT <bucket> AS bucket, <groupByCol> AS series, <agg> AS value FROM <fromTarget> WHERE <timeCol> IS NOT NULL AND <groupByCol> IS NOT NULL GROUP BY bucket, series ORDER BY bucket ASC LIMIT <maxIntervals>`.
    - buildNumericLineSql grouped: `SELECT FLOOR(<xField> / <binWidth>) * <binWidth> AS bucket, <groupByCol> AS series, <agg> AS value FROM <fromTarget> WHERE <xField> IS NOT NULL AND <groupByCol> IS NOT NULL GROUP BY bucket, series ORDER BY bucket ASC LIMIT <maxBuckets + 1>`.
    - When an explicit `seriesIn` (top-N value list) is provided alongside groupByColumn, append `AND <groupByCol> IN (<quoted-or-numeric values>)` to the WHERE and scale the LIMIT so no series is clipped mid-range (see action).
  </behavior>
  <action>
    Add an OPTIONAL field `groupByColumn?: string` to BOTH `BuildTimelineSqlArgs` and `BuildNumericLineSqlArgs`. Add an OPTIONAL `seriesIn?: (string | number)[]` field to support the top-N IN-filter pre-query path (see Task 2 / renderer Wave 2).

    In each builder, branch on `groupByColumn` being a non-empty string:
    - If absent/empty → return the EXACT current string (do not refactor the existing return into the grouped branch; keep the ungrouped branch literally identical so the byte-for-byte spec passes).
    - If present → emit the grouped shape above. Insert `${groupByColumn} AS series` immediately after the `bucket` select term; append `AND ${groupByColumn} IS NOT NULL` to the existing WHERE; change `GROUP BY bucket` to `GROUP BY bucket, series`.
    - LIMIT when grouped: multiply the bucket bound by the series cap so buckets×series are not clipped. Import `MAX_SERIES` from `./groupedSeries` (created in Task 3) and use `LIMIT <bucketBound> * MAX_SERIES` (timeline bucketBound = maxIntervals; numericline bucketBound = maxBuckets + 1). When `seriesIn` is provided (length N>0), instead emit `AND ${groupByColumn} IN (${seriesIn formatted})` and `LIMIT <bucketBound> * N`. Format seriesIn values: numbers verbatim, strings single-quoted with internal single-quotes doubled (e.g. `O'Brien` → `'O''Brien'`).

    Keep zero React/Recharts/Zustand imports (pure module). Keep the existing doc-comment "Emitted shape" blocks accurate — update them to document the grouped branch.

    STALE-TEST MIGRATION: grep both spec files for any existing assertion that would break. Today's assertions all call the builders WITHOUT a group-by arg, so they must remain green untouched — do NOT edit existing test bodies. If TypeScript flags the new optional fields anywhere, that is a real call-site you must NOT change (callers pass no group-by yet; renderers add it in Wave 2).
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/lib/buildTimelineSql.spec.ts src/lib/buildNumericLineSql.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "groupByColumn" packages/web/src/lib/buildTimelineSql.ts` ≥ 1 AND same for buildNumericLineSql.ts.
    - `grep -c "AS series" packages/web/src/lib/buildTimelineSql.ts` ≥ 1 AND `grep "GROUP BY bucket, series" packages/web/src/lib/buildTimelineSql.ts` matches; same two greps pass for buildNumericLineSql.ts.
    - `grep "IS NOT NULL" buildTimelineSql.ts` shows the grouped branch ANDs `${groupByColumn} IS NOT NULL`.
    - The ORIGINAL buildTimelineSql.spec.ts Test 1 (`...GROUP BY bucket ORDER BY bucket ASC LIMIT 200`) and buildNumericLineSql equivalents still pass UNCHANGED (proves byte-identical ungrouped path).
    - `npx vitest run src/lib/buildTimelineSql.spec.ts src/lib/buildNumericLineSql.spec.ts` exits 0.
  </acceptance_criteria>
  <done>Both builders accept optional groupByColumn (+ seriesIn); ungrouped output unchanged; grouped output matches the locked shape; existing specs green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add grouped-builder spec coverage (backward-compat + grouped shape + seriesIn)</name>
  <files>packages/web/src/lib/buildTimelineSql.spec.ts, packages/web/src/lib/buildNumericLineSql.spec.ts</files>
  <read_first>
    - packages/web/src/lib/buildTimelineSql.spec.ts (mirror its style + ladder-entry helpers)
    - packages/web/src/lib/buildNumericLineSql.spec.ts
    - packages/web/src/lib/buildTimelineSql.ts (post-Task-1)
    - packages/web/src/lib/buildNumericLineSql.ts (post-Task-1)
  </read_first>
  <behavior>
    - Backward-compat: builder called with no group-by → exact same string as a baseline literal (lock regression).
    - Grouped: SQL contains `<groupByCol> AS series`, `GROUP BY bucket, series`, `<groupByCol> IS NOT NULL`, and LIMIT scaled by MAX_SERIES.
    - seriesIn path: SQL contains `<groupByCol> IN (...)` with string values single-quoted and a single-quote escaped, numbers verbatim; LIMIT scaled by the IN-list length.
  </behavior>
  <action>
    APPEND new `describe` blocks to the EXISTING spec files (do not create separate files; keep the existing tests intact). For buildTimelineSql.spec.ts add a `describe("buildTimelineSql — grouped")` block:
    - "no group-by arg → byte-identical to baseline" asserting `toBe` against the existing baseline literal (copy from Test 1).
    - "groupByColumn emits series + GROUP BY bucket, series + IS NOT NULL": assert `toContain("vendor AS series")`, `toContain("GROUP BY bucket, series")`, `toContain("AND vendor IS NOT NULL")`.
    - "grouped LIMIT scales by MAX_SERIES": import MAX_SERIES, assert `toContain(\`LIMIT ${200 * MAX_SERIES}\`)`.
    - "seriesIn emits IN-filter with quoting": pass `seriesIn: ["A", "O'Brien"]`, assert `toContain("vendor IN ('A', 'O''Brien')")` and `toContain(\`LIMIT ${200 * 2}\`)`.
    Mirror the same four for buildNumericLineSql.spec.ts (numeric seriesIn e.g. `[1, 2]` → `IN (1, 2)`; bucketBound = maxBuckets + 1).

    STALE-TEST MIGRATION: none expected (only appending). Confirm the FULL file runs.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/lib/buildTimelineSql.spec.ts src/lib/buildNumericLineSql.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both spec files contain a `grouped` describe block (`grep -c "grouped" src/lib/buildTimelineSql.spec.ts` ≥ 1).
    - `grep "byte-identical" src/lib/buildTimelineSql.spec.ts` matches the backward-compat test.
    - `npx vitest run src/lib/buildTimelineSql.spec.ts src/lib/buildNumericLineSql.spec.ts` exits 0 with the new tests counted.
  </acceptance_criteria>
  <done>Grouped + backward-compat + seriesIn assertions present and green in both builder spec files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create groupedSeries.ts shared helper (MAX_SERIES + selectTopSeries + pivotSeriesRows) with spec</name>
  <files>packages/web/src/lib/groupedSeries.ts, packages/web/src/lib/groupedSeries.spec.ts</files>
  <read_first>
    - packages/web/src/lib/buildTimelineSql.ts (imports MAX_SERIES from this module)
    - packages/web/src/components/charts/TimelineRenderer.tsx → step-4 merge loop (lines ~262-276) — pivotSeriesRows mirrors its bucket-keyed/missing→null semantics
    - packages/web/src/phases/72-group-by-timeline-numericline/72-CONTEXT.md → <decisions> "Top-N series cap" + "Renderer: series pivot"
  </read_first>
  <behavior>
    - `MAX_SERIES = 12` exported constant.
    - `selectTopSeries(rows, { max })`: given grouped rows `{ series: string; value: number }[]` (the top-N pre-query result), returns the top `max` (default MAX_SERIES) series values ranked by total metric value DESC, plus a `truncated: boolean` and `total: number` (distinct series count) so the renderer can show "top 12 of N". Ties broken by series string asc (stable).
    - `pivotSeriesRows(rows, seriesValues)`: given grouped main-query rows `{ bucket: string; series: string; value: number | null }[]` and the ordered seriesValues list, returns Recharts-ready rows `{ bucket, [seriesValue]: number | null }[]` sorted by bucket; buckets present in any row; missing (bucket × series) combos → null (gap), matching today's metric-merge semantics. Rows whose series is NOT in seriesValues are dropped.
  </behavior>
  <action>
    Create `packages/web/src/lib/groupedSeries.ts` — a PURE module (zero React/Recharts/Zustand imports). Export `MAX_SERIES`, `selectTopSeries`, `pivotSeriesRows` with the behavior above. Type the row shapes with exported types `SeriesTotalRow` and `GroupedRow`. Bucket sort: accept an optional `numericBuckets` flag on pivotSeriesRows so the numeric-line renderer can sort buckets numerically (default lexical for timeline ISO buckets, matching TimelineRenderer `.sort()` vs NumericLineRenderer `.sort((a,b)=>Number(a)-Number(b))`).

    Create `packages/web/src/lib/groupedSeries.spec.ts` covering: MAX_SERIES === 12; selectTopSeries ranks DESC, caps at max, sets truncated + total; pivotSeriesRows produces bucket-keyed rows with null gaps and drops out-of-set series; numericBuckets flag sorts numerically.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/lib/groupedSeries.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export const MAX_SERIES = 12" packages/web/src/lib/groupedSeries.ts` matches.
    - `grep -E "export function selectTopSeries|export function pivotSeriesRows" packages/web/src/lib/groupedSeries.ts` matches both.
    - `grep -c "import" packages/web/src/lib/groupedSeries.ts` shows NO react/recharts/zustand import (pure module).
    - `npx vitest run src/lib/groupedSeries.spec.ts` exits 0.
  </acceptance_criteria>
  <done>groupedSeries.ts exports MAX_SERIES/selectTopSeries/pivotSeriesRows with passing spec; buildTimelineSql/buildNumericLineSql import MAX_SERIES from it without circular-import error.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx vitest run src/lib/buildTimelineSql.spec.ts src/lib/buildNumericLineSql.spec.ts src/lib/groupedSeries.spec.ts` exits 0.
- `cd packages/web && npx tsc --noEmit` clean (no broken call-sites — renderers still pass no group-by until Wave 2).
- No server diff (`git status packages/server` empty).
- Ungrouped builder output proven byte-identical via the unchanged baseline assertions.
</verification>

<success_criteria>
- buildTimelineSql + buildNumericLineSql accept an optional groupByColumn (and seriesIn) arg; ungrouped path byte-identical to today.
- Grouped SQL emits `<groupByCol> AS series`, `GROUP BY bucket, series`, `<groupByCol> IS NOT NULL`, LIMIT scaled to avoid per-series clipping.
- groupedSeries.ts provides MAX_SERIES (12), selectTopSeries (top-N + truncated affordance), pivotSeriesRows (bucket-keyed, null gaps) — the contract Wave 2 renderers build against.
- All three spec files green; web tsc clean.
</success_criteria>

<output>
After completion, create `.planning/phases/72-group-by-timeline-numericline/72-01-SUMMARY.md`
</output>
