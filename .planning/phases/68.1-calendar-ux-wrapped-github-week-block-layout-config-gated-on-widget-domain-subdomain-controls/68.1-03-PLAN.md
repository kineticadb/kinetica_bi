---
phase: 68.1-calendar-ux-wrapped-github-week-block-layout-config-gated-on-widget-domain-subdomain-controls
plan: 03
type: execute
wave: 2
depends_on: ["68.1-01", "68.1-02"]
files_modified:
  - packages/web/src/components/charts/CalendarRenderer.tsx
  - packages/web/src/components/charts/CalendarRenderer.spec.tsx
autonomous: true
requirements: [CALUX-V113-01, CALUX-V113-02]
must_haves:
  truths:
    - "The calendar renders wrapped, compact domain-group blocks (via layoutCalendar) instead of one tall flat column"
    - "Each domain block shows a group label above it; sparse DOW labels (Mon/Wed/Fri) render down the left for day subdomains"
    - "layoutMode 'wrap' wraps blocks left→right then down; 'strip' lays them in one horizontal strip"
    - "When showDomainSubdomainControls is ON, a top control bar with Domain + Subdomain dropdowns renders; when OFF (default) no dropdowns render"
    - "The viewer's Subdomain dropdown only lists VALID_DOMAIN_SUBDOMAIN[effectiveDomain]; changing either dropdown re-fetches (runSql again)"
    - "The viewer override is VIEW-LOCAL — it never calls onChange/patch and is used as effective domain/subdomain for SQL, labels, cell bounds, AND drill"
    - "Phase 68 cell-drill, active-cell highlight, empty-cell non-interactivity, respondToFilters FROM-gating, and the no-materialize-import invariant all still hold against the new layout"
  artifacts:
    - path: "packages/web/src/components/charts/CalendarRenderer.tsx"
      provides: "Wrapped block layout + top control bar + view-local effective domain/subdomain"
      contains: "layoutCalendar"
    - path: "packages/web/src/components/charts/CalendarRenderer.spec.tsx"
      provides: "Specs for wrapped render + control bar + effective-value drill; preserves Phase 67/68 tests"
  key_links:
    - from: "packages/web/src/components/charts/CalendarRenderer.tsx"
      to: "packages/web/src/lib/calendarLayout.ts"
      via: "layoutCalendar(grid.rows, effDomain, effSubdomain) → positioned blocks"
      pattern: "layoutCalendar"
    - from: "CalendarRenderer effective domain/subdomain"
      to: "buildCalendarSql + computeCellBounds + handleCellClick + labels"
      via: "effDomain = viewerDomain ?? config.domain; effSubdomain = viewerSubdomain ?? config.subdomain"
      pattern: "effDomain|effSubdomain|viewer"
---

<objective>
Rewrite the CalendarRenderer SVG body (CalendarRenderer.tsx §411-516) to consume the Plan 01
`layoutCalendar` helper — rendering wrapped, compact, date-positioned domain-group blocks
(CALUX-V113-01) — and add the config-gated viewer control bar with VIEW-LOCAL effective
domain/subdomain (CALUX-V113-02), while PRESERVING every Phase 67 (5/5) and Phase 68 (4/4) behavior.

Purpose: The operator saw the live calendar render "everything in one line / too long vertically"
(flat `domainKeys.map(col) → subdomainKeys.map(row)` at §466-469). This plan replaces the flat (x,y)
math with `layoutCalendar` blocks, replaces the single top-domain-axis + left-subdomain-axis label
scheme (§436-463) with per-block group labels + sparse DOW labels, and adds 2 optional viewer
dropdowns. Plan 03 is the SOLE editor of CalendarRenderer.tsx (no other Wave-2 plan touches it).

Output: Rewritten CalendarRenderer.tsx + updated CalendarRenderer.spec.tsx.

⚠ WEEK-START / PHASE 69 LINKAGE: block DOW positioning uses `WEEK_START` from calendarLayout.ts
(Monday-ISO interim). The Phase 65 Kinetica DATE_TRUNC week-anchor spike is NOT-RUN; Phase 69
verifies/corrects it in that ONE constant. Do NOT hardcode a week anchor anywhere in this renderer —
always go through layoutCalendar.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68.1-calendar-ux-wrapped-github-week-block-layout-config-gated-on-widget-domain-subdomain-controls/68.1-CONTEXT.md

<interfaces>
From packages/web/src/lib/calendarLayout.ts (Plan 01 — consume, do not modify):
```typescript
export const WEEK_START: number; // Monday-ISO (1). Phase 69 may flip.
export type PositionedCell  = { cell: CalendarCell; col: number; row: number };
export type PositionedBlock = { domainKey: string; label: string; cols: number; rows: number; cells: PositionedCell[] };
export function layoutCalendar(args: { rows: CalendarRow[]; domain: CalendarDomain; subdomain: CalendarSubdomain }): PositionedBlock[];
```

From packages/web/src/components/charts/CalendarConfigPanel.tsx (Plan 02 — read these config fields):
```typescript
type CalendarConfig = { /* ... */ layoutMode?: "wrap" | "strip"; showDomainSubdomainControls?: boolean; };
```

From packages/web/src/lib/calendarBin.ts (drive the viewer subdomain dropdown gating):
```typescript
export const VALID_DOMAIN_SUBDOMAIN: Record<CalendarDomain, readonly CalendarSubdomain[]>;
export function isValidCombo(domain, subdomain): boolean;
export function computeCellBounds(dateIso, subdomainUnit): [string, string];
```

PRESERVE (CalendarRenderer.tsx CURRENT):
- handleCellClick §364-409 (BETWEEN drill, table vs dv routing, toggle-off, toast) — MUST use the
  EFFECTIVE subdomain in computeCellBounds (today reads the static `subdomain` const §369).
- appliedCell useMemo §182-193 (active BETWEEN bounds; selected-cell highlight).
- gap-fill `grid` §324, colorDomain §328, colors §330, useChartAxisColors §334.
- FROM resolution + respondToFilters gating §205-320 (the whole useEffect) — effective domain/subdomain
  feed buildCalendarSql AND join the dep array.
- The no-import invariant: NO import of materializeFilter / dropFilterView / fromSwap.
</interfaces>

<spec_anchor>
From CalendarRenderer.spec.tsx (CURRENT — Tests 0-24 MUST stay green):
- Tests probe `getByTestId("calendar-renderer")`, click `rect` elements lacking `data-empty`, assert
  `<title>` on populated rects, `data-empty="true"` on greys, stroke on the active cell, runSql call
  counts, setBulkFilters/addDvFilter/markMaterializing/markDvMaterializing dispatch, toggle-off via
  removeFilter, toast, and the static no-import grep (Tests 0 & 22).
- KEEP: the populated cells must still be `<rect>` with `<title>`, no `data-empty`, an onClick → drill;
  greys still `<rect data-empty="true">` with `pointerEvents:none` and no `<title>`; the active cell
  still gets a non-"none" stroke. The wrapped layout changes (x,y) only — element shapes are preserved.
</spec_anchor>
</context>

<tasks>

<task type="auto">
  <name>Task 1: View-local effective domain/subdomain state + control bar + re-fetch wiring</name>
  <files>packages/web/src/components/charts/CalendarRenderer.tsx</files>
  <read_first>
    - packages/web/src/components/charts/CalendarRenderer.tsx §96-123 (config reads — add layoutMode +
      showDomainSubdomainControls + viewer state here), §195-320 (the fetch useEffect — swap to effective
      values + extend dep array), §364-409 (handleCellClick — swap subdomain→effSubdomain)
    - packages/web/src/lib/calendarBin.ts §63-78 (VALID_DOMAIN_SUBDOMAIN / isValidCombo)
    - packages/web/src/components/charts/CalendarConfigPanel.tsx §339-346 (handleDomainChange reset
      pattern — the control bar's domain change must likewise reset an invalid subdomain to validSubs[0])
  </read_first>
  <action>
    1) Read new config (near §110-114):
       const layoutMode = cfg.layoutMode ?? "wrap";
       const showControls = cfg.showDomainSubdomainControls ?? false;

    2) Add VIEW-LOCAL state (with the other useState hooks, ~§197 — keep the eslint-disable
       rules-of-hooks comment style):
       const [viewerDomain, setViewerDomain] = useState<CalendarDomain | null>(null);
       const [viewerSubdomain, setViewerSubdomain] = useState<CalendarSubdomain | null>(null);
       // effective values — viewer override ?? operator config. Used EVERYWHERE (SQL, labels, bounds, drill).
       const effDomain = viewerDomain ?? domain;
       const effSubdomain = viewerSubdomain ?? subdomain;
       NOTE: viewer state must NEVER call patch/onChange (view-local; resets on reload — fresh mount =
       null override = config default).

    3) In the fetch useEffect (§264-271 buildCalendarSql call), pass effDomain/effSubdomain instead of
       domain/subdomain. Add effDomain, effSubdomain to the dep array (§303-320) — changing a viewer
       dropdown re-fetches. (Replace the existing `domain`/`subdomain` deps with effDomain/effSubdomain.)

    4) In handleCellClick (§369) replace `computeCellBounds(cell.subdomainKey, subdomain)` with
       `computeCellBounds(cell.subdomainKey, effSubdomain)`. Likewise the appliedCell highlight bounds
       (§492) and the timeSlice label (§488) use effDomain/effSubdomain. The drill must reflect the
       value the viewer is actually looking at.

    5) Control-bar handlers (view-local; dependent gating mirrors CalendarConfigPanel.handleDomainChange):
       const handleViewerDomainChange = (d: CalendarDomain) => {
         setViewerDomain(d);
         const valid = VALID_DOMAIN_SUBDOMAIN[d];
         if (!(valid as readonly string[]).includes(effSubdomain)) setViewerSubdomain(valid[0]);
       };
       const handleViewerSubdomainChange = (s: CalendarSubdomain) => setViewerSubdomain(s);

    6) Render the control bar at the TOP of the widget body (before the scrollable SVG wrapper §427),
       ONLY when showControls is true. Reuse .ds-select styling; theme tokens only:
       {showControls && (
         <div data-testid="calendar-control-bar" style={{ display: "flex", gap: 8, padding: "4px 8px", flexShrink: 0 }}>
           <select className="ds-select" aria-label="Domain" value={effDomain}
                   onChange={(e) => handleViewerDomainChange(e.target.value as CalendarDomain)}>
             {(Object.keys(VALID_DOMAIN_SUBDOMAIN) as CalendarDomain[]).map((d) => (
               <option key={d} value={d}>{d}</option>
             ))}
           </select>
           <select className="ds-select" aria-label="Subdomain" value={effSubdomain}
                   onChange={(e) => handleViewerSubdomainChange(e.target.value as CalendarSubdomain)}>
             {VALID_DOMAIN_SUBDOMAIN[effDomain].map((s) => (
               <option key={s} value={s}>{s}</option>
             ))}
           </select>
         </div>
       )}
    Do NOT import or call patch/onChange/materializeFilter/dropFilterView/fromSwap. No raw hex.
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit 2>&1 | grep -i calendarrenderer || echo "no tsc errors in CalendarRenderer"</automated>
  </verify>
  <acceptance_criteria>
    - CalendarRenderer.tsx contains `effDomain` and `effSubdomain` and `viewerDomain` / `viewerSubdomain`.
    - CalendarRenderer.tsx control bar `<select aria-label="Subdomain">` maps over
      `VALID_DOMAIN_SUBDOMAIN[effDomain]` (dependent gating).
    - CalendarRenderer.tsx fetch useEffect calls buildCalendarSql with `domain: effDomain, subdomain: effSubdomain`
      and the dep array includes `effDomain` and `effSubdomain`.
    - handleCellClick uses `computeCellBounds(cell.subdomainKey, effSubdomain)` (effective, not the raw config subdomain).
    - CalendarRenderer.tsx does NOT match `/import.*(materializeFilter|dropFilterView|fromSwap)/` and
      does NOT call `patch(` / `onChange(` (view-local). grep confirms.
    - `cd packages/web && npx tsc --noEmit` reports no new errors for CalendarRenderer.tsx.
  </acceptance_criteria>
  <done>
    Effective (viewer ?? config) domain/subdomain thread through SQL + bounds + labels + drill; the
    config-gated control bar renders dependent-gated dropdowns and re-fetches on change, view-local.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace the flat grid with wrapped layoutCalendar blocks + per-block + DOW labels</name>
  <files>packages/web/src/components/charts/CalendarRenderer.tsx</files>
  <read_first>
    - packages/web/src/components/charts/CalendarRenderer.tsx §411-516 (the flat domain-axis +
      subdomain-axis + cell-grid block to REPLACE), §322-334 (grid/colorDomain/colors/axis colors to keep)
    - packages/web/src/lib/calendarLayout.ts (Plan 01 — layoutCalendar return shape)
    - packages/web/src/lib/timelineBin.ts (formatTimelineTick — used for the per-block group label)
  </read_first>
  <action>
    Replace §411-516 (flat axes + `calendarRows.map(col) → cells.map(row)`) with a layoutCalendar-driven
    block render. Keep §322-334 (grid, colorDomain, colors, axis/emptyCell/accent) and the legend
    §519-544 unchanged.

    1) Compute blocks:
       const blocks = useMemo(
         () => layoutCalendar({ rows: grid.rows, domain: effDomain, subdomain: effSubdomain }),
         [grid, effDomain, effSubdomain],
       );

    2) For EACH block render (wrap with layoutMode):
       - A group label above the block: <text>{formatTimelineTick(block.domainKey, effDomain as TimelineIntervalKey)}</text>
       - Sparse DOW labels down the LEFT of the block when effSubdomain === "day" (rows are day-of-week):
         show Mon/Wed/Fri only (rows 0,2,4 with WEEK_START=Monday). Use the `axis` color.
       - Cells: for each PositionedCell, draw a <rect> at (block originX + col*(CELL_PX+GAP),
         block originY + row*(CELL_PX+GAP)). PRESERVE the exact cell semantics:
           * empty (cell.value === null): <rect data-empty="true" fill={emptyCell}
             style={{ pointerEvents: "none" }} /> — NO onClick, NO <title>.
           * populated: <rect fill={colors[quantizeToBucket(value, colorDomain, CALENDAR_BUCKET_COUNT)]}
             stroke={isActive ? accent : "none"} strokeWidth={isActive ? 2 : 0}
             onClick={() => handleCellClick(cell)} style={{ cursor: "pointer" }}><title>{tooltipText}</title></rect>
           * isActive computed via computeCellBounds(cell.subdomainKey, effSubdomain) vs appliedCell
             (carry the §491-496 logic, using effSubdomain).
           * tooltipText = `${timeSlice} · ${aggLabel}: ${value.toLocaleString()}` with timeSlice built
             from effDomain/effSubdomain (carry §488).

    3) Macro layout (layoutMode):
       - "wrap" (default): flow blocks left→right; when the running x + block width exceeds the available
         width, wrap to a new row (advance y by max block height in the row + gap). Compute block width =
         block.cols*(CELL_PX+GAP) + a left gutter for DOW labels; block height = block.rows*(CELL_PX+GAP)
         + label height. Pixel math (gaps, gutter, breakpoints, responsive sizing) is planner discretion.
       - "strip": lay all blocks side-by-side in ONE horizontal row (no vertical wrap); the existing
         scrollable wrapper (§428 overflow:auto) provides horizontal scroll.
       Keep data-testid="calendar-renderer" on the root <svg>. Size the <svg> width/height to the laid-out
       content. Theme tokens only — NO raw hex (axis/emptyCell/accent/colors are theme-sourced).

    Remove the now-dead flat constants usage if unused (LEFT_AXIS_WIDTH / TOP_AXIS_HEIGHT may be
    repurposed as block gutter/label-height — planner discretion).
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/styles/theme-guard.spec.ts && npx tsc --noEmit 2>&1 | grep -i calendarrenderer || echo "tsc clean for CalendarRenderer"</automated>
  </verify>
  <acceptance_criteria>
    - CalendarRenderer.tsx imports and calls `layoutCalendar` (grep: `layoutCalendar`).
    - The flat double-map `calendarRows.map(` over domain×subdomain index positioning is GONE (grep:
      CalendarRenderer.tsx no longer contains `subdomainKeys.map` for cell positioning).
    - Populated cells are `<rect>` with an `onClick` and a `<title>`; empty cells are
      `<rect data-empty="true">` with `pointerEvents: "none"` and no `<title>` (preserved semantics).
    - CalendarRenderer.tsx branches on `layoutMode` (grep: `layoutMode` and both `"wrap"`/`"strip"` or
      a strip-vs-wrap conditional).
    - `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` passes (no raw hex).
    - `cd packages/web && npx tsc --noEmit` clean for CalendarRenderer.tsx.
  </acceptance_criteria>
  <done>
    Wrapped/strip block layout via layoutCalendar with per-block group labels + sparse DOW labels;
    cell drill/highlight/empty-greys preserved at the new coordinates; theme-guard green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Specs — wrapped render + control bar + effective-value drill; preserve Phase 67/68 (Tests 0-24)</name>
  <files>packages/web/src/components/charts/CalendarRenderer.spec.tsx</files>
  <read_first>
    - packages/web/src/components/charts/CalendarRenderer.spec.tsx (full — Tests 0-24; the makeWidget
      fixture §150-171, makeCalendarResponse §177-184, CANNED_ROWS §187-192, beforeEach §194-211)
  </read_first>
  <behavior>
    - Test (wrapped layout): with CANNED_ROWS (domain=month, subdomain=day) the renderer still shows
      calendar-renderer and at least one populated `<rect>` with a `<title>` — confirms blocks render
      (Tests 2/6 keep passing).
    - Test (group labels): the SVG contains `<text>` group label(s) for the domain blocks (>=1 <text>).
    - Test (control bar OFF by default): with no showDomainSubdomainControls, queryByTestId
      ("calendar-control-bar") is null AND no `aria-label="Domain"` select is on the widget.
    - Test (control bar ON): with showDomainSubdomainControls:true, calendar-control-bar renders with a
      Domain select and a Subdomain select.
    - Test (dependent gating): with controls ON and config domain="week", the Subdomain select lists ONLY
      VALID_DOMAIN_SUBDOMAIN.week (["day","hour"]) — not "month"/"week".
    - Test (re-fetch on viewer change): with controls ON, changing the viewer Domain select fires runSql
      again (call count increases) — view-local re-fetch.
    - Test (view-local, no persist): the renderer does NOT receive an onChange prop / never writes config —
      assert there is no patch/onChange call path. (Renderer has no onChange prop, so assert by source-grep
      that CalendarRenderer.tsx does not contain `onChange(` / `patch(`.)
    - Test (effective-value drill): with controls ON, change the viewer Subdomain to a different unit, then
      click a populated cell → setBulkFilters is called with a between filter whose bounds match
      computeCellBounds(subdomainKey, VIEWER subdomain) — drill uses the effective (viewer) value.
  </behavior>
  <action>
    Append new tests; do NOT alter the mock scaffolding (§10-133) or Tests 0-24. Reuse makeWidget
    (pass { showDomainSubdomainControls: true, domain, subdomain } overrides), makeCalendarResponse,
    fireEvent.change on the aria-labelled selects, and the existing runSql / store mocks. Run the FULL
    file — Tests 0-24 (the Phase 67 5/5 + Phase 68 4/4 criteria: no-import grep, reactive domain,
    fetch/grid, FROM resolution OFF/ON, filterVersion re-fetch, gap-fill greys, drill dispatch table+dv,
    dv isolation, empty-cell guard, toggle-off, toast, highlight, chip lifecycle) must ALL stay green.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/CalendarRenderer.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/web && npx vitest run src/components/charts/CalendarRenderer.spec.tsx` passes — Tests
      0-24 (Phase 67 5/5 + Phase 68 4/4) NOT regressed + the new 68.1 tests green.
    - The spec asserts calendar-control-bar is ABSENT by default and PRESENT when
      showDomainSubdomainControls:true.
    - The spec asserts the viewer Subdomain select lists only VALID_DOMAIN_SUBDOMAIN[domain].
    - The spec asserts changing a viewer dropdown increases the runSql call count (re-fetch).
    - The spec asserts a cell drill after a viewer-subdomain change uses the EFFECTIVE subdomain bounds.
    - The spec re-asserts the no-import invariant (Test 0/22 still present and green).
  </acceptance_criteria>
  <done>
    New layout + control-bar + effective-value-drill specs pass; Phase 67 (5/5) and Phase 68 (4/4)
    criteria are explicitly preserved (cell-drill, active-cell highlight, respondToFilters gating,
    dv-isolation, no-materialize-import) against the new wrapped layout.
  </done>
</task>

</tasks>

<verification>
- `cd packages/web && npx vitest run src/components/charts/CalendarRenderer.spec.tsx` green (Tests 0-24 + new).
- `cd packages/web && npx vitest run` (full frontend suite) 100% — no cross-file regressions.
- `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` green (no raw hex in CalendarRenderer).
- `cd packages/web && npx tsc --noEmit` clean.
- Static grep: CalendarRenderer.tsx still has NO import of materializeFilter/dropFilterView/fromSwap
  (Tests 0 & 22) and never calls patch/onChange (view-local invariant).
- Week-start anchor flows only through calendarLayout.WEEK_START (Phase 69 spike correction lands there).
</verification>

<success_criteria>
- CALUX-V113-01: wrapped/strip compact domain-group blocks via layoutCalendar; per-block group labels +
  sparse DOW labels; drill/highlight/gap-fill/respondToFilters preserved at the new coordinates.
- CALUX-V113-02: config-gated top control bar with 2 dependent-gated viewer dropdowns, view-local
  effective domain/subdomain (never persisted), re-fetch on change; effective values used for SQL,
  labels, bounds AND drill.
- Phase 67 (5/5) + Phase 68 (4/4) criteria preserved and statically/behaviorally re-asserted.
</success_criteria>

<output>
After completion, create `.planning/phases/68.1-calendar-ux-wrapped-github-week-block-layout-config-gated-on-widget-domain-subdomain-controls/68.1-03-SUMMARY.md`
</output>
