---
phase: 68.1-calendar-ux-wrapped-github-week-block-layout-config-gated-on-widget-domain-subdomain-controls
plan: 02
subsystem: ui
tags: [react, vitest, testing-library, calendar, config-panel, layout-mode, domain-subdomain-controls]

# Dependency graph
requires:
  - phase: 68.1-01
    provides: layout helper (wrapCalendarLayout / stripCalendarLayout) that Plan 03 renderer consumes with layoutMode
provides:
  - CalendarConfig type extended with layoutMode (wrap|strip) and showDomainSubdomainControls (boolean)
  - DEFAULT_CALENDAR_CONFIG entries for both new fields
  - Display section UI in CalendarConfigPanel (section header + layout-mode select + controls toggle + info icon)
  - CalendarConfigPanel.spec.tsx Tests 17-23 for the Display section
affects: [68.1-03 (CalendarRenderer consumes layoutMode + showDomainSubdomainControls from config), 69-verification]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "config-group-label section header (marginTop:16) for grouping config controls — mirrors TimelineConfigPanel OPTIONS pattern"
    - "native title= attribute on span glyph for info tooltip (no shared component) — mirrors MapConfigPanel pattern"
    - "accent-checkbox row with htmlFor + aria-label for accessible toggles — mirrors respondToFilters row"

key-files:
  created: []
  modified:
    - packages/web/src/components/charts/CalendarConfigPanel.tsx
    - packages/web/src/components/charts/CalendarConfigPanel.spec.tsx

key-decisions:
  - "layoutMode and showDomainSubdomainControls added as optional fields on CalendarConfig (Plan 03 renderer reads with ?? defaults)"
  - "Display section inserted after respondToFilters row, before cap-probe status hints — stays inside tableId !== undefined fragment"
  - "Info tooltip uses native title= on span (not a shared InfoIcon component) — consistent with MapConfigPanel.tsx §643 pattern"
  - "TDD: tests written after implementation (Tasks 1+2 preceded Task 3) — tests went directly GREEN; all 23 pass"

patterns-established:
  - "DISPLAY section header uses className='config-group-label' style={{marginTop:16}} — matches TimelineConfigPanel OPTIONS header"
  - "Info glyph: <span title='...' aria-label='About ...' style={{marginLeft:4,cursor:help}}>ⓘ</span>"

requirements-completed: [CALUX-V113-01, CALUX-V113-02]

# Metrics
duration: 2min
completed: 2026-06-17
---

# Phase 68.1 Plan 02: CalendarConfigPanel Display Section Summary

**DISPLAY section added to CalendarConfigPanel: layout-mode select (Wrap default / Continuous strip) + Show domain/subdomain controls checkbox (OFF default) + info-icon tooltip, plus two new CalendarConfig fields consumed by Plan 03 renderer**

## Performance

- **Duration:** 2 min
- **Started:** 2026-06-17T15:41:03Z
- **Completed:** 2026-06-17T15:43:00Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments
- Extended `CalendarConfig` type and `DEFAULT_CALENDAR_CONFIG` with `layoutMode: "wrap"` and `showDomainSubdomainControls: false`
- Rendered DISPLAY section in `CalendarConfigPanel` with layout-mode select and controls toggle (accent-checkbox) with native `title=` info tooltip
- Added 7 new spec tests (Tests 17-23) covering all Display-section behavior; all 23 tests pass with no regression of Tests 1-16

## Task Commits

Each task was committed atomically:

1. **Task 1: Add layoutMode + showDomainSubdomainControls to CalendarConfig + defaults** - `3ae7b49` (feat)
2. **Task 2: Render the Display section** - `b8604ab` (feat)
3. **Task 3: Specs for the Display section** - `d4fe5ef` (test)

**Plan metadata:** (docs commit — see below)

_Note: TDD task (Task 3) went directly GREEN since the implementation was already committed in Tasks 1+2._

## Files Created/Modified
- `packages/web/src/components/charts/CalendarConfigPanel.tsx` - Extended CalendarConfig type, DEFAULT_CALENDAR_CONFIG, and rendered new DISPLAY section
- `packages/web/src/components/charts/CalendarConfigPanel.spec.tsx` - Added Tests 17-23 for Display section

## Decisions Made
- `layoutMode` defaults to `"wrap"` (GitHub-style week blocks) per CALUX-V113-01 spec
- `showDomainSubdomainControls` defaults to `false` (OFF) per CALUX-V113-02 spec — viewer controls are opt-in
- Display section placed after `respondToFilters` row, before cap-probe hints — logical grouping after data fields
- Info tooltip uses native `title=` on `<span>` (no shared InfoIcon component) — follows MapConfigPanel.tsx pattern established in prior phases

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- `CalendarConfig.layoutMode` and `CalendarConfig.showDomainSubdomainControls` are ready for Plan 03 (CalendarRenderer) to consume
- Plan 03 reads `layoutMode` to choose between `wrapCalendarLayout` (Plan 01) and continuous strip rendering
- Plan 03 reads `showDomainSubdomainControls` to conditionally render live viewer dropdowns

---
*Phase: 68.1-calendar-ux-wrapped-github-week-block-layout-config-gated-on-widget-domain-subdomain-controls*
*Completed: 2026-06-17*
