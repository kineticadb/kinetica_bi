---
phase: 68.1-calendar-ux-wrapped-github-week-block-layout-config-gated-on-widget-domain-subdomain-controls
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/charts/CalendarConfigPanel.tsx
  - packages/web/src/components/charts/CalendarConfigPanel.spec.tsx
autonomous: true
requirements: [CALUX-V113-01, CALUX-V113-02]
must_haves:
  truths:
    - "CalendarConfigPanel shows a new 'Display' section after the existing fields"
    - "The Display section has a layout-mode control offering Wrap (default) and Continuous strip"
    - "The Display section has a 'Show domain/subdomain controls' checkbox, default OFF, with an info-icon tooltip"
    - "DEFAULT_CALENDAR_CONFIG defaults layoutMode to wrap and showDomainSubdomainControls to false"
    - "The existing Phase 66 (table/dv, timestamp, metric, domain/subdomain, palette, cap) + Phase 68 (respondToFilters) fields and specs still pass"
  artifacts:
    - path: "packages/web/src/components/charts/CalendarConfigPanel.tsx"
      provides: "CalendarConfig type + DEFAULT_CALENDAR_CONFIG + new Display section UI"
      contains: "showDomainSubdomainControls"
    - path: "packages/web/src/components/charts/CalendarConfigPanel.spec.tsx"
      provides: "Specs for the Display section (layout mode + controls toggle default OFF + info icon)"
  key_links:
    - from: "packages/web/src/components/charts/CalendarConfigPanel.tsx"
      to: "widget.config (CalendarConfig consumed by CalendarRenderer)"
      via: "patch({ layoutMode }) / patch({ showDomainSubdomainControls })"
      pattern: "layoutMode|showDomainSubdomainControls"
---

<objective>
Add a new "Display" section to `CalendarConfigPanel` (CALUX-V113-02 config surface + the CALUX-V113-01
layout-mode config field), grouping BOTH new options after the existing domain/subdomain/metric/palette
fields:
  1. Layout mode: Wrap (default) | Continuous strip.
  2. "Show domain/subdomain controls" checkbox — DEFAULT OFF — with an info-icon (native `title=`
     tooltip) giving a friendlier description.
Plus the two new `CalendarConfig` fields + `DEFAULT_CALENDAR_CONFIG` entries, consumed by the Plan 03
renderer.

Purpose: This plan owns the config-panel surface ONLY. It does NOT touch CalendarRenderer.tsx (Plan 03
is the sole editor of that file), so it runs in Wave 1 parallel with the layout helper (Plan 01).

Output: Updated `CalendarConfigPanel.tsx` (Display section + 2 config fields) + updated spec.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68.1-calendar-ux-wrapped-github-week-block-layout-config-gated-on-widget-domain-subdomain-controls/68.1-CONTEXT.md

<interfaces>
From packages/web/src/components/charts/CalendarConfigPanel.tsx (CURRENT — extend, do not break):
```typescript
export type CalendarConfig = {
  tableId?: number; tableRef?: string; dynamicViewId?: number;
  timeCol: string; metricColumn: string; aggregation: TimelineAggregation;
  domain: CalendarDomain; subdomain: CalendarSubdomain; colorTheme: string;
  respondToFilters?: boolean; // Phase 68-03
  // ADD: layoutMode + showDomainSubdomainControls
};
export const DEFAULT_CALENDAR_CONFIG: CalendarConfig = { /* ...existing... */ };
// patch(partial) => onChange({ ...config, ...partial });  (§135-137)
```
Existing field markup uses `.ds-field` / `.ds-field-label` / `.ds-select` (selects) and
`.accent-checkbox` (the respondToFilters checkbox §518-531). Reuse these classes — no raw hex
(theme-guard recursively scans charts/*.tsx).
</interfaces>

<pattern_refs>
- TimelineConfigPanel.tsx §357-406: an OPTIONS section header is `<div className="config-group-label"
  style={{ marginTop: 16 }}>OPTIONS</div>` followed by `.ds-field` / `.config-toggle` rows. Mirror this
  shape for a `DISPLAY` section header.
- MapConfigPanel.tsx §643: info-icon pattern is a native `title="..."` attribute on an element (NO
  shared InfoIcon component). Use a small `<span title="...">ⓘ</span>` glyph beside the checkbox label.
- CalendarConfigPanel.tsx §518-531: the existing respondToFilters `.accent-checkbox` row is the exact
  template for the new "Show domain/subdomain controls" checkbox.
</pattern_refs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add layoutMode + showDomainSubdomainControls to CalendarConfig + defaults</name>
  <files>packages/web/src/components/charts/CalendarConfigPanel.tsx</files>
  <read_first>
    - packages/web/src/components/charts/CalendarConfigPanel.tsx §43-64 (CalendarConfig type +
      DEFAULT_CALENDAR_CONFIG — extend both)
  </read_first>
  <action>
    Extend the `CalendarConfig` type (§43-54) with TWO optional fields:
      layoutMode?: "wrap" | "strip";              // CALUX-V113-01 — default "wrap"
      showDomainSubdomainControls?: boolean;       // CALUX-V113-02 — default false (OFF)
    Add to DEFAULT_CALENDAR_CONFIG (§56-64):
      layoutMode: "wrap",
      showDomainSubdomainControls: false,
    Read them in the component body alongside the existing reads (§113-123):
      const layoutMode = (cfg.layoutMode ?? DEFAULT_CALENDAR_CONFIG.layoutMode) as "wrap" | "strip";
      const showDomainSubdomainControls = cfg.showDomainSubdomainControls ?? false;
    Field identifiers are the planner's choice (locked: the two enum LABELS "Wrap"/"Continuous strip"
    and the checkbox label "Show domain/subdomain controls"); use layoutMode / showDomainSubdomainControls.
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit 2>&1 | grep -i calendarconfigpanel || echo "no tsc errors in CalendarConfigPanel"</automated>
  </verify>
  <acceptance_criteria>
    - CalendarConfigPanel.tsx contains `layoutMode` and `showDomainSubdomainControls` in the
      CalendarConfig type.
    - DEFAULT_CALENDAR_CONFIG contains `layoutMode: "wrap"` and `showDomainSubdomainControls: false`.
    - `cd packages/web && npx tsc --noEmit` reports no new errors for CalendarConfigPanel.tsx.
  </acceptance_criteria>
  <done>Both config fields typed + defaulted; component reads effective values.</done>
</task>

<task type="auto">
  <name>Task 2: Render the "Display" section (layout-mode select + controls toggle + info icon)</name>
  <files>packages/web/src/components/charts/CalendarConfigPanel.tsx</files>
  <read_first>
    - packages/web/src/components/charts/CalendarConfigPanel.tsx §501-545 (palette + respondToFilters +
      cap-status block — insert the new Display section AFTER the respondToFilters row, still inside the
      `tableId !== undefined` fragment, before the cap-status hints)
    - packages/web/src/components/charts/TimelineConfigPanel.tsx §357-406 (OPTIONS section header +
      config-toggle pattern)
    - packages/web/src/components/charts/MapConfigPanel.tsx §643 (native title= tooltip)
  </read_first>
  <action>
    Inside the configured-source fragment (after the respondToFilters `.ds-field` at §531, before the
    cap-probe hints §533), add a Display section header + two controls:

    1) Section header (mirror Timeline OPTIONS §358):
       <div className="config-group-label" style={{ marginTop: 16 }}>DISPLAY</div>

    2) Layout mode (a `.ds-select`, planner discretion 2-option select vs checkbox — use a select for
       clarity; LABELS LOCKED):
       <div className="ds-field">
         <span className="ds-field-label">Layout</span>
         <select className="ds-select" aria-label="Layout"
                 value={layoutMode}
                 onChange={(e) => patch({ layoutMode: e.target.value as "wrap" | "strip" })}>
           <option value="wrap">Wrap</option>
           <option value="strip">Continuous strip</option>
         </select>
       </div>

    3) "Show domain/subdomain controls" checkbox (mirror the respondToFilters .accent-checkbox row
       §518-531) WITH an info icon (native title=, MapConfigPanel pattern — NO shared component):
       <div className="ds-field">
         <label className="ds-field-label" htmlFor="calendar-show-controls">
           Show domain/subdomain controls
           <span
             title="When on, viewers get dropdowns on the widget to change the time grouping live, without editing the widget. The viewer's choice is not saved and resets on reload."
             aria-label="About show domain/subdomain controls"
             style={{ marginLeft: 4, cursor: "help" }}
           >ⓘ</span>
         </label>
         <input
           id="calendar-show-controls"
           type="checkbox"
           className="accent-checkbox"
           checked={showDomainSubdomainControls}
           onChange={(e) => patch({ showDomainSubdomainControls: e.target.checked })}
           aria-label="Show domain/subdomain controls"
         />
       </div>

    Theme tokens only — NO raw hex (theme-guard scans this file). The checkbox label text must be
    EXACTLY "Show domain/subdomain controls". Info wording is planner discretion (above is a good default).
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/styles/theme-guard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - CalendarConfigPanel.tsx contains the exact string "Show domain/subdomain controls".
    - CalendarConfigPanel.tsx contains a DISPLAY section header (`>DISPLAY<`).
    - CalendarConfigPanel.tsx contains layout `<option value="wrap">Wrap</option>` and
      `<option value="strip">Continuous strip</option>`.
    - CalendarConfigPanel.tsx contains a `title=` attribute on the info glyph (native tooltip).
    - `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` passes (no raw hex added).
  </acceptance_criteria>
  <done>Display section renders both new controls; info-icon tooltip present; theme-guard green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Specs for the Display section — and re-run the full Phase 66/68 panel suite green</name>
  <files>packages/web/src/components/charts/CalendarConfigPanel.spec.tsx</files>
  <read_first>
    - packages/web/src/components/charts/CalendarConfigPanel.spec.tsx (full — Tests 1-16 must keep
      passing; append new Display-section tests; reuse renderPanel helper §76-96)
  </read_first>
  <behavior>
    - Test (Display layout default): renderPanel({tableId:1, tableRef:"demo.events"}) → the "Layout"
      select value === "wrap" (DEFAULT_CALENDAR_CONFIG.layoutMode).
    - Test (Display layout options): the Layout select lists both "Wrap" and "Continuous strip".
    - Test (layout change): changing Layout to "strip" calls onChange with layoutMode:"strip".
    - Test (controls toggle present + default OFF): a checkbox named /show domain.subdomain controls/i
      is present and unchecked when showDomainSubdomainControls is absent/false.
    - Test (controls toggle ON): clicking it calls onChange with showDomainSubdomainControls:true.
    - Test (info icon): an element with the info tooltip is present (getByLabelText
      /about show domain.subdomain controls/i, or query the title attribute).
    - Test (defaults object): DEFAULT_CALENDAR_CONFIG has layoutMode:"wrap" and
      showDomainSubdomainControls:false.
  </behavior>
  <action>
    Append the new tests to CalendarConfigPanel.spec.tsx (do NOT modify Tests 1-16). Use the existing
    `renderPanel(...)` helper and `screen.getByLabelText` / `getByRole("checkbox", {name})` patterns
    already used by Tests 12-16. Run the FULL file — all old + new tests must pass.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/CalendarConfigPanel.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/web && npx vitest run src/components/charts/CalendarConfigPanel.spec.tsx` passes —
      Phase 66 Tests 1-11 + Phase 68 Tests 12-16 + the NEW Display-section tests all green (no regression).
    - The spec asserts the Layout select default value is "wrap".
    - The spec asserts the "Show domain/subdomain controls" checkbox defaults unchecked (OFF).
    - The spec asserts DEFAULT_CALENDAR_CONFIG.showDomainSubdomainControls === false and
      .layoutMode === "wrap".
  </acceptance_criteria>
  <done>Display-section specs pass; the pre-existing Phase 66/68 config-panel criteria are NOT regressed.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx vitest run src/components/charts/CalendarConfigPanel.spec.tsx` green (old + new).
- `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` green (no raw hex added).
- `cd packages/web && npx tsc --noEmit` clean.
- This plan does NOT edit CalendarRenderer.tsx (Plan 03 owns it) — safe to run Wave 1 parallel with Plan 01.
</verification>

<success_criteria>
- New "Display" section in CalendarConfigPanel after existing fields, grouping layout mode
  (Wrap default / Continuous strip) + "Show domain/subdomain controls" toggle (default OFF) + info icon.
- CalendarConfig + DEFAULT_CALENDAR_CONFIG carry layoutMode + showDomainSubdomainControls for Plan 03.
- Phase 66 (5/5 config criteria) + Phase 68 respondToFilters specs preserved (not regressed).
</success_criteria>

<output>
After completion, create `.planning/phases/68.1-calendar-ux-wrapped-github-week-block-layout-config-gated-on-widget-domain-subdomain-controls/68.1-02-SUMMARY.md`
</output>
