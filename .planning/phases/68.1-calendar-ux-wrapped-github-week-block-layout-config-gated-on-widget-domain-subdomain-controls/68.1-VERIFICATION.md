---
phase: 68.1-calendar-ux-wrapped-github-week-block-layout-config-gated-on-widget-domain-subdomain-controls
verified: 2026-06-17T00:00:00Z
status: passed
score: 10/10 must-haves verified
re_verification: false
---

# Phase 68.1: Wrapped Calendar Layout + On-Widget Domain/Subdomain Controls — Verification Report

**Phase Goal:** Replace the calendar's flat one-tall-column layout with wrapped GitHub-style domain-group blocks (compact in both dimensions), and add a config-gated set of viewer-facing on-widget domain/subdomain dropdowns — preserving the Phase 67/68 cell-drill, highlight, gap-fill, color scale, and respondToFilters gating against the new layout.
**Verified:** 2026-06-17
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | CalendarRenderer renders via layoutCalendar() positioned blocks (day → 7-row DOW × week columns; hour → 6×4 grid; week/month → compact grids) | VERIFIED | `layoutCalendar` called in `useMemo` at line 342-344; flat `subdomainKeys.map` double-loop is ABSENT |
| 2 | Domain groups are separate labeled blocks; layoutMode "wrap" (default) / "strip" config option | VERIFIED | `layoutMode` branches at line 474; "wrap"/"strip" options in `CalendarConfigPanel`; per-block `<text>` labels rendered |
| 3 | WEEK_START is a single exported constant (Monday-ISO=1) in calendarLayout.ts; no scattered hardcoded anchor | VERIFIED | `export const WEEK_START = 1` at calendarLayout.ts:41; renderer imports and uses it via `layoutCalendar`; Phase 69 dependency annotated |
| 4 | Sparse DOW labels (Mon/Wed/Fri) + per-block group labels render | VERIFIED | `DOW_LABELS = ["Mon","Wed","Fri"]` at line 450; `DOW_LABEL_ROWS = [0,2,4]`; group labels via `formatTimelineTick(block.domainKey)` at line 550 |
| 5 | Phase 68 cell-drill (handleCellClick), active-cell highlight (appliedCell), empty-cell grey + non-interactive, gap-fill, color scale, tooltip, FROM/respondToFilters gating — all preserved | VERIFIED | `handleCellClick` uses `effSubdomain` at line 390; `appliedCell` useMemo at line 186; `respondToFilters` read at line 115 and in dep array at line 325; `gapFillCalendar` useMemo at line 338; `data-empty="true"` with `pointerEvents:none` pattern preserved |
| 6 | CalendarConfig.showDomainSubdomainControls default FALSE; checkbox labeled "Show domain/subdomain controls" + info icon; in a new "DISPLAY" section | VERIFIED | `showDomainSubdomainControls: false` in `DEFAULT_CALENDAR_CONFIG` at line 67; section header `>DISPLAY<` at line 540; info span with `title=` at line 561; `aria-label="About show domain/subdomain controls"` at line 562 |
| 7 | layoutMode config field present in Display section (Wrap / Continuous strip) | VERIFIED | `<option value="wrap">Wrap</option>` at line 551; `<option value="strip">Continuous strip</option>` at line 552 |
| 8 | When showDomainSubdomainControls ON: 2 viewer dropdowns render; subdomain gated to VALID_DOMAIN_SUBDOMAIN[effDomain]; changing re-fetches | VERIFIED | `data-testid="calendar-control-bar"` at line 501; `VALID_DOMAIN_SUBDOMAIN[effDomain].map` at line 518; `effDomain`/`effSubdomain` in dep array at lines 323-324 |
| 9 | VIEW-LOCAL: renderer never calls patch/onChange to persist; viewer override resets on reload | VERIFIED | Static grep: CLEAN — no `patch(` or `onChange(` calls in CalendarRenderer.tsx; viewer state via `useState<...|null>(null)` resets on mount |
| 10 | Effective domain/subdomain = viewer override ?? config — used consistently for buildCalendarSql, labels, computeCellBounds, AND handleCellClick drill | VERIFIED | `effDomain = viewerDomain ?? domain` at line 206; `effSubdomain = viewerSubdomain ?? subdomain` at line 207; used in buildCalendarSql (lines 283-284), layoutCalendar (line 343), computeCellBounds (lines 390 and 595), group labels (line 550), DOW gutter (lines 455, 536) |

**Score:** 10/10 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/web/src/lib/calendarLayout.ts` | Pure (cells, domain, subdomain) → PositionedBlock[] helper | VERIFIED | 290 lines; exports `layoutCalendar`, `WEEK_START`, `PositionedCell`, `PositionedBlock`; zero React/Zustand/Recharts imports; UTC-only date parsing via `parseUTCMs` |
| `packages/web/src/lib/calendarLayout.spec.ts` | TDD specs for block geometry per subdomain + WEEK_START | VERIFIED | 296 lines; covers empty input, day (7-row DOW), hour (6×4 compact), week (ordinal compact), month (4-col compact), identity preservation |
| `packages/web/src/components/charts/CalendarConfigPanel.tsx` | CalendarConfig type + DEFAULT_CALENDAR_CONFIG + Display section | VERIFIED | `layoutMode?: "wrap"|"strip"` at line 54; `showDomainSubdomainControls?: boolean` at line 55; defaults set at lines 66-67; DISPLAY section at line 540 |
| `packages/web/src/components/charts/CalendarConfigPanel.spec.tsx` | Specs for Display section (Tests 17-23) | VERIFIED | 407 lines; Tests 17-23 cover layout default/options/change, controls toggle default OFF/ON, info icon, DEFAULT_CALENDAR_CONFIG values |
| `packages/web/src/components/charts/CalendarRenderer.tsx` | Wrapped block layout + top control bar + view-local effective domain/subdomain | VERIFIED | Imports `layoutCalendar`, `WEEK_START`; `effDomain`/`effSubdomain` threaded throughout; `calendar-control-bar` gated on `showControls`; `handleViewerDomainChange`/`handleViewerSubdomainChange` view-local |
| `packages/web/src/components/charts/CalendarRenderer.spec.tsx` | Specs for wrapped render + control bar + effective-value drill; preserves Tests 0-24 | VERIFIED | 977 lines; Tests 25-32 cover: wrapped layout, group labels, control bar OFF/ON, dependent gating, re-fetch on viewer change, no-persist static assertion, effective-value drill |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `calendarLayout.ts` | `calendarGapFill.ts` | `import type { CalendarCell, CalendarRow }` | WIRED | Imports CalendarCell/CalendarRow at line 29 |
| `calendarLayout.ts` | `calendarBin.ts` | `import type { CalendarDomain, CalendarSubdomain }` | WIRED | Type imports at line 30 |
| `CalendarRenderer.tsx` | `calendarLayout.ts` | `layoutCalendar({ rows: grid.rows, domain: effDomain, subdomain: effSubdomain })` | WIRED | Import at line 41; useMemo call at lines 342-344 |
| `CalendarRenderer.tsx` (effDomain/effSubdomain) | `buildCalendarSql + computeCellBounds + handleCellClick + labels` | `effDomain = viewerDomain ?? domain; effSubdomain = viewerSubdomain ?? subdomain` | WIRED | Lines 206-207; used at lines 283-284 (SQL), 390+595 (drill), 550 (labels) |
| `CalendarConfigPanel.tsx` | `widget.config (CalendarConfig consumed by CalendarRenderer)` | `patch({ layoutMode }) / patch({ showDomainSubdomainControls })` | WIRED | Lines 549, 571 |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CALUX-V113-01 | 68.1-01, 68.1-02, 68.1-03 | Wrapped GitHub-style domain-group blocks via layoutCalendar; DOW/week/month compact grids; layoutMode config; Phase 68 behaviors preserved | SATISFIED | calendarLayout.ts pure helper; CalendarRenderer uses layoutCalendar; flat double-map ABSENT; all Phase 68 invariants preserved |
| CALUX-V113-02 | 68.1-02, 68.1-03 | Config-gated viewer dropdowns (showDomainSubdomainControls, default OFF); view-local effective domain/subdomain; dependent gating; re-fetch on change | SATISFIED | Display section in CalendarConfigPanel; control bar in CalendarRenderer; view-local state (no patch/onChange); VALID_DOMAIN_SUBDOMAIN[effDomain] gating |

Both requirements marked Complete in REQUIREMENTS.md (lines 76-77).

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `calendarLayout.ts` | — | Phase 69 spike annotation | INFO | Intentional — WEEK_START=1 is documented as NOT-YET-VERIFIED, Phase 69 corrects. No code impact. |

No blockers or warnings found.

---

### Locked Invariants

| Invariant | Check | Result |
|-----------|-------|--------|
| CalendarRenderer does NOT import materializeFilter/dropFilterView/fromSwap | Static grep on import lines | CLEAN |
| No raw hex in CalendarRenderer.tsx | grep `#[0-9a-fA-F]{3,6}` excluding comments | CLEAN |
| No raw hex in CalendarConfigPanel.tsx | grep `#[0-9a-fA-F]{3,6}` excluding comments | CLEAN |
| CalendarRenderer never calls patch()/onChange() | Static grep | CLEAN |
| Phase 66 Tests 1-16 not regressed | CalendarConfigPanel.spec.tsx Tests 1-16 preserved (Tests 17-23 appended) | VERIFIED (orchestrator confirmed 2322 tests pass) |
| Phase 67/68 Tests 0-24 not regressed | CalendarRenderer.spec.tsx Tests 0-24 preserved (Tests 25-32 appended) | VERIFIED (orchestrator confirmed 2322 tests pass) |

---

### Human Verification Required

#### 1. GitHub-style wrapped block visual appearance

**Test:** Open a calendar widget with domain=year, subdomain=day in a browser. Confirm domain-group blocks wrap left-to-right then downward (not one tall column), each with a group label above and Mon/Wed/Fri DOW labels on the left.
**Expected:** Compact tiled blocks resembling a GitHub contribution calendar grid, with labeled rows.
**Why human:** Visual layout correctness cannot be verified by grep or unit tests.

#### 2. "Continuous strip" layoutMode behavior

**Test:** In CalendarConfigPanel, switch Layout to "Continuous strip" and observe a calendar widget.
**Expected:** All domain blocks render in one horizontal row with horizontal scroll; no vertical wrapping.
**Why human:** SVG pixel layout behavior requires visual inspection.

#### 3. Viewer dropdown re-fetch feels instant and correct

**Test:** With showDomainSubdomainControls ON, change the Domain dropdown on the widget from "month" to "week". Confirm the calendar re-renders with the new grouping and the Subdomain dropdown updates to show only valid options (day/hour).
**Expected:** Smooth re-fetch; Subdomain options restricted to VALID_DOMAIN_SUBDOMAIN["week"] = ["day","hour"]; selection resets on page reload.
**Why human:** Network re-fetch behavior and UX feel require browser testing.

---

### Gaps Summary

No gaps. All 10 observable truths are verified, all 6 artifacts pass all three levels (exists, substantive, wired), all key links are wired, both requirements are satisfied, and all locked invariants hold.

---

_Verified: 2026-06-17_
_Verifier: Claude (gsd-verifier)_
