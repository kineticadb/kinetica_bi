---
status: complete
phase: 104-synchronized-map-viewports
source: [104-01-SUMMARY.md, 104-02-SUMMARY.md]
started: 2026-07-08T00:38:25Z
updated: 2026-07-08T18:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Sync toggle appears in map config (default OFF)
expected: Open a map widget's config (gear). A "VIEWPORT SYNC" group shows a "Sync map viewport" toggle, OFF by default.
result: pass

### 2. Two sync-enabled maps pan/zoom together
expected: On one dashboard, enable "Sync map viewport" on two map widgets. Pan and zoom one map — the other map pans/zooms to the same viewport (center + zoom) to match.
result: pass

### 3. Sync-disabled map stays put
expected: Add a third map with "Sync map viewport" OFF. When you pan/zoom the synced maps, the third map does NOT move.
result: pass

### 4. No oscillation under rapid panning
expected: Rapidly pan/zoom one synced map back and forth. The maps settle to the moved viewport with no jitter, bounce-back, or infinite oscillation between them (echo-loop guard holds).
result: pass
note: Oscillation behavior passed. The info-popup issue observed during this test was diagnosed and fixed — see Test 7.

### 7. Map info popup works (regression discovered during Test 4)
expected: Clicking a map feature opens the info popup with feature details (pre-existing v1.4 Map Info Popup behavior).
result: pass
note: Root cause was NOT the Phase 104 sync wiring but a pre-existing multi-map scoping bug — the global infoSelectionStore.activeLayerId is shared across all maps, so on a dashboard with 2+ maps each map's InfoSelectionView reset the store when the active layer wasn't in its own layer set, closing/stealing the popup the clicked map just opened. Fixed by scoping the active selection to the owning map widget (activeWidgetId) in commit 9652182. User confirmed the popup works correctly.

### 5. Per-dashboard scope + reset on dashboard switch/logout
expected: Move a synced map, then switch to a different dashboard (or log out and back in). Maps on the other dashboard are NOT driven by the first dashboard's viewport; returning shows no stale forced viewport leaking across dashboards.
result: [pending]

### 6. Backward-compat — existing maps unchanged
expected: A map with sync OFF/never-configured pans, zooms, filters, and renders exactly as before — no behavior change from this feature.
result: pass

### 5. Per-dashboard scope + reset on dashboard switch/logout
expected: Move a synced map, then switch dashboards (or log out/in). Maps on the other dashboard are NOT driven by the first dashboard's viewport; no stale forced viewport leaks across dashboards.
result: pass

## Summary

total: 7
passed: 7
issues: 0
pending: 0
skipped: 0

## Gaps

- truth: "Clicking a map feature opens the info popup with feature details (pre-existing v1.4 behavior)"
  status: resolved
  reason: "User reported: this passes but i noticed the info popup on the map is not working"
  severity: major
  test: 7
  root_cause: "Pre-existing (NOT a Phase 104 regression): infoSelectionStore.activeLayerId is a single dashboard-global value with no per-widget scope. Every MapChartRenderer renders an InfoPopup, and each InfoSelectionView reset() the store when the active layer wasn't in its own eligible-layer set. On a dashboard with 2+ maps using different layers, clicking one map set the global active layer; the other map's view then saw a foreign layer and reset the store, closing/stealing the popup the clicked map just opened."
  fix: "Added activeWidgetId to infoSelectionStore; the map click fan-out sets it to widget.id; InfoPopup renders/dismisses only when it owns the selection; InfoSelectionView takes ownerWidgetId. Dashboard-scoped Info Card still mirrors any map. Regression test InfoPopup H7 added."
  artifacts: [commit 9652182]
  missing: []
  debug_session: ""
  resolved_at: "2026-07-08"
