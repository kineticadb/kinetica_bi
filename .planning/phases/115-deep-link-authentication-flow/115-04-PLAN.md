---
phase: 115-deep-link-authentication-flow
plan: 04
type: execute
wave: 4
depends_on: ["115-01", "115-02", "115-03"]
files_modified:
  - .planning/phases/115-deep-link-authentication-flow/115-UAT.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [DLINK-V121-03]

must_haves:
  truths:
    - "Every automated gate the project defines is green on the finished phase, not just the specs this phase wrote."
    - "The locked milestone constraints are verified by static audit, not by assertion: no server diff, no new dependency, no router, no second sessionStorage key, no new CSS class."
    - "The two requirements that cannot be proven by grep — the login banner's legibility in both themes, and the visual correctness of the landed dashboard after a real OIDC round trip — are checked by a human against a running app."
    - "DLINK-V121-03 is recorded as Complete only after the operator signs off."
  artifacts:
    - path: ".planning/phases/115-deep-link-authentication-flow/115-UAT.md"
      provides: "the operator's recorded walk-through result"
      contains: "UAT-115"
  key_links:
    - from: "115-UAT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "DLINK-V121-03 status flipped to Complete only on operator approval"
      pattern: "DLINK-V121-03"
---

<objective>
Prove the phase, including the two things no grep can prove.

Purpose: CLAUDE.md documents this project's specific failure mode — seven heatmap UI defects
passed `tsc`, `vitest` AND `theme-guard` while rendering visibly broken, and Phase 114's
light-mode banner defect (`.onboarding-banner` hardcoding `#cae8ff`) reached the operator's eye
because theme-guard exempts `global.css` from its hex scan wholesale. This phase adds a new line
of copy to `.login-banner`, which has the SAME hardcoded-hex shape (`global.css:2212` sets
`color: #cae8ff`, with a `:root[data-theme="light"]` override at `:2173`). And the OIDC round trip
cannot be executed in jsdom at all — the whole point of the mechanism is a full-page navigation to
a real IdP. Both go to a human.

Output: a green four-gate run, a static constraint audit, an operator UAT record, and the
requirement flipped.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/115-deep-link-authentication-flow/115-CONTEXT.md
@.planning/phases/115-deep-link-authentication-flow/115-01-SUMMARY.md
@.planning/phases/115-deep-link-authentication-flow/115-02-SUMMARY.md
@.planning/phases/115-deep-link-authentication-flow/115-03-SUMMARY.md
@.planning/phases/114-deep-link-load-error-states/114-UAT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full gate run + static audit of the locked constraints</name>
  <files>(none — read-only audit; output recorded in 115-04-SUMMARY.md)</files>

  <read_first>
    - CLAUDE.md §"Test gates" (the exact four gates, and the rule that the server gate is SET-BASED — irrelevant here only if the server diff is genuinely empty, which criterion 3 below proves)
    - .planning/phases/115-deep-link-authentication-flow/115-01-SUMMARY.md, 115-02-SUMMARY.md, 115-03-SUMMARY.md (what was actually built, and any deviations to re-verify)
  </read_first>

  <action>
Run each command below from the repository root and record the literal output in the SUMMARY.
Do NOT paraphrase a result; if a gate is red, STOP and report rather than proceeding to the
checkpoint.

```bash
cd packages/web && npx tsc --noEmit
cd packages/web && npx vitest run
cd packages/web && npx vitest run src/styles/theme-guard.spec.ts
git diff --stat HEAD~4 -- packages/server            # adjust the range to the phase's first commit
git diff --stat HEAD~4 -- packages/web/package.json
git diff --stat HEAD~4 -- packages/web/src/styles/global.css
```

Then run the static constraint audit below and record every count. Each of these encodes a LOCKED
constraint; a non-matching count is a phase failure, not a rounding error.
  </action>

  <acceptance_criteria>
All greps run from `packages/web` unless stated. Every "before" count was executed against the
phase-start tree.

1. `npx tsc --noEmit` → no output, exit 0.
2. `npx vitest run` → 100% pass, total test count **≥ 3727** (the phase-start baseline; this phase only adds tests, never removes — the one amended test in `App.deeplink.spec.tsx` was replaced by two).
3. `npx vitest run src/styles/theme-guard.spec.ts` → green.
4. **Frontend-only:** `git diff --stat <phase-start-commit> -- packages/server` → **empty output**. No server change, no OIDC `state` threading, no `WEB_REDIRECT_BASE` edit.
5. **No new dependency, no router:** `git diff --stat <phase-start-commit> -- packages/web/package.json` → **empty output**, and `grep -rl "react-router\|useNavigate\|BrowserRouter\|useSearchParams" src` → **no files** (before: no files).
6. **One sessionStorage mechanism:** `grep -rc "kbi_returnTo" src/App.tsx` → **1** (before: 1), and `grep -rl "kbi_pendingDashboard\|kbi_deepLink\|kbi_pendingLink" src` → **no files** (before: no files). ROADMAP §Phase 115 criterion 2.
7. **Two write moments, deliberately:** `grep -c "sessionStorage.setItem" src/App.tsx` → **2** (before: **1**), `grep -c "sessionStorage.setItem" src/components/LoginPage.tsx` → **0** (before: 0), and `grep -c "DELIBERATELY NOT UNIFIED" src/App.tsx` → **1** (before: **0**).
8. **No new CSS class, no new colour:** `git diff --stat <phase-start-commit> -- src/styles/global.css` → **empty output**, and `grep -c "login-banner" src/components/LoginPage.tsx` → **1** (before: **2** — the two duplicated blocks collapsed into one shared element; no third class was invented).
9. **The amendment happened and is recorded:** `grep -c "a pasted link beats the Phase 7 ReturnTo page restore" src/App.deeplink.spec.tsx` → **0** (before: **1**); `grep -c "PHASE 115 AMENDMENT" src/App.deeplink.spec.tsx` → **1** (before: **0**); `grep -c "DEEPLINK-114:" src/App.deeplink.spec.tsx` → **9** (before: **10** — 9 test titles plus the file-header mention).
10. **Suppression is immediate, not delayed:** `grep -c "deepLinkConsumedRef.current = true" src/App.tsx` → **2** (before: **1**).
11. **Phase-wide test anchor:** `grep -rc "AUTHLINK-115" src | grep -v ":0" | wc -l` → **≥ 6** files (before: **0** files anywhere in `src/`).
  </acceptance_criteria>

  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run && npx vitest run src/styles/theme-guard.spec.ts</automated>
  </verify>

  <done>All three gates green, all 11 audit criteria hold, and every literal count is recorded in the SUMMARY. Any mismatch is reported, not smoothed over.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator walk-through — the two things no grep can prove</name>
  <files>.planning/phases/115-deep-link-authentication-flow/115-UAT.md</files>

  <read_first>
    - .planning/phases/114-deep-link-load-error-states/114-UAT.md (the format and the precedent — including UAT-114-2, recorded honestly as "not exercised" because the loading state was too fast to observe. Record the same way if something cannot be exercised; do not upgrade an unobserved step to a pass.)
  </read_first>

  <action>
Pause and hand the running app to the operator. Present <what-built> and <how-to-verify> below
verbatim, then WAIT. Do not self-certify any step: the two requirements this checkpoint exists for
(banner legibility in both themes, and the real OIDC round trip) are the two 115-RESEARCH.md
identified as unprovable by grep, and CLAUDE.md forbids dressing an unprovable requirement in a
guard that only looks rigorous.

Create `.planning/phases/115-deep-link-authentication-flow/115-UAT.md` from the operator's answers,
one `UAT-115-<n>` row per numbered step, each marked PASS / FAIL / NOT EXERCISED with a note —
mirroring `114-UAT.md`'s format, including its precedent of recording a step honestly as NOT
EXERCISED rather than upgrading it to a pass.
  </action>

  <what-built>
    - A logged-out visit to `?dashboard=<id>` now shows the login page with a new line in the
      existing `.login-banner`: **"Sign in to open this dashboard."** — in BOTH the SSO and the
      password branches. If the session just expired, the existing "Your session has ended."
      message stands alone instead; the two are never stacked.
    - Password mode: signing in lands directly on the linked dashboard (this already worked; it is
      now pinned by `src/App.passwordDeepLink.spec.tsx`).
    - OIDC mode: clicking "Sign in with SSO" stores the pending dashboard id in the existing
      `kbi_returnTo` sessionStorage key before the browser leaves; after the IdP round trip the
      app opens that dashboard and rewrites the address bar back to `?dashboard=<id>`.
    - An expiry that caught you on a page OTHER than the dashboard list returns you to THAT page,
      strips the stale `?dashboard=` from the bar, and kills the stale link for the session.
    - A link that resolves to nothing after sign-in (deleted, no access, or signed in as the wrong
      account) lands on the dashboard list with Phase 114's EXISTING combined message — no new
      string, no new code path, and deliberately no "signed in as X".
  </what-built>

  <how-to-verify>
    Record each step in `.planning/phases/115-deep-link-authentication-flow/115-UAT.md` as
    `UAT-115-<n>` with PASS / FAIL / NOT EXERCISED and a note.

    **Group A — the banner, which theme-guard structurally cannot check.**
    `.login-banner` hardcodes `color: #cae8ff` (`global.css:2212`) with a light-mode override at
    `:2173`; theme-guard exempts `global.css` from its hex scan wholesale, which is exactly the
    hole the Phase 114 `.onboarding-banner` defect came through. This needs eyes.
    1. Log out. Visit `<app>/?dashboard=<an id you can see>`. Confirm the login page appears (not
       an error, not a blank page) and the banner reads "Sign in to open this dashboard."
    2. Confirm the address bar still shows `?dashboard=<id>` while you are on the login page.
    3. Toggle to LIGHT theme and repeat step 1. Confirm the banner text is legible against its
       background — not washed out, not low-contrast. This is the specific defect class Phase 114
       shipped and the operator caught.
    4. Toggle to DARK theme and repeat step 1. Same judgement.
    5. Let a session EXPIRE (or force a 401) while a dashboard is open. Confirm the login page
       shows ONLY "Your session has ended. Please sign in again." — the deep-link line must NOT
       be stacked underneath it.

    **Group B — password mode (run wherever `AUTH_MODE` is password).**
    6. Logged out, visit `<app>/?dashboard=<id>`, sign in with the password form. Confirm you land
       DIRECTLY on that dashboard — not on the dashboard list, and with no visible flash of the
       list on the way.
    7. Confirm the dashboard is the right one, its widgets render, NO filters are applied, and no
       config panel is open. (Locked restore depth: the dashboard in its default state.)

    **Group C — OIDC round trip. This cannot be automated at all** — the mechanism IS a full-page
    navigation to a real IdP, which jsdom cannot perform. Run it against a real OIDC deployment.
    8. Logged out, visit `<app>/?dashboard=<id>`. Confirm the banner from step 1.
    9. Click "Sign in with SSO", complete authentication at the IdP.
    10. Confirm you land DIRECTLY on that dashboard — not the list.
    11. Confirm the address bar reads `?dashboard=<id>` again after landing (it was a bare `/`
        when the IdP redirected you back).
    12. Press browser Back ONCE. Confirm you do not get ejected out of the application (the
        address-bar restore uses replaceState, so no extra entry should exist).
    13. Press F5 on the landed dashboard. Confirm it reloads into the same dashboard.
    14. Repeat 8-10 but sign in as an account that CANNOT see that dashboard. Confirm you land on
        the dashboard list with the message "This dashboard isn't available — it may have been
        deleted, or you may not have access." and that the message does NOT name the account, the
        id, or which of the two reasons applies.
    15. In a NEW tab, click "Sign in with SSO" from a plain `<app>/` with no `?dashboard=` param.
        Confirm you land on the dashboard list as usual — the deep-link path did not hijack a
        normal sign-in.

    **Group D — the conflict rule (the hardest to hit; NOT EXERCISED is an acceptable answer).**
    16. Open a dashboard, navigate to Roles, and have the session expire before the address bar
        clears (this is a race — if you cannot reproduce it, record NOT EXERCISED rather than
        guessing). Sign back in. Expected: you return to ROLES, the address bar has NO
        `?dashboard=` param, and no "This dashboard isn't available" banner appears.
    17. Still in that session, click Dashboards in the sidebar. Expected: the dashboard LIST —
        the stale link must not silently reopen.

    **Decisions to confirm, not just behaviour.** Two deliberate choices are recorded in the code
    and the SUMMARYs; say whether you accept them:
    - `kbi_returnTo` is written from TWO places (the expiry handler and the sign-in commit) and
      they are deliberately NOT unified — at expiry time in-memory page state is the only source
      of where you were; at sign-in time the URL is the only source of the dashboard id.
    - One Phase 114 test (`a pasted link beats the Phase 7 ReturnTo page restore`) was replaced by
      two, because its hand-written setup is the state Phase 115 locks the other way. Phase 114's
      intent is still tested, in the shape the app actually produces.
  </how-to-verify>


  <done>115-UAT.md exists with a verdict recorded for every numbered step, and the operator has either typed "approved" or listed specific failing step numbers.</done>

  <resume-signal>Type "approved" to record the phase as verified, or list the failing UAT-115-<n> numbers with what you saw.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the result</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/REQUIREMENTS.md (the status table — DLINK-V121-03 currently reads `Pending`; the surrounding rows show the wording convention used for Complete entries)
    - .planning/phases/115-deep-link-authentication-flow/115-UAT.md (Task 2's record — the source of truth for whether this flip is allowed)
  </read_first>

  <action>
ONLY if Task 2 was approved:

1. In `.planning/REQUIREMENTS.md`, flip the checkbox for **DLINK-V121-03** in the requirements
   list from `- [ ]` to `- [x]`, and change its row in the Phase-mapping table from
   `| DLINK-V121-03 | Phase 115 | Pending |` to a Complete entry naming the evidence, in the
   style the DLINK-V121-04/05 rows already use — e.g.
   `Complete (115-01/02/03 code + tests; 115-04 operator UAT approved <date> — OIDC round trip and both banner themes confirmed live)`.
   Do NOT touch any other requirement row (a parallel-executor clobber risk this repo has hit
   before; this plan is the ONLY one in its wave, which is why the shared-doc edit lives here).

2. Do NOT hand-edit `.planning/ROADMAP.md`'s progress table or plan checkboxes — `execute-phase`
   owns those.

If Task 2 was NOT approved, make NO edit to `.planning/REQUIREMENTS.md` and report the gaps
instead; gap closure is a separate `/gsd:plan-phase --gaps` run.
  </action>

  <acceptance_criteria>
Run from the repository root.

1. `grep -c "^- \[x\] \*\*DLINK-V121-03\*\*" .planning/REQUIREMENTS.md` → **1** (before: **0** — the line currently reads `- [ ] **DLINK-V121-03**`)
2. `grep -c "| DLINK-V121-03 | Phase 115 | Pending |" .planning/REQUIREMENTS.md` → **0** (before: **1**)
3. `grep -c "DLINK-V121-0[4567]" .planning/REQUIREMENTS.md` → **9** (before: **9**) — proves no neighbouring requirement row was disturbed
4. `ls .planning/phases/115-deep-link-authentication-flow/115-UAT.md` → exists, and `grep -c "UAT-115" .planning/phases/115-deep-link-authentication-flow/115-UAT.md` → **≥ 17**
  </acceptance_criteria>

  <verify>
    <automated>grep -c "^- \[x\] \*\*DLINK-V121-03\*\*" .planning/REQUIREMENTS.md && grep -c "UAT-115" .planning/phases/115-deep-link-authentication-flow/115-UAT.md</automated>
  </verify>

  <done>DLINK-V121-03 reads Complete with its evidence named, no neighbouring requirement row changed, and 115-UAT.md records all 17 steps with an explicit PASS / FAIL / NOT EXERCISED each.</done>
</task>

</tasks>

<verification>
- All four project gates green (web tsc, web vitest 100%, theme-guard, and an empty server diff standing in for the server gates)
- 115-UAT.md exists with every step recorded, and any step that could not be reproduced is recorded as NOT EXERCISED rather than upgraded to a pass (the 114-UAT precedent)
- DLINK-V121-03 is Complete in REQUIREMENTS.md
</verification>

<success_criteria>
- Every locked constraint is verified by a command whose before-count was 0 or whose result is an empty diff — not by assertion
- The banner was seen by a human in BOTH themes
- The OIDC round trip was executed against a real IdP, or explicitly recorded as not exercised with a reason
- v1.21's last open requirement is closed
</success_criteria>

<output>
After completion, create `.planning/phases/115-deep-link-authentication-flow/115-04-SUMMARY.md`
with the literal gate output, the 11 audit counts, and the operator's verdict.
</output>
