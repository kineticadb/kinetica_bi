---
phase: 64-verification-live-uat
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - .planning/phases/64-verification-live-uat/64-UAT.md
  - .planning/phases/64-verification-live-uat/64-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [VERIFY-V112-01]

must_haves:
  truths:
    - "The operator runs 64-UAT.md against live deployed Kinetica (password mode) and attests each section at a BLOCKING checkpoint:human-verify"
    - "64-VERIFICATION.md exists in the 61-VERIFICATION.md / 54-VERIFICATION.md format with frontmatter overall_status (passed|gaps_found) and a per-SC evidence table"
    - "overall_status: passed ONLY if all 4 SCs attested PASS in 64-UAT.md AND every deterministic gate in 64-01 is green; any red deterministic gate or any FAIL section forces gaps_found (route to 64.x) — never passed against a red gate"
    - "SC1 evidence reflects the dv-isolated live drill (pie + >= 1 other chart type; same-dv widgets update; source-table/other-dv UNAFFECTED) + the chip-clear-to-unfiltered-dv; SC2 reflects the table-path-unchanged regression + the sole-materialize-trigger invariant"
    - "VERIFY-V112-01 is ticked [x] in REQUIREMENTS.md IFF overall_status is passed"
    - "ROADMAP Phase 64 marked Complete IFF passed"
  artifacts:
    - path: ".planning/phases/64-verification-live-uat/64-VERIFICATION.md"
      provides: "Compiled v1.12 milestone-gate verification record"
      contains: "overall_status"
  key_links:
    - from: "64-VERIFICATION.md"
      to: "64-01-AUTOMATED-GATES.md + 64-UAT.md"
      via: "SC evidence rows citing the gate verdict + operator attestation"
      pattern: "64-01-AUTOMATED-GATES|64-UAT"
---

<objective>
Gate the v1.12 milestone: have the operator run the authored `64-UAT.md` against live deployed Kinetica at a BLOCKING checkpoint, then compile `64-VERIFICATION.md` from the 64-01 automated-gate record + the 64-02 operator attestations, set `overall_status`, and — if passed — tick VERIFY-V112-01 in REQUIREMENTS.md and mark ROADMAP Phase 64 Complete. This is the artifact that closes the v1.12 milestone gate.

Purpose: SC1 (dv-isolated live drill + chip clear) and SC2 (table-path-unchanged + sole-materialize-trigger) are behaviors only a live walk can prove. This plan owns the operator checkpoint (mirrors how 61-02/61-03 gated on attestation — "at checkpoint awaiting operator walk-through attestation") and then compiles a single citable verification record mapping the 4 ROADMAP SCs to evidence, refusing `passed` against any red deterministic gate or any FAIL section.

Output: `.planning/phases/64-verification-live-uat/64-VERIFICATION.md` + (iff passed) REQUIREMENTS.md tick + ROADMAP Complete.

NO feature code. The walk exercises the running app; the compile is pure bookkeeping. If 64-UAT finds gaps, this plan records `gaps_found`, does NOT tick the requirement, and routes to a separate 64.x repro-test-driven phase — after which this plan re-runs to re-compile.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<!-- FORMAT PRECEDENT — clone this compiled-verification structure (frontmatter overall_status, Goal Achievement / Observable Truths table, SC->Evidence mapping, requirements coverage, overall verdict) -->
@.planning/phases/61-verification-live-uat/61-03-PLAN.md
@.planning/phases/63-client-dv-drill-down/63-VERIFICATION.md

<!-- THE TWO EVIDENCE INPUTS THIS COMPILES -->
@.planning/phases/64-verification-live-uat/64-01-AUTOMATED-GATES.md
@.planning/phases/64-verification-live-uat/64-UAT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator runs the live dv-drill-down walk against deployed Kinetica and attests each section in 64-UAT.md</name>
  <files>.planning/phases/64-verification-live-uat/64-UAT.md</files>
  <action>Operator executes the authored 64-UAT.md (shipped "pending attestation" by 64-02) against the deployed app (PASSWORD mode) as RPereira@kinetica.com; fills each check's status/evidence and the Attestation Summary. Exact steps are in how-to-verify below. This task BLOCKS — the executor STOPS here until the operator types "approved" (or reports gaps). Do NOT compile 64-VERIFICATION.md (Task 2) until this attestation is recorded.</action>
  <read_first>
    - .planning/phases/64-verification-live-uat/64-UAT.md (the walk to execute — every fenced item)
    - .planning/phases/64-verification-live-uat/64-01-AUTOMATED-GATES.md (confirm §0 P4 — SC3 gates ALL PASS)
  </read_first>
  <what-built>
    A complete, self-contained UAT walk-through doc (`64-UAT.md`) covering the 4 ROADMAP SCs of the v1.12 dynamic-view drill-down, plus the SC3 automated-gate record (`64-01-AUTOMATED-GATES.md`, overall_verdict recorded). The app is the existing deployed build — Phases 62 (server FROM-dv-view) + 63 (client dv drill) are already committed; NO feature code changed in Phase 64.
  </what-built>
  <how-to-verify>
    1. Confirm §0 preconditions P1-P4: app running against deployed Kinetica in PASSWORD mode; a dashboard with a materialized dynamic view, a dv-backed PIE (drillDownColumn set), >= 1 other same-dv widget, a SOURCE-TABLE widget on the same underlying table, and ideally a second dv with its own widget; a table-backed drill-capable widget for §3; 64-01 gates recorded ALL PASS. Mark each PASS/FAIL with evidence.
    2. Execute §1 (dv PIE drill): click a pie slice on the dv-backed pie -> the pie + every same-dv widget re-render LIVE to the drilled slice (1.1, 1.2). Confirm the SOURCE-TABLE widget (and any second-dv widget) does NOT change — the dv drill did NOT filter the source table (1.3 — the killed bug). Confirm a removable chip labeled with the DV NAME + clicked value appears (1.4). Remove the chip -> the dv widgets revert to the unfiltered dv (1.5). Attest 1.1-1.5.
    3. Execute §2 (other chart type): repeat the dv drill on >= 1 OTHER dv-backed type (bar/line/scatter/table/records) -> same dv-isolated live behavior + dv chip + clear-reverts. Record which type(s). Attest 2.1 (2.2 optional).
    4. Execute §3 (invariants): drill a TABLE-backed widget -> it filters the source table exactly as before, same-table widgets update, a table-scoped chip appears, clear reverts; dv-backed widgets are NOT pulled in (3.1). In DevTools Network, confirm a dv drill fires exactly one dv-scoped `POST /api/filter/materialize` (body has `dynamicViewId`) and a table drill fires the table-scoped one (no dynamicViewId), with no rogue/duplicate materialize and the scopes never crossing (3.2 — sole-materialize-trigger + existing route). Attest 3.1-3.2.
    5. §4.1: confirm SC3/SC4 are covered by the 64-01 record (record-only, no rerun).
    6. Fill every `status:`/`evidence:` block, the gaps YAML, and the Attestation Summary (`overall_result`, sections passed/failed, attested_by, attested_on) in 64-UAT.md.

    Routing: any FAIL halts the UAT and routes to a 64.x repro-test-driven gap plan (failing RED repro -> fix -> re-walk the affected item before Task 2 compiles). Trivial fixes may ride inline at executor discretion. Gaps are NOT accepted as tech debt — this is the v1.12 milestone gate.
  </how-to-verify>
  <verify>Operator attestation recorded in 64-UAT.md Attestation Summary (overall_result set to passed or gaps_found).</verify>
  <acceptance_criteria>
    - Every §0-§4 item in 64-UAT.md has status PASS or FAIL (no PENDING) with one-line evidence
    - Attestation Summary completed: overall_result, sections_passed/failed/skipped, attested_by, attested_on
    - §1.3 (source-table widget UNAFFECTED — the killed bug) is explicitly attested
    - §3.1 (table-backed path unchanged) + §3.2 (sole-materialize-trigger / existing route) explicitly attested
  </acceptance_criteria>
  <done>64-UAT.md fully attested; overall_result is passed (all sections PASS) or gaps_found (-> 64.x).</done>
  <resume-signal>Type "approved" (sets overall_result: passed) or describe the gaps found (sets overall_result: gaps_found -> spin a 64.x plan).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Compile 64-VERIFICATION.md from the gate record + UAT attestations</name>
  <read_first>
    - .planning/phases/61-verification-live-uat/61-03-PLAN.md (the compiled-verification structure + gating rule to mirror)
    - .planning/phases/63-client-dv-drill-down/63-VERIFICATION.md (the verification-record format precedent for this milestone — frontmatter, Observable-Truths table, SC->Evidence, requirements coverage, invariants, overall verdict)
    - .planning/phases/64-verification-live-uat/64-01-AUTOMATED-GATES.md (SC3 evidence — overall_verdict, commit, gate numbers)
    - .planning/phases/64-verification-live-uat/64-UAT.md (SC1/SC2 attestations, gaps block, overall_result)
  </read_first>
  <files>.planning/phases/64-verification-live-uat/64-VERIFICATION.md</files>
  <action>
    Write `64-VERIFICATION.md` cloning the 63-VERIFICATION.md / 61-VERIFICATION.md structure:

    Frontmatter:
    ```
    ---
    phase: 64-verification-live-uat
    verified: <ISO timestamp>
    status: <passed|gaps_found>
    overall_status: <passed|gaps_found>
    score: <N>/4 ROADMAP success criteria verified
    operator: RPereira@kinetica.com
    requirements:
      VERIFY-V112-01: <satisfied|pending>
    ---
    ```

    GATING RULE (enforce strictly): `overall_status: passed` ONLY IF — (a) every deterministic gate in 64-01 is green (frontend vitest 100% from packages/web, web tsc clean, server tsc clean, targeted v1.12 web + server spec groups green) AND the server vitest set-gate ⊆ TD-V16-TEST-ISOLATION, AND (b) 64-UAT.md `overall_result: passed` (all sections PASS, gaps `[]`). If any deterministic gate is red OR any UAT section FAILED -> `overall_status: gaps_found` (route to 64.x); NEVER `passed` against a red gate. `failed` is not an acceptable close state.

    Body (mirror the 63/61-VERIFICATION headings):
    - Phase Goal + Verified/Status/Operator/Attestation header.
    - Automated Gate Results table (transcribe from 64-01: frontend vitest count from packages/web, web tsc, server tsc, server set-gate ⊆ known-flaky, targeted v1.12 web + server spec groups, source-tree clean guard).
    - Goal Achievement -> Observable Truths (ROADMAP SC) table: one row per SC1-SC4 with Status (VERIFIED/GAP) + Evidence.
      * SC1 evidence MUST state: dv-backed PIE drill filtered the dv's data LIVE; same-dv widgets updated; the SOURCE-TABLE widget (+ other-dv widget if present) stayed UNAFFECTED (dv-isolated — the killed bug); a dv-NAME chip appeared and clearing reverted to the unfiltered dv (§1.1-1.5); a >= 1 OTHER chart type confirmed the same (§2).
      * SC2 evidence MUST state: the TABLE-backed drill path behaved exactly as before (§3.1) AND the sole-materialize-trigger invariant held (§3.2 — a dv drill used the existing `POST /api/filter/materialize` with `dynamicViewId`, no new route, no rogue materialize, scopes never crossed).
      * SC3 -> 64-01 gates (frontend vitest 100% from packages/web, web+server tsc clean, server set-gate ⊆ known-flaky, targeted v1.12 specs green).
      * SC4 -> 64-VERIFICATION.md itself is the compiled record; VERIFY-V112-01 ticked + ROADMAP Phase 64 Complete (Task 3).
    - ROADMAP Phase 64 SC -> Evidence mapping table.
    - Requirements Coverage table: VERIFY-V112-01 -> satisfied (iff passed) with evidence citing both 64-01 and 64-UAT. Note the v1 dv-drill reqs DVDRILL-V112-01..05 were exercised live in the walk (cite the §s).
    - Invariants table: dv-isolated scope HELD; table-backed path UNCHANGED; sole-materialize-trigger PRESERVED; no new server route; zero Phase-64 source diff (docs only).
    - Human Verification Results: operator attestation transcribed from 64-UAT.md.
    - Gaps Summary: from 64-UAT.md §5 gaps block (empty iff passed; else list + 64.x routing).
    - Overall Verdict section.

    Score = count of SCs VERIFIED out of 4.
  </action>
  <verify>
    <automated>test -f .planning/phases/64-verification-live-uat/64-VERIFICATION.md && grep -qE "overall_status:\s*(passed|gaps_found)" .planning/phases/64-verification-live-uat/64-VERIFICATION.md && grep -qiE "dv-isolated|source-table|isolated to|unaffected" .planning/phases/64-verification-live-uat/64-VERIFICATION.md && grep -qiE "sole.{0,12}materialize|table-backed|unchanged" .planning/phases/64-verification-live-uat/64-VERIFICATION.md && grep -q "VERIFY-V112-01" .planning/phases/64-verification-live-uat/64-VERIFICATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists with frontmatter `overall_status` (passed|gaps_found) consistent with the gating rule
    - Per-SC evidence table for SC1-SC4; SC1 row reflects the dv-isolated live drill + source-table-unaffected + dv-name chip + clear-reverts + the other chart type; SC2 row reflects the table-path-unchanged regression + the sole-materialize-trigger invariant
    - Automated Gate Results table transcribed from 64-01; requirements coverage cites VERIFY-V112-01 (and notes DVDRILL-V112-01..05 exercised live)
    - overall_status is NOT passed if any deterministic gate is red or any UAT section FAILED
  </acceptance_criteria>
  <done>64-VERIFICATION.md compiled with an honest overall_status mapping all 4 SCs to evidence from 64-01 + 64-UAT.</done>
</task>

<task type="auto">
  <name>Task 3: Tick VERIFY-V112-01 + mark ROADMAP Phase 64 Complete (IFF passed)</name>
  <read_first>
    - .planning/phases/64-verification-live-uat/64-VERIFICATION.md (the overall_status just compiled in Task 2)
    - .planning/REQUIREMENTS.md (the VERIFY-V112-01 line + the traceability row)
    - .planning/ROADMAP.md (Phase 64 entry, the Progress table, and the v1.12 Progress table)
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
    CONDITIONAL on Task 2's `overall_status`:

    IF `overall_status: passed`:
    - REQUIREMENTS.md: change `- [ ] **VERIFY-V112-01**` -> `- [x] **VERIFY-V112-01**`; update its Traceability row (the `| VERIFY-V112-01 | Phase 64 | Pending |` line) status `Pending` -> `Complete`.
    - ROADMAP.md: mark the `[ ] **Phase 64: Verification + Live UAT**` checkbox `[x]`; in BOTH the top Progress table (the `| 64. verification-live-uat | v1.12 | Not started | - |` row) and the v1.12 Progress table set Phase 64 status `Not started` -> `Complete` with the completion date; update the §Phase 64 "Plans: TBD" line to note 64-VERIFICATION.md compiled `passed`. Update the milestone line `🚧 **v1.12 ...** — Phases 62-64 (active, ...)` to reflect v1.12 ready to ship (do NOT mark the milestone shipped here — that is `/gsd:complete-milestone`; just reflect Phase 64 Complete).

    IF `overall_status: gaps_found`:
    - Do NOT tick VERIFY-V112-01 (leave `[ ]`, Traceability status `Pending`/`Gaps`).
    - ROADMAP.md: leave Phase 64 open; add a note that a 64.x gap-closure phase is required before re-compile. Record the gap IDs from 64-UAT.md §5.

    .planning/REQUIREMENTS.md + .planning/STATE.md are tracked on the SHARED origin while the rest of .planning is gitignored locally — `git add -f` is needed to stage REQUIREMENTS.md (ROADMAP.md is gitignored locally; commit what the tooling tracks). Do NOT force-push; fetch before push (per shared-remote convention). Do not touch any other requirement rows or phases.
  </action>
  <verify>
    <automated>cd /Users/rydelpereira/Documents/projects/kinetica_bi; STATUS=$(grep -m1 -oE "overall_status:[[:space:]]*(passed|gaps_found)" .planning/phases/64-verification-live-uat/64-VERIFICATION.md | grep -oE "passed|gaps_found"); echo "overall_status=$STATUS"; if [ "$STATUS" = "passed" ]; then grep -q "\[x\] \*\*VERIFY-V112-01\*\*" .planning/REQUIREMENTS.md && grep -qE "\[x\] \*\*Phase 64" .planning/ROADMAP.md && echo "TICKED + ROADMAP COMPLETE OK"; else grep -q "\[ \] \*\*VERIFY-V112-01\*\*" .planning/REQUIREMENTS.md && echo "LEFT UNTICKED OK"; fi</automated>
  </verify>
  <acceptance_criteria>
    - VERIFY-V112-01 ticked [x] in REQUIREMENTS.md (+ Traceability row Complete) IFF overall_status is passed; left [ ] otherwise
    - ROADMAP Phase 64 marked Complete (with date, in both Progress tables + the §Phase 64 plans line) IFF passed; left open with a 64.x note otherwise
    - No unrelated requirement rows or phases modified
  </acceptance_criteria>
  <done>REQUIREMENTS.md + ROADMAP.md reflect the verification outcome consistently with 64-VERIFICATION.md overall_status.</done>
</task>

</tasks>

<verification>
- Operator executed the live dv-drill-down walk against deployed Kinetica (password mode); every 64-UAT.md section attested PASS/FAIL with evidence; Attestation Summary completed with overall_result
- 64-VERIFICATION.md compiled in the 63/61-VERIFICATION.md format with overall_status honest to the gates + attestations; SC1 reflects dv-isolated live drill + source-table-unaffected + chip-clear; SC2 reflects table-path-unchanged + sole-materialize-trigger
- VERIFY-V112-01 ticked + Phase 64 Complete IFF passed; otherwise left open with 64.x routing
- Refuses passed against any red deterministic gate or any FAIL section
</verification>

<success_criteria>
`64-VERIFICATION.md` records the v1.12 milestone-gate outcome with overall_status (passed -> ready for `/gsd:complete-milestone 1.12`; gaps_found -> 64.x). VERIFY-V112-01 and ROADMAP reflect that outcome. The headline payoff (a dv chart drill filters the dynamic view LIVE, isolated from the source table, chip clears back to the unfiltered dv) is attested.
</success_criteria>

<output>
After completion, create `.planning/phases/64-verification-live-uat/64-03-SUMMARY.md`.
</output>
