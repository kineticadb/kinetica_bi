---
phase: 64-verification-live-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/64-verification-live-uat/64-01-AUTOMATED-GATES.md
autonomous: true
requirements: [VERIFY-V112-01]

must_haves:
  truths:
    - "Frontend vitest is 100% green (run FROM packages/web, NOT --root from repo root), >= 2133 baseline, 0 failures"
    - "web tsc --noEmit -p tsconfig.json clean (exit 0) AND server tsc --noEmit -p tsconfig.json clean (exit 0) as SEPARATE gates"
    - "server vitest failing FILE set is a SUBSET of the TD-V16-TEST-ISOLATION known-flaky set (NOT a fixed pass-count)"
    - "The v1.12 dv-drill-down targeted specs (web + server) all pass — proving the chain under test"
    - "git diff --name-only -- packages/server is captured for context (source tree should be clean — Phases 62/63 already committed)"
  artifacts:
    - path: ".planning/phases/64-verification-live-uat/64-01-AUTOMATED-GATES.md"
      provides: "SC3 automated-gate evidence record consumed by 64-02 §0 + 64-03"
      contains: "overall_verdict"
  key_links:
    - from: "64-01-AUTOMATED-GATES.md"
      to: "TD-V16-TEST-ISOLATION known-flaky set"
      via: "set-subset assertion table (server vitest)"
      pattern: "auth.oidc|auth.routes|boot.hardening|boot.wipe|bootstrap|db.smoke|oidc.module|routes.wms"
---

<objective>
Run the v1.12 SC3 automated gates fresh against HEAD and record the results in `64-01-AUTOMATED-GATES.md` — the evidence file the operator cites in §0 of the UAT (64-02) and that 64-03 compiles into the verification record.

Purpose: SC3 of the milestone gate is a set of deterministic automated gates (frontend vitest 100% from packages/web, web + server tsc clean, server vitest set-based ⊆ known-flaky). Recording them once, authoritatively, frees the live walk-through from re-running anything and gives 64-03 a citable artifact. Mirrors 61-01-AUTOMATED-GATES.md / 57-01-AUTOMATED-GATES.md.

Output: `.planning/phases/64-verification-live-uat/64-01-AUTOMATED-GATES.md` with `overall_verdict: ALL PASS` (or a clearly-recorded failure if a deterministic gate is red).

NO feature code. This plan only RUNS gates and RECORDS results. If a deterministic gate is red, record it as-is and surface it — do NOT fix code here (gap fixes go to a separate 64.x decimal phase).
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- FORMAT PRECEDENT — reuse this structure exactly (frontmatter overall_verdict, gate-results table, per-gate detail, server set-subset table, closing verdict block) -->
@.planning/phases/61-verification-live-uat/61-01-AUTOMATED-GATES.md

<!-- WHAT SHIPPED — current frontend baseline (2133/2133) + the gates the verifier already ran -->
@.planning/phases/63-client-dv-drill-down/63-VERIFICATION.md

<gates_reality>
<!-- Locked test-gate policy for SC3 (ROADMAP §Phase 64 SC3 + v1.12 locked invariants). -->
- Frontend vitest is DETERMINISTIC -> must be 100% green. Run FROM `packages/web` (`cd packages/web && npx vitest run`). DO NOT use `--root` from repo root — that doubles src/src paths and breaks cwd-relative specs (TEST CWD PITFALL). Baseline at Phase 63 close = 2133 (63-VERIFICATION gate table); count must be >= 2133 with 0 failures.
- Server vitest is FLAKY -> SET-BASED gate ONLY. Pass iff the failing FILE set is a subset of the TD-V16-TEST-ISOLATION known-flaky set: {auth.oidc, auth.routes, boot.hardening, boot.wipe, bootstrap, db.smoke, oidc.module, routes.wms}. NEVER assert a fixed pass-count for the server suite.
- web tsc AND server tsc are SEPARATE gates: `cd packages/web && npx tsc --noEmit -p tsconfig.json` and `cd packages/server && npx tsc --noEmit -p tsconfig.json`, each must exit 0.
- v1.12 split: Phase 62 = server-only, Phase 63 = frontend-only. Both are already committed. The working tree source should be CLEAN. `git diff --name-only -- packages/server` is context (expect empty or only committed-already state); flag anything unexpected.
</gates_reality>

<targeted_specs>
<!-- The v1.12 dv-drill-down specs to record as a targeted green group (proves the chain under test). -->
<!-- WEB (run FROM packages/web): -->
- src/store/filterStore.spec.ts          (dvFilters slice + addDvFilter/removeDvFilter/clearDvFilters + reset)
- src/store/filterViewStore.spec.ts      (dvViews slice + setDvView/markDvMaterializing/clearDvView + reset)
- src/api/client.spec.ts                 (dynamicViewId on materialize/drop + kind-scoped cache key)
- src/components/charts/WidgetRenderer.spec.tsx  (dv-aware dispatchDrillDown + dv materialize trigger + filtered-dv->dv read-path; THE bug-fix test ~2618 + isolation 2657/2687)
- src/components/DashboardsPage.spec.tsx  (removable dv-name chips + lifecycle reset)
<!-- SERVER (run FROM packages/server): -->
- tests/lib.viewNaming.spec.ts                  (buildFilterViewName _dv<id> segment vs table _t<id> unchanged)
- tests/routes.filter-materialize-dv.spec.ts    (dv-path FROM dv-view, distinct filter view, 404/400 guards, fail-safe, DELETE dv branch — both auth modes)
- tests/routes.filter-materialize.spec.ts       (table-path byte-unchanged regression)
</targeted_specs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the SC3 gates + the targeted v1.12 spec groups and capture raw output</name>
  <read_first>
    - .planning/phases/61-verification-live-uat/61-01-AUTOMATED-GATES.md (the exact record format to mirror — gate-results table, per-gate detail, server set-subset table)
    - .planning/phases/63-client-dv-drill-down/63-VERIFICATION.md (current frontend baseline 2133/2133 + the gates the verifier already ran; the targeted spec line:col references)
  </read_first>
  <files>(no source files — this task runs commands and captures output for Task 2)</files>
  <action>
    Capture HEAD commit: `git rev-parse --short HEAD`.

    Run each gate and capture the exact tail/summary output (tests, files, failures, exit code):

    1. Frontend vitest (DETERMINISTIC, 100% bar — RUN FROM packages/web, NOT --root):
       `cd packages/web && npx vitest run`
       Record: tests passed/total, files passed/total, failures. MUST be 0 failures and >= 2133 tests.

    2. Web tsc (separate gate):
       `cd packages/web && npx tsc --noEmit -p tsconfig.json` — record exit code (must be 0, no output).

    3. Server tsc (separate gate):
       `cd packages/server && npx tsc --noEmit -p tsconfig.json` — record exit code (must be 0, no output).

    4. Server vitest (SET-BASED gate — failing-file set only):
       `cd packages/server && npx vitest run`
       Record the list of FAILING test FILES (NOT the pass-count). Then assert each failing file is in the TD-V16-TEST-ISOLATION known-flaky set {auth.oidc, auth.routes, boot.hardening, boot.wipe, bootstrap, db.smoke, oidc.module, routes.wms}.

    5. Targeted v1.12 WEB spec group (proves the client chain under test — run FROM packages/web):
       `cd packages/web && npx vitest run src/store/filterStore.spec.ts src/store/filterViewStore.spec.ts src/api/client.spec.ts src/components/charts/WidgetRenderer.spec.tsx src/components/DashboardsPage.spec.tsx`
       Record tests/files passed; all must be green. Note the bug-fix/isolation assertions (WidgetRenderer.spec.tsx ~2618 dv drill fills dvFilters AND leaves filters[tableId] empty; 2657/2687 reverse isolation).

    6. Targeted v1.12 SERVER spec group (proves the server FROM-dv-view path — run FROM packages/server):
       `cd packages/server && npx vitest run tests/lib.viewNaming.spec.ts tests/routes.filter-materialize-dv.spec.ts tests/routes.filter-materialize.spec.ts`
       Record tests/files passed; all must be green (these are NOT in the known-flaky set, so they must be a hard 100% green).

    7. Source-tree clean guard (Phases 62/63 already committed):
       `git status --porcelain -- packages/server packages/web` and `git diff --name-only -- packages/server packages/web`.
       Expect a clean working tree for source (this phase produces DOCS only). Record verbatim; flag any unexpected uncommitted source diff.

    Do NOT modify any source. If a deterministic gate (frontend vitest, web tsc, server tsc, the targeted server group) is RED, record the failure verbatim — do not attempt a fix.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run 2>&1 | tail -5 ; npx tsc --noEmit -p tsconfig.json ; echo "web tsc exit=$?" ; cd ../server && npx tsc --noEmit -p tsconfig.json ; echo "server tsc exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Frontend vitest output captured (run from packages/web): 0 failures, >= 2133 tests, >= 95 files
    - web tsc exit 0; server tsc exit 0 (both captured, each via `-p tsconfig.json` from its own package dir)
    - Server vitest failing-file list captured and each file confirmed ⊆ TD-V16-TEST-ISOLATION set {auth.oidc, auth.routes, boot.hardening, boot.wipe, bootstrap, db.smoke, oidc.module, routes.wms}
    - Targeted v1.12 WEB group (5 files) captured all-green; targeted v1.12 SERVER group (3 files) captured all-green
    - `git status --porcelain` / `git diff --name-only` for packages/server + packages/web captured (expected clean source tree)
  </acceptance_criteria>
  <done>Raw output for all gates + the targeted v1.12 web/server groups + the clean-tree guard is captured and ready to transcribe into the record.</done>
</task>

<task type="auto">
  <name>Task 2: Author 64-01-AUTOMATED-GATES.md mirroring the 61-01 format</name>
  <read_first>
    - .planning/phases/61-verification-live-uat/61-01-AUTOMATED-GATES.md (clone its frontmatter + section structure exactly)
  </read_first>
  <files>.planning/phases/64-verification-live-uat/64-01-AUTOMATED-GATES.md</files>
  <action>
    Write `64-01-AUTOMATED-GATES.md` cloning the 61-01 structure:

    Frontmatter:
    ```
    ---
    automated_gates_ref: "64-01"
    phase: 64-verification-live-uat
    recorded_on: "<ISO timestamp>"
    commit: "<short HEAD>"
    overall_verdict: ALL PASS   # or a recorded failure if a deterministic gate is red
    ---
    ```

    Body sections (reuse 61-01 headings):
    - Title "# Phase 64: SC3 Automated Gate Results" + Recorded/HEAD/Operator (RPereira@kinetica.com)/Purpose line ("Evidence record for SC3 of the v1.12 milestone-gate verification. Consumed by 64-02 §0 (UAT preconditions) and 64-03 (compiled verification).").
    - Gate Results summary table: rows for `frontend_vitest`, `web_tsc`, `server_tsc`, `server_vitest_setgate`, `targeted_v112_web_specs`, `targeted_v112_server_specs`, `source_tree_clean_guard` — each with Command / Result / Numbers-or-Notes columns, transcribed from Task 1.
    - Gate Detail subsections (one per gate) with the exact command, observed output, exit code, verdict.
    - Server vitest SET-BASED subsection: a "Failing test FILES" table with an "In TD-V16-TEST-ISOLATION known-flaky list?" YES/NO column for each failing file, plus an explicit subset-check sentence. State plainly that the exact failing test-COUNT is irrelevant per the set-based policy — only the file set matters.
    - Targeted v1.12 specs subsections: a per-file pass table for the 5 WEB specs and the 3 SERVER specs. Call out the bug-fix/isolation assertions in WidgetRenderer.spec.tsx (dv drill fills dvFilters[dvId] AND leaves filters[sourceTableId] EMPTY — the original bug killed + regression-locked).
    - A "Source-tree clean guard" note: Phases 62 (server) + 63 (client) already committed; `git status --porcelain` / `git diff --name-only -- packages/server packages/web` clean; this verification phase produces DOCS only.
    - Final `## overall_verdict: ALL PASS` section enumerating the gate outcomes (mirror 61-01's closing block).

    The doc must be self-describing — 64-03 reads only this file for SC3 evidence.
  </action>
  <verify>
    <automated>test -f .planning/phases/64-verification-live-uat/64-01-AUTOMATED-GATES.md && grep -q "overall_verdict" .planning/phases/64-verification-live-uat/64-01-AUTOMATED-GATES.md && grep -qiE "TD-V16-TEST-ISOLATION" .planning/phases/64-verification-live-uat/64-01-AUTOMATED-GATES.md && grep -qE "frontend_vitest|frontend vitest" .planning/phases/64-verification-live-uat/64-01-AUTOMATED-GATES.md && grep -qE "targeted_v112|v1.12" .planning/phases/64-verification-live-uat/64-01-AUTOMATED-GATES.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists with frontmatter containing `overall_verdict`, `commit`, `recorded_on`
    - Contains all gate rows + a server-vitest set-subset table referencing the TD-V16-TEST-ISOLATION known-flaky files
    - Contains both targeted v1.12 spec group results (web 5-file + server 3-file) and the source-tree clean guard note
    - `overall_verdict` reflects the actual gate outcomes (ALL PASS only if every deterministic gate is green)
  </acceptance_criteria>
  <done>64-01-AUTOMATED-GATES.md exists, fully transcribed from Task 1, ready for the operator (64-02 §0) and the compiler (64-03).</done>
</task>

</tasks>

<verification>
- Frontend vitest 100% green from packages/web (>= 2133, 0 failures) — recorded
- web tsc clean AND server tsc clean (separate gates, each `-p tsconfig.json` from its package dir) — recorded
- Server vitest failing-file set ⊆ TD-V16-TEST-ISOLATION — recorded with the subset table
- Targeted v1.12 web (5) + server (3) dv-drill-down spec groups all green — recorded
- 64-01-AUTOMATED-GATES.md committed with an honest overall_verdict
</verification>

<success_criteria>
SC3 automated evidence is captured in `64-01-AUTOMATED-GATES.md` with `overall_verdict` reflecting the true gate state, citable by 64-02 §0 and 64-03. No source code touched.
</success_criteria>

<output>
After completion, create `.planning/phases/64-verification-live-uat/64-01-SUMMARY.md`.
</output>
