---
phase: 67-svg-calendar-renderer-read-only
plan: 03
type: execute
wave: 3
depends_on: ["67-02"]
files_modified:
  - packages/web/src/components/charts/WidgetRenderer.tsx
  - packages/web/src/components/charts/WidgetRenderer.spec.tsx
autonomous: true
requirements:
  - CAL-V113-04
must_haves:
  truths:
    - "A widget with type 'calendar' renders <CalendarRenderer widget tables /> — NOT the Phase 66 placeholder, NOT AggregatedWidgetRenderer"
    - "The calendar short-circuit stays BEFORE the AggregatedWidgetRenderer fallthrough (usesAggregation:false → sole-materialize-trigger invariant preserved)"
    - "A static-grep test re-asserts CalendarRenderer.tsx does NOT import materializeFilter / dropFilterView"
  artifacts:
    - path: "packages/web/src/components/charts/WidgetRenderer.tsx"
      provides: "calendar branch wired to the real CalendarRenderer"
      contains: "<CalendarRenderer"
    - path: "packages/web/src/components/charts/WidgetRenderer.spec.tsx"
      provides: "calendar widget routes to CalendarRenderer + sole-materialize-trigger static assertion"
      contains: "CalendarRenderer"
  key_links:
    - from: "packages/web/src/components/charts/WidgetRenderer.tsx"
      to: "packages/web/src/components/charts/CalendarRenderer.tsx"
      via: "effectiveWidget.type === 'calendar' branch renders <CalendarRenderer widget tables />"
      pattern: "<CalendarRenderer"
---

<objective>
Wire the real renderer into the dashboard: replace the Phase 66 placeholder body at
WidgetRenderer.tsx (the `effectiveWidget.type === "calendar"` branch, ~line 347) with
`<CalendarRenderer widget={effectiveWidget} tables={tables} />`, keeping the branch BEFORE
the AggregatedWidgetRenderer fallthrough so the sole-materialize-trigger invariant holds. Add
the import. Then add/extend the WidgetRenderer spec to assert a calendar widget routes to
CalendarRenderer, and re-assert (static grep) that CalendarRenderer never imports the
materialize functions.

Purpose: completes CAL-V113-04 by making a configured calendar widget actually render on a
dashboard. Isolated as its own small wave-3 plan so the wiring touches the shared
WidgetRenderer.tsx exactly once, after the renderer (67-02) is proven.
Output: WidgetRenderer.tsx edit + import; WidgetRenderer.spec.tsx assertions.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-svg-calendar-renderer-read-only/67-02-SUMMARY.md

<interfaces>
<!-- The branch to REPLACE (WidgetRenderer.tsx ~347, Phase 66 placeholder) — full current text: -->
```tsx
} else if (effectiveWidget.type === "calendar") {
  // Phase 66 (CAL-V113-01): calendar short-circuits BEFORE AggregatedWidgetRenderer
  // (usesAggregation:false → sole-materialize-trigger invariant preserved). The real
  // SVG CalendarRenderer ships in Phase 67; Phase 66 renders a placeholder so adding
  // a configured calendar widget does NOT route through AggregatedWidgetRenderer.
  body = (
    <div className="widget-placeholder" style={{ padding: 12, color: "var(--text-muted)" }} role="note">
      Calendar Heatmap — renderer coming in Phase 67.
    </div>
  );
} else {
  body = <AggregatedWidgetRenderer widget={effectiveWidget} />;
}
```

CalendarRenderer default export (Plan 67-02): `CalendarRenderer({ widget, tables }: { widget: WidgetDto; tables: TableDto[] })`.
Sibling import pattern in WidgetRenderer.tsx (mirror): `import TimelineRenderer from "./TimelineRenderer";`
The `tables` prop is already threaded into RenderWidgetBody (used by DataFilterRenderer/TimelineRenderer/InfoCardRenderer branches).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the calendar placeholder branch with the real CalendarRenderer + import</name>
  <read_first>
    - packages/web/src/components/charts/WidgetRenderer.tsx (the file being MODIFIED — lines ~30-58 imports + ~330-359 the calendar/timeline branches)
    - packages/web/src/components/charts/CalendarRenderer.tsx (the default export being wired — prop shape in &lt;interfaces&gt;)
  </read_first>
  <action>
    1. Add the import alongside the sibling renderer imports near the top of WidgetRenderer.tsx (mirror `import TimelineRenderer from "./TimelineRenderer";`): `import CalendarRenderer from "./CalendarRenderer";`.
    2. Replace ONLY the placeholder body inside the existing `else if (effectiveWidget.type === "calendar") {` branch with:
       `body = <CalendarRenderer widget={effectiveWidget} tables={tables} />;`
       Update the comment to note the Phase 66 placeholder is now the real Phase 67 renderer; keep the sole-materialize-trigger note. Do NOT move the branch — it must stay BEFORE the final `else { body = <AggregatedWidgetRenderer ... /> }` fallthrough.
    3. Do not touch any other branch.
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `WidgetRenderer.tsx` matches `import CalendarRenderer from "./CalendarRenderer"`
    - `WidgetRenderer.tsx` matches `<CalendarRenderer widget={effectiveWidget} tables={tables}`
    - `WidgetRenderer.tsx` no longer matches `Calendar Heatmap — renderer coming in Phase 67` (placeholder removed)
    - The `effectiveWidget.type === "calendar"` branch still appears BEFORE `body = <AggregatedWidgetRenderer` in source order
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Calendar widgets render the real CalendarRenderer (placeholder gone), branch ordering preserved, import added, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: WidgetRenderer spec — calendar routes to CalendarRenderer + re-assert no-materialize-import invariant</name>
  <read_first>
    - packages/web/src/components/charts/WidgetRenderer.spec.tsx (the file being EXTENDED — existing branch-routing test patterns)
    - packages/web/src/components/charts/DataFilterRenderer.spec.tsx (the canonical static source-grep `does NOT import materializeFilter/dropFilterView` assertion to mirror)
    - packages/web/src/components/charts/CalendarRenderer.tsx (the source asserted by the static grep)
  </read_first>
  <action>
    Extend packages/web/src/components/charts/WidgetRenderer.spec.tsx (or add a focused spec block) with:
    1. A render test: a WidgetDto with type "calendar" + a minimal valid config (tableId/tableRef/timeCol) renders the CalendarRenderer (assert the `calendar-renderer` / `calendar-loading` / `calendar-empty` testid from 67-02 appears, NOT the old placeholder text and NOT AggregatedWidgetRenderer output). Mock CalendarRenderer's data deps as the existing WidgetRenderer specs do for sibling renderers (or mock the CalendarRenderer module to a sentinel and assert it is invoked — match whichever pattern the existing WidgetRenderer.spec.tsx uses for TimelineRenderer).
    2. A static source-grep assertion (mirror DataFilterRenderer.spec.tsx): readFileSync CalendarRenderer.tsx and `expect(source).not.toMatch(/materializeFilter|dropFilterView/)` — re-asserting the sole-materialize-trigger invariant at the integration boundary (Phase 68 re-asserts again).
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/WidgetRenderer.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `WidgetRenderer.spec.tsx` matches `CalendarRenderer` and asserts a `type: "calendar"` widget routes to it (not the placeholder, not AggregatedWidgetRenderer)
    - `WidgetRenderer.spec.tsx` contains a `not.toMatch(/materializeFilter|dropFilterView/)` (or equivalent) static assertion against CalendarRenderer.tsx source
    - `npx vitest run src/components/charts/WidgetRenderer.spec.tsx` passes
  </acceptance_criteria>
  <done>Spec proves calendar widgets route to CalendarRenderer and re-asserts the no-materialize-import invariant; green.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx vitest run src/components/charts/WidgetRenderer.spec.tsx` — green
- `cd packages/web && npx tsc --noEmit` — clean
- `cd packages/web && npx vitest run` — full frontend suite 100% (existing + all Phase 67 specs)
- `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` — green (CalendarRenderer.tsx in scope)
</verification>

<success_criteria>
- A configured calendar widget renders the real SVG CalendarRenderer on a dashboard (CAL-V113-04 integration complete)
- Calendar branch stays before AggregatedWidgetRenderer fallthrough (sole-materialize-trigger invariant preserved + statically re-asserted)
- Full frontend vitest suite 100% from packages/web; web tsc clean
</success_criteria>

<output>
After completion, create `.planning/phases/67-svg-calendar-renderer-read-only/67-03-SUMMARY.md` noting the WidgetRenderer wiring is live and the renderer is dashboard-mounted — the stable structure Phase 68's cell-click handler builds on.
</output>
