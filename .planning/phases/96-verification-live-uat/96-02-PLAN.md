---
phase: 96-verification-live-uat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/charts/FilterSelectionPanel.tsx
  - packages/web/src/components/charts/FilterSelectionPanel.spec.tsx
  - packages/web/src/components/charts/ChartConfigPanel.tsx
  - packages/web/src/components/charts/KineticaWmsLayerForm.tsx
autonomous: true
gap_closure: true
requirements: [FSCOPE-V118-03]
must_haves:
  truths:
    - "Test 2: enabling Customize starts with ALL current sources CHECKED (start state == accept-all); unchecking one excludes that source."
    - "Test 7: a dv-bound viz's Filter Scope source list shows ONLY same-dv sibling widgets — NO 'Spatial draws (map)' row."
  artifacts:
    - path: "packages/web/src/components/charts/FilterSelectionPanel.tsx"
      provides: "Customize-on pre-populates all source ids; allowSpatial prop suppresses the spatial sentinel row for dv-bound vizs"
      contains: "allowSpatial"
    - path: "packages/web/src/components/charts/FilterSelectionPanel.spec.tsx"
      provides: "default-all-checked scenario + spatial-suppressed-for-dv scenario"
      contains: "allowSpatial"
  key_links:
    - from: "FilterSelectionPanel handleCustomizeToggle"
      to: "allowedSourceWidgetIds initialized to ALL current source ids"
      via: "map sources to ids (+ SPATIAL_DRAWS_SENTINEL when allowSpatial)"
      pattern: "allowedSourceWidgetIds"
    - from: "ChartConfigPanel + KineticaWmsLayerForm"
      to: "FilterSelectionPanel allowSpatial prop"
      via: "isDvBound -> allowSpatial=false"
      pattern: "allowSpatial"
---

<objective>
Close the two config-UI gaps. Both touch FilterSelectionPanel.tsx and its two consumers (ChartConfigPanel, KineticaWmsLayerForm) — grouped into ONE plan to avoid file conflicts. No file overlap with Plan 01 or Plan 03, so this runs in Wave 1 in parallel.

4. GAP 4 [MINOR, Test 2] — Enabling Customize starts ALL-UNCHECKED ("No sources selected — ignores all filters"), contradicting accept-all-by-default. User decision (locked): keep allow-list semantics but DEFAULT TO ALL-CHECKED on enable (pre-check every current source incl. the spatial sentinel where applicable) so the start state == accept-all; the user unchecks to exclude. Keep the accept-none warning for manual-uncheck-all.
5. GAP 5 [MINOR, Test 7] — "Spatial draws (map)" listed as a source for dv-bound vizs, but dv+spatial is server-rejected (400). Suppress the SPATIAL_DRAWS_SENTINEL row when the viz is dv-bound.

Purpose: Unblock the live UAT re-walk for Test 2 and the dv portion of Test 7.
Output: A FilterSelectionPanel that starts Customize at accept-all and hides the spatial row for dv-bound vizs, wired from both config surfaces; updated panel spec.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-verification-live-uat/96-UAT.md

<interfaces>
FilterSelectionPanel current props (packages/web/src/components/charts/FilterSelectionPanel.tsx):
  value: FilterSelectionConfig | undefined
  onChange: (next: FilterSelectionConfig | undefined) => void
  widgets: WidgetDto[]            // already filtered by caller to same-dv siblings for dv-bound
  selfWidgetId?: number

Current Customize-on handler (~41-49) sets { sourceMode: "allowlist", allowedSourceWidgetIds: [] } (empty = the bug).
Current sources list (~62-64): widgets.filter(w => isFilterProducingWidget(w.type) && w.id !== selfWidgetId).
Spatial sentinel row (~136-145) is ALWAYS rendered; toggled via toggleSource(SPATIAL_DRAWS_SENTINEL).
SPATIAL_DRAWS_SENTINEL + isFilterProducingWidget imported from "./filterSourceTypes".

Consumers:
  ChartConfigPanel.tsx ~740-753: renders FilterSelectionPanel with widgets={filterSourceWidgets}, selfWidgetId={widgetId}. draftDynamicViewId !== undefined means dv-bound. Gate already hides the panel when (draftDynamicViewId !== undefined && dvFilterScopeDisabled).
  KineticaWmsLayerForm.tsx ~1659-1667: renders FilterSelectionPanel with widgets={same-dv-filtered when dv}. layer?.dynamic_view_id != null means dv-bound. No selfWidgetId (layers are not widgets).
</interfaces>

@packages/web/src/components/charts/FilterSelectionPanel.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FilterSelectionPanel — default-all-checked on Customize + allowSpatial prop to suppress the spatial row (GAPs 4+5)</name>
  <files>packages/web/src/components/charts/FilterSelectionPanel.tsx, packages/web/src/components/charts/FilterSelectionPanel.spec.tsx</files>
  <read_first>
    packages/web/src/components/charts/FilterSelectionPanel.tsx — handleCustomizeToggle (~41-49), sources computation (~62-64), hasSentinel/showAcceptNoneWarning (~76-86), the spatial sentinel row (~136-145). packages/web/src/components/charts/FilterSelectionPanel.spec.tsx — existing scenarios for style + factories.
  </read_first>
  <behavior>
    - default-all-checked (chart): with 2 filter-producing sibling widgets and allowSpatial=true, clicking Customize calls onChange with sourceMode:"allowlist" and allowedSourceWidgetIds containing BOTH sibling ids AND SPATIAL_DRAWS_SENTINEL. Every checkbox renders checked; the accept-none warning is NOT shown.
    - default-all-checked (dv, allowSpatial=false): clicking Customize pre-checks the same-dv sibling ids only (NO sentinel); no spatial row renders.
    - spatial-suppressed-for-dv: with allowSpatial=false, the "Spatial draws (map)" row is NOT in the DOM regardless of mode.
    - accept-none warning preserved: starting from all-checked, manually unchecking every source (incl. sentinel) shows the existing "No sources selected — ignores all filters" warning.
  </behavior>
  <action>
    Add a new optional prop `allowSpatial?: boolean` (default true) to FilterSelectionPanelProps. Document: false for dv-bound vizs (dv+spatial is server-rejected).
    Rewrite handleCustomizeToggle's else-branch (~46-48): instead of an empty array, build the all-checked set from the CURRENTLY-LISTED sources: `const ids: (number|string)[] = sources.map((w) => w.id); if (allowSpatial) ids.push(SPATIAL_DRAWS_SENTINEL); onChange({ sourceMode: "allowlist", allowedSourceWidgetIds: ids });`. (Move the `sources` computation above the handlers, or recompute the source ids inline — keep selfWidgetId exclusion + isFilterProducingWidget.)
    Suppress the spatial row: wrap the SPATIAL_DRAWS_SENTINEL `<label>` (~136-145) in `{allowSpatial && ( ... )}`. Also exclude the sentinel from hasSentinel/showAcceptNoneWarning math when allowSpatial=false (when allowSpatial is false the sentinel cannot be selected, so showAcceptNoneWarning depends only on hasLiveSelection — confirm the existing computation still reads correctly; the warning should show when no live numeric source is selected and (allowSpatial ? !hasSentinel : true)).
    Keep all existing CSS classes (config-group, config-group-label, config-toggle, config-hint) — NO new class names. Danger text stays var(--danger). No raw hex.
    Add the two scenarios above to FilterSelectionPanel.spec.tsx.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/FilterSelectionPanel.spec.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Customize-on pre-checks all current sources (start == accept-all); allowSpatial=false hides the spatial row and pre-checks same-dv siblings only; accept-none warning still fires on manual uncheck-all; spec scenarios green; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Thread allowSpatial from both config surfaces (dv-bound -> allowSpatial=false)</name>
  <files>packages/web/src/components/charts/ChartConfigPanel.tsx, packages/web/src/components/charts/KineticaWmsLayerForm.tsx</files>
  <read_first>
    packages/web/src/components/charts/ChartConfigPanel.tsx ~740-753 (FilterSelectionPanel usage; draftDynamicViewId determines dv-bound). packages/web/src/components/charts/KineticaWmsLayerForm.tsx ~1659-1667 (FilterSelectionPanel usage; layer?.dynamic_view_id determines dv-bound).
  </read_first>
  <action>
    ChartConfigPanel: pass `allowSpatial={draftDynamicViewId === undefined}` to FilterSelectionPanel (~746). dv-bound (draftDynamicViewId defined) -> allowSpatial=false; table-bound -> true. Do not change the existing dvFilterScopeDisabled hide gate.
    KineticaWmsLayerForm: pass `allowSpatial={layer?.dynamic_view_id == null}` to FilterSelectionPanel (~1660). dv-bound layer -> false; table-bound layer -> true. Do not change the existing hide gate.
    No other change. Run tsc to confirm the prop typechecks at both sites.
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && grep -q "allowSpatial" src/components/charts/ChartConfigPanel.tsx && grep -q "allowSpatial" src/components/charts/KineticaWmsLayerForm.tsx</automated>
  </verify>
  <done>Both config surfaces pass allowSpatial (false for dv-bound, true for table-bound); tsc clean.</done>
</task>

</tasks>

<verification>
Web gates (run from packages/web):
- npx tsc --noEmit — clean.
- npx vitest run — 100% (esp. FilterSelectionPanel.spec.tsx).
- npx vitest run src/styles/theme-guard.spec.ts — green (no new classes/hex; var(--danger) only).
No server change (pure client UI). Sole-materialize-trigger invariant untouched by this plan.
</verification>

<success_criteria>
- Test 2: enabling Customize starts all-checked (accept-all); unchecking excludes a source; accept-none warning preserved.
- Test 7 (UI portion): dv-bound viz Filter Scope shows only same-dv siblings, no spatial row.
- tsc clean, vitest 100%, theme-guard green.
</success_criteria>

<output>
After completion, create `.planning/phases/96-verification-live-uat/96-02-SUMMARY.md`.
</output>
