---
phase: 96-verification-live-uat
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/lib/useFilterScopeSummary.ts
  - packages/web/src/lib/resolveLegendLayers.ts
  - packages/web/src/components/LayersLegendPanel.tsx
  - packages/web/src/components/LayersLegendPanel.spec.tsx
  - packages/web/src/components/charts/MapChartRenderer.tsx
autonomous: true
gap_closure: true
requirements: [COMM-V118-02, COMM-V118-01, FSCOPE-V118-03]
must_haves:
  truths:
    - "Test 4 addition (COMM-V118-02): a WMS layer that ignores at least one active filter shows an 'X of Y filters' indicator in the LayersLegendPanel; an accept-all layer shows none."
    - "Test 7 (badge/legend): with DISABLE_DV_FILTER_SCOPE=true, dv-bound widgets show NO badge and dv-bound layers show NO legend filter indicator (accept-all)."
  artifacts:
    - path: "packages/web/src/components/LayersLegendPanel.tsx"
      provides: "Per-layer 'X of Y filters' indicator row, reusing the widget-filter-badge class + theme tokens"
      contains: "widget-filter-badge"
    - path: "packages/web/src/lib/resolveLegendLayers.ts"
      provides: "ResolvedLegendLayer carries an optional filterSummary { appliedCount, totalCount } computed by the caller"
      contains: "filterSummary"
    - path: "packages/web/src/lib/useFilterScopeSummary.ts"
      provides: "dvFilterScopeDisabled forces accept-all (no ignored) for dv-bound sources"
      contains: "dvFilterScopeDisabled"
  key_links:
    - from: "MapChartRenderer resolvedLegendLayers"
      to: "computeFilterScopeSummary per layer (layer.filter_scope vs active filters + spatial; dv uses dvFilters, no spatial; respects dvFilterScopeDisabled)"
      via: "filterSummary field on ResolvedLegendLayer"
      pattern: "filterSummary"
    - from: "LayersLegendPanel layer row"
      to: "'X of Y filters' span (rendered only when appliedCount < totalCount)"
      via: "widget-filter-badge class"
      pattern: "widget-filter-badge"
    - from: "useFilterScopeSummary (badge) + MapChartRenderer (legend)"
      to: "accept-all when dv-bound AND dvFilterScopeDisabled"
      via: "force cfg=undefined / no ignored for dv sources under the flag"
      pattern: "dvFilterScopeDisabled"
---

<objective>
Close the legend-indicator scope-addition (COMM-V118-02) and the badge/legend half of the dv-disable gap (gap 3). These touch the legend/badge read path (LayersLegendPanel, resolveLegendLayers, MapChartRenderer, useFilterScopeSummary) — disjoint from Plan 01 (orchestrator/WidgetRenderer) and Plan 02 (config UI), so this runs in Wave 1 in parallel. useFilterScopeSummary.ts is owned EXCLUSIVELY by this plan (both the badge and the new legend indicator consume it).

6. GAP 6 [MINOR/SCOPE, COMM-V118-02, Test 4 addition] — Add a per-layer "X of Y filters" indicator in the LayersLegendPanel, mirroring the widget badge: reuse computeFilterScopeSummary semantics (layer.filter_scope vs the layer's table active filters + spatial; dv-bound layers use dvFilters, no spatial), show only when the layer ignores at least one filter, reuse the widget-filter-badge class + theme tokens, no invented classes.
3b. GAP 3 (badge/legend portion) — When dvFilterScopeDisabled is set, dv-bound widget badge (useFilterScopeSummary) + dv-bound layer legend indicator show NOTHING (accept-all), matching the orchestrator's full-disable resolution in Plan 01.

Purpose: Unblock the Test 4 legend addition and the badge/legend visuals for Test 7.
Output: A legend that surfaces per-layer applied/ignored filters (suppressed for dv layers under the disable flag) and a badge that disappears for dv widgets under the flag.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-verification-live-uat/96-UAT.md

<interfaces>
computeFilterScopeSummary (packages/web/src/lib/useFilterScopeSummary.ts) — PURE, reusable:
  computeFilterScopeSummary({ cfg: FilterSelectionConfig|undefined, activeFilters: ActiveFilter[], activeShapes: Shape[], spatialCapable: boolean }) -> { appliedCount, totalCount, applied, ignored }
  // Visibility rule (mirrors WidgetFilterBadge): show only when appliedCount < totalCount.

LayersLegendPanel is PURE-PRESENTATIONAL (NO store imports — PANEL-V17-01 lock). The caller (MapChartRenderer) computes per-layer data and passes it via ResolvedLegendLayer.

ResolvedLegendLayer (packages/web/src/lib/resolveLegendLayers.ts ~32-37):
  { layer: DashboardLayerDto; visible: boolean; dvStatus?: DvLayerStatus }
  -> ADD: filterSummary?: { appliedCount: number; totalCount: number }

MapChartRenderer resolvedLegendLayers useMemo (packages/web/src/components/charts/MapChartRenderer.tsx ~640-664) already enriches each entry with dvStatus from useDynamicViewStore.getState(). filter_scope is a TOP-LEVEL layer field (layer.filter_scope). layer.dynamic_view_id != null = dv-bound. Active filters: useFilterStore.getState().filters[tableId] (table) or .dvFilters[dvId] (dv). Spatial: useSpatialFilterStore.getState().shapes; eligibility via the renderer's existing getSpatialTargets({ config: widget.config }).filter(isSpatialTargetEligible) pattern (~lines 686-694), table-bound only — NOT aggregateSpatialTargetsByTable (not imported; widget is a single WidgetDto, not an array). dvFilterScopeDisabled from useAuthStore.

WidgetFilterBadge (packages/web/src/components/WidgetFilterBadge.tsx) renders `<span className="widget-filter-badge">{appliedCount} of {totalCount} filters</span>` only when appliedCount < totalCount. .widget-filter-badge lives in global.css. REUSE this class for the legend indicator.
</interfaces>

@packages/web/src/lib/useFilterScopeSummary.ts
@packages/web/src/lib/resolveLegendLayers.ts
@packages/web/src/components/LayersLegendPanel.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useFilterScopeSummary honors dvFilterScopeDisabled (accept-all for dv) — badge half of GAP 3</name>
  <files>packages/web/src/lib/useFilterScopeSummary.ts, packages/web/src/components/WidgetFilterBadge.spec.tsx</files>
  <read_first>
    packages/web/src/lib/useFilterScopeSummary.ts — the hook (~80-116; dv branch ~97-105). packages/web/src/store/auth.ts — dvFilterScopeDisabled. The WidgetFilterBadge spec for style.
  </read_first>
  <behavior>
    - dv-disable badge hidden: for a dv-bound source (dynamicViewId defined) with dvFilterScopeDisabled=true and a saved excluding cfg, useFilterScopeSummary returns appliedCount === totalCount (no ignored) — so WidgetFilterBadge renders null.
    - flag-off unchanged: with dvFilterScopeDisabled=false, the dv-bound summary still reflects the saved cfg (existing behavior).
    - table-bound unaffected: the flag never alters table-bound summaries.
  </behavior>
  <action>
    In useFilterScopeSummary (the HOOK, not the pure fn), subscribe to the flag: `const dvFilterScopeDisabled = useAuthStore((s) => s.dvFilterScopeDisabled);` (add useAuthStore import). Inside the useMemo, when isDv (dynamicViewId defined) AND dvFilterScopeDisabled, pass `cfg: undefined` to computeFilterScopeSummary (accept-all => no ignored => appliedCount===totalCount => badge hidden). Add dvFilterScopeDisabled to the useMemo deps. Do NOT change the pure computeFilterScopeSummary fn (keep it dumb/testable). Add a brief comment citing GAP 3 / Test 7. Add a hook-level scenario (or extend WidgetFilterBadge.spec) asserting the dv-bound + flag-on case yields no badge.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/WidgetFilterBadge.spec.tsx && npx tsc --noEmit && grep -q "dvFilterScopeDisabled" src/lib/useFilterScopeSummary.ts</automated>
  </verify>
  <done>dv-bound badge is hidden when the flag is on; table-bound + flag-off unchanged; tsc clean; spec green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-layer 'X of Y filters' indicator in LayersLegendPanel (COMM-V118-02, GAP 6) + dv-disable suppression</name>
  <files>packages/web/src/lib/resolveLegendLayers.ts, packages/web/src/components/LayersLegendPanel.tsx, packages/web/src/components/LayersLegendPanel.spec.tsx, packages/web/src/components/charts/MapChartRenderer.tsx</files>
  <read_first>
    packages/web/src/lib/resolveLegendLayers.ts (ResolvedLegendLayer type ~32-37). packages/web/src/components/LayersLegendPanel.tsx — the per-layer header block (~244-303, layer name span ~293, dv-status badge ~294-302). packages/web/src/components/charts/MapChartRenderer.tsx — resolvedLegendLayers useMemo (~640-664) + its deps; the existing primitive selectors for filters/shapes (filterVersion, shapesKey ~607, dynamicViewsKey). packages/web/src/lib/useFilterScopeSummary.ts (computeFilterScopeSummary signature). packages/web/src/components/WidgetFilterBadge.tsx (the class + visibility rule). packages/web/src/lib/spatialTargets.ts (getSpatialTargets + isSpatialTargetEligible — already imported by MapChartRenderer and used at ~lines 686-694 for eligibleTargetTableNames; reuse this for spatialCapable, NOT aggregateSpatialTargetsByTable). packages/web/src/components/LayersLegendPanel.spec.tsx for style.
  </read_first>
  <behavior>
    - legend indicator shown: a layer whose filter_scope excludes a source while an active filter exists shows "{appliedCount} of {totalCount} filters" in its legend row.
    - accept-all hidden: a layer with no filter_scope (or applying all) shows NO indicator (appliedCount === totalCount -> not rendered).
    - dv layer uses dvFilters, no spatial: a dv-bound layer's summary uses dvFilters[dvId] and spatialCapable=false.
    - dv-disable suppression: with dvFilterScopeDisabled=true, a dv-bound layer shows NO indicator (accept-all).
    - panel stays presentational: LayersLegendPanel renders from props only (no store import added).
  </behavior>
  <action>
    Step A — resolveLegendLayers.ts: add `filterSummary?: { appliedCount: number; totalCount: number }` to ResolvedLegendLayer. Do NOT compute it here (resolveLegendLayers stays a pure projection); the caller fills it.
    Step B — MapChartRenderer.tsx resolvedLegendLayers useMemo (~640-664): after the dvStatus enrichment, compute filterSummary per layer using computeFilterScopeSummary (imperative getState reads — same state-time pattern as the dvViews read at ~653):
      - read const dvFilterScopeDisabled = useAuthStore.getState().dvFilterScopeDisabled; (add useAuthStore import + import computeFilterScopeSummary from "../../lib/useFilterScopeSummary").
      - for each entry: const dvId = entry.layer.dynamic_view_id; const isDv = dvId != null;
      - cfg = (isDv && dvFilterScopeDisabled) ? undefined : (entry.layer.filter_scope ?? undefined);
      - activeFilters = isDv ? (useFilterStore.getState().dvFilters[dvId] ?? []) : (useFilterStore.getState().filters[entry.layer.table_id] ?? []);
      - spatialCapable = !isDv && getSpatialTargets({ config: widget.config as Pick<MapWidgetConfig, "spatialTargets"> }).filter(isSpatialTargetEligible).some((t) => t.tableId === entry.layer.table_id); activeShapes = spatialCapable ? useSpatialFilterStore.getState().shapes : [];
        (use the renderer's EXISTING spatial-target pattern — the same getSpatialTargets({ config }).filter(isSpatialTargetEligible) call already used at ~lines 686-694 for eligibleTargetTableNames. MapChartRenderer receives a single widget: WidgetDto (NOT a widgets array) and does NOT import aggregateSpatialTargetsByTable — do NOT reference either. getSpatialTargets + isSpatialTargetEligible are already imported.)
      - const summary = computeFilterScopeSummary({ cfg, activeFilters, activeShapes, spatialCapable });
      - attach filterSummary: { appliedCount: summary.appliedCount, totalCount: summary.totalCount } onto the entry.
      Add filterVersion + shapesKey (the existing primitive selectors — MapChartRenderer already subscribes shapesKey at ~line 607; there is NO spatialFilterVersion selector, so use shapesKey and do NOT add one) to the useMemo deps so the summary recomputes on filter/shape changes (filterVersion likely already subscribed; if not, add the primitive selector at component top — DO NOT subscribe to arrays).
    Step C — LayersLegendPanel.tsx: in the per-layer header (after the layer-name span ~293, alongside the dv-status badge ~294-302), render the indicator ONLY when filterSummary is present AND filterSummary.appliedCount < filterSummary.totalCount:
      `{filterSummary && filterSummary.appliedCount < filterSummary.totalCount && (<span className="widget-filter-badge" role="status" aria-label={`${filterSummary.appliedCount} of ${filterSummary.totalCount} filters applied`}>{filterSummary.appliedCount} of {filterSummary.totalCount} filters</span>)}`.
      Destructure filterSummary from the layer entry in the map callback (~212). REUSE the existing widget-filter-badge class — NO new class. NO raw hex. Keep the panel store-free.
    Add LayersLegendPanel.spec scenarios: indicator shown when appliedCount < totalCount; hidden when equal; hidden when filterSummary undefined.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/LayersLegendPanel.spec.tsx && npx tsc --noEmit && grep -q "widget-filter-badge" src/components/LayersLegendPanel.tsx && grep -q "filterSummary" src/lib/resolveLegendLayers.ts</automated>
  </verify>
  <done>Legend rows show "X of Y filters" only when a layer ignores at least one filter (reusing widget-filter-badge); dv layers use dvFilters/no spatial; dv-disable flag suppresses the dv-layer indicator; LayersLegendPanel stays store-free; specs green; tsc clean.</done>
</task>

</tasks>

<verification>
Web gates (run from packages/web):
- npx tsc --noEmit — clean.
- npx vitest run — 100% (esp. LayersLegendPanel.spec.tsx + WidgetFilterBadge.spec.tsx).
- npx vitest run src/styles/theme-guard.spec.ts — green. CRITICAL per CLAUDE.md: the legend indicator REUSES the existing widget-filter-badge class — confirm grep shows no invented class name and no raw hex was added.
No server change (pure client read path). Sole-materialize-trigger invariant untouched.
</verification>

<success_criteria>
- COMM-V118-02 / Test 4 addition: a scoped layer shows "X of Y filters" in the legend; an accept-all layer shows none.
- Test 7 (badge/legend): dv-bound widget badge + dv-bound layer indicator hidden when DISABLE_DV_FILTER_SCOPE=true.
- LayersLegendPanel remains pure-presentational (no store import).
- tsc clean, vitest 100%, theme-guard green (reused class, no hex).
</success_criteria>

<output>
After completion, create `.planning/phases/96-verification-live-uat/96-03-SUMMARY.md`.
</output>
