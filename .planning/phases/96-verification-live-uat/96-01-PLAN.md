---
phase: 96-verification-live-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/hooks/useCombinationOrchestrator.ts
  - packages/web/src/hooks/useCombinationOrchestrator.spec.ts
  - packages/web/src/components/charts/WidgetRenderer.tsx
autonomous: true
gap_closure: true
requirements: [COMBO-V118-03, COMBO-V118-01, COMBO-V118-04, FSCOPE-V118-03]
must_haves:
  truths:
    - "Test 10: with ceiling=2 and 3+ distinct scopes on a table, over-ceiling widgets render data on the fallback view, show NO stuck Filtering spinner, and the warning toast fires once per over-ceiling table."
    - "Test 1: a Records Table + a chart sharing one table under accept-all materialize exactly ONE combination view (no second legacy per-table view with identical WHERE)."
    - "Test 7: with DISABLE_DV_FILTER_SCOPE=true, a dv-bound widget that previously had a saved filter scope now applies ALL dv filters (saved scope neutralized) and shares the all-dv-filters combination view."
    - "The dv base view does NOT re-materialize on unrelated base-table column-filter changes (confirmed; fixed only if filter-triggered)."
  artifacts:
    - path: "packages/web/src/hooks/useCombinationOrchestrator.ts"
      provides: "Ceiling remap that never orphans a materializing flag; records enumerated as a trigger type; dv-disable accept-all resolution"
      contains: "dvFilterScopeDisabled"
    - path: "packages/web/src/components/charts/WidgetRenderer.tsx"
      provides: "RecordsTableRenderer reads filterCombinationStore (vizToHash to registry), no own materialize trigger"
      contains: "useFilterCombinationStore"
    - path: "packages/web/src/hooks/useCombinationOrchestrator.spec.ts"
      provides: "Ceiling-no-stuck scenario + records-shares-combo scenario + dv-disable accept-all scenario + updated sole-trigger gate"
      contains: "materializing"
  key_links:
    - from: "useCombinationOrchestrator STEP B ceiling remap"
      to: "filterCombinationStore registry entry materializing flag"
      via: "clear orphaned markMaterializing entries + ensure fallback hash resolves materializing:false"
      pattern: "clearEntry|materializing"
    - from: "RecordsTableRenderer effectiveViewName"
      to: "filterCombinationStore vizToHash to registry viewName"
      via: "comboKey selector mirroring AggregatedWidgetRenderer"
      pattern: "vizToHash"
    - from: "useCombinationOrchestrator dv-bound resolution"
      to: "all-dv-filters accept-all hash"
      via: "dvFilterScopeDisabled then ignore saved filterSelection"
      pattern: "dvFilterScopeDisabled"
---

<objective>
Close the three orchestrator-side gaps (the heaviest plan — all touch useCombinationOrchestrator.ts, plus the records migration in WidgetRenderer.tsx). Coordinated in ONE plan because gaps 1/2/3 all mutate the same orchestrator file and the records change spans the orchestrator enumeration AND its renderer.

1. GAP 1 [BLOCKER, Test 10] — Ceiling fallback leaves vizs stuck Filtering. STEP B remap can leave a viz bound to a hash whose materializing:true flag never resolves (orphaned markMaterializing with no in-flight POST, or a fallback entry STEP D skips re-dispatching).
2. GAP 2 [MAJOR, Test 1] — RecordsTableRenderer is a legacy island (own filterViewStore read + own materialize trigger, excluded via NON_TRIGGER_TYPES) so records+chart on one table materialize two views with identical WHERE. Migrate records into the combination model.
3. GAP 3 [MAJOR, Test 7] — DISABLE_DV_FILTER_SCOPE only hides the editor; saved dv scopes still apply. Make the orchestrator treat dv-bound vizs as ACCEPT-ALL when the flag is set.

Plus a CONFIRM-ONLY diagnosis: does the dv BASE view re-materialize on unrelated base-table filters? (Likely benign — useDynamicViewMaterializeChain deps are [dashboardId, dynamicViewVersion], not filterVersion.)

Purpose: Unblock the live UAT re-walk for Tests 1, 7, and 10 (Test 10 is a BLOCKER).
Output: A correct orchestrator, a records renderer reading the combo store, updated specs, and a strengthened sole-materialize-trigger gate.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-verification-live-uat/96-UAT.md

<interfaces>
filterCombinationStore actions (packages/web/src/store/filterCombinationStore.ts):
  setEntry(hash, entry) — bumps combinationVersion
  markMaterializing(hash, dashboardId, sourceType "table"|"dv", sourceId) — placeholder {materializing:true, viewName:"", refCount:0,...} if missing; else sets materializing:true
  clearEntry(hash) — delete-key; no-op when absent; bumps combinationVersion
  setVizHash(vizKey, hash|undefined) — does NOT bump combinationVersion
  acquire(hash) / release(hash) — refCount +/-; release DROP-at-0 deletes entry; both bump combinationVersion
  registry[hash] = { viewName, expiresAt, materializing, materializeVersion, refCount, dashboardId, sourceType, sourceId }
  vizToHash keys: "w:<widgetId>" | "l:<layerId>"  (dv-bound widgets/layers ALSO use "w:"/"l:"; hash prefix "dv" vs "table" distinguishes)

AggregatedWidgetRenderer combo-read pattern to MIRROR (WidgetRenderer.tsx ~411-423, ~519-524, ~566-587):
  const vizKey = `w:${widget.id}`;
  const comboKey = useFilterCombinationStore((s) => { const h = s.vizToHash[vizKey]; const e = h && !h.endsWith(`:${NOFILTER_SENTINEL}`) ? s.registry[h] : undefined; return `${e?.viewName ?? ""}:${e?.expiresAt ?? 0}:${e?.materializing ? "1" : "0"}`; });
  const combinationVersion = useFilterCombinationStore((s) => s.combinationVersion);
  // inside fetch effect (imperative read, no stale closure):
  const comboHash = useFilterCombinationStore.getState().vizToHash[vizKey];
  const comboEntry = comboHash && !comboHash.endsWith(`:${NOFILTER_SENTINEL}`) ? useFilterCombinationStore.getState().registry[comboHash] : undefined;
  if (comboEntry?.materializing) return;            // suspend gate
  const effectiveViewName = comboEntry?.viewName ?? "";   // "" -> base table (use || table)
  // dv path: dvComboEntry?.viewName || recordsDvViewName

auth store flag (packages/web/src/store/auth.ts): useAuthStore((s) => s.dvFilterScopeDisabled) boolean; orchestrator reads useAuthStore.getState().dvFilterScopeDisabled.

NON_TRIGGER_TYPES currently contains "records" (useCombinationOrchestrator.ts ~70-80). isTriggerType(t) = !NON_TRIGGER_TYPES.has(t).
Orchestrator dv-widget loop reads cfg = w.config.filterSelection (~288); dv-layer loop reads cfg = layer.filter_scope (~317). resolveFilterSet(undefined, dvFilters) = accept-all (all dvFilters).
</interfaces>

@packages/web/src/store/filterCombinationStore.ts
@packages/web/src/hooks/useCombinationOrchestrator.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix the ceiling remap so over-ceiling vizs never get stuck materializing (GAP 1, Test 10)</name>
  <files>packages/web/src/hooks/useCombinationOrchestrator.ts, packages/web/src/hooks/useCombinationOrchestrator.spec.ts</files>
  <read_first>
    packages/web/src/hooks/useCombinationOrchestrator.ts — STEP B ceiling remap (~341-418), STEP D dispatch (~459-556, esp. `if (liveEntry) continue;` ~468-472 and `liveCheck?.materializing` skip ~476-479), STEP E (~564-648), controllersRef (~166-175). packages/web/src/store/filterCombinationStore.ts — markMaterializing/clearEntry. packages/web/src/hooks/useCombinationOrchestrator.spec.ts — ceiling scenario 8 (~410-470) + helpers (bumpFilterVersion/advanceDebounce/factories).
  </read_first>
  <behavior>
    - ceiling-no-stuck: ceiling=2; TABLE_A has 3 distinct scopes across w1/w2/w3. After the tick, EVERY viz on TABLE_A has a vizToHash entry whose registry[hash].materializing === false (kept hashes resolve via POST; over-ceiling widgets remap to a fallback hash dispatched-or-already-materialized). No bound entry left materializing:true without a resolving POST.
    - orphaned-cleared: a hash markMaterializing'd on a prior tick, dropped from byTable/desired by the remap (no viz bound, not the fallback) has its registry entry cleared.
    - toast once: the ceiling warning toast fires exactly once for the table.
    - data correct: the fallback hash IS in desired and receives a materializeFilter POST (or reuses an entry with a non-empty viewName).
  </behavior>
  <action>
    Root cause (verified): STEP B deletes over-ceiling hashes from hashMap and remaps widgets' vizKeyToHash to fallbackHash. Two failure modes: (a) the fallback hash already exists as materializing:true from a prior tick so STEP D `if (liveEntry) continue;` fires no POST while a remapped viz reads a flag that may never resolve; (b) a dropped hash keeps an orphaned materializing:true entry no STEP D pass resolves.
    Fix:
    1. Inside STEP B, after the per-table remap loop (after ~411), collect removedHashes (hashes deleted from hashMap). For each: if useFilterCombinationStore.getState().registry[removedHash]?.materializing === true AND it is NOT fallbackHash AND no vizKeyToHash value equals it AND hashMap no longer contains it, abort its controller (controllersRef.current.get(removedHash)?.abort(); controllersRef.current.delete(removedHash);) and clearEntry(removedHash).
    2. STEP D: replace `if (liveEntry) { continue; }` with `if (liveEntry && (liveEntry.viewName !== "" || controllersRef.current.has(hash))) { continue; }` — reuse only when there is a real viewName OR a genuine in-flight controller; otherwise fall through to markMaterializing + materializeFilter so a bare placeholder fallback gets a real POST. Keep the subsequent materializing guard but skip only when controllersRef.current.has(hash).
    3. Leave single-toast-per-table unchanged.
    Do NOT change the Effect deps array. Imperative getState() only (S-02). Preserve AbortController-per-hash.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/hooks/useCombinationOrchestrator.spec.ts</automated>
  </verify>
  <done>New ceiling-no-stuck scenario passes (every post-remap viz resolves materializing:false; fallback dispatched; orphaned entries cleared; toast once). All pre-existing orchestrator scenarios green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate RecordsTableRenderer into the combination model + re-enumerate records (GAP 2, Test 1)</name>
  <files>packages/web/src/hooks/useCombinationOrchestrator.ts, packages/web/src/components/charts/WidgetRenderer.tsx</files>
  <read_first>
    packages/web/src/components/charts/WidgetRenderer.tsx — RecordsTableRenderer legacy reads (~1669-1702), OWN materialize-trigger effect (~1828-1908: dv branch ~1836-1860, table branch ~1862-1899), page-fetch (~1920-2009, effectiveViewName ~1970-1972), count-fetch (~2015-2065, effectiveViewName ~2054-2056), CSV (~1748-1750); AggregatedWidgetRenderer combo-read (~411-423, ~519-524, ~547-587). packages/web/src/hooks/useCombinationOrchestrator.ts — NON_TRIGGER_TYPES (~70-80), table loop (~204-237), dv-widget loop (~279-307).
  </read_first>
  <behavior>
    - orchestrator enumerates records: a "records" widget with a tableId + active column filter enters byTable; vizToHash["w:<id>"] equals the sibling "bar" chart hash on the same table under accept-all (records has no filterSelection). materializeFilter ONCE; refCount 2.
    - dv-bound records enumerated: a dv-bound "records" widget (config.dynamicViewId, dv materialized) with a dv filter shares the dv combo hash with a sibling dv chart — ONE POST.
    - records renderer reads combo store: effectiveViewName from filterCombinationStore.vizToHash["w:<id>"] to registry[hash].viewName (table; dv prefers dv-combo entry, falls back to raw dv view); suspend gate uses registry[hash].materializing.
  </behavior>
  <action>
    Part A — orchestrator: remove "records" from NON_TRIGGER_TYPES (~79); update the comment block (~57-69) to state records is now orchestrator-owned (shares the accept-all combo; resolveFilterSet(undefined,...) = accept-all). Keep "table" out of the set. No other orchestrator code change — the table loop reads w.config.filterSelection (undefined for records) and the dv loop reads w.config.dynamicViewId; both handle records once it is a trigger type.
    Part B — RecordsTableRenderer: DELETE the OWN materialize-trigger effect (~1828-1908, both branches) + remove recordsMaterializeAbortRef and its unmount cleanup if now unused. Add const vizKey = `w:${widget.id}`. Replace filterViewStore selectors (viewName ~1669, recordsTableExpiresAt ~1673, recordsTableMaterializing ~1683) with comboKey + combinationVersion selectors (copy <interfaces>). Replace dv-filter-view selectors (recordsDvFilterViewName/recordsDvFilterMaterializing ~1698-1702) with the dv-combo read (vizToHash[vizKey] to registry[hash]) mirroring AWR ~566-587; KEEP recordsDvViewName/recordsDvStatus (raw dv view) as fallback. In page-fetch, count-fetch, CSV: replace effectiveViewName with the imperative combo read (table: comboEntry?.viewName ?? ""; dv: dvComboEntry?.viewName || recordsDvViewName); replace the recordsTableMaterializing suspend gate with comboEntry?.materializing (imperative); replace proactive-expiry clearView(tableId) with clearEntry(comboHash) (mirror AWR ~551-560). Update dep arrays: drop viewName/recordsTableExpiresAt/recordsTableMaterializing/recordsDvFilterViewName/recordsDvFilterMaterializing; add comboKey/combinationVersion. Verify NO materialize/drop/mark/setView/clearView/markDv/setDv/clearDv calls remain in WidgetRenderer.tsx. Remove now-unused imports. Run tsc.
    NOTE — do NOT touch these non-records sites: the AWR drill-click synchronous markers at line 159 (useFilterViewStore.getState().markDvMaterializing(...)) and line 198 (useFilterViewStore.getState().markMaterializing(...)) in the AggregatedWidgetRenderer addFilter handler are AWR drill-click markers, NOT records — leave them intact; the line-31 import (import { ..., materializeFilter, dropFilterView ... }) and the comment strings at lines 364/483 are likewise non-records and MUST be left intact. After migration the records section makes NO materialize/drop CALLS — but the import + AWR markers + comments legitimately remain, which is why the verify gate below is scoped to the RecordsTableRenderer section and matches CALL-SITE syntax (open-paren) only.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/hooks/useCombinationOrchestrator.spec.ts src/components/charts/WidgetRenderer.spec.tsx && npx tsc --noEmit && [ "$(awk 'NR>=1604' src/components/charts/WidgetRenderer.tsx | grep -qE "materializeFilter\(|dropFilterView\(|markMaterializing\(|markDvMaterializing\(|setDvView\(|clearDvView\(|setView\(|clearView\(" && echo FAIL || echo OK)" = "OK" ]</automated>
  </verify>
  <done>Records enumerated as a trigger type sharing the accept-all combo (ONE POST for records+chart on one table); RecordsTableRenderer reads the combo store; the scoped call-site grep (RecordsTableRenderer section, from line 1604 onward — confirm/adjust this boundary line if the file shifts) finds ZERO materialize/drop/marker CALLS, while the line-31 import + AWR drill markers (lines 159/198) + comments (364/483) remain untouched; tsc clean; specs green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Honor dvFilterScopeDisabled as full-disable (accept-all) in the orchestrator + update the sole-trigger gate (GAP 3, Test 7)</name>
  <files>packages/web/src/hooks/useCombinationOrchestrator.ts, packages/web/src/hooks/useCombinationOrchestrator.spec.ts</files>
  <read_first>
    packages/web/src/hooks/useCombinationOrchestrator.ts — dv-bound widget loop (~279-307, cfg read ~288), dv-bound layer loop (~310-334, cfg read ~317), the auth/ceiling subscription block (~112-115). packages/web/src/store/auth.ts — dvFilterScopeDisabled (~20,~37,~52). packages/web/src/hooks/useCombinationOrchestrator.spec.ts — Phase 94 dv section (~1131+) for dv factories + the sole-trigger gate (~1098-1128) comment that still references the records legacy island in WidgetRenderer.tsx.
  </read_first>
  <behavior>
    - dv-disable accept-all: with dvFilterScopeDisabled=true, a dv-bound widget that has a saved filterSelection (allowlist excluding a source) resolves to ALL dvFilters (cfg ignored) and produces the SAME hash as a sibling dv widget with no scope — they share ONE dv combo view.
    - flag-off unchanged: with dvFilterScopeDisabled=false, the saved filterSelection still applies (existing behavior).
    - gate updated: the sole-trigger grep gate no longer treats WidgetRenderer.tsx as a legacy records exception — it asserts the orchestrator is the sole materializeFilter caller and that WidgetRenderer.tsx contains no materializeFilter/dropFilterView.
  </behavior>
  <action>
    In useCombinationOrchestrator.ts, read the flag imperatively at the top of the main effect body (STEP A): `const dvScopeDisabled = useAuthStore.getState().dvFilterScopeDisabled;` (do NOT add it to the deps array — it is read at fire-time; the existing deps already cover the relevant changes, and /api/me sets it once at login). In BOTH dv loops (widget ~288 and layer ~317), when dvScopeDisabled is true pass `undefined` as the cfg to resolveFilterSet: `const cfg = dvScopeDisabled ? undefined : (w.config.filterSelection as FilterSelectionConfig | undefined);` and the layer equivalent. Do NOT touch the table loops (table-bound scope is unaffected by the flag). Add a comment citing GAP 3 / Test 7 / full-disable decision.
    Update the sole-trigger gate (spec ~1098-1128): change the comment + assertions so it no longer says "records' legacy path is in WidgetRenderer.tsx". Add an assertion that, within the RecordsTableRenderer section of fs.readFileSync(WidgetRenderer.tsx), NO materialize/drop CALL remains — use a comment-excluding, call-site regex (NOT a plain-string .includes("materializeFilter"), which would false-fail on the line-31 import and the 364/483 comments). Assert that `/^(?!\s*\/\/).*materializeFilter\(/m` and `/^(?!\s*\/\/).*dropFilterView\(/m` do NOT match within the RecordsTableRenderer section (slice the file from the RecordsTableRenderer boundary, ~line 1604, onward — confirm/adjust the boundary if the file shifts). Intent unchanged: RecordsTableRenderer makes NO materialize/drop CALLS after migration; the line-31 import, the AWR drill-click markers at lines 159/198, and the comments at 364/483 are NOT records-related and MUST be left intact. Keep the orchestrator-contains-spatialFilters+combinationKey assertions and the no-other-hook assertion.
    Add a dv-disable spec scenario in the Phase 94 dv section: render the orchestrator with two dv widgets (one with an excluding allowlist, one with no scope) under useAuthStore.setState({ dvFilterScopeDisabled: true }); assert both vizToHash entries point to the same hash and materializeFilter is called once. Reset the flag in afterEach.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/hooks/useCombinationOrchestrator.spec.ts && grep -q "dvFilterScopeDisabled" src/hooks/useCombinationOrchestrator.ts</automated>
  </verify>
  <done>With the flag on, dv-bound vizs resolve to accept-all and share one dv combo view; flag-off behavior unchanged; the sole-trigger gate asserts WidgetRenderer.tsx has zero materialize/drop calls; new dv-disable scenario passes.</done>
</task>

<task type="auto">
  <name>Task 4: CONFIRM whether the dv base view re-materializes on base-table filters (diagnose-only; fix only if filter-triggered)</name>
  <files>packages/web/src/hooks/useDynamicViewMaterializeChain.ts</files>
  <read_first>
    packages/web/src/hooks/useDynamicViewMaterializeChain.ts — the cascade fire effect deps (~236-254, matVersionKey/dynamicViews/fireCascade) and the list-refresh effect deps (~60-74, [dashboardId, dynamicViewVersion]). Confirm filterVersion / spatialFilterVersion are NOT deps of any materialize-firing effect.
  </read_first>
  <action>
    Read useDynamicViewMaterializeChain.ts and verify the materialize-firing effects depend ONLY on dv-lifecycle signals (dashboardId, dynamicViewVersion, matVersionKey) and NOT on filterVersion/spatialFilterVersion. If confirmed benign (a page-load / dv-version artifact, not filter-triggered), make NO code change — record the finding in the SUMMARY ("dv base re-materialize is a dv-version/page-load artifact; deps are [dashboardId, dynamicViewVersion]; not filter-triggered; no fix needed"). ONLY if you find filterVersion/spatialFilterVersion actually wired into a materialize-firing effect (it should not be), remove that dep and add a one-line spec asserting a base-table filterVersion bump does not call materializeDynamicView; otherwise add no test.
  </action>
  <verify>
    <automated>cd packages/web && ! grep -nE "filterVersion|spatialFilterVersion" src/hooks/useDynamicViewMaterializeChain.ts</automated>
  </verify>
  <done>The diagnosis is recorded in the SUMMARY. The grep confirms no filterVersion/spatialFilterVersion dep wires the dv base materialize (benign), OR a fix + spec was added if one was actually found.</done>
</task>

</tasks>

<verification>
Web gates (run from packages/web):
- npx tsc --noEmit — clean.
- npx vitest run — 100% (esp. useCombinationOrchestrator.spec.ts + WidgetRenderer.spec.tsx).
- npx vitest run src/styles/theme-guard.spec.ts — green (no new component CSS/hex introduced here, but confirm).
Sole-materialize-trigger invariant (now records is orchestrator-owned, NOT a legacy exception):
- awk 'NR>=1604' packages/web/src/components/charts/WidgetRenderer.tsx | grep -qE "materializeFilter\(|dropFilterView\(|markMaterializing\(|markDvMaterializing\(|setDvView\(|clearDvView\(|setView\(|clearView\(" && echo FAIL || echo OK — expect OK (no records-section materialize/marker CALLS remain). Scoped to the RecordsTableRenderer section (line 1604 onward — confirm/adjust if the file shifts) and matches CALL-SITE syntax (open-paren) so it does NOT false-fail on the line-31 import, the AWR drill-click markers at lines 159/198, or the comments at 364/483 (all non-records, all intentionally retained).
- The orchestrator remains the only hooks/ file calling materializeFilter with combinationKey (asserted by the updated sole-trigger gate).
No server change expected (all client fixes) — no server gates required for this plan.
</verification>

<success_criteria>
- Test 10 (BLOCKER): over-ceiling widgets render data on the fallback view, NO stuck spinner, toast once.
- Test 1: records + chart on one table under accept-all = ONE combination view.
- Test 7: dv-disable flag fully neutralizes saved dv scopes (accept-all, shared view).
- dv-base re-materialize diagnosis recorded.
- tsc clean, vitest 100%, theme-guard green, sole-trigger grep finds zero call sites in WidgetRenderer.tsx.
</success_criteria>

<output>
After completion, create `.planning/phases/96-verification-live-uat/96-01-SUMMARY.md`.
</output>
