---
phase: 58-action-engine-contract-allow-list-canary
plan: 02
type: execute
wave: 2
depends_on: ["58-01"]
files_modified:
  - packages/web/src/store/widgetActionStore.ts
  - packages/web/src/store/widgetActionStore.spec.ts
  - packages/web/src/lib/applyWidgetAction.ts
  - packages/web/src/lib/applyWidgetAction.spec.ts
  - packages/web/src/components/DashboardContext.tsx
  - packages/web/src/components/charts/WidgetRenderer.tsx
  - packages/web/src/components/charts/MapChartRenderer.tsx
  - packages/web/src/components/DashboardsPage.tsx
  - packages/web/src/components/charts/actionEngine.canary.spec.tsx
  - packages/web/src/lib/actionEngineDecoupling.spec.ts
autonomous: true
requirements: [ENGINE-V111-02, ENGINE-V111-03, ENGINE-V111-04, SAFETY-V111-02]
must_haves:
  truths:
    - "A control interaction applies an action to a SESSION overlay LIVE — no runtime PATCH, no updateWidget/updateLayer call at runtime"
    - "The overlay store resets on logout / dashboard-switch (next store in the canonical lifecycle cleanup chain)"
    - "A MOUNTED widget.config target re-renders from the overlay-merged config with NO remount (canary)"
    - "A MOUNTED map-layer target re-renders from the overlay-merged layer config (incl. top-level track_config/cb_config) with NO remount (canary)"
    - "applyWidgetAction routes to all 3 target kinds (widget / layer / dynamicView), validates against the allow-list, returns a typed result"
    - "A dangling target (deleted widget/layer/dv) or rejected patch is a safe no-op + a non-blocking toast — no crash, no partial write"
    - "The engine never imports filter-store symbols and never references filterVersion (static grep)"
  artifacts:
    - path: "packages/web/src/store/widgetActionStore.ts"
      provides: "Session overlay store: widget overrides + layer overrides + dynamicView overrides, applyOverride/clearOverride/reset"
      contains: "reset"
    - path: "packages/web/src/lib/applyWidgetAction.ts"
      provides: "applyWidgetAction(action) entry — validates, idempotency-guards, writes overlay, returns WidgetActionResult"
      contains: "applyWidgetAction"
    - path: "packages/web/src/components/charts/actionEngine.canary.spec.tsx"
      provides: "Live-re-render canary covering a widget.config target AND a map-layer target"
    - path: "packages/web/src/lib/actionEngineDecoupling.spec.ts"
      provides: "Static source-grep: engine modules never import filter symbols / reference filterVersion"
  key_links:
    - from: "packages/web/src/lib/applyWidgetAction.ts"
      to: "packages/web/src/lib/actionAllowList.ts"
      via: "validateActionPatch before any overlay write"
      pattern: "validateActionPatch"
    - from: "packages/web/src/components/charts/WidgetRenderer.tsx"
      to: "packages/web/src/store/widgetActionStore.ts"
      via: "scoped overlay selector merged into effectiveWidget.config at render time"
      pattern: "useWidgetActionStore"
    - from: "packages/web/src/components/charts/MapChartRenderer.tsx"
      to: "packages/web/src/store/widgetActionStore.ts"
      via: "layer-overlay merged into the allLayers render path"
      pattern: "useWidgetActionStore"
    - from: "packages/web/src/components/DashboardContext.tsx"
      to: "packages/web/src/lib/applyWidgetAction.ts"
      via: "applyWidgetAction threaded as a DashboardContextValue field"
      pattern: "applyWidgetAction"
    - from: "packages/web/src/components/DashboardsPage.tsx"
      to: "packages/web/src/store/widgetActionStore.ts"
      via: "reset() added to the dashboard-switch / logout cleanup chain"
      pattern: "useWidgetActionStore.getState\\(\\).reset"
---

<objective>
Build the runtime action engine: a session-scoped overlay store, the single `applyWidgetAction` dispatch entry threaded via DashboardContext, render-time overlay merge for all three target kinds (the map-layer render-path being the hardest), dangling-target + rejection safety, an idempotency guard, and the day-0 live-re-render canary. Plus the static decoupling grep that locks the engine away from the filter/materialize system.

Purpose: ENGINE-V111-02 (single dispatch path → MOUNTED target re-renders LIVE, no remount — proven by canary), ENGINE-V111-03 (routes to widget.config / map-layer-config-incl-top-level-track_config/cb_config / dynamic-view), ENGINE-V111-04 (same-dashboard only; dangling target / absent field = safe no-op + typed signal, no partial write), SAFETY-V111-02 (engine never touches filter stores / filterVersion — static grep).

Apply model is TRANSIENT-FOR-EVERYONE (58-CONTEXT locked): runtime applies to a session overlay LIVE; it NEVER calls updateWidget/updateLayer at runtime and NEVER persists. The dashboard's saved config is always the baseline; the overlay layers on top at render time and clears on reload / dashboard-switch.

Output: `store/widgetActionStore.ts`, `lib/applyWidgetAction.ts`, DashboardContext + WidgetRenderer + MapChartRenderer + DashboardsPage wiring, the canary spec (widget + map), and the decoupling grep spec. NO server changes.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/58-action-engine-contract-allow-list-canary/58-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/58-action-engine-contract-allow-list-canary/58-01-SUMMARY.md

<interfaces>
<!-- Extracted from the live codebase + Plan 58-01. Use directly — no exploration needed. -->

From Plan 58-01 (packages/web/src/lib/widgetAction.ts):
```typescript
export const TARGET_KINDS = ["widget", "layer", "dynamicView"] as const;
export type WidgetAction = { target: { kind: "widget"|"layer"|"dynamicView"; id: number }; configPatch: Record<string, unknown> };
export type WidgetActionTarget = WidgetAction["target"];
export type WidgetActionResult =
  | { status: "applied"; target: WidgetActionTarget }
  | { status: "target_not_found"; target: WidgetActionTarget }
  | { status: "rejected"; target: WidgetActionTarget; reasons: string[] };
```
From Plan 58-01 (packages/web/src/lib/actionAllowList.ts):
```typescript
export function validateActionPatch(kind, widgetType: string | undefined, configPatch): { valid: true } | { valid: false; reasons: string[] };
```

DashboardContext (packages/web/src/components/DashboardContext.tsx) — CURRENT shape (extend it):
```typescript
export type DashboardContextValue = {
  dashboardId: number;
  widgets: WidgetDto[];
  dynamicViews: DynamicViewRow[];
  retryDynamicView: (dynamicViewId: number) => void;
};
// DashboardContextProvider takes the same 4 props + children; value is useMemo'd over [dashboardId, widgets, dynamicViews, retryDynamicView].
// useDashboardContext() THROWS on missing provider. useDashboardContextOptional() returns null.
// Fail-loud guard means EVERY existing spec that mounts a renderer in the provider passes 4 props — adding a 5th REQUIRED prop will break them. Make applyWidgetAction OPTIONAL in the provider props with a safe default, OR update all provider usages. Prefer optional-with-default to avoid touching many specs.
```

WidgetRenderer (packages/web/src/components/charts/WidgetRenderer.tsx):
- `import { useDashboardContext } from "../DashboardContext";` (line 43)
- AggregatedWidgetRenderer reads `const cfg = widget.config ?? {};` (line 286); RecordsTableRenderer reads `const cfg = widget.config ?? {};` (line 1411). These are render-time reads — the overlay must merge into `widget` BEFORE these branches.
- `Props` carries `widget: WidgetDto`. The top-level WidgetRenderer dispatches by `widget.type`.

MapChartRenderer (packages/web/src/components/charts/MapChartRenderer.tsx):
- `export default function MapChartRenderer({ widget, tables = [] }: Props)` (line 432)
- `import { useDashboardLayersStore } from "../../store/dashboardLayersStore";` (line 71)
- `const allLayers = useDashboardLayersStore((s) => s.layers);` (line 459) — THE map reads layers from the layers store, NOT widget.config. Downstream: `allLayers` is filtered by includedLayerIds (493-494) and fed to the WMS/legend pipeline (509). The layer-overlay must merge into `allLayers` here so the rendered layer reflects the overlay-merged config (incl. top-level track_config/cb_config).

dashboardLayersStore (packages/web/src/store/dashboardLayersStore.ts):
```typescript
updateLayer: (id: number, patch: Partial<DashboardLayerDto>) => void;  // reference-stable; CONFIG-TIME path — do NOT call at runtime
```

toast store (packages/web/src/store/toast.ts):
```typescript
export const useToastStore = create(...);  // showToast(message: string, kind?: "info"|"error"|...)
// Use useToastStore.getState().showToast(...) from non-React code (applyWidgetAction).
```

DashboardsPage cleanup chain (packages/web/src/components/DashboardsPage.tsx, ~lines 447-472) — 6 existing resets in order:
filterViewStore → filterStore → infoSelectionStore → lastInfoClickContextStore → spatialFilterStore → dynamicViewStore. ADD widgetActionStore.reset() as the 7th in this same effect's cleanup. DashboardContextProvider is mounted ~line 950 wrapping `widgets.map(w => <WidgetRenderer widget={w} .../>)`.

dynamicViewStore (packages/web/src/store/dynamicViewStore.ts): `views: Record<number, DynamicViewEntry>` — there is NO existing client-side per-view config-update fn; kind (c) routing is light (overlay store records it + a unit test exercises validateActionPatch + the result; no render-path wiring required this phase).
</interfaces>

CRITICAL FLAGS:
- TRANSIENT ONLY: applyWidgetAction NEVER calls updateWidget/updateLayer/PATCH. It only writes the overlay store + returns a typed result. (Grep acceptance below enforces this.)
- MAP-LAYER RENDER PATH (PITFALL S-01, MapChartRenderer:458-459): the map reads `allLayers` from useDashboardLayersStore, so the layer overlay MUST reach `allLayers` via a render-time merge inside MapChartRenderer — a widget.config merge does NOT reach map layers. track_config/cb_config are TOP-LEVEL DashboardLayerDto fields ([[track-config-toplevel-field]] memory) — merge them at the top level of the layer object, not under config.
- IDEMPOTENCY (PITFALLS P1): before writing the overlay, compare the proposed merged value to current; skip the write if unchanged (prevents render loops). Use a JSON.stringify fingerprint for complex fields (mirrors lastEmittedParamsRef).
- DECOUPLING (SAFETY-V111-02): the engine modules (widgetAction.ts, actionAllowList.ts, applyWidgetAction.ts, widgetActionStore.ts) must contain ZERO imports of materializeFilter/dropFilterView/addFilter/setBulkFilters and ZERO references to filterVersion.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Session overlay store + applyWidgetAction dispatch (transient, validated, idempotent, dangling-safe)</name>
  <files>packages/web/src/store/widgetActionStore.ts, packages/web/src/store/widgetActionStore.spec.ts, packages/web/src/lib/applyWidgetAction.ts, packages/web/src/lib/applyWidgetAction.spec.ts</files>
  <read_first>
    - packages/web/src/lib/widgetAction.ts + packages/web/src/lib/actionAllowList.ts (Plan 58-01 — WidgetAction, WidgetActionResult, validateActionPatch)
    - packages/web/src/store/filterStore.ts (the reset()/session-store shape to mirror — and confirm the test reset shim covers src/store/*.ts)
    - packages/web/src/store/toast.ts (showToast signature for failure UX)
    - .planning/research/PITFALLS.md (P1 idempotency, P4 dangling no-op, P5 decoupling)
  </read_first>
  <behavior>
    - applyOverride/clearOverride store overrides keyed by kind+id; reset() empties all three maps
    - applyWidgetAction(action, lookups) with an allow-listed, type-valid patch on an EXISTING target → writes overlay, returns { status: "applied", target }
    - A patch failing validateActionPatch → NO overlay write, returns { status: "rejected", target, reasons }, fires a non-blocking toast
    - A target id that does not exist among the provided lookups (deleted widget/layer/dv) → NO overlay write, returns { status: "target_not_found", target }, fires a toast
    - Idempotency: dispatching the SAME action twice writes the overlay AT MOST once on the second call (no-op when the merged value is unchanged)
    - applyWidgetAction performs ZERO network / updateWidget / updateLayer calls (transient-only)
  </behavior>
  <action>
    1. Create `packages/web/src/store/widgetActionStore.ts` (Zustand; lives under src/store/* so the test reset shim auto-covers it):
       - State: `widgetOverrides: Record<number, Record<string, unknown>>`, `layerOverrides: Record<number, Partial<DashboardLayerDto>>`, `dynamicViewOverrides: Record<number, Record<string, unknown>>`.
       - Actions: `applyWidgetOverride(id, patch)`, `applyLayerOverride(id, patch)`, `applyDynamicViewOverride(id, patch)` (each reference-stable merge into the keyed entry, mirroring dashboardLayersStore.updateLayer's slice-and-replace), `clearOverride(kind, id)`, and `reset()` (sets all three maps to {} — canonical reset name matching the other stores).
       - Header comment: session-scoped, transient-for-everyone, 7th store in the lifecycle cleanup chain; `// INVARIANT: ACTION-ENGINE-NO-FILTER` (never imports filter stores).
    2. Create `packages/web/src/lib/applyWidgetAction.ts`:
       - `export type ActionLookups = { widgets: WidgetDto[]; layers: DashboardLayerDto[]; dynamicViewIds: number[] };` (the caller supplies the current same-dashboard entities so the dispatch can detect dangling targets without importing React state).
       - `export function applyWidgetAction(action: WidgetAction, lookups: ActionLookups): WidgetActionResult`:
         a. Resolve the target by kind+id from lookups. For "widget": find widget; capture widgetType = widget.type. For "layer": find layer. For "dynamicView": check id ∈ dynamicViewIds. If not found → toast ("That control's target is no longer available") + return `{ status: "target_not_found", target: action.target }`. NO write.
         b. Call `validateActionPatch(action.target.kind, widgetType, action.configPatch)`. If invalid → toast (summarize reasons) + return `{ status: "rejected", target, reasons }`. NO write.
         c. Idempotency: compute the prospective merged value (widget: `{ ...widget.config, ...configPatch }`; layer: top-level field merge `{ ...layer, ...configPatch }` honoring top-level track_config/cb_config; dv: `{ ...override?, ...configPatch }`). Compare via JSON.stringify against current overlay-or-baseline; if unchanged → return `{ status: "applied", target }` WITHOUT a redundant store write.
         d. Otherwise write the appropriate overlay (applyWidgetOverride / applyLayerOverride / applyDynamicViewOverride) and return `{ status: "applied", target }`.
       - Header comment: TRANSIENT-ONLY (no PATCH/updateWidget/updateLayer); same-dashboard only (lookups ARE the same-dashboard set); `// INVARIANT: ACTION-ENGINE-NO-FILTER`; this is the MCP-future hook (same envelope an AI/MCP layer produces).
       - Use `useToastStore.getState().showToast(...)` for the failure UX; also `console.warn` the typed result for the dev signal.
    3. Create `packages/web/src/store/widgetActionStore.spec.ts` (store: apply/clear/reset behavior) and `packages/web/src/lib/applyWidgetAction.spec.ts` covering ALL behaviors above: applied (each of the 3 kinds), rejected (allow-list failure → no write + toast spied), target_not_found (deleted target → no write + toast), idempotency (double-dispatch → at most one effective store write), and an assertion that applyWidgetAction triggers NO updateWidget/updateLayer (spy/mock the client module and assert zero calls).
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/store/widgetActionStore.spec.ts src/lib/applyWidgetAction.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "reset" packages/web/src/store/widgetActionStore.ts` succeeds
    - `grep -q "validateActionPatch" packages/web/src/lib/applyWidgetAction.ts` succeeds
    - `grep -qE "updateWidget|updateLayer|api/widgets|api/layers|materializeFilter" packages/web/src/lib/applyWidgetAction.ts` returns NOTHING (transient-only, no PATCH, no filter)
    - `grep -qE "filterStore|filterViewStore|spatialFilterStore|filterVersion" packages/web/src/store/widgetActionStore.ts packages/web/src/lib/applyWidgetAction.ts` returns NOTHING
    - `cd packages/web && npx vitest run src/store/widgetActionStore.spec.ts src/lib/applyWidgetAction.spec.ts` is green (incl. rejected + target_not_found + idempotency + zero-PATCH tests)
  </acceptance_criteria>
  <done>Overlay store + transient validated idempotent dangling-safe applyWidgetAction exist; the dispatch never persists; tests cover applied/rejected/target_not_found/idempotency across the 3 kinds.</done>
</task>

<task type="auto">
  <name>Task 2: Thread applyWidgetAction via DashboardContext + render-time overlay merge (widget.config AND map-layer render path)</name>
  <files>packages/web/src/components/DashboardContext.tsx, packages/web/src/components/charts/WidgetRenderer.tsx, packages/web/src/components/charts/MapChartRenderer.tsx, packages/web/src/components/DashboardsPage.tsx</files>
  <read_first>
    - packages/web/src/components/DashboardContext.tsx (FULL — the 4-field value, the useMemo deps, the fail-loud guard; extend WITHOUT breaking existing specs)
    - packages/web/src/components/charts/WidgetRenderer.tsx (lines ~280-292 + ~1405-1415 cfg reads; the top-level WidgetRenderer body where the overlay merge goes; useDashboardContext import line 43)
    - packages/web/src/components/charts/MapChartRenderer.tsx (lines 432-433 signature/widgetConfig; 458-459 allLayers read; 491-509 includedLayerIds filter + downstream consumers) — THE map-layer render-path integration, highest risk
    - packages/web/src/store/widgetActionStore.ts (the selectors from Task 1)
    - packages/web/src/components/DashboardsPage.tsx (lines ~395 widgets state, ~412 hook mounts, ~447-472 cleanup chain, ~950 DashboardContextProvider, ~969-1024 widgets.map → WidgetRenderer)
  </read_first>
  <action>
    1. DashboardContext.tsx: add `applyWidgetAction: (action: WidgetAction) => WidgetActionResult;` to `DashboardContextValue`. Add the matching provider prop. IMPORTANT — to avoid breaking the many existing specs that mount renderers with the 4-prop provider under the fail-loud guard: make the new prop OPTIONAL on the provider with a safe default (a no-op that returns `{ status: "target_not_found", target: action.target }`), and include `applyWidgetAction` in the useMemo deps. Document why the default is safe (specs that never dispatch an action are unaffected).
    2. WidgetRenderer.tsx: at the TOP of the top-level WidgetRenderer component body (before dispatch by widget.type), add a scoped overlay selector and merge:
       `const overlay = useWidgetActionStore((s) => s.widgetOverrides[widget.id] ?? null);`
       `const effectiveWidget = overlay ? { ...widget, config: { ...widget.config, ...overlay } } : widget;`
       Pass `effectiveWidget` (not `widget`) to all renderer branches so AggregatedWidgetRenderer/RecordsTableRenderer read the overlay-merged config at render time. (Shallow merge; document the depth-1 constraint, consistent with the allow-list.)
    3. MapChartRenderer.tsx (THE hard one): after `const allLayers = useDashboardLayersStore((s) => s.layers);` (459), subscribe to the layer overlays and produce an overlay-merged layers array used everywhere `allLayers` currently feeds the render/WMS/legend pipeline:
       `const layerOverrides = useWidgetActionStore((s) => s.layerOverrides);`
       Build `const effectiveLayers = useMemo(() => allLayers.map((l) => layerOverrides[l.id] ? { ...l, ...layerOverrides[l.id] } : l), [allLayers, layerOverrides]);`
       Replace the DOWNSTREAM uses of `allLayers` (the includedLayerIds filter at 493-494 and the legend/WMS feeds at ~509+) with `effectiveLayers` so the rendered layer reflects the overlay — INCLUDING the top-level track_config/cb_config fields (the merge is top-level on the layer object, so these are covered). Do NOT change the legendKey/primitive-selector pattern; only swap the array the pipeline reads. Leave `allLayers` (raw) where the raw list is genuinely needed (e.g. count), but the render/WMS path must read effectiveLayers.
    4. DashboardsPage.tsx:
       - Construct the dispatch closure: `const applyAction = useCallback((action) => applyWidgetAction(action, { widgets, layers, dynamicViewIds }), [widgets, layers, dynamicViewIds])` where `layers` = useDashboardLayersStore layers and `dynamicViewIds` derives from `dynamicViews`. (Read the current layers source in this file; if layers aren't already in scope here, read them from useDashboardLayersStore.getState() inside the closure to keep it simple — Claude's discretion.)
       - Pass `applyWidgetAction={applyAction}` into `<DashboardContextProvider ...>`.
       - Add `useWidgetActionStore.getState().reset();` as the 7th reset in the existing cleanup effect (after `useDynamicViewStore.getState().reset();` ~line 472). Add a comment: "Phase 58 (ENGINE-V111): 7th store — session action overlay; transient-for-everyone, must not leak across dashboard switch."
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/components/DashboardContext.spec.tsx src/components/charts/WidgetRenderer.spec.tsx src/components/charts/MapChartRenderer.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "applyWidgetAction" packages/web/src/components/DashboardContext.tsx` succeeds
    - `grep -q "useWidgetActionStore" packages/web/src/components/charts/WidgetRenderer.tsx` succeeds
    - `grep -q "effectiveLayers" packages/web/src/components/charts/MapChartRenderer.tsx` succeeds (overlay reaches the map render path)
    - `grep -q "useWidgetActionStore.getState().reset" packages/web/src/components/DashboardsPage.tsx` succeeds (7th-store cleanup)
    - `npx tsc --noEmit -p packages/web` is clean
    - Existing DashboardContext / WidgetRenderer / MapChartRenderer specs stay green (no regression from the optional-prop default)
  </acceptance_criteria>
  <done>applyWidgetAction is threaded via DashboardContext (non-breaking); widget.config targets merge the overlay at render time; map-layer targets reach the map render path via effectiveLayers (incl. top-level track_config/cb_config); the overlay store resets as the 7th store on dashboard-switch/logout.</done>
</task>

<task type="auto">
  <name>Task 3: Live-re-render CANARY (widget + map) and the decoupling static-grep</name>
  <files>packages/web/src/components/charts/actionEngine.canary.spec.tsx, packages/web/src/lib/actionEngineDecoupling.spec.ts</files>
  <read_first>
    - packages/web/src/components/charts/DataFilterRenderer.spec.tsx (lines ~609-619 — the readFileSync + path.resolve + expect(source).not.toMatch static-assertion precedent to mirror)
    - packages/web/src/components/charts/DataFilterRenderer.tsx (lines ~15-18 — the SOLE MATERIALIZE TRIGGER INVARIANT comment to mirror)
    - packages/web/src/store/widgetActionStore.ts + packages/web/src/lib/applyWidgetAction.ts (Task 1 modules to grep)
    - packages/web/src/components/charts/WidgetRenderer.tsx + MapChartRenderer.tsx (Task 2 overlay merges the canary exercises)
    - .planning/research/PITFALLS.md (P2 read-once-at-mount — the canary is the gate)
  </read_first>
  <action>
    1. Create `packages/web/src/components/charts/actionEngine.canary.spec.tsx` — the day-0 live-re-render canary. Two cases, each: render a MOUNTED target inside DashboardContextProvider (4 existing props + applyWidgetAction wired to a real applyWidgetAction over the test lookups), tag the mounted DOM node (e.g. via a stable test id or by reading a config-derived rendered value), dispatch an action that patches an allow-listed field, then assert the rendered output reflects the new config WITHOUT a remount:
       - CASE A (widget.config target): mount WidgetRenderer for a simple widget whose visible output is driven by a config field on the allow-list; dispatch applyWidgetAction → store overlay write → assert the rendered value changed AND the original mounted node identity is preserved (no unmount/remount — e.g. a ref/attribute set on mount survives). Use the store directly to apply the overlay if wiring the full closure is heavy — but the merge path under test MUST be WidgetRenderer's render-time merge from Task 2.
       - CASE B (map-layer target): mount MapChartRenderer with a layer in useDashboardLayersStore, apply a layer overlay (e.g. render_mode or a top-level track_config/cb_config field) via the overlay store, assert the map's effectiveLayers-driven output reflects the change without remounting. This is where read-once/wrong-store bugs hide — assert the overlay actually reached the layer the map renders (e.g. the WMS param/legend derived from the layer reflects the new value). Reuse the existing MapChartRenderer.spec.tsx mounting/mocking setup as the template.
       - If a mounted-renderer DOM assertion for the map is impractical in jsdom, assert at the next-best live boundary: that the effectiveLayers memo (or the value it feeds, e.g. the emitted WMS param fingerprint) updates after the overlay write within the SAME mounted instance — document the chosen assertion point. The non-negotiable: no remount, and the map path reads the overlay.
    2. Create `packages/web/src/lib/actionEngineDecoupling.spec.ts` — static source-grep mirroring DataFilterRenderer.spec lines 609-619. For EACH engine module (widgetAction.ts, actionAllowList.ts, applyWidgetAction.ts, widgetActionStore.ts): `const source = fs.readFileSync(path.resolve(__dirname, "..."), "utf8");` then assert `expect(source).not.toMatch(/materializeFilter/)`, `/dropFilterView/`, `/addFilter/`, `/setBulkFilters/`, and `/filterVersion/`. (Read the real relative paths; widgetAction/actionAllowList/applyWidgetAction live in src/lib, widgetActionStore in src/store.)
    3. Add the SOLE MATERIALIZE TRIGGER INVARIANT comment block (mirroring DataFilterRenderer.tsx:15) to applyWidgetAction.ts if not already present from Task 1.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/actionEngine.canary.spec.tsx src/lib/actionEngineDecoupling.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The canary spec contains BOTH a widget.config-target case AND a map-layer-target case (grep for both `MapChartRenderer` and `WidgetRenderer` in the spec)
    - The decoupling spec asserts `not.toMatch(/materializeFilter/)` and `not.toMatch(/filterVersion/)` against all 4 engine modules
    - `cd packages/web && npx vitest run src/components/charts/actionEngine.canary.spec.tsx src/lib/actionEngineDecoupling.spec.ts` is green
  </acceptance_criteria>
  <done>The live-re-render canary proves a MOUNTED widget.config target AND a MOUNTED map-layer target re-render from the overlay-merged config with no remount; the static grep proves all engine modules are decoupled from the filter/materialize system.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx vitest run` — FULL suite 100% green (deterministic gate; baseline 1725 + new engine/canary/decoupling/store/dispatch tests).
- `npx tsc --noEmit -p packages/web` — clean (separate gate).
- ZERO server diff: `git diff --name-only -- packages/server` is empty.
- Manual confirm of the locked decisions: no updateWidget/updateLayer/PATCH in applyWidgetAction (transient); overlay store is the 7th reset in DashboardsPage cleanup; map-layer overlay reaches effectiveLayers.
</verification>

<success_criteria>
- ENGINE-V111-02: single dispatch path; MOUNTED target re-renders LIVE with no remount — proven by the canary (widget + map cases).
- ENGINE-V111-03: routes to widget.config, map-layer-config (incl. top-level track_config/cb_config via effectiveLayers), and dynamicView (overlay + validateActionPatch exercised); (a)+(b) verified, (c) exercised.
- ENGINE-V111-04: same-dashboard only (lookups are the same-dashboard set); dangling target / absent field → safe no-op + typed { status: "target_not_found" } + toast; no partial/corrupt write.
- SAFETY-V111-02: engine modules never import filter symbols / reference filterVersion — static grep green.
- Transient-for-everyone honored: zero runtime PATCH; overlay resets on dashboard-switch/logout.
</success_criteria>

<output>
After completion, create `.planning/phases/58-action-engine-contract-allow-list-canary/58-02-SUMMARY.md`. Record: the WidgetRenderer + MapChartRenderer overlay-merge points (line refs), the canary's chosen map-assertion boundary, the 7th-store cleanup location, and confirm zero server diff + full vitest green.
</output>
