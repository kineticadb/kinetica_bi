---
phase: 93-filter-scope-config-ui
plan: "02"
type: execute
wave: 2
depends_on: ["93-01"]
files_modified:
  - packages/server/src/types.ts
  - packages/server/src/db.ts
  - packages/server/src/index.ts
  - packages/server/tests/layers.spec.ts
  - packages/web/src/api/client.ts
  - packages/web/src/hooks/useCombinationOrchestrator.ts
  - packages/web/src/hooks/useCombinationOrchestrator.spec.ts
  - packages/web/src/components/charts/KineticaWmsLayerForm.tsx
  - packages/web/src/components/LayersModal.tsx
  - packages/web/src/components/LayersModal.spec.tsx
  - packages/web/src/components/DashboardsPage.tsx
autonomous: true
requirements: [FSCOPE-V118-02]
must_haves:
  truths:
    - "A map layer's form shows the same Filter Scope section at the LAYER level (not the map widget level)"
    - "Layer filter scope is stored on the top-level filter_scope column, never inside layer.config"
    - "Saving a layer filter scope PATCHes filter_scope and survives a page reload (re-opens with the allow-list intact)"
    - "The dashboard widget list reaches KineticaWmsLayerForm so its checklist lists real source widgets"
    - "filter_scope naming is consistent across DB column, server type, route body, updateDashboardLayer, DTO, and the orchestrator read site (no camelCase/snake_case mismatch)"
  artifacts:
    - path: "packages/server/src/db.ts"
      provides: "filter_scope TEXT column (fresh DDL + PRAGMA-guarded ALTER), mapDashboardLayer parse, updateDashboardLayer persistence"
      contains: "filter_scope"
    - path: "packages/web/src/components/charts/KineticaWmsLayerForm.tsx"
      provides: "Filter Scope section + widgets prop + filterScope/onChangeFilterScope callback"
      contains: "FilterSelectionPanel"
    - path: "packages/server/tests/layers.spec.ts"
      provides: "filter_scope round-trip / null-clear / preserve-on-omit in BOTH auth modes"
      contains: "filter_scope"
  key_links:
    - from: "packages/web/src/components/charts/KineticaWmsLayerForm.tsx"
      to: "LayersModal onPatch"
      via: "onChangeFilterScope → onPatch(layerId, { filter_scope })"
      pattern: "onChangeFilterScope"
    - from: "packages/web/src/api/client.ts updateLayer"
      to: "PATCH /api/dashboards/:id/layers/:layerId"
      via: "filter_scope in the Pick<DashboardLayerDto> body"
      pattern: "filter_scope"
    - from: "packages/server/src/db.ts updateDashboardLayer"
      to: "dashboard_layers.filter_scope column"
      via: "'filter_scope' in attrs discriminant + UPDATE SQL"
      pattern: "filter_scope"
---

<objective>
Persist per-layer filter scope as a TOP-LEVEL `filter_scope` field mirroring `track_config` end-to-end (SQLite column → server type → PATCH route → updateDashboardLayer → DTO → updateLayer Pick), reconcile the existing camelCase `filterScope` stub to one consistent snake_case name, integrate the shared `FilterSelectionPanel` (from Plan 01) into `KineticaWmsLayerForm`, thread the dashboard widget list down to the form, and prove round-trip persistence with both-auth-mode supertests.

Purpose: Map WMS layers get the same filter-scope config as chart widgets, stored top-level so the orchestrator's `layer.filter_scope` read works. Satisfies FSCOPE-V118-02.
Output: 6 persistence gaps closed + naming reconciliation; KineticaWmsLayerForm Filter Scope section; widgets threaded DashboardsPage → LayersModal → KineticaWmsLayerForm; both-auth-mode supertests.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/93-filter-scope-config-ui/93-RESEARCH.md
@CLAUDE.md

# Plan 01 produced FilterSelectionPanel — reuse it; do NOT recreate it.
@.planning/phases/93-filter-scope-config-ui/93-01-SUMMARY.md

<interfaces>
<!-- track_config is the canonical mirror. filter_scope follows EVERY step. -->

SERVER — packages/server/src/types.ts (DashboardLayer, lines 82–92):
```typescript
  dynamic_view_id: number | null;
  cb_config: string | null;
  track_config: string | null;   // ← add `filter_scope: string | null;` right after
```

SERVER — packages/server/src/db.ts:
- DDL (fresh installs), lines 107–108: `cb_config TEXT` / `track_config TEXT` → add `filter_scope TEXT,` after track_config (before created_at).
- PRAGMA-guarded ALTER (existing deployments), lines 339–344 — add a THIRD guarded ALTER:
  ```ts
  if (!layerColNames.has("filter_scope")) {
    instance.exec("ALTER TABLE dashboard_layers ADD COLUMN filter_scope TEXT");
  }
  ```
- mapDashboardLayer (lines 407–429): track_config surfaces as a RAW STRING (`row.track_config ?? null`).
  filter_scope must surface as a PARSED OBJECT on the DTO (the DTO/orchestrator type is FilterSelectionConfig,
  NOT a string). Add after the cb_config/track_config lines (line 426):
  ```ts
  filter_scope: row.filter_scope ? JSON.parse(row.filter_scope) : undefined,
  ```
  (NOTE: the SERVER DashboardLayer.filter_scope column type is `string | null`, but the wire/DTO response must
   carry the PARSED object — `FilterSelectionConfig | undefined` — so the client DTO and orchestrator read it as
   an object. Read side = JSON.parse; write side = JSON.stringify in the route (see PATCH route below). Keep BOTH
   sides explicit so server tsc stays clean and the object round-trips. If the column-shaped return type rejects the
   parsed object, type mapDashboardLayer's return as the DTO-facing shape rather than the raw column shape, or cast
   the single field: `filter_scope: (row.filter_scope ? JSON.parse(row.filter_scope) : undefined) as any`.)
- updateDashboardLayer (lines 692–733): add `filter_scope` to the Pick<> signature (after track_config, line 703),
  to the UPDATE SQL column list + `?` placeholder (line 709), and a `"filter_scope" in attrs ? attrs.filter_scope :
  existing.filter_scope` discriminant in the .run(...) args (after track_config, line 730). (At this layer the value
  is ALREADY a string — the route stringified it before calling — so updateDashboardLayer stays shape-ignorant,
  symmetric with cb_config/track_config.)

SERVER — packages/server/src/index.ts PATCH route (lines 979–994): add `"filter_scope"` to the body
`Partial<Pick<DashboardLayer, ...>>` list (after track_config, line 993). The route body Picks `filter_scope` as
`string | null` (the column shape), BUT the client sends a `FilterSelectionConfig` OBJECT. So the route MUST
JSON.stringify the object on write BEFORE calling updateDashboardLayer. Spell out the concrete pattern (do NOT
write a vague "cast as needed"):
```ts
const rawFilterScope = (body as any).filter_scope;
body.filter_scope = rawFilterScope == null ? null : JSON.stringify(rawFilterScope);
```
Place this right before the `updateDashboardLayer(layerId, body)` call, guarded by `"filter_scope" in body` so it
only runs when the field is present (omitted field stays absent → preserve-on-omit). This is option (a): route
stringifies on write; mapDashboardLayer parses on read. updateDashboardLayer remains ignorant of shape — symmetric
with how cb_config/track_config arrive already-stringified.

CLIENT — packages/web/src/api/client.ts:
- DTO field (line 645): the existing stub is `filterScope?: FilterSelectionConfig`. RENAME to snake_case to match
  the column + locked decision:
  ```typescript
  filter_scope?: import("../types/filterSelection").FilterSelectionConfig | null;
  ```
- updateLayer Pick<> (lines 681–691): add `| "filter_scope"` (after `"track_config"`).

CLIENT — packages/web/src/hooks/useCombinationOrchestrator.ts line 179: the ONLY live read of the old name:
  `const cfg = layer.filterScope as FilterSelectionConfig | undefined;` → change to
  `const cfg = layer.filter_scope ?? undefined;` (already-an-object; the `as any` cast can drop). Update the
  comment at line 177 from "filterScope" to "filter_scope" if it names the field.

CLIENT — packages/web/src/hooks/useCombinationOrchestrator.spec.ts: the test factory (lines 77, 92) declares/spreads
  `filterScope` — rename both to `filter_scope` so the fixture matches the DTO.

NOTE: MapChartRenderer.tsx line 554 mentions "filterScopes" only in a COMMENT — no code read; leave or reword, no behavior change.
</interfaces>

<layersmodal_split>
<!-- KineticaWmsLayerForm wiring mirrors the onChangeInfoConfig top-level pattern, NOT the config-blob merge. -->
LayersModal.tsx renders <KineticaWmsLayerForm> at lines 548–588. info_* fields ride a SEPARATE
`onChangeInfoConfig` callback (lines 575–581) — they are TOP-LEVEL columns, NOT merged into the config blob.
filter_scope follows THAT pattern, NOT the cb_config/track_config config-merge (lines 557–574). Add:
```tsx
  filterScope={selectedLayer.filter_scope ?? undefined}
  onChangeFilterScope={(next) => onPatch(selectedLayer.id, { filter_scope: next ?? null })}
```
Do NOT add filter_scope to the config={{...}} merge object, and do NOT destructure it out of nextConfig in
onChange. (Pitfall 1 in 93-RESEARCH: merging it into config silently breaks the top-level read.)

LayersModal also needs the `widgets` prop threaded (see thread_widgets) and forwarded to KineticaWmsLayerForm.
</layersmodal_split>

<thread_widgets>
<!-- DashboardsPage (has `widgets`) → LayersModal → KineticaWmsLayerForm. Each prop optional, [] default. -->
1. LayersModalProps (LayersModal.tsx line 45): add `widgets?: WidgetDto[];` (import WidgetDto from "../api/client").
2. LayersModal destructure + pass `widgets={widgets ?? []}` to <KineticaWmsLayerForm>.
3. KineticaWmsLayerFormProps (KineticaWmsLayerForm.tsx line 63): add `widgets?: WidgetDto[];`
   plus `filterScope?: import("../../api/client").DashboardLayerDto["filter_scope"];` and
   `onChangeFilterScope?: (next: FilterSelectionConfig | undefined) => void;`.
4. DashboardsPage.tsx <LayersModal> render (line 1193): add `widgets={widgets}`.
All optional with empty-array default so existing callers (MapConfigPanel embeds KineticaWmsLayerForm WITHOUT
widgets; LayersModal spec fixtures) compile unchanged.
</thread_widgets>

<wms_form_placement>
<!-- KineticaWmsLayerForm.tsx — add the Filter Scope section as the LAST config-group, after Info Popup. -->
The Info Popup config-group is at lines ~1554–1634; the form's outer container closes at lines 1634–1637.
Insert the FilterSelectionPanel as the LAST section before the outer closing `</div>` (after the Info Popup
block, around line 1634). Render unconditionally (layers always have a table source). selfWidgetId is NOT
passed (layers are not widgets). NO self-exclusion. The shared panel ALSO renders the spatial-draws sentinel
row here (delivered by Plan 01) — layers can opt to accept/ignore map spatial draws like any other source.
```tsx
<FilterSelectionPanel
  value={filterScope}
  onChange={(next) => onChangeFilterScope?.(next)}
  widgets={widgets ?? []}
/>
```
FilterSelectionPanel renders its own config-group wrapper — do not double-wrap.
</wms_form_placement>

<supertest_pattern>
<!-- Mirror the dynamic_view_id pattern in layers.spec.ts (lines 231–314 password block; 526+ oidc smoke block). -->
Password block (`describe("layers")`, after the dynamic_view_id tests ~line 314), add three its:
  - "PATCH accepts filter_scope and round-trips it (AUTH_MODE=password)": create layer (filter_scope null on
    fresh), PATCH `{ filter_scope: { sourceMode: "allowlist", allowedSourceWidgetIds: [5] } }`, expect 200 and
    body.filter_scope deep-equals that OBJECT (server parses on read).
  - "PATCH { filter_scope: null } clears (AUTH_MODE=password)": set then clear; expect body.filter_scope === null
    (or undefined — match mapDashboardLayer's null-vs-undefined return; assert it is falsy/cleared, not the object).
  - "PATCH that omits filter_scope preserves (AUTH_MODE=password)": set, then PATCH `{ position: 1 }`, expect
    filter_scope unchanged + position: 1.
OIDC smoke block (`describe("layers ... AUTH_MODE=oidc smoke")`, lines 526+): add at least the round-trip +
null-clear its mirroring the dynamic_view_id oidc variants (createDashboardOidc / createTableOidc helpers).
Server vitest is SET-BASED: failing files must be ⊆ TD-V16-TEST-ISOLATION; do NOT assert a fixed pass-count.
</supertest_pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server filter_scope persistence — DDL + ALTER + type + mapDashboardLayer + updateDashboardLayer + PATCH route + both-auth-mode supertests</name>
  <files>packages/server/src/types.ts, packages/server/src/db.ts, packages/server/src/index.ts, packages/server/tests/layers.spec.ts</files>
  <action>
    Mirror track_config at every site (see <interfaces>):
    1. types.ts: add `filter_scope: string | null;` to DashboardLayer (after track_config, line 90).
    2. db.ts DDL: add `filter_scope TEXT,` after `track_config TEXT,` (line 108).
    3. db.ts PRAGMA ALTER: add the third guarded `ALTER TABLE dashboard_layers ADD COLUMN filter_scope TEXT`
       after the track_config guard (line 343–344). Idempotent — second boot is a no-op.
    4. db.ts mapDashboardLayer: emit the PARSED object on read (read side — explicit JSON.parse):
       `filter_scope: row.filter_scope ? JSON.parse(row.filter_scope) : undefined,` after line 426
       (so the DTO carries FilterSelectionConfig | undefined, satisfying the orchestrator's object read).
       If the column-shaped return type rejects it, cast the single field `as any` or type the return as the
       DTO-facing shape (see <interfaces> note).
    5. db.ts updateDashboardLayer: add filter_scope to the Pick<> (line 703), the UPDATE SQL column + `?` (line 709),
       and `"filter_scope" in attrs ? attrs.filter_scope : existing.filter_scope` in the .run() args (after line 730).
       (Value is already a STRING at this layer — the route stringified it.)
    6. index.ts PATCH route: add `"filter_scope"` to the body Pick<> (line 993). BEFORE calling updateDashboardLayer,
       when `"filter_scope" in body`, stringify the OBJECT to a STRING explicitly (write side — option (a)):
       ```ts
       const rawFilterScope = (body as any).filter_scope;
       body.filter_scope = rawFilterScope == null ? null : JSON.stringify(rawFilterScope);
       ```
       The route body field is typed `string | null` but the client sends a `FilterSelectionConfig` object; the
       `(body as any)` read + reassignment keeps server tsc clean. mapDashboardLayer (step 4) parses on read, so
       the object round-trips. (Both write=stringify and read=parse sides are now explicit — no vague "cast as needed".)
    7. layers.spec.ts: add the password-mode trio + oidc-smoke variants per <supertest_pattern>.

    Run server tsc + the layers supertest in both modes. Server vitest is SET-BASED — failing files must be ⊆
    TD-V16-TEST-ISOLATION; never assert a fixed pass-count.
  </action>
  <verify>
    <automated>cd packages/server && npx tsc --noEmit && npx vitest run tests/layers.spec.ts</automated>
  </verify>
  <done>filter_scope column exists (fresh + migrated); PATCH round-trips the object (route stringifies on write, mapDashboardLayer parses on read), clears on null, preserves on omit, in BOTH auth modes; server tsc clean; layers.spec.ts green.</done>
</task>

<task type="auto">
  <name>Task 2: Client naming reconciliation — DTO filter_scope + updateLayer Pick + orchestrator read + spec factory</name>
  <files>packages/web/src/api/client.ts, packages/web/src/hooks/useCombinationOrchestrator.ts, packages/web/src/hooks/useCombinationOrchestrator.spec.ts</files>
  <action>
    1. client.ts DTO (line 645): rename the stub `filterScope?: FilterSelectionConfig` →
       `filter_scope?: import("../types/filterSelection").FilterSelectionConfig | null;` (snake_case; matches the
       column + locked decision). Update the surrounding comment to say filter_scope.
    2. client.ts updateLayer Pick<> (lines 681–691): add `| "filter_scope"` after `"track_config"`.
    3. useCombinationOrchestrator.ts line 179: `const cfg = layer.filterScope as FilterSelectionConfig | undefined;`
       → `const cfg = layer.filter_scope ?? undefined;` (drop the cast — already an object). Update the line-177 comment.
    4. useCombinationOrchestrator.spec.ts: rename `filterScope` → `filter_scope` in the makeLayer factory
       (lines 77, 92) so the fixture matches the renamed DTO field.
    5. grep to confirm ZERO remaining `filterScope` (camelCase) references in packages/web/src EXCEPT comments:
       `grep -rn "filterScope" packages/web/src` — any live (non-comment) hit is a bug. MapChartRenderer.tsx:554
       is a comment-only mention (acceptable; reword to filter_scope if desired).
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/hooks/useCombinationOrchestrator.spec.ts</automated>
  </verify>
  <done>One consistent snake_case `filter_scope` everywhere (DTO, Pick, orchestrator read, spec factory); web tsc clean; orchestrator spec green; no live camelCase filterScope references remain.</done>
</task>

<task type="auto">
  <name>Task 3: KineticaWmsLayerForm Filter Scope section + thread widgets DashboardsPage → LayersModal → form + LayersModal spec</name>
  <files>packages/web/src/components/charts/KineticaWmsLayerForm.tsx, packages/web/src/components/LayersModal.tsx, packages/web/src/components/DashboardsPage.tsx, packages/web/src/components/LayersModal.spec.tsx</files>
  <action>
    1. KineticaWmsLayerForm.tsx: add to KineticaWmsLayerFormProps (line 63): `widgets?: WidgetDto[];`,
       `filterScope?: DashboardLayerDto["filter_scope"];`, `onChangeFilterScope?: (next: FilterSelectionConfig | undefined) => void;`
       (import WidgetDto + FilterSelectionConfig). Destructure them. Render the shared FilterSelectionPanel
       (import from "./FilterSelectionPanel") as the LAST config-group after Info Popup (see <wms_form_placement>),
       passing value={filterScope}, onChange forwarding to onChangeFilterScope, widgets={widgets ?? []}, NO selfWidgetId.
    2. LayersModal.tsx: add `widgets?: WidgetDto[]` to LayersModalProps (line 45) + destructure; pass
       `widgets={widgets ?? []}`, `filterScope={selectedLayer.filter_scope ?? undefined}`, and
       `onChangeFilterScope={(next) => onPatch(selectedLayer.id, { filter_scope: next ?? null })}` to
       <KineticaWmsLayerForm> (lines 548–588). Do NOT touch the config-merge block (Pitfall 1).
    3. DashboardsPage.tsx <LayersModal> render (line 1193): add `widgets={widgets}`.
    4. LayersModal.spec.tsx — add TWO assertions:
       (a) WIDGET-THREADING: render LayersModal with a widgets fixture (incl. a filter-producing widget, e.g. a
           bar) + a selected layer; toggle "Customize" in the rendered Filter Scope section; assert a
           filter-producing source row appears in the checklist (proves widgets thread through to the form).
       (b) TOP-LEVEL vs CONFIG-MERGE SPLIT (guards Pitfall 1 — filter_scope must NEVER be merged into config):
           with the same render, trigger the source selection so `onChangeFilterScope` fires (e.g. check a source
           row or the spatial-draws row), then assert on the `onPatch` mock:
             - `onPatch` was called with `filter_scope` at the TOP LEVEL of the patch object
               (e.g. `expect(onPatch).toHaveBeenCalledWith(expect.any(Number), expect.objectContaining({ filter_scope: expect.anything() }))`).
             - the SAME patch's `config` does NOT contain `filter_scope`
               (e.g. assert the patch arg's `config` is undefined OR `expect(patchArg.config?.filter_scope).toBeUndefined()`).
           Keep existing assertions green.

    Reuse ONLY existing css classes (FilterSelectionPanel already does). No new class, no raw hex.
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/components/LayersModal.spec.tsx</automated>
  </verify>
  <done>KineticaWmsLayerForm shows the Filter Scope section at layer level (incl. spatial-draws row); widgets reach the form via LayersModal; onChangeFilterScope PATCHes filter_scope at the TOP LEVEL (spec asserts patch.config has NO filter_scope); LayersModal spec green; web tsc clean.</done>
</task>

</tasks>

<verification>
- `cd packages/server && npx tsc --noEmit` — clean.
- `cd packages/server && npx vitest run` — SET-BASED: failing files ⊆ TD-V16-TEST-ISOLATION; layers.spec.ts green; never assert a fixed pass-count.
- `cd packages/web && npx tsc --noEmit` — clean.
- `cd packages/web && npx vitest run` — 100% (no regressions).
- `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` — green.
- `grep -rn "filterScope" packages/web/src` — only comment mentions remain (no live camelCase reads).
- Reload survival: PATCH a layer filter_scope, GET /layers returns the parsed object on filter_scope (proven by Task 1 supertest round-trip).
</verification>

<success_criteria>
Maps to Phase 93 ROADMAP success criteria:
- SC2 (Filter Scope at the LAYER level; stored on layer.filter_scope top-level, NOT in layer.config): Task 3 (form section + onChangeFilterScope top-level patch, with a spec assertion that filter_scope is top-level and absent from config) + Task 1 (column) + Task 2 (DTO/read reconciliation).
- SC4 (saving persists via PATCH + survives reload — re-opens with the allow-list intact): Task 1 (round-trip supertest: route stringifies, mapDashboardLayer parses) + Task 2 (updateLayer Pick) + Task 3 (form reads selectedLayer.filter_scope).
- SC5 (server tsc clean; server vitest SET-BASED ⊆ TD-V16-TEST-ISOLATION; web vitest 100%; web tsc clean; theme-guard green): verification block.
(SC1 + SC3 are delivered by Plan 01; this plan reuses that panel verbatim, so the same checklist/orphan/spatial-draws UX applies to layers.)
</success_criteria>

<output>
After completion, create `.planning/phases/93-filter-scope-config-ui/93-02-SUMMARY.md` documenting: the 6 closed persistence gaps (with the wire-format decision — object on the wire, route stringifies via JSON.stringify, mapDashboardLayer parses via JSON.parse), the naming reconciliation (filterScope → filter_scope at every site), the widget-threading chain, the LayersModal top-level-vs-config split assertion, and the both-auth-mode supertest coverage.
</output>
</content>
</invoke>
