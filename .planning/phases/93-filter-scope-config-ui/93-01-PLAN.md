---
phase: 93-filter-scope-config-ui
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/charts/FilterSelectionPanel.tsx
  - packages/web/src/components/charts/FilterSelectionPanel.spec.tsx
  - packages/web/src/components/charts/filterSourceTypes.ts
  - packages/web/src/components/charts/ChartConfigPanel.tsx
  - packages/web/src/components/DashboardsPage.tsx
  - packages/web/src/types/filterSelection.ts
autonomous: true
requirements: [FSCOPE-V118-01]
must_haves:
  truths:
    - "Opening a chart widget's config panel shows a 'Filter Scope' section with a Customize opt-out toggle (default: accept all)"
    - "The source-widget checklist lists ONLY filter-producing widgets (bar/line/pie/scatter/table drill-downs, datafilter, calendar/timeline/numericline) — NOT records, info-card, legend, map, bignumber, heatmap, radio-group"
    - "In allowlist mode a 'Spatial draws (map)' sentinel row ALWAYS renders (dashboard-global, not derived from the widget list); checking it adds SPATIAL_DRAWS_SENTINEL to allowedSourceWidgetIds and it persists like any other entry"
    - "The widget being configured never appears in its own source list (the spatial-draws sentinel is exempt from self-exclusion — it always exists)"
    - "A referenced source id no longer present on the dashboard renders an orphan warning (the spatial-draws sentinel is exempt — it is never an orphan)"
    - "Selecting Customize then leaving every source unchecked surfaces an accept-none warning; unchecking Customize reverts to accept-all (config cleared to undefined)"
  artifacts:
    - path: "packages/web/src/components/charts/filterSourceTypes.ts"
      provides: "FILTER_PRODUCING_TYPES Set + isFilterProducingWidget(type) helper + SPATIAL_DRAWS_SENTINEL reserved id"
      contains: "SPATIAL_DRAWS_SENTINEL"
    - path: "packages/web/src/components/charts/FilterSelectionPanel.tsx"
      provides: "Shared Filter Scope config-group (chart + layer) including the always-present spatial-draws sentinel row"
      min_lines: 60
    - path: "packages/web/src/components/charts/FilterSelectionPanel.spec.tsx"
      provides: "Unit coverage for default/expanded/orphan/empty/self-exclusion/spatial-sentinel/onChange-payload"
  key_links:
    - from: "packages/web/src/components/charts/ChartConfigPanel.tsx"
      to: "FilterSelectionPanel"
      via: "render after Drill-Down config-group, value=draft.filterSelection, onChange=set('filterSelection', ...)"
      pattern: "FilterSelectionPanel"
    - from: "packages/web/src/components/DashboardsPage.tsx"
      to: "ChartConfigPanel"
      via: "WidgetConfigModal passes widgetId={widget.id} for self-exclusion"
      pattern: "widgetId"
---

<objective>
Build the shared `FilterSelectionPanel` component + the explicit filter-source enumeration constant (incl. the `SPATIAL_DRAWS_SENTINEL`), and integrate the panel into `ChartConfigPanel` so chart-widget filter scope persists via `widget.config.filterSelection`. This is the FRONTEND-ONLY half of Phase 93 — it creates the component that Plan 02 reuses for map layers.

Purpose: Designers can configure, per chart widget, which active filters it applies (source-widget allow-list incl. a persistent spatial-draws entry, opt-out/accept-all default). Satisfies FSCOPE-V118-01.
Output: `FilterSelectionPanel.tsx` (shared), `filterSourceTypes.ts` (source enumeration + spatial sentinel), `FilterSelectionPanel.spec.tsx`, ChartConfigPanel integration, `widgetId` threaded for self-exclusion. Zero server diff.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/93-filter-scope-config-ui/93-RESEARCH.md
@CLAUDE.md

# Phase 88 foundation (types already exist — DO NOT recreate)
@packages/web/src/types/filterSelection.ts

<interfaces>
<!-- These already exist (Phase 88). Import them — do NOT redefine. -->

From packages/web/src/types/filterSelection.ts:
```typescript
export type FilterSelectionConfig = {
  sourceMode: "all" | "allowlist";
  allowedSourceWidgetIds: number[];
};
export const DEFAULT_FILTER_SELECTION: FilterSelectionConfig = {
  sourceMode: "all",
  allowedSourceWidgetIds: [],
};
```

NOTE (TYPE WIDENING — required by the spatial sentinel): `allowedSourceWidgetIds` is currently
`number[]`, but `SPATIAL_DRAWS_SENTINEL` is a STRING reserved id ("__spatial_draws__"). For the
sentinel to be storable AND survive round-trip (it is just another entry in the array), widen the field
in `packages/web/src/types/filterSelection.ts` to `(number | string)[]`:
```typescript
  allowedSourceWidgetIds: (number | string)[];
```
This is a minimal, backward-compatible widening (numbers still valid). Do NOT change DEFAULT_FILTER_SELECTION
(still `[]`). Phase 93.5's resolver will narrow/branch on the sentinel string vs numeric ids when it reads
the array; Phase 93 only stores it.

From packages/web/src/api/client.ts (WidgetDto — line 462):
```typescript
export type WidgetDto = {
  id: number;
  dashboard_id: number;
  title: string;       // approximate — verify exact field for the display label
  type: string;        // line 466 — the registry type string ("bar","timeline","datafilter",...)
  config: Record<string, unknown>;
  // ...
};
```

ChartConfigPanel relevant state (ChartConfigPanel.tsx):
- `widgets?: WidgetDto[]` prop already exists (line 43) — dashboard widget list.
- `selectedSource` (line 179) — defined when the widget is bound to a data source. Gate the Filter Scope section on `selectedSource` (same as the Drill-Down section, line 685).
- `draft` (line 82) holds the working config; `set(key, value)` (line 461) writes into draft; the Apply handler spreads `...draft` into the saved config (line 763) — so `set("filterSelection", v)` persists automatically with NO save-path change.
- The Drill-Down `config-group` is at lines 685–718; insert the Filter Scope section immediately after it, before the chart-specific groups (line 720).

WidgetConfigModal (DashboardsPage.tsx line 1255) destructures `widget` (a WidgetDto) and renders `<ChartConfigPanel ... />` at line 1288. `widget.id` is in scope there.
</interfaces>

<spatial_draws_sentinel>
<!-- BLOCKER FIX: the spatial-draws sentinel is a REAL, SELECTABLE, PERSISTENT checkbox — config-only,
     forward-compatible with Phase 93.5. NOT a disabled "(future)" placeholder. Required by ROADMAP SC1,
     REQUIREMENTS FSCOPE-V118-01, and Phase 93.5's explicit dependency ("Phase 93 config UI writes the
     spatial-draws allow-list entry the resolver reads"). NOTE: 93-RESEARCH D2 recommended OMITTING it —
     that recommendation is OVERRIDDEN here by the roadmap/requirements/93.5-dependency. -->

In filterSourceTypes.ts:
```typescript
// Reserved, NON-widget id for "map spatial draws" in allowedSourceWidgetIds. A dashboard-global /
// per-table source that is NOT any real widget id. Phase 93 STORES it in config only; Phase 93.5's
// resolver branches on it (spatial shapes) vs numeric widget ids.
export const SPATIAL_DRAWS_SENTINEL = "__spatial_draws__";
```

In FilterSelectionPanel.tsx — when `sourceMode === "allowlist"`, ALWAYS render a checklist row labeled
"Spatial draws (map)", regardless of the widget list (it is dashboard-global / per-table, NOT derived
from `widgets`). Behavior:
- checked = `value.allowedSourceWidgetIds.includes(SPATIAL_DRAWS_SENTINEL)`.
- Checking ADDS `SPATIAL_DRAWS_SENTINEL` to allowedSourceWidgetIds; unchecking removes it. Persists exactly
  like any other entry (rides the same onChange / same sourceMode).
- It is NOT subject to self-exclusion (selfWidgetId never matches a string sentinel — it always renders).
- It is NOT subject to the orphan-source warning (it always "exists" — never flagged as a deleted widget).
- It renders in BOTH surfaces (chart-widget panel AND layer form) since FilterSelectionPanel is shared.
- The accept-none warning logic: the sentinel COUNTS as a live selected source. So if the only checked
  entry is the spatial sentinel, do NOT show the accept-none warning (a source IS selected).

BOUNDARY (keep crisp): Phase 93 ONLY STORES the sentinel in config. Making spatial actually scope data
(reading SPATIAL_DRAWS_SENTINEL from allowedSourceWidgetIds to gate spatial shapes) is Phase 93.5. Add NO
resolver / orchestrator / materialize behavior in this plan.
</spatial_draws_sentinel>

<orphan_pattern>
<!-- Mirror RadioGroupConfigPanel.tsx orphan warning EXACTLY (theme-guard compliant). -->
Orphan / danger text uses an EXISTING class + a token-var inline style:
```tsx
<span className="config-hint" style={{ color: "var(--danger)" }}>⚠ Deleted widget (id: {orphanId})</span>
```
See RadioGroupConfigPanel.tsx lines 460–461, 512–513, 689–690 for the precedent.
NOTE: orphan detection iterates NUMERIC widget ids only — SKIP the string SPATIAL_DRAWS_SENTINEL
(it is never an orphan). Guard with `typeof id === "number"` (or `id !== SPATIAL_DRAWS_SENTINEL`).
</orphan_pattern>

<css_classes>
<!-- REUSE these existing classes ONLY. global.css lines: 1222 .config-group, 1228 .config-group-label,
     1238 .config-hint, 1244 .config-toggle (+ 1253 its checkbox). NO new class names. -->
- `config-group`        — wrapping div (flex column)
- `config-group-label`  — "Filter Scope" header
- `config-toggle`       — each checkbox row: `<label className="config-toggle"><input type="checkbox" .../> label</label>`
- `config-hint`         — descriptive text / empty state / accept-none warning / orphan (with style={{color:"var(--danger)"}})
</css_classes>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Source-enumeration constant (+ spatial sentinel) + shared FilterSelectionPanel component (with spec)</name>
  <files>packages/web/src/components/charts/filterSourceTypes.ts, packages/web/src/components/charts/FilterSelectionPanel.tsx, packages/web/src/components/charts/FilterSelectionPanel.spec.tsx, packages/web/src/types/filterSelection.ts</files>
  <behavior>
    filterSelection.ts:
    - Widen `allowedSourceWidgetIds: number[]` → `(number | string)[]` (so the string sentinel is storable).
      Backward-compatible; DEFAULT_FILTER_SELECTION unchanged.

    filterSourceTypes.ts:
    - export const FILTER_PRODUCING_TYPES = new Set([
        "bar","line","pie","scatter","table","datafilter","calendar","timeline","numericline"
      ]) — EXPLICIT list. NOTE: "records" is EXCLUDED even though its registry supportsDrillDown:true
        (research: records is a filter TARGET, never a SOURCE). "legend","info-card","map","bignumber",
        "heatmap","radio-group" excluded.
    - export function isFilterProducingWidget(type: string): boolean { return FILTER_PRODUCING_TYPES.has(type); }
    - export const SPATIAL_DRAWS_SENTINEL = "__spatial_draws__" — reserved NON-widget id (see <spatial_draws_sentinel>).

    FilterSelectionPanel.spec.tsx (RED first — write these assertions before the component):
    - Renders accept-all default when value === undefined: shows "Filter Scope" label + a "Customize" config-toggle UNCHECKED + an accept-all config-hint; no checklist.
    - Checking "Customize" fires onChange with { sourceMode: "allowlist", allowedSourceWidgetIds: [] }.
    - In allowlist mode, the checklist lists ONLY filter-producing widgets: given widgets of types
      ["bar","records","legend","datafilter","map"], only the bar + datafilter rows render.
    - In allowlist mode, the "Spatial draws (map)" sentinel row ALWAYS renders — even when `widgets` is EMPTY
      (it is not derived from the widget list).
    - Checking the "Spatial draws (map)" row fires onChange whose allowedSourceWidgetIds CONTAINS
      SPATIAL_DRAWS_SENTINEL; unchecking it removes the sentinel from the array.
    - selfWidgetId excludes the matching widget from the checklist (a bar widget whose id === selfWidgetId is absent),
      BUT the spatial-draws sentinel row still renders (sentinel is exempt from self-exclusion).
    - Checking a source row fires onChange adding that id to allowedSourceWidgetIds; unchecking removes it.
    - allowlist with empty allowedSourceWidgetIds shows the accept-none warning config-hint.
    - allowlist with ONLY SPATIAL_DRAWS_SENTINEL selected does NOT show the accept-none warning (a source IS selected).
    - Unchecking "Customize" fires onChange(undefined) (revert to accept-all).
    - Orphan: value.allowedSourceWidgetIds contains a NUMERIC id NOT in widgets → renders a config-hint with
      style color var(--danger) and the orphan id; the id stays in the array (non-destructive). The string
      sentinel is NEVER treated as an orphan.
    - Empty source list (no filter-producing widgets after self-exclusion) → "No filter-producing widgets
      on this dashboard." config-hint for the WIDGET section; the spatial-draws sentinel row STILL renders.
  </behavior>
  <action>
    Create FilterSelectionPanel.tsx as a pure presentational component (no store reads — value/onChange/widgets/selfWidgetId props only):

    Props (exact):
    ```typescript
    type FilterSelectionPanelProps = {
      value: FilterSelectionConfig | undefined;
      onChange: (next: FilterSelectionConfig | undefined) => void;
      widgets: WidgetDto[];
      selfWidgetId?: number;   // chart widgets pass their own id; layers omit (Plan 02)
    };
    ```
    Import FilterSelectionConfig from "../../types/filterSelection" and WidgetDto from "../../api/client".
    Import { isFilterProducingWidget, SPATIAL_DRAWS_SENTINEL } from "./filterSourceTypes".

    Render:
    1. Always: `<div className="config-group"><div className="config-group-label">Filter Scope</div> ...`.
       (The component renders its OWN config-group wrapper so it drops into any parent.)
    2. A "Customize" row: `<label className="config-toggle"><input type="checkbox" checked={isAllowlist} onChange={...} /> Customize</label>`
       where isAllowlist = value !== undefined && value.sourceMode === "allowlist".
       - On check → onChange({ sourceMode: "allowlist", allowedSourceWidgetIds: [] }).
       - On uncheck → onChange(undefined).
    3. When NOT allowlist (accept-all): show `<span className="config-hint">Accept all filters</span>`. No checklist.
    4. When allowlist:
       - Compute sources = widgets.filter(w => isFilterProducingWidget(w.type) && w.id !== selfWidgetId).
       - If sources.length === 0: `<span className="config-hint">No filter-producing widgets on this dashboard.</span>`.
       - Else render a checklist: for each source, a `<label className="config-toggle"><input type="checkbox"
         checked={value.allowedSourceWidgetIds.includes(w.id)} onChange={toggle(w.id)} /> {w.title} ({w.type})</label>`.
         toggle adds/removes w.id from allowedSourceWidgetIds and calls onChange with the new array (same sourceMode).
       - SPATIAL DRAWS SENTINEL ROW (ALWAYS rendered in allowlist mode, independent of `sources`):
         `<label className="config-toggle"><input type="checkbox"
            checked={value.allowedSourceWidgetIds.includes(SPATIAL_DRAWS_SENTINEL)}
            onChange={toggle(SPATIAL_DRAWS_SENTINEL)} /> Spatial draws (map)</label>`.
         Use the SAME toggle helper (it adds/removes the value, numeric OR string). NOT self-excluded, NOT orphan-checked.
       - Accept-none warning when allowlist && there is NO live selected source — i.e. the allowedSourceWidgetIds
         has no LIVE numeric match (intersection with live source ids is empty) AND does NOT include
         SPATIAL_DRAWS_SENTINEL: `<span className="config-hint" style={{ color: "var(--danger)" }}>No sources
         selected — this visualization ignores all filters. Uncheck Customize to accept all.</span>`.
         (The spatial sentinel counts as a selected source → suppresses this warning.)
       - Orphan: for each NUMERIC id in allowedSourceWidgetIds NOT present in the full widgets list (deleted widget),
         render `<span className="config-hint" style={{ color: "var(--danger)" }}>⚠ Deleted widget (id: {id})</span>`.
         SKIP the string SPATIAL_DRAWS_SENTINEL (never an orphan). Keep ids in the array (non-destructive) — only an
         explicit uncheck removes them. (No live checkbox row exists for orphans since they are not in `sources`.)

    Use the widget display label field that WidgetDto actually exposes (verify the exact field name — likely
    `title`). Use ONLY the css_classes listed above; NO invented class names; NO raw hex (var(--danger) only).
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/FilterSelectionPanel.spec.tsx</automated>
  </verify>
  <done>FilterSelectionPanel.spec.tsx green; component + filterSourceTypes.ts (incl. SPATIAL_DRAWS_SENTINEL) exist; allowedSourceWidgetIds widened to (number|string)[]; spatial-draws row renders + persists in allowlist mode; theme-guard clean (no new class, no raw hex).</done>
</task>

<task type="auto">
  <name>Task 2: Integrate FilterSelectionPanel into ChartConfigPanel + thread widgetId for self-exclusion</name>
  <files>packages/web/src/components/charts/ChartConfigPanel.tsx, packages/web/src/components/DashboardsPage.tsx</files>
  <action>
    1. ChartConfigPanel.tsx:
       - Add `widgetId?: number;` to the `Props` type (near `widgets?` at line 43) and to the destructure (line 70–80).
       - Import FilterSelectionPanel from "./FilterSelectionPanel" and FilterSelectionConfig from "../../types/filterSelection".
       - Insert a Filter Scope section IMMEDIATELY AFTER the Drill-Down config-group (after line 718, before the
         chart-specific `{groups.map(...)}` at line 720). Gate it on `selectedSource` (same condition as Drill-Down):
         ```tsx
         {selectedSource && (
           <FilterSelectionPanel
             value={draft.filterSelection as FilterSelectionConfig | undefined}
             onChange={(next) => set("filterSelection", next)}
             widgets={widgets ?? []}
             selfWidgetId={widgetId}
           />
         )}
         ```
         (FilterSelectionPanel renders its own config-group wrapper — do NOT wrap it in another config-group.)
       - Confirm NO save-path change is needed: the Apply handler spreads `...draft` (line 763), so `set("filterSelection", ...)`
         persists into widget.config automatically. When reverting to accept-all, set writes `filterSelection: undefined`
         — confirm `undefined` round-trips harmlessly (orchestrator treats absent/undefined as accept-all).

    2. DashboardsPage.tsx — WidgetConfigModal (line 1288 render): add `widgetId={widget.id}` to the <ChartConfigPanel> props.
       `widget` is the WidgetDto in scope in WidgetConfigModal (line 1256). No new prop threading beyond this one line —
       WidgetConfigModal already has the widget.

    3. Verify existing ChartConfigPanel.spec.tsx still compiles (widgetId is optional, so existing call sites
       at spec lines 91+ are unaffected). If any spec asserts an exact count of config-groups, update it to account
       for the new Filter Scope section ONLY for widgets with a data source.
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/components/charts/ChartConfigPanel.spec.tsx src/components/charts/FilterSelectionPanel.spec.tsx</automated>
  </verify>
  <done>Opening a chart widget config panel renders the Filter Scope section after Drill-Down (incl. the spatial-draws row in allowlist mode); widgetId is threaded so the widget is absent from its own source list; web tsc clean; specs green.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx tsc --noEmit` — clean.
- `cd packages/web && npx vitest run` — 100% (no regressions; new specs green).
- `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` — green (no new CSS class, no raw hex; var(--danger) only).
- `git diff --name-only packages/server` — EMPTY (FRONTEND-ONLY).
- Manual grep: `grep -n "records" packages/web/src/components/charts/filterSourceTypes.ts` returns NO match inside the Set (records intentionally excluded).
- Manual grep: `grep -n "SPATIAL_DRAWS_SENTINEL" packages/web/src/components/charts/filterSourceTypes.ts` returns the export (sentinel present).
</verification>

<success_criteria>
Maps to Phase 93 ROADMAP success criteria:
- SC1 (chart Filter Scope section + opt-out toggle + filter-producing-only checklist INCLUDING the persistent map spatial-draws sentinel row, NOT records/info-popup/legend): Task 1 (enumeration + SPATIAL_DRAWS_SENTINEL + panel incl. the always-present spatial row) + Task 2 (ChartConfigPanel integration).
- SC3 (orphan warning mirrors RadioGroupConfigPanel; sentinel exempt): Task 1 orphan branch.
- SC5 (web tsc clean; web vitest 100%; theme-guard green): verification block.
(SC2 layer-level + SC4 persist-across-reload are Plan 02's responsibility — this plan's chart-widget persistence rides the existing widget.config PATCH with no schema change, so chart-side reload survival is inherent. The spatial-draws sentinel persists identically as just another allowedSourceWidgetIds entry.)
</success_criteria>

<output>
After completion, create `.planning/phases/93-filter-scope-config-ui/93-01-SUMMARY.md` documenting: the FILTER_PRODUCING_TYPES set (and the records-exclusion rationale), the SPATIAL_DRAWS_SENTINEL value + its config-only/forward-compatible boundary (stored in 93, read by 93.5), the allowedSourceWidgetIds type widening to (number|string)[], the FilterSelectionPanel prop contract (so Plan 02 reuses it), the css classes reused, and the widgetId threading.
</output>
</content>
</invoke>
