---
phase: 68.2-calendar-week-anchor-spike-per-group-date-range-gap-fill
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/lib/calendarLayout.ts
  - packages/web/src/lib/calendarBin.ts
  - .planning/phases/68.2-calendar-week-anchor-spike-per-group-date-range-gap-fill/68.2-02-SPIKE.md
autonomous: true
requirements: [CALUX-V113-03]

must_haves:
  truths:
    - "The live Kinetica DATE_TRUNC('week') anchor is either confirmed and WEEK_START aligned to it, OR recorded NOT-RUN with WEEK_START kept and Phase 69 re-flagged"
    - "WEEK_START in calendarLayout.ts reflects the spike result (1=Monday if confirmed/unrun; 0=Sunday only if the live probe returned a Sunday anchor)"
    - "The gap-fill fix (Plan 03) is NEVER blocked on instance reachability"
  artifacts:
    - path: ".planning/phases/68.2-calendar-week-anchor-spike-per-group-date-range-gap-fill/68.2-02-SPIKE.md"
      provides: "Recorded anchor probe result (RAN with anchor day, or NOT-RUN with reason + Phase 69 flag)"
      min_lines: 20
    - path: "packages/web/src/lib/calendarLayout.ts"
      provides: "WEEK_START constant set/confirmed from the spike"
      contains: "export const WEEK_START"
  key_links:
    - from: "packages/web/src/lib/calendarLayout.ts"
      to: "DATE_TRUNC('week') live anchor"
      via: "WEEK_START constant aligned to the probe result"
      pattern: "WEEK_START\\s*="
---

<objective>
Resolve the coupled week-anchor question: run the live Kinetica `DATE_TRUNC('week', ts)` probe that Phase 65 deferred (recorded NOT-RUN), confirm which day Kinetica anchors weeks to (Mon/Sun/other), and set `WEEK_START` in `calendarLayout.ts` to that anchor — so week-domain day groups stay column-clean once Plan 03's per-group enumeration lands.

CRITICAL FALLBACK (this is the most likely path): Phase 65-02 could NOT run the probe because `/api/sql` requires a session whose credentials live in `packages/server/.env`, and reading `.env` is security-prohibited. If that barrier persists, record NOT-RUN AGAIN, keep `WEEK_START = 1`, re-flag Phase 69, and DO NOT block anything. The gap-fill fix (Plan 03) is correct for whatever value `WEEK_START` holds — this plan only TUNES the constant.

Purpose: Align the visual week anchor (CALUX-V113-03) and clear the Phase 65 NOT-YET-VERIFIED annotation IF (and only if) the probe runs.
Output: `68.2-02-SPIKE.md` record + a possible one-line `WEEK_START` edit + annotation update.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68.2-calendar-week-anchor-spike-per-group-date-range-gap-fill/68.2-CONTEXT.md
@.planning/phases/65-calendar-sql-builder-kinetica-spike/65-02-SUMMARY.md

<interfaces>
<!-- WEEK_START + the week-offset math it drives. -->
From packages/web/src/lib/calendarLayout.ts §36-41:
```typescript
export const WEEK_START = 1; // Monday-ISO. offset = (getUTCDay() + 7 - WEEK_START) % 7
```
From packages/web/src/lib/calendarBin.ts §36-52:
```typescript
// KINETICA_DATE_TRUNC_UNITS — comment currently annotated "NOT YET VERIFIED" + "Phase 69".
// computeCellBounds week case uses offset = (getUTCDay() + 6) % 7 (Monday-ISO).
```
The exact probe queries are pre-written in 65-02-SUMMARY.md §"Exact Queries for Phase 69 UAT" (Query A = week anchor on 2024-01-01, a Monday).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the live week-anchor probe (or record NOT-RUN) and align WEEK_START</name>
  <files>packages/web/src/lib/calendarLayout.ts, packages/web/src/lib/calendarBin.ts, .planning/phases/68.2-calendar-week-anchor-spike-per-group-date-range-gap-fill/68.2-02-SPIKE.md</files>
  <read_first>
    - .planning/phases/65-calendar-sql-builder-kinetica-spike/65-02-SUMMARY.md §"Exact Queries" (Query A week-anchor) + §"Spike Status: NOT-RUN" (the auth barrier that will likely recur)
    - packages/web/src/lib/calendarLayout.ts §32-41 (WEEK_START + the offset formula it feeds)
    - packages/web/src/lib/calendarBin.ts §36-52 (KINETICA_DATE_TRUNC_UNITS NOT-YET-VERIFIED annotation) and §129-138 (computeCellBounds week offset)
    - .planning/phases/68.2-.../68.2-CONTEXT.md §decisions (1) — the spike + fallback rules
  </read_first>
  <action>
    1. Probe attempt (bounded — do NOT spend more than a couple of attempts; do NOT read packages/server/.env):
       - Check the dev server: `curl -s http://localhost:4000/api/health` (or the configured port). 
       - Attempt the anchor probe via the authenticated path ONLY if a session is already obtainable WITHOUT reading .env (e.g. an existing session cookie/env the harness legitimately exposes). Query A:
         `SELECT DATE_TRUNC('week', TIMESTAMP '2024-01-01 00:00:00') AS wk`
         2024-01-01 is a Monday: result "2024-01-01 ..." → Monday anchor; "2023-12-31 ..." → Sunday anchor; anything else → record verbatim.
       - DO NOT read, print, echo, cat, or commit packages/server/.env. If the only way to authenticate is via .env credentials, treat the probe as UNREACHABLE → NOT-RUN.

    2a. IF THE PROBE RAN and returned an anchor:
       - Monday → leave `WEEK_START = 1` (no edit needed; confirm value).
       - Sunday → set `WEEK_START = 0` in calendarLayout.ts (one-line edit). Also update the calendarLayout.ts WEEK_START comment + the calendarBin.ts week-offset note to reflect Sunday. NOTE: per-group day enumeration in Plan 03/Plan 01 snaps to Monday defensively; if anchor is Sunday, also flag that Plan 01's enumerateGroupBuckets week-start snap and computeCellBounds week offset should be revisited — record this clearly in the SPIKE doc as a follow-up for Plan 03 wave.
       - Other value → record verbatim, keep WEEK_START=1, flag for human decision.
       - Clear the Phase 65 NOT-YET-VERIFIED annotation on KINETICA_DATE_TRUNC_UNITS in calendarBin.ts: replace the "NOT YET VERIFIED ... FLAGGED for Phase 69" comment with "VERIFIED <date> via live DATE_TRUNC('week') probe — week anchor = <Mon|Sun|other>; all units VALID" (only assert units VALID if Query B was also run; otherwise scope the annotation to the week anchor only).

    2b. IF THE PROBE DID NOT RUN (unreachable / auth-gated by .env — the EXPECTED path):
       - Keep `WEEK_START = 1` unchanged.
       - Leave the calendarBin.ts NOT-YET-VERIFIED annotation in place (do NOT clear it) but UPDATE its reference so it points at Phase 69 (it already does) and note "re-attempted in Phase 68.2-02, still NOT-RUN (auth barrier persists)".
       - Re-flag Phase 69 in the SPIKE doc as the verification owner.

    3. Write `.planning/phases/68.2-.../68.2-02-SPIKE.md` recording: STATUS (RAN / NOT-RUN), the probe command attempted, the verbatim result (or the exact barrier), the resulting WEEK_START value, whether the calendarBin annotation was cleared, and the Phase 69 follow-up (if NOT-RUN). Mirror the Phase 65-02 SUMMARY format.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/lib/calendarBin.spec.ts src/lib/calendarLayout.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/68.2-.../68.2-02-SPIKE.md` exists and contains a STATUS line matching `^.*STATUS.*(RAN|NOT-RUN)` and the resulting WEEK_START value.
    - `grep -Eq "WEEK_START\s*=\s*[01]" packages/web/src/lib/calendarLayout.ts` (WEEK_START is 1 if NOT-RUN or Monday-confirmed; 0 ONLY if the probe returned Sunday).
    - If STATUS=NOT-RUN: `grep -Eqi "NOT.?YET.?VERIFIED" packages/web/src/lib/calendarBin.ts` STILL passes AND `grep -Eqi "Phase 69" packages/web/src/lib/calendarBin.ts` passes (annotation retained, re-flagged).
    - If STATUS=RAN: `grep -Eqi "VERIFIED" packages/web/src/lib/calendarBin.ts` reflects the cleared annotation with the recorded anchor.
    - packages/server/.env was NEVER read, printed, or committed (assert in SPIKE doc, as Phase 65-02 did).
    - `cd packages/web && npx vitest run src/lib/calendarBin.spec.ts src/lib/calendarLayout.spec.ts` GREEN (a WEEK_START flip to 0, if it happens, must keep both specs green — update spec expectations in the same plan if needed).
  </acceptance_criteria>
  <done>Anchor probe result recorded; WEEK_START reflects it; calendarBin annotation cleared (if RAN) or retained+re-flagged (if NOT-RUN); gap-fill fix unblocked either way.</done>
</task>

</tasks>

<verification>
- 68.2-02-SPIKE.md records STATUS + WEEK_START + Phase 69 follow-up (if NOT-RUN).
- `cd packages/web && npx vitest run src/lib/calendarBin.spec.ts src/lib/calendarLayout.spec.ts` green.
- `cd packages/web && npx tsc --noEmit -p tsconfig.json` exit 0.
- packages/server/.env untouched.
</verification>

<success_criteria>
The week anchor is resolved one of two ways — confirmed-and-aligned, or NOT-RUN-and-re-flagged — with WEEK_START set accordingly and the gap-fill fix never blocked on instance availability.
</success_criteria>

<output>
After completion, create `.planning/phases/68.2-calendar-week-anchor-spike-per-group-date-range-gap-fill/68.2-02-SUMMARY.md`
</output>
