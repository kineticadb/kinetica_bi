---
phase: 68.2-calendar-week-anchor-spike-per-group-date-range-gap-fill
plan: 02
spike: week-anchor DATE_TRUNC probe
status: NOT-RUN
week_start_result: 1 (Monday-ISO, unchanged)
phase_69_flagged: true
env_read: false
completed: 2026-06-17
---

# Phase 68.2 Plan 02: Kinetica DATE_TRUNC Week-Anchor Spike

## STATUS: NOT-RUN

**Reason:** Same auth barrier as Phase 65-02. The `POST /api/sql` route on `localhost:4000`
requires a valid Kinetica session (established via `POST /api/auth/login` with live Kinetica
credentials). The executor cannot obtain a session without reading `packages/server/.env`,
which is security-prohibited per the plan's explicit constraint.

## Probe Attempt Log

### Step 1 — Server Health Check

```
curl -s http://localhost:4000/api/health
→ {"status":"ok","service":"kinetica-bi-backend","version":"0.1.0"}
```

Server confirmed running.

### Step 2 — Week-Anchor Probe (Query A)

```
curl -s -X POST http://localhost:4000/api/sql \
  -H "Content-Type: application/json" \
  -d '{"sql": "SELECT DATE_TRUNC(''week'', TIMESTAMP ''2024-01-01 00:00:00'') AS wk"}'
→ {"error":"Authentication required.","code":"REAUTH_REQUIRED"}
```

**Barrier:** Session authentication required. The probe did NOT reach Kinetica.

### Step 3 — .env Access

`packages/server/.env` was NOT read, printed, echoed, or committed.
No credentials were extracted or surfaced. This assertion is true and intentional.

## Result

| Item | Value |
|------|-------|
| STATUS | NOT-RUN |
| Probe attempted | YES (two attempts: health + /api/sql) |
| Barrier | REAUTH_REQUIRED — session auth via .env credentials |
| WEEK_START set to | 1 (Monday-ISO, unchanged from Phase 65-02) |
| calendarBin.ts annotation cleared | NO — retained + updated to note Phase 68.2-02 re-attempt |
| Phase 69 re-flagged | YES |
| packages/server/.env accessed | NEVER |

## WEEK_START = 1 (Monday-ISO) — Confirmed as Current Value

`calendarLayout.ts` line 41:
```typescript
export const WEEK_START = 1; // Monday-ISO
```

No change made. This value is kept as the documented assumption pending Phase 69 UAT.

## calendarBin.ts Annotation Update

The `KINETICA_DATE_TRUNC_UNITS` comment in `packages/web/src/lib/calendarBin.ts` was updated
to note the Phase 68.2-02 re-attempt:

```
NOT YET VERIFIED against live Kinetica — spike could not run in this env
(the /api/sql route on localhost:4000 requires a valid Kinetica session ...)
Re-attempted in Phase 68.2-02 (2026-06-17): still NOT-RUN, same auth barrier persists.
See ... 68.2-02-SPIKE.md for the exact spike queries to run during Phase 69 UAT.
```

The annotation was NOT cleared (only cleared on RAN + confirmed anchor).

## Phase 69 Follow-Up

**CAL-V113-03 — DATE_TRUNC week-anchor spike (NOT-RUN in Phase 65-02, NOT-RUN again in Phase 68.2-02)**

During the Phase 69 live operator walk-through, run the following query against the live
Kinetica instance via the authenticated `/api/sql` endpoint (or Kinetica Web UI):

### Query A — Week Anchor (2024-01-01 is a Monday)

```sql
SELECT DATE_TRUNC('week', TIMESTAMP '2024-01-01 00:00:00') AS wk
```

**Interpretation:**
- Result = `2024-01-01` → week starts **Monday** (ISO) — `WEEK_START = 1` confirmed, no code change.
- Result = `2023-12-31` → week starts **Sunday** → set `WEEK_START = 0` in `calendarLayout.ts`;
  update `computeCellBounds` week-offset formula in `calendarBin.ts` from `(dow + 6) % 7` to
  `dow % 7`; update `calendarBin.spec.ts` week-case expected values; rerun specs.
- Any other value → record verbatim; human decision required.

**If anchor = Sunday, also review:**
- `layoutDayBlock` in `calendarLayout.ts` — `WEEK_START` is already threaded via the `offsetToMonday`
  and `rowIdx` calculations; a flip to 0 will automatically snap weeks to Sunday.
- `enumerateGroupBuckets` week case in `calendarGapFill.ts` — the week-start snap uses the same
  `(dow + 6) % 7` formula; update to `dow % 7` if anchor is Sunday.
- Rerun the full suite: `cd packages/web && npx vitest run`.

### Full Queries from Phase 65-02 (Queries B–D)

See `.planning/phases/65-calendar-sql-builder-kinetica-spike/65-02-SUMMARY.md` §"Exact Queries
for Phase 69 UAT" for the complete set (per-unit validity, UTC format, quarter availability).

## Context: Why This Matters for Plan 03 (Gap-Fill Fix)

The gap-fill fix (Plan 03 — `enumerateGroupBuckets` in `calendarGapFill.ts`) is NOT blocked on
this spike. `enumerateGroupBuckets` uses the same `WEEK_START` constant to snap week starts; it
will be correct for whatever value `WEEK_START` holds when Plan 03 ships.

The spike only TUNES the constant — Phase 69 confirms or corrects it.

## Verification

| Gate | Result |
|------|--------|
| `grep -Eq "WEEK_START\s*=\s*1" packages/web/src/lib/calendarLayout.ts` | PASSES |
| `grep -Eqi "NOT.?YET.?VERIFIED" packages/web/src/lib/calendarBin.ts` | PASSES |
| `grep -Eqi "Phase 69" packages/web/src/lib/calendarBin.ts` | PASSES |
| `grep -Eqi "68.2-02" packages/web/src/lib/calendarBin.ts` | PASSES |
| `packages/server/.env` read/printed/committed | NEVER |
