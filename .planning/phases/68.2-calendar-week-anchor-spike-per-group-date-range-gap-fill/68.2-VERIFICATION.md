---
phase: 68.2-calendar-week-anchor-spike-per-group-date-range-gap-fill
verified: 2026-06-17T00:00:00Z
status: passed
score: 6/6 must-haves verified
re_verification: false
---

# Phase 68.2: Calendar Week-Anchor Spike + Per-Group Date-Range Gap-Fill — Verification Report

**Phase Goal:** Fix the calendar gap-fill so each domain group only shows time slices within its own range (no global cross-fill that gives week×day a phantom month-shaped block) — in-range gaps grey, out-of-range slots blank — and verify/align the visual week anchor (WEEK_START) to the live Kinetica DATE_TRUNC('week') anchor (the NOT-RUN Phase 65 spike).

**Verified:** 2026-06-17
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | enumerateGroupBuckets(domainKey, domain, subdomain) exists, exported, pure, emits "YYYY-MM-DD HH:mm:ss" UTC keys | VERIFIED | calendarBuckets.ts:58 — exported; fmtUTC uses getUTC* only, no toISOString; all new Date() calls are single-arg (epoch ms); type-only import from calendarBin; 218 lines (min_lines=60 satisfied) |
| 2 | All 8 combos covered including leap Feb (29 keys) and all boundary cases; key format regex enforced in spec | VERIFIED | calendarBuckets.spec.ts (235 lines, min_lines=80): toHaveLength(7/28/29/30/31/12/24/365/366/168), not.toContain exclusions, KEY_FORMAT regex applied to every key set |
| 3 | gapFillCalendar rewired to per-group signature gapFillCalendar(rows, domain, subdomain); consumes enumerateGroupBuckets; no global cross-fill | VERIFIED | calendarGapFill.ts:81-85 (signature); line 30 imports enumerateGroupBuckets; lines 112-126 build per-group cells via enumerateGroupBuckets per domainKey; no global subdomainSet cross-fill |
| 4 | In-range missing bucket → value:null (GREY); out-of-range → no cell (BLANK); week×day group = exactly 7 cells; spec updated — old dense 2×2 global-cross-fill assertion removed | VERIFIED | calendarGapFill.spec.ts: "dense 2×2" absent; lines 121-157 assert week A gets 7 cells, 6 null, no week B days; lines 162-182 assert 5-of-7 → 7 cells 2 null; lines 245-261 assert out-of-range cells are undefined |
| 5 | CalendarRenderer gapFillCalendar call updated to gapFillCalendar(data, effDomain, effSubdomain) with correct dep array; locked invariants preserved | VERIFIED | CalendarRenderer.tsx:338 — exact call gapFillCalendar(data, effDomain, effSubdomain) with dep array [data, effDomain, effSubdomain]; no materializeFilter/dropFilterView/fromSwap import; no raw hex; value===null guard at line 387 and 572; drill/tooltip/respondToFilters/activeCell all present |
| 6 | Week-anchor spike: SPIKE.md records NOT-RUN result (REAUTH_REQUIRED documented fallback); WEEK_START=1 retained; calendarBin annotation kept+updated to note Phase 68.2-02 re-attempt; Phase 69 re-flagged | VERIFIED | 68.2-02-SPIKE.md: STATUS=NOT-RUN, WEEK_START=1, env never read; calendarBin.ts:38-46 annotation retained and updated with "Re-attempted in Phase 68.2-02 (2026-06-17): still NOT-RUN"; Phase 69 re-flagged |

**Score:** 6/6 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/web/src/lib/calendarBuckets.ts` | Pure per-group enumerator exporting enumerateGroupBuckets | VERIFIED | 218 lines; pure (type-only import from calendarBin; no React/Zustand/Recharts); fmtUTC uses getUTC*; no toISOString; no local-time Date constructors |
| `packages/web/src/lib/calendarBuckets.spec.ts` | Exhaustive combo/leap/boundary/UTC tests | VERIFIED | 235 lines; covers all 8 combos; leap Feb 2024 = 29 keys; non-leap 2023 = 28; year×month = 12; day×hour = 24; format regex on every key set; exclusion assertions via not.toContain |
| `packages/web/src/lib/calendarGapFill.ts` | Per-group gap-fill consuming enumerateGroupBuckets | VERIFIED | 143 lines; imports enumerateGroupBuckets; new signature takes domain+subdomain; per-group loop via enumerateGroupBuckets; in-range missing → null; out-of-range never enters cells |
| `packages/web/src/lib/calendarGapFill.spec.ts` | Per-group semantics; old global-cross-fill assertion removed | VERIFIED | 298 lines; all gapFillCalendar calls pass (rows, domain, subdomain); "dense 2×2" absent; week×day no-cross-fill at lines 121-157; in-range-grey at 162-182; out-of-range blank at 245-261; day×hour no-cross-fill at 209-240 |
| `packages/web/src/components/charts/CalendarRenderer.tsx` | Updated gapFill call | VERIFIED | 652 lines; line 338: gapFillCalendar(data, effDomain, effSubdomain) with dep array [data, effDomain, effSubdomain] |
| `.planning/phases/68.2-.../68.2-02-SPIKE.md` | Recorded spike result (NOT-RUN with reason + Phase 69 flag) | VERIFIED | 132 lines (min_lines=20); STATUS=NOT-RUN; probe attempt logged; WEEK_START=1; Phase 69 re-flagged; .env never accessed |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| calendarBuckets.ts | calendarBin.ts | import type { CalendarDomain, CalendarSubdomain } | WIRED | calendarBuckets.ts:23 — `import type { CalendarDomain, CalendarSubdomain } from "./calendarBin"` |
| calendarGapFill.ts | calendarBuckets.ts | imports enumerateGroupBuckets | WIRED | calendarGapFill.ts:30 — `import { enumerateGroupBuckets } from "./calendarBuckets"` — consumed at line 114 per domainKey |
| CalendarRenderer.tsx | gapFillCalendar | useMemo passes (data, effDomain, effSubdomain) | WIRED | CalendarRenderer.tsx:338 — `const grid = useMemo(() => gapFillCalendar(data, effDomain, effSubdomain), [data, effDomain, effSubdomain])` |
| calendarLayout.ts | DATE_TRUNC('week') live anchor | WEEK_START constant | PARTIAL/DEFERRED | WEEK_START=1 (Monday-ISO, unchanged); live probe NOT-RUN due to REAUTH_REQUIRED; alignment deferred to Phase 69 per documented fallback |

---

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CALUX-V113-03 | 68.2-01, 68.2-02, 68.2-03 | Per-group gap-fill + week anchor aligned to live Kinetica DATE_TRUNC('week') | PARTIALLY SATISFIED | Gap-fill part: COMPLETE (all three plans delivered per-group semantics, per-group spec, renderer update). Week-anchor part: NOT-RUN (deferred to Phase 69 per documented fallback — REAUTH_REQUIRED barrier, .env security-prohibited). REQUIREMENTS.md §79 marks CALUX-V113-03 as "Complete" for Phase 68.2 but the live anchor verification is explicitly deferred. |

**Note on CALUX-V113-03 split scope:** The requirement has two parts. Part (a) — per-group gap-fill — is fully delivered and verifiable. Part (b) — live Kinetica DATE_TRUNC('week') anchor verified and WEEK_START aligned — is NOT-RUN, same barrier as Phase 65. This is the documented, valid fallback per the plan. WEEK_START=1 (Monday-ISO) is retained as the current assumption. Phase 69 is the verification owner. The phase verdict is PASSED for its deliverable scope.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | — | — | — | — |

No TODOs/FIXMEs/stubs/empty implementations found in the phase deliverables. No raw hex in CalendarRenderer.tsx. No forbidden imports (materializeFilter/dropFilterView/fromSwap appear only in comments, not actual imports).

---

### Human Verification Required

None for automated checks. The following items are noted as Phase 69 UAT items (not blocking this phase):

**1. Live Kinetica DATE_TRUNC week anchor**

Test: Run `SELECT DATE_TRUNC('week', TIMESTAMP '2024-01-01 00:00:00') AS wk` against the live Kinetica instance via an authenticated /api/sql session.

Expected: Result "2024-01-01" confirms Monday anchor (WEEK_START=1 correct, no code change). Result "2023-12-31" means Sunday anchor (WEEK_START must flip to 0 in calendarLayout.ts + computeCellBounds formula update).

Why human: Requires authenticated Kinetica session — executor cannot obtain one without reading packages/server/.env (security-prohibited per plan).

---

### Gaps Summary

No gaps. All deliverable-scope must-haves are verified.

The only deferred item is the live Kinetica week-anchor probe (part (b) of CALUX-V113-03), which is carried to Phase 69 per the plan's documented fallback. The NOT-RUN outcome is a valid completion for this phase — the gap-fill fix is correct for whatever value WEEK_START holds, and the spike record is complete with exact queries for Phase 69 UAT.

---

_Verified: 2026-06-17_
_Verifier: Claude (gsd-verifier)_
