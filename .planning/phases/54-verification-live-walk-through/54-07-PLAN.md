---
phase: 54-verification-live-walk-through
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/lib/wmsUrlBuilder.ts
  - packages/web/src/lib/wmsUrlBuilder.spec.ts
  - packages/web/src/components/charts/KineticaWmsLayerForm.tsx
  - packages/web/src/components/charts/KineticaWmsLayerForm.spec.tsx
  - packages/web/src/components/charts/CbConfigForm.spec.tsx
autonomous: false
requirements: [TRACKFIX-V19-06]
gap_closure: true

must_haves:
  truths:
    - "Under Track + Class Break, TRACKHEADCOLORS / TRACKLINECOLORS / TRACKMARKERCOLORS each emit the comma-separated list of per-break colors (cb.breaks[].color, normalized AARRGGBB), positionally matching CB_VALS — NOT a single color replicated"
    - "Under Track + Class Break, non-color track params (TRACKHEADSHAPES/SIZES, TRACKMARKERSHAPES/SIZES, TRACKLINEWIDTHS) stay uniform expand(N)"
    - "Under Track + Raster, all TRACK_* colors stay single-value from TRACK STYLE (unchanged from 54-06)"
    - "Under Track + Class Break, the form shows the FULL CB builder (column, method, auto-suggest, N slider, color theme, per-break rows WITH per-break color picker)"
    - "Under Track + Class Break, the single-color TRACK STYLE color controls (Head color + alpha, Track line color + alpha, Track marker color + alpha) are hidden; head/marker shape+size + track line width stay visible"
    - "Operator confirms via live §3 re-walk that Kinetica renders DIFFERENT per-break track colors"
  artifacts:
    - path: "packages/web/src/lib/wmsUrlBuilder.ts"
      provides: "Per-break track color emission under track+classbreak lane"
      contains: "cb.breaks.map"
    - path: "packages/web/src/components/charts/KineticaWmsLayerForm.tsx"
      provides: "TRACK STYLE color controls gated on effectiveRenderMode !== classbreak"
      contains: "effectiveRenderMode"
  key_links:
    - from: "packages/web/src/lib/wmsUrlBuilder.ts (track block)"
      to: "cb_config.breaks[].color"
      via: "cb.breaks.map(b => normalizeAARRGGBB(b.color, ...)).join(',') for the 3 color params"
      pattern: "TRACKHEADCOLORS"
    - from: "packages/web/src/components/charts/KineticaWmsLayerForm.tsx (TRACK STYLE)"
      to: "effectiveRenderMode"
      via: "conditional render of the 3 color control blocks (effectiveRenderMode !== 'classbreak')"
      pattern: "effectiveRenderMode !== \"classbreak\""
---

<objective>
Close GAP-54-07 / TRACKFIX-V19-06: categorical per-break track coloring under Track + Class Break.

§2 (Track + Raster) already passed in the 54-06 re-walk. This plan closes the §3 gap: under Track + Class Break the tracks must render with a DISTINCT color per class break (driven by cb_config.breaks[].color), not a single uniform color — and the form must surface the full normal CB builder (including per-break color pickers) while hiding the now-redundant single-color TRACK STYLE color controls.

Purpose: This is the un-deferred TRACK-V20-03. The operator saw single-color tracks under CB live (54-UAT §3) and re-opened it as an in-scope gap. Per-break track color is the categorical-coloring behavior operators expect from Class Break, applied to track segments.
Output: Per-break TRACK_* color emission in wmsUrlBuilder; gated TRACK STYLE color controls in the form; updated specs; operator §3 re-walk confirmation that Kinetica honors the per-break colors.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/54-verification-live-walk-through/54-UAT.md

# Source under change
@packages/web/src/lib/wmsUrlBuilder.ts
@packages/web/src/components/charts/KineticaWmsLayerForm.tsx
@packages/web/src/components/charts/CbConfigForm.tsx

# Pure helpers (read-only — do NOT modify shapes)
@packages/web/src/lib/cbConfig.ts
@packages/web/src/lib/trackConfig.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. Use directly — no exploration needed. -->

From lib/cbConfig.ts:
```typescript
export type CbBreak = { value: string | number; min?: number; max?: number; color: string; label?: string; /* + advanced fields */ };
export type CbConfig = { attr: string; valsType: "numeric" | "categorical"; breaks: CbBreak[]; includeOtherBucket?: boolean };
export function coalesceCbConfig(raw: string | null): CbConfig;       // EMPTY_CB_CONFIG on null/parse-fail
export function isCbConfigConfigured(cfg: CbConfig): boolean;          // attr.length > 0 && breaks.length > 0
```

From lib/trackConfig.ts:
```typescript
export type TrackConfig = { enabled: boolean; headColor?: string; trailColor?: string; markerColor?: string;
  headSize?: number; trailSize?: number; lineWidth?: number; headShape?: string; markerShape?: string; markerSize?: number; /* ...attrs */ };
export const TRACK_DEFAULTS = { headColor: "FFFFFFFF", trailColor: "FF00FF00", markerColor: "FF0000FF", /* ... */ };
```

From lib/colorHex.ts (already imported in both files):
```typescript
export function normalizeAARRGGBB(value: string, fallback?: string): string;   // → 8-char AARRGGBB
```

wmsUrlBuilder.ts track block CURRENT shape (~452-487):
```typescript
const isCb = config.renderMode === "classbreak";
const cbForTrack = isCb && layerJsonFields ? coalesceCbConfig(layerJsonFields.cb_config) : null;
const n = isCb && cbForTrack && isCbConfigConfigured(cbForTrack) ? cbForTrack.breaks.length : 1;
const expand = (v: string): string => Array.from({ length: n }, () => v).join(",");
// ...then ALL params (colors + sizes + shapes) use expand(...)
```

KineticaWmsLayerForm.tsx TRACK STYLE section (~755): rendered when `spatialMode === "track"`,
contains in order: Head color + Head color alpha, Head size, Head shape, Track line color +
Track line color alpha, Track line width, Track marker color + Track marker color alpha,
Track marker shape, Track marker size. `effectiveRenderMode` is already in scope (~288).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-break track color emission under track + classbreak</name>
  <files>packages/web/src/lib/wmsUrlBuilder.ts, packages/web/src/lib/wmsUrlBuilder.spec.ts</files>
  <behavior>
    Under track + classbreak with a CONFIGURED cb_config (N = breaks.length):
    - TRACKHEADCOLORS === cb.breaks.map(b => normalizeAARRGGBB(b.color, <headColor default>)).join(",")
    - TRACKLINECOLORS === SAME per-break color list (one break color drives all three)
    - TRACKMARKERCOLORS === SAME per-break color list
    - Each positionally matches CB_VALS (already emitted by the classbreak branch).
    - TRACKHEADSHAPES / TRACKHEADSIZES / TRACKLINEWIDTHS / TRACKMARKERSHAPES / TRACKMARKERSIZES stay uniform expand(N) (single value ×N).
    - CB_ATTR / CB_VALS still emitted (unchanged); POINT*/SHAPE* suppression from 54-06 still deletes those keys.
    - Under track + RASTER (no CB): TRACK_* colors stay single-value from TRACK STYLE — UNCHANGED from 54-06.
    - When cb_config is null/unconfigured under classbreak: fall back to the single-color expand path (n collapses, the per-break list is unavailable) — track still renders with the TRACK STYLE single color.
  </behavior>
  <action>
    In wmsUrlBuilder.ts track block (~452-487), the existing logic already computes `isCb`,
    `cbForTrack` (= coalesceCbConfig of cb_config), `n`, and `expand`.

    Add a per-break color list helper ALONGSIDE expand, available only when CB is configured:
      const cbColors: string[] | null =
        isCb && cbForTrack && isCbConfigConfigured(cbForTrack) && cbForTrack.breaks.length > 0
          ? cbForTrack.breaks.map((b: CbBreak) => normalizeAARRGGBB(b.color, "FFFFFFFF"))
          : null;
    (Import of CbBreak + coalesceCbConfig + isCbConfigConfigured already exist at top of file.)

    Define a per-color emitter so the THREE color params use the per-break list when available,
    else fall back to expand(single):
      const colorList = (single: string): string => (cbColors ? cbColors.join(",") : expand(single));

    Change ONLY the three color emissions:
      - TRACKHEADCOLORS:   params.TRACKHEADCOLORS = colorList(normalizeAARRGGBB(tc.headColor, "FFFFFFFF"));
      - TRACKLINECOLORS:   params.TRACKLINECOLORS = colorList(normalizeAARRGGBB(tc.trailColor, "FF00FF00"));
      - TRACKMARKERCOLORS: params.TRACKMARKERCOLORS = colorList(normalizeAARRGGBB(tc.markerColor, "FF0000FF"));
    Keep their existing `if (tc.headColor !== undefined)` / `if (tc.trailColor !== undefined)` /
    `if (tc.markerColor !== undefined)` guards exactly as-is — only the right-hand assignment changes.

    NOTE: cbColors uses the SAME per-break color for all three params (operator spec: one break
    color drives head/line/marker for that class). Do NOT try to source distinct per-break head vs
    line vs marker colors — cb_config has a single color per break.

    Leave TRACKHEADSHAPES, TRACKHEADSIZES, TRACKLINEWIDTHS, TRACKMARKERSHAPES, TRACKMARKERSIZES
    on expand(...) UNCHANGED (uniform per spec).

    SPEC UPDATES (wmsUrlBuilder.spec.ts) — only the track-CB color assertions change; everything
    else stays byte-unchanged:
      - ~line 920-921: the test "under STYLES=cb_raster: emits N comma-separated TRACK_* ...":
        cb_config has breaks colors [FF000000, FFFFFFFF, FF112233]. Change:
          TRACKHEADCOLORS   "FFFFFFFF,FFFFFFFF,FFFFFFFF"  →  "FF000000,FFFFFFFF,FF112233"
          TRACKLINECOLORS   "FF00FF00,FF00FF00,FF00FF00"  →  "FF000000,FFFFFFFF,FF112233"
          TRACKMARKERCOLORS "FF0000FF,FF0000FF,FF0000FF"  →  "FF000000,FFFFFFFF,FF112233"
        Leave TRACKHEADSIZES ("10,10,10"), TRACKLINEWIDTHS ("3,3,3"), TRACKHEADSHAPES
        ("circle,circle,circle"), TRACKMARKERSHAPES ("none,none,none"), TRACKMARKERSIZES ("2,2,2")
        assertions UNCHANGED.
      - ~line 1132-1133: the test "cb_raster: track+enabled emits N comma-separated TRACK_* ...":
        cb_config breaks colors [FF000000, FFFFFFFF, FF112233]. Change:
          TRACKHEADCOLORS "FFFF0000,FFFF0000,FFFF0000" → "FF000000,FFFFFFFF,FF112233"
          TRACKLINECOLORS "FF0000FF,FF0000FF,FF0000FF" → "FF000000,FFFFFFFF,FF112233"
        Leave TRACKHEADSIZES ("8,8,8") and TRACKLINEWIDTHS ("2,2,2") UNCHANGED.
        Also add an assertion in this test (or a sibling test) for
          TRACKMARKERCOLORS === "FF000000,FFFFFFFF,FF112233".
        Update the test title to reflect "per-break colors" rather than "byte-lock single color".
      - ADD one new test: track + classbreak with cb_config === null (or unconfigured) →
        TRACKHEADCOLORS/TRACKLINECOLORS/TRACKMARKERCOLORS fall back to the single TRACK STYLE color
        (no commas), proving the null-CB fallback path.
      - Do NOT touch the track + RASTER single-color tests (e.g. ~1100-1101, ~1154-1156) — they
        stay green unchanged (raster never enters the cbColors branch).
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/lib/wmsUrlBuilder.spec.ts</automated>
  </verify>
  <done>wmsUrlBuilder.spec.ts passes; under track+classbreak the 3 color params emit per-break colors matching cb.breaks[].color positionally with CB_VALS; non-color params stay uniform; track+raster single-color tests unchanged and green.</done>
</task>

<task type="auto">
  <name>Task 2: Hide single-color TRACK STYLE color controls under classbreak; confirm full CB builder</name>
  <files>packages/web/src/components/charts/KineticaWmsLayerForm.tsx, packages/web/src/components/charts/KineticaWmsLayerForm.spec.tsx, packages/web/src/components/charts/CbConfigForm.spec.tsx</files>
  <action>
    PART A — Gate the THREE color control blocks in the TRACK STYLE section (KineticaWmsLayerForm.tsx ~755-1023).
    The TRACK STYLE section stays rendered whenever `spatialMode === "track"`. Under classbreak, the
    per-break colors define track color, so the single-color color controls are redundant and must hide.

    Wrap each of these THREE control pairs in `{effectiveRenderMode !== "classbreak" && ( ... )}`:
      1. "Head color" (config-color-field) + "Head color alpha" (config-range-field)  [~762-817]
      2. "Track line color" + "Track line color alpha"  [~861-916]
      3. "Track marker color" + "Track marker color alpha"  [~936-991]

    KEEP visible (NOT gated — uniform controls valid under both modes):
      - Head size (~820-833), Head shape (~836-848)
      - Track line width (~919-932)
      - Track marker shape (~994-1006), Track marker size (~1009-1022)

    Use `effectiveRenderMode` (already in scope ~288) NOT raw `renderMode`, so the silent
    heatmap→raster coercion path is honored consistently. `effectiveRenderMode !== "classbreak"`
    means: visible under raster (track+raster), hidden under classbreak (track+CB).

    Do NOT alter the RASTER PARAMS gate (~1028 `effectiveRenderMode === "raster" && spatialMode !== "track"`)
    or the CbConfigForm mount (~1367). Under raster, ALL TRACK STYLE controls show (unchanged from 54-06).

    PART B — Confirm CbConfigForm trackContext does NOT over-hide (verification, likely NO code change).
    CbConfigForm.tsx already gates ONLY the per-row chevron (~827) and the per-row advanced panel (~948)
    on `!trackContext`. The column picker, Advanced "force categorical", cardinality, categorical
    <other> toggle, auto-suggest panel (method + N slider + button), color theme picker, and the
    per-break rows (value/range + per-break COLOR picker + label + remove) are ALL rendered
    regardless of trackContext. This already satisfies the spec ("full normal CB builder, per-break
    color visible/editable; only per-break ADVANCED point/shape panels hidden").
    If — and ONLY if — review finds trackContext suppressing the column/method/auto-suggest/theme
    controls or the per-break color picker, fix it so track+CB matches the non-track CB core experience.
    Otherwise make NO change to CbConfigForm.tsx.

    PART C — Tests.
    KineticaWmsLayerForm.spec.tsx — add a describe block "Track + Class Break — TRACK STYLE color gating
    (TRACKFIX-V19-06)" rendering the form with spatialMode="track", renderMode="classbreak", a
    track_config with enabled:true, and a configured cb_config. Assert:
      - Head color / Track line color / Track marker color controls are ABSENT
        (queryByLabelText("Track head color (RGB)") etc. → null; and the alpha sliders absent).
      - Head shape, Head size, Track line width, Track marker shape, Track marker size are PRESENT.
      - The CbConfigForm core controls are present: CB column picker (label "CB column"),
        "Auto-suggest breaks" button, "N" slider, and "Color theme" (when breaks exist) — proving
        the full CB builder shows under track.
    Also add (or extend an existing track+raster test) an assertion that under renderMode="raster"
    + spatialMode="track" the three color controls ARE present (guards the raster path).

    CbConfigForm.spec.tsx — add/confirm a test under trackContext={true} that asserts the per-break
    COLOR picker (aria-label `Color (RGB) for break 1`) is PRESENT and editable, while the per-row
    advanced chevron (aria-label `Toggle advanced for row 1`) is ABSENT. (If an equivalent assertion
    already exists from 53-01, extend its title to reference TRACKFIX-V19-06 rather than duplicating.)
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/KineticaWmsLayerForm.spec.tsx src/components/charts/CbConfigForm.spec.tsx</automated>
  </verify>
  <done>Under track+classbreak the 3 TRACK STYLE color controls are hidden and shape/size/width controls + the full CB builder (incl. per-break color picker) are shown; under track+raster all TRACK STYLE controls show; both spec files pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator live §3 re-walk — per-break track coloring under Track + Class Break</name>
  <action>Operator-executed live verification (NOT auto-verifiable). The executor pauses at this checkpoint and presents the what-built + how-to-verify steps below; the operator runs the app, walks UAT §3, and returns the resume-signal. See <how-to-verify> for the exact steps.</action>
  <what-built>
    Per-break track coloring under Track + Class Break:
    - wmsUrlBuilder now emits TRACKHEADCOLORS / TRACKLINECOLORS / TRACKMARKERCOLORS as the
      comma-separated per-break color list (cb_config.breaks[].color), positionally matching CB_VALS.
    - The form hides the single-color Head/Line/Marker color controls under Class Break and shows
      the full normal CB builder (column, method, auto-suggest, N, color theme, per-break color pickers);
      head/marker shape+size and track line width stay as uniform controls.
    This is the un-deferred TRACK-V20-03. This re-walk is effectively the spike confirming Kinetica
    visually honors per-break track colors.
  </what-built>
  <how-to-verify>
    Run the app (`npm run dev` web + `npm run dev:server` from repo root). Open the demo.track layer.

    1. Set spatial mode = Track, render mode = Class Break.
    2. Confirm the FULL CB builder shows: CB column picker, classification Method (numeric),
       Auto-suggest breaks button + N slider, Color theme picker, and per-break rows EACH with a
       per-break color picker + value/range + label. Per-break ADVANCED chevron panels should be ABSENT.
    3. Confirm the single-color TRACK STYLE color controls are HIDDEN: NO "Head color", NO "Track line
       color", NO "Track marker color" (and their alpha sliders). Confirm KEPT: Head shape, Head size,
       Track line width, Track marker shape, Track marker size.
    4. Configure (or auto-suggest) at least 3 breaks and give them DISTINCT, easily-distinguishable
       colors (e.g. red / green / blue) via the per-break color pickers. Optionally apply a Color theme.
    5. Save the layer.
    6. Open browser devtools → Network. Confirm a WMS GetMap request fires on load/zoom and inspect
       its query string: TRACKHEADCOLORS, TRACKLINECOLORS, TRACKMARKERCOLORS must each be a
       comma-separated list of DIFFERENT colors matching your per-break colors (and CB_VALS positionally),
       NOT one color repeated.
    7. Confirm the rendered map tiles show DIFFERENT track colors per break (categorical coloring on
       track segments), reflecting the per-break colors / theme.

    EXPECTATION NOTE: If Kinetica does NOT visually honor per-break track colors (tiles render with a
    single color despite the distinct TRACK*COLORS list in the request), this is a FINDING, not a code
    bug — record it as the operator note. The fallback is to revert per-break emission to the single
    TRACK STYLE color and open a dedicated spike for Kinetica per-break track-color support.
  </how-to-verify>
  <resume-signal>Type "approved" if per-break-colored tracks render and the form behaves as described, or describe what you saw (e.g. "request shows distinct colors but tiles render single-color" → finding for fallback + spike).</resume-signal>
</task>

</tasks>

<verification>
- `cd packages/web && npx vitest run` → 1638/1638 (or higher with the new tests added) green; NO regressions.
- `cd packages/web && npx tsc --noEmit` → clean.
- Non-track CB specs + non-track raster specs byte-unchanged; track-raster single-color specs from 54-06 green.
- Operator §3 re-walk PASS (or documented finding → fallback + spike).
</verification>

<success_criteria>
- Under track+classbreak: TRACKHEADCOLORS/TRACKLINECOLORS/TRACKMARKERCOLORS each = per-break color list (cb.breaks[].color, normalized), positionally matching CB_VALS; non-color track params stay uniform expand(N).
- Under track+raster: TRACK_* colors stay single-value (54-06 behavior unchanged).
- Form: TRACK STYLE color controls hidden under classbreak; shape/size/width kept; full CB builder (incl. per-break color picker) shown under track.
- Operator confirms via live §3 re-walk that Kinetica renders per-break-colored tracks (or records a finding routing to fallback + dedicated spike).
</success_criteria>

<output>
After completion, create `.planning/phases/54-verification-live-walk-through/54-07-SUMMARY.md`.
You MAY update `.planning/STATE.md`. DO NOT edit `.planning/ROADMAP.md` (orchestrator owns it).
</output>
