---
phase: 54-verification-live-walk-through
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/hooks/useMapOnlySpatialMaterialize.ts
  - packages/web/src/hooks/useMapOnlySpatialMaterialize.spec.ts
  - packages/web/src/components/DashboardsPage.tsx
autonomous: false
requirements: [TRACKFIX-V19-09]
gap_closure: true

must_haves:
  truths:
    - "Drawing a shape on a dashboard whose ONLY widget for table T is a map fires POST /api/filter/materialize for T exactly once"
    - "For a table T that has an aggregated-chart or records-table widget, the map-side effect does NOT fire materialize (no double-DDL — the sole-trigger invariant holds)"
    - "Clearing shapes / removing the target on a map-only table drops the materialized view"
    - "Switching dashboards or logging out aborts in-flight map-side materializes and resets state"
  artifacts:
    - path: "packages/web/src/hooks/useMapOnlySpatialMaterialize.ts"
      provides: "Dashboard-scope map-only spatial materialize orchestrator hook"
      min_lines: 80
      exports: ["useMapOnlySpatialMaterialize"]
    - path: "packages/web/src/hooks/useMapOnlySpatialMaterialize.spec.ts"
      provides: "Specs asserting sole-trigger preservation + map-only single fire"
      contains: "useMapOnlySpatialMaterialize"
    - path: "packages/web/src/components/DashboardsPage.tsx"
      provides: "Mount of the hook once in DashboardOpen"
      contains: "useMapOnlySpatialMaterialize"
  key_links:
    - from: "packages/web/src/components/DashboardsPage.tsx"
      to: "useMapOnlySpatialMaterialize"
      via: "hook call in DashboardOpen body with (dashboard.id, widgets)"
      pattern: "useMapOnlySpatialMaterialize\\("
    - from: "packages/web/src/hooks/useMapOnlySpatialMaterialize.ts"
      to: "/api/filter/materialize"
      via: "materializeFilter call gated on map-only tables"
      pattern: "materializeFilter\\("
    - from: "packages/web/src/hooks/useMapOnlySpatialMaterialize.ts"
      to: "aggregateSpatialTargetsByTable"
      via: "per-table eligible-target resolution"
      pattern: "aggregateSpatialTargetsByTable\\("
---

<objective>
Close TRACKFIX-V19-09 (GAP-54-10, carried TD-V15-MAP-ONLY-TRIGGER): a table shown ONLY on a map
widget never materializes when a spatial shape is drawn, because the only materialize triggers are
AggregatedWidgetRenderer/RecordsTableRenderer Effect 1 firing for THEIR OWN tableId
(WidgetRenderer.tsx:401-463 / 1410+). MapChartRenderer is a pure consumer (0 materialize calls).

Purpose: a map-only track (or any map-only) table must filter when the operator draws a shape, WITHOUT
breaking the Phase 30 sole-trigger invariant for tables that DO have a chart/records widget.

Output: a dashboard-scope orchestrator hook `useMapOnlySpatialMaterialize` (mirrors
`useDynamicViewMaterializeChain`), mounted once in `DashboardOpen`, that fires materialize ONLY for
tables with an eligible spatial target + drawn shapes + NO trigger widget. Server, route, and
spatialWhereClause.ts are UNCHANGED (confirmed correct).
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@packages/web/src/hooks/useDynamicViewMaterializeChain.ts
@packages/web/src/hooks/useDynamicViewMaterializeChain.spec.ts
@packages/web/src/lib/spatialTargets.ts
@packages/web/src/store/filterViewStore.ts
@packages/web/src/store/spatialFilterStore.ts
@packages/web/src/store/filterStore.ts

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — use directly, no exploration. -->

From src/lib/spatialTargets.ts:
```typescript
export type SpatialTarget = {
  tableId: number;
  spatialMode: "latlon" | "wkt" | "wkb";
  lonCol?: string; latCol?: string; spatialCol?: string;
};
// First-eligible-target-per-table; map widgets only; WKB/incomplete dropped.
export function aggregateSpatialTargetsByTable(widgets: WidgetDto[]): Map<number, SpatialTarget>;
export function isSpatialTargetEligible(target: SpatialTarget): boolean;
```

From src/store/spatialFilterStore.ts:
```typescript
export type Shape = { id: string; type: ...; wkt: string; label?: string; measurement?: ...; addedAt: number };
// useSpatialFilterStore.getState().shapes : Shape[]
// useSpatialFilterStore((s) => s.spatialFilterVersion) : number  // bumps on every shape mutation; 0 after reset()
```

From src/store/filterStore.ts:
```typescript
export type ActiveFilter = { column: string; value: ...; dataType: ...; ... };
// useFilterStore.getState().filters : Record<number /*tableId*/, ActiveFilter[]>
```

From src/store/filterViewStore.ts:
```typescript
// markMaterializing(tableId, dashboardId) → await materializeFilter(...) → setView(tableId, result, dashboardId)
// clearView(tableId); clearMaterializing(tableId)  // lift suspend gate on error
```

From src/api/client.ts:
```typescript
export type MaterializeFilterArgs = {
  dashboardId: number; tableId: number; filters: ActiveFilter[];
  spatialFilters?: { id: string; wkt: string }[];  // both spatial fields present together or both omitted
  spatialTarget?: SpatialTarget;
};
export const materializeFilter: (args, signal?) => Promise<{ viewName; expiresAt }>;
// In-flight dedup cache keyed `${dashboardId}:${tableId}` — concurrent calls JOIN the in-flight
// promise (does NOT honor a second caller's signal). This is the final double-DDL backstop.
export const dropFilterView: (args, signal?) => Promise<{ dropped: true }>;
```

WidgetRenderer.tsx dispatch (lines 240-272) — the TRIGGER-WIDGET truth source:
```
map | info-card | legend | datafilter | timeline | numericline  → pure consumers, NO Effect-1 materialize
records                                                          → RecordsTableRenderer  (HAS Effect-1 trigger for cfg.tableId)
bar | line | pie | scatter | (default else branch)              → AggregatedWidgetRenderer (HAS Effect-1 trigger for cfg.tableId)
```
Both trigger renderers read `widget.config.tableId`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useMapOnlySpatialMaterialize hook + specs (sole-trigger-preserving)</name>
  <files>packages/web/src/hooks/useMapOnlySpatialMaterialize.ts, packages/web/src/hooks/useMapOnlySpatialMaterialize.spec.ts</files>
  <behavior>
    Crux predicate — a table T needs the MAP-SIDE trigger iff ALL hold:
      (a) aggregateSpatialTargetsByTable(widgets).has(T)  — eligible spatial target exists
      (b) drawn shapes exist (useSpatialFilterStore.getState().shapes.length > 0)
      (c) NO trigger widget on T — i.e. no widget where widget.config.tableId === T AND
          widget.type is a TRIGGER type. TRIGGER types = "records" PLUS every type that falls to
          AggregatedWidgetRenderer (bar, line, pie, scatter, and anything NOT in the pure-consumer
          set). Implement (c) as: NON-trigger types = {"map","info-card","legend","datafilter",
          "timeline","numericline"}; a widget triggers T iff its config.tableId === T AND its type is
          NOT in that non-trigger set. (Allow-list the consumers, treat everything else as a trigger —
          matches WidgetRenderer's `else → AggregatedWidgetRenderer` default so future chart types are
          covered automatically.)

    Specs (mirror useDynamicViewMaterializeChain.spec.ts harness: renderHook + vi.mock("../api/client")
    with materializeFilter/dropFilterView as vi.fn, real Zustand stores via the reset shim, fake timers
    for the 300ms debounce):
      - Test 1 (MAP-ONLY FIRES ONCE): widgets = [map widget on T with eligible latlon target]; draw a
        shape (addShape → spatialFilterVersion bump). Assert materializeFilter called exactly ONCE with
        { dashboardId, tableId:T, filters:[], spatialFilters:[{id,wkt}], spatialTarget }. setView called.
      - Test 2 (SOLE-TRIGGER PRESERVED — no double-DDL): widgets = [map on T with target, bar/aggregated
        on T]. Draw a shape. Assert the HOOK's materializeFilter is NOT called for T (WidgetRenderer owns
        T). Use a per-call assertion that no call with tableId===T originated from the hook (since the
        hook is the only caller in this test harness, assert materializeFilter NOT called at all, or not
        called for T).
      - Test 3 (records also counts as trigger): widgets = [map on T with target, records on T] →
        hook does NOT fire for T.
      - Test 4 (no shapes → no fire): map-only T with target but zero shapes → no materialize; if a stale
        view exists for T, drop it (clearView + dropFilterView) — mirror the WidgetRenderer drop-when-
        nothing guard.
      - Test 5 (no eligible target → no fire): map on T but target ineligible (wkb / missing cols) → no
        materialize for T.
      - Test 6 (column filters composed): map-only T with target + useFilterStore.filters[T] populated +
        a shape → materializeFilter args.filters === filters[T] (column + spatial composed).
      - Test 7 (per-table AbortController dedup): rapid spatialFilterVersion bumps abort the prior
        in-flight materialize for the SAME table (assert prior controller.signal.aborted).
      - Test 8 (clear shapes drops view): after Test-1 state, clearAllShapes → hook drops T's view
        (dropFilterView + clearView).
      - Test 9 (multi map-only tables): two map-only tables T1,T2 each with targets + shapes →
        materialize fires for BOTH, once each.
  </behavior>
  <action>
Create `packages/web/src/hooks/useMapOnlySpatialMaterialize.ts` exporting
`useMapOnlySpatialMaterialize(dashboardId: number, widgets: WidgetDto[]): void`.

Structure (mirror useDynamicViewMaterializeChain — dashboard-scope, per-table AbortController Map):

1. Define `const NON_TRIGGER_TYPES = new Set(["map","info-card","legend","datafilter","timeline","numericline"]);`
   (module-level const). Comment: keep in sync with WidgetRenderer.tsx:240-272 dispatch — the allow-list
   is the pure-consumer set; everything else (records + AggregatedWidgetRenderer default branch) is a
   trigger that already materializes its own tableId via Effect 1.

2. `const targetsByTable = useMemo(() => aggregateSpatialTargetsByTable(widgets), [widgets]);`

3. `const triggeredTableIds = useMemo(...)`: Set<number> of tableIds that have a trigger widget —
   for each widget where typeof config.tableId === "number" AND !NON_TRIGGER_TYPES.has(widget.type),
   add config.tableId. (Read tableId via `(w.config as { tableId?: number }).tableId`.)

4. `const mapOnlyTargets = useMemo(...)`: array of [tableId, SpatialTarget] from targetsByTable where
   !triggeredTableIds.has(tableId). This is the set the hook owns. Deps [targetsByTable, triggeredTableIds].

5. Subscribe to `spatialFilterVersion` primitive: `const spatialFilterVersion = useSpatialFilterStore((s) => s.spatialFilterVersion);`
   (NOT the shapes array — primitive dep per PITFALL S-02, mirrors WidgetRenderer.tsx:385).

6. `const controllersRef = useRef<Map<number, AbortController>>(new Map());`

7. Materialize effect — deps `[dashboardId, spatialFilterVersion, mapOnlyTargets]`
   (eslint-disable-next-line react-hooks/exhaustive-deps; mapOnlyTargets is the memoized identity that
   changes only on widgets change). Debounce 300ms via setTimeout (mirror WidgetRenderer.tsx:403-457),
   cleanup clears the timer. Inside the debounce callback:
     - Read `const shapes = useSpatialFilterStore.getState().shapes;` imperatively (one-shot).
     - Build `const activeTableIds = new Set(mapOnlyTargets.map(([tid]) => tid));`
     - For each `[tableId, target]` in mapOnlyTargets:
         - `const tableFilters = useFilterStore.getState().filters[tableId] ?? [];`
         - `const hasShapes = shapes.length > 0;`
         - DROP guard (mirror WidgetRenderer.tsx:416-424): if `tableFilters.length === 0 && !hasShapes`
           → `dropFilterView({ dashboardId, tableId }).catch(() => {});`
           `useFilterViewStore.getState().clearView(tableId);` then `continue;`
         - Abort prior in-flight for THIS table only:
           `controllersRef.current.get(tableId)?.abort();`
           `const controller = new AbortController(); controllersRef.current.set(tableId, controller);`
         - `useFilterViewStore.getState().markMaterializing(tableId, dashboardId);`
         - Build args (pair-completeness — both spatial fields together or both omitted, byte-parity
           with WidgetRenderer.tsx:434-442):
           `const args: MaterializeFilterArgs = hasShapes
              ? { dashboardId, tableId, filters: tableFilters, spatialFilters: shapes.map(s => ({ id: s.id, wkt: s.wkt })), spatialTarget: target }
              : { dashboardId, tableId, filters: tableFilters };`
         - `try { const result = await materializeFilter(args, controller.signal);
              useFilterViewStore.getState().setView(tableId, result, dashboardId); }
            catch (err) { if ((err as Error)?.name === "AbortError") return;
              useFilterViewStore.getState().clearMaterializing(tableId);
              useToastStore.getState().showToast((err as Error).message, "error"); }`
           (mirror WidgetRenderer.tsx:446-456 error handling exactly).
     - Note: the materializeFilter in-flight cache (keyed `${dashboardId}:${tableId}`) is the final
       double-DDL backstop — even if a race let WidgetRenderer fire T concurrently, the second caller
       JOINS the in-flight promise. Document this in a comment.
     - PITFALL 2 cleanup (mirror dynamic-view chain): after the loop, prune controllers for tables no
       longer in `activeTableIds` — `for (const [tid, ctrl] of controllersRef.current.entries()) { if (!activeTableIds.has(tid)) { ctrl.abort(); controllersRef.current.delete(tid); } }`.

8. Unmount cleanup effect (deps []): abort all controllers + clear the Map (mirror dynamic-view chain
   lines 257-263). Dashboard-switch is handled because the hook is keyed to DashboardOpen's lifecycle
   (DashboardOpen already resets useSpatialFilterStore + useFilterViewStore on unmount at
   DashboardsPage.tsx:414-451) and dashboardId in deps re-runs the effect on switch.

Use `import type { WidgetDto, MaterializeFilterArgs } from "../api/client";`,
`import { materializeFilter, dropFilterView } from "../api/client";`,
`import { aggregateSpatialTargetsByTable } from "../lib/spatialTargets";`,
plus the three stores + useToastStore. Keep WORK frontend-only — touch NO packages/server file.

Write the spec file per the <behavior> tests, mirroring the spec harness in
useDynamicViewMaterializeChain.spec.ts (vi.mock client, real stores, fake timers; use
`act` + advance timers past 300ms; `await` microtasks). Mock map widgets as
`{ id, type:"map", config:{ spatialTargets:[{ tableId, spatialMode:"latlon", lonCol, latCol }] } }`
and trigger widgets as `{ id, type:"bar"|"records", config:{ tableId } }`.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest --run src/hooks/useMapOnlySpatialMaterialize.spec.ts</automated>
  </verify>
  <done>
    Hook exists and exports useMapOnlySpatialMaterialize; all 9 specs pass. Test 2 + Test 3 prove the
    sole-trigger invariant (no map-side fire for tables with a chart/records widget); Test 1 + Test 9
    prove map-only tables fire exactly once per table.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mount the hook in DashboardOpen + verify gates + assert no server diff</name>
  <files>packages/web/src/components/DashboardsPage.tsx</files>
  <action>
In `DashboardsPage.tsx` `DashboardOpen` body, mount the hook ONCE alongside the existing
`useDynamicViewMaterializeChain(dashboard.id)` call (around line 395). Add the import at the top
(near line 27): `import { useMapOnlySpatialMaterialize } from "../hooks/useMapOnlySpatialMaterialize";`.

Mount AFTER `widgets` state is available (widgets is declared at line 378 via useState and synced at
405; the hook call must be below the `const [widgets, setWidgets]` declaration). Call:
`useMapOnlySpatialMaterialize(dashboard.id, widgets);`
Add a brief comment: "TRACKFIX-V19-09 (GAP-54-10): dashboard-scope map-only spatial materialize.
Fires materialize for tables shown ONLY on a map (no chart/records trigger widget). Mirrors
useDynamicViewMaterializeChain; preserves the Phase 30 sole-trigger invariant — tables with a
chart/records widget are skipped here (WidgetRenderer Effect 1 owns them)."

Do NOT add any materialize call to MapChartRenderer (it stays a pure consumer). Do NOT edit ROADMAP.md.
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest --run && git diff --name-only packages/server | grep -q . && echo "SERVER-DIFF-FAIL" || echo "SERVER-CLEAN"</automated>
  </verify>
  <done>
    Hook is mounted once in DashboardOpen with (dashboard.id, widgets); `tsc --noEmit` clean; full web
    vitest suite green at baseline (~1656/1656); the server-diff guard prints "SERVER-CLEAN" (no
    packages/server files modified).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator live re-walk — map-only spatial filter fires + sole-trigger holds</name>
  <action>
    Operator-driven live verification (no code change). Confirm a map-only track table materializes on
    draw and that the Phase 30 sole-trigger invariant still holds for tables that have a chart/records
    widget. Follow the how-to-verify steps below.
  </action>
  <what-built>
    A dashboard-scope hook (`useMapOnlySpatialMaterialize`) mounted in DashboardOpen that fires
    POST /api/filter/materialize for any table shown ONLY on a map widget (an eligible spatial target
    + drawn shapes + no chart/records widget), deduped against the in-flight materialize cache so it
    never double-fires with the existing WidgetRenderer trigger path. Server unchanged.
  </what-built>
  <how-to-verify>
    1. Open a dashboard whose track table T is shown ONLY on a map widget (no bar/line/pie/scatter/records
       widget bound to T) and that has an eligible spatial target configured for T (latlon X/Y or WKT).
    2. Open DevTools → Network, filter to "materialize".
    3. Draw a spatial shape (polygon/box) on the map.
    4. EXPECT: exactly ONE `POST /api/filter/materialize` request fires for table T (status 200), and the
       track tiles re-render narrowed to the drawn shape.
    5. Clear the shape → EXPECT a DELETE /api/filter/materialize (drop) and tiles return to full extent.
    6. Regression — on a dashboard where table T ALSO has an aggregated chart or records table: draw a
       shape and confirm still exactly ONE materialize POST for T (NOT two) — the sole-trigger invariant
       holds (the chart/records widget triggers; the map-side hook skips T).
  </how-to-verify>
  <verify>
    <automated>MISSING — operator-only live verification; covered by Task 1 specs (automated) + Task 2 gates</automated>
  </verify>
  <done>Operator confirms one materialize POST on draw of a map-only table + tiles narrow to the shape; no double-fire on tables with a chart/records widget.</done>
  <resume-signal>Type "approved" or describe what you observed (e.g. request count, tile behavior).</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean in packages/web.
- Full web vitest suite green at baseline (~1656/1656); the 9 new specs included.
- `git diff --name-only packages/server` is EMPTY (server + spatialWhereClause.ts + route untouched).
- Sole-trigger invariant: hook fires ONLY for map-only tables; tables with a chart/records widget are
  skipped (Test 2/3 + operator regression step 6).
- Map-only table fires exactly once on draw (Test 1/9 + operator step 4).
</verification>

<success_criteria>
- A spatial shape drawn on a map-only table materializes + filters that table (TRACKFIX-V19-09 closed).
- No double-DDL for tables that have a chart/records widget (Phase 30 sole-trigger invariant preserved).
- Frontend-only change; server confirmed unchanged.
- Operator live re-walk confirms one materialize POST on draw + tiles narrow to the shape.
</success_criteria>

<output>
After completion, create `.planning/phases/54-verification-live-walk-through/54-10-SUMMARY.md`
</output>
