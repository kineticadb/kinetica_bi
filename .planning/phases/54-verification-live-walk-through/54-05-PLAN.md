---
phase: 54-verification-live-walk-through
plan: 05
type: execute
wave: 2
depends_on: ["54-04"]
files_modified:
  - packages/web/src/components/charts/KineticaWmsLayerForm.tsx
  - packages/web/src/components/charts/KineticaWmsLayerForm.spec.tsx
autonomous: false
gap_closure: true
requirements: [TRACKFIX-V19-02, TRACKFIX-V19-03]

must_haves:
  truths:
    - "The TRACK STYLE section surfaces an explicitly-labelled track line color control (color swatch + AARRGGBB hex + alpha) and a track line width control"
    - "The line color control persists into track_config and emits via the existing TRACKLINECOLORS param"
    - "The line width control persists into track_config and emits via the existing TRACKLINEWIDTHS param"
    - "Operator re-walk of UAT §2 (Track+Raster) and §3 (Track+CB form surface) passes — tracks render single-color; §3 form shows CB builder + TRACK STYLE incl. line controls; per-break categorical track coloring remains deferred (TRACK-V20-03)"
  artifacts:
    - path: "packages/web/src/components/charts/KineticaWmsLayerForm.tsx"
      provides: "Track line color + track line width controls in the TRACK STYLE section, wired through onSetTrackField"
      contains: "Track line"
    - path: "packages/web/src/components/charts/KineticaWmsLayerForm.spec.tsx"
      provides: "Tests asserting the line color/width controls write trailColor/trailSize (→ TRACKLINECOLORS/TRACKLINEWIDTHS) into track_config"
  key_links:
    - from: "Track line color control"
      to: "track_config.trailColor → TRACKLINECOLORS (wmsUrlBuilder ~449)"
      via: "onSetTrackField('trailColor', ...) AARRGGBB idiom"
      pattern: "trailColor"
    - from: "Track line width control"
      to: "track_config.trailSize → TRACKLINEWIDTHS (wmsUrlBuilder ~455-457)"
      via: "onSetTrackField('trailSize', ...)"
      pattern: "trailSize"
---

<objective>
Close GAP-54-03 (TRACKFIX-V19-02): the operator reported the TRACK STYLE section has no controls for TRACKLINECOLOR (connecting-line color) or TRACKLINEWIDTH (line width).

**Clarification — trail == line; there is NO distinct Kinetica param.** Confirmed in wmsUrlBuilder.ts:
  - `track_config.trailColor` is emitted as **TRACKLINECOLORS** (line 449).
  - `track_config.trailSize` (or `lineWidth`) is emitted as **TRACKLINEWIDTHS** (line 455-457).
So the form's existing "Trail color" + "Line width" controls (KineticaWmsLayerForm.tsx ~850-922) ALREADY drive the TRACKLINECOLORS / TRACKLINEWIDTHS params the operator named. The emission path is complete and the §2.2 UAT expectation already lists "trail color, line width". GAP-54-03 is therefore a NAMING / DISCOVERABILITY gap, not a missing emission: the operator looked for "track line color / track line width" and saw "Trail color / Line width". The Phase 37 spike notes define a single connecting-line color+width (TRACKLINECOLORS/TRACKLINEWIDTHS) — there is no separate per-segment "trail vs line" pair.

**Decision (resolve in the plan):** Relabel and explicitly surface the existing controls as track-line styling so they match the operator's mental model, while keeping the same `trailColor`/`trailSize` track_config fields and the same TRACKLINECOLORS/TRACKLINEWIDTHS emission (no schema or builder change). Ensure the line color control has the full color-swatch + AARRGGBB hex + alpha idiom (mirrors head/trail color), and the line width control is a 0-20 range. If the operator's intent at re-walk is genuinely two distinct params, that is a NEW Kinetica capability requiring a spike (out of scope here) — note it but do not invent params.

Then drive TRACKFIX-V19-03: an operator re-walk checkpoint for UAT §2 + §3 (single-color tracks render; CB form surface correct; per-break categorical coloring deferred to TRACK-V20-03).

Purpose: Give the operator clearly-labelled track-line styling controls and confirm the end-to-end track flow on the running app after the 54-04 render fix.
Output: Relabelled/confirmed line color + line width controls with tests, and a passed operator re-walk of §2/§3.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/54-verification-live-walk-through/54-UAT.md

@packages/web/src/components/charts/KineticaWmsLayerForm.tsx
@packages/web/src/lib/trackConfig.ts
@packages/web/src/lib/wmsUrlBuilder.ts

<interfaces>
<!-- Existing TRACK STYLE wiring the executor extends. Do NOT rediscover. -->

KineticaWmsLayerForm.tsx — existing helpers:
  onSetTrackField(key: keyof TrackConfig, v: string | number | undefined)  // ~345: merges {enabled:true,[key]:v} into track_config JSON, preserving other fields
  trackCfg = coalesceTrackConfig((config.track_config as string|null) ?? null)  // ~332
  Color idiom helpers already imported: rgbFromAARRGGBB, alphaFromAARRGGBB, joinAARRGGBB,
    normalizeAARRGGBB, alphaHexToPercent, alphaPercentToHex
  TRACK_DEFAULTS (lib/trackConfig.ts): { headColor:"FFFF0000", trailColor:"FF0000FF",
    headSize:8, trailSize:2, headShape:"circle" }

Existing controls to relabel/confirm (KineticaWmsLayerForm.tsx):
  - "Trail color" label + color-row + alpha range  (~850-906) → writes trailColor → TRACKLINECOLORS
  - "Line width" range 0-20                         (~908-922) → writes trailSize  → TRACKLINEWIDTHS

wmsUrlBuilder.ts emission (already correct — do NOT change):
  trailColor → TRACKLINECOLORS (line 449, expand() for cb_raster)
  trailSize ?? lineWidth → TRACKLINEWIDTHS (line 455-457)

TrackConfig (lib/trackConfig.ts): trailColor?: string (8-char AARRGGBB); trailSize?: number;
  lineWidth?: number (alias; trailSize takes precedence). No new fields required.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface explicit "Track line color" + "Track line width" controls in TRACK STYLE</name>
  <files>packages/web/src/components/charts/KineticaWmsLayerForm.tsx</files>
  <action>
    In the TRACK STYLE section (~752-923), relabel and confirm the connecting-line controls
    so they match the operator's TRACKLINECOLOR / TRACKLINEWIDTH mental model, WITHOUT
    changing the underlying track_config fields or the wmsUrlBuilder emission:

    1. Rename the "Trail color" label → "Track line color" (keep the existing color-swatch +
       AARRGGBB hex text + alpha range trio exactly as-is; it already writes trailColor via
       onSetTrackField and emits TRACKLINECOLORS). Update the aria-labels accordingly
       (e.g. "Track line color (RGB)", "Track line color (AARRGGBB hex)",
       "Track line color alpha"). Keep using trackCfg.trailColor / TRACK_DEFAULTS.trailColor.
    2. Rename the "Line width" label → "Track line width" and its aria-label "Track line width"
       (keep the 0-20 range writing trailSize via onSetTrackField → emits TRACKLINEWIDTHS).
    3. Do NOT add new TrackConfig fields, do NOT touch wmsUrlBuilder.ts, do NOT change the
       head color/size/shape controls. trail* fields ARE the Kinetica line params — keeping
       them avoids a schema migration and preserves the §5 color round-trip (already PASS).
    4. Add a short inline comment block citing GAP-54-03 + the trailColor==TRACKLINECOLORS /
       trailSize==TRACKLINEWIDTHS mapping so future readers don't re-introduce a duplicate
       "trail vs line" pair.

    Theme tokens: reuse existing CSS classes (config-color-field / config-color-row /
    config-color-picker / config-color-text / config-range-field / config-range /
    config-range-value) — no new colors or hardcoded values.
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit</automated>
  </verify>
  <done>TRACK STYLE shows "Track line color" (swatch + hex + alpha) and "Track line width" (0-20); both still write trailColor/trailSize into track_config; web tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests — line controls persist into track_config and emit the line params</name>
  <files>packages/web/src/components/charts/KineticaWmsLayerForm.spec.tsx</files>
  <behavior>
    - Rendering the TRACK STYLE section (spatialMode=track, renderMode=raster) shows controls
      labelled "Track line color" and "Track line width".
    - Changing the track line color fires onChange with a track_config JSON whose trailColor
      is the new AARRGGBB value (round-trips through the existing color idiom).
    - Changing the track line width fires onChange with a track_config JSON whose trailSize
      is the new numeric value.
    - End-to-end emission lock (unit-level): feeding the resulting track_config through
      buildWmsParams yields TRACKLINECOLORS = <trailColor> and TRACKLINEWIDTHS = <trailSize>
      under renderMode=raster (assert in wmsUrlBuilder.spec.ts if a form-level emission test
      is awkward — the existing Track-block describe already covers the mapping; extend only
      if a gap exists).
  </behavior>
  <action>
    Add focused tests to KineticaWmsLayerForm.spec.tsx for the relabelled controls (query by
    the new accessible labels). Follow the existing form-spec patterns (render with a track
    config prop, fireEvent/userEvent on the color + range inputs, assert the onChange payload's
    parsed track_config). Confirm the trailColor/trailSize fields are what get written — the
    emission to TRACKLINECOLORS/TRACKLINEWIDTHS is already locked by
    wmsUrlBuilder.spec.ts "Track block (SCHEMA-V17-04)".
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/KineticaWmsLayerForm.spec.tsx</automated>
  </verify>
  <done>New label-based tests pass; form spec green; track line color/width changes confirmed to write trailColor/trailSize.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator re-walk of UAT §2 (Track+Raster) + §3 (Track+CB form surface)</name>
  <action>
    Hand off to the operator for the live re-walk described in how-to-verify. This is operator-
    driven verification of TRACKFIX-V19-03 — it is NOT auto-verifiable. The fixes from 54-04
    (render) + this plan (line controls) must be running (npm run dev + npm run dev:server)
    before the operator begins.
  </action>
  <what-built>
    GAP-54-01 render fix (Plan 54-04) + GAP-54-03 track-line controls (this plan). A saved
    Track+Raster layer now constructs an ImageWMS source and fires WMS requests; the TRACK
    STYLE section exposes "Track line color" and "Track line width". Note: per-break
    categorical track coloring under Track+Class Break is DEFERRED (TRACK-V20-03) — Track+CB
    is expected to render single-color tracks with the CB form surface present.
  </what-built>
  <how-to-verify>
    Re-walk UAT §2 (Track+Raster) and §3 (Track+Class Break form surface) against the running
    app (npm run dev + npm run dev:server), reusing 54-UAT.md Section setup:

    §2 Track+Raster (was FAIL on render):
    1. Open the demo.track layer; confirm spatial mode Track + four pickers pre-filled (regression check).
    2. Select Raster. Confirm TRACK STYLE present with: head color, head size, head shape,
       Track line color (swatch + AARRGGBB hex + alpha), Track line width. RASTER PARAMS absent.
    3. Pick a distinct head color + alpha and a distinct Track line color + alpha; set a Track line width.
    4. Save. CONFIRM the map tiles now RENDER the track (a WMS request fires on load/zoom —
       previously zero). Tiles visibly reflect the chosen head + line colors incl. alpha.

    §3 Track+Class Break (form surface + single-color render):
    5. Switch render mode to Class Break. Confirm BOTH the CB break builder AND TRACK STYLE
       (incl. the new line controls) show simultaneously; per-break advanced [▸] chevrons absent.
    6. Configure ≥1 class break, keep track colors, save. CONFIRM tiles render the track as a
       SINGLE color (per-break categorical track coloring is deferred — TRACK-V20-03; this is
       the EXPECTED behavior, not a failure). CB params still emit.

    §6 no-regression spot check: a latlon layer still renders normally.
  </how-to-verify>
  <verify>Operator-driven re-walk — not auto-verifiable. Resume on operator attestation.</verify>
  <done>Operator attests §2 renders with colors reflected, §3 form is correct with single-color tracks rendering, and latlon no-regression holds; 54-UAT.md §2/§3 flipped to PASS.</done>
  <resume-signal>Type "approved" if §2 renders + colors reflect and §3 form is correct with single-color tracks; otherwise describe the failure (sections + observed vs expected) for a follow-up gap plan.</resume-signal>
</task>

</tasks>

<verification>
- Track line color + track line width controls present, labelled, and persisting into track_config (trailColor/trailSize).
- Emission unchanged and correct: trailColor→TRACKLINECOLORS, trailSize→TRACKLINEWIDTHS (locked by existing builder spec).
- Operator re-walk of §2 + §3 passes per the deferred-categorical expectation; latlon no-regression confirmed.
- web tsc clean; form + builder specs green.
</verification>

<success_criteria>
- TRACKFIX-V19-02 met: TRACK STYLE gains discoverable track line color (color+alpha idiom) + track line width controls, persisted to track_config and emitted via existing TRACKLINECOLORS/TRACKLINEWIDTHS.
- TRACKFIX-V19-03 met: §2 and §3 re-walk PASS (single-color tracks render; CB form correct; categorical CB track coloring deferred to TRACK-V20-03 and explicitly NOT expected).
- packages/web only; theme tokens; no server change; no new TrackConfig schema fields.
</success_criteria>

<output>
After completion, create `.planning/phases/54-verification-live-walk-through/54-05-SUMMARY.md`.
Record the trail==line clarification (no distinct Kinetica param), the relabel decision, and the operator re-walk verdict for §2/§3.
</output>
