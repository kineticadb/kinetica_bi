---
phase: 54-verification-live-walk-through
plan: 02
type: execute
wave: 2
depends_on: ["54-01"]
files_modified:
  - .planning/phases/54-verification-live-walk-through/54-UAT.md
autonomous: false
requirements: [VERIFY-V19-01]
user_setup:
  - service: kinetica
    why: "Live track-rendering walk-through needs a real Kinetica track table + a dashboard with a map widget"
    dashboard_config:
      - task: "Ensure the demo.track table is registered with columns X (DOUBLE), Y (DOUBLE), TRACKID (VARCHAR), TIMESTAMP (BIGINT)"
        location: "Kinetica (table catalog / DDL)"
      - task: "Have a dashboard with a map widget available to add/open a layer on demo.track"
        location: "Kinetica BI app (as admin/designer)"
must_haves:
  truths:
    - "54-UAT.md exists as a self-contained track-rendering walk-through checklist the operator can execute against the running app"
    - "The checklist covers Track auto-suggest + the DOUBLE x/y picker visibility (the 5e3514b fix — X and Y explicitly checked) + TRACKID/TIMESTAMP defaults"
    - "The checklist covers Track+Raster (Raster+Class Break only, no heatmap; TRACK STYLE present, RASTER PARAMS absent; head/trail color pickers with alpha; tiles render)"
    - "The checklist covers Track+Class Break (CB builder + TRACK STYLE both show; per-break advanced hidden; tiles render)"
    - "The checklist covers silent heatmap→raster coercion, color round-trip persistence, and a latlon/WKT no-regression check"
    - "Each check item carries a PASS/FAIL/SKIPPED status field + evidence field, plus a gaps block"
    - "After presenting the checklist, the operator's per-section attestations are recorded back into 54-UAT.md"
  artifacts:
    - path: ".planning/phases/54-verification-live-walk-through/54-UAT.md"
      provides: "The live guided track-rendering UAT checklist + recorded operator attestations + gap table"
      contains: "Preconditions"
  key_links:
    - from: "54-UAT.md §2/§3 save steps"
      to: "WMS tiles rendered on the map with the chosen track colors"
      via: "explicit save → map-render attestation"
      pattern: "tiles render|tiles reflect"
---

<objective>
Author the live, guided track-rendering UAT checklist (`54-UAT.md`), then run the human checkpoint: present it to the operator, collect per-section attestation, and record results. The checklist walks one coherent end-to-end script — configure a track layer from scratch via the new Phase 52/53 flow (auto-suggest, defaults incl. the now-visible DOUBLE x/y columns, both render modes, param surfaces, alpha color pickers, coercion, color round-trip) plus a latlon/WKT no-regression pass.

Purpose: VERIFY-V19-01's core clause — "Live operator walk-through — configure a track layer end-to-end via the new flow." This is the only part of v1.9 verification that cannot be proven by spec; the automated gates (54-01) cover the rest.
Output: `54-UAT.md` with the full checklist AND the operator's recorded attestations + any gap rows.

NOTE: This plan is NOT autonomous. Task 2 is a blocking human checkpoint. Any FAIL halts for a 54.x inline fix + re-walk before 54-03 compiles.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/53-render-narrowing-param-surfaces-color-cutover/53-CONTEXT.md
@.planning/phases/54-verification-live-walk-through/54-01-AUTOMATED-GATES.md

<read_first>
- `.planning/phases/52-track-spatial-mode-foundation/52-VERIFICATION.md` — what 52 shipped (the four typed x/y/trackID/ordering pickers, auto-suggest via isTrackTable, defaults seeding) + the `## Follow-up` 5e3514b note: DOUBLE columns were invisible in numeric pickers until that fix; X and Y must now appear.
- `.planning/phases/53-render-narrowing-param-surfaces-color-cutover/53-VERIFICATION.md` — render narrowing (heatmap absent), TRACK STYLE section vs RASTER PARAMS gating, silent heatmap→raster coercion (no toast), CbConfigForm trackContext hides per-break advanced chevron, the AARRGGBB color-input+alpha idiom, WMS emission byte-locks.
- `.planning/phases/24-verification/24-01-UAT-NOTES.md` — the UAT precedent format (per-item name/status/evidence + a gaps block). Mirror this structure, extended into the track-rendering sections below.
</read_first>

<interfaces>
Run the app for UAT: `npm run dev` (web) + `npm run dev:server` (server) from repo root.

The demo.track table shape (operator-provisioned): X DOUBLE, Y DOUBLE, TRACKID VARCHAR, TIMESTAMP BIGINT. Track auto-suggest fires on the TRACKID + x + y + TIMESTAMP shape (case-insensitive). Defaults at suggestion time: trackID → TRACKID, ordering → TIMESTAMP; x/y prefilled from the isTrackTable match.

Expected behaviors to attest against (from Phase 52/53 VERIFICATION + ROADMAP SC2-4):
- Track is offered as a spatial mode; selecting it reveals four typed pickers (x, y, track ID, ordering). x/y pickers list DOUBLE columns (the 5e3514b fix) — X and Y must both be selectable.
- Render-mode picker under Track offers exactly Raster + Class Break — Heatmap is ABSENT from the list (not greyed — absent).
- Track + Raster: TRACK STYLE section present (head color/size/shape, trail color/line width); RASTER PARAMS group (pointColor/pointSize/pointShape/shapeline/shapefill) ABSENT.
- Track + Class Break: CB break builder AND TRACK STYLE both present; per-break advanced [▸] chevron panels absent.
- Head/trail colors are proper color controls: native color swatch + AARRGGBB hex + an alpha control. 8-char AARRGGBB persists.
- A layer whose persisted renderMode is heatmap, switched to Track, silently shows Raster — no toast, no confirm dialog, no error.
- Saving emits WMS params so the map tiles render the track with the chosen head/trail colors (incl. alpha).
- Reopening a saved track layer shows the previously chosen head/trail colors + alpha persisted.
- Existing latlon/WKT layers configure + render exactly as before (no regression).

ROADMAP Phase 54 Success Criteria (the must-cover map):
- SC2: end-to-end track config — auto-suggest, defaults incl. demo.track DOUBLE x/y visible, both render modes → §1 + §2 + §3
- SC3: color picker exercised with alpha; saved tiles reflect chosen colors → §2 + §5
- SC4: Track+Raster TRACK STYLE only; Track+CB CB builder + track params, per-break advanced hidden; heatmap not offered → §2 + §3
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 54-UAT.md — the full track-rendering live checklist</name>
  <files>.planning/phases/54-verification-live-walk-through/54-UAT.md</files>
  <action>
Write `.planning/phases/54-verification-live-walk-through/54-UAT.md` mirroring the 24-01-UAT-NOTES.md precedent (frontmatter: `plan: 54-02`, `operator: RPereira@kinetica.com`, `started_on:` left blank for the operator). Each checklist item gets a `status: PENDING` field (operator/Task-2 flips to PASS/FAIL/SKIPPED) and an `evidence:` field. Enumerate the FULL content below verbatim — do not leave scope to be invented at checkpoint time.

## Section 0 — Preconditions (operator confirms BEFORE the walk)
- [ ] App running: `npm run dev` + `npm run dev:server` from the repo root
- [ ] `demo.track` table registered with columns X (DOUBLE), Y (DOUBLE), TRACKID (VARCHAR), TIMESTAMP (BIGINT)
- [ ] A dashboard with a map widget exists (to add/open a layer on demo.track)
- [ ] All 54-01 automated gates recorded PASS (frontend 100% green, tsc clean, server set-gate, track-spec group, builds)

## Section 1 — Spatial mode + auto-suggest + column pickers  [ROADMAP SC2]
- 1.1 Add a new map layer (or open the layer config) on the `demo.track` table. Confirm the spatial mode is auto-suggested as **Track** (TRACKID+x+y+TIMESTAMP shape match).
- 1.2 Confirm the four typed column pickers appear: **x**, **y**, **track ID**, **ordering**.
- 1.3 DOUBLE-PRECISION CHECK (the 5e3514b fix): open the **x** picker and confirm the DOUBLE column **X** is listed and selectable; open the **y** picker and confirm the DOUBLE column **Y** is listed and selectable. (Before the fix, DOUBLE columns were invisible and these pickers showed only int columns — both X and Y MUST appear now.)
- 1.4 Confirm defaults are pre-filled: **track ID** defaults to `TRACKID`, **ordering** defaults to `TIMESTAMP` (case-insensitive match), and x/y are pre-filled from the track-shape match.
- 1.5 Confirm you can freely switch the spatial mode to another value (e.g. lat/lon) and back to Track (no lock-in). Return it to Track for the rest of the walk.

## Section 2 — Track + Raster  [ROADMAP SC2 + SC3 + SC4]
- 2.1 With Track selected, open the render-mode picker. Confirm it offers ONLY **Raster** and **Class Break** — **Heatmap is ABSENT from the list** (not greyed out — absent). Select **Raster**.
- 2.2 Confirm a **TRACK STYLE** section is present (head color, head size, head shape, trail color, line width) and that the **RASTER PARAMS** group (pointColor / pointSize / pointShape / shapeline / shapefill) is **ABSENT**.
- 2.3 COLOR PICKER (alpha): on the **head color** control, confirm it is a proper color picker — native color swatch + AARRGGBB hex field + an alpha control. Pick a distinct head color and adjust its alpha. Repeat on the **trail color** control with a different color + alpha.
- 2.4 Save the layer. Confirm the map tiles **render the track** with the chosen head/trail colors (the chosen colors are visibly reflected on the rendered tiles).

## Section 3 — Track + Class Break  [ROADMAP SC4]
- 3.1 Switch the render mode to **Class Break**. Confirm BOTH the **CB break builder** AND the **TRACK STYLE** section are shown.
- 3.2 Confirm the per-break **advanced [▸] chevron** panels are hidden in track context (no per-break point/shape advanced fields).
- 3.3 Configure at least one class break, keep the track colors from §2, and **save**. Confirm the map tiles render the track under Class Break.

## Section 4 — Silent heatmap→raster coercion  [ROADMAP SC4 / RENDER-V19-01]
- 4.1 Take (or simulate) a layer whose persisted renderMode is **heatmap**, then set its spatial mode to **Track**. Confirm the render mode silently shows **Raster** — **no error, no toast, no confirm dialog**. (If no stale-heatmap layer is available, mark SKIPPED with a note — the automated coercion spec in 54-01 covers it; this is the live confirmation only.)

## Section 5 — Color round-trip persistence  [ROADMAP SC3 / COLOR-V19-01]
- 5.1 Close and **reopen** the track layer saved in §2/§3. Confirm the previously chosen **head color + alpha** and **trail color + alpha** are persisted and shown in the color controls (8-char AARRGGBB round-trips).

## Section 6 — No-regression: normal latlon / WKT layer  [guard]
- 6.1 Add/open a NORMAL **latlon** (or WKT) map layer on a non-track table. Confirm it configures exactly as before: the render-mode picker still offers **Heatmap** (latlon is not narrowed), the RASTER PARAMS group shows under Raster, and there is NO TRACK STYLE section.
- 6.2 Save and confirm the latlon/WKT layer renders unchanged.

## Section 7 — Gaps block
Add an empty `gaps:` YAML block (mirror 24-01 format: id, severity, title, description, resolution) for any defect found during the walk → routed to a 54.x inline plan-revision.

Add a short "## Traceability" footer mapping ROADMAP SC2→§1+§2+§3, SC3→§2+§5, SC4→§2+§3+§4. Every check item carries `status: PENDING`.
  </action>
  <verify>
    <automated>test -f /Users/rydelpereira/Documents/projects/kinetica_bi/.planning/phases/54-verification-live-walk-through/54-UAT.md && grep -qi "Preconditions" /Users/rydelpereira/Documents/projects/kinetica_bi/.planning/phases/54-verification-live-walk-through/54-UAT.md && grep -qi "DOUBLE" /Users/rydelpereira/Documents/projects/kinetica_bi/.planning/phases/54-verification-live-walk-through/54-UAT.md && grep -c "status:" /Users/rydelpereira/Documents/projects/kinetica_bi/.planning/phases/54-verification-live-walk-through/54-UAT.md</automated>
  </verify>
  <done>54-UAT.md exists with Sections 0-7: preconditions, spatial-mode+auto-suggest with the explicit DOUBLE X/Y picker check, Track+Raster (heatmap-absent + TRACK STYLE present + RASTER PARAMS absent + alpha color pickers + tiles render), Track+Class Break (CB builder + TRACK STYLE + per-break advanced hidden + tiles render), silent heatmap→raster coercion, color round-trip, latlon/WKT no-regression, gaps block, and SC2-SC4 traceability. Every check item carries `status: PENDING`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live track-rendering walk-through checkpoint — collect attestations</name>
  <files>.planning/phases/54-verification-live-walk-through/54-UAT.md</files>
  <action>BLOCKING HUMAN CHECKPOINT. Present the 54-UAT.md checklist to the operator, have them walk all sections live against the running app, then record each per-item attestation back into 54-UAT.md (flip status PENDING→PASS/FAIL/SKIPPED, fill evidence, populate the gaps block). Do not auto-attest — wait for the operator. See what-built / how-to-verify / resume-signal below.</action>
  <what-built>The complete 54-UAT.md live checklist (Sections 0-7) for end-to-end track-layer configuration via the new Phase 52/53 flow: Track auto-suggest + the DOUBLE x/y picker fix, TRACKID/TIMESTAMP defaults, render-mode narrowing (Raster+Class Break, no heatmap), TRACK STYLE vs RASTER PARAMS surfaces, alpha color pickers, silent heatmap→raster coercion, color round-trip persistence, and a latlon/WKT no-regression pass. All automated gates passed in 54-01.</what-built>
  <how-to-verify>
1. Confirm the Section 0 preconditions: app running (`npm run dev` + `npm run dev:server`), `demo.track` registered with X/Y DOUBLE + TRACKID VARCHAR + TIMESTAMP BIGINT, a dashboard with a map widget available.
2. Walk the checklist top to bottom, section by section.
3. §1: add/open a layer on demo.track → confirm Track auto-suggested; OPEN THE x AND y PICKERS and confirm the DOUBLE columns X and Y both appear (the 5e3514b fix); confirm trackID=TRACKID, ordering=TIMESTAMP defaults.
4. §2 (Track+Raster): confirm render picker = Raster + Class Break only (no Heatmap); TRACK STYLE present, RASTER PARAMS absent; exercise the head + trail color pickers WITH ALPHA; save → confirm tiles render the track in your chosen colors.
5. §3 (Track+Class Break): switch mode → confirm CB builder + TRACK STYLE both show, per-break advanced chevron hidden; save → tiles render.
6. §4: a stale-heatmap layer flipped to Track silently shows Raster (no error/toast) — or mark SKIPPED if no such layer.
7. §5: reopen the saved layer → chosen head/trail colors + alpha persisted.
8. §6: a normal latlon/WKT layer still configures (heatmap offered, RASTER PARAMS shown, no TRACK STYLE) + renders unchanged.
9. For each item report PASS or FAIL (with what you saw). For any failure, describe it so it can be logged as a gap → 54.x inline fix + re-walk.
  </how-to-verify>
  <resume-signal>Provide per-section attestations (e.g. "§1-§3 all PASS incl. X/Y DOUBLE visible and tiles rendered; §4 PASS; §5 colors persisted; §6 latlon unchanged") or describe any failures. Then this task records them into 54-UAT.md (flip status fields, fill evidence, populate gaps block). If any FAIL, halt and report so the orchestrator spins a 54.x revision before 54-03 compiles.</resume-signal>
  <verify>
    <automated>grep -qE "status: (PASS|FAIL|SKIPPED)" /Users/rydelpereira/Documents/projects/kinetica_bi/.planning/phases/54-verification-live-walk-through/54-UAT.md && ! grep -q "status: PENDING" /Users/rydelpereira/Documents/projects/kinetica_bi/.planning/phases/54-verification-live-walk-through/54-UAT.md && echo OK</automated>
  </verify>
  <done>Every 54-UAT.md item has a resolved status (no PENDING remaining); operator attestations and any gaps recorded. Any FAIL halts for a 54.x revision before 54-03.</done>
</task>

</tasks>

<verification>
- 54-UAT.md exists with all sections and PENDING→PASS/FAIL/SKIPPED status fields populated after the checkpoint.
- The explicit DOUBLE X/Y picker-visibility check, the heatmap-absent check, TRACK STYLE vs RASTER PARAMS, the alpha color pickers, the color round-trip, and the latlon/WKT no-regression check are all present in the checklist text.
- The save→tiles-render attestation is present for both Track+Raster and Track+Class Break.
- Operator attestations and any gaps are recorded back into 54-UAT.md.
</verification>

<success_criteria>
- The operator has configured a track layer end-to-end via the new flow live against the running app, across both render modes.
- The DOUBLE x/y picker visibility, alpha color pickers, and tile rendering with chosen colors are confirmed.
- latlon/WKT no-regression confirmed.
- All attestations recorded; any defect captured as a gap routed to a 54.x revision (re-walked before close).
</success_criteria>

<output>
After the checkpoint resolves, create `.planning/phases/54-verification-live-walk-through/54-02-SUMMARY.md` summarizing the attestation outcome and listing any gaps raised.
</output>
