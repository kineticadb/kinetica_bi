---
phase: 54-verification-live-walk-through
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/54-verification-live-walk-through/54-01-AUTOMATED-GATES.md
autonomous: true
requirements: [VERIFY-V19-01]
must_haves:
  truths:
    - "Frontend vitest is 100% green (0 failures) against the current baseline (1614/1614 expected)"
    - "Frontend tsc --noEmit is clean (zero errors)"
    - "Server tsc --noEmit is clean (zero errors)"
    - "Server vitest failing FILES are a subset of the 14 known-flaky TD-V16-TEST-ISOLATION list (no new failing files)"
    - "Both production builds (web + server) complete clean (exit 0)"
    - "The targeted track-spec group (columnTypes, spatialColumns, trackConfig, wmsUrlBuilder, KineticaWmsLayerForm, CbConfigForm) is green"
    - "Every gate result is recorded verbatim in 54-01-AUTOMATED-GATES.md for the 54-03 compiler"
  artifacts:
    - path: ".planning/phases/54-verification-live-walk-through/54-01-AUTOMATED-GATES.md"
      provides: "Recorded pass/fail + counts for every automated gate, consumed by 54-03 VERIFICATION compile"
      contains: "frontend_vitest"
  key_links:
    - from: "54-01-AUTOMATED-GATES.md server_vitest_setgate section"
      to: "TD-V16-TEST-ISOLATION known-flaky list"
      via: "failing-file subset verdict"
      pattern: "known-flaky|subset"
---

<objective>
Run and record EVERY automated gate that feeds the v1.9 milestone-close VERIFICATION report. This is the autonomous, machine-verifiable half of Phase 54 — it runs to completion before any human walk-through. No code changes are expected: Phases 52 and 53 already shipped and verified (52 at 7/7 + the 5e3514b double-precision follow-up; 53 at 11/11). This plan only OBSERVES and RECORDS the current green state; it does not modify product code or specs.

Purpose: VERIFY-V19-01 requires "frontend/server test gates green at established baselines (frontend 100% green; server set-based known-flaky gate)." This plan produces that evidence for the 54-03 compiler.
Output: `54-01-AUTOMATED-GATES.md` capturing all gate results.

CRITICAL: If a gate is RED in a way that is NOT a documented known-flaky/pre-existing item, do NOT fix it blindly — record `result: FAIL`, surface the offending file(s)/error in the plan return, and let the orchestrator decide. A red gate here means something regressed since 52/53 verification and needs investigation, not a reflexive patch.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/52-track-spatial-mode-foundation/52-VERIFICATION.md
@.planning/phases/53-render-narrowing-param-surfaces-color-cutover/53-VERIFICATION.md

<read_first>
- Phase 52 VERIFICATION (passed 7/7) + its `## Follow-up` note: `5e3514b` fixed DOUBLE ("double precision") type recognition in all 3 NUMERIC_TYPES copies, with regression specs in columnTypes.spec.ts. Baseline after that fix was 1595/1595; the planning-context baseline is now 1614/1614.
- Phase 53 VERIFICATION (passed 11/11): production code touched only KineticaWmsLayerForm.tsx + CbConfigForm.tsx; wmsUrlBuilder.ts had 0 diff. Frontend 100% green; server tsc clean.
</read_first>

<interfaces>
Repo root: `/Users/rydelpereira/Documents/projects/kinetica_bi`. Run all gates from there.

Commands (verified against package.json scripts during planning):
- Frontend vitest (full): `npm run test -- --run`
- Frontend tsc: `npx -w packages/web tsc --noEmit`
- Server tsc: `npx -w packages/server tsc --noEmit`
- Server vitest (full, set-gate): `npm run test:server` (already appends --run)
- Frontend build: `npm run build`
- Server build: `npm run build:server`
- Targeted track-spec group (single vitest invocation, pattern OR): `npm run test -- --run columnTypes spatialColumns trackConfig wmsUrlBuilder KineticaWmsLayerForm CbConfigForm`

The 14 known-flaky server files (TD-V16-TEST-ISOLATION — EXCLUDED from the gate; may fail in the full run only; the gate accepts any SUBSET of these):
auth.oidc.spec.ts, auth.routes.spec.ts, boot.hardening.spec.ts, boot.wipe.spec.ts, bootstrap.spec.ts, db.smoke.spec.ts, errorMiddleware.spec.ts, kinetica.creds.routes.spec.ts, oidc.module.spec.ts, routes.discovery.spec.ts, routes.materialize.spec.ts, routes.sql.spec.ts, routes.wms.spec.ts (13 named in the Phase 51 close report; "14 known-flaky" is the historical ceiling — any subset passes the gate).

The 6 track-spec files (all confirmed present during planning):
packages/web/src/lib/columnTypes.spec.ts, packages/web/src/lib/spatialColumns.spec.ts, packages/web/src/lib/trackConfig.spec.ts, packages/web/src/lib/wmsUrlBuilder.spec.ts, packages/web/src/components/charts/KineticaWmsLayerForm.spec.tsx, packages/web/src/components/charts/CbConfigForm.spec.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run and record all automated gates into 54-01-AUTOMATED-GATES.md</name>
  <files>.planning/phases/54-verification-live-walk-through/54-01-AUTOMATED-GATES.md</files>
  <action>
Run each gate from the repo root `/Users/rydelpereira/Documents/projects/kinetica_bi` and capture the headline result (pass/fail + counts + any failing file names). Run them in this order; record the tail summary line of each. Do NOT change any product code or spec — this plan only observes the post-52/53 green state.

1. **Frontend vitest (full):** `npm run test -- --run` — expect 100% green (baseline ~1614/1614). Record passed/total + file count. If ANY failure appears, STOP and report it verbatim (it is a regression since 53 verification — out of scope to silently ignore or patch).
2. **Frontend tsc:** `npx -w packages/web tsc --noEmit`. Record clean (no output) or the exact errors. Any error here is a regression to surface, not fix.
3. **Server tsc:** `npx -w packages/server tsc --noEmit`. Record clean or errors.
4. **Server vitest (full, set-based gate):** `npm run test:server 2>&1 | tail -50`. Extract the list of FAILING files. The gate PASSES iff every failing file is in the 14 known-flaky TD-V16-TEST-ISOLATION list (see interfaces). Record: total passed, total failed, the failing-file list, a per-file "IN / NOT IN known-flaky list" membership check, and an explicit "failing files ⊆ known-flaky list: YES/NO" verdict. If NO, name the offending file(s) and mark the section FAIL.
5. **Targeted track-spec group:** `npm run test -- --run columnTypes spatialColumns trackConfig wmsUrlBuilder KineticaWmsLayerForm CbConfigForm 2>&1 | tail -25`. Record per-pattern/per-file pass counts; all six MUST be green (this is the v1.9 track-rendering regression surface — columnTypes carries the 5e3514b double-precision specs; wmsUrlBuilder carries the Phase 53 emission byte-locks; KineticaWmsLayerForm + CbConfigForm carry the render-narrowing/param-surface/color specs).
6. **Builds:** `npm run build 2>&1 | tail -12` then `npm run build:server 2>&1 | tail -12`. Record clean/errors for each (the frontend chunk-size warning is a pre-existing advisory, not an error — note it as such; exit 0 is the pass condition).

Write `.planning/phases/54-verification-live-walk-through/54-01-AUTOMATED-GATES.md` with frontmatter (`plan: 54-01`, a run timestamp, `executor:`), a one-line note of the commit it was run against (`git rev-parse --short HEAD`), and one section per gate using these EXACT heading anchors so 54-03 can grep them: `## frontend_vitest`, `## frontend_tsc`, `## server_tsc`, `## server_vitest_setgate`, `## track_spec_group`, `## builds`. Each section: a one-line `result: PASS|FAIL` plus the captured counts/file-list as evidence. End with a `## gate_summary` table (gate | result | evidence). Mirror the structure of the Phase 51 `51-01-AUTOMATED-GATES.md` precedent.

If any gate FAILS in a way that is NOT a documented known-flaky/pre-existing item, record `result: FAIL` and surface it in the plan return so the orchestrator can spin a 54.x investigation BEFORE 54-02 proceeds. Do not paper over it.
  </action>
  <verify>
    <automated>test -f /Users/rydelpereira/Documents/projects/kinetica_bi/.planning/phases/54-verification-live-walk-through/54-01-AUTOMATED-GATES.md && grep -qE "## (frontend_vitest|frontend_tsc|server_tsc|server_vitest_setgate|track_spec_group|builds|gate_summary)" /Users/rydelpereira/Documents/projects/kinetica_bi/.planning/phases/54-verification-live-walk-through/54-01-AUTOMATED-GATES.md && echo OK</automated>
  </verify>
  <done>54-01-AUTOMATED-GATES.md exists with all 6 gate sections + gate_summary; frontend vitest 100% green recorded; frontend + server tsc clean recorded; server set-gate verdict (failing files ⊆ known-flaky) recorded; track-spec group all-green recorded; both builds recorded clean (exit 0). Any new (non-known-flaky) failure is surfaced in the plan return, not hidden.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run` reports 0 failures (frontend TRUE 100% green).
- 54-01-AUTOMATED-GATES.md contains a PASS/FAIL line for each of the 6 gates plus the gate_summary table.
- The server set-based gate verdict explicitly states whether failing files are a subset of the 14 known-flaky list.
- The track-spec group section records all six spec files green.
</verification>

<success_criteria>
- Every automated gate VERIFY-V19-01 demands is run and recorded with evidence for the 54-03 compiler.
- No product code or spec changed — this plan observes the post-52/53 green baseline.
- Any new (non-known-flaky) failure is surfaced, not hidden or blindly patched.
</success_criteria>

<output>
After completion, create `.planning/phases/54-verification-live-walk-through/54-01-SUMMARY.md`.
</output>
