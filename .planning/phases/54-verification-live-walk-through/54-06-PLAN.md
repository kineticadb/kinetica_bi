---
phase: 54-verification-live-walk-through
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/lib/trackConfig.ts
  - packages/web/src/lib/trackConfig.spec.ts
  - packages/web/src/lib/wmsUrlBuilder.ts
  - packages/web/src/lib/wmsUrlBuilder.spec.ts
  - packages/web/src/components/charts/KineticaWmsLayerForm.tsx
  - packages/web/src/components/charts/KineticaWmsLayerForm.spec.tsx
autonomous: false
requirements: [TRACKFIX-V19-04, TRACKFIX-V19-05, TRACKFIX-V19-03]
gap_closure: true

must_haves:
  truths:
    - "Under spatialMode==='track' (track block active), the WMS request emits NO POINTCOLORS/POINTOPACITY/POINTSIZES/POINTSHAPES/SHAPEFILLCOLORS/SHAPELINECOLORS/SHAPELINEWIDTHS"
    - "Track requests emit all 8 TRACK_* params: TRACKHEADCOLORS, TRACKHEADSHAPES, TRACKHEADSIZES, TRACKLINECOLORS, TRACKLINEWIDTHS, TRACKMARKERCOLORS, TRACKMARKERSHAPES, TRACKMARKERSIZES"
    - "headShape emits TRACKHEADSHAPES (not TRACKMARKERSHAPES); the new markerShape control emits TRACKMARKERSHAPES — they are distinct params"
    - "TRACK STYLE section has a UI control for each of the 8 fields (head color/shape/size, line color/width, marker color/shape/size)"
    - "Non-track layers (latlon/wkt/cb-without-track) still emit POINT*/SHAPE* params byte-unchanged"
  artifacts:
    - path: "packages/web/src/lib/trackConfig.ts"
      provides: "TrackConfig extended with markerColor/markerShape/markerSize; TRACK_DEFAULTS updated to Kinetica doc defaults"
      contains: "markerColor"
    - path: "packages/web/src/lib/wmsUrlBuilder.ts"
      provides: "Track block: suppression of 7 point/shape keys + full TRACK_* emission with TRACKHEADSHAPES/TRACKMARKERSHAPES distinct"
      contains: "TRACKHEADSHAPES"
    - path: "packages/web/src/components/charts/KineticaWmsLayerForm.tsx"
      provides: "TRACK STYLE controls for head shape (TRACKHEADSHAPES), marker color/shape/size"
      contains: "Track marker"
  key_links:
    - from: "wmsUrlBuilder.ts track block"
      to: "params delete of 7 point/shape keys"
      via: "delete after DOTRACKS set"
      pattern: "delete params\\.POINTCOLORS"
    - from: "KineticaWmsLayerForm.tsx marker controls"
      to: "track_config.markerColor/markerShape/markerSize"
      via: "onSetTrackField"
      pattern: "onSetTrackField\\(\"marker"
---

<objective>
Close GAP-54-05 (suppression) and GAP-54-06 (full TRACK_* surface) from the 54-UAT walk. Two defects:

1. **Suppression (TRACKFIX-V19-04):** Under track mode the request still carries the raster/cb point+shape params (POINTCOLORS, POINTOPACITY, POINTSIZES, POINTSHAPES, SHAPEFILLCOLORS, SHAPELINECOLORS, SHAPELINEWIDTHS). Kinetica ignores TRACK_* styling when these are present — they must be stripped so the track tiles reflect TRACK_* styling.

2. **Full TRACK_* surface (TRACKFIX-V19-05):** Only 5 of the 8 documented TRACK_* params are emitted, and head shape is MIS-NAMED — headShape currently emits TRACKMARKERSHAPES (the OQ-9 misnaming). Fix: headShape → TRACKHEADSHAPES, and add 3 NEW marker params (TRACKMARKERCOLORS/SHAPES/SIZES) with their own controls. Update TRACK_DEFAULTS to Kinetica doc defaults.

3. **Re-walk (TRACKFIX-V19-03):** Operator re-walks §2/§3 of 54-UAT after the code lands — blocking human checkpoint.

Purpose: Track layers must render with full operator-controllable head/line/marker styling, and must not carry the point/shape params that suppress that styling on the Kinetica side.

Output: Extended TrackConfig + emission + form controls (packages/web only, no server change — TrackConfig is a JSON blob in the existing track_config column, no schema migration), updated regression specs, and an operator re-walk attestation.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-verification-live-walk-through/54-UAT.md
@.planning/REQUIREMENTS.md

@packages/web/src/lib/trackConfig.ts
@packages/web/src/lib/wmsUrlBuilder.ts
@packages/web/src/components/charts/KineticaWmsLayerForm.tsx

<interfaces>
<!-- Contracts the executor needs. Use directly — no codebase exploration. -->

Current TrackConfig (lib/trackConfig.ts) — EXTEND with 3 marker fields:
```typescript
export type TrackConfig = {
  enabled: boolean;
  trackIdAttr?: string;
  trackOrderAttr?: string;
  xCol?: string;
  yCol?: string;
  headColor?: string;     // 8-char AARRGGBB
  trailColor?: string;    // 8-char AARRGGBB — emitted as TRACKLINECOLORS
  headSize?: number;
  trailSize?: number;     // emitted as TRACKLINEWIDTHS
  lineWidth?: number;     // alias for trailSize
  headShape?: string;
  // ADD:
  markerColor?: string;   // 8-char AARRGGBB — TRACKMARKERCOLORS
  markerShape?: string;   // TRACKMARKERSHAPES
  markerSize?: number;    // TRACKMARKERSIZES
};
```

Current TRACK_DEFAULTS (lib/trackConfig.ts) — UPDATE values + ADD marker defaults:
```typescript
export const TRACK_DEFAULTS = {
  headColor: "FFFF0000",   // → CHANGE to "FFFFFFFF" (white)
  trailColor: "FF0000FF",  // → CHANGE to "FF00FF00" (green)
  headSize: 8,             // → CHANGE to 10
  trailSize: 2,            // → CHANGE to 3
  headShape: "circle",     // keep
  // ADD:
  markerColor: "FF0000FF", // blue
  markerShape: "none",
  markerSize: 2,
} as const;
```

POINT_SHAPES (exported from lib/wmsUrlBuilder.ts) — 12 entries, matches the required
shape dropdown options EXACTLY (none, circle, dash, diamond, dot, hollowcircle,
hollowdiamond, hollowsquare, hollowsquarewithplus, pipe, plus, square). REUSE for
both head shape and marker shape dropdowns — do NOT define a new TRACK_SHAPES list.

colorHex helpers (lib/colorHex.ts) used by the form color idiom:
```typescript
normalizeAARRGGBB(hex, fallback): string
rgbFromAARRGGBB(hex, fallback): string
alphaFromAARRGGBB(hex, fallback): string
joinAARRGGBB(alpha, rgb): string
alphaHexToPercent(hex): number
alphaPercentToHex(pct): string   // used inside the alpha range onChange
```

Form helpers already present (KineticaWmsLayerForm.tsx):
- `trackCfg = coalesceTrackConfig((config.track_config as string|null) ?? null)`
- `onSetTrackField(key: keyof TrackConfig, v: string|number|undefined)` — merges one
  field into track_config JSON preserving all others. Use for the new marker controls.
- The head-color control idiom (lines ~761-815) is the exact template to copy for
  marker color. The head-shape `<select>` (lines ~840-845, maps over POINT_SHAPES) is
  the template for both the head-shape control and the new marker-shape control.

Current emission gate (wmsUrlBuilder.ts ~432-465): track block fires when
`tc.enabled && (renderMode === "raster" || renderMode === "classbreak")`. The
`expand(v)` helper repeats a value N times comma-joined (N = breaks.length under
cb_raster, else 1). `normalizeAARRGGBB` is in scope.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend TrackConfig + TRACK_DEFAULTS, fix emission (suppression + full TRACK_* surface), update emission specs</name>
  <files>packages/web/src/lib/trackConfig.ts, packages/web/src/lib/trackConfig.spec.ts, packages/web/src/lib/wmsUrlBuilder.ts, packages/web/src/lib/wmsUrlBuilder.spec.ts</files>
  <behavior>
    trackConfig.spec.ts (UPDATE the TRACK_DEFAULTS describe block, lines 58-77):
    - TRACK_DEFAULTS.headColor === "FFFFFFFF" (was FFFF0000)
    - TRACK_DEFAULTS.trailColor === "FF00FF00" (was FF0000FF)
    - TRACK_DEFAULTS.headSize === 10 (was 8)
    - TRACK_DEFAULTS.trailSize === 3 (was 2)
    - TRACK_DEFAULTS.headShape === "circle" (unchanged)
    - NEW: TRACK_DEFAULTS.markerColor === "FF0000FF", markerShape === "none", markerSize === 2
    - The coalesceTrackConfig "full config verbatim" test (lines 35-55) may add markerColor/markerShape/markerSize round-trip assertions (optional; type still passes through verbatim).

    wmsUrlBuilder.spec.ts (UPDATE the "Track block (SCHEMA-V17-04)" describe, lines 781-849):
    - Raster single-value test: when track_config sets headColor/trailColor/headSize/trailSize/headShape/markerColor/markerShape/markerSize, assert:
      - TRACKHEADCOLORS, TRACKLINECOLORS, TRACKHEADSIZES, TRACKLINEWIDTHS as before
      - headShape now emits **TRACKHEADSHAPES** (NOT TRACKMARKERSHAPES) — change the existing line 804 assertion
      - NEW: TRACKMARKERCOLORS, TRACKMARKERSHAPES, TRACKMARKERSIZES present and distinct from head
      - assert TRACKHEADSHAPES !== undefined AND TRACKMARKERSHAPES !== undefined with the right respective values
    - NEW suppression test: under renderMode "raster" with track enabled AND config carrying pointColor/pointSize/pointShape/shapeFillColor/shapeLineColor/shapeLineWidth/pointOpacity — assert params does NOT have POINTCOLORS, POINTOPACITY, POINTSIZES, POINTSHAPES, SHAPEFILLCOLORS, SHAPELINECOLORS, SHAPELINEWIDTHS, while DOTRACKS/TRACK_* ARE present.
    - NEW suppression test under classbreak: with cb_config configured AND track enabled, assert the 7 point/shape keys absent but CB_ATTR/CB_VALS present.
    - cb_raster expand test: TRACK_* (incl. the new marker params) expand to N comma-sep matching breaks.length.
    - Backward-compat snapshot tests (lines 852-886) and the "track spatial mode (Phase 52)" describe MUST stay green — latlon/cb-without-track still emit POINT*/SHAPE*.
  </behavior>
  <action>
    **trackConfig.ts:**
    1. Add `markerColor?: string;`, `markerShape?: string;`, `markerSize?: number;` to TrackConfig (with the TRACKMARKER* comments).
    2. Update TRACK_DEFAULTS to Kinetica doc defaults: headColor "FFFFFFFF", trailColor "FF00FF00", headSize 10, trailSize 3, headShape "circle", and add markerColor "FF0000FF", markerShape "none", markerSize 2. Update the doc comment block to reference Kinetica WMS defaults (white head / green line / blue marker).

    **wmsUrlBuilder.ts track block (~432-465):**
    1. SUPPRESSION (TRACKFIX-V19-04): immediately after `params.DOTRACKS = "TRUE";` (inside the `tc.enabled && raster|classbreak` block), delete the 7 keys so it's robust whether the raster lane or cb lane emitted them:
       ```
       delete params.POINTCOLORS;
       delete params.POINTOPACITY;
       delete params.POINTSIZES;
       delete params.POINTSHAPES;
       delete params.SHAPEFILLCOLORS;
       delete params.SHAPELINECOLORS;
       delete params.SHAPELINEWIDTHS;
       ```
       Keep DOTRACKS, TRACK_ID_ATTR, TRACK_ORDER_ATTR, all TRACK_* params, and (under classbreak) CB_ATTR/CB_VALS. Add a comment citing TRACKFIX-V19-04 + GAP-54-05.
    2. FULL SURFACE (TRACKFIX-V19-05): keep the existing TRACKHEADCOLORS / TRACKLINECOLORS / TRACKHEADSIZES / TRACKLINEWIDTHS emission. Then:
       - **FIX the misnaming:** change the headShape branch (lines ~459-463) so `tc.headShape` emits `params.TRACKHEADSHAPES = expand(tc.headShape)` (NOT TRACKMARKERSHAPES). Update/remove the OQ-9 comment — note headShape→TRACKHEADSHAPES is now operator-confirmed against Kinetica WMS docs (2026-06-07).
       - **ADD the 3 marker params** (each guarded `if (tc.X !== undefined)`, each `expand(...)`, color via `normalizeAARRGGBB`):
         - `params.TRACKMARKERCOLORS = expand(normalizeAARRGGBB(tc.markerColor, "FF0000FF"))`
         - `params.TRACKMARKERSHAPES = expand(tc.markerShape)`
         - `params.TRACKMARKERSIZES = expand(String(tc.markerSize))`
       - Update the normalizeAARRGGBB fallbacks for head/line to the new doc defaults for self-consistency (headColor fallback "FFFFFFFF", trailColor fallback "FF00FF00") — these are belt-and-suspenders since the form always supplies a value.
       - DO NOT add SYMBOLROTATIONS or WORLDLAYERS (out of scope).
    3. Write the spec changes per <behavior>. Run RED first (the renamed TRACKHEADSHAPES + new marker assertions fail against current code), then GREEN.

    Constraint: non-track emission paths (latlon raster, wkt, cb-without-track, heatmap) MUST stay byte-identical — the delete only runs inside the enabled-track block. Verify the Phase 52 "track spatial mode" describe and backward-compat snapshot describe remain unchanged-and-green.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/lib/trackConfig.spec.ts src/lib/wmsUrlBuilder.spec.ts</automated>
  </verify>
  <done>TrackConfig has markerColor/markerShape/markerSize; TRACK_DEFAULTS at Kinetica doc values; track block deletes the 7 point/shape keys; headShape emits TRACKHEADSHAPES; markerShape emits TRACKMARKERSHAPES (distinct); all 8 TRACK_* params emit; trackConfig.spec + wmsUrlBuilder.spec green; non-track specs unchanged-and-green.</done>
</task>

<task type="auto">
  <name>Task 2: Add TRACK STYLE form controls — head shape (already present), marker color/shape/size; verify all 8 controls present</name>
  <files>packages/web/src/components/charts/KineticaWmsLayerForm.tsx, packages/web/src/components/charts/KineticaWmsLayerForm.spec.tsx</files>
  <action>
    In the TRACK STYLE section (KineticaWmsLayerForm.tsx, the `spatialMode === "track"` block ~755-933), confirm head color/size/shape and the relabelled Track line color/width controls remain (from 54-05 — do not touch their labels or emission). Then add a MARKER subgroup after the Track line width control (~line 932, before the closing `</div>` of the config-group):

    1. **Track marker color** — copy the head-color idiom (color swatch + AARRGGBB hex text + alpha range), writing `markerColor` via `onSetTrackField`, defaulting to `TRACK_DEFAULTS.markerColor`. Use aria-labels: "Track marker color (RGB)", "Track marker color (AARRGGBB hex)", "Track marker color alpha". Visible label "Track marker color".

    2. **Track marker shape** — copy the head-shape `<select>` (maps over `POINT_SHAPES`), writing `markerShape` via `onSetTrackField`, defaulting to `TRACK_DEFAULTS.markerShape`. aria-label "Track marker shape", visible label "Track marker shape". (POINT_SHAPES already includes "none" — that satisfies the "shape dropdown incl none" requirement.)

    3. **Track marker size** — copy the head-size range (min 0 max 20 step 1), writing `markerSize` via `onSetTrackField`, defaulting to `TRACK_DEFAULTS.markerSize`. aria-label "Track marker size", visible label "Track marker size".

    Use existing theme token classes (config-color-field, config-color-row, config-color-picker, config-color-text, config-range-field, config-range, config-range-value) — no new CSS. Reuse imported helpers; no new imports beyond what is already present (POINT_SHAPES, TRACK_DEFAULTS, the colorHex helpers are all imported).

    **Spec (KineticaWmsLayerForm.spec.tsx):** In the Track+Raster / TRACK STYLE describe (~1243-1322), add assertions:
    - "Track marker color (RGB)", "Track marker color (AARRGGBB hex)", "Track marker color alpha" controls present.
    - "Track marker shape" select present with 12 options (POINT_SHAPES length).
    - "Track marker size" range present.
    - Editing marker color hex calls onChange with track_config JSON where markerColor is set AND existing fields (enabled, xCol/yCol) preserved.
    - The existing "head shape" select assertion stays (still 12 options). Do NOT change the head-shape or Track line control assertions — they passed in 54-05.
    Keep the latlon control test (`Control: latlon+raster shows RASTER PARAMS; no TRACK STYLE`) green — marker controls only appear under track.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/KineticaWmsLayerForm.spec.tsx && npx tsc --noEmit -p packages/web/tsconfig.json</automated>
  </verify>
  <done>TRACK STYLE renders 8 controls (head color/shape/size, line color/width, marker color/shape/size); marker controls write markerColor/markerShape/markerSize via onSetTrackField preserving other fields; form spec green; web tsc clean; latlon no-regression test green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator re-walk §2/§3 of 54-UAT (TRACKFIX-V19-03)</name>
  <files>.planning/phases/54-verification-live-walk-through/54-UAT.md</files>
  <action>
    BLOCKING human checkpoint — operator-driven, NOT auto-verifiable. After Tasks 1+2 land and the app is rebuilt, the operator re-walks §2 (Track+Raster) and §3 (Track+Class Break) of 54-UAT.md per <what-built> / <how-to-verify>, then records the result by flipping §2/§3 statuses and the GAP-54-05/GAP-54-06 resolution in 54-UAT.md.
  </action>
  <what-built>
    Track WMS emission now suppresses the 7 point/shape params under track mode and emits all 8 TRACK_* params with TRACKHEADSHAPES and TRACKMARKERSHAPES as distinct params. TRACK STYLE has controls for head color/shape/size, track line color/width, and marker color/shape/size, defaulting to Kinetica doc defaults (white head, green line, blue marker).
  </what-built>
  <how-to-verify>
    With `npm run dev` (web) + `npm run dev:server` running, open the demo.track layer config (spatial mode auto-suggests Track). Then:

    1. **§2 Track+Raster:** Select Raster, save. Open the browser Network tab and inspect the WMS GetMap request URL for the track layer:
       - Confirm it does NOT contain POINTCOLORS, POINTOPACITY, POINTSIZES, POINTSHAPES, SHAPEFILLCOLORS, SHAPELINECOLORS, SHAPELINEWIDTHS.
       - Confirm it DOES contain all 8: TRACKHEADCOLORS, TRACKHEADSHAPES, TRACKHEADSIZES, TRACKLINECOLORS, TRACKLINEWIDTHS, TRACKMARKERCOLORS, TRACKMARKERSHAPES, TRACKMARKERSIZES — with TRACKHEADSHAPES and TRACKMARKERSHAPES present as distinct keys.
       - Confirm DOTRACKS=TRUE, TRACK_ID_ATTR, TRACK_ORDER_ATTR present.
    2. Confirm the TRACK STYLE section shows all 8 controls (head color/shape/size, track line color/width, marker color/shape/size) and they are visible/editable. Change head color, line color, and marker shape/size; save.
    3. Confirm track tiles RENDER and visibly reflect the chosen head/line/marker styling.
    4. **§3 Track+Class Break:** Switch to Class Break. Confirm CB builder + TRACK STYLE both show. Configure one break, save. Confirm tiles render (single-color track is EXPECTED — per-break categorical track coloring stays deferred, TRACK-V20-03). Confirm the request still suppresses POINT*/SHAPE* and carries TRACK_* + CB_ATTR/CB_VALS.

    Then update 54-UAT.md §2 (2.1-2.4) and §3 (3.1, 3.3) statuses to PASS (or note residual gaps), and set GAP-54-05/GAP-54-06 resolution to resolved in the gaps block.
  </how-to-verify>
  <verify>
    Manual operator re-walk — no automated command (this is the live confirmation the unit specs in Task 1/2 cannot provide). Automated coverage of the contract lands in Task 1 (wmsUrlBuilder.spec) + Task 2 (KineticaWmsLayerForm.spec).
  </verify>
  <done>Operator attests §2 (2.1-2.4) + §3 (3.1, 3.3) PASS in 54-UAT.md: no POINT*/SHAPE* in the track request, all 8 distinct TRACK_* present, controls visible/effective, tiles render reflecting head/line/marker styling; GAP-54-05/GAP-54-06 marked resolved.</done>
  <resume-signal>Type "approved" if §2/§3 pass (no POINT*/SHAPE* in request, all 8 distinct TRACK_* present, controls effective, tiles render), or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- `cd packages/web && npx vitest run` — full web suite green (baseline 1625/1625 + new track tests; net count rises with added suppression/marker tests).
- `npx tsc --noEmit -p packages/web/tsconfig.json` — clean.
- No server-side files modified (grep confirms changes are under packages/web only; no schema migration — track_config is a JSON blob in the existing column).
- Non-track emission paths unchanged: latlon/wkt/cb-without-track still emit POINT*/SHAPE* (backward-compat snapshot + Phase 52 track-spatial describes green and unmodified in intent).
- Operator re-walk §2/§3 attested PASS in 54-UAT.md.
</verification>

<success_criteria>
- Under track mode the WMS request emits NO POINTCOLORS/POINTOPACITY/POINTSIZES/POINTSHAPES/SHAPEFILLCOLORS/SHAPELINECOLORS/SHAPELINEWIDTHS.
- All 8 TRACK_* params emit; TRACKHEADSHAPES and TRACKMARKERSHAPES are distinct.
- TRACK STYLE has a working control for each of the 8 fields, defaulting to Kinetica doc defaults.
- Non-track layers unchanged.
- Operator confirms §2/§3 of 54-UAT render correctly with full track styling.
</success_criteria>

<output>
After completion, create `.planning/phases/54-verification-live-walk-through/54-06-SUMMARY.md`.
</output>
