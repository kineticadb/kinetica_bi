---
phase: 86-chart-y-axis-number-format
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/charts/FormatSpecEditor.tsx
  - packages/web/src/components/charts/FormatSpecEditor.spec.tsx
  - packages/web/src/components/ColumnFormatEditorModal.tsx
  - packages/web/src/components/charts/TimelineConfigPanel.tsx
  - packages/web/src/components/charts/TimelineConfigPanel.spec.tsx
  - packages/web/src/components/charts/NumericLineConfigPanel.tsx
  - packages/web/src/components/charts/NumericLineConfigPanel.spec.tsx
autonomous: true
requirements: [AXIS-V117-01, AXIS-V117-02]

must_haves:
  truths:
    - "A designer sees a Y-axis number-format control in BOTH the timeline and the line chart config panels"
    - "The control offers the same number-format kinds as the Column Format editor, including SI smart-abbreviation"
    - "Picking a kind writes yAxisFormat into the widget config (override set)"
    - "Choosing 'Use column default' clears the override (yAxisFormat becomes undefined)"
    - "Extracting the shared controls leaves the existing Column Format editor behavior unchanged (its tests still pass)"
  artifacts:
    - path: "packages/web/src/components/charts/FormatSpecEditor.tsx"
      provides: "Shared kind picker + per-kind controls (NumberControls/DateControls/D3Controls/SIControls), extracted from ColumnFormatEditorModal"
      contains: "export function FormatSpecEditor"
    - path: "packages/web/src/components/charts/TimelineConfigPanel.tsx"
      provides: "TimelineConfig.yAxisFormat field + Y-axis format section using FormatSpecEditor"
      contains: "yAxisFormat"
    - path: "packages/web/src/components/charts/NumericLineConfigPanel.tsx"
      provides: "NumericLineConfig.yAxisFormat field + Y-axis format section using FormatSpecEditor"
      contains: "yAxisFormat"
  key_links:
    - from: "packages/web/src/components/ColumnFormatEditorModal.tsx"
      to: "packages/web/src/components/charts/FormatSpecEditor.tsx"
      via: "import of the extracted *Controls sub-components"
      pattern: "from \"./charts/FormatSpecEditor\"|from \"\\./charts/FormatSpecEditor\""
    - from: "packages/web/src/components/charts/TimelineConfigPanel.tsx"
      to: "packages/web/src/components/charts/FormatSpecEditor.tsx"
      via: "import + render of FormatSpecEditor in OPTIONS section"
      pattern: "FormatSpecEditor"
    - from: "packages/web/src/components/charts/NumericLineConfigPanel.tsx"
      to: "packages/web/src/components/charts/FormatSpecEditor.tsx"
      via: "import + render of FormatSpecEditor in OPTIONS section"
      pattern: "FormatSpecEditor"
---

<objective>
Build the shared Y-axis number-format UI (AXIS-V117-01) and add the per-widget storage field with set/clear semantics (AXIS-V117-02, config side) to BOTH the timeline and line chart config panels.

This plan does NOT touch renderers — it only adds the config field and the control that writes it. The renderer-side resolution + tick wiring is Plan 02 (depends on this plan's `yAxisFormat` field and the new `FormatSpecEditor` component).

Purpose: Designers need one consistent format picker (reusing the exact Column Format editor controls, incl. the Phase 85 SI smart-abbreviation kind) on both chart config panels, and a clean way to clear back to the column default.

Output:
- New `packages/web/src/components/charts/FormatSpecEditor.tsx` — extracted kind picker + all 4 per-kind controls (`NumberControls`/`DateControls`/`D3Controls`/`SIControls`) + `defaultSpecForKind`, plus a leading "Use column default" option that maps to a cleared (undefined) override.
- `ColumnFormatEditorModal.tsx` re-imports the extracted controls/helper so its behavior + tests stay unchanged (pure refactor).
- `TimelineConfig` + `NumericLineConfig` gain optional `yAxisFormat?: FormatSpec` (mirroring the v1.14 `groupByColumn?: string` precedent).
- Both config panels render a "Y-AXIS FORMAT" section wiring `FormatSpecEditor`.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-chart-y-axis-number-format/86-RESEARCH.md

<interfaces>
<!-- Extracted from the live codebase. Use these directly — no further exploration needed. -->

From packages/web/src/lib/columnFormatter.ts (FormatSpec union, all 5 kinds incl. Phase 85 SI):
```typescript
export type FormatSpecNumber = { kind: "number"; /* ...sep/decimals/currency/percent fields... */ };
export type FormatSpecDate   = { kind: "date";   /* ...preset/pattern fields... */ };
export type FormatSpecD3     = { kind: "d3";     /* ...spec string... */ };
export type FormatSpecNone   = { kind: "none" };               // explicit no-op (operator cleared a prior spec)
export type FormatSpecSI     = { kind: "si"; decimals: number }; // Phase 85 SI smart-abbreviation
export type FormatSpec = FormatSpecNumber | FormatSpecDate | FormatSpecD3 | FormatSpecNone | FormatSpecSI;

export function buildFormatter(spec: FormatSpec): (v: unknown) => string | unknown;
export function defaultFormatKind(/* ... */): FormatSpec["kind"]; // NOTE: never returns "si" — SI is opt-in only
```

From packages/web/src/components/ColumnFormatEditorModal.tsx (CURRENT — the extraction source; line numbers approximate):
- `defaultSpecForKind(kind: FormatSpec["kind"]): FormatSpec` — line ~52
- kind picker `<select>` with 5 `<option>`s (none/number/date/d3/si) — lines ~395-399
- per-kind controls block: `{spec.kind === "number" && <NumberControls .../>}` etc. — lines ~404-426 (number/date/d3/si)
- `function NumberControls(...)` — line ~455
- `function DateControls(...)` — line ~534
- `function D3Controls(...)` — line ~583
- `function SIControls(...)` — line ~616
All 4 *Controls + `defaultSpecForKind` are self-contained and use ONLY existing CSS classes: `config-group`, `config-group-label`, `ds-field`, `ds-field-label`, `ds-select`, `config-hint`.

From packages/web/src/components/charts/TimelineConfigPanel.tsx:
- `export type TimelineConfig = { ... groupByColumn?: string; ... }` — line ~31, groupByColumn at line ~36
- `const patch = (partial: Partial<TimelineConfig>) => { onChange({ ...(config as Record<string, unknown>), ...partial }); }` — line ~171
- OPTIONS / group-by section with `config-group-label` headers — around lines ~313-369

From packages/web/src/components/charts/NumericLineConfigPanel.tsx (mirrors Timeline):
- `export type NumericLineConfig = { ... groupByColumn?: string; ... }` — line ~27, groupByColumn at line ~32
- `const patch = (partial: Partial<NumericLineConfig>) => { ... }` — line ~160
- group-by section around lines ~293-347
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract FormatSpecEditor (shared kind picker + 4 controls) and re-import into ColumnFormatEditorModal</name>
  <files>packages/web/src/components/charts/FormatSpecEditor.tsx, packages/web/src/components/charts/FormatSpecEditor.spec.tsx, packages/web/src/components/ColumnFormatEditorModal.tsx</files>
  <read_first>
    - packages/web/src/components/ColumnFormatEditorModal.tsx (the FULL file — source of the extraction: the kind picker JSX, the per-kind controls block, `NumberControls`/`DateControls`/`D3Controls`/`SIControls`, and `defaultSpecForKind`)
    - packages/web/src/lib/columnFormatter.ts (FormatSpec union + all 5 FormatSpec* type exports + buildFormatter signature)
    - packages/web/src/components/ColumnFormatEditorModal.spec.tsx (the existing tests that MUST stay green after extraction — understand what they assert about the picker, controls, and live preview)
    - packages/web/src/styles/global.css (confirm config-group / config-group-label / ds-field / ds-field-label / ds-select / config-hint exist — reuse ONLY these)
  </read_first>
  <behavior>
    - FormatSpecEditor renders a kind `<select>` whose options are: a leading "— Use column default —" option (value ""), then None / Number / Date / Advanced (d3-format) / Smart abbreviation (k / M / G / T).
    - When `spec` prop is null/undefined → the "Use column default" option is selected and NO per-kind controls render.
    - When `spec` is a FormatSpec → the matching option is selected and the matching `*Controls` render below the picker.
    - Selecting "Use column default" calls `onChange(null)`.
    - Selecting a real kind calls `onChange(defaultSpecForKind(kind))`.
    - Editing a control (e.g. SI decimals) calls `onChange(updatedSpec)`.
    - ColumnFormatEditorModal behavior is UNCHANGED: its own kind picker + controls still work (it imports the extracted `*Controls` + `defaultSpecForKind`), and ColumnFormatEditorModal.spec.tsx passes with NO edits to that spec file.
  </behavior>
  <action>
    1. Create `packages/web/src/components/charts/FormatSpecEditor.tsx`. MOVE (cut) these from `ColumnFormatEditorModal.tsx` into it and `export` each: `NumberControls`, `DateControls`, `D3Controls`, `SIControls`, and `defaultSpecForKind`. Keep their bodies byte-for-byte identical (zero logic change) — they already use only existing CSS classes.
    2. Import the FormatSpec types from the formatter lib:
       `import { type FormatSpec, type FormatSpecNumber, type FormatSpecDate, type FormatSpecD3, type FormatSpecSI } from "../../lib/columnFormatter";`
    3. Add the new shared component:
       `export function FormatSpecEditor({ spec, onChange }: { spec: FormatSpec | null; onChange: (s: FormatSpec | null) => void }): JSX.Element`
       - Render a `<div className="ds-field"><span className="ds-field-label">Format kind</span><select className="ds-select" value={spec?.kind ?? ""} onChange={...}>...</select></div>`.
       - First option: `<option value="">— Use column default —</option>`. Then `<option value="none">None</option>`, `number`, `date`, `<option value="d3">Advanced (d3-format)</option>`, `<option value="si">Smart abbreviation (k / M / G / T)</option>` — copy the labels verbatim from the modal's existing picker.
       - onChange handler: if `e.target.value === ""` → `onChange(null)`; else `onChange(defaultSpecForKind(e.target.value as FormatSpec["kind"]))`.
       - Below the picker, conditionally render the matching controls exactly as the modal does: `{spec?.kind === "number" && <NumberControls spec={spec as FormatSpecNumber} onChange={onChange} />}` and likewise for `date`/`d3`/`si`. (The `*Controls` onChange signature is `(s: FormatSpec) => void`; pass `onChange` directly — `FormatSpec` is assignable to the `FormatSpec | null` param.)
    4. In `ColumnFormatEditorModal.tsx`: DELETE the now-moved function bodies and ADD an import bringing them back:
       `import { NumberControls, DateControls, D3Controls, SIControls, defaultSpecForKind } from "./charts/FormatSpecEditor";`
       Do NOT change the modal's own picker JSX, controls block, live preview, or save path — only swap local definitions for the imported ones. The modal does NOT need to use `FormatSpecEditor` itself (it keeps its own picker with the label field + preview); it only reuses the extracted controls + helper.
    5. Create `packages/web/src/components/charts/FormatSpecEditor.spec.tsx` (vitest + @testing-library/react, mirror the ColumnFormatEditorModal.spec.tsx setup): assert (a) with `spec={null}` the select value is "" and no decimals input renders; (b) selecting "si" fires `onChange` with `{ kind: "si", decimals: <default> }`; (c) with an si spec, editing decimals fires onChange with the new decimals; (d) selecting "— Use column default —" (value "") fires `onChange(null)`.
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/components/charts/FormatSpecEditor.spec.tsx src/components/ColumnFormatEditorModal.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/web/src/components/charts/FormatSpecEditor.tsx` succeeds.
    - `grep -c "export function FormatSpecEditor" packages/web/src/components/charts/FormatSpecEditor.tsx` == 1.
    - `grep -E "export (function|const) (NumberControls|DateControls|D3Controls|SIControls|defaultSpecForKind)" packages/web/src/components/charts/FormatSpecEditor.tsx` returns 5 matches.
    - `grep -E "import \{[^}]*defaultSpecForKind[^}]*\} from \"\\./charts/FormatSpecEditor\"" packages/web/src/components/ColumnFormatEditorModal.tsx` matches (modal re-imports the extracted helpers).
    - `grep -c "function defaultSpecForKind" packages/web/src/components/ColumnFormatEditorModal.tsx` == 0 (the local definition was removed, not duplicated).
    - `grep -E "value=\"\"" packages/web/src/components/charts/FormatSpecEditor.tsx` matches (the "Use column default" sentinel option exists).
    - Negative: `grep -nE "className=\"(format-editor|fmt-editor|yaxis|y-axis-format)" packages/web/src/components/charts/FormatSpecEditor.tsx` returns NOTHING (no invented classes).
    - `npx tsc --noEmit` (from packages/web) exits 0.
    - `npx vitest run src/components/charts/FormatSpecEditor.spec.tsx src/components/ColumnFormatEditorModal.spec.tsx` exits 0 (ColumnFormatEditorModal.spec.tsx unchanged and still green).
    - `git diff --name-only packages/server` is empty.
  </acceptance_criteria>
  <done>FormatSpecEditor.tsx exists exporting the shared component + the 4 controls + defaultSpecForKind; ColumnFormatEditorModal re-imports them with no behavior change; its spec stays green; tsc clean; no server diff; no invented classes.</done>
</task>

<task type="auto">
  <name>Task 2: Add yAxisFormat field + Y-AXIS FORMAT section to both config panels</name>
  <files>packages/web/src/components/charts/TimelineConfigPanel.tsx, packages/web/src/components/charts/TimelineConfigPanel.spec.tsx, packages/web/src/components/charts/NumericLineConfigPanel.tsx, packages/web/src/components/charts/NumericLineConfigPanel.spec.tsx</files>
  <read_first>
    - packages/web/src/components/charts/TimelineConfigPanel.tsx (TimelineConfig type at ~line 31 incl. groupByColumn at ~line 36; the `patch()` helper at ~line 171; the OPTIONS/group-by section ~lines 313-369 — this is where the new section goes)
    - packages/web/src/components/charts/NumericLineConfigPanel.tsx (NumericLineConfig type at ~line 27 incl. groupByColumn at ~line 32; `patch()` at ~line 160; group-by section ~lines 293-347)
    - packages/web/src/components/charts/FormatSpecEditor.tsx (the component created in Task 1 — its props: `{ spec: FormatSpec | null; onChange: (s: FormatSpec | null) => void }`)
    - packages/web/src/lib/columnFormatter.ts (the `FormatSpec` type to import into both config panels)
    - packages/web/src/components/charts/TimelineConfigPanel.spec.tsx + NumericLineConfigPanel.spec.tsx (existing test setup/onChange-capture pattern to mirror)
    - .planning/STATE.md v1.14 section (the groupByColumn optional-field threading precedent)
  </read_first>
  <action>
    For BOTH `TimelineConfigPanel.tsx` and `NumericLineConfigPanel.tsx` (apply the identical pattern, mirroring the v1.14 groupByColumn precedent):
    1. Import the FormatSpec type and the editor:
       `import { type FormatSpec } from "../../lib/columnFormatter";`
       `import { FormatSpecEditor } from "./FormatSpecEditor";`
    2. Add the optional field to the config type, directly after the existing `groupByColumn?: string;` line:
       `yAxisFormat?: FormatSpec;  // Phase 86: per-widget Y-axis tick formatter override. Absent → bound column default.`
       (In TimelineConfig after line ~36; in NumericLineConfig after line ~32.)
    3. Read the current value near the other cfg reads: `const yAxisFormat = cfg.yAxisFormat ?? null;`
    4. In the OPTIONS area (immediately AFTER the existing group-by `config-group`/section, still inside the `tableId !== undefined` fragment that already gates the group-by UI), render:
       ```tsx
       <div className="config-group-label" style={{ marginTop: 16 }}>Y-AXIS FORMAT</div>
       <FormatSpecEditor
         spec={yAxisFormat}
         onChange={(s) => patch({ yAxisFormat: s ?? undefined })}
       />
       <div className="config-hint">Applied to the Y-axis tick labels only (tooltips + data labels keep their column formatting). Applied to all Y-axis metrics. Clear to use the bound column's default.</div>
       ```
       (Use the `style={{ marginTop: 16 }}` inline spacing exactly as the research shows; `config-group-label` + `config-hint` are existing classes.)
       Setting a kind → `patch({ yAxisFormat: <spec> })`. Clearing (`s === null`) → `patch({ yAxisFormat: undefined })` (field absent → renderer falls back to column default in Plan 02). Do NOT use `{ kind: "none" }` to represent "no override".
    5. Extend BOTH spec files: add a test that the "Y-AXIS FORMAT" label renders; that changing the FormatSpecEditor kind select to "si" calls the captured `onChange`/`patch` with config containing `yAxisFormat: { kind: "si", ... }`; and that selecting "— Use column default —" calls onChange with `yAxisFormat: undefined`. Query the kind select via its existing `Format kind` label / role (no new test ids needed).
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/components/charts/TimelineConfigPanel.spec.tsx src/components/charts/NumericLineConfigPanel.spec.tsx && npx vitest run src/styles/theme-guard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "yAxisFormat?" packages/web/src/components/charts/TimelineConfigPanel.tsx` >= 1 AND `grep -c "yAxisFormat?" packages/web/src/components/charts/NumericLineConfigPanel.tsx` >= 1 (optional field added to both types).
    - `grep -c "FormatSpecEditor" packages/web/src/components/charts/TimelineConfigPanel.tsx` >= 2 (import + render) AND same for NumericLineConfigPanel.tsx.
    - `grep -c "Y-AXIS FORMAT" packages/web/src/components/charts/TimelineConfigPanel.tsx` == 1 AND == 1 in NumericLineConfigPanel.tsx.
    - `grep -E "patch\(\{ yAxisFormat: undefined \}\)|yAxisFormat: s \?\? undefined" packages/web/src/components/charts/TimelineConfigPanel.tsx` matches (clear path patches undefined) AND same for NumericLineConfigPanel.tsx.
    - Negative: `grep -nE "className=\"(yaxis-|y-axis-|format-section|yaxis-format)" packages/web/src/components/charts/TimelineConfigPanel.tsx packages/web/src/components/charts/NumericLineConfigPanel.tsx` returns NOTHING.
    - `npx tsc --noEmit` (from packages/web) exits 0.
    - `npx vitest run src/components/charts/TimelineConfigPanel.spec.tsx src/components/charts/NumericLineConfigPanel.spec.tsx` exits 0.
    - `npx vitest run src/styles/theme-guard.spec.ts` exits 0 (no raw hex introduced).
    - `git diff --name-only packages/server` is empty.
  </acceptance_criteria>
  <done>Both TimelineConfig + NumericLineConfig have optional `yAxisFormat?: FormatSpec`; both panels render a "Y-AXIS FORMAT" section wiring FormatSpecEditor with set/clear (undefined-on-clear) semantics; specs assert the control appears + set + clear; tsc clean; theme-guard green; no server diff; no invented classes.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx tsc --noEmit` clean.
- `cd packages/web && npx vitest run src/components/charts/FormatSpecEditor.spec.tsx src/components/ColumnFormatEditorModal.spec.tsx src/components/charts/TimelineConfigPanel.spec.tsx src/components/charts/NumericLineConfigPanel.spec.tsx` exits 0.
- `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` green.
- `git diff --name-only packages/server` empty (FRONTEND-ONLY).
- ColumnFormatEditorModal.spec.tsx green WITHOUT edits to that spec file (pure refactor of the controls extraction).
</verification>

<success_criteria>
- AXIS-V117-01: both config panels expose a Y-axis number-format control offering the same kinds as the Column Format editor (incl. SI), via the shared `FormatSpecEditor`.
- AXIS-V117-02 (config side): setting a kind writes `yAxisFormat`; clearing to "Use column default" sets `yAxisFormat: undefined`. (Renderer fallback resolution is Plan 02.)
- The extraction is a pure refactor: ColumnFormatEditorModal behavior + its tests unchanged.
- No new CSS classes; theme-tokens only; no server diff; no new dependency.
</success_criteria>

<output>
After completion, create `.planning/phases/86-chart-y-axis-number-format/86-01-SUMMARY.md`
</output>
