---
phase: 65-calendar-sql-builder-kinetica-spike
plan: 02
type: execute
wave: 2
depends_on: ["65-01"]
files_modified:
  - packages/web/src/lib/calendarBin.ts
autonomous: true
requirements:
  - CAL-V113-03
must_haves:
  truths:
    - "Each calendar DATE_TRUNC unit (year, month, week, day, hour) is confirmed valid against the live Kinetica instance — or the spike is explicitly recorded NOT-RUN with the exact queries for the operator + flagged as a Phase 69 UAT item"
    - "The DATE_TRUNC('week', ...) start-day anchor (Mon vs Sun vs ISO) is recorded against the live instance, reconciling the documented Monday assumption in calendarBin.ts"
    - "The returned timestamp format + UTC bucketing behavior is recorded"
    - "KINETICA_DATE_TRUNC_UNITS reflects the confirmed set; the constant's comment is annotated with the spike outcome (confirmed-on-DATE or NOT-RUN-flagged-for-Phase-69)"
  artifacts:
    - path: ".planning/phases/65-calendar-sql-builder-kinetica-spike/65-02-SUMMARY.md"
      provides: "Recorded spike results: per-unit validity, week anchor, timestamp format, UTC contract — OR the NOT-RUN record with exact queries + Phase 69 flag"
      contains: "DATE_TRUNC"
    - path: "packages/web/src/lib/calendarBin.ts"
      provides: "KINETICA_DATE_TRUNC_UNITS comment annotated with the spike outcome"
      contains: "KINETICA_DATE_TRUNC_UNITS"
  key_links:
    - from: ".planning/phases/65-calendar-sql-builder-kinetica-spike/65-02-SUMMARY.md"
      to: "packages/web/src/lib/calendarBin.ts"
      via: "spike outcome annotated into the KINETICA_DATE_TRUNC_UNITS comment + the week-anchor comment"
      pattern: "confirmed|NOT-RUN|Phase 69"
---

<objective>
Run the Kinetica DATE_TRUNC verification spike against the LIVE instance via the running app's SQL
path, and record the results: confirm each calendar unit string (year, month, week, day, hour) is
valid, capture the `week` start-day anchor (Mon/Sun/ISO), and confirm the returned timestamp format +
UTC bucketing behavior. Annotate `KINETICA_DATE_TRUNC_UNITS` with the outcome.

Purpose: Plan 65-01 built the lib to a documented assumption (UTC; Monday/ISO week start). This spike
turns that assumption into a verified fact — OR explicitly records it as NOT-RUN and flags Phase 69 UAT
to verify, so no assumption ships silently.

Output: `65-02-SUMMARY.md` with the recorded results; an annotated comment in `calendarBin.ts`.
This plan touches ZERO production logic beyond a comment annotation. NO renderer, NO server change.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/65-calendar-sql-builder-kinetica-spike/65-01-SUMMARY.md

<!-- The constant to annotate (created in 65-01) -->
@packages/web/src/lib/calendarBin.ts

<!-- How the app runs SQL (the path the spike uses) -->
@packages/web/src/lib/timelineBin.ts

<interfaces>
<!-- The SQL passthrough the spike must use (server packages/server/src/index.ts:2178):
       POST /api/sql  body: { sql: string, options?: object }
     Gated by requireConfig — needs the dev server running + a logged-in session (creds in
     packages/server/.env). The frontend wrapper is runSql(sql, options?, signal?) in
     packages/web/src/api/client.ts:166. -->

<!-- Dev servers:
       npm run dev:server   (Express + Kinetica passthrough, packages/server)
       npm run dev          (Vite web app, packages/web)
     The spike issues SQL through this authenticated path — exactly as the operator chose
     (executor runs it via the app). DO NOT read/print packages/server/.env secrets. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the DATE_TRUNC spike via the app SQL path (with documented NOT-RUN fallback)</name>
  <files>.planning/phases/65-calendar-sql-builder-kinetica-spike/65-02-SUMMARY.md, packages/web/src/lib/calendarBin.ts</files>
  <read_first>
    - packages/web/src/lib/calendarBin.ts (the KINETICA_DATE_TRUNC_UNITS + week-anchor comment to annotate)
    - packages/server/src/index.ts around line 2178 (the POST /api/sql route — how SQL is executed)
    - packages/web/src/api/client.ts line 166 (runSql wrapper signature)
    - .planning/research/STACK.md "Kinetica vs Postgres Dialect Flags" (week-start alignment FLAG)
    - .planning/research/PITFALLS.md Pitfall 3 (unsupported DATE_TRUNC unit → silent NULL/error)
  </read_first>
  <action>
    PRIMARY PATH — run the spike through the running app's authenticated SQL path:

    1. Start the dev server if not already running: `npm run dev:server` (Express + Kinetica passthrough).
       The /api/sql route is gated by requireConfig — it needs valid Kinetica creds already present in
       packages/server/.env (DO NOT open, print, or commit that file). If a logged-in session/token is
       required, use the existing dev login flow the same way the web app does.

    2. Issue these EXACT queries via POST /api/sql (curl with the session, or a tiny throwaway node
       script using the runSql wrapper). Record the raw returned value for each:

       a. WEEK ANCHOR — Jan 1 2024 is a MONDAY:
          `SELECT DATE_TRUNC('week', TIMESTAMP '2024-01-01 00:00:00') AS wk`
          - If it returns 2024-01-01 → week starts Monday (matches the documented assumption).
          - If 2023-12-31 → Sunday start.  If 2023-12-25 or other → record exactly what came back.

       b. PER-UNIT VALIDITY — probe each candidate unit; a SQL error or all-NULL means unsupported:
          `SELECT DATE_TRUNC('hour',  TIMESTAMP '2024-03-15 13:45:30') AS h`
          `SELECT DATE_TRUNC('day',   TIMESTAMP '2024-03-15 13:45:30') AS d`
          `SELECT DATE_TRUNC('week',  TIMESTAMP '2024-03-15 13:45:30') AS w`
          `SELECT DATE_TRUNC('month', TIMESTAMP '2024-03-15 13:45:30') AS mo`
          `SELECT DATE_TRUNC('year',  TIMESTAMP '2024-03-15 13:45:30') AS y`

       c. QUARTER availability (v2/out-of-scope, but record whether it exists for future phases):
          `SELECT DATE_TRUNC('quarter', TIMESTAMP '2024-03-15 13:45:30') AS q`
          - Record VALID or ERROR. Do NOT add it to KINETICA_DATE_TRUNC_UNITS (out of v1.13 scope).

       d. UTC + FORMAT — confirm the returned timestamp string shape and that no implicit TZ conversion
          occurs. Note the exact returned format (e.g. "2024-03-15 13:00:00.000") so Phase 67's label
          parser and Phase 68's drill bounds align with it.

    3. RECORD in 65-02-SUMMARY.md a results table: unit | query | returned value | VALID/ERROR.
       Record the week anchor finding, the timestamp format, and the UTC behavior.

    4. ANNOTATE calendarBin.ts:
       - Update the comment on KINETICA_DATE_TRUNC_UNITS to: "// Confirmed valid against live Kinetica
         on <DATE> via /api/sql spike (Plan 65-02). week anchor = <Monday|Sunday|ISO>." Keep the array
         contents = ['year','month','week','day','hour'] unless the spike PROVES a unit invalid.
       - If the spike shows week starts on a different day than the documented Monday assumption baked
         into computeCellBounds (65-01), UPDATE the week-start offset in computeCellBounds to match AND
         note the change in the SUMMARY. Re-run `cd packages/web && npx vitest run src/lib/calendarBin.spec.ts`
         (adjusting the week-case expected values to the confirmed anchor) so the suite stays green.

    FALLBACK PATH — if Kinetica is genuinely unreachable (server down, no creds, no network in this
    environment): DO NOT silently assume. Instead:
       (a) Record the spike as NOT-RUN in 65-02-SUMMARY.md, with the EXACT queries above pasted verbatim
           for the operator to run later, and the precise reason it could not run.
       (b) Leave the lib at the documented assumptions (UTC; Monday/ISO week behind KINETICA_DATE_TRUNC_UNITS
           and the computeCellBounds week offset) — already correct from 65-01.
       (c) Annotate the KINETICA_DATE_TRUNC_UNITS comment: "// NOT YET VERIFIED against live Kinetica —
           spike could not run in this env (<reason>); FLAGGED for Phase 69 UAT (CAL-V113-03)."
       (d) Add an explicit "Phase 69 UAT verification item" section to the SUMMARY so it is not lost.

    DO NOT touch, read the contents of, or commit packages/server/.env. DO NOT add new server routes.
    DO NOT modify any renderer/panel/WidgetRenderer.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/lib/calendarBin.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/65-calendar-sql-builder-kinetica-spike/65-02-SUMMARY.md`
    - `grep -q "DATE_TRUNC" .planning/phases/65-calendar-sql-builder-kinetica-spike/65-02-SUMMARY.md` (queries/results recorded)
    - `grep -Eqi "week.*(Monday|Sunday|ISO|anchor)" .planning/phases/65-calendar-sql-builder-kinetica-spike/65-02-SUMMARY.md` (week anchor recorded)
    - `grep -Eqi "confirmed|NOT-RUN|NOT YET VERIFIED|Phase 69" packages/web/src/lib/calendarBin.ts` (constant annotated with the spike outcome)
    - SUMMARY records either per-unit VALID results OR a NOT-RUN block with the exact queries + a Phase 69 UAT flag
    - `cd packages/web && npx vitest run src/lib/calendarBin.spec.ts` 100% green (week-case expectations match the recorded/assumed anchor)
    - `git diff --name-only -- packages/server` is EMPTY
    - `git diff --name-only -- packages/web/src/components` is EMPTY
  </acceptance_criteria>
  <done>Spike results (or an explicit NOT-RUN + Phase-69-flag record) are in 65-02-SUMMARY.md; KINETICA_DATE_TRUNC_UNITS is annotated with the outcome; calendar specs are green; zero server/components diff.</done>
</task>

</tasks>

<verification>
1. Spike outcome recorded: `65-02-SUMMARY.md` exists and contains the per-unit results (or NOT-RUN block).
2. Constant annotated: `calendarBin.ts` KINETICA_DATE_TRUNC_UNITS comment reflects confirmed-or-flagged.
3. Calendar specs green from packages/web: `cd packages/web && npx vitest run src/lib/calendarBin.spec.ts`.
4. Full suite green: `cd packages/web && npx vitest run`.
5. Web tsc clean: `cd packages/web && npx tsc --noEmit -p tsconfig.json` (exit 0).
6. Pure-lib-only gates: `git diff --name-only -- packages/server` EMPTY; `git diff --name-only -- packages/web/src/components` EMPTY.
7. packages/server/.env was never read, printed, or committed.
</verification>

<success_criteria>
- ROADMAP success criterion 3: the Kinetica spike documents the confirmed DATE_TRUNC valid unit set +
  the week start-day anchor + UTC bucketing contract; unsupported units rejected behind the shared
  KINETICA_DATE_TRUNC_UNITS constant — MET (or explicitly NOT-RUN-and-flagged-for-Phase-69, with the
  exact queries recorded, so the assumption never ships silently).
- The week-anchor finding reconciles (or corrects) the documented Monday/ISO assumption from Plan 65-01.
- Zero server diff; zero components diff; full vitest green; web tsc clean.
</success_criteria>

<output>
After completion, ensure `.planning/phases/65-calendar-sql-builder-kinetica-spike/65-02-SUMMARY.md`
captures: the spike outcome (RUN with results table, or NOT-RUN with the exact queries + Phase 69 flag),
the confirmed/assumed week anchor, the timestamp format, and the UTC contract. This SUMMARY is the
authoritative record downstream phases (66/67/68/69) cite.
</output>
