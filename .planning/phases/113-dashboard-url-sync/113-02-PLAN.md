---
phase: 113-dashboard-url-sync
plan: 02
type: execute
wave: 2
depends_on: ["113-01"]
files_modified:
  - .planning/phases/113-dashboard-url-sync/113-UAT.md
autonomous: false
requirements: [DLINK-V121-01, DLINK-V121-06, DLINK-V121-07]

must_haves:
  truths:
    - "In a real browser, opening a dashboard shows ?dashboard=<id> in the address bar"
    - "In a real browser, the browser Back button returns to the dashboard list with the param gone"
    - "In a real browser, the in-app Back button leaves the history stack one entry deep — pressing browser Back again leaves the app rather than re-opening the dashboard just left"
  artifacts:
    - path: ".planning/phases/113-dashboard-url-sync/113-UAT.md"
      provides: "Operator's recorded pass/fail for the four real-browser checks"
      contains: "UAT-113"
  key_links:
    - from: "operator's browser address bar"
      to: "the pushed history entry"
      via: "manual walk-through of list -> open -> back"
      pattern: "dashboard="
---

<objective>
Confirm in a REAL browser that the address bar and the Back button behave as specified. jsdom proves the mechanism; only a browser proves the behaviour a person actually experiences.

Purpose: history-stack depth and address-bar rendering are exactly the class of thing CLAUDE.md §"Writing verifiable acceptance criteria" says to route to a human rather than dress in a grep. Plan 01's automated gates cannot observe "pressing Back twice walks you out of the app instead of looping back into dashboards you already left".

Output: `.planning/phases/113-dashboard-url-sync/113-UAT.md` recording the operator's result for each check.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-dashboard-url-sync/113-01-PLAN.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator walk-through of the address bar and Back button</name>

  <read_first>
    - `.planning/phases/113-dashboard-url-sync/113-01-SUMMARY.md` — what Plan 01 actually shipped, so the walk-through matches the code.
    - `packages/web/src/lib/dashboardUrl.ts` — the exact param name and the push/replace decision being verified.
  </read_first>

  <files>.planning/phases/113-dashboard-url-sync/113-UAT.md</files>

  <what-built>
    Opening a dashboard now pushes one history entry and writes `?dashboard=<id>` into the address bar immediately. The in-app "Back to dashboards" button pops that entry (so the app's Back IS the browser's Back), and a `popstate` with no `dashboard` param returns the screen to the dashboard list. Nothing reads the URL on load yet — that is Phase 114.
  </what-built>

  <how-to-verify>
    Start the dev server (`npm run dev` at the repo root, plus `npm run dev:server`) and open the app in a **real browser tab that you have just opened** (so the tab's history starts clean). Then:

    1. **Address bar on open.** From the Dashboards list, click "Open" on any dashboard. The address bar must change to `…/?dashboard=<that dashboard's id>` the instant the dashboard screen appears — before its widgets have finished loading. Expected: yes, and the id matches the dashboard you opened.

    2. **Browser Back.** With that dashboard open, press the browser's Back button. Expected: the dashboard **list** comes back on screen AND `?dashboard=…` disappears from the address bar. Both, not one.

    3. **In-app Back leaves the stack shallow.** Open the dashboard again, then click the in-app "Back to dashboards" button. Expected: you are on the list with a clean URL. Now press the browser's Back button once more. Expected: you **leave the application** (back to whatever the tab showed before, or a blank new-tab page) — you must NOT land back on the dashboard you just left. Seeing the dashboard re-open here means the in-app button pushed a second entry instead of popping the one we pushed, which is the rejected design.

    4. **Copy-paste sanity.** With a dashboard open, copy the address bar, and confirm it reads `…/?dashboard=<id>` with the numeric id — not a name or slug. (Pasting it into a new tab is expected to land on the dashboard **list** for now; opening from a URL is Phase 114. That is correct behaviour at this point, not a failure.)

    Note on the no-history-to-pop case: it is fully covered by the automated test `writes the list URL when there is no history entry to pop` in `src/lib/dashboardUrl.spec.ts`, and it is not reachable by hand in a real browser until Phase 114 makes deep-link arrival possible. Do not try to reproduce it manually.

    Record each check's result in `.planning/phases/113-dashboard-url-sync/113-UAT.md`, one line per check, prefixed `UAT-113-1` … `UAT-113-4`.
  </how-to-verify>

  <acceptance_criteria>
    1. `.planning/phases/113-dashboard-url-sync/113-UAT.md` exists and `grep -c "UAT-113-" .planning/phases/113-dashboard-url-sync/113-UAT.md` -> `>= 4` (file does not exist today, so this cannot pass early).
    2. Every one of the four checks is recorded as PASS, or any FAIL is written up with the observed behaviour and routed to a gap-closure plan — a FAIL is never silently accepted.
    3. Check 3 in particular is recorded explicitly: pressing browser Back after the in-app Back must exit the app, not re-open the dashboard.
  </acceptance_criteria>

  <action>
    Present checks 1-4 to the operator, collect their observations verbatim, and write `.planning/phases/113-dashboard-url-sync/113-UAT.md` with a `UAT-113-<n>: PASS|FAIL — <observation>` line for each.
  </action>

  <verify>
    <automated>grep -c "UAT-113-" .planning/phases/113-dashboard-url-sync/113-UAT.md</automated>
  </verify>

  <resume-signal>Type "approved" if all four checks pass, or describe exactly what you saw for any that did not.</resume-signal>

  <done>113-UAT.md records a result for all four checks and the operator has approved, or the failures are captured for gap closure.</done>
</task>

</tasks>

<verification>
- `.planning/phases/113-dashboard-url-sync/113-UAT.md` exists with >= 4 `UAT-113-` lines.
- No source file changed in this plan: `git status --short packages/` is empty unless a gap was found and fixed under a separate plan.
</verification>

<success_criteria>
All three ROADMAP success criteria for Phase 113 confirmed by a person in a real browser:
1. Opening a dashboard immediately updates the address bar to a URL identifying that dashboard.
2. Back while a dashboard is open returns to the list, on screen and in the address bar.
3. Going back to the list clears the dashboard identifier — and the history stack stays shallow, so Back again leaves the app rather than re-opening what was just left.
</success_criteria>

<output>
After completion, create `.planning/phases/113-dashboard-url-sync/113-02-SUMMARY.md`.
</output>
