---
status: complete
phase: 113-dashboard-url-sync
source: [113-01-SUMMARY.md, 113-02-SUMMARY.md]
started: 2026-09-11T00:00:00Z
updated: 2026-09-11T00:00:00Z
verdict: APPROVED — 6/6 checks passed
---

## Current Test

[testing complete]

## Tests

### UAT-113-1. Address bar updates on open
expected: `…/?dashboard=<numeric id>` appears the instant the dashboard renders; does NOT appear-then-vanish
result: pass
note: |
  The appear-then-vanish FAIL condition is the React 18 StrictMode double-invoke
  regression — a dev-only failure the entire automated suite passes through.
  Confirmed absent.

### UAT-113-2. Browser Back returns to the list
expected: both the list reappears AND the param clears
result: pass

### UAT-113-3. In-app Back keeps the history stack shallow
expected: after in-app "Back to dashboards", one more browser Back LEAVES the app rather than re-opening the dashboard
result: pass
reported: "app leaves back to login page but then sends you to dashboard page since i am already logged in … i did not go back to the dashboard page"
note: |
  The login → dashboards bounce is ordinary auth boot, not a history artefact.
  The operative fact is that the dashboard did NOT re-open, proving the in-app
  Back POPS the entry pushed on open rather than pushing a second one. Had it
  pushed, the stack would grow and repeated Back would walk backwards through
  dashboards already left.

### UAT-113-4. URL format is the numeric id
expected: `?dashboard=<plain number>`, not a name or slug
result: pass
operator_confirmed: "yes, dashboard followed by a plain number"
note: |
  Operator initially recorded this FAIL because pasting the URL into a new tab
  lands on the dashboard LIST rather than opening the dashboard. That is correct
  behaviour for Phase 113 — opening a dashboard FROM a URL is Phase 114. The
  check's wording buried that in a parenthetical and read like a feature test;
  the misread was the check's fault, not the operator's. Same applies to the
  operator's manual id edit not navigating.

### UAT-113-5. Back-then-Forward leaves no stale param
expected: after in-app Back then browser Forward, the bar stays clean and the list stays on screen
result: pass
operator_confirmed: 'address bar reads "http://localhost:5173/"'
note: |
  This check exists because the first plan left this gap: the forward entry was
  still marked, so re-entering it showed the list with `?dashboard=12` in the
  bar. Confirmed closed.

### UAT-113-6. Sidebar navigation away clears the param
expected: open a dashboard, click Datasets — bar has no `?dashboard=`; return to Dashboards — still clean
result: pass
operator_confirmed: "screenshots show `localhost:5173` after both steps, no param in either"
note: |
  The path that exercises the deferred unmount cleanup, added after review found
  sidebar-away left a stale param — an ordinary one-click flow, not an edge case.
  Invisible without checking the address bar: the Datasets screen looks correct
  either way.

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0

## Gaps

None from this phase.

## Findings outside this phase

- **Sidebar "Dashboards" link is a no-op while a dashboard is open.** Discovered
  by the operator while performing check 6 (they clicked Dashboards rather than
  Datasets). `App.tsx:248` calls `setPage("dashboards")`; when `page` is already
  `"dashboards"` that is a no-op, so `DashboardsPage` never unmounts, its internal
  view stays `{ mode: "open" }`, and the user remains on the dashboard instead of
  returning to the list.

  **PRE-EXISTING — not caused by Phase 113.** Verified: this branch's diff against
  `origin/master` for `App.tsx` is empty, and the `DashboardsPage` changes touch
  only the open / in-app-back / popstate paths.

  **Not a DLINK-V121-07 violation:** the user genuinely is still on the dashboard,
  and the URL still carries its id — screen and URL agree. This is a navigation
  UX defect, not a URL-sync defect.

  Logged for the backlog rather than fixed here.
