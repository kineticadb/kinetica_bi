---
phase: 100-custom-metrics-tables-area-editor-metric-picker-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/lib/customMetricSql.ts
  - packages/web/src/lib/customMetricSql.spec.ts
  - packages/web/src/lib/timelineBin.ts
  - packages/web/src/lib/buildCalendarSql.ts
  - packages/web/src/lib/buildTimelineSql.ts
  - packages/web/src/lib/buildNumericLineSql.ts
  - packages/web/src/components/charts/ChartConfigPanel.tsx
  - packages/web/src/lib/buildCalendarSql.spec.ts
  - packages/web/src/lib/buildTimelineSql.spec.ts
  - packages/web/src/lib/buildNumericLineSql.spec.ts
autonomous: true
requirements: [METRIC-V119-04]
must_haves:
  truths:
    - "A metric selection can be marked custom by carrying an opaque metric id; absent id = real column (byte-identical)"
    - "At SQL-build time a custom selection emits the metric's raw expression with NO AGG(...) wrapper"
    - "A real-column selection emits AGG(column) exactly as before — byte-identical to current output"
    - "An orphaned custom id (deleted metric) resolves to no metric so the widget falls through to its existing empty/error state"
  artifacts:
    - path: "packages/web/src/lib/customMetricSql.ts"
      provides: "Shared resolve-selection → SQL-fragment helper (custom raw expression vs real AGG), orphan-safe"
      contains: "selectMetrics"
    - path: "packages/web/src/lib/timelineBin.ts"
      provides: "metricId marker field on TimelineMetric (shared by numeric-line)"
      contains: "metricId"
  key_links:
    - from: "packages/web/src/lib/customMetricSql.ts"
      to: "packages/web/src/store/customMetricsStore.ts"
      via: "selectMetrics(tableId) resolution of id → expression"
      pattern: "selectMetrics"
    - from: "packages/web/src/lib/buildTimelineSql.ts"
      to: "packages/web/src/lib/customMetricSql.ts"
      via: "resolveMetricExpr branch (custom raw vs real AGG)"
      pattern: "resolveMetricExpr|customMetricSql"
    - from: "packages/web/src/components/charts/ChartConfigPanel.tsx"
      to: "packages/web/src/lib/customMetricSql.ts"
      via: "generatedSql AGG emission branch (scalar + grouped)"
      pattern: "resolveMetricExpr|customMetricSql"
---

<objective>
Establish the custom-vs-real metric SELECTION CONTRACT and the shared SQL-emission branch used everywhere: a `metricId?: number` marker on the metric config shapes, and a pure helper `customMetricSql.ts` that — given `(metricId | undefined, realAggExpr, tableId)` — returns the metric's RAW `expression` (custom, resolved via `selectMetrics(tableId)`, NO AGG wrapper) or the `realAggExpr` unchanged (real column), or `null` when a custom id is orphaned. Branch all FOUR SQL-emission sites through it: ChartConfigPanel `generatedSql` (scalar + grouped), `buildCalendarSql`, `buildTimelineSql`, `buildNumericLineSql`. Lock backward-compat with byte-identical tests and lock the custom path with raw-expression tests.

Purpose: The trickiest coexistence piece (METRIC-V119-04). Defines the marker shape Plan 03's pickers write. Establishes the invariant: custom emitted with NO extra AGG wrapper; real-column widgets byte-identical.
Output: helper + spec, marker field on TimelineMetric + CalendarConfig + ChartConfigPanel draft, branched builders + ChartConfigPanel, regression + custom tests.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-custom-metrics-tables-area-editor-metric-picker-integration/100-CONTEXT.md

<interfaces>
<!-- Extracted from codebase — the exact current emission points to branch. -->

From packages/web/src/store/customMetricsStore.ts:
```typescript
export const selectMetrics: (tableId: number) => CustomMetricRow[]; // label-sorted; [] when absent
// CustomMetricRow.expression is the raw SQL aggregate (e.g. "SUM(revenue)/SUM(cost)")
```

From packages/web/src/lib/timelineBin.ts (shared by numeric-line via re-export as NumericMetric):
```typescript
export type TimelineMetric = { column: string; aggregation: TimelineAggregation; color: string; label?: string };
```

Current real-column AGG emission (the branch points):
- buildTimelineSql.ts aggExpr (~55-60): COUNT_DISTINCT → `COUNT(DISTINCT col)` else `AGG(col)`; used at ~93 `const agg = aggExpr(metric)`.
- buildNumericLineSql.ts aggExpr (~47-52) + ~90 `const agg = aggExpr(metric)`.
- buildCalendarSql.ts aggExpr (~50-55) + ~85 `const agg = aggExpr(aggregation, metricColumn)`.
- ChartConfigPanel.tsx generatedSql: scalar (~320-323) `const aggExpr = ...; return \`SELECT ${aggExpr} AS value FROM ${table}${cw}\`` and grouped (~331-339) `const aggExpr = ...; return \`SELECT ${groupByColumn}, ${aggExpr} AS value FROM ...\``.
</interfaces>

@packages/web/src/lib/buildTimelineSql.ts
@packages/web/src/lib/buildNumericLineSql.ts
@packages/web/src/lib/buildCalendarSql.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared customMetricSql helper + marker field + spec</name>
  <read_first>
    - packages/web/src/lib/customWhere.ts (the `andCustomWhere` pure-helper precedent — same shape: a pure lib that reads a store/config and returns a SQL fragment; mirror its purity + test style).
    - packages/web/src/store/customMetricsStore.ts (selectMetrics — the id→row lookup).
    - packages/web/src/lib/timelineBin.ts (TimelineMetric type to extend; NumericMetric is a re-export so it inherits the field automatically).
  </read_first>
  <behavior>
    - resolveMetricExpr(undefined, "SUM(x)", tableId) === "SUM(x)"  (real column → passthrough, byte-identical).
    - resolveMetricExpr(id, "SUM(x)", tableId) === "<raw expression>"  when selectMetrics(tableId) contains {id, expression:"SUM(revenue)/SUM(cost)"} → returns "SUM(revenue)/SUM(cost)" with NO surrounding AGG().
    - resolveMetricExpr(id, "SUM(x)", tableId) === null  when id not present in selectMetrics(tableId) (orphan / deleted metric).
    - isCustomSelection(metricId) is true only for a number, false for undefined/null.
  </behavior>
  <action>
    (a) Add an OPTIONAL marker to the metric shape in `packages/web/src/lib/timelineBin.ts`:
    `export type TimelineMetric = { column: string; aggregation: TimelineAggregation; color: string; label?: string; metricId?: number };`
    Because `NumericMetric` is `export type { TimelineMetric as NumericMetric }`, numeric-line inherits it automatically — do NOT duplicate.

    (b) Create `packages/web/src/lib/customMetricSql.ts` — a PURE helper (imports ONLY `selectMetrics` from the store, mirroring how customWhere is a pure fragment helper; zero React/Recharts):
    ```typescript
    import { selectMetrics } from "../store/customMetricsStore";

    /** A selection is custom iff it carries a numeric metricId. */
    export const isCustomSelection = (metricId: number | undefined | null): metricId is number =>
      typeof metricId === "number";

    /**
     * Resolve the SELECT aggregate fragment for a metric selection.
     * - real column (metricId undefined) → return realAggExpr UNCHANGED (byte-identical).
     * - custom (metricId number) → return the metric's RAW expression with NO AGG wrapper,
     *   resolved live via selectMetrics(tableId) so edits flow through.
     * - orphaned custom id (not found) → null; caller falls through to its existing empty/error state.
     */
    export function resolveMetricExpr(
      metricId: number | undefined | null,
      realAggExpr: string,
      tableId: number | undefined,
    ): string | null {
      if (!isCustomSelection(metricId)) return realAggExpr;
      if (tableId === undefined) return null;
      const found = selectMetrics(tableId).find((m) => m.id === metricId);
      return found ? found.expression : null;
    }
    ```

    (c) Create `packages/web/src/lib/customMetricSql.spec.ts` covering the four behaviors above. Seed the store via `useCustomMetricsStore.getState().setConfig(tableId, [{id, table_id, label, expression, format_spec:null, created_at, updated_at}])` before assertions; the zustand reset shim isolates tests.
  </action>
  <acceptance_criteria>
    - `test -f packages/web/src/lib/customMetricSql.ts` exits 0.
    - `grep -q "selectMetrics" packages/web/src/lib/customMetricSql.ts`
    - `grep -q "metricId" packages/web/src/lib/timelineBin.ts`
    - `cd packages/web && npx vitest run src/lib/customMetricSql.spec.ts` exits 0 (all four behaviors, incl. orphan→null and real→passthrough).
    - `cd packages/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/web && npx vitest run src/lib/customMetricSql.spec.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Helper returns raw expression for custom, passthrough for real, null for orphan; marker on TimelineMetric; spec green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Branch the 3 own-SQL builders (calendar / timeline / numeric-line) + byte-identical + custom tests</name>
  <read_first>
    - packages/web/src/lib/buildCalendarSql.ts (aggExpr ~50-55, emit ~85-99; BuildCalendarSqlArgs ~22-42).
    - packages/web/src/lib/buildTimelineSql.ts (aggExpr ~55-60, emit ~87-99; args ~18-43).
    - packages/web/src/lib/buildNumericLineSql.ts (aggExpr ~47-52, emit ~86-93; args ~20-44).
    - The Phase 98 byte-identical test precedent in each builder's existing spec (buildCalendarSql.spec.ts / buildTimelineSql.spec.ts / buildNumericLineSql.spec.ts) — mirror the "customWhere absent → byte-identical" test style for the metric branch.
  </read_first>
  <behavior>
    - CALENDAR: with NO metricId (real) → output is BYTE-IDENTICAL to the current builder (the `${agg}` still = `aggExpr(aggregation, metricColumn)`). With `metricId` set + store seeded → the `value` term is the raw `expression` (no AGG wrapper), i.e. `SELECT DATE_TRUNC(...) ..., <rawExpr> AS value FROM ...`.
    - TIMELINE: real metric → byte-identical (ungrouped + grouped). custom metric → `agg` term = raw expression, no AGG wrapper, both paths.
    - NUMERIC-LINE: real metric → byte-identical. custom metric → `agg` term = raw expression.
    - Orphaned custom id (store missing the metric) → the builder should emit a query that the renderer/existing empty-state handles; simplest: fall back to emitting the current real `aggExpr` is WRONG (would wrap). Instead, when resolveMetricExpr returns null, emit a benign no-value marker the existing empty-state catches — reuse the builder's existing "incomplete config" behavior if present, else emit the resolved-null case as documented below.
  </behavior>
  <action>
    In each of the three builders, import `resolveMetricExpr` from `./customMetricSql`. Add a `tableId?: number` field to each builder's Args type (calendar: `BuildCalendarSqlArgs`; timeline: `BuildTimelineSqlArgs`; numeric: `BuildNumericLineSqlArgs`) — used only to resolve custom ids; absent/real → byte-identical.

    Replace each `const agg = aggExpr(...)` (or `const agg = aggExpr(aggregation, metricColumn)` for calendar) with:
    ```typescript
    const realAgg = aggExpr(/* existing args */);
    const metricId = /* calendar: args.metricId; timeline/numeric: metric.metricId */;
    const resolved = resolveMetricExpr(metricId, realAgg, tableId);
    const agg = resolved ?? realAgg; // orphan → fall through so existing empty/error state applies
    ```
    Keep the SURROUNDING SQL string literally unchanged (same `${agg} AS value`, same FROM/WHERE/GROUP BY). CRITICAL byte-identical rule: when `metricId` is undefined, `resolveMetricExpr` returns `realAgg` UNCHANGED and `resolved ?? realAgg === realAgg`, so the emitted string is character-for-character identical — this is what the regression test locks.

    For calendar: add `metricId?: number` to `BuildCalendarSqlArgs`, read `args.metricId`; the caller (CalendarConfigPanel/Renderer, Plan 03) passes it.
    For timeline/numeric: the id lives on the `metric` object (`metric.metricId`) since these are per-metric-row; read it there.

    ORPHAN handling: `agg = resolved ?? realAgg` means an orphaned custom id currently falls back to the real aggExpr (which references `metric.column` — likely "" for a custom selection → invalid SQL → the widget's EXISTING query-error/empty state fires, no crash). This satisfies "falls back to its existing error/empty state" without a new code path. Document this in a comment at each site: `// orphaned custom id → realAgg (col likely "") → existing empty/error state, per Phase 100 orphan decision`.

    TESTS: extend each builder's spec:
    - A byte-identical assertion: build once with the current args (no metricId) and assert `toBe` the exact current expected string (copy the existing golden string). This LOCKS criterion 4 for these three builders.
    - A custom assertion: seed the store (`useCustomMetricsStore.getState().setConfig(tableId, [{id:7, ..., expression:"SUM(revenue)/SUM(cost)"}])`), build with `metricId:7` (+ `tableId`), assert the output contains `SUM(revenue)/SUM(cost) AS value` and does NOT contain a wrapping `SUM(SUM(` / `SUM()` artifact.
  </action>
  <acceptance_criteria>
    - `grep -q "resolveMetricExpr" packages/web/src/lib/buildCalendarSql.ts` AND buildTimelineSql.ts AND buildNumericLineSql.ts.
    - `grep -q "metricId" packages/web/src/lib/buildCalendarSql.ts` (arg added).
    - Byte-identical regression tests present + green: `cd packages/web && npx vitest run src/lib/buildCalendarSql.spec.ts src/lib/buildTimelineSql.spec.ts src/lib/buildNumericLineSql.spec.ts` exits 0.
    - Custom-emits-raw-expression test present in each of the three specs (grep the specs for `SUM(revenue)/SUM(cost)` or an equivalent raw-expression assertion).
    - `cd packages/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/web && npx vitest run src/lib/buildCalendarSql.spec.ts src/lib/buildTimelineSql.spec.ts src/lib/buildNumericLineSql.spec.ts && npx tsc --noEmit</automated>
  </verify>
  <done>All 3 builders branch through resolveMetricExpr; real → byte-identical (locked), custom → raw expression (locked), orphan → existing empty state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Branch ChartConfigPanel generatedSql (scalar + grouped) + marker on draft</name>
  <read_first>
    - packages/web/src/components/charts/ChartConfigPanel.tsx generatedSql memo (~289-340): the scalar branch (~316-323) and grouped branch (~328-339), and the deps array (~340). Note `selectedTable?.id` gives the tableId for table-bound; for dv-bound custom metrics are out of scope (dv column source), so resolve tableId as `selectedTable?.id`.
    - packages/web/src/lib/customMetricSql.ts (from Task 1).
  </read_first>
  <behavior>
    - Real-column selection (no `draft.metricId`) → generatedSql is BYTE-IDENTICAL to current for both scalar (bignumber) and grouped (bar/pie/line/area) — locked by an existing-behavior test.
    - Custom selection (`draft.metricId` set + store seeded) → scalar emits `SELECT <rawExpr> AS value FROM ...`; grouped emits `SELECT <groupByColumn>, <rawExpr> AS value FROM ... GROUP BY ... ORDER BY value ...` — NO AGG wrapper around the expression.
  </behavior>
  <action>
    In ChartConfigPanel.tsx:
    - Import `resolveMetricExpr` from `../../lib/customMetricSql`.
    - Inside `generatedSql`, in BOTH the scalar (`!requiresGroupBy`) and grouped branches, after computing the existing `const aggExpr = aggregation === "COUNT_DISTINCT" ? \`COUNT(DISTINCT ${metricColumn})\` : \`${aggregation}(${metricColumn})\``, add:
      ```typescript
      const metricId = draft.metricId as number | undefined;
      const tableId = selectedTable?.id;
      const metricExpr = resolveMetricExpr(metricId, aggExpr, tableId) ?? aggExpr;
      ```
      and substitute `${metricExpr}` for `${aggExpr}` in the returned SELECT string.
    - GUARD the "incomplete config" early-returns: when `metricId` is a number, the `if (!metricColumn || !aggregation)` incomplete-check should NOT trip (a custom selection has no metricColumn). Change the incomplete guard to: for scalar `if (!metricExpr) return \`SELECT * FROM ${table} LIMIT 100\`` — but compute metricExpr BEFORE the guard using a real aggExpr only when metricColumn+aggregation exist, else "". Concretely: if `isCustomSelection(metricId)` treat config as complete when the resolved expression is non-null; else keep the existing `!metricColumn || !aggregation` guard. Keep byte-identical output for the real path (the real branch and its placeholder string are untouched when metricId is undefined).
    - Add `draft.metricId` to the generatedSql deps array.
    - Add the marker to the draft type/handling only enough for TS: `draft.metricId` is read via `as number | undefined` (the draft is a `Record<string, unknown>`-style object; no type change needed if it already indexes by string — confirm from the file). Plan 03 sets it from the picker.
  </action>
  <acceptance_criteria>
    - `grep -q "resolveMetricExpr" packages/web/src/components/charts/ChartConfigPanel.tsx`
    - `grep -q "draft.metricId" packages/web/src/components/charts/ChartConfigPanel.tsx` AND it appears in the generatedSql deps array.
    - A ChartConfigPanel (or a dedicated generatedSql) test asserts: real-column scalar + grouped output byte-identical (golden strings) AND custom scalar/grouped emit the raw expression. Prefer extending an existing ChartConfigPanel spec; if none tests generatedSql, add `packages/web/src/components/charts/ChartConfigPanel.customMetric.spec.tsx` that renders the panel with a seeded store + custom draft and asserts the generated SQL (via the panel's SQL-preview element or the onChange payload). `cd packages/web && npx vitest run <that spec>` exits 0.
    - `cd packages/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/components/charts/</automated>
  </verify>
  <done>ChartConfigPanel emits raw expression for custom (scalar+grouped) and byte-identical SQL for real; metricId in deps; tests lock both.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx tsc --noEmit` clean.
- `cd packages/web && npx vitest run` 100% (byte-identical + custom tests green at all 4 emission sites).
- `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` green.
- `git diff --name-only packages/server` empty (FRONTEND-ONLY).
- Invariant: custom metric emitted with NO extra AGG wrapper (grep the custom tests for `SUM(revenue)/SUM(cost) AS value`, no `((`).
</verification>

<success_criteria>
A metric selection carrying a `metricId` emits its raw expression directly into the widget SELECT with no additional aggregation wrapper at ALL FOUR emission sites; real-column selections stay byte-identical; orphaned ids fall through to the existing empty/error state. (METRIC-V119-04.)
</success_criteria>

<output>
After completion, create `.planning/phases/100-custom-metrics-tables-area-editor-metric-picker-integration/100-02-SUMMARY.md`.
</output>
