---
phase: 100-custom-metrics-tables-area-editor-metric-picker-integration
plan: 03
type: execute
wave: 2
depends_on: ["100-02"]
files_modified:
  - packages/web/src/lib/customMetricSql.ts
  - packages/web/src/components/charts/ChartConfigPanel.tsx
  - packages/web/src/components/charts/CalendarConfigPanel.tsx
  - packages/web/src/components/charts/TimelineConfigPanel.tsx
  - packages/web/src/components/charts/NumericLineConfigPanel.tsx
  - packages/web/src/components/charts/CalendarRenderer.tsx
  - packages/web/src/components/charts/TimelineRenderer.tsx
  - packages/web/src/components/charts/NumericLineRenderer.tsx
  - packages/web/src/components/charts/metricPicker.spec.tsx
autonomous: true
requirements: [METRIC-V119-01, METRIC-V119-03]
must_haves:
  truths:
    - "Custom metrics appear in a distinct 'Custom metrics' group in every metric picker (bar/pie/line/area/big-number, calendar, timeline incl. multi-metric, numeric-line incl. multi-metric)"
    - "Selecting a custom metric writes its id (metricId marker) into config and HIDES the aggregation selector"
    - "Custom metrics always appear regardless of the numeric-only / drilldown-safe column filter"
    - "A widget referencing a deleted metric shows a '(deleted metric)' marker in the picker and the widget falls back to its existing empty/error state"
    - "The renderers pass tableId to the builders so custom ids resolve to raw expressions at build time"
  artifacts:
    - path: "packages/web/src/components/charts/CalendarConfigPanel.tsx"
      provides: "Custom metrics optgroup + hide-agg-when-custom + metricId write"
      contains: "Custom metrics"
    - path: "packages/web/src/components/charts/TimelineConfigPanel.tsx"
      provides: "Per-metric-row custom optgroup + hide-agg + metricId marker"
      contains: "Custom metrics"
  key_links:
    - from: "packages/web/src/components/charts/ChartConfigPanel.tsx"
      to: "packages/web/src/store/customMetricsStore.ts"
      via: "selectMetrics(selectedTable.id) appended to the metric picker"
      pattern: "selectMetrics"
    - from: "packages/web/src/components/charts/CalendarRenderer.tsx"
      to: "packages/web/src/lib/buildCalendarSql.ts"
      via: "tableId + metricId threaded into buildCalendarSql"
      pattern: "tableId"
    - from: "packages/web/src/components/charts/TimelineRenderer.tsx"
      to: "packages/web/src/lib/buildTimelineSql.ts"
      via: "tableId threaded so metric.metricId resolves"
      pattern: "tableId"
---

<objective>
Surface custom metrics in every metric-configuring widget's picker and thread the selection through to SQL build time. Each picker gains a distinct "Custom metrics" `<optgroup>` (always shown, regardless of the numeric-only/drilldown-safe filter); selecting a custom metric writes the `metricId` marker (Plan 02 contract) and HIDES the aggregation selector; a stored id no longer in `selectMetrics(tableId)` renders a "(deleted metric)" option so the picker never shows a blank + the widget falls through to its existing empty/error state. Renderers pass `tableId` into the builders so `resolveMetricExpr` resolves the live expression.

Purpose: METRIC-V119-03 (appear in every picker) + METRIC-V119-01 picker half. Scatter excluded (no metric picker).
Output: 4 config panels + 3 renderers updated (+ ChartConfigPanel already-branched generatedSql now fed by the picker), one integration spec. A small shared `renderMetricOptions` helper in customMetricSql.ts to avoid 4× duplication of the optgroup + deleted-marker logic.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-custom-metrics-tables-area-editor-metric-picker-integration/100-CONTEXT.md

<interfaces>
<!-- Plan 02 contract (already on disk when this runs) + the picker sites. -->

From packages/web/src/lib/customMetricSql.ts (Plan 02):
```typescript
export const isCustomSelection: (metricId: number | undefined | null) => metricId is number;
export function resolveMetricExpr(metricId, realAggExpr, tableId): string | null;
```
From packages/web/src/store/customMetricsStore.ts:
```typescript
export const selectMetrics: (tableId: number) => CustomMetricRow[]; // {id,label,expression,...}
```
Marker (Plan 02): TimelineMetric/NumericMetric gained `metricId?: number`; CalendarConfig + ChartConfigPanel draft carry `metricId?: number` (calendar: add to CalendarConfig type here if Plan 02 did not).

Picker sites (current):
- ChartConfigPanel.tsx: Metric Column `<select>` ~596-613 (numericColumns), Aggregation `<select>` ~615-627, `allColumns` ~210-216, `numericColumns` ~269-272. `selectedTable?.id` = tableId.
- CalendarConfigPanel.tsx: metric `<select>` ~468-483 (metricColumns ~212-222), aggregation `<select>` ~486-501, cfg.tableId available.
- TimelineConfigPanel.tsx: per-row metric `<select>` ~393 (metricColumns ~116-126), aggregation select nearby; metrics[] rows; cfg.tableId.
- NumericLineConfigPanel.tsx: per-row metric `<select>` ~371 (metricColumns ~108-117); cfg.tableId.

Renderers (thread tableId in):
- CalendarRenderer.tsx buildCalendarSql call ~300-308 (has cfg.tableId as `tableId`).
- TimelineRenderer.tsx buildTimelineSql calls ~329 + ~366 (has `tableId`); metric objects carry metricId.
- NumericLineRenderer.tsx buildNumericLineSql calls ~305 + ~344.
</interfaces>

@packages/web/src/components/charts/ChartConfigPanel.tsx
@packages/web/src/components/charts/CalendarConfigPanel.tsx
@packages/web/src/components/charts/TimelineConfigPanel.tsx
@packages/web/src/components/charts/NumericLineConfigPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared picker helpers (encode/decode custom option value + deleted-marker) in customMetricSql.ts</name>
  <read_first>
    - packages/web/src/lib/customMetricSql.ts (Plan 02 — extend, do not rewrite the resolve logic).
    - packages/web/src/store/customMetricsStore.ts selectMetrics.
    - One existing `<optgroup>` usage in CalendarConfigPanel.tsx (~414-433) for the markup convention to reuse (native `<optgroup label="...">`).
  </read_first>
  <action>
    Extend `customMetricSql.ts` with a pure encode/decode + option-model helper so the 4 pickers stay DRY. Use a sentinel-prefixed option value so a single `<select value>` can represent BOTH real columns (value = column name) and custom metrics (value = `cm:<id>`):
    ```typescript
    const CUSTOM_PREFIX = "cm:";
    export const encodeCustomValue = (id: number): string => `${CUSTOM_PREFIX}${id}`;
    export const decodeMetricSelection = (value: string):
      | { kind: "real"; column: string }
      | { kind: "custom"; metricId: number } => {
      if (value.startsWith(CUSTOM_PREFIX)) return { kind: "custom", metricId: Number(value.slice(CUSTOM_PREFIX.length)) };
      return { kind: "real", column: value };
    };
    /** The <select value> to show for a stored selection (metricId wins over column). */
    export const metricSelectValue = (metricId: number | undefined, column: string): string =>
      isCustomSelection(metricId) ? encodeCustomValue(metricId) : column;
    /** Is a stored custom id orphaned (deleted) for this table? */
    export const isOrphanedMetric = (metricId: number | undefined, tableId: number | undefined): boolean =>
      isCustomSelection(metricId) && (tableId === undefined || !selectMetrics(tableId).some((m) => m.id === metricId));
    ```
    Add unit tests to `customMetricSql.spec.ts` for encode/decode round-trip, metricSelectValue (custom vs real), and isOrphanedMetric (present vs missing).
  </action>
  <acceptance_criteria>
    - `grep -Eq "encodeCustomValue|decodeMetricSelection|metricSelectValue|isOrphanedMetric" packages/web/src/lib/customMetricSql.ts`
    - `cd packages/web && npx vitest run src/lib/customMetricSql.spec.ts` exits 0 (encode/decode + orphan cases green).
    - `cd packages/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/web && npx vitest run src/lib/customMetricSql.spec.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Encode/decode + select-value + orphan helpers exist and are tested.</done>
</task>

<task type="auto">
  <name>Task 2: Picker integration in ChartConfigPanel + CalendarConfigPanel</name>
  <read_first>
    - packages/web/src/components/charts/ChartConfigPanel.tsx metric+agg selects (~594-627), allColumns/numericColumns memos (~210-272), the `set(key, value)` draft mutator, generatedSql metricId branch (Plan 02).
    - packages/web/src/components/charts/CalendarConfigPanel.tsx metric+agg selects (~468-501), metricColumns (~212-222), CalendarConfig type (~45-62), the `patch(...)` mutator (~472).
    - customMetricSql helpers from Task 1.
  </read_first>
  <action>
    Common pattern for BOTH panels:
    - Compute `const customMetrics = selectMetrics(tableId)` where tableId = `selectedTable?.id` (ChartConfigPanel) / `cfg.tableId` (Calendar). Subscribe to `useCustomMetricsStore((s) => s.configVersion)` so the list re-renders after edits; call `loadConfig(tableId)` in a mount effect if not already loaded (Calendar renderer already loads; for the config panel add a `useEffect` calling `useCustomMetricsStore.getState().loadConfig(tableId)` when tableId is set).
    - Metric `<select>`: set its `value` via `metricSelectValue(draft.metricId as number | undefined, metricColumn)`. Keep the existing real-column `<option>`s inside an `<optgroup label="Columns">` (or leave them ungrouped for the real list and add ONLY the custom group — pick grouped for clarity). Append `<optgroup label="Custom metrics">{customMetrics.map((m) => <option key={m.id} value={encodeCustomValue(m.id)}>{m.label}</option>)}</optgroup>` — ALWAYS rendered regardless of the numeric-only filter (customMetrics is independent of numericColumns).
    - DELETED marker: if `isOrphanedMetric(draft.metricId, tableId)` is true, render an extra `<option value={encodeCustomValue(draft.metricId)}>(deleted metric)</option>` (inside the custom optgroup or standalone) so the select shows "(deleted metric)" instead of a blank; do NOT auto-clear the stored id (widget falls through to its existing empty/error state).
    - onChange: `const sel = decodeMetricSelection(e.target.value);` — if `sel.kind === "custom"`: write `metricId = sel.metricId` and CLEAR the real column/aggregation appropriately (ChartConfigPanel: `set("metricId", sel.metricId); set("metricColumn", ""); ` keep aggregation untouched but it will be hidden; Calendar: `patch({ metricId: sel.metricId, metricColumn: "" })`). If `sel.kind === "real"`: write the column AND clear the marker (`set("metricId", undefined); set("metricColumn", sel.column)` / `patch({ metricId: undefined, metricColumn: sel.column })`).
    - HIDE aggregation selector when a custom metric is selected: wrap the Aggregation `<select>` block in `{!isCustomSelection(draft.metricId as number | undefined) && (<...>)}` (ChartConfigPanel) / `{!isCustomSelection(cfg.metricId) && (...)}` (Calendar). Real-column behavior unchanged.
    - CalendarConfig type: if Plan 02 did not add it, add `metricId?: number;` to `CalendarConfig` in CalendarConfigPanel.tsx.
    - Reuse ONLY existing classNames (`ds-field`, `ds-field-label`, `ds-select`, native `<optgroup>`); NO invented classes; theme tokens only.
  </action>
  <acceptance_criteria>
    - `grep -q "Custom metrics" packages/web/src/components/charts/ChartConfigPanel.tsx` AND `grep -q "Custom metrics" packages/web/src/components/charts/CalendarConfigPanel.tsx`
    - `grep -q "selectMetrics" packages/web/src/components/charts/ChartConfigPanel.tsx` AND CalendarConfigPanel.tsx.
    - `grep -q "isCustomSelection" packages/web/src/components/charts/ChartConfigPanel.tsx` (agg-hide guard) AND CalendarConfigPanel.tsx.
    - `grep -q "(deleted metric)" packages/web/src/components/charts/ChartConfigPanel.tsx` AND CalendarConfigPanel.tsx.
    - `grep -q "metricId" packages/web/src/components/charts/CalendarConfigPanel.tsx` (CalendarConfig field present).
    - `cd packages/web && npx tsc --noEmit` exits 0; theme-guard green.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/styles/theme-guard.spec.ts</automated>
  </verify>
  <done>ChartConfigPanel + Calendar pickers show a Custom metrics group (always), hide agg when custom, mark deleted ids, write metricId; tsc + theme-guard green.</done>
</task>

<task type="auto">
  <name>Task 3: Picker integration in Timeline + NumericLine multi-metric rows</name>
  <read_first>
    - packages/web/src/components/charts/TimelineConfigPanel.tsx per-metric-row metric `<select>` (~385-410 region), aggregation select in the same row, `metricColumns` (~116-126), the metrics[] row mutator (updateMetric / setMetrics pattern), cfg.tableId.
    - packages/web/src/components/charts/NumericLineConfigPanel.tsx per-row metric `<select>` (~363-385 region), metricColumns (~108-117), row mutator, cfg.tableId.
    - customMetricSql helpers (Task 1) + the marker on TimelineMetric/NumericMetric (Plan 02).
  </read_first>
  <action>
    For BOTH panels, apply the SAME pattern as Task 2 but PER metrics[] row (each row is a TimelineMetric/NumericMetric carrying its own optional `metricId`):
    - `const customMetrics = selectMetrics(cfg.tableId)`; subscribe to configVersion; mount-effect loadConfig(cfg.tableId).
    - Each row's metric `<select value>` = `metricSelectValue(m.metricId, m.column)`.
    - Append `<optgroup label="Custom metrics">` of `<option value={encodeCustomValue(mm.id)}>{mm.label}</option>` (always shown). Add the "(deleted metric)" option when `isOrphanedMetric(m.metricId, cfg.tableId)`.
    - Row onChange: `decodeMetricSelection` → custom: update that row to `{ ...m, metricId: sel.metricId, column: "" }`; real: `{ ...m, metricId: undefined, column: sel.column }` (via the existing updateMetric(idx, next) mutator).
    - HIDE that row's aggregation `<select>` when `isCustomSelection(m.metricId)` (each row independently).
    - Multi-metric completeness guards (e.g. `metrics.every((mm) => mm.column !== "" && ...)`) must also treat a custom row as complete: update those `configComplete`/valid checks to `metrics.every((mm) => isCustomSelection(mm.metricId) || (mm.column !== "" && mm.aggregation !== undefined))`. Do the same for the grouped-mode `m0` completeness check.
    - Reuse existing classNames only; theme tokens only.
  </action>
  <acceptance_criteria>
    - `grep -q "Custom metrics" packages/web/src/components/charts/TimelineConfigPanel.tsx` AND NumericLineConfigPanel.tsx.
    - `grep -q "isCustomSelection" packages/web/src/components/charts/TimelineConfigPanel.tsx` AND NumericLineConfigPanel.tsx (agg-hide + completeness).
    - `grep -q "(deleted metric)" packages/web/src/components/charts/TimelineConfigPanel.tsx` AND NumericLineConfigPanel.tsx.
    - `grep -q "encodeCustomValue" packages/web/src/components/charts/TimelineConfigPanel.tsx` AND NumericLineConfigPanel.tsx.
    - `cd packages/web && npx tsc --noEmit` exits 0; theme-guard green.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/styles/theme-guard.spec.ts</automated>
  </verify>
  <done>Timeline + NumericLine multi-metric rows each show the Custom metrics group, hide per-row agg when custom, mark deleted ids, and treat custom rows as complete.</done>
</task>

<task type="auto">
  <name>Task 4: Thread tableId into the 3 renderers' builder calls + integration spec</name>
  <read_first>
    - packages/web/src/components/charts/CalendarRenderer.tsx buildCalendarSql call (~300-308) — cfg.tableId is `tableId` in scope (~verify).
    - packages/web/src/components/charts/TimelineRenderer.tsx buildTimelineSql calls (~329 grouped, ~366 ungrouped) — `tableId` in scope (~115).
    - packages/web/src/components/charts/NumericLineRenderer.tsx buildNumericLineSql calls (~305, ~344).
    - metricPicker markup from Tasks 2-3 for the integration spec.
  </read_first>
  <action>
    - CalendarRenderer: pass `tableId` (and `metricId: cfg.metricId`) into the `buildCalendarSql({...})` args object. The renderer already loads customMetrics via the store (or add `loadConfig(tableId)` if absent) and re-runs on configVersion — add `configVersion` to the fetch effect's dep array so an edited expression re-queries.
    - TimelineRenderer: add `tableId` to BOTH buildTimelineSql calls (grouped ~329 + ungrouped ~366). The per-metric `metricId` already rides on the `metric` object. Add `configVersion` (from useCustomMetricsStore) to the fetch effect deps + ensure loadConfig(tableId) is called on mount.
    - NumericLineRenderer: same — add `tableId` to both buildNumericLineSql calls + configVersion dep + loadConfig.
    - Integration spec `packages/web/src/components/charts/metricPicker.spec.tsx`: seed the store with a custom metric; render ChartConfigPanel (or the relevant panel) for a table binding; assert (a) the "Custom metrics" optgroup + the metric's label render; (b) selecting the custom option hides the Aggregation control (query absence of the agg `<select>` / its aria-label); (c) with a stored metricId whose row was removed from the store, the picker shows "(deleted metric)". Prefer testing-library queries by role/label. Keep to one representative panel plus a Timeline row assertion if cheap.
  </action>
  <acceptance_criteria>
    - `grep -q "tableId" packages/web/src/components/charts/CalendarRenderer.tsx` in the buildCalendarSql args (the call now passes tableId).
    - Timeline + NumericLine renderers pass `tableId` into their builder calls (grep the call sites).
    - `grep -q "configVersion" packages/web/src/components/charts/TimelineRenderer.tsx` (re-query on metric edit).
    - `cd packages/web && npx vitest run src/components/charts/metricPicker.spec.tsx` exits 0 (optgroup + hide-agg + deleted-marker cases).
    - Full suite green: `cd packages/web && npx vitest run` exits 0; `npx tsc --noEmit` clean; theme-guard green.
    - `git diff --name-only packages/server` empty.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>Renderers resolve custom ids to live expressions (tableId + configVersion threaded); integration spec locks optgroup + hide-agg + deleted-marker; full web gates green; zero server diff.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx tsc --noEmit` clean.
- `cd packages/web && npx vitest run` 100% (picker + resolution + Plan 02 byte-identical/custom tests all green).
- `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` green (no invented classNames, no raw hex).
- `git diff --name-only packages/server` empty (FRONTEND-ONLY).
- Custom metrics appear in ALL 4 picker sites; agg hidden when custom; scatter untouched (no metric picker).
- Orphan: "(deleted metric)" shown; widget falls to existing empty/error state (no crash, no auto-clear).
</verification>

<success_criteria>
Custom metrics appear in every visualization's metric picker in a distinct group (always shown), selecting one writes its id + hides the aggregation selector, deleted metrics show "(deleted metric)" and degrade gracefully, and renderers resolve the live expression at build time. (METRIC-V119-03 + METRIC-V119-01 picker half.)
</success_criteria>

<output>
After completion, create `.planning/phases/100-custom-metrics-tables-area-editor-metric-picker-integration/100-03-SUMMARY.md`.
</output>
