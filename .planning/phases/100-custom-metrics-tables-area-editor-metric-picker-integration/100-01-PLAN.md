---
phase: 100-custom-metrics-tables-area-editor-metric-picker-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/CustomMetricsEditorModal.tsx
  - packages/web/src/components/CustomMetricsEditorModal.css
  - packages/web/src/components/DatasetsPage.tsx
  - packages/web/src/components/CustomMetricsEditorModal.spec.tsx
autonomous: true
requirements: [METRIC-V119-01]
must_haves:
  truths:
    - "From the Tables area, a user opens a Custom metrics editor for a table"
    - "The user can add a new metric (label + SQL expression + optional default format) and it persists via the API"
    - "The user can edit an existing metric's label/expression/format and delete a metric"
    - "A duplicate label surfaces the server 409 as an inline error, not a crash"
  artifacts:
    - path: "packages/web/src/components/CustomMetricsEditorModal.tsx"
      provides: "Two-pane CRUD editor modal for a table's custom metrics"
      contains: "useCustomMetricsStore"
    - path: "packages/web/src/components/DatasetsPage.tsx"
      provides: "Custom metrics button + modal render on TableDetail"
      contains: "CustomMetricsEditorModal"
  key_links:
    - from: "packages/web/src/components/CustomMetricsEditorModal.tsx"
      to: "packages/web/src/api/client.ts"
      via: "createCustomMetric / updateCustomMetric / deleteCustomMetric"
      pattern: "createCustomMetric|updateCustomMetric|deleteCustomMetric"
    - from: "packages/web/src/components/DatasetsPage.tsx"
      to: "CustomMetricsEditorModal"
      via: "TableDetail button + conditional render (mirrors Format columns)"
      pattern: "CustomMetricsEditorModal"
---

<objective>
Build the Tables-area authoring UI for custom metrics: a new `CustomMetricsEditorModal` (two-pane growable CRUD list mirroring `ColumnFormatEditorModal`) reached from `DatasetsPage.tsx` `TableDetail` via a button next to "Format columns". Left pane = the table's metrics + "Add metric"; right pane = the edit form (label + SQL expression textarea + optional default format via the existing `FormatSpecEditor`). Load/save through the Phase 99 `customMetricsStore` + api client CRUD.

Purpose: Completes the authoring half of METRIC-V119-01 (server half shipped Phase 99). No picker integration here — that is Plan 03.
Output: `CustomMetricsEditorModal.tsx` (+ `.css`), the DatasetsPage reach, and a component spec.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-custom-metrics-tables-area-editor-metric-picker-integration/100-CONTEXT.md
@.planning/phases/99-custom-metrics-server-store-foundation/99-CONTEXT.md

<interfaces>
<!-- Phase 99 foundation — consume, do NOT modify. Extracted from codebase. -->

From packages/web/src/api/client.ts:
```typescript
export type CustomMetricRow = {
  id: number;
  table_id: number;
  label: string;
  expression: string;
  format_spec: FormatSpec | null;
  created_at: string;
  updated_at: string;
};
export const listCustomMetrics: (tableId: number) => Promise<CustomMetricRow[]>;
export const createCustomMetric: (tableId: number, label: string, expression: string, formatSpec: FormatSpec | null) => Promise<CustomMetricRow>;
export const updateCustomMetric: (tableId: number, id: number, label: string, expression: string, formatSpec: FormatSpec | null) => Promise<CustomMetricRow>;
export const deleteCustomMetric: (tableId: number, id: number) => Promise<void>;
```

From packages/web/src/store/customMetricsStore.ts:
```typescript
export const useCustomMetricsStore: <...>;  // state: { configs, configVersion, setConfig, upsertMetric, removeMetric, loadConfig, reset }
export const selectMetrics: (tableId: number) => CustomMetricRow[]; // label-sorted, [] when absent
// loadConfig(tableId): fetch listCustomMetrics + setConfig; upsertMetric/removeMetric mutate cache
```

From packages/web/src/lib/columnFormatter.ts + ./charts/FormatSpecEditor:
```typescript
export type FormatSpec = FormatSpecNone | FormatSpecNumber | FormatSpecDate | FormatSpecD3 | FormatSpecSI;
// FormatSpecEditor exports: NumberControls, DateControls, D3Controls, SIControls, defaultSpecForKind(kind)
```

Reuse the SAME modal classNames as ColumnFormatEditorModal (all already in global.css / its own css):
`modal-overlay`, `modal-content`, `modal-header`, `modal-title`, `modal-left`, `modal-right`,
plus form conventions `ds-field`, `ds-field-label`, `ds-select`, `config-group`, `config-group-label`,
and buttons `btn-primary`, `ghost-sm`, `ghost-sm ghost-danger`, `ds-actions`.
The `.css` file may add ONLY layout classes analogous to `col-format-editor-*` (grid/scroll), theme-tokens only, NO raw hex.
</interfaces>

@packages/web/src/components/ColumnFormatEditorModal.tsx
@packages/web/src/components/DatasetsPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CustomMetricsEditorModal (two-pane CRUD) + CSS</name>
  <read_first>
    - packages/web/src/components/ColumnFormatEditorModal.tsx (FULL — the two-pane structure ~261-327, load-on-mount ~81-123, save via store+api ~187-231, dirty-guard close ~144-159, sub-form ~344-438). Mirror its skeleton.
    - packages/web/src/styles/global.css §~4531-4636 (the `col-format-editor-*` layout classes to analogize; there is NO ColumnFormatEditorModal.css — the modal imports no CSS, its classes live in global.css). Also §~744-776 for the shared `modal-*` classes.
    - packages/web/src/store/customMetricsStore.ts (loadConfig, selectMetrics, upsertMetric, removeMetric).
    - packages/web/src/api/client.ts custom-metric CRUD block (CustomMetricRow + create/update/delete).
    - packages/web/src/components/charts/FormatSpecEditor.tsx (NumberControls/DateControls/D3Controls/SIControls/defaultSpecForKind — reuse verbatim for the optional format).
  </read_first>
  <action>
    Create `packages/web/src/components/CustomMetricsEditorModal.tsx`, default-export a component `CustomMetricsEditorModal({ table, onClose }: { table: TableDto; onClose: () => void })`. Adapt ColumnFormatEditorModal from a fixed column list to a GROWABLE metric CRUD list:

    STATE:
    - On mount (useEffect keyed on `table.id`, cancelled-guard pattern): call `useCustomMetricsStore.getState().loadConfig(table.id)`, then seed local list from `selectMetrics(table.id)`. On error, `useToastStore.getState().showToast("Failed to load custom metrics: " + msg, "error")`.
    - `const rows = useCustomMetricsStore((s) => { void s.configVersion; return selectMetrics(table.id); })` OR read from store getState after each mutation — pick the reactive form so the left list re-renders after create/delete (subscribe to configVersion, then call selectMetrics).
    - `selectedId: number | null` (the metric being edited) plus a sentinel `"new"` mode for the Add form.
    - Working form state: `{ label: string; expression: string; spec: FormatSpec }` for the currently-edited/new metric; seed spec via `defaultSpecForKind("none")` for new, or from `row.format_spec ?? defaultSpecForKind("none")` when selecting an existing row.
    - `saving: boolean`, `formError: string | null` (for inline 409 / validation).

    LAYOUT (reuse ColumnFormatEditorModal classNames exactly):
    - `<div className="modal-overlay" onClick={handleCloseRequest}>` → `<div className="modal-content custom-metrics-editor-modal" onClick={stopPropagation}>`.
    - Header: `modal-header` with `modal-title` = `Custom metrics — {table.name}` + a `ghost-sm` Close button (calls handleCloseRequest).
    - Body wrapper `custom-metrics-editor-body` (analogous to `col-format-editor-body`), containing:
      - LEFT `modal-left custom-metrics-editor-left`: one row per metric (`custom-metric-row`, `active` when selectedId===row.id, role="button", tabIndex, Enter/Space handler) showing `row.label`; below the list an "Add metric" affordance — a `ghost-sm` button that sets mode to "new" and resets the working form.
      - RIGHT `modal-right custom-metrics-editor-right`: the edit/new form (see below), or an empty-state `custom-metrics-editor-empty` "Select a metric or add a new one." when nothing selected.

    FORM (right pane):
    - `ds-field` → `ds-field-label` "Label" + `<input type="text">` bound to working.label.
    - `ds-field` → `ds-field-label` "SQL expression" + `<textarea>` bound to working.expression (placeholder e.g. `SUM(revenue) / SUM(cost)`). aria-label "SQL expression".
    - Optional default format: reuse the SAME kind picker + kind-specific controls block from ColumnFormatEditorModal's ColumnEditorForm (Format kind `<select className="ds-select">` with none/number/date/d3/si; then conditional NumberControls/DateControls/D3Controls/SIControls). On kind change call `defaultSpecForKind(kind)`.
    - Inline error: when `formError` non-null, render `<div className="config-hint custom-metrics-editor-error">{formError}</div>`. IMPORTANT: `config-hint-warning` is NOT a defined CSS class (it is a silent no-op / latent bug at ChartConfigPanel.tsx:588 — do NOT copy it; an undefined className passes tsc/vitest/theme-guard but renders unstyled). Define a REAL `.custom-metrics-editor-error { color: var(--danger); }` rule in the new `.css` (theme token, no raw hex) so the error renders styled. `config-hint` IS defined (global.css:1238) and is fine for the base.
    - Actions row `<div className="ds-actions">`: primary Save (`btn-primary btn-sm`, disabled when saving OR label.trim()==="" OR expression.trim()==="") + for an existing metric a `ghost-sm ghost-danger` Delete.

    HANDLERS:
    - `handleSave`: client-validate non-empty trimmed label + expression (else set formError "Label and expression are required." and return). Set saving. If mode "new": `const created = await createCustomMetric(table.id, label, expression, spec.kind === "none" ? null : spec)`; then `useCustomMetricsStore.getState().upsertMetric(table.id, created)`; select the new id. If editing existing id: `const updated = await updateCustomMetric(table.id, id, label, expression, spec.kind === "none" ? null : spec)`; then `upsertMetric(table.id, updated)`. On success clear formError, toast "Custom metric saved". CATCH: read `(err as Error).message`. `throwForStatus` surfaces the server's `body.error` text (NOT the numeric status), so the Phase 99 route's exact 409 text is `"A custom metric with this label already exists on this table."` — set `formError` directly to that surfaced message (or, if you want a match guard, test `message.toLowerCase().includes("already exists")`). Do NOT match on the substring `"409"` — it is never in the message. Finally clear saving. Do NOT close on error.
    - `handleDelete`: `window.confirm("Delete this metric?")` guard; `await deleteCustomMetric(table.id, id)`; `useCustomMetricsStore.getState().removeMetric(table.id, id)`; clear selection; toast "Custom metric deleted".
    - `handleCloseRequest`: if the working form is dirty vs the selected row (or non-empty in "new" mode), `window.confirm("Discard unsaved changes?")` guard; else `onClose()`. Add ESC-key listener mirroring ColumnFormatEditorModal.

    CSS: create `packages/web/src/components/CustomMetricsEditorModal.css` with layout analogous to the `col-format-editor-*` classes in `global.css` (§~4531-4636) — define `.custom-metrics-editor-body` (two-column flex/grid), `.custom-metrics-editor-left` (scroll), `.custom-metrics-editor-right`, `.custom-metric-row` (+ `.active`), `.custom-metrics-editor-empty`, AND `.custom-metrics-editor-error { color: var(--danger); }` (the inline-error class from the FORM section — it must be a REAL rule, NOT the phantom `config-hint-warning`). THEME TOKENS ONLY (var(--border), var(--panel), var(--accent), var(--text), var(--muted), var(--danger)); NO raw hex. Import the css at the top of the .tsx (`import "./CustomMetricsEditorModal.css";`).
  </action>
  <acceptance_criteria>
    - `test -f packages/web/src/components/CustomMetricsEditorModal.tsx` exits 0.
    - `grep -q "useCustomMetricsStore" packages/web/src/components/CustomMetricsEditorModal.tsx`
    - `grep -Eq "createCustomMetric|updateCustomMetric|deleteCustomMetric" packages/web/src/components/CustomMetricsEditorModal.tsx`
    - `grep -q "selectMetrics" packages/web/src/components/CustomMetricsEditorModal.tsx`
    - `grep -q "modal-overlay" packages/web/src/components/CustomMetricsEditorModal.tsx` AND `grep -q "modal-left" ...` AND `grep -q "modal-right" ...` (reused classes present).
    - `grep -Ei "already exists|unique" packages/web/src/components/CustomMetricsEditorModal.tsx` (409 inline handling present).
    - No raw hex introduced: `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` exits 0.
    - `cd packages/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/styles/theme-guard.spec.ts</automated>
  </verify>
  <done>Modal exists, imports Phase 99 store + api CRUD, mirrors ColumnFormatEditorModal classNames, surfaces 409 inline, tsc + theme-guard green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the Custom metrics button + modal render into DatasetsPage TableDetail</name>
  <read_first>
    - packages/web/src/components/DatasetsPage.tsx TableDetail (~124-192): the `showFormatEditor` state (~126), the "Format columns" button (~135-137), and the conditional `<ColumnFormatEditorModal .../>` render (~184-189). Mirror exactly.
  </read_first>
  <action>
    In `DatasetsPage.tsx` `TableDetail`:
    - Add `import CustomMetricsEditorModal from "./CustomMetricsEditorModal";` alongside the ColumnFormatEditorModal import.
    - Add `const [showMetricsEditor, setShowMetricsEditor] = useState(false);` next to `showFormatEditor`.
    - In the `actions` prop `<>...</>`, add a `<button className="ghost-sm" onClick={() => setShowMetricsEditor(true)}>Custom metrics</button>` immediately AFTER the "Format columns" button (before "Back").
    - At the bottom, next to the existing `{showFormatEditor && <ColumnFormatEditorModal .../>}`, add:
      `{showMetricsEditor && (<CustomMetricsEditorModal table={table} onClose={() => setShowMetricsEditor(false)} />)}`.
    - Do NOT alter any other TableDetail behavior. Zero server-side change.
  </action>
  <acceptance_criteria>
    - `grep -q "CustomMetricsEditorModal" packages/web/src/components/DatasetsPage.tsx`
    - `grep -q "Custom metrics" packages/web/src/components/DatasetsPage.tsx`
    - `grep -q "showMetricsEditor" packages/web/src/components/DatasetsPage.tsx`
    - `cd packages/web && npx tsc --noEmit` exits 0.
    - `git diff --name-only packages/server` prints NOTHING (zero server diff).
  </acceptance_criteria>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && test -z "$(cd /Users/rydelpereira/Documents/projects/kinetica_bi && git diff --name-only packages/server)"</automated>
  </verify>
  <done>TableDetail shows a "Custom metrics" button that opens the modal; zero server diff.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Component spec — CRUD lifecycle + 409 inline error</name>
  <read_first>
    - An existing component spec that mocks the api client + store, e.g. a spec near ColumnFormatEditorModal (grep `packages/web/src -l "ColumnFormatEditorModal"` for its test, or the nearest modal spec) for the mocking idiom (vi.mock api/client, render, fireEvent, waitFor).
    - packages/web/src/test/setup.ts (zustand reset shim auto-applies).
  </read_first>
  <behavior>
    - Renders with a table having no metrics → shows the empty/right-pane placeholder + an "Add metric" affordance.
    - Add flow: click Add metric, type label + expression, click Save → `createCustomMetric` called with (tableId, label, expression, null) and the new metric appears in the left list.
    - Duplicate-label flow: mock `createCustomMetric` to reject with `new Error("A custom metric with this label already exists on this table.")` (the real Phase 99 server 409 text surfaced by throwForStatus) → an inline error (`.custom-metrics-editor-error`) is shown with that message and `onClose` is NOT called.
    - Delete flow: with an existing metric selected, confirm() true → `deleteCustomMetric` called and the row disappears.
  </behavior>
  <action>
    Create `packages/web/src/components/CustomMetricsEditorModal.spec.tsx`. `vi.mock("../api/client", ...)` returning stubbed `listCustomMetrics` (resolves []), `createCustomMetric`, `updateCustomMetric`, `deleteCustomMetric` as `vi.fn()`. Stub `window.confirm` to return true for the delete test. Provide a minimal `TableDto` fixture (id, name, schema, columns:{}, created_at, updated_at). Use `@testing-library/react` render + `fireEvent`/`userEvent` + `waitFor`. Assert the four behaviors above. Keep expressions/labels non-empty so Save enables.
  </action>
  <acceptance_criteria>
    - `cd packages/web && npx vitest run src/components/CustomMetricsEditorModal.spec.tsx` exits 0 with the 409-inline and delete cases passing.
    - The full suite stays green: `cd packages/web && npx vitest run` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/CustomMetricsEditorModal.spec.tsx</automated>
  </verify>
  <done>Spec covers add / duplicate-409-inline / delete; full web vitest green.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx tsc --noEmit` clean.
- `cd packages/web && npx vitest run` 100%.
- `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` green.
- `git diff --name-only packages/server` empty (FRONTEND-ONLY).
</verification>

<success_criteria>
From the Tables area a user can create / edit / delete a labeled custom metric with an optional default format, persisting through the Phase 99 store + api; a duplicate label surfaces inline; reached via a "Custom metrics" button mirroring "Format columns". (METRIC-V119-01 UI half.)
</success_criteria>

<output>
After completion, create `.planning/phases/100-custom-metrics-tables-area-editor-metric-picker-integration/100-01-SUMMARY.md`.
</output>
