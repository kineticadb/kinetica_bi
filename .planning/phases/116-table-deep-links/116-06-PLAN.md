---
phase: 116-table-deep-links
plan: 06
type: execute
wave: 5
depends_on: ["116-05"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/116-table-deep-links/116-UAT.md
autonomous: false
requirements: [TLINK-V121-01, TLINK-V121-02, TLINK-V121-03, TLINK-V121-04, TLINK-V121-05, TLINK-V121-06, TLINK-V121-07]

must_haves:
  truths:
    - "The dashboard link behaviour built in Phases 113-115 is demonstrably unchanged: its tests pass AND its files are byte-identical."
    - "The two operator decisions of this phase — the scoped-out unsaved-edit Back guard, and the narrowed table failure copy — are recorded with their reasoning, not silently dropped."
    - "Every behaviour that cannot be proven by a grep or a jsdom test is written down as a live operator check, runnable later, with the Kinetica-dependent steps separated from the app-shell-only ones."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "The deferred TLINK-F* entries carrying this phase's scoped-out work forward"
      contains: "TLINK-F1"
    - path: ".planning/phases/116-table-deep-links/116-UAT.md"
      provides: "The deferred operator walkthrough, split into shell-only and live-data sections"
      contains: "UAT-116-"
  key_links:
    - from: "the Phase 116 diff"
      to: "the Phase 113/114/115 files"
      via: "git diff --numstat producing no output for every protected path"
      pattern: "git diff --numstat"
---

<objective>
Close the phase: prove ROADMAP §Phase 116 criterion 6 with evidence rather than assumption, record
the two operator decisions and the deferred work they imply, and write the live operator
walkthrough for later execution.

Purpose: criterion 6 (no dashboard regression) is the one success criterion no earlier plan owns
end-to-end, and the Kinetica instance is currently unavailable so the live checks must be captured
now and run later.

Output: an updated `.planning/REQUIREMENTS.md` Future section + a new `116-UAT.md`.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/116-table-deep-links/116-CONTEXT.md
@.planning/phases/116-table-deep-links/116-RESEARCH.md
@.planning/REQUIREMENTS.md

<the_two_operator_decisions_recorded_verbatim>
Both were made on 2026-09-14 in answer to 116-RESEARCH's two Open Questions. Both are LOCKED and
both must appear, with their reasoning, in this plan's outputs.

**DECISION 1 — the unsaved-edit-form Back guard is SCOPED OUT of this phase.**
> Do not build the popstate-undo guard, and do not build the narrower Cancel/Sidebar-away variant.
> Reasoning to record in the plan: implementing either requires adding dirty-state tracking to the
> table edit form, which does not exist today — that is form-state work, not URL work. And this
> phase does not create the risk: today, browser Back from a table edit leaves the application
> entirely; after this phase it lands on the tables list, which is strictly better. Add it to the
> phase's deferred/tech-debt notes, do not silently drop it.

**DECISION 2 — the failure-banner copy is NARROWED for tables.**
> Use wording along the lines of "This table isn't available — it may have been deleted." Do NOT
> reuse the dashboard's two-clause "…or you may not have access."
> Reasoning to record: Phase 114's combined wording existed *because* deleted and not-permitted
> were genuinely indistinguishable for dashboards. Research established that for tables they are
> not — `GET /api/tables` and `GET /api/tables/:id` (`index.ts:2352`) carry no permission
> middleware, no `DATASETS_VIEW` permission exists, and the nav is ungated, so "not permitted" is
> unreachable today. Keeping the second clause would copy Phase 114's words while discarding the
> reasoning that justified them. Note in the plan that if per-table permissions are ever added,
> this copy must change alongside them.
</the_two_operator_decisions_recorded_verbatim>

<the_protected_set>
Phase 113/114/115 files that must be byte-identical at the end of this phase:

| Path | Tests |
|---|---|
| `packages/web/src/lib/dashboardUrl.ts` | (source) |
| `packages/web/src/lib/dashboardUrl.spec.ts` | 39 |
| `packages/web/src/hooks/useDeepLinkDashboard.ts` | (source) |
| `packages/web/src/hooks/useDeepLinkDashboard.spec.ts` | 16 |
| `packages/web/src/components/DashboardsPage.tsx` | (source) |
| `packages/web/src/components/DashboardsPage.urlsync.spec.tsx` | 13 |
| `packages/web/src/App.spec.tsx` | 38 |
| `packages/web/src/App.deeplink.spec.tsx` | 16 |
| `packages/web/src/App.passwordDeepLink.spec.tsx` | 3 |
| `packages/web/src/App.signincommit.spec.tsx` | 7 |
| `packages/web/src/components/LoginPage.spec.tsx` | 20 |
| `packages/web/src/components/DatasetsPage.spec.tsx` | 6 |
| **Total protected tests** | **158** |

`App.tsx`, `LoginPage.tsx` and `DatasetsPage.tsx` are the three SOURCE files this phase legitimately
edits. Everything else above must show zero diff.

Pre-phase full-suite baseline, executed 2026-09-14:
`Test Files 168 passed (168)` / `Tests 3778 passed (3778)`.
</the_protected_set>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove criterion 6 and record the deferred work</name>

  <read_first>
    - `.planning/REQUIREMENTS.md` §"Future Requirements" (the `DLINK-F*` block is the format to mirror; `DLINK-F4`'s "PARTIALLY PROMOTED" note is the precedent for recording a scope change in place)
    - `.planning/phases/116-table-deep-links/116-01-SUMMARY.md` … `116-05-SUMMARY.md` (all five, for the mutation-probe numbers to collate)
    - `.planning/phases/116-table-deep-links/116-RESEARCH.md` §Q3 (the full implementation sketch for the deferred Back guard, so the deferred entry can point at it)
  </read_first>

  <files>.planning/REQUIREMENTS.md</files>

  <action>
**1. Run the criterion-6 evidence, and capture the literal output of each command** into the
SUMMARY. Do not paraphrase; paste the numbers.

```bash
cd packages/web
npx tsc --noEmit                                   # expect: clean, exit 0
npx vitest run                                     # expect: 175 files, 0 failed
npx vitest run src/styles/theme-guard.spec.ts      # expect: green
npx vitest run \
  src/lib/dashboardUrl.spec.ts \
  src/hooks/useDeepLinkDashboard.spec.ts \
  src/components/DashboardsPage.urlsync.spec.tsx \
  src/components/DatasetsPage.spec.tsx \
  src/App.spec.tsx src/App.deeplink.spec.tsx \
  src/App.passwordDeepLink.spec.tsx src/App.signincommit.spec.tsx \
  src/components/LoginPage.spec.tsx                # expect: 158 passed, 0 failed
cd ../..
git diff --numstat -- \
  packages/web/src/lib/dashboardUrl.ts \
  packages/web/src/lib/dashboardUrl.spec.ts \
  packages/web/src/hooks/useDeepLinkDashboard.ts \
  packages/web/src/hooks/useDeepLinkDashboard.spec.ts \
  packages/web/src/components/DashboardsPage.tsx \
  packages/web/src/components/DashboardsPage.urlsync.spec.tsx \
  packages/web/src/components/DatasetsPage.spec.tsx \
  packages/web/src/App.spec.tsx \
  packages/web/src/App.deeplink.spec.tsx \
  packages/web/src/App.passwordDeepLink.spec.tsx \
  packages/web/src/App.signincommit.spec.tsx \
  packages/web/src/components/LoginPage.spec.tsx   # expect: NO OUTPUT
git diff --stat -- packages/server                 # expect: NO OUTPUT
```

If the `git diff --numstat` command produces ANY line, criterion 6 has FAILED. Do not proceed to
Task 2; report the offending file and the reason in bold at the top of the SUMMARY.

**2. Run the "shared code is generalized rather than duplicated" half of criterion 6.** Criterion 6
has two clauses; the first is the test gate above, the second is about the shape of the code.
Record, as literal numbers:
- `wc -l packages/web/src/lib/dashboardUrl.ts packages/web/src/lib/tableUrl.ts`
- `wc -l packages/web/src/hooks/useDeepLinkDashboard.ts packages/web/src/hooks/useDeepLinkTable.ts`
and state plainly in the SUMMARY that this phase chose a **sibling-module** generalization over a
shared factory, that the choice was forced by the 132-test import-path blast radius documented in
116-RESEARCH §Q1, and that the cost is two structurally parallel files that must be kept in step.
**Do not claim the code was de-duplicated — it was not.** Log the consequence as a named piece of
tech debt (below) rather than describing the outcome more favourably than it is.

**3. Edit `.planning/REQUIREMENTS.md`.** Mark all seven TLINK requirements complete (`- [x]`) in the
§"Table Links" list AND in the traceability table at the bottom (change `Pending` → `Complete` for
TLINK-V121-01..07). Then add a new subsection under §"Future Requirements", mirroring the
`DLINK-F*` format exactly:

```markdown
### Table Links

- **TLINK-F1**: Unsaved-edit leave guard for the table edit form — deliberately scoped OUT of
  Phase 116 by operator decision (2026-09-14). Implementing it requires adding dirty-state
  tracking to `TableEdit`, which does not exist today; that is form-state work, not URL work.
  Phase 116 does not create the risk it would address: before the phase, browser Back from a
  table edit left the application entirely; after it, Back lands on the tables list, which is
  strictly better. Covers all three leave vectors (in-app Cancel, sidebar navigation away, and
  browser Back). A full implementation sketch — including the `popstate`-undo-via-repush technique,
  the `brandPageGuard`-style singleton, and the named caveats — is in
  `.planning/phases/116-table-deep-links/116-RESEARCH.md` §Q3.
- **TLINK-F2**: Revisit the table unavailable-message copy if per-table view permissions are ever
  added. Phase 116 narrowed it to "This table isn't available — it may have been deleted."
  (dropping the dashboard's "…or you may not have access" clause) because, verified against
  `packages/server/src/index.ts`, neither `GET /api/tables` nor `GET /api/tables/:id` carries any
  permission middleware, no `DATASETS_VIEW` permission exists, and the Datasets nav is ungated —
  so "not permitted" is currently an UNREACHABLE state for tables. The moment that changes, this
  copy must change with it.
- **TLINK-F3**: Sidebar "Datasets" is a no-op while a table is open — the tables twin of the
  pre-existing Phase 113 UAT tech debt on "Dashboards" (`App.tsx`'s `onSelect` calls `setPage`,
  which does nothing when already on that page, so the open table stays on screen with its URL).
  Recorded, deliberately not fixed in Phase 116. {Only include this bullet if Plan 03's SUMMARY
  confirmed the behaviour; if it was not observed, say so instead of asserting it.}
- **TLINK-F4**: `lib/tableUrl.ts` / `hooks/useDeepLinkTable.ts` are structural DUPLICATES of their
  dashboard siblings, not a shared abstraction. This was forced: renaming or parameterizing the
  originals would have required import-path edits across 7 spec files / 132 tests, violating
  ROADMAP §Phase 116 criterion 6. The standing cost is that a fix to one must be mirrored into the
  other. If a third linkable entity is ever added (Roles/Settings, `DLINK-F4`), extract the shared
  core THEN, in a phase whose gate permits touching the existing specs.
- **DLINK-F4** (existing entry, update in place): note that the Tables half is now DELIVERED by
  Phase 116; Roles and Settings remain deferred.
```

Use the actual, verified wording from the source for the TLINK-F2 message quote (read it out of
`hooks/useDeepLinkTable.ts` rather than retyping it from this plan).

Do NOT edit `.planning/ROADMAP.md` or `.planning/STATE.md` in this task — the orchestrator owns
those, and a parallel write is how a previous milestone lost ROADMAP edits.
  </action>

  <verify>
    <automated>cd packages/web && npx vitest run && npx tsc --noEmit && npx vitest run src/styles/theme-guard.spec.ts</automated>
  </verify>

  <acceptance_criteria>
1. The `git diff --numstat` command over all 12 protected paths produces **zero lines of output**. Paste the (empty) result into the SUMMARY explicitly, alongside the exact command.
2. `cd packages/web && npx vitest run src/lib/dashboardUrl.spec.ts src/hooks/useDeepLinkDashboard.spec.ts src/components/DashboardsPage.urlsync.spec.tsx src/components/DatasetsPage.spec.tsx src/App.spec.tsx src/App.deeplink.spec.tsx src/App.passwordDeepLink.spec.tsx src/App.signincommit.spec.tsx src/components/LoginPage.spec.tsx` reports **158 passed, 0 failed** (39+16+13+6+38+16+3+7+20).
3. `cd packages/web && npx vitest run` reports **0 failed** and **≥ 175 test files** and **> 3778 tests** (pre-phase baseline, executed 2026-09-14: 168 files / 3778 tests). Record the actual final numbers.
4. `git diff --stat -- packages/server` produces **no output**.
5. `grep -c "TLINK-F1" .planning/REQUIREMENTS.md` → BEFORE **0** (the string `TLINK-F` occurs 0 times in that file today). AFTER: **≥ 1**.
6. `grep -c "TLINK-F2\|TLINK-F4" .planning/REQUIREMENTS.md` → BEFORE **0**. AFTER: **≥ 2**.
7. `grep -c "or you may not have access" .planning/REQUIREMENTS.md` → BEFORE **0**. AFTER: **≥ 1** — the narrowed-copy reasoning must be recorded with the clause it dropped, so a later reader can see WHAT was dropped and WHY, not just that something was.
8. `grep -c "\- \[ \] \*\*TLINK-V121" .planning/REQUIREMENTS.md` → BEFORE **7** (all seven unchecked today). AFTER: **0**. And `grep -c "\- \[x\] \*\*TLINK-V121" .planning/REQUIREMENTS.md` → BEFORE **0**, AFTER **7**. A real before/after delta on the checkboxes themselves.
9. `grep -c "| TLINK-V121-0. | Phase 116 | Pending |" .planning/REQUIREMENTS.md` → BEFORE **7**. AFTER: **0**.
10. `grep -rc "tableEditGuard" packages/web/src | grep -v ":0" | wc -l` → **0**. Operator decision 1's guard, from the code side: the singleton was never built.
11. `grep -c "window.confirm" packages/web/src/components/DatasetsPage.tsx` → **1** (unchanged from the pre-phase count). Operator decision 1's guard from the other side.
  </acceptance_criteria>

  <done>Criterion 6 is proven by pasted command output rather than asserted; all seven TLINK requirements are marked complete in both places; and the two operator decisions plus the sibling-duplication cost are recorded as named, findable deferred entries.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Write the deferred live-operator walkthrough (116-UAT.md)</name>

  <read_first>
    - `.planning/phases/113-dashboard-url-sync/113-UAT.md` and `.planning/phases/114-deep-link-load-error-states/114-UAT.md` (the format, the numbering convention, and what the operator actually checked and reported last time)
    - All five Phase 116 SUMMARYs
  </read_first>

  <files>.planning/phases/116-table-deep-links/116-UAT.md</files>

  <action>
Write `.planning/phases/116-table-deep-links/116-UAT.md` per the `<how-to-verify>` script below,
then PAUSE. Mirror `113-UAT.md`'s format: a short header naming the phase and the build under test,
then numbered checks each with a `Result:` line left blank for the operator, then the KNOWN NOT
COVERED section. Do NOT attempt to run the walkthrough — the operator's Kinetica instance is
unavailable at planning time, which is exactly why the script is being captured now.
  </action>

  <verify>
    <automated>test -f .planning/phases/116-table-deep-links/116-UAT.md && grep -c "UAT-116-" .planning/phases/116-table-deep-links/116-UAT.md</automated>
  </verify>

  <what-built>
Table deep links, end to end: `?table=<id>` and `?table=<id>&mode=edit` written on open, read on
boot, cleared on leave, surviving browser Back and a logged-out arrival — generalized from the
Phases 113-115 dashboard mechanism via sibling modules, with zero changes to the dashboard code.
  </what-built>

  <how-to-verify>
**⚠️ THE OPERATOR'S KINETICA INSTANCE IS CURRENTLY UNAVAILABLE.** This walkthrough is therefore
written to be RUN LATER, and the checks are split into two groups so the shell-only group can be
run the moment a browser is available, without waiting for live data.

Write `.planning/phases/116-table-deep-links/116-UAT.md` containing the following, with each check
numbered `UAT-116-N`, and a `Result:` line left blank for the operator to fill in
(pass / fail / not exercised), mirroring 113-UAT.md's format.

**GROUP A — needs only the app shell and at least one registered dataset row.**
(The Datasets list only needs rows in the app's own `tables` registry; it does not require the
Kinetica cluster to answer for the *contents* of those tables. If the list itself cannot load,
skip to Group B and record Group A as not exercised.)
  1. Open Datasets. Click **View** on a dataset. The address bar reads `?table=<id>`. — TLINK-V121-01
  2. Click **Back**. The address bar loses the param and the list is on screen. — TLINK-V121-06
  3. Click **Edit** on a dataset. The address bar reads `?table=<id>&mode=edit`. — TLINK-V121-07
  4. From the open edit screen, press the **browser Back** button. You land on the tables list and
     are still inside the application — you are NOT thrown out to a blank page or the previous
     site. — TLINK-V121-05, criterion 4
  5. Copy the `?table=<id>&mode=edit` URL, open a NEW TAB, paste it. The edit screen appears
     directly. **Watch carefully for a flash of the tables LIST before it** — there must not be
     one. — TLINK-V121-02, criterion 2
  6. In that new tab (a deep-link arrival with no prior history entry), press **browser Back**
     once. You must land on the tables list and stay in the app. This is the edge case 113-CONTEXT
     called "the thing most likely to be got wrong". — criterion 4
  7. Paste `?table=999999` (an id that does not exist). You land on the tables list with a banner
     reading "This table isn't available — it may have been deleted." The banner dismisses. The
     `?table=` param is gone from the address bar. — TLINK-V121-04, criterion 3
  8. **Read that banner in BOTH light and dark mode.** Theme-guard exempts `global.css` from its
     hex scan, and a Phase 114 light-mode banner defect got through that hole and was caught only
     by eye. The class is reused, not new, so risk is low — but look. — unprovable by grep
  9. Paste `?table=<id>&mode=banana`. The table opens in VIEW mode. It is NOT an error. — TLINK-V121-07
 10. Open a table, then click **Dashboards** in the sidebar. The `?table=` param leaves the address
     bar. — TLINK-V121-06
 11. **Dashboard no-regression, by hand:** open a dashboard, confirm `?dashboard=<id>` appears,
     press Back, confirm it clears, paste a dashboard link in a new tab and confirm it opens
     directly. Nothing about the dashboard experience may have changed. — criterion 6
 12. Sign out. Paste a `?table=<id>&mode=edit` link. You land on the login page, the param is still
     in the address bar, and the login card shows **"Sign in to open this table."** Sign in. You
     land on that table, in EDIT mode, with the param back in the address bar. — TLINK-V121-03,
     criterion 5
     - Run this in whichever auth mode the instance is configured for, and record which. If the
       instance is OIDC, this is the path that genuinely needs sessionStorage; if password, it is
       the path that proves the URL alone carries it.
 13. Read the login banner in BOTH light and dark mode. — unprovable by grep

**GROUP B — needs the live Kinetica cluster.**
 14. Open a table's **View** screen via a pasted link and confirm the column list and the
     Format columns / Custom metrics modals still work from a deep-link arrival (they were never
     reached this way before — the page previously only ever entered view mode by a click).
 15. From a deep-linked **Edit** screen, change the name and Save. Confirm the screen becomes the
     view screen, the address bar drops `&mode=edit` and keeps `?table=<id>`, and pressing
     browser Back afterwards lands on the tables list rather than ejecting you. This is the
     in-place mode-change writer, the one piece of this phase with no dashboard precedent.
 16. Repeat 15 but starting from a table opened by CLICKING Edit in the list (not a pasted link).
     Back after Save must POP, not write — i.e. you should also end on the tables list, with no
     extra entry left behind. The two paths must behave the same to the user despite taking
     different code branches.

**KNOWN NOT COVERED — state this explicitly in the file so the operator does not test for it and
report a false defect:**
- **Unsaved changes in the table edit form are discarded silently** on Cancel, on sidebar
  navigation away, and on browser Back. This is DELIBERATE and operator-decided for this phase
  (see `TLINK-F1` in REQUIREMENTS.md). It is not a regression: before this phase, Back from a table
  edit left the application entirely.
- `create` screens are not linkable, by design.
- Roles and Settings are still not linkable (`DLINK-F4`).
  </how-to-verify>

  <acceptance_criteria>
1. `.planning/phases/116-table-deep-links/116-UAT.md` exists.
2. `grep -c "UAT-116-" .planning/phases/116-table-deep-links/116-UAT.md` → BEFORE **0** (the file does not exist). AFTER: **≥ 16**.
3. `grep -c "GROUP A\|GROUP B" .planning/phases/116-table-deep-links/116-UAT.md` → **≥ 2**. The shell-only / live-data split is required because the instance is unavailable now.
4. `grep -c "Result:" .planning/phases/116-table-deep-links/116-UAT.md` → **≥ 16** — one blank result line per check, so the operator can fill it in later.
5. `grep -c "TLINK-F1" .planning/phases/116-table-deep-links/116-UAT.md` → **≥ 1** — the KNOWN NOT COVERED section points at the deferred entry by id, so a "defect" report about unsaved changes resolves immediately.
6. Every one of the seven TLINK-V121-0x ids is named at least once in the file: `for i in 1 2 3 4 5 6 7; do grep -c "TLINK-V121-0$i" .planning/phases/116-table-deep-links/116-UAT.md; done` → all **≥ 1**.
  </acceptance_criteria>

  <resume-signal>
This checkpoint is WRITE-then-PAUSE. The file is produced autonomously; the walkthrough itself is
then deferred until the operator's Kinetica instance is reachable.

Operator: reply "defer" to accept the walkthrough as written and close the phase pending live UAT,
or run Group A now and reply with the results (or with issues) if a browser against the app shell
is available.
  </resume-signal>

  <done>`116-UAT.md` exists with ≥16 numbered, result-blanked checks, split into shell-only and live-Kinetica groups, every TLINK requirement id referenced, and the deliberately-uncovered behaviours named so they are not mistaken for defects.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx tsc --noEmit` — clean.
- `cd packages/web && npx vitest run` — 0 failed, ≥175 files, >3778 tests.
- `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` — green.
- `git diff --numstat` over the 12 protected paths — NO OUTPUT.
- `git diff --stat -- packages/server` — NO OUTPUT.
</verification>

<success_criteria>
- Criterion 6 proven with pasted evidence (158 protected tests green AND zero diff on 12 protected paths).
- All seven TLINK-V121 requirements marked complete in REQUIREMENTS.md, in both the checklist and the traceability table.
- `TLINK-F1` … `TLINK-F4` recorded, carrying forward the scoped-out Back guard, the copy caveat, the sidebar no-op and the sibling-duplication cost.
- `116-UAT.md` written for deferred execution, Group A / Group B separated.
</success_criteria>

<output>
After completion, create `.planning/phases/116-table-deep-links/116-06-SUMMARY.md` containing:
- the literal output of every criterion-6 command;
- a collated table of all mutation-probe results from Plans 01-05 (probe, failed-count when
  applied, green after revert) — this is the phase's evidence that its acceptance criteria could
  actually fail;
- the final test-file and test counts;
- an explicit statement that live UAT is DEFERRED, and why.
</output>
