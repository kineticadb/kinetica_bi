---
phase: 77-apply-labels-formatting-at-render-surfaces
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/charts/ColumnFormatTooltip.tsx
  - packages/web/src/components/charts/ColumnFormatTooltip.spec.tsx
  - packages/web/src/components/charts/WidgetRenderer.tsx
  - packages/web/src/components/charts/WidgetRenderer.spec.tsx
  - packages/web/src/components/charts/TimelineRenderer.tsx
  - packages/web/src/components/charts/TimelineRenderer.spec.tsx
  - packages/web/src/components/charts/NumericLineRenderer.tsx
  - packages/web/src/components/charts/NumericLineRenderer.spec.tsx
autonomous: true
requirements: [COLAPPLY-V115-02]

must_haves:
  truths:
    - "A chart tooltip shows the category via the groupByColumn's label and the numeric value formatted via the metricColumn's formatter"
    - "An in-chart series legend shows config.yFieldLabel when set, else the metricColumn's label, else the raw y key"
    - "An axis title shows the user-entered axis label when set, else the resolved column label (X=groupByColumn, Y=metricColumn); axis titles are NOT formatted"
    - "A chart with no groupByColumn/metricColumn or an undefined tableId keeps current (legacy) behavior — never breaks"
    - "Editing a label/format in the Phase 76 editor re-renders the open chart's tooltip/series/axes live (configVersion-driven)"
  artifacts:
    - path: "packages/web/src/components/charts/ColumnFormatTooltip.tsx"
      provides: "Shared custom Recharts Tooltip content component formatting value + labeling category; theme-token styled (no hex)"
      contains: "RECHARTS_TOOLTIP_PROPS"
    - path: "packages/web/src/components/charts/WidgetRenderer.tsx"
      provides: "Bar/Line/Pie/Scatter use ColumnFormatTooltip; series name + axis-title fallback chains; configVersion subscription"
      contains: "ColumnFormatTooltip"
    - path: "packages/web/src/components/charts/TimelineRenderer.tsx"
      provides: "Timeline tooltip uses ColumnFormatTooltip; configVersion subscription"
    - path: "packages/web/src/components/charts/NumericLineRenderer.tsx"
      provides: "NumericLine tooltip uses ColumnFormatTooltip; configVersion subscription"
  key_links:
    - from: "chart renderers <Tooltip>"
      to: "ColumnFormatTooltip (formats value via resolveFormatter(tableId, metricColumn))"
      via: "Tooltip content prop"
      pattern: "content=\\{<ColumnFormatTooltip"
    - from: "series legend name"
      to: "config.yFieldLabel || resolveLabel(tableId, metricColumn) || y"
      via: "Bar/Line/Area name= prop"
      pattern: "resolveLabel\\(tableId, metricColumn\\)"
    - from: "chart renderers"
      to: "useColumnDisplayConfigStore configVersion"
      via: "primitive selector subscription"
      pattern: "useColumnDisplayConfigStore\\(\\(s\\) => s.configVersion\\)"
---

<objective>
Format chart tooltip values and label chart axes/series/tooltip categories via the Phase 75 resolution layer. Build ONE shared custom Recharts Tooltip content component (`ColumnFormatTooltip`) reused across Bar/Line/Pie/Scatter (WidgetRenderer) + TimelineRenderer + NumericLineRenderer. Wire series-legend and axis-title label fallback chains. configVersion-driven live re-render; legacy/dv-bound widgets keep current behavior.

Purpose: COLAPPLY-V115-02 — chart tooltips show label + formatted value; axis titles + in-chart series legends show custom labels.
Output: New `ColumnFormatTooltip.tsx` + edits to three renderers + tests.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-apply-labels-formatting-at-render-surfaces/77-CONTEXT.md

<interfaces>
From packages/web/src/store/columnDisplayConfigStore.ts:
```typescript
export const resolveLabel = (tableId: number, columnName: string): string;            // label ?? raw name
export const resolveFormatter = (tableId: number, columnName: string): (v: unknown) => string | unknown; // ?? identity
export const useColumnDisplayConfigStore: <T>(selector: (s: ColumnDisplayConfigState) => T) => T;
// state has configVersion: number and loadConfig: (tableId: number) => Promise<void>
```

From packages/web/src/lib/chartTheme.ts:
```typescript
// Theme-token-styled container style. Spread onto the custom Tooltip's wrapper to pass theme-guard.
export const RECHARTS_TOOLTIP_PROPS: { contentStyle: { background: string; border: string; borderRadius: number; color: string } };
```

Recharts Tooltip custom content contract (props passed by recharts to a `content` element/fn):
```typescript
type TooltipContentProps = {
  active?: boolean;
  // each payload entry: { name, value, payload (the source row), dataKey, color }
  payload?: Array<{ name?: string; value?: unknown; payload?: Record<string, unknown>; dataKey?: string; color?: string }>;
  label?: unknown; // the category axis value (x value) for cartesian charts
};
```
</interfaces>

<existing-current-behavior>
WidgetRenderer.tsx chart renderers (Bar :970, Line :1098, Pie :1211, Scatter :1303):
- Each receives `tableId` via the spread `{...drillProps}` (DrillProps.tableId :945) and `config` prop.
- `config.groupByColumn` (:1423 in TableRenderer; same key used by resolveKeys :960) = category/x source column.
- `config.metricColumn` (:1425) = the metric's source column. `value` is the synthetic agg alias.
- Tooltip sites: Bar :1060 `<Tooltip {...RECHARTS_TOOLTIP_PROPS} />`; Line/Area :1172/:1194; Pie :1296 (adds itemStyle/labelStyle); Scatter :1374.
- Series name= sites: Bar :1066, Area :1181, Line :1202 → `(config.yFieldLabel as string) || y`.
- Axis-title objects: built at :990-:997 from `config.xAxisLabel` / `config.yAxisLabel` (BarRenderer) → xLabelObj/yLabelObj used at :1051-:1057. (Other renderers don't render axis titles today — only Bar does; keep parity.)
- resolveKeys (:958) returns `{ x, y }` where x=groupByColumn (or first key), y="value" (or second key).

TimelineRenderer.tsx: `tableId = cfg.tableId` (:105), `groupByColumn` (:109), Tooltip at :590. metric column for timeline lives in `metrics[]` (per-metric column) — for a SINGLE-metric / grouped timeline the metric source column is `metrics[0].column`. Series names already set (:571 grouped uses series value; :584 ungrouped uses m.label || agg(col)).
NumericLineRenderer.tsx: `tableId = cfg.tableId` (:95), `groupByColumn` (:99), Tooltip at :552, same metrics[] shape.
</existing-current-behavior>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the shared ColumnFormatTooltip content component (+ unit test)</name>
  <files>packages/web/src/components/charts/ColumnFormatTooltip.tsx, packages/web/src/components/charts/ColumnFormatTooltip.spec.tsx</files>
  <read_first>
    - packages/web/src/lib/chartTheme.ts:20 (RECHARTS_TOOLTIP_PROPS — reuse contentStyle for the wrapper so theme-guard passes WITHOUT allowlisting)
    - packages/web/src/store/columnDisplayConfigStore.ts (resolveLabel/resolveFormatter signatures)
    - packages/web/src/components/charts/WidgetRenderer.tsx:1293-1296 (pie tooltip itemStyle/labelStyle var(--text) precedent for readable item text)
    - packages/web/src/styles/theme-guard.spec.ts (ALLOWLIST — confirm the new file must NOT be added; use var(--…)/RECHARTS_TOOLTIP_PROPS only)
  </read_first>
  <behavior>
    - Given active=true and a payload row, the tooltip renders the category label line using resolveLabel(tableId, groupByColumn) as the prefix and the row's category value, and renders the numeric value via resolveFormatter(tableId, metricColumn)(value) → String(...).
    - When tableId is undefined OR groupByColumn/metricColumn is empty/missing, it falls back: category prefix = raw label (recharts `label`/payload), value = raw value (current behavior — no crash).
    - When active=false or payload empty, renders null.
    - Container uses theme tokens only (RECHARTS_TOOLTIP_PROPS.contentStyle / var(--text)) — NO raw hex.
  </behavior>
  <action>
    Create `packages/web/src/components/charts/ColumnFormatTooltip.tsx` exporting a component
    `ColumnFormatTooltip` usable as a Recharts `content` element. Props (own, beyond recharts-injected):
    ```
    type ColumnFormatTooltipProps = {
      tableId: number | undefined;
      groupByColumn: string;   // category source column ("" when unknown)
      metricColumn: string;    // metric source column ("" when unknown)
      // recharts-injected (optional):
      active?: boolean;
      payload?: Array<{ name?: string; value?: unknown; payload?: Record<string, unknown>; color?: string }>;
      label?: unknown;
    };
    ```
    Logic:
    - If `!active || !payload || payload.length === 0` → return null.
    - Category line text: `const catLabel = (tableId !== undefined && groupByColumn) ? resolveLabel(tableId, groupByColumn) : undefined;` Render the category value = recharts `label` (or payload[0].payload[groupByColumn] fallback). Display as `${catLabel ?? ""}: ${categoryValue}` (when no catLabel, just the value — matching current bare behavior).
    - Value line(s): for each payload entry, format its numeric value:
      `const fmt = (tableId !== undefined && metricColumn) ? resolveFormatter(tableId, metricColumn) : (v: unknown) => v;`
      render `String(fmt(entry.value) ?? "")`. Keep the series `entry.name` prefix so multi-series still reads.
      Use `entry.color` for the item text color where available, else `var(--text)`.
    - Wrap in a `<div style={{ ...RECHARTS_TOOLTIP_PROPS.contentStyle, padding: "6px 10px" }}>`. Item text color: `var(--text)` or `entry.color` (entry.color is a series color string from recharts, not a literal in this file — OK). NO hardcoded hex literals in this file.

    When used in JSX you pass it as `content={<ColumnFormatTooltip tableId={...} groupByColumn={...} metricColumn={...} />}` — recharts injects active/payload/label onto the element.

    Also create `ColumnFormatTooltip.spec.tsx` covering the four behaviors above. Seed the real
    columnDisplayConfigStore (upsertColumn) for the label+format cases; render the component directly with
    a synthetic active/payload/label prop set (no full chart needed).
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/ColumnFormatTooltip.spec.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/web/src/components/charts/ColumnFormatTooltip.tsx` exists exporting `ColumnFormatTooltip`.
    - `grep -n "RECHARTS_TOOLTIP_PROPS" packages/web/src/components/charts/ColumnFormatTooltip.tsx` matches (reused for container style).
    - `grep -n "resolveFormatter" packages/web/src/components/charts/ColumnFormatTooltip.tsx` and `grep -n "resolveLabel" ...` both match.
    - `grep -nE "#[0-9a-fA-F]{3,6}\\b" packages/web/src/components/charts/ColumnFormatTooltip.tsx` → NO matches (no raw hex).
    - The file is NOT added to the ALLOWLIST in src/styles/theme-guard.spec.ts.
    - `cd packages/web && npx vitest run src/components/charts/ColumnFormatTooltip.spec.tsx` → pass.
  </acceptance_criteria>
  <done>ColumnFormatTooltip renders label+formatted value, falls back cleanly, returns null when inactive, uses theme tokens only, and its unit test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ColumnFormatTooltip + series/axis label chains + configVersion into WidgetRenderer chart renderers</name>
  <files>packages/web/src/components/charts/WidgetRenderer.tsx</files>
  <read_first>
    - packages/web/src/components/charts/WidgetRenderer.tsx (Bar :970-:1096 incl. tooltip :1060, series name :1066, axis objs :990-:997 + usage :1051-:1057; Line/Area :1098-:1208 tooltip :1172/:1194, name :1181/:1202; Pie :1211-:1301 tooltip :1296; Scatter :1303-:1393 tooltip :1374; resolveKeys :958; drillProps/DrillProps tableId :895/:945)
    - packages/web/src/components/charts/ColumnFormatTooltip.tsx (the component built in Task 1)
    - packages/web/src/store/columnDisplayConfigStore.ts (resolveLabel + configVersion)
  </read_first>
  <action>
    Goal: each chart renderer derives `tableId` (already in props via DrillProps), `groupByColumn = (config.groupByColumn as string) || ""`, `metricColumn = (config.metricColumn as string) || ""`, subscribes to configVersion, replaces its `<Tooltip {...RECHARTS_TOOLTIP_PROPS} />` with the custom content, and applies the series/axis fallback chains.

    1. Import at top of WidgetRenderer.tsx:
       `import { ColumnFormatTooltip } from "./ColumnFormatTooltip";`
       and ensure `useColumnDisplayConfigStore`, `resolveLabel` are imported (Plan 01 may already add them — if Plan 01 lands first the import exists; if not, add it. Imports are idempotent — verify single import line).

    2. In EACH of BarRenderer, LineRenderer, PieRenderer, ScatterRenderer:
       - Add `const configVersion = useColumnDisplayConfigStore((s) => s.configVersion);` (primitive selector — forces re-render on edit; reference it in render path, e.g. via the loadConfig effect or a void).
       - Add a `useEffect` to call `loadConfig(tableId)` once when tableId known (same guarded pattern as Plan 01 Task 1 step 3).
       - Compute `const groupByColumn = (config.groupByColumn as string) || "";` and `const metricColumn = (config.metricColumn as string) || "";`.
       - Replace the tooltip element with:
         `<Tooltip {...RECHARTS_TOOLTIP_PROPS} content={<ColumnFormatTooltip tableId={tableId} groupByColumn={groupByColumn} metricColumn={metricColumn} />} />`
         For Pie, KEEP its itemStyle/labelStyle var(--text) props on the `<Tooltip>` (they coexist with content — content takes precedence for body, the style props are harmless; if recharts ignores style when content is set, that is acceptable since ColumnFormatTooltip already sets var(--text)).

    3. Series legend name fallback (Bar :1066, Area :1181, Line :1202): change
       `name={(config.yFieldLabel as string) || y}` to
       `name={(config.yFieldLabel as string) || (tableId !== undefined && metricColumn ? resolveLabel(tableId, metricColumn) : "") || y}`
       (user yFieldLabel wins; else resolved metric label; else raw y).

    4. Axis titles — BarRenderer ONLY (it is the only renderer rendering axis-title objects today, :990-:997).
       When the user has NOT set the axis label, fall back to the resolved column label (do NOT format titles):
       ```
       const xTitle = (config.xAxisLabel as string) || (tableId !== undefined && groupByColumn ? resolveLabel(tableId, groupByColumn) : "");
       const yTitle = (config.yAxisLabel as string) || (tableId !== undefined && metricColumn ? resolveLabel(tableId, metricColumn) : "");
       ```
       Use xTitle/yTitle to build xLabelObj/yLabelObj (only build the object when the resolved title is non-empty — preserve the "undefined when blank" behavior at :992-:997). Leave the margin logic (:1042-:1043) gated on the same non-empty check.
       Do NOT add axis titles to Line/Pie/Scatter (they don't render them today — out of scope; keep current behavior).

    5. Where groupByColumn/metricColumn is empty or tableId undefined → all the above ternaries fall back to
       the raw `y` / no title / raw tooltip value (legacy behavior preserved — never break dv-bound/legacy widgets).

    Do NOT add materialize/fromSwap/dropFilterView imports.
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "content={<ColumnFormatTooltip" packages/web/src/components/charts/WidgetRenderer.tsx` ≥ 4 (Bar/Line+Area/Pie/Scatter — Line+Area count as the LineRenderer's two branches; minimum 4 distinct chart renderers covered).
    - `grep -n "resolveLabel(tableId, metricColumn)" packages/web/src/components/charts/WidgetRenderer.tsx` matches (series name chain).
    - `grep -n "resolveLabel(tableId, groupByColumn)" packages/web/src/components/charts/WidgetRenderer.tsx` matches (X axis title chain).
    - `grep -c "useColumnDisplayConfigStore((s) => s.configVersion)" packages/web/src/components/charts/WidgetRenderer.tsx` ≥ 4 (one per chart renderer; RecordsTable from Plan 01 may add a 5th).
    - `grep -nE "materializeFilter|fromSwap|dropFilterView" packages/web/src/components/charts/WidgetRenderer.tsx` shows no NEW occurrences from this plan.
    - `cd packages/web && npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Bar/Line/Pie/Scatter use ColumnFormatTooltip; series names + Bar axis titles use the fallback chains; each subscribes to configVersion; legacy/dv-bound widgets unchanged; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire ColumnFormatTooltip + configVersion into TimelineRenderer and NumericLineRenderer</name>
  <files>packages/web/src/components/charts/TimelineRenderer.tsx, packages/web/src/components/charts/NumericLineRenderer.tsx</files>
  <read_first>
    - packages/web/src/components/charts/TimelineRenderer.tsx (tableId :105, groupByColumn :109, metrics[] shape ~:267-:286, Tooltip :590, series names :571/:584, RECHARTS_TOOLTIP_PROPS import :58)
    - packages/web/src/components/charts/NumericLineRenderer.tsx (tableId :95, groupByColumn :99, metrics[] ~:239-:258, Tooltip :552, RECHARTS_TOOLTIP_PROPS import :49)
    - packages/web/src/components/charts/ColumnFormatTooltip.tsx (Task 1 component)
    - packages/web/src/store/columnDisplayConfigStore.ts
  </read_first>
  <action>
    For BOTH TimelineRenderer.tsx and NumericLineRenderer.tsx (apply consistently):

    1. Import `{ ColumnFormatTooltip }` from "./ColumnFormatTooltip" and `{ useColumnDisplayConfigStore }`
       from "../../store/columnDisplayConfigStore".
    2. Add `const configVersion = useColumnDisplayConfigStore((s) => s.configVersion);` (primitive selector)
       and a guarded `loadConfig(tableId)` useEffect (tableId is `number` here after the early-return at
       Timeline :129 / NumericLine :118, but keep a `tableId !== undefined` guard for safety).
    3. Derive `const groupByColumn = (cfg.groupByColumn as string) || "";` and the metric source column.
       For these renderers the metric column is `metrics[0]?.column ?? ""` (single-metric/grouped is the
       common case; for multi-metric the per-series name already carries m.label — do NOT change those).
       Set `const metricColumn = metrics[0]?.column ?? "";`.
    4. Replace `<Tooltip {...RECHARTS_TOOLTIP_PROPS} />` (Timeline :590, NumericLine :552) with:
       `<Tooltip {...RECHARTS_TOOLTIP_PROPS} content={<ColumnFormatTooltip tableId={tableId} groupByColumn={groupByColumn} metricColumn={metricColumn} />} />`
    5. Do NOT change the existing series `name=` props (grouped uses the series value; ungrouped uses
       m.label || agg(col) — already correct and user-overridable). Do NOT add axis titles (these renderers
       manage their own time/bucket axes — out of scope; keep current behavior).
    6. Where tableId/groupByColumn/metricColumn is unresolvable, ColumnFormatTooltip already falls back to raw.

    Do NOT add materialize/fromSwap/dropFilterView imports (these renderers DO call setBulkFilters/markMaterializing
    in their drill handlers per existing code — that is pre-existing and untouched; add NO new such imports).
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "content={<ColumnFormatTooltip" packages/web/src/components/charts/TimelineRenderer.tsx` matches.
    - `grep -n "content={<ColumnFormatTooltip" packages/web/src/components/charts/NumericLineRenderer.tsx` matches.
    - `grep -n "useColumnDisplayConfigStore((s) => s.configVersion)" packages/web/src/components/charts/TimelineRenderer.tsx` and same for NumericLineRenderer.tsx both match.
    - Existing series `name=` props at TimelineRenderer :571/:584 unchanged.
    - `cd packages/web && npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Timeline + NumericLine tooltips use ColumnFormatTooltip with configVersion subscription; series names untouched; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Tests — chart tooltip value format, series/axis label fallback chain, configVersion re-render</name>
  <files>packages/web/src/components/charts/WidgetRenderer.spec.tsx, packages/web/src/components/charts/TimelineRenderer.spec.tsx, packages/web/src/components/charts/NumericLineRenderer.spec.tsx, packages/web/src/components/charts/ColumnFormatTooltip.spec.tsx</files>
  <read_first>
    - packages/web/src/components/charts/WidgetRenderer.spec.tsx, TimelineRenderer.spec.tsx, NumericLineRenderer.spec.tsx (existing render harnesses + recharts mocking conventions — recharts often needs a fixed-size container / ResponsiveContainer mock; mirror existing tests)
    - packages/web/src/components/charts/ColumnFormatTooltip.spec.tsx (Task 1 — reuse the seeding approach for value/label assertions)
    - packages/web/src/store/columnDisplayConfigStore.ts (upsertColumn to seed; auto-reset via zustand shim)
  </read_first>
  <behavior>
    - WidgetRenderer (bar): with a saved metricColumn format + groupByColumn label, the rendered Tooltip content (or the series name / Bar axis title) reflects the resolved label; series name = yFieldLabel when set, else resolved metric label, else raw y.
    - Bar axis title: with config.xAxisLabel unset but a saved label for groupByColumn, the X axis title resolves to the label; when config.xAxisLabel is set, it wins.
    - Legacy/dv-bound chart (tableId undefined OR no groupByColumn/metricColumn) → series name = raw y, no axis title, no crash.
    - Timeline + NumericLine: tooltip content uses ColumnFormatTooltip (assert the content element / component is wired — e.g. via a shallow assertion that the metric formatter is applied or the component is present).
    - configVersion re-render: after upsertColumn changes the metric label, the chart's series name / axis title updates (act()).
  </behavior>
  <action>
    Prefer asserting the FALLBACK CHAINS and configVersion reactivity at the component level where recharts
    rendering is hard to introspect: render the chart with a fixed-size container (mirror existing harness),
    then assert on the series `name` (legend text) and Bar axis-title text in the DOM, and on live update
    after a store mutation. For the tooltip VALUE FORMATTING, rely primarily on ColumnFormatTooltip.spec.tsx
    (Task 1) for the unit-level proof, and add a lighter integration assertion in WidgetRenderer.spec.tsx that
    the `content` prop is a ColumnFormatTooltip carrying the right tableId/groupByColumn/metricColumn (e.g. by
    mocking the recharts Tooltip to render its `content` with synthetic active/payload, or by asserting the
    element type).

    Add the assertions across WidgetRenderer.spec.tsx (bar: series name + axis title + dv-bound fallback +
    configVersion live update), TimelineRenderer.spec.tsx (tooltip wired), NumericLineRenderer.spec.tsx
    (tooltip wired). Use the REAL columnDisplayConfigStore (auto-reset).
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/WidgetRenderer.spec.tsx src/components/charts/TimelineRenderer.spec.tsx src/components/charts/NumericLineRenderer.spec.tsx src/components/charts/ColumnFormatTooltip.spec.tsx 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - WidgetRenderer.spec.tsx asserts: series name fallback chain (yFieldLabel wins / resolved metric label / raw y), Bar axis-title fallback (user label wins / resolved groupByColumn label), dv-bound raw fallback, configVersion live update.
    - TimelineRenderer.spec.tsx + NumericLineRenderer.spec.tsx each assert the tooltip is wired to ColumnFormatTooltip (component present / formatter applied).
    - ColumnFormatTooltip.spec.tsx proves value formatting via resolveFormatter(metricColumn).
    - `cd packages/web && npx vitest run` of the four files → 100% pass.
  </acceptance_criteria>
  <done>All four chart-surface spec files pass, covering tooltip value formatting, series/axis label fallback chains, dv-bound fallback, and at least one configVersion-driven re-render.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx tsc --noEmit` clean.
- `cd packages/web && npx vitest run src/components/charts/ColumnFormatTooltip.spec.tsx src/components/charts/WidgetRenderer.spec.tsx src/components/charts/TimelineRenderer.spec.tsx src/components/charts/NumericLineRenderer.spec.tsx` 100% pass.
- `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` green — ColumnFormatTooltip.tsx contains NO raw hex and is NOT on the ALLOWLIST.
- No new `materializeFilter`/`fromSwap`/`dropFilterView` imports in the three renderers or the new component.
</verification>

<success_criteria>
Chart tooltips show the resolved category label and the metric-formatted value; in-chart series legends and Bar axis titles follow the documented fallback chains (user value > resolved label > raw); legacy/dv-bound charts are unchanged; a Phase 76 edit re-renders the chart live; the custom Tooltip passes theme-guard un-allowlisted.
</success_criteria>

<output>
After completion, create `.planning/phases/77-apply-labels-formatting-at-render-surfaces/77-02-SUMMARY.md`
</output>
