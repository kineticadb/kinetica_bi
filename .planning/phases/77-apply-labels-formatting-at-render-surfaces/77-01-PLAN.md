---
phase: 77-apply-labels-formatting-at-render-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/charts/WidgetRenderer.tsx
  - packages/web/src/components/charts/WidgetRenderer.spec.tsx
autonomous: true
requirements: [COLAPPLY-V115-01]

must_haves:
  truths:
    - "On the Records Table widget, a column with a saved display label renders that label in the header (sort arrow preserved)"
    - "On the Records Table widget, cell values render through the column's saved formatter"
    - "A Records Table column with NO saved config renders the raw column name in the header and the raw value in the cell (fallback)"
    - "A dv-bound Records Table widget (tableId undefined) renders raw names/values unchanged (accepted fallback)"
    - "Editing a label/format in the Phase 76 editor re-renders the open Records Table live (configVersion-driven)"
  artifacts:
    - path: "packages/web/src/components/charts/WidgetRenderer.tsx"
      provides: "RecordsTableRenderer header=resolveLabel, cell=resolveFormatter, configVersion subscription"
      contains: "resolveLabel("
    - path: "packages/web/src/components/charts/WidgetRenderer.spec.tsx"
      provides: "Records header-label + cell-format + raw-fallback + configVersion re-render tests"
  key_links:
    - from: "RecordsTableRenderer header <th>"
      to: "resolveLabel(tableId, col)"
      via: "header text render"
      pattern: "resolveLabel\\(tableId, col\\)"
    - from: "RecordsTableRenderer cell <td>"
      to: "resolveFormatter(tableId, col)(row[col])"
      via: "cell value render"
      pattern: "resolveFormatter\\(tableId, col\\)"
    - from: "RecordsTableRenderer"
      to: "useColumnDisplayConfigStore configVersion"
      via: "primitive selector subscription"
      pattern: "useColumnDisplayConfigStore\\(\\(s\\) => s.configVersion\\)"
---

<objective>
Inject the resolved display label (headers) and value formatter (cells) into the Records Table visualization (`RecordsTableRenderer` in WidgetRenderer.tsx), with configVersion-driven live re-render and raw fallback for unconfigured columns / dv-bound widgets.

Purpose: COLAPPLY-V115-01 — Records Table shows custom labels on headers and formatted values in cells.
Output: Modified `RecordsTableRenderer` + tests proving label, format, raw-fallback, and configVersion re-render.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-apply-labels-formatting-at-render-surfaces/77-CONTEXT.md

<interfaces>
<!-- Phase 75 resolution layer — call these directly; do NOT explore the store internals. -->

From packages/web/src/store/columnDisplayConfigStore.ts:
```typescript
// PURE getState()-based helpers (NOT hooks). Safe to call inline in render.
// resolveLabel: stored label ?? raw column name (fallback built in).
export const resolveLabel = (tableId: number, columnName: string): string;
// resolveFormatter: buildFormatter(spec) ?? identity passthrough (v) => v. Never throws.
export const resolveFormatter = (tableId: number, columnName: string): (v: unknown) => string | unknown;

// Store. loadConfig(tableId) fetches + caches a table's config (bumps configVersion).
// configVersion is the canonical primitive re-render trigger (mirror filterVersion).
export const useColumnDisplayConfigStore: <T>(selector: (s: ColumnDisplayConfigState) => T) => T;
// state shape: { configs: Record<number, ...>; configVersion: number; loadConfig: (tableId: number) => Promise<void>; ... }
```

NOTE on tableId typing: `resolveLabel`/`resolveFormatter` are typed `(tableId: number, ...)`. In
RecordsTableRenderer `tableId` is `number | undefined` (line :1598). When `tableId === undefined`
(dv-bound widget) DO NOT call resolve* — short-circuit to the raw value/name so the dv-bound
fallback is honored without a type cast. Use a small local helper or inline ternary:
`const headerLabel = tableId !== undefined ? resolveLabel(tableId, col) : col;`
`const fmt = tableId !== undefined ? resolveFormatter(tableId, col) : (v: unknown) => v;`
</interfaces>

<existing-current-behavior>
RecordsTableRenderer (WidgetRenderer.tsx):
- `tableId = cfg.tableId as number | undefined` at :1598 (dv-bound widgets leave it undefined).
- Header render at :2134-:2147 — `{col}{arrow}` inside a sortable `<th>` (keep the arrow + onClick + title).
- Cell render at :2185-:2186 — `<td key={col}>{String(row[col] ?? "")}</td>`.
- `columnOrder` is the array mapped for both header and cells.
</existing-current-behavior>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire resolveLabel/resolveFormatter + configVersion into RecordsTableRenderer</name>
  <files>packages/web/src/components/charts/WidgetRenderer.tsx</files>
  <read_first>
    - packages/web/src/components/charts/WidgetRenderer.tsx (RecordsTableRenderer :1582-:2210; header :2134-:2147; cell :2185-:2186; tableId :1598; existing filterVersion primitive-selector precedent :394)
    - packages/web/src/store/columnDisplayConfigStore.ts (resolveLabel/resolveFormatter signatures + loadConfig + configVersion semantics — full file)
    - packages/web/src/components/charts/LegendRenderer.tsx:88-104 (dynamicViewVersion primitive selector + useMemo-dep reactivity pattern to mirror)
  </read_first>
  <action>
    In `RecordsTableRenderer` (WidgetRenderer.tsx):

    1. Add imports at top of file (alongside existing store imports):
       `import { useColumnDisplayConfigStore, resolveLabel, resolveFormatter } from "../../store/columnDisplayConfigStore";`
       (Verify the relative path resolves to src/store/columnDisplayConfigStore.ts.)

    2. Inside RecordsTableRenderer, after `tableId` is read (:1598), subscribe to configVersion with a
       PRIMITIVE selector (mirror the filterVersion pattern at :394):
       `const configVersion = useColumnDisplayConfigStore((s) => s.configVersion);`
       This subscription forces a re-render when the Phase 76 editor mutates the config. Because resolve*
       are getState()-based, the re-render is what re-reads fresh values — `configVersion` must be
       referenced in the render path (it is, via the subscription causing re-render; reference it once in a
       comment or void to satisfy lint if needed — prefer including it in any useMemo you add).

    3. Populate the cache once per table: add a useEffect that calls `loadConfig(tableId)` when tableId is
       known. Guard against undefined and redundant loads:
       ```
       useEffect(() => {
         if (tableId !== undefined) {
           void useColumnDisplayConfigStore.getState().loadConfig(tableId);
         }
       }, [tableId]);
       ```
       (The store caches; loadConfig replaces the table entry and bumps configVersion. Keying the effect on
       tableId alone loads once per table — acceptable; the editor's own upsert bumps configVersion for live
       updates without needing a reload here.)

    4. Header (:2134-:2147): replace the displayed `{col}{arrow}` text with the resolved label while KEEPING
       the sort arrow, onClick={() => handleHeaderClick(col)}, and the existing `title`. Compute:
       `const headerLabel = tableId !== undefined ? resolveLabel(tableId, col) : col;`
       then render `{headerLabel}{arrow}`. Leave `title={`Sort by ${col}`}` referencing the raw col (sort key
       is by raw column — do not change sort semantics).

    5. Cell (:2185-:2186): replace `{String(row[col] ?? "")}` with the formatted value:
       ```
       const fmt = tableId !== undefined ? resolveFormatter(tableId, col) : (v: unknown) => v;
       const formatted = fmt(row[col]);
       return <td key={col}>{String(formatted ?? "")}</td>;
       ```
       (resolveFormatter returns identity when no spec; String(... ?? "") preserves the existing
       null/undefined → "" behavior.)

    Do NOT add any materialize/fromSwap/dropFilterView import. This is read-path only.
    Do NOT add raw hex — no styling changes here.
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "resolveLabel(tableId, col)" packages/web/src/components/charts/WidgetRenderer.tsx` matches (header) — OR the guarded `tableId !== undefined ? resolveLabel(tableId, col) : col` form is present.
    - `grep -n "resolveFormatter(tableId, col)" packages/web/src/components/charts/WidgetRenderer.tsx` matches (cell).
    - `grep -n "useColumnDisplayConfigStore((s) => s.configVersion)" packages/web/src/components/charts/WidgetRenderer.tsx` matches.
    - `grep -n "loadConfig" packages/web/src/components/charts/WidgetRenderer.tsx` matches inside a useEffect keyed on tableId.
    - `grep -nE "materializeFilter|fromSwap|dropFilterView" packages/web/src/components/charts/WidgetRenderer.tsx` shows NO new occurrences beyond any pre-existing (compare to pre-edit; the records change adds none).
    - `cd packages/web && npx tsc --noEmit` is clean (no new errors).
    - The sort arrow ` ▲`/` ▼` and onClick handleHeaderClick are still present on the `<th>`.
  </acceptance_criteria>
  <done>Records headers render resolveLabel, cells render resolveFormatter, configVersion subscribed, loadConfig called once per table, dv-bound (undefined tableId) falls back to raw, tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests — records header label, cell format, raw fallback, configVersion re-render</name>
  <files>packages/web/src/components/charts/WidgetRenderer.spec.tsx</files>
  <read_first>
    - packages/web/src/components/charts/WidgetRenderer.spec.tsx (existing test setup, render helpers, store mocking conventions)
    - packages/web/src/store/columnDisplayConfigStore.ts (use the real store: upsertColumn / setConfig to seed a label+spec; reset() between tests — the zustand reset shim auto-resets per src/test/setup.ts)
    - packages/web/src/lib/columnFormatter.ts (FormatSpec union shape — to build a valid format_spec for the cell-format assertion)
  </read_first>
  <behavior>
    - Records widget with tableId=T and a saved label for column "amount" → header shows the label text, not "amount" (and the sort arrow still appears on the active sort column).
    - Records widget with a saved numeric format_spec for "amount" → a cell value (e.g. 1234.5) renders formatted (e.g. "1,234.5" or "1,235" per the spec) — assert it differs from the raw String(value).
    - Records widget with NO config for column "name" → header shows raw "name" and cell shows the raw value (fallback).
    - dv-bound records widget (config.tableId undefined, dynamicViewId set) → header shows raw column name, cell shows raw value (no crash, accepted fallback).
    - After rendering, calling upsertColumn(T, "amount", "New Label", null) re-renders the widget so the header updates to "New Label" (configVersion-driven). Use act() + the store's setState/upsert; assert the DOM updates.
  </behavior>
  <action>
    Add a `describe("RecordsTable column display config (COLAPPLY-V115-01)")` block to WidgetRenderer.spec.tsx
    (or a new sibling describe in the same file). Use the existing render harness for a records-type widget.

    Seed config via the REAL store before render:
    `useColumnDisplayConfigStore.getState().upsertColumn(T, "amount", "Amount Label", { kind: "number", ... })`
    (read columnFormatter.ts for the exact FormatSpec shape for a thousands-separator/decimal number; a
    minimal valid spec is sufficient — the assertion only needs formatted != raw).

    Write the five behaviors above. For the live-update test, wrap the mutation in `act(() => { ... })` and
    re-query the header text. For dv-bound, render a widget whose config has tableId undefined + dynamicViewId set.

    If the existing harness mocks the columnDisplayConfigStore, prefer using the REAL store (it is auto-reset
    by the zustand shim per src/test/setup.ts). Do NOT mock resolveLabel/resolveFormatter — exercise them.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/WidgetRenderer.spec.tsx 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - WidgetRenderer.spec.tsx contains a describe referencing "COLAPPLY-V115-01" (or "column display config").
    - Tests assert: header shows label; cell formatted-differs-from-raw; raw fallback when no config; dv-bound raw fallback; configVersion-driven live header update after upsertColumn.
    - `cd packages/web && npx vitest run src/components/charts/WidgetRenderer.spec.tsx` → 100% pass.
  </acceptance_criteria>
  <done>WidgetRenderer.spec.tsx passes with the five records-config behaviors; the configVersion re-render case is exercised at least once.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx tsc --noEmit` clean.
- `cd packages/web && npx vitest run src/components/charts/WidgetRenderer.spec.tsx` 100% pass.
- `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` green (no new hex introduced; WidgetRenderer not newly allowlisted by this plan).
- No new `materializeFilter`/`fromSwap`/`dropFilterView` imports added to WidgetRenderer.tsx.
</verification>

<success_criteria>
Records Table headers display the custom label (sort arrow intact), cells display the formatter output, unconfigured columns and dv-bound widgets fall back to raw, and a Phase 76 edit re-renders the open table live — all proven by passing tests and clean tsc.
</success_criteria>

<output>
After completion, create `.planning/phases/77-apply-labels-formatting-at-render-surfaces/77-01-SUMMARY.md`
</output>
