---
phase: 77-apply-labels-formatting-at-render-surfaces
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/lib/renderInfoTemplate.ts
  - packages/web/src/lib/renderInfoTemplate.spec.ts
  - packages/web/src/components/charts/InfoSelectionView.tsx
  - packages/web/src/components/charts/InfoSelectionView.spec.tsx
  - packages/web/src/components/LayersLegendPanel.spec.tsx
autonomous: true
requirements: [COLAPPLY-V115-03, COLAPPLY-V115-04]

must_haves:
  truths:
    - "Map info popup template mode: {column} substitution emits the column's formatted value"
    - "Map info popup KV mode: key cell shows the column's resolved label, value cell shows the formatted value"
    - "renderInfoTemplate remains a PURE lib — it imports no store; the formatter is injected via a callback argument"
    - "Editing a label/format in the Phase 76 editor re-renders the open info popup live (configVersion-driven)"
    - "The map layers legend (LayersLegendPanel) renders a column with a saved display label/format UNCHANGED — locked by an explicit guard test"
  artifacts:
    - path: "packages/web/src/lib/renderInfoTemplate.ts"
      provides: "formatValue callback param threaded into the {column} substitution; lib stays store-free"
      contains: "formatValue"
    - path: "packages/web/src/components/charts/InfoSelectionView.tsx"
      provides: "Binds formatValue with layer.table_id for template mode; KV key=resolveLabel + value=formatKvValue(resolveFormatter(...)); configVersion subscription"
      contains: "resolveFormatter(activeLayer.table_id"
    - path: "packages/web/src/components/LayersLegendPanel.spec.tsx"
      provides: "Guard test: a column with saved label/format renders UNCHANGED in the legend"
  key_links:
    - from: "InfoSelectionView template render"
      to: "renderInfoTemplate({ ..., formatValue })"
      via: "formatValue=(col,value)=>resolveFormatter(activeLayer.table_id, col)(value)"
      pattern: "formatValue"
    - from: "InfoSelectionView KV key cell"
      to: "resolveLabel(activeLayer.table_id, col)"
      via: "<th> render"
      pattern: "resolveLabel\\(activeLayer.table_id"
    - from: "InfoSelectionView KV value cell"
      to: "formatKvValue(resolveFormatter(activeLayer.table_id, col)(value))"
      via: "<td> render"
      pattern: "resolveFormatter\\(activeLayer.table_id"
    - from: "InfoSelectionView"
      to: "useColumnDisplayConfigStore configVersion"
      via: "primitive selector subscription"
      pattern: "useColumnDisplayConfigStore\\(\\(s\\) => s.configVersion\\)"
---

<objective>
Apply resolved labels + value formatters to map info popups in BOTH render modes (template `{column}` substitution and key/value), keeping `renderInfoTemplate` a PURE store-free lib by injecting the formatter via a callback. Add the explicit COLAPPLY-V115-04 guard test that the map layers legend is NOT affected.

Purpose: COLAPPLY-V115-03 (map popups both modes) + COLAPPLY-V115-04 (legend exclusion, test-locked).
Output: callback-extended `renderInfoTemplate.ts`, wired `InfoSelectionView.tsx`, and a `LayersLegendPanel.spec.tsx` guard test — plus tests for the popup modes.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-apply-labels-formatting-at-render-surfaces/77-CONTEXT.md

<interfaces>
From packages/web/src/store/columnDisplayConfigStore.ts:
```typescript
export const resolveLabel = (tableId: number, columnName: string): string;
export const resolveFormatter = (tableId: number, columnName: string): (v: unknown) => string | unknown;
export const useColumnDisplayConfigStore: <T>(selector: (s: ColumnDisplayConfigState) => T) => T;
// configVersion: number; loadConfig: (tableId: number) => Promise<void>
```

Current renderInfoTemplate.ts PURE contract (packages/web/src/lib/renderInfoTemplate.ts):
```typescript
export type RenderResult =
  | { mode: "template"; html: string }
  | { mode: "kv"; pairs: { col: string; value: unknown }[] };
export type RenderInfoTemplateArgs = {
  template: string | null;
  columns: string[];
  row: Record<string, unknown>;
  infoColumns?: string | null;
};
export function renderInfoTemplate(args: RenderInfoTemplateArgs): RenderResult;
// Template branch ({column} substitution) at :48-:53: currently emits raw String(v), "" for null/undefined.
```

InfoSelectionView.tsx call site (:419-:448):
- `activeLayer` is in scope and non-null in the render block (guarded at :354). `activeLayer.table_id` is the map-popup tableId (always defined — layers are table-bound).
- Template render at :426-:434 (dangerouslySetInnerHTML of result.html — NO sanitization, locked).
- KV render at :436-:447 — `result.pairs.map(({ col, value }) => <tr><th>{col}</th><td>{formatKvValue(value)}</td></tr>)`.
- `formatKvValue(v: unknown): string` at :478-:482 — coerces to string ("" for null/undefined, JSON.stringify objects).
</interfaces>

<constraints>
- renderInfoTemplate.ts MUST stay PURE: NO import of columnDisplayConfigStore (or any store). The formatter is passed IN as a callback. (Locked in 77-CONTEXT.md + the file's own header: "Pure libs take data/callbacks as args — no store imports.")
- The NO-SANITIZATION lock on the template branch stays (do not add a sanitizer; the formatted string still flows to dangerouslySetInnerHTML — formatted values are author-trusted, same risk model as today).
- LayersLegendPanel.tsx rendering is NOT modified and gets NO tableId/resolve* (COLAPPLY-V115-04 lock).
</constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a formatValue callback to renderInfoTemplate (keep lib pure) + unit tests</name>
  <files>packages/web/src/lib/renderInfoTemplate.ts, packages/web/src/lib/renderInfoTemplate.spec.ts</files>
  <read_first>
    - packages/web/src/lib/renderInfoTemplate.ts (full file — RenderInfoTemplateArgs, the :48-:53 template substitution, the kv-mode pairs construction :73-:76, the header lock comments)
    - packages/web/src/lib/renderInfoTemplate.spec.ts (existing test conventions — template substitution + kv-mode fallback cases)
  </read_first>
  <behavior>
    - When a `formatValue` callback is provided, the `{column}` substitution emits `formatValue(col, row[col])` (coerced to string) instead of raw `String(v)`. null/undefined → still "" (do NOT pass null/undefined through the formatter; preserve the empty-string lock).
    - When `formatValue` is NOT provided (undefined), behavior is IDENTICAL to today (raw String(v), "" for null/undefined) — backward compatible.
    - KV mode is UNCHANGED in this lib (pairs still carry raw `value`; the caller formats KV values — keeps the lib's kv contract stable and lets the caller resolve labels for keys).
    - The lib imports NO store (assert no columnDisplayConfigStore import).
  </behavior>
  <action>
    Extend `RenderInfoTemplateArgs` with an OPTIONAL callback:
    ```
    /** Optional value formatter for template-mode {column} substitution. (col, rawValue) => display string.
     *  Injected by the caller (InfoSelectionView) bound to layer.table_id — keeps this lib store-free.
     *  When omitted, substitution falls back to String(rawValue) (current behavior). */
    formatValue?: (col: string, value: unknown) => string;
    ```
    In the template branch (:48-:53): keep the null/undefined → "" guard FIRST, then:
    ```
    if (v === null || v === undefined) return "";
    return args.formatValue ? args.formatValue(col, v) : String(v);
    ```
    Leave KV mode (:73-:76) and the info_columns lenient-parse logic untouched.
    Add NO imports (the lib stays pure).

    Add unit tests to renderInfoTemplate.spec.ts:
    - template mode with a formatValue that uppercases / formats → substitution uses it.
    - template mode WITHOUT formatValue → identical raw behavior (regression guard).
    - null/undefined value with a formatValue present → still "" (formatter not invoked for null/undefined).
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/lib/renderInfoTemplate.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "formatValue" packages/web/src/lib/renderInfoTemplate.ts` matches in BOTH the type and the substitution branch.
    - `grep -nE "import .*columnDisplayConfigStore|from .*store/" packages/web/src/lib/renderInfoTemplate.ts` → NO matches (lib stays store-free).
    - renderInfoTemplate.spec.ts has the three new cases (with-formatter, without-formatter regression, null-stays-empty).
    - `cd packages/web && npx vitest run src/lib/renderInfoTemplate.spec.ts` → pass.
  </acceptance_criteria>
  <done>renderInfoTemplate accepts an optional formatValue callback used in template substitution, stays pure (no store import), is backward compatible, and its unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire formatter + label resolution into InfoSelectionView (template + KV) + configVersion</name>
  <files>packages/web/src/components/charts/InfoSelectionView.tsx</files>
  <read_first>
    - packages/web/src/components/charts/InfoSelectionView.tsx (activeLayer guard :354; render block :393-:474; template render :419-:434; KV render :436-:447; formatKvValue :478-:482; layer.table_id usage :181/:215/:260/:295)
    - packages/web/src/lib/renderInfoTemplate.ts (the formatValue param added in Task 1)
    - packages/web/src/store/columnDisplayConfigStore.ts (resolveLabel/resolveFormatter/loadConfig/configVersion)
    - packages/web/src/components/charts/LegendRenderer.tsx:88-104 (primitive-selector + useMemo reactivity pattern)
  </read_first>
  <action>
    In InfoSelectionView.tsx:

    1. Import `{ useColumnDisplayConfigStore, resolveLabel, resolveFormatter }` from "../../store/columnDisplayConfigStore".

    2. Subscribe to configVersion with a primitive selector near the other store subscriptions:
       `const configVersion = useColumnDisplayConfigStore((s) => s.configVersion);`
       Reference it so the render re-runs on edit (the resolve* are getState()-based; the subscription is the
       re-render trigger). It is read in the render path naturally once you use resolve* below; if lint flags
       it unused, include it in the loadConfig effect deps or void it.

    3. Populate the cache: add a useEffect calling `loadConfig(activeLayer.table_id)` when the active layer
       (and thus table_id) is known. Guard against activeLayer === null. Key the effect on activeLayer?.table_id.
       (Place it with the other effects; activeLayer is resolved at :123.)

    4. TEMPLATE mode (:419-:434): pass a formatValue callback bound to activeLayer.table_id into
       renderInfoTemplate:
       ```
       const result = renderInfoTemplate({
         template: activeLayer.info_template,
         columns: sortedColumns,
         row: currentRow,
         infoColumns: activeLayer.info_columns,
         formatValue: (col, value) => String(resolveFormatter(activeLayer.table_id, col)(value) ?? ""),
       });
       ```
       (activeLayer.table_id is always defined for a layer. The template branch still flows result.html to
       dangerouslySetInnerHTML — NO sanitizer added, lock preserved.)

    5. KV mode (:436-:447): key cell = resolved label, value cell = formatted value:
       ```
       {result.pairs.map(({ col, value }) => (
         <tr key={col}>
           <th scope="row">{resolveLabel(activeLayer.table_id, col)}</th>
           <td>{formatKvValue(resolveFormatter(activeLayer.table_id, col)(value))}</td>
         </tr>
       ))}
       ```
       (resolveFormatter(...)(value) returns string|unknown; formatKvValue coerces it safely — keep formatKvValue
       as the final coercion so objects/null still render as today.)

    Do NOT modify the layer dropdown header, nav footer, or the info-query logic. Add NO materialize/fromSwap
    imports (InfoSelectionView already calls infoQuery — a READ; add nothing new).
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "formatValue:" packages/web/src/components/charts/InfoSelectionView.tsx` matches inside the renderInfoTemplate call.
    - `grep -n "resolveLabel(activeLayer.table_id" packages/web/src/components/charts/InfoSelectionView.tsx` matches (KV key).
    - `grep -n "resolveFormatter(activeLayer.table_id" packages/web/src/components/charts/InfoSelectionView.tsx` matches (template callback AND KV value).
    - `grep -n "useColumnDisplayConfigStore((s) => s.configVersion)" packages/web/src/components/charts/InfoSelectionView.tsx` matches.
    - `grep -n "loadConfig" packages/web/src/components/charts/InfoSelectionView.tsx` matches inside a useEffect keyed on activeLayer table_id.
    - `grep -nE "materializeFilter|fromSwap|dropFilterView" packages/web/src/components/charts/InfoSelectionView.tsx` → no NEW occurrences.
    - `cd packages/web && npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Template mode substitutes formatted values via the injected callback (bound to layer.table_id), KV mode shows resolved label keys + formatted values, InfoSelectionView subscribes to configVersion and loads config per active layer, tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests — popup template + KV apply label/format + configVersion; LEGEND guard (exclusion)</name>
  <files>packages/web/src/components/charts/InfoSelectionView.spec.tsx, packages/web/src/components/LayersLegendPanel.spec.tsx</files>
  <read_first>
    - packages/web/src/components/charts/InfoSelectionView.spec.tsx (existing harness: how it seeds an active layer, infoSelectionStore, layers with table_id, template vs KV modes)
    - packages/web/src/components/LayersLegendPanel.spec.tsx (existing legend render tests — extend with the guard case; understand how it renders a layer/break with a column name)
    - packages/web/src/components/LayersLegendPanel.tsx (confirm it is props-only/presentational — the guard test asserts it ignores column display config)
    - packages/web/src/store/columnDisplayConfigStore.ts (upsertColumn to seed a label/format; auto-reset via shim)
  </read_first>
  <behavior>
    - InfoSelectionView TEMPLATE mode: with a saved format for a column referenced in the template (e.g. `{amount}`), the rendered popup HTML shows the FORMATTED value, not the raw value.
    - InfoSelectionView KV mode: with a saved label + format for a column, the key cell shows the label and the value cell shows the formatted value; a column with NO config shows raw name + raw value (fallback).
    - configVersion: after upsertColumn changes the label/format, the open popup re-renders with the new label/format (act()).
    - LEGEND GUARD (COLAPPLY-V115-04): seed columnDisplayConfigStore with a saved display label AND format for a column/value that the LayersLegendPanel renders (e.g. a break label or a column referenced by the legend). Render LayersLegendPanel. ASSERT the legend text is the RAW break/layer/column value — UNCHANGED — i.e. the saved label/format is NOT applied. The test name must clearly reference the exclusion (e.g. "COLAPPLY-V115-04: legend ignores column display config").
  </behavior>
  <action>
    InfoSelectionView.spec.tsx: add a describe ("InfoSelectionView column display config (COLAPPLY-V115-03)")
    seeding the REAL columnDisplayConfigStore via upsertColumn for the active layer's table_id, then assert
    template-mode formatted output, KV label+format, raw fallback, and a configVersion live update.

    LayersLegendPanel.spec.tsx: add a guard describe/it ("COLAPPLY-V115-04: layers legend is NOT affected by
    column display config"). Seed columnDisplayConfigStore with a label/format for a column name that appears
    in the legend's rendered break/layer data, render the panel with that data, and assert the rendered text
    equals the RAW value (no label substitution, no formatting). Confirm by grepping the rendered output for
    the raw string and asserting the configured label string is ABSENT.

    Use the REAL store (auto-reset). Do NOT add resolve*/tableId to LayersLegendPanel.tsx — the guard proves
    the component already ignores the config without any wiring.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/InfoSelectionView.spec.tsx src/components/LayersLegendPanel.spec.tsx 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - InfoSelectionView.spec.tsx contains a describe referencing "COLAPPLY-V115-03" with template-format, KV label+format, raw-fallback, and configVersion-live-update assertions.
    - LayersLegendPanel.spec.tsx contains a test referencing "COLAPPLY-V115-04" that seeds a saved label/format and asserts the legend renders the RAW value UNCHANGED (configured label string absent from output).
    - `grep -nE "resolveLabel|resolveFormatter|columnDisplayConfig|tableId" packages/web/src/components/LayersLegendPanel.tsx` → NO matches (component never wired to the config).
    - `cd packages/web && npx vitest run src/components/charts/InfoSelectionView.spec.tsx src/components/LayersLegendPanel.spec.tsx` → 100% pass.
  </acceptance_criteria>
  <done>Popup template + KV format/label assertions pass (incl. raw fallback + configVersion re-render); the LayersLegendPanel guard test passes proving the legend renders unchanged; LayersLegendPanel.tsx has no config wiring.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx tsc --noEmit` clean.
- `cd packages/web && npx vitest run src/lib/renderInfoTemplate.spec.ts src/components/charts/InfoSelectionView.spec.tsx src/components/LayersLegendPanel.spec.tsx` 100% pass.
- `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` green (no new component hex introduced).
- renderInfoTemplate.ts imports no store; LayersLegendPanel.tsx unmodified (no resolve*/tableId).
- No new `materializeFilter`/`fromSwap`/`dropFilterView` imports in InfoSelectionView.tsx.
</verification>

<success_criteria>
Map info popups show resolved labels + formatted values in BOTH template and KV modes (with raw fallback and live configVersion re-render), `renderInfoTemplate` stays a pure store-free lib (formatter injected via callback), and an explicit guard test locks the map layers legend as UNAFFECTED by column display config.
</success_criteria>

<output>
After completion, create `.planning/phases/77-apply-labels-formatting-at-render-surfaces/77-03-SUMMARY.md`
</output>
