---
phase: 57-verification-live-uat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/57-verification-live-uat/57-UAT.md
autonomous: false
requirements: [VERIFY-V110-01]

must_haves:
  truths:
    - "57-UAT.md scripts §1 analyst restriction (list shows only granted; Manage-access button absent; ungranted/revoked open shows inline no-access panel + Back)"
    - "57-UAT.md scripts §2 grant/revoke immediate effect for a USER grant AND a ROLE grant, including the pre-provisioning headline (grant before first login)"
    - "57-UAT.md scripts §3 bypass non-regression (admin AND designer see/open ALL dashboards, unchanged)"
    - "Each section has an explicit operator attestation slot (PASS/FAIL + notes) and a gaps block"
    - "The operator runs the walk live against deployed Kinetica (password mode) using the separate non-admin logins and attests every section"
  artifacts:
    - path: ".planning/phases/57-verification-live-uat/57-UAT.md"
      provides: "Self-contained operator UAT script for the 3 live SCs + per-section attestation + gaps block + attestation summary"
      contains: "overall_result"
  key_links:
    - from: "57-UAT.md §0 preconditions"
      to: "57-01-AUTOMATED-GATES.md"
      via: "automated_gates_ref header citing the SC4 record"
      pattern: "automated_gates_ref"
    - from: "57-UAT.md attestation summary"
      to: "57-VERIFICATION.md (57-03)"
      via: "overall_result + per-section PASS/FAIL consumed by the compiler"
      pattern: "overall_result"
---

<objective>
Author `57-UAT.md` — a self-contained operator walk-through proving the three LIVE v1.10 per-dashboard view-permission success criteria — then PAUSE for the operator to run it against the deployed app and attest each section. Mirrors the v1.9 `54-UAT.md` format (sections + per-section PASS/FAIL + gaps block).

Purpose: SC1 (analyst restriction on list + direct nav), SC2 (grant/revoke immediate effect for user and role grants, incl. pre-provisioning), and SC3 (admin/designer bypass non-regression) are user-observable behaviors against the running system — only a live operator walk can attest them. SC4 (automated gates) is covered by 57-01 and cited in §0.

Output: `.planning/phases/57-verification-live-uat/57-UAT.md` authored by Claude, then attested PASS/FAIL by the operator at the blocking checkpoint.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/57-verification-live-uat/57-CONTEXT.md

<!-- Format precedent — reuse this doc's structure (Section 0 preconditions → numbered sections with id/check/status/evidence blocks → gaps yaml → attestation summary → traceability) -->
@.planning/phases/54-verification-live-walk-through/54-UAT.md

<!-- The shipped behavior under test — cite exact strings/affordances when writing the checks -->
@.planning/phases/55-access-model-server-enforcement/55-VERIFICATION.md
@.planning/phases/56-access-management-ui-list-open-ux/56-VERIFICATION.md

<!-- SC4 record this UAT cites in §0 (authored by sibling plan 57-01; same wave) -->
@.planning/phases/57-verification-live-uat/57-01-AUTOMATED-GATES.md

<feature_facts>
<!-- Exact shipped affordances/strings to write the live checks against (from 55/56 verification). -->
- List view is server-filtered (ENFORCE-V110-01): GET /api/dashboards returns only viewable dashboards. There is NO client-side filter — the list simply shows what the server returns.
- "Manage access" button: ghost-sm button in each dashboard list row's .ds-actions cell, gated by hasPermission("dashboards:manage_access") — ABSENT from the DOM (not disabled) for users without the permission (GRANTUI-V110-03).
- DashboardAccessModal: People section (free-text username input + datalist — supports pre-provisioning of an unknown username) + Roles section (dropdown from listRoles()); info line "Admins and designers always have access"; add/remove persist via the grant API and the list reflects the returned grants immediately (GRANTUI-V110-01/02).
- Empty list message (analyst with zero grants): "No dashboards have been shared with you yet." (LISTUX-V110-01).
- Direct nav / stale URL to a non-permitted dashboard: server returns 404 "Dashboard not found." on the scoped routes; UI short-circuits to an inline "No access" ChartCard reading "You don't have access to this dashboard." with a "Back to dashboards" button — NOT a broken/empty grid (LISTUX-V110-02).
- Bypass roles = admin + designer (see/open ALL); analyst is restricted to grants. Grants resolve via union of direct user grant OR any of the user's role grants (ACCESS-V110-03). Usernames are stored lowercased server-side.
- Test identities the operator has ready: an analyst-role login (restricted subject) and a non-admin manage_access login (designer or user_admin) for grant/revoke. Operator (admin, RPereira@kinetica.com) drives the UAT and can use Roles/Users pages mid-walk if needed.
- Environment: live deployed Kinetica, password mode.
</feature_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 57-UAT.md (self-contained operator walk-through)</name>
  <read_first>
    - .planning/phases/54-verification-live-walk-through/54-UAT.md (copy its structure: YAML header w/ operator + automated_gates_ref; Section 0 preconditions; numbered sections of id/check/status:TBD/evidence blocks; Section: gaps yaml; Attestation Summary; Traceability)
    - The <feature_facts> block above (exact strings + affordances to write checks against)
    - .planning/phases/56-access-management-ui-list-open-ux/56-VERIFICATION.md (confirm the exact UI strings)
  </read_first>
  <action>
    Write `.planning/phases/57-verification-live-uat/57-UAT.md` in the 54-UAT.md shape. Author ALL prose now; leave each
    check's `status:` as `TBD` and `evidence:` blank for the operator to fill at the checkpoint.

    YAML header: `plan: 57-02`, `operator: RPereira@kinetica.com`, `started_on: <today>`,
    `automated_gates_ref: .planning/phases/57-verification-live-uat/57-01-AUTOMATED-GATES.md`,
    `automated_gates_verdict: <to be filled from 57-01 ALL PASS verdict>`.

    Intro: purpose = live proof of the 3 user-observable v1.10 SCs; self-contained (no other planning files needed to
    run the walk); environment = deployed Kinetica, password mode; outcome routing = any `status: FAIL` halts the UAT
    and spins a decimal 57.x repro-test-driven gap plan, then the affected section is re-walked before 57-03 compiles
    (per 57-CONTEXT gap policy — security feature, NOT accept-as-tech-debt; trivial fixes may ride inline at executor discretion).

    ## Section 0 — Preconditions (each a PASS/FAIL block):
      - P1: App is running (web + server) against deployed Kinetica in password mode.
      - P2: Operator has the two non-admin logins ready — an analyst-role user and a non-admin manage_access user
            (designer or user_admin). State each username (lowercased) the operator will use.
      - P3: At least two dashboards exist; identify which will be GRANTED to the analyst vs. left UNGRANTED.
      - P4: 57-01 automated gates recorded ALL PASS (cite 57-01-AUTOMATED-GATES.md header) — record-only, no manual rerun.

    ## Section 1 — Analyst Restriction  [ROADMAP SC1]  (log in as the analyst-role user)
      - 1.1: The dashboard list shows ONLY dashboards granted to the analyst — ungranted dashboards are ABSENT from the
             list (server-filtered; not greyed, not present). If the analyst currently has zero grants, the friendly
             empty state "No dashboards have been shared with you yet." appears.
      - 1.2: The "Manage access" button is ABSENT from the DOM on every list row (hide-don't-disable) — not present, not
             disabled. (GRANTUI-V110-03)
      - 1.3: Direct-nav / stale-URL to an UNGRANTED dashboard (paste the dashboard's URL) shows the inline no-access
             panel: a "No access" card reading "You don't have access to this dashboard." with a "Back to dashboards"
             button — NOT a broken or empty dashboard grid. "Back to dashboards" returns to the list. (LISTUX-V110-02)

    ## Section 2 — Grant / Revoke Immediate Effect  [ROADMAP SC2]  (use the manage_access login to mutate; the analyst login to observe)
      - 2.1 USER grant: as the manage_access user, open Manage access on a dashboard the analyst CANNOT currently see;
             add a USER grant for the analyst's username (free-text). Then, as the analyst, refresh the list / re-open —
             the dashboard now APPEARS and OPENS. (GRANTUI-V110-02 + ENFORCE-V110-01/02)
      - 2.2 ROLE grant: as the manage_access user, on a different dashboard the analyst cannot see, add a ROLE grant for
             a role the analyst HOLDS (choose from the role dropdown). As the analyst, the dashboard now APPEARS/OPENS
             (union-via-role resolution). (ACCESS-V110-03)
      - 2.3 PRE-PROVISIONING (headline): pick a username that has NEVER logged in (or whose grant is added before that
             user's next login); as the manage_access user, add a USER grant for that username; then log in as that user
             for the first time → the granted dashboard is visible/openable on first login. (the headline workflow)
      - 2.4 REVOKE user grant: remove the 2.1 user grant; as the analyst, the dashboard DISAPPEARS from the list and a
             direct-nav open now shows the no-access panel.
      - 2.5 REVOKE role grant: remove the 2.2 role grant; as the analyst, that dashboard DISAPPEARS / open denied.

    ## Section 3 — Bypass Non-Regression  [ROADMAP SC3]  (log in as admin, then as designer)
      - 3.1 ADMIN: sees and opens ALL dashboards (including ones with no grants and ones granted only to others) —
             unchanged from before v1.10; existing governance workflow intact.
      - 3.2 DESIGNER: sees and opens ALL dashboards likewise (designer is a bypass role); the design/edit workflow is
             unaffected — designer still sees Edit/Delete affordances as before.

    ## Section 4 — Gaps Block:  a ```yaml gaps:``` block (id/severity/in_scope/sections/title/resolution) — empty list
        if the walk is clean; one entry per defect otherwise (GAP-57-NN), routed to a 57.x plan.

    ## Attestation Summary: ```overall_result: passed|gaps_found```, sections_passed, sections_failed,
        operator_notes, attested_by, attested_on (operator fills at the checkpoint).

    ## Traceability table: map ROADMAP SC1→§1, SC2→§2, SC3→§3, SC4→57-01-AUTOMATED-GATES.md; VERIFY-V110-01 → §0–§4 (all).
  </action>
  <verify>
    <automated>test -f .planning/phases/57-verification-live-uat/57-UAT.md && grep -q 'automated_gates_ref' .planning/phases/57-verification-live-uat/57-UAT.md && grep -q 'overall_result' .planning/phases/57-verification-live-uat/57-UAT.md && grep -Eiq 'pre-provision' .planning/phases/57-verification-live-uat/57-UAT.md && grep -q "don't have access to this dashboard" .planning/phases/57-verification-live-uat/57-UAT.md && grep -Eq 'Section 1|## Section 1' .planning/phases/57-verification-live-uat/57-UAT.md && grep -Eq 'Section 3|## Section 3' .planning/phases/57-verification-live-uat/57-UAT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `57-UAT.md` exists with the 54-UAT.md structure (header + §0 preconditions + numbered sections + gaps yaml + attestation summary + traceability).
    - §1 covers list-only-granted + Manage-access-button-absent + inline no-access panel on direct nav.
    - §2 covers USER grant, ROLE grant, the pre-provisioning headline, and REVOKE of each (immediate effect).
    - §3 covers admin AND designer bypass non-regression.
    - Header `automated_gates_ref` cites 57-01-AUTOMATED-GATES.md; traceability maps SC4 to it.
    - Every check carries a fillable `status:` slot; an `overall_result` slot exists in the attestation summary.
  </acceptance_criteria>
  <done>57-UAT.md authored and committed, all checks scripted with TBD status slots, ready for the operator to run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Operator runs 57-UAT.md live and attests SC1/SC2/SC3</name>
  <action>Operator executes the authored 57-UAT.md against the deployed app (password mode), switching between the analyst, non-admin manage_access, and admin/designer logins; fills each check's status/evidence and the attestation summary. Exact steps are in how-to-verify below.</action>
  <what-built>
    Claude authored `57-UAT.md` — a self-contained operator walk-through covering the three live v1.10 success
    criteria (SC1 analyst restriction on list + direct nav, SC2 grant/revoke immediate effect for user + role grants
    incl. pre-provisioning, SC3 admin/designer bypass non-regression). SC4 (automated gates) is recorded in
    57-01-AUTOMATED-GATES.md and cited in §0. No application code was changed.
  </what-built>
  <how-to-verify>
    1. Confirm the app is running against the deployed Kinetica in password mode, and that 57-01 recorded ALL PASS.
    2. Open `.planning/phases/57-verification-live-uat/57-UAT.md` and execute it top to bottom, switching between your
       three logins (analyst, non-admin manage_access user, admin/designer):
         §0 Preconditions — confirm each P-item.
         §1 (as analyst): list shows ONLY granted dashboards (ungranted absent / friendly empty state); "Manage access"
            button absent on every row; direct-nav to an ungranted dashboard URL shows the "You don't have access to
            this dashboard." panel + Back — not a broken grid.
         §2 (manage_access user grants; analyst observes): grant the analyst a USER grant → analyst sees/opens it on
            next list/open; grant a ROLE the analyst holds → analyst sees it; PRE-PROVISION a username before its first
            login → that user sees the dashboard on first login; then REVOKE the user grant and the role grant → each
            disappears for the analyst (and direct open is denied).
         §3 (as admin, then designer): each sees and opens ALL dashboards, unchanged.
    3. Fill `status: PASS|FAIL` + `evidence:` for every check; complete the Attestation Summary
       (`overall_result: passed` if all PASS; `gaps_found` if any FAIL) and fill the gaps yaml for each defect.
    4. If any section FAILS: stop, log the gap (GAP-57-NN). A 57.x repro-test-driven gap plan will close it, the
       affected section is re-walked, then 57-03 compiles.
  </how-to-verify>
  <resume-signal>Type "approved" when all sections attest PASS (overall_result: passed), or describe each FAIL / gap so a 57.x gap plan can be spun.</resume-signal>
</task>

</tasks>

<verification>
- `57-UAT.md` exists, is self-contained, mirrors the 54-UAT.md format, and cites 57-01-AUTOMATED-GATES.md in §0.
- After the checkpoint: every check has `status` filled, the Attestation Summary has `overall_result`, and any FAIL has a corresponding gaps entry.
- No application/production code modified by this plan.
</verification>

<success_criteria>
The three live success criteria (SC1/SC2/SC3) are scripted and attested by the operator against the deployed system. `overall_result: passed` means the milestone-gate live walk is clean; `gaps_found` routes to a 57.x cycle before 57-03 compiles.
</success_criteria>

<output>
After the checkpoint resolves, create `.planning/phases/57-verification-live-uat/57-02-SUMMARY.md` capturing the operator's verdict per section and any gaps logged.
</output>
