---
phase: 57-verification-live-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/57-verification-live-uat/57-01-AUTOMATED-GATES.md
autonomous: true
requirements: [VERIFY-V110-01]

must_haves:
  truths:
    - "Frontend vitest is recorded 100% green (run from packages/web), no failures"
    - "Web tsc --noEmit is recorded clean (zero errors)"
    - "Server tsc --noEmit is recorded clean (zero errors)"
    - "Server vitest failing-file set is recorded as a SUBSET of the TD-V16-TEST-ISOLATION known-flaky list (never a fixed pass-count)"
    - "Targeted dashboard-access specs (server + web) are recorded as PASS"
  artifacts:
    - path: ".planning/phases/57-verification-live-uat/57-01-AUTOMATED-GATES.md"
      provides: "Recorded SC4 automated-gate results with commands, outputs, and PASS/FAIL verdict per gate"
      contains: "overall_verdict"
  key_links:
    - from: "57-01-AUTOMATED-GATES.md"
      to: "57-VERIFICATION.md (57-03)"
      via: "automated_gates_ref header + ALL-PASS verdict consumed by the compiler"
      pattern: "overall_verdict"
---

<objective>
Run all four SC4 automated gates fresh against HEAD and record the verdict into `57-01-AUTOMATED-GATES.md`. This is the SC4 evidence record for the v1.10 milestone-gate verification. No feature code — this plan runs commands and writes a record.

Purpose: SC4 of the ROADMAP requires "frontend vitest 100%, web + server tsc clean, and the server vitest set-based known-flaky gate holds." The compiled verification (57-03) and the UAT preconditions (57-02 §0) both reference this record, so it must be authored first and be self-contained.

Output: `.planning/phases/57-verification-live-uat/57-01-AUTOMATED-GATES.md` with one block per gate (command, observed output, verdict) and a final overall verdict.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

<!-- Format precedent — author 57-01 in this shape -->
@.planning/phases/54-verification-live-walk-through/54-VERIFICATION.md

<!-- What shipped + the canonical known-flaky set + the targeted specs -->
@.planning/phases/55-access-model-server-enforcement/55-VERIFICATION.md
@.planning/phases/56-access-management-ui-list-open-ux/56-VERIFICATION.md

<gate_facts>
<!-- Encoded from 57-CONTEXT.md <code_context> and the 55/56 verification reports. Use these directly. -->

FRONTEND vitest (DETERMINISTIC, 100% bar):
  - Run from packages/web: `cd packages/web && npx vitest run`
  - Last recorded baseline: 1725 tests across 82 files passed (56-02-SUMMARY, 56-VERIFICATION).
  - A pre-existing unhandled-rejection in WidgetRenderer.spec.tsx (timer artifact in "no_filter over-threshold")
    is NOT a Phase-57 regression — present since before Phase 56 (commit 705b7b1). All tests still pass; record it as a known non-regression note. If the test COUNT regresses below 1725 or any test FAILS, that IS a gate failure.

WEB tsc: `npx tsc --noEmit -p packages/web` — MUST be clean (zero output, exit 0).
SERVER tsc: `npx tsc --noEmit -p packages/server` — MUST be clean (zero output, exit 0). Separate gate; vitest does not type-check.

SERVER vitest (NONDETERMINISTICALLY FLAKY → SET-BASED gate only, NEVER a fixed pass-count):
  - Run from packages/server: `cd packages/server && npx vitest run`  (or root `npm run test:server`)
  - GATE PASSES iff the set of FAILING FILES ⊆ the TD-V16-TEST-ISOLATION known-flaky list:
      auth.oidc, auth.routes, boot.hardening, boot.wipe, bootstrap, db.smoke, oidc.module, routes.wms
    (matched by spec filename; the exact failing count varies run-to-run and is irrelevant).
  - If a failing file is NOT in that list → GATE FAILS (real regression).

TARGETED dashboard-access specs (the feature-under-test must be GREEN):
  - Server (from packages/server):
      `npx vitest run tests/routes.dashboard-access.spec.ts tests/lib.dashboardAccessDb.spec.ts`
  - Web (from packages/web):
      `npx vitest run src/api/client.dashboard-access.spec.ts src/components/DashboardAccessModal.spec.tsx src/components/DashboardsPage.spec.tsx`
</gate_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run + record the four SC4 gates and the targeted dashboard-access specs</name>
  <read_first>
    - .planning/phases/54-verification-live-walk-through/54-VERIFICATION.md (the "Automated Gate Results" table format to mirror)
    - 57-CONTEXT.md <code_context> "Test-gate reality (for SC4)"
    - The <gate_facts> block above (exact commands + the known-flaky file set)
  </read_first>
  <action>
    Run each gate command below from the repo root (or with the `cd` shown), capturing exit code and the tail of output. Run, do not skip — these must be FRESH against current HEAD.

    1. Frontend vitest (deterministic, 100% bar):
         `cd packages/web && npx vitest run`
       Record: total test count, file count, failures. PASS iff 0 failures (count must be >= 1725).
       Note the known pre-existing WidgetRenderer.spec.tsx unhandled-rejection as a non-regression footnote.

    2. Web tsc: `npx tsc --noEmit -p packages/web`  → PASS iff clean (no output, exit 0).

    3. Server tsc: `npx tsc --noEmit -p packages/server`  → PASS iff clean (no output, exit 0).

    4. Server vitest SET-BASED gate:
         `cd packages/server && npx vitest run`
       Extract the list of FAILING spec FILES. PASS iff that set ⊆ {auth.oidc, auth.routes, boot.hardening,
       boot.wipe, bootstrap, db.smoke, oidc.module, routes.wms}. Record the failing-file set explicitly and the
       subset verdict. DO NOT record a required pass-count — this is set-based only.

    5. Targeted dashboard-access specs (feature-under-test, must be GREEN):
         server: `cd packages/server && npx vitest run tests/routes.dashboard-access.spec.ts tests/lib.dashboardAccessDb.spec.ts`
         web:    `cd packages/web && npx vitest run src/api/client.dashboard-access.spec.ts src/components/DashboardAccessModal.spec.tsx src/components/DashboardsPage.spec.tsx`
       Record per-file pass counts. PASS iff all targeted files green.

    Then WRITE `.planning/phases/57-verification-live-uat/57-01-AUTOMATED-GATES.md` mirroring the 54-VERIFICATION
    "Automated Gate Results" shape:
      - YAML header: `recorded_on`, `commit` (current HEAD short sha from `git rev-parse --short HEAD`), `overall_verdict: ALL PASS | GATE FAILED`.
      - A markdown table: | Gate | Command | Result | Numbers/Notes | with one row per gate (frontend_vitest, web_tsc, server_tsc, server_vitest_setgate, targeted_dashboard_access_specs).
      - For server_vitest_setgate: list the failing files and assert "⊆ TD-V16-TEST-ISOLATION known-flaky list".
      - A short "Known non-regression" note for WidgetRenderer.spec.tsx.
      - A final `overall_verdict` line.

    If any gate FAILS (frontend failure/count regression, either tsc dirty, a server failing-file outside the
    known-flaky set, or any targeted spec red): record `overall_verdict: GATE FAILED`, document precisely which gate
    and why, and STOP (do not fabricate a pass). A genuine gate failure is a defect → routes to a 57.x gap cycle,
    same policy as a UAT FAIL.
  </action>
  <verify>
    <automated>test -f .planning/phases/57-verification-live-uat/57-01-AUTOMATED-GATES.md && grep -q 'overall_verdict' .planning/phases/57-verification-live-uat/57-01-AUTOMATED-GATES.md && grep -Eq 'server_vitest_setgate|set-based|known-flaky' .planning/phases/57-verification-live-uat/57-01-AUTOMATED-GATES.md && grep -q 'TD-V16-TEST-ISOLATION' .planning/phases/57-verification-live-uat/57-01-AUTOMATED-GATES.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `57-01-AUTOMATED-GATES.md` exists with a YAML header containing `overall_verdict`.
    - All four SC4 gates each have a recorded command + observed result.
    - The server vitest gate is recorded SET-BASED (failing files enumerated + "⊆ TD-V16-TEST-ISOLATION") with NO required pass-count.
    - Targeted dashboard-access specs (server + web) recorded PASS.
    - If `overall_verdict: ALL PASS` — frontend 0 failures, both tsc clean, server failing-set ⊆ known-flaky, all targeted specs green.
  </acceptance_criteria>
  <done>57-01-AUTOMATED-GATES.md committed with a per-gate record and an explicit overall_verdict (ALL PASS or GATE FAILED with the failing gate documented).</done>
</task>

</tasks>

<verification>
- File `.planning/phases/57-verification-live-uat/57-01-AUTOMATED-GATES.md` exists.
- Contains `overall_verdict`, the four gates, the set-based server gate language, and the TD-V16-TEST-ISOLATION reference.
- No production code modified (this plan touches only the planning doc).
</verification>

<success_criteria>
SC4 automated gates are run fresh and recorded with a clear verdict. The record is self-contained enough that 57-02 §0 (preconditions) and 57-03 (compiler) can cite it by reference.
</success_criteria>

<output>
After completion, create `.planning/phases/57-verification-live-uat/57-01-SUMMARY.md`.
</output>
