---
phase: 117-dashboard-settings-links
plan: 05
type: execute
wave: 5
depends_on: ["117-04"]
files_modified:
  - packages/web/src/App.deeplink.spec.tsx
  - packages/web/src/App.signincommit.spec.tsx
  - packages/web/src/App.passwordDeepLink.spec.tsx
  - packages/web/src/App.spec.tsx
autonomous: true
requirements: [DSET-V122-03, DSET-V122-04, DSET-V122-07, DSET-V122-08]

must_haves:
  truths:
    - "Visiting ?dashboard=12&mode=view lands on the settings screen with no dashboard-list page in between."
    - "Visiting ?dashboard=12&mode=edit lands on the edit screen."
    - "Visiting a bare ?dashboard=12 still lands on the running dashboard, and the address bar still carries no mode qualifier."
    - "A settings link to a dashboard that is missing or not permitted lands on the list with the one combined, non-leaking message."
    - "A logged-out visit to a settings link routes through login and lands on the named screen, in both password and OIDC modes."
  artifacts:
    - path: "packages/web/src/App.deeplink.spec.tsx"
      provides: "End-to-end arrival coverage for all three modes, including the no-flash and failure paths"
      contains: "DSET-117:"
    - path: "packages/web/src/App.signincommit.spec.tsx"
      provides: "kbi_returnTo round-trip coverage for the mode qualifier, including read validation"
      contains: "DSET-117:"
  key_links:
    - from: "the DashboardsPage stub's data-mode attribute"
      to: "App.tsx's initialOpenDashboard payload"
      via: "assertions read data-mode to prove the resolved mode reached the page"
      pattern: "data-mode"
---

<objective>
Prove, at the `App` level, that a dashboard settings link does what the milestone promises — and
that the bare link everyone already has still does what it always did.

Purpose: DSET-V122-03 (arrival in the named mode, no list flash), -04 (the combined failure
message), -07 (the logged-out journey, both auth modes, one storage key), and -08, which gets its
own dedicated audit task rather than being assumed from a green suite.

Output: new coverage in four existing App spec files. **No production code changes.** If any test
written here fails, the fix belongs in Plans 01-04's files — do not adjust an assertion to match
the implementation.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/117-dashboard-settings-links/117-RESEARCH.md
@.planning/phases/115-deep-link-authentication-flow/115-CONTEXT.md

<a_correction_to_research_worth_knowing_before_you_start>
`117-RESEARCH.md` §Q5 suggests `App.spec.tsx` should gain "a `dashboardMode` junk-value block,
mirroring the existing `page`/`dashboardId` corrupt-value template". **That file's harness cannot
carry most of it**, and its own comment at `:195-198` says why: its `vi.mock("./api/client")` does
NOT stub `listDashboards`, so any ReturnTo carrying a VALID `dashboardId` would leave the deep link
`"pending"` forever and attempt a real network call. Only NEGATIVE (rejected) cases can live there.

A junk `dashboardMode` next to a junk `dashboardId` proves nothing — the id does all the rejecting,
so the criterion could not discriminate. Therefore:
- The mode READ-VALIDATION tests (valid id + junk/absent/unrecognised mode) go in
  `App.signincommit.spec.tsx`, which DOES stub `listDashboards`.
- `App.spec.tsx` gets exactly ONE new test, the one its harness can actually carry: a ReturnTo
  containing a `dashboardMode` but NO `dashboardId` must not wedge the boot.

Record this deviation in the SUMMARY.
</a_correction_to_research_worth_knowing_before_you_start>

<the_page_stub_trap_third_consecutive_phase>
`App.deeplink.spec.tsx:27-32`'s own comment, `115-RESEARCH` §Q1 and `116-RESEARCH` §Q5 have each
independently hit and re-documented this, and the project memory log shows it has caught researchers
who had already read the warning.

Plan 02 already fixed the three stubs to the `{ dashboard, mode }` shape while KEEPING the
`useState(() => initialOpenDashboard)` mount-only lazy capture. **Do not "simplify" them back to a
plain prop reflector while writing these tests.** A dumb reflector FALSELY FAILS against correct
code, because `App`'s own effects (the `deepLinkConsumedRef` flip, `setDashboardViewMode`) force a
second render in the same effect flush after which the real prop is already `undefined` — which is
irrelevant to the real component, since it never re-reads the prop after mount.

If a new test seems to need the stub to re-read the prop, the test is wrong, not the stub.
</the_page_stub_trap_third_consecutive_phase>

<interfaces>
<!-- The stub shape Plan 02 installed in all three App spec files. Assertions below read data-mode. -->
```tsx
vi.mock("./components/DashboardsPage", () => ({
  default: ({ initialOpenDashboard }: { initialOpenDashboard?: { dashboard: { id: number }; mode: string } }) => {
    const [captured] = useState(() => initialOpenDashboard);
    return (
      <main data-testid="page-dashboards"
            data-deeplink={captured ? String(captured.dashboard.id) : ""}
            data-mode={captured?.mode ?? ""}>
        Dashboards
      </main>
    );
  },
}));
```

<!-- The ALREADY-SHIPPED table equivalents whose test SHAPES these mirror. Read only. -->
packages/web/src/App.tableDeeplink.spec.tsx — `:144` opens in VIEW, `:155` opens in EDIT,
`:164` `&mode=banana` opens in VIEW not as a failure, `:174`/`:184`/`:193` the banner trio,
`:224` unauthenticated arrival keeps the param, `:233`/`:243` the dashboard-wins pair.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: App-level arrival coverage for all three modes</name>

  <read_first>
    - `packages/web/src/App.deeplink.spec.tsx` (FULL FILE, 286 lines, 16 tests — the harness, the
      stub block Plan 02 updated, and the existing `DEEPLINK-114`/`AUTHLINK-115` arrival tests whose
      shapes these mirror)
    - `packages/web/src/App.tableDeeplink.spec.tsx` (FULL FILE, 12 tests — the ALREADY-SHIPPED table
      equivalent. **Read only — a diff fails criterion 7.**)
    - `<the_page_stub_trap_third_consecutive_phase>` above
  </read_first>

  <files>packages/web/src/App.deeplink.spec.tsx</files>

  <behavior>
    New tests, every title prefixed `DSET-117: `. One `it()` per bullet:

      1. `?dashboard=77&mode=view` resolves and the page receives `data-mode="view"`
      2. `?dashboard=77&mode=edit` resolves and the page receives `data-mode="edit"`
      3. **a bare `?dashboard=77` resolves and the page receives `data-mode="open"`** (DSET-V122-08)
      4. **after a bare arrival resolves, `window.location.search` is exactly `?dashboard=77` — it does not contain the substring `mode`** (DSET-V122-08)
      5. after a `&mode=view` arrival resolves, `window.location.search` is exactly `?dashboard=77&mode=view`
      6. `?dashboard=77&mode=banana` resolves to `data-mode="open"`, NOT to the failure banner
      7. the app-level `Loading…` is held for the whole of a `&mode=edit` resolution — `page-dashboards` is never in the document while it is pending (the no-flash contract, expressed structurally)
      8. `?dashboard=99&mode=view` not in the list -> the dashboard list with the combined banner, and BOTH params stripped from the bar
      9. that banner names neither the reason nor the requested id (DSET-V122-04 regression)
      10. an UNAUTHENTICATED `?dashboard=77&mode=edit` arrival goes to login and keeps BOTH params in the bar
      11. `?dashboard=7&mode=view&table=12` — the DASHBOARD opens, `page-datasets` is not rendered, and the stale `?table=` is stripped (the dashboard-wins precedence rule, re-confirmed against a mode-carrying dashboard link)
  </behavior>

  <action>
Extend `packages/web/src/App.deeplink.spec.tsx` in place with a new `describe` block. Reuse the
file's existing harness exactly — the same child stubs, the same `vi.mock("./api/client")` with its
`listDashboards` mock, the same auth seeding and the same `beforeEach` history reset.

Assertions read the stub's attributes:
`expect(screen.getByTestId("page-dashboards")).toHaveAttribute("data-mode", "view")`.

Bullet 4 is the one that carries DSET-V122-08 at this level, and it must be written so it can
actually fail. Assert the search string EXACTLY (`toBe("?dashboard=77")`), and add a second,
independent assertion that it does not contain the qualifier
(`expect(window.location.search).not.toContain("mode")`). Two different failure modes, two
assertions — a single `toBe` would also pass if the whole param were dropped.

Bullet 7 asserts the NEGATIVE while pending — `expect(screen.queryByTestId("page-dashboards")).toBeNull()`
alongside `expect(screen.getByText("Loading…")).toBeInTheDocument()` — before flushing the
`listDashboards` promise. Mirror how the existing `DEEPLINK-114` loading-hold test sequences this;
do not invent a different flush.

Bullet 11 mirrors `App.tableDeeplink.spec.tsx:233` and `:243`, which already prove the rule for a
bare dashboard link. This adds the mode-carrying case, so a future reader can see the precedence
rule was re-confirmed rather than assumed to be mode-agnostic.

Do NOT introduce `vi.useFakeTimers()` (MEMORY: "Web vitest parallel fake-timer leak").
  </action>

  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/App.deeplink.spec.tsx</automated>
  </verify>

  <acceptance_criteria>
1. `grep -o 'DSET-117:' packages/web/src/App.deeplink.spec.tsx | wc -l` — BEFORE: **0** (this file uses only `DEEPLINK-114:` and `AUTHLINK-115:` prefixes today). AFTER: **>= 11**.
2. `cd packages/web && npx vitest run src/App.deeplink.spec.tsx` — BEFORE: **16 passed**. AFTER: **>= 27 passed, 0 failed**. Record the literal number.
3. `grep -o 'data-mode' packages/web/src/App.deeplink.spec.tsx | wc -l` — BEFORE (as Plan 02 left it): **1** (the stub factory only; it was **0** before Plan 02). AFTER: **>= 7** (the stub plus the assertions). **This delta is what proves the tests read the resolved mode rather than only the id.**
4. `grep -c "useState(() => initialOpenDashboard)" packages/web/src/App.deeplink.spec.tsx` -> **still exactly 1**. The lazy capture survived; the stub was not "simplified" into a prop reflector.
5. **MUTATION PROBE A — the mode reaches the page.** Temporarily change `App.tsx`'s `initialOpenDashboard` payload back to `mode: "open" as const`. Re-run this spec: MUST report **>= 2 failed** (bullets 1, 2). Record the count, revert.
6. **MUTATION PROBE B — the bare URL is not silently modified.** Temporarily change `App.tsx`'s `restoreDashboardUrl(deepLink.dashboard.id, deepLink.mode)` to `restoreDashboardUrl(deepLink.dashboard.id, "view")`. Re-run: MUST report **>= 1 failed** (bullet 4). Record the count, revert. **If this does not redden, bullet 4's assertions are not reachable — report it plainly rather than proceeding.**
7. `git diff --name-only -- packages/web/src/App.tableDeeplink.spec.tsx packages/web/src/App.tablePasswordDeepLink.spec.tsx packages/web/src/components/DatasetsPage.urlsync.spec.tsx` — **NO OUTPUT**.
8. `git diff --numstat -- packages/web/src/App.tsx packages/web/src/components/DashboardsPage.tsx packages/web/src/lib/dashboardUrl.ts packages/web/src/hooks/useDeepLinkDashboard.ts` — **NO OUTPUT**. This plan writes tests only; every probe above must have been reverted.
9. `cd packages/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>

  <done>All 11 behaviour bullets are covered at >= 27 passing tests; `data-mode` is asserted at least six times; both mutation probes reddened the suite with literal counts recorded; no production file and no table spec has a diff.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The logged-out journey carries the mode through the one storage key</name>

  <read_first>
    - `packages/web/src/App.signincommit.spec.tsx` (FULL FILE, 197 lines, 7 tests — especially the
      two closed-shape assertions at `:115` and `:128`, `expect(readReturnTo()).toEqual({ dashboardId:
      12, page: "dashboards" })`, which MUST keep passing unmodified)
    - `packages/web/src/App.passwordDeepLink.spec.tsx` (FULL FILE, 158 lines, 3 tests)
    - `packages/web/src/App.spec.tsx` lines 160-240 (the ReturnTo corrupt-value template and the
      `assertJunkDashboardIdRejected` helper, plus the comment at `:195-198` explaining why only
      negative cases can live in that file)
    - `packages/web/src/App.tablePasswordDeepLink.spec.tsx` (the ALREADY-SHIPPED table equivalent.
      **Read only.**)
    - `<a_correction_to_research_worth_knowing_before_you_start>` above
  </read_first>

  <files>packages/web/src/App.signincommit.spec.tsx, packages/web/src/App.passwordDeepLink.spec.tsx, packages/web/src/App.spec.tsx</files>

  <behavior>
    New tests, every title prefixed `DSET-117: `. One `it()` per bullet.

    In `App.signincommit.spec.tsx` (OIDC; this file stubs listDashboards):
      1. committing with a pending `?dashboard=12&mode=view` writes `{ dashboardId: 12, dashboardMode: "view", page: "dashboards" }`
      2. committing with a pending `?dashboard=12&mode=edit` writes `{ dashboardId: 12, dashboardMode: "edit", page: "dashboards" }`
      3. **committing with a pending BARE `?dashboard=12` writes exactly `{ dashboardId: 12, page: "dashboards" }` — no `dashboardMode` key at all** (this is what keeps the two pre-existing closed-shape assertions honest, restated as a deliberate DSET-117 test rather than left implicit)
      4. a stored `{ dashboardId: 12, dashboardMode: "edit", page: "dashboards" }` on a bare URL restores to the edit screen (`data-mode="edit"`) and puts `?dashboard=12&mode=edit` back in the bar
      5. a stored `{ dashboardId: 12, dashboardMode: "hacker", page: "dashboards" }` restores to the RUNNING dashboard (`data-mode="open"`) — an unrecognised stored mode falls back, it does not fail
      6. a stored `{ dashboardId: 12, page: "dashboards" }` with NO mode restores to `data-mode="open"` (backward compatibility with ReturnTo blobs written before this phase)
      7. the key is still single-use after a mode-carrying restore — `sessionStorage.getItem("kbi_returnTo")` is null afterwards
      8. only ONE storage key is ever written: after a mode-carrying commit, `Object.keys(sessionStorage)` contains `kbi_returnTo` and no other `kbi_`-prefixed deep-link key

    In `App.passwordDeepLink.spec.tsx` (password mode; the URL itself is the carrier):
      9. a password-mode `?dashboard=12&mode=edit` arrival writes NOTHING to `kbi_returnTo` on commit, and after authentication lands on `data-mode="edit"`

    In `App.spec.tsx` (harness limits: negative cases only):
      10. a ReturnTo of `{ page: "dashboards", dashboardMode: "edit" }` with NO `dashboardId` does not wedge the boot — the dashboards page renders, `Loading…` is absent, and the key is cleared
  </behavior>

  <action>
Extend the three files in place. Reuse each file's existing harness exactly — in
`App.signincommit.spec.tsx` that means the `commit-signin` LoginPage stub, its `readReturnTo()`
helper and its `setAuth` helper; in `App.spec.tsx` it means the `assertJunkDashboardIdRejected`
sibling shape.

**Do not modify the two existing closed-shape assertions at `:115` and `:128`.** Bullet 3 exists
precisely so the bare-URL payload shape has a test that NAMES it as a Phase 117 requirement, rather
than surviving only as an incidental Phase 115 assertion that a future refactor might relax.

Bullet 8 should enumerate the storage keys rather than asserting on a single one:
```typescript
const kbiKeys = Object.keys(sessionStorage).filter((k) => k.startsWith("kbi_"));
expect(kbiKeys).toEqual(["kbi_returnTo"]);
```
That is what makes "no second storage key" a checkable fact rather than a claim.

Bullets 4-6 need `listDashboards` to resolve a list containing id 12; reuse the mock that file
already sets up.

Do NOT introduce `vi.useFakeTimers()` in any of the three files.
  </action>

  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/App.signincommit.spec.tsx src/App.passwordDeepLink.spec.tsx src/App.spec.tsx</automated>
  </verify>

  <acceptance_criteria>
1. `grep -o 'DSET-117:' packages/web/src/App.signincommit.spec.tsx | wc -l` — BEFORE: **0**. AFTER: **>= 8**.
2. `grep -o 'DSET-117:' packages/web/src/App.passwordDeepLink.spec.tsx | wc -l` — BEFORE: **0**. AFTER: **>= 1**.
3. `grep -o 'DSET-117:' packages/web/src/App.spec.tsx | wc -l` — BEFORE: **0**. AFTER: **>= 1**.
4. `cd packages/web && npx vitest run src/App.signincommit.spec.tsx src/App.passwordDeepLink.spec.tsx src/App.spec.tsx` — BEFORE: **48 passed** (7 + 3 + 38). AFTER: **>= 58 passed, 0 failed**. Record the literal number.
5. **The two closed-shape assertions are untouched.** `git diff -U0 -- packages/web/src/App.signincommit.spec.tsx | grep -c '^-.*toEqual({ dashboardId: 12, page: "dashboards" })'` -> **0**.
6. `grep -c 'startsWith("kbi_")' packages/web/src/App.signincommit.spec.tsx` — BEFORE: **0**. AFTER: **>= 1**. (Bullet 8's key enumeration exists.)
7. **MUTATION PROBE A — the conditional commit payload.** Temporarily change `App.tsx`'s commit branch to ALWAYS include the key, i.e. `payload = { dashboardId: d.id, dashboardMode: d.mode, page: "dashboards" }` with no `d.mode === "open"` test. Re-run: MUST report **>= 3 failed** (bullet 3 plus the two pre-existing closed-shape tests at `:115`/`:128`). Record the count, revert. **This probe is the reason the payload is built conditionally at all; if it reddens fewer than 3, say which and why.**
8. **MUTATION PROBE B — the stored mode is actually read.** Temporarily change `readPendingDashboard`'s mode derivation in `App.tsx` to always return `"open"`. Re-run: MUST report **>= 1 failed** (bullet 4). Record the count, revert.
9. **MUTATION PROBE C — the stored-mode fallback.** Temporarily change that same derivation so an unrecognised value returns `"view"` instead of `"open"`. Re-run: MUST report **>= 1 failed** (bullets 5, 6). Record the count, revert.
10. `git diff --numstat -- packages/web/src/App.tsx packages/web/src/components/DashboardsPage.tsx packages/web/src/lib/dashboardUrl.ts packages/web/src/hooks/useDeepLinkDashboard.ts` — **NO OUTPUT**. Tests only; every probe reverted.
11. `git diff --name-only -- packages/web/src/App.tablePasswordDeepLink.spec.tsx packages/web/src/App.tableDeeplink.spec.tsx` — **NO OUTPUT**.
12. `cd packages/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>

  <done>All 10 behaviour bullets are covered at >= 58 passing tests across the three files; the two pre-existing closed-shape assertions are byte-identical; the single-storage-key claim is enumerated, not asserted; probes A, B and C reddened the suite with literal counts recorded; no production file has a diff.</done>
</task>

<task type="auto">
  <name>Task 3: The DSET-V122-08 audit — prove the bare link is unchanged, do not assume it</name>

  <read_first>
    - `.planning/phases/117-dashboard-settings-links/117-01-SUMMARY.md`, `117-02-SUMMARY.md`,
      `117-03-SUMMARY.md`, `117-04-SUMMARY.md` (the recorded test totals and probe results this
      audit reconciles against)
    - `packages/web/src/components/DashboardsPage.urlsync.spec.tsx` lines 117-247 (the
      `URLSYNC-113` block, 10 tests, which exercises `open` mode EXCLUSIVELY and asserts the bar
      reads exactly `?dashboard=<id>` with no mode param anywhere)
    - `117-RESEARCH.md` §Q2 "The bare-URL regression surface DSET-V122-08 protects"
  </read_first>

  <files>packages/web/src/App.deeplink.spec.tsx</files>

  <behavior>
    This task adds no new feature behaviour. It produces EVIDENCE, recorded as literal numbers,
    that the 132-test dashboard family still describes the same bare-URL contract it described
    before Phase 117 — and that no pre-existing assertion was deleted or weakened to get there.
  </behavior>

  <action>
A green suite does not prove DSET-V122-08: a suite stays green if an inconvenient assertion is
deleted. This task looks for that specifically.

**Step 1 — Run the whole dashboard family and record it.**
```
cd packages/web && npx vitest run \
  src/lib/dashboardUrl.spec.ts \
  src/hooks/useDeepLinkDashboard.spec.ts \
  src/components/DashboardsPage.urlsync.spec.tsx \
  src/App.spec.tsx \
  src/App.deeplink.spec.tsx \
  src/App.passwordDeepLink.spec.tsx \
  src/App.signincommit.spec.tsx
```
Record the literal file and test totals. The pre-Phase-117 baseline for these seven files is
**132 tests**; the total must now be strictly greater, with 0 failures.

**Step 2 — Look for deleted assertions.** This is the step that actually discriminates.
```
git diff -U0 -- \
  packages/web/src/lib/dashboardUrl.spec.ts \
  packages/web/src/hooks/useDeepLinkDashboard.spec.ts \
  packages/web/src/components/DashboardsPage.urlsync.spec.tsx \
  packages/web/src/App.spec.tsx \
  packages/web/src/App.deeplink.spec.tsx \
  packages/web/src/App.passwordDeepLink.spec.tsx \
  packages/web/src/App.signincommit.spec.tsx \
  | grep -c '^-.*expect('
```
Every legitimate edit this phase made to these files was a FIXTURE or STUB reshape
(`initialOpenDashboard={DASH_77}` -> `{ dashboard: DASH_77, mode: "open" }`, `storedId` ->
`{ id, mode }`, `captured.id` -> `captured.dashboard.id`). None of those are `expect(` lines. The
expected result is therefore **0**. If it is not 0, list every removed assertion in the SUMMARY with
a justification per line — a removed assertion is exactly how a bare-URL regression ships green.

**Step 3 — Count deleted whole tests.**
```
git diff -U0 -- <the same seven files> | grep -c '^-.*it("'
```
Expected **0**. Any non-zero result must be enumerated and justified.

**Step 4 — The fallback mutation probe.** Temporarily change `readDashboardModeFromSearch`'s final
`return "open"` (in `lib/dashboardUrl.ts`) to `return "view"`. This is the single edit that would
silently redefine what a bare `?dashboard=<id>` means. Re-run the seven-file command from Step 1
and record BOTH the number of failing FILES and the number of failing TESTS. Revert and confirm
green. **The expectation is that this reddens at least THREE of the seven files** — the lib spec,
the page spec and at least one App spec — because the bare-URL contract is asserted at three
different layers. If it reddens fewer, name which layer is unguarded and add the missing test to
`App.deeplink.spec.tsx` before finishing.

**Step 5 — Record the residual `?mode=` collision.** Confirm the accepted behaviour is still what
Plan 01 pinned, and note in the SUMMARY that no table file was edited to accommodate it:
```
git diff --name-only -- packages/web/src/lib/tableUrl.ts packages/web/src/hooks/useDeepLinkTable.ts \
  packages/web/src/components/DatasetsPage.tsx packages/web/src/lib/tableUrl.spec.ts \
  packages/web/src/hooks/useDeepLinkTable.spec.ts packages/web/src/components/DatasetsPage.urlsync.spec.tsx \
  packages/web/src/App.tableDeeplink.spec.tsx packages/web/src/App.tablePasswordDeepLink.spec.tsx
```
Expected: no output.

Only if Step 4 reveals an unguarded layer does this task write code — one test in
`App.deeplink.spec.tsx`, prefixed `DSET-117: `, closing that specific gap.
  </action>

  <verify>
    <automated>cd packages/web && npx vitest run src/lib/dashboardUrl.spec.ts src/hooks/useDeepLinkDashboard.spec.ts src/components/DashboardsPage.urlsync.spec.tsx src/App.spec.tsx src/App.deeplink.spec.tsx src/App.passwordDeepLink.spec.tsx src/App.signincommit.spec.tsx</automated>
  </verify>

  <acceptance_criteria>
1. The seven-file run reports **0 failed** and a test total **strictly greater than 132** (the recorded pre-Phase-117 baseline for exactly these files). Both numbers recorded literally.
2. Step 2's removed-assertion count is recorded literally. Target **0**; any other value is accompanied by a line-by-line justification in the SUMMARY.
3. Step 3's removed-test count is recorded literally. Target **0**; same justification rule.
4. Step 4's mutation probe is recorded as two literal numbers (failing files, failing tests) and the probe reddened **>= 3 of the 7 files**. If fewer, the SUMMARY names the unguarded layer AND the closing test was added.
5. Step 5's `git diff --name-only` over the eight table-family files produces **NO OUTPUT**.
6. `git diff --numstat -- packages/web/src/lib/dashboardUrl.ts packages/web/src/hooks/useDeepLinkDashboard.ts packages/web/src/components/DashboardsPage.tsx packages/web/src/App.tsx` — **NO OUTPUT**. The Step 4 probe was reverted.
7. `cd packages/web && npx vitest run` — 100% pass, **175 test files**. Record the literal test total.
8. `git diff --stat -- packages/server` — **NO OUTPUT**.
  </acceptance_criteria>

  <done>DSET-V122-08 is backed by recorded evidence rather than a green tick: the dashboard family's test count grew, no pre-existing assertion or test was deleted, and a deliberate mutation to the bare-URL default reddened at least three independent layers.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx tsc --noEmit` — clean.
- `cd packages/web && npx vitest run` — 100% pass at **175 files**.
- `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` — green.
- `git diff --numstat -- packages/web/src/App.tsx packages/web/src/components/DashboardsPage.tsx packages/web/src/lib/dashboardUrl.ts packages/web/src/hooks/useDeepLinkDashboard.ts` — NO OUTPUT (this plan is tests only).
- `git diff --name-only -- packages/web/src/lib/tableUrl.ts packages/web/src/hooks/useDeepLinkTable.ts packages/web/src/components/DatasetsPage.tsx` — NO OUTPUT.
- `git diff --stat -- packages/server` — NO OUTPUT.
</verification>

<success_criteria>
- All three modes are proved end-to-end through `App`, including the no-flash hold and the failure banner.
- The logged-out journey carries the mode through `kbi_returnTo`, with the key set enumerated rather than assumed.
- The bare `?dashboard=<id>` contract is backed by an audit that looks for deleted assertions, not by a green suite.
- Zero production-code diff and zero table-family diff.
</success_criteria>

<output>
After completion, create `.planning/phases/117-dashboard-settings-links/117-05-SUMMARY.md`. Record,
as literal numbers: each file's new test total, the seven-file dashboard-family total, Step 2's and
Step 3's deleted-assertion/deleted-test counts, Step 4's failing-file and failing-test counts, and
the failure counts for Task 1 probes A-B and Task 2 probes A-C. Record the deviation from
`117-RESEARCH.md` §Q5 regarding where the mode read-validation tests were placed, and why.
</output>
