---
phase: 117-dashboard-settings-links
plan: 02
type: execute
wave: 2
depends_on: ["117-01"]
files_modified:
  - packages/web/src/components/DashboardsPage.tsx
  - packages/web/src/components/DashboardsPage.urlsync.spec.tsx
  - packages/web/src/App.tsx
  - packages/web/src/App.deeplink.spec.tsx
  - packages/web/src/App.signincommit.spec.tsx
  - packages/web/src/App.passwordDeepLink.spec.tsx
autonomous: true
requirements: [DSET-V122-01, DSET-V122-02, DSET-V122-03, DSET-V122-05, DSET-V122-08]

must_haves:
  truths:
    - "Clicking View on a dashboard puts ?dashboard=<id>&mode=view in the address bar; clicking Edit puts ?dashboard=<id>&mode=edit."
    - "Clicking Open still puts exactly ?dashboard=<id> with no mode qualifier — unchanged."
    - "Clicking Edit from INSIDE the settings screen changes the qualifier on the SAME history entry, adding no entry."
    - "Saving from the edit screen drops the qualifier back to view on the SAME entry, adding no entry."
    - "In-app Back from a settings screen pops our entry when we pushed it, and WRITES the list URL when the user arrived on a deep link — the user is never ejected from the app."
    - "A dashboard handed in via initialOpenDashboard mounts straight into the mode the link named — the dashboard LIST is never rendered."
  artifacts:
    - path: "packages/web/src/components/DashboardsPage.tsx"
      provides: "URL writers on all twelve in-component mode transitions, the three-screen popstate reconciliation, and the mode-carrying initialOpenDashboard mount capture"
      contains: "setDashboardMode"
    - path: "packages/web/src/components/DashboardsPage.urlsync.spec.tsx"
      provides: "Page-level coverage of the view/edit transitions and mode-carrying arrivals"
      contains: "DSET-117:"
  key_links:
    - from: "DashboardsPage View/Edit list buttons"
      to: "openDashboardUrl"
      via: "onClick calls openDashboardUrl(dash.id, mode) alongside setView"
      pattern: "openDashboardUrl\\(dash\\.id, \""
    - from: "DashboardDetail onEdit AND DashboardEdit onSaved"
      to: "setDashboardMode"
      via: "in-place mode change on the current history entry, both directions"
      pattern: "setDashboardMode\\("
    - from: "App.tsx initialOpenDashboard"
      to: "DashboardsPage initialOpenDashboard prop"
      via: "a { dashboard, mode } payload instead of a bare DashboardDto"
      pattern: "dashboard: deepLink\\.dashboard"
---

<objective>
Wire `DashboardsPage.tsx`'s five-mode state machine to the three-mode URL module Plan 01 built:
write the URL on every mode transition, reconcile on history navigation, and accept a
mode-carrying `initialOpenDashboard` so a deep-link arrival mounts straight into view or edit.

Purpose: DSET-V122-01 (view link), -02 (distinct edit link, from BOTH the list and the in-app Edit
button), -03 (arrival mounts in the named mode, no list flash), -05 (Back returns to the list and
never ejects), -08 (the bare Open path is untouched).

Output: modified `DashboardsPage.tsx` + extended `DashboardsPage.urlsync.spec.tsx`, plus the
minimum edits to `App.tsx` and three App spec stubs needed to keep the tree compiling and the
whole suite green. (`DSET-V122-06`, the unmount clear, is Plan 03. The App-side mode threading is
Plan 04.)

**Why the App.tsx line and the three stub fixes live in THIS plan:** the `initialOpenDashboard`
prop's TYPE changes here, from `DashboardDto` to `{ dashboard, mode }`. `App.tsx` passes that prop
and three App spec stubs read `captured.id` off it. A type change cannot be split across plans
without leaving `npx tsc --noEmit` broken and three spec files red between waves. It lands atomically.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/117-dashboard-settings-links/117-CONTEXT.md
@.planning/phases/117-dashboard-settings-links/117-RESEARCH.md
@.planning/phases/116-table-deep-links/116-03-PLAN.md

<the_complete_transition_table>
Verified line-by-line against the CURRENT `components/DashboardsPage.tsx` (1756 lines).
**`117-CONTEXT.md`'s own list of transitions is INCOMPLETE — do not use it. Use this one.**
Thirteen sites; twelve are this plan's, #13 is Plan 03's.

| # | Transition | Trigger (current line) | Current code | URL action required |
|---|---|---|---|---|
| 1 | list -> open | Open button `:240` | `openDashboardUrl(dash.id); setView({mode:"open",...})` | **NONE. LEAVE BYTE-IDENTICAL.** Bare id, no mode param. This is the live-in-the-wild path DSET-V122-08 protects; Plan 01 defaulted the new argument specifically so this line never has to change. Do not "tidy" it by adding an explicit `"open"`. |
| 2 | list -> view | View button `:243` | `setView({ mode: "view", dashboard: dash })` — no URL call | `openDashboardUrl(dash.id, "view")` — push, marked |
| 3 | list -> edit | Edit button `:247` | `setView({ mode: "edit", dashboard: dash })` — no URL call | `openDashboardUrl(dash.id, "edit")` — push, marked |
| 4 | list -> create | New Dashboard `:211` | `setView({ mode: "create" })` | **none** — create is not linkable (`DSET-F1`, deferred) |
| 5 | create -> view | `DashboardCreate.onSaved` `:173-175` | `setView({ mode: "view", dashboard: created })` | `openDashboardUrl(created.id, "view")` — push. **The first-ever linkable moment for a fresh dashboard.** `117-CONTEXT.md` omits this transition entirely; tables needed it too (116-03-PLAN row 4). |
| 6 | create -> list | `DashboardCreate.onBack` `:172` | `setView({ mode: "list" })` | **none** — create never touched the URL |
| 7 | open -> list | `DashboardOpen.onBack` `:160` | `leaveDashboardUrl(); setView({mode:"list"})` | **NONE. ALREADY WIRED. LEAVE BYTE-IDENTICAL.** |
| 8 | view -> list | `DashboardDetail.onBack` `:185` | `setView({ mode: "list" })` — no URL call | `leaveDashboardUrl()` |
| 9 | **view -> edit** | **`DashboardDetail.onEdit` `:186`** | `setView({ mode: "edit", dashboard: view.dashboard })` | **`setDashboardMode(view.dashboard.id, "edit")`. THIS TRANSITION HAS NO TABLE ANALOGUE** — `TableDetail` has no Edit affordance, `DashboardDetail` does (`canEdit && <button onClick={onEdit}>Edit</button>`, `:296`). It is an IN-PLACE change on the SAME entry: not a leave, not a fresh open. **This is the single easiest site in the whole phase to miss, precisely because Phase 116 had no precedent for it.** |
| 10 | edit -> list | `DashboardEdit.onBack` `:195` | `setView({ mode: "list" })` — no URL call | `leaveDashboardUrl()` |
| 11 | edit -> view | `DashboardEdit.onSaved` `:196-198` | `setView({ mode: "view", dashboard: updated })` | `setDashboardMode(updated.id, "view")` — in-place. Reachable from TWO origins (a fresh list->edit push, or an in-place view->edit per #9); ONE call site handles both, because `setDashboardMode` preserves whatever `window.history.state` already was. |
| 12 | open -> open | `DashboardOpen.onDashboardUpdated` `:161-162` | `setView({ mode: "open", dashboard: updated })` | **none, verified.** `open` never transitions in place to view/edit; nothing enters `open` except the list's Open button or a deep-link arrival. |
| 13 | unmount of a view/edit screen | *(nothing today)* | *(nothing today)* | **PLAN 03.** Do not attempt it here. |

`openDashboardUrl` / `setDashboardMode` / `leaveDashboardUrl` are called **alongside** `setView`, in
the same handler — never inside an effect. 113-CONTEXT's locked decision: the URL updates
immediately on open, before data loads.
</the_complete_transition_table>

<pitfall_1_recorded_verbatim>
From `117-RESEARCH.md` §Pitfall 1 — **the mode-preserving replace, now at TWO call sites instead of one.**

What goes wrong: `setDashboardMode` is correct in `lib/dashboardUrl.ts` (Plan 01 built it to pass
`window.history.state` through), but a plan copying Phase 116 by rote wires it at ONE site — the
after-Save `edit->view` — and misses the `view->edit` in-app Edit button, which tables never had.

Warning sign: a test suite that only exercises "list -> edit push -> Save" (marker always true) and
never "list -> view push -> in-app Edit click -> Save" (marker true through two in-place changes) or
"deep-link arrival into view -> in-app Edit click" (marker absent throughout) will not catch it.

Task 2's behaviour bullets 6, 7 and 8 exist specifically to close this. The count criterion is
`setDashboardMode(` occurring **exactly 2** times, not ">= 1".
</pitfall_1_recorded_verbatim>

<interfaces>
<!-- From Plan 01. Use these directly; no codebase exploration needed. -->
packages/web/src/lib/dashboardUrl.ts:
```typescript
export const DASHBOARD_URL_PARAM: string;       // "dashboard"
export const DASHBOARD_MODE_PARAM: string;      // "mode"
export const DASHBOARD_HISTORY_MARKER: string;  // "kbiDashboardEntry"
export type DashboardMode = "open" | "view" | "edit";
export function readDashboardIdFromSearch(search: string): number | null;
export function readDashboardModeFromSearch(search: string): DashboardMode;   // absent/unknown -> "open"
export function openDashboardUrl(id: number, mode?: DashboardMode): void;     // default "open"
export function clearDashboardUrl(): void;
export function restoreDashboardUrl(id: number, mode?: DashboardMode): void;  // default "open"
export function setDashboardMode(id: number, mode: DashboardMode): void;      // in-place, preserves history.state
export function leaveDashboardUrl(): "popped" | "wrote";
```

<!-- The CURRENT DashboardsPage import line (`:74`) that must gain setDashboardMode -->
```typescript
import { openDashboardUrl, leaveDashboardUrl, clearDashboardUrl, readDashboardIdFromSearch } from "../lib/dashboardUrl";  // Phase 113 (DLINK-V121-01/06/07)
```

<!-- The CURRENT App.tsx payload (`:170-171`) whose SHAPE changes in this plan -->
```typescript
const initialOpenDashboard =
  deepLink.status === "opened" && !deepLinkConsumedRef.current ? deepLink.dashboard : undefined;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire all twelve in-component transitions and change initialOpenDashboard to carry a mode</name>

  <read_first>
    - `packages/web/src/components/DashboardsPage.tsx` lines 74-275 (the import line, the `View`
      union, the component signature, the mount-time lazy capture, the popstate listener, all five
      render branches, and the list's action buttons — every line number in the transition table
      above refers to this file) and lines 277-300 (`DashboardDetail`, whose `onEdit` prop is
      transition #9)
    - `packages/web/src/components/DatasetsPage.tsx` lines 1-140 (the ALREADY-SHIPPED table
      equivalent this mirrors — its handler shapes, its popstate listener, its `initialOpenTable`
      capture. **Read only. A diff on this file fails criterion 12.**)
    - `packages/web/src/lib/dashboardUrl.ts` (Plan 01 output)
    - `packages/web/src/App.tsx` lines 143-182 (the `initialOpenDashboard` payload and the
      consumed-once ref whose gate must not change)
    - `packages/web/src/App.deeplink.spec.tsx` lines 27-40, `App.signincommit.spec.tsx` lines 26-35,
      `App.passwordDeepLink.spec.tsx` lines 20-30 (the three `DashboardsPage` stubs that read `captured.id`)
    - `CLAUDE.md` §"UI Conventions" — this task adds NO new markup and NO new className
  </read_first>

  <files>packages/web/src/components/DashboardsPage.tsx, packages/web/src/App.tsx, packages/web/src/App.deeplink.spec.tsx, packages/web/src/App.signincommit.spec.tsx, packages/web/src/App.passwordDeepLink.spec.tsx</files>

  <behavior>
    - `<DashboardsPage />` with no prop starts on the list, exactly as today.
    - `<DashboardsPage initialOpenDashboard={{ dashboard, mode: "open" }} />` mounts on the running dashboard (today's behaviour, new prop shape).
    - `<DashboardsPage initialOpenDashboard={{ dashboard, mode: "view" }} />` mounts on the settings screen; the list chrome is never in the document.
    - `<DashboardsPage initialOpenDashboard={{ dashboard, mode: "edit" }} />` mounts on the edit form.
    - A later change to the prop re-opens nothing (mount-only lazy capture).
    - Clicking Open -> search is exactly `?dashboard=42`, `history.length` +1.
    - Clicking View -> search is `?dashboard=42&mode=view`, `history.length` +1.
    - Clicking Edit in the LIST row -> search is `?dashboard=42&mode=edit`, `history.length` +1.
    - Clicking Edit INSIDE the settings screen -> search becomes `?dashboard=42&mode=edit`, `history.length` UNCHANGED.
    - Saving from the edit screen -> search becomes `?dashboard=42&mode=view`, `history.length` UNCHANGED.
    - Creating a dashboard -> search becomes `?dashboard=<new id>&mode=view`, `history.length` +1.
    - `popstate` with no dashboard param -> the list renders.
    - `popstate` with a dashboard param while the LIST is on screen -> the stale param is cleared in place; nothing re-opens.
    - `popstate` with a dashboard param while a view/edit/open screen is on screen -> nothing is cleared.
  </behavior>

  <action>
**No new markup, no new className, no new `window.confirm`.** The unsaved-edit leave guard is OUT
of scope (`TLINK-F1`, deferred; `117-RESEARCH.md` §Q3 re-confirms the Phase 116 operator decision).

**A — `packages/web/src/components/DashboardsPage.tsx`**

1. Extend the `:74` import to add `setDashboardMode` and `type DashboardMode`:
```typescript
import { openDashboardUrl, leaveDashboardUrl, clearDashboardUrl, readDashboardIdFromSearch, setDashboardMode, type DashboardMode } from "../lib/dashboardUrl";  // Phase 113 (DLINK-V121-01/06/07) + Phase 117 (DSET-V122-01/02)
```

2. Change the prop type on the component signature (`:89-93`):
```typescript
const DashboardsPage = ({ onViewChange, initialOpenDashboard }: {
  onViewChange?: (mode: string) => void;
  // Phase 114 (DLINK-V121-02) / Phase 117 (DSET-V122-03): a dashboard resolved from
  // ?dashboard=<id>[&mode=view|edit] at boot, handed down by App.tsx together with WHICH SCREEN the
  // link named. Was a bare DashboardDto before Phase 117, when `open` was the only linkable mode.
  // Consumed ONCE, by the useState initializer below.
  initialOpenDashboard?: { dashboard: DashboardDto; mode: DashboardMode };
}) => {
```

3. Replace the mount-time lazy capture (`:105-107`). Keep the existing comment block above it
verbatim — it already explains the no-flash contract and why `openDashboardUrl()` is deliberately
absent — and append one sentence noting the mode now comes in with the dashboard. The initializer
must narrow explicitly so the `View` union type-checks:
```typescript
  const [view, setViewState] = useState<View>(() => {
    if (!initialOpenDashboard) return { mode: "list" };
    const { dashboard, mode } = initialOpenDashboard;
    // Explicit narrowing, not a spread: View's three dashboard-carrying members are separate union
    // arms, and `create`/`list` carry no dashboard. DashboardMode's three values map 1:1 onto the
    // three that do.
    if (mode === "view") return { mode: "view", dashboard };
    if (mode === "edit") return { mode: "edit", dashboard };
    return { mode: "open", dashboard };
  });
```

4. Apply the transition table exactly. Concretely, the six lines that change:
   - `:173-175` `DashboardCreate.onSaved` — after `setDashboards(...)`, call `openDashboardUrl(created.id, "view")`, then `setView({ mode: "view", dashboard: created })`.
   - `:185` `DashboardDetail.onBack` -> `onBack={() => { leaveDashboardUrl(); setView({ mode: "list" }); }}`
   - `:186` `DashboardDetail.onEdit` -> `onEdit={() => { setDashboardMode(view.dashboard.id, "edit"); setView({ mode: "edit", dashboard: view.dashboard }); }}`
     Comment it: *in-place mode change on the SAME entry — not a leave and not a fresh open. No table analogue: TableDetail has no Edit affordance. `setDashboardMode` preserves whether this entry is ours, which `leaveDashboardUrl()` must still read correctly afterwards.*
   - `:195` `DashboardEdit.onBack` -> `onBack={() => { leaveDashboardUrl(); setView({ mode: "list" }); }}`
   - `:196-198` `DashboardEdit.onSaved` — after `setDashboards(...)`, call `setDashboardMode(updated.id, "view")`, then `setView({ mode: "view", dashboard: updated })`.
   - `:243` list View button -> `onClick={() => { openDashboardUrl(dash.id, "view"); setView({ mode: "view", dashboard: dash }); }}`
   - `:247` list Edit button -> `onClick={() => { openDashboardUrl(dash.id, "edit"); setView({ mode: "edit", dashboard: dash }); }}`

   **Do not touch `:160` (open -> list) or `:240` (list -> open).** Both are already correct and
   both are on the DSET-V122-08 protected path.

5. Widen the popstate listener's second branch (`:139-146`) from one dashboard-screen state to three:
```typescript
      } else if (view.mode !== "open" && view.mode !== "view" && view.mode !== "edit") {
        clearDashboardUrl();
      }
```
   Update the surrounding comment: case 2 now means "the param is present but NO dashboard screen is
   on screen (we are on the list, or on create)". Add: *the mode qualifier is deliberately NOT
   reconciled here. `view <-> edit` does reach a differing mode on the same entry (transitions #9 and
   #11), but only ever via `replaceState`, never via a `pushState`/`popstate` pair — so `popstate`
   itself can never observe a mismatch, and a third case would be dead code.*

**B — `packages/web/src/App.tsx`, ONE expression (`:170-171`)**
```typescript
  const initialOpenDashboard =
    deepLink.status === "opened" && !deepLinkConsumedRef.current
      // Phase 117: DashboardsPage now needs to know WHICH screen the link named, not just which
      // dashboard. TEMPORARY LITERAL — Plan 04 replaces `"open" as const` with `deepLink.mode`
      // once useDeepLinkDashboard resolves the qualifier. Until then every arrival is `open`,
      // which is exactly today's behaviour, so nothing changes for the user in this wave.
      ? { dashboard: deepLink.dashboard, mode: "open" as const }
      : undefined;
```
Change NOTHING else in `App.tsx` in this plan — in particular leave the `deepLinkConsumedRef` flip
effect and its `page === "dashboards"` gate exactly as they are.

**C — the three App spec stubs.** In `App.deeplink.spec.tsx`, `App.signincommit.spec.tsx` and
`App.passwordDeepLink.spec.tsx`, update the `vi.mock("./components/DashboardsPage", ...)` factory to
the new prop shape. **Keep the `useState(() => initialOpenDashboard)` lazy capture** — it is not
decoration: `App.deeplink.spec.tsx:27-32`'s own comment records that a dumb prop-reflector stub
FALSELY FAILS against correct code, and this is the THIRD consecutive phase to be warned about it.
Both things must be right at once:
```tsx
vi.mock("./components/DashboardsPage", () => ({
  default: ({ initialOpenDashboard }: { initialOpenDashboard?: { dashboard: { id: number }; mode: string } }) => {
    const [captured] = useState(() => initialOpenDashboard);
    return (
      <main
        data-testid="page-dashboards"
        data-deeplink={captured ? String(captured.dashboard.id) : ""}
        data-mode={captured?.mode ?? ""}
      >
        Dashboards
      </main>
    );
  },
}));
```
Do NOT add or change any assertion in these three files in this task — only the stub factories. All
26 of their existing tests must pass unchanged, because every arrival they exercise is a bare
`?dashboard=<id>`, which still resolves to `open`.
  </action>

  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/App.deeplink.spec.tsx src/App.signincommit.spec.tsx src/App.passwordDeepLink.spec.tsx src/components/DashboardsPage.urlsync.spec.tsx</automated>
  </verify>

  <acceptance_criteria>
All BEFORE counts were executed 2026-09-15 against this tree, from `packages/web/src`.
Where OCCURRENCES rather than LINES matter the command is `grep -o ... | wc -l`, because
`grep -c` counts matching lines.

1. `grep -o "openDashboardUrl(" components/DashboardsPage.tsx | wc -l` — BEFORE: **2** (one real call at `:240`, one inside a prose comment at `:102`). AFTER: **exactly 4** (same comment + Open unchanged + list View + create->view). **3 means the create->view push — the transition `117-CONTEXT.md` omits — was missed. 5 means something not in the transition table also pushes.**
2. `grep -o "leaveDashboardUrl(" components/DashboardsPage.tsx | wc -l` — BEFORE: **2** (one real call at `:160`, one inside a prose comment at `:104`). AFTER: **exactly 4** (comment + open's existing call + view->list + edit->list).
3. `grep -o "setDashboardMode(" components/DashboardsPage.tsx | wc -l` — BEFORE: **0**. AFTER: **exactly 2** — `DashboardDetail.onEdit` and `DashboardEdit.onSaved`. **NOT 1. A count of 1 means the `view->edit` in-place transition (table row #9) was missed — the one transition in this phase with no table precedent, and the one Pitfall 1 predicts will be skipped.** (The import statement has no `(` and does not match.)
3b. `grep -c "setDashboardMode(view.dashboard.id, \"edit\")" components/DashboardsPage.tsx` -> **exactly 1**, and `grep -c "setDashboardMode(updated.id, \"view\")" components/DashboardsPage.tsx` -> **exactly 1**. Two DIFFERENT call sites in two DIFFERENT directions; criterion 3 alone would pass if the same site were duplicated.
4. **DSET-V122-08 stability guard.** `grep -c 'openDashboardUrl(dash.id); setView({ mode: "open"' components/DashboardsPage.tsx` — BEFORE: **1**. AFTER: **still exactly 1**, byte-identical. The Open button's handler is on the live-in-the-wild path and this plan must not have edited it.
5. `grep -c "initialOpenDashboard?: DashboardDto" components/DashboardsPage.tsx` — BEFORE: **1**. AFTER: **0**. And `grep -c "initialOpenDashboard?: { dashboard: DashboardDto; mode: DashboardMode }" components/DashboardsPage.tsx` -> **1**.
6. `grep -c 'view.mode !== "open" && view.mode !== "view" && view.mode !== "edit"' components/DashboardsPage.tsx` — BEFORE: **0**. AFTER: **1**. (The popstate listener's second branch now covers all three dashboard screens.)
7. `grep -o 'addEventListener("popstate"' components/DashboardsPage.tsx | wc -l` — BEFORE: **1**. AFTER: **still exactly 1**. No second listener was added.
8. `grep -c "window.confirm" components/DashboardsPage.tsx` — BEFORE: **1** (the pre-existing Delete confirm). AFTER: **still exactly 1**. The unsaved-edit guard is out of scope; this must not become 2.
9. `grep -rl "dashboardEditGuard" packages/web/src` — **0 files**, before and after. Same guard, from the other direction.
10. Stub shape, per file. `grep -c "captured.id" X` — BEFORE **1**, AFTER **0** — and `grep -c "data-mode" X` — BEFORE **0**, AFTER **1** — for each of `X` in {`App.deeplink.spec.tsx`, `App.signincommit.spec.tsx`, `App.passwordDeepLink.spec.tsx`}. Also `grep -c "useState(() => initialOpenDashboard)" X` -> **1** in each: the lazy capture survived the shape change.
11. `grep -c 'mode: "open" as const' App.tsx` — BEFORE: **0** (`grep -c 'mode: "open"' App.tsx` is also 0 today). AFTER: **exactly 1**. Plan 04's criterion drives this back to 0; recording it now is what makes that later delta meaningful.
12. `git diff --name-only -- packages/web/src/lib/tableUrl.ts packages/web/src/hooks/useDeepLinkTable.ts packages/web/src/components/DatasetsPage.tsx packages/web/src/components/DatasetsPage.urlsync.spec.tsx` — **NO OUTPUT**.
13. `cd packages/web && npx tsc --noEmit` exits 0.
14. `cd packages/web && npx vitest run src/App.deeplink.spec.tsx src/App.signincommit.spec.tsx src/App.passwordDeepLink.spec.tsx` — **26 passed, 0 failed** (16 + 7 + 3), with no assertion in those files edited: `git diff -U0 -- packages/web/src/App.deeplink.spec.tsx packages/web/src/App.signincommit.spec.tsx packages/web/src/App.passwordDeepLink.spec.tsx | grep -c "^[-+].*expect("` -> **0**. Only stub factories changed.
15. `cd packages/web && npx vitest run src/components/DashboardsPage.urlsync.spec.tsx` — the 4 `DEEPLINK-114` tests that pass `initialOpenDashboard={DASH_77}` (lines 265, 281, 293) will now fail to type-check/behave. **Fixing them is Task 2's job, not this task's.** For THIS task the acceptance is narrower: `npx tsc --noEmit` is clean for `src/` production code. If `tsc` flags those three spec lines, note the exact errors and hand them to Task 2 rather than loosening the prop type.
  </acceptance_criteria>

  <done>All twelve in-component transitions write (or deliberately do not write) the URL exactly per the table; `setDashboardMode` appears at exactly two DIFFERENT call sites in two directions; the Open-button line is byte-identical; the prop carries a mode; `App.tsx` passes the new shape with a temporary `"open"` literal; the three App stubs read `captured.dashboard.id` and expose `data-mode` while keeping their lazy capture; no table file changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend DashboardsPage.urlsync.spec.tsx with view/edit transition and arrival coverage</name>

  <read_first>
    - `packages/web/src/components/DashboardsPage.urlsync.spec.tsx` (FULL FILE, 305 lines — the
      harness, the OpenLayers/ResizeObserver stubs, the `DASH_77` fixture at `:241`, and the two
      existing describes: `URLSYNC-113` at `:117` and `initialOpenDashboard (Phase 114)` at `:249`,
      whose three `initialOpenDashboard={DASH_77}` renders at `:265/:281/:293` must be updated to the
      new `{ dashboard, mode }` shape)
    - `packages/web/src/components/DatasetsPage.urlsync.spec.tsx` (FULL FILE — the ALREADY-SHIPPED
      table equivalent, 17 tests. Mirror its test SHAPES, especially the marker-preservation pair at
      `:113` and `:126`. **Read only — a diff on this file fails criterion 8.**)
    - `packages/web/src/components/DashboardsPage.tsx` (Task 1 output)
    - `CLAUDE.md` §"Writing verifiable acceptance criteria"
  </read_first>

  <files>packages/web/src/components/DashboardsPage.urlsync.spec.tsx</files>

  <behavior>
    First, a FIXTURE FIX (not new coverage): the three existing `DEEPLINK-114` renders at `:265`,
    `:281`, `:293` become `initialOpenDashboard={{ dashboard: DASH_77, mode: "open" }}`. Their
    assertions do NOT change — they describe `open`-mode behaviour, which is unchanged.

    Then NEW tests, every title prefixed `DSET-117: ` (the prefix is free tree-wide; Plan 01 also
    uses it, in a different file). One `it()` per bullet:

    Writing the link (4)
      1. clicking View puts `?dashboard=42&mode=view` in the address bar
      2. clicking Edit in the list row puts `?dashboard=42&mode=edit`
      3. each of those pushes exactly ONE history entry
      4. **clicking Open still puts exactly `?dashboard=42` with no mode qualifier** (DSET-V122-08 at the page level)

    The two in-place transitions (5) — Pitfall 1's coverage
      5. clicking Edit INSIDE the settings screen rewrites the bar to `&mode=edit` and does NOT change `history.length`
      6. Saving from the edit screen rewrites the bar to `&mode=view` and does NOT change `history.length`
      7. **list -> View -> in-app Edit -> Save: after BOTH in-place changes the in-app Back still POPS** (marker survived two `replaceState`s)
      8. **deep-link arrival into view -> in-app Edit: the in-app Back still WRITES** (marker correctly still absent — the user is not ejected)
      9. **deep-link arrival into edit -> Save: the in-app Back still WRITES**

    Back / popstate (3)
      10. browser Back from the settings screen returns to the dashboard list
      11. browser Forward into an entry already left clears the stale param instead of re-opening
      12. the in-app Back from a deep-link arrival into view writes the list URL and the list renders (the user stays in the app)

    Creating (1)
      13. creating a dashboard lands on its settings screen with `?dashboard=<new id>&mode=view` and `history.length` +1

    Arrival / no flash (4)
      14. `{ dashboard, mode: "view" }` mounts straight into the settings screen — the list chrome (the `ds-header` "Updated" column header, and the "+ New Dashboard" button) is NOT in the document
      15. `{ dashboard, mode: "edit" }` mounts straight into the edit form
      16. `{ dashboard, mode: "open" }` still mounts into the running dashboard
      17. an arrival pushes no history entry of its own (`window.history.state`'s marker is undefined)
  </behavior>

  <action>
Extend `packages/web/src/components/DashboardsPage.urlsync.spec.tsx` in place. Do NOT create a new
spec file: `117-RESEARCH.md` §Q5 is explicit that this phase extends the existing seven
dashboard-family files rather than forking siblings, because the entity already owns those files.

Keep the file's existing harness exactly as it is — the ResizeObserver stub, the OpenLayers mock
block, `seedDesignerStore()`, and the `beforeEach` that does
`window.history.replaceState(null, "", "/")` plus the `listDashboards`/`listDashboardTables`/
`listViews`/`listWidgets` mock resets. The new tests need the same seeded designer permissions,
because both the list's Edit button and `DashboardDetail`'s in-app Edit button are gated on
`PERMISSIONS.DASHBOARDS_EDIT`.

Add a third `describe` block for the new coverage rather than interleaving it into the two existing
ones, so a future reader can see at a glance which tests came from which phase.

Seeding the two marker states, the way production reaches them:
- SELF-OPENED (marker true): render the list and click the real View/Edit button; that calls the
  real `openDashboardUrl`. Assert on
  `(window.history.state as Record<string, unknown> | null ?? {})[DASHBOARD_HISTORY_MARKER]`,
  importing the marker from `../lib/dashboardUrl` exactly as the existing `URLSYNC-113` tests do.
- ARRIVAL (marker absent): `window.history.replaceState(null, "", "/?dashboard=77&mode=view")` and
  render with `initialOpenDashboard={{ dashboard: DASH_77, mode: "view" }}`.

For bullets 7-9 and 12 the assertion that matters is the pop-vs-write BRANCH, not jsdom's
asynchronous `history.back()`. Reuse whatever `await`/`waitFor` shape the existing
`URLSYNC-113` popped-branch tests at `:144-161` already use — do not invent a different one.

Bullet 13 needs `createDashboard` mocked to resolve a new DTO; add it to the existing
`vi.mock("../api/client", ...)` partial mock alongside the four already stubbed there.

For bullets 14-16, the components render DIFFERENT chrome: the settings screen shows an "Edit" and a
"Back" button inside `ds-actions`; the edit form shows "Save"/"Saving…" and "Cancel"; the running
dashboard shows the `DashboardOpen` surface. Assert on those, plus the NEGATIVE that the list's
"+ New Dashboard" button and the "Updated" column header are absent — the negative is what makes it
a no-flash test rather than a rendering test.

Do NOT introduce `vi.useFakeTimers()` anywhere in this file (MEMORY: "Web vitest parallel fake-timer
leak"). Where a macrotask must flush, use
`await act(async () => { await new Promise((r) => setTimeout(r, 0)); })`, the idiom the existing
`DEEPLINK-114` tests at `:274-277` already use.
  </action>

  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/components/DashboardsPage.urlsync.spec.tsx</automated>
  </verify>

  <acceptance_criteria>
1. `grep -c "initialOpenDashboard={DASH_77}" components/DashboardsPage.urlsync.spec.tsx` — BEFORE: **3** (lines 265, 281, 293). AFTER: **0**; and `grep -c "dashboard: DASH_77, mode:" components/DashboardsPage.urlsync.spec.tsx` -> **>= 3**.
2. `grep -o 'DSET-117:' components/DashboardsPage.urlsync.spec.tsx | wc -l` — BEFORE: **0**. AFTER: **>= 17**.
3. `cd packages/web && npx vitest run src/components/DashboardsPage.urlsync.spec.tsx` — BEFORE: **13 passed**. AFTER: **>= 30 passed, 0 failed**. Record the literal number.
4. `grep -c "vi.useFakeTimers" components/DashboardsPage.urlsync.spec.tsx` -> **0**.
5. **MUTATION PROBE A — the missed in-place `view->edit` write (Pitfall 1's exact failure).** Temporarily delete the `setDashboardMode(view.dashboard.id, "edit");` call from `DashboardDetail.onEdit` in `DashboardsPage.tsx`, leaving its `setView`. Re-run this spec: MUST report **>= 1 failed** (behaviour bullet 5, and probably 7). Record the literal count, revert, confirm green.
6. **MUTATION PROBE B — the hardcoded history state at the view->edit site.** Temporarily change the `onEdit` handler to call `openDashboardUrl(view.dashboard.id, "edit")` instead of `setDashboardMode(...)`. Re-run: MUST report **>= 1 failed** (bullet 5's `history.length` assertion, and/or bullet 8's arrival branch). Record the count, revert. **This probe and probe A must fail for at least one DIFFERENT test between them** — if they redden exactly the same single test, the suite proves only "something happens on Edit", not "the RIGHT thing happens", and a test distinguishing push from in-place replace is required before proceeding.
7. **MUTATION PROBE C — the bare-URL page path (DSET-V122-08).** Temporarily change the list's Open button from `openDashboardUrl(dash.id)` to `openDashboardUrl(dash.id, "view")`. Re-run: MUST report **>= 1 failed** (bullet 4). Record the count, revert. If this does NOT redden the suite, the page-level bare-URL guard is not guarding anything — say so in the SUMMARY.
8. `git diff --name-only -- packages/web/src/components/DatasetsPage.urlsync.spec.tsx packages/web/src/lib/tableUrl.spec.ts packages/web/src/hooks/useDeepLinkTable.spec.ts` — **NO OUTPUT**.
9. `cd packages/web && npx tsc --noEmit` exits 0.
10. `cd packages/web && npx vitest run` — 100% pass, **175 test files** (no new spec file was created). Report the literal test total.
11. `git diff --stat -- packages/server` — **NO OUTPUT**.
  </acceptance_criteria>

  <done>All 17 behaviour bullets are covered at >= 30 passing tests in the file; the three legacy fixture renders carry the new prop shape with their assertions untouched; all three mutation probes reddened the suite and probes A and B reddened at least one different test each, with the literal counts recorded; no table spec and no new spec file.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx tsc --noEmit` — clean.
- `cd packages/web && npx vitest run` — 100% pass at **175 files** (unchanged; this plan creates no spec file). Report the test total and its delta from Plan 01's recorded total.
- `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` — green (no CSS, no new className).
- `git diff --name-only -- packages/web/src/lib/tableUrl.ts packages/web/src/hooks/useDeepLinkTable.ts packages/web/src/components/DatasetsPage.tsx packages/web/src/components/DatasetsPage.urlsync.spec.tsx packages/web/src/lib/tableUrl.spec.ts packages/web/src/hooks/useDeepLinkTable.spec.ts` — NO OUTPUT.
- `git diff --stat -- packages/server` — NO OUTPUT.
</verification>

<success_criteria>
- All twelve in-component transitions from the verified table are handled, including the two with no table analogue (create->view push, view->edit in-place) — proved by exact counts, not ">= 1" checks.
- `setDashboardMode` is called at exactly two DIFFERENT sites in two DIFFERENT directions.
- The list's Open handler is byte-identical and a page-level test proves `?dashboard=42` still carries no mode qualifier.
- A deep-link arrival mounts straight into the named screen with the list chrome absent from the document.
- In-app Back pops when we pushed and writes when the user arrived — proved after one, two and zero in-place mode changes.
</success_criteria>

<output>
After completion, create `.planning/phases/117-dashboard-settings-links/117-02-SUMMARY.md`. Record,
as literal numbers: the new `DashboardsPage.urlsync.spec.tsx` test total, the full-suite test total,
and the failure count from mutation probes A, B and C — including which TESTS each reddened, so the
"A and B must differ" check is auditable. Carry forward, do NOT fix: the Sidebar "Dashboards" entry
is a no-op while a dashboard screen is open (`TLINK-F3`, pre-existing, affects both pages).
</output>
