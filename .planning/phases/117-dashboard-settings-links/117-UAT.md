---
status: complete
phase: 117-dashboard-settings-links
source: [117-01-SUMMARY.md, 117-02-SUMMARY.md, 117-03-SUMMARY.md, 117-04-SUMMARY.md, 117-05-SUMMARY.md]
started: 2026-09-15T00:00:00Z
updated: 2026-09-15T00:00:00Z
verdict: APPROVED — 23 pass (Groups A-F, G24-G26), UAT-117-G27 not exercised (password-mode-only instance; OIDC half never observed live — standing debt)
---

# Phase 117 — Dashboard Settings Links — Operator Walkthrough

**What was built:** Dashboard settings and edit screens are now reachable by URL, exactly as a
table's are, while the bare running-dashboard link is unchanged:
- `?dashboard=<id>` — the RUNNING dashboard (unchanged; this is the link people already have)
- `?dashboard=<id>&mode=view` — the settings/detail screen
- `?dashboard=<id>&mode=edit` — the edit screen

jsdom proves the state machine end to end: the right URL is written, the right screen mounts,
the marker is preserved across in-place mode changes, and the bar is cleared on every genuine
leave (220 tests across the seven dashboard-family spec files, mutation-probe verified — see
`117-05-SUMMARY.md`). It structurally **cannot** prove three things, which is what this
walkthrough is for: whether the in-place Edit transition FLASHES in a real browser, whether the
failure banner READS correctly in both themes, and whether an arrival can eject the user out of
the application.

Each check below has an unambiguous PASS condition and a stated failure shape, per
`CLAUDE.md`'s "Writing verifiable acceptance criteria" rule — state plainly what PASS looks
like and what FAIL looks like, never bury the real expectation in a parenthetical.

**Report per check:** the check ID, PASS or FAIL, and for any FAIL, what you saw instead. A FAIL
is a bug in Plans 01-05's code, not a reason to amend these steps.

## Current Test

[testing complete]

## GROUP A — needs only the app shell and at least one dashboard you can open/view/edit

### UAT-117-A1/A2. The bare link is untouched (DSET-V122-08) — do this FIRST
expected: Open the app, go to Dashboards, click **Open** on any dashboard.
PASS = the address bar reads exactly `?dashboard=<id>` with **no `&mode=`** anywhere.
FAIL = any `mode=` appears — a live, bookmarked URL has been changed.
Result: pass

### UAT-117-A3/A4. The bare link opens the running dashboard directly in a new tab
expected: Copy that URL from A1/A2, open it in a NEW TAB.
PASS = the running dashboard loads directly, with widgets, exactly as before this change.
FAIL = you land on the settings screen, or on the dashboard LIST first.
Result: pass

### UAT-117-B5. The View button writes the settings link
expected: Back on the list, click **View** on a dashboard.
PASS = the address bar reads `?dashboard=<id>&mode=view`.
Result: pass

### UAT-117-B6. Browser Back from the settings screen clears the param
expected: Press browser **Back**.
PASS = you are on the dashboard list and the param is gone.
Result: pass

### UAT-117-B7. The Edit button writes a distinct link
expected: Click **Edit** on a dashboard in the list.
PASS = the address bar reads `?dashboard=<id>&mode=edit`.
Result: pass

### UAT-117-B8. Browser Back from the edit screen clears the param and stays in the app
expected: Press browser **Back**.
PASS = the dashboard list, param gone.
FAIL = you leave the application entirely.
Result: pass

### UAT-117-C9/C10. The in-place view->edit transition does not flash
expected: Click **View** on a dashboard, then click the **Edit** button INSIDE that settings
screen.
PASS = the edit form appears with **no flash of the dashboard list** in between, and the address
bar changes from `&mode=view` to `&mode=edit`.
FAIL = you see the list, even for a frame; or the bar loses the param; or pressing Back once
lands you back on the settings screen instead of the list (a new history entry was wrongly
created).
Result: pass

### UAT-117-C11/C12. Save lands on settings and the param survives a deferred timer
expected: Change the dashboard's name, click **Save**.
PASS = you land on the settings screen showing the new name, the bar reads `&mode=view`, **and
the param is still there a second later** (wait a beat, then look again).
FAIL = the URL silently empties about a tick after Save — a deferred-timer bug.
Result: pass

### UAT-117-D13/D14. A deep-linked edit URL loads directly with no history behind it
expected: Open a NEW TAB and paste `<app-url>/?dashboard=<id>&mode=edit`.
PASS = the edit form loads directly; you never see the dashboard list, not even briefly.
Result: pass

### UAT-117-D15. Cancel from a deep-link arrival stays inside the app
expected: Click **Cancel**.
PASS = you land on the dashboard list INSIDE the app and the param is gone.
FAIL = the tab navigates away from the application entirely (the ejection bug).
Result: pass

### UAT-117-D16. The view-arrival -> Edit -> Save -> Back chain all stays inside the app
expected: In another new tab, paste `<app-url>/?dashboard=<id>&mode=view`, then click the
in-screen **Edit**, then **Save**, then **Back**.
PASS = each step stays inside the app and ends on the list.
Result: pass

### UAT-117-E17/E18. The failure banner shows one honest, non-leaking message
expected: Paste `<app-url>/?dashboard=999999&mode=view` (an id that does not exist).
PASS = you land on the dashboard LIST with a banner saying the dashboard isn't available and
that it may have been deleted **or** you may not have access — one message, both clauses. The
`?dashboard=` param is stripped from the bar.
FAIL = a blank screen, a raw error, a message naming which of the two reasons applies, or a
message that quotes the id back.
Result: pass

### UAT-117-E19. The banner is dismissible
expected: Dismiss the banner from E17/E18.
PASS = it goes away and the list is usable.
Result: pass

### UAT-117-E20. The failure banner is legible in BOTH themes
expected: **Switch to the other colour theme and repeat E17/E18's paste.**
PASS = the banner text is legible and its colours read correctly.
FAIL = washed-out, invisible, or clashing text.
(This check exists because `theme-guard.spec.ts` exempts `global.css` wholesale from its hex
scan — a light-mode banner defect has shipped through a fully green suite on this codebase
before.)
Result: pass

### UAT-117-F21/F22. Leaving by the sidebar from the settings screen clears the param
expected: Open a dashboard's settings screen, then click **Datasets** in the sidebar.
PASS = within a moment the bar carries no `?dashboard=` and no `&mode=`.
Result: pass

### UAT-117-F23. Leaving by the sidebar from the edit screen clears the param
expected: Repeat F21/F22 starting from the EDIT screen.
PASS = same as F21/F22.
Result: pass

## GROUP G — only if your instance can sign out and back in

### UAT-117-G24/G25. Logged-out arrival routes to login with both params intact
expected: Sign out. Paste `<app-url>/?dashboard=<id>&mode=edit`.
PASS = you get the login page and the URL still shows both `dashboard` and `mode` params.
Result: pass

### UAT-117-G26. Signing back in lands on the named screen, not the list or the running dashboard
expected: Sign in.
PASS = you land on the EDIT screen for that dashboard.
Result: pass

### UAT-117-G27. OIDC note
If your instance runs `AUTH_MODE=oidc`, say so explicitly — the `kbi_returnTo` carry has never
been observed in a browser on this codebase (a standing debt item, see `.planning/REQUIREMENTS.md`
"OIDC never browser-verified"), and this would be the first time.
Auth mode used: password
Result: not exercised (password mode)

## GROUP B — needs the live Kinetica cluster

**None.** `DashboardDetail` and `DashboardEdit` make no `fetchKinetica*` calls — Save is
`PATCH /api/dashboards/:id`, which is local SQLite (`packages/server/src/index.ts:794`), exactly
mirroring Phase 116's table equivalent. This mirrors `116-UAT.md`'s identical finding for the
table side of the same mechanism.

## KNOWN NOT COVERED

State this explicitly so the operator does not test for it and report a false defect:

- **Unsaved changes in the dashboard edit form are discarded silently** on Cancel, on sidebar
  navigation away, and on browser Back. This is DELIBERATE and matches the Phase 116 operator
  decision for tables (`TLINK-F1` in `.planning/REQUIREMENTS.md` — "both or neither"; Phase 117
  built neither). It is NOT a regression: before this phase, browser Back from a dashboard edit
  left the application entirely; after this phase it lands on the dashboard list, which is
  strictly better even without a dirty-state guard.
- `create` screens are not linkable, by design (`DSET-F1` — nothing to identify a not-yet-saved
  dashboard by).
- Roles and Settings pages are still not linkable (`DLINK-F4`, unchanged by this phase).
- Sidebar "Dashboards" is a no-op while a dashboard is already open (`TLINK-F3`, pre-existing,
  affects both Datasets and Dashboards). Confirmed present, deliberately not fixed here.
- A hand-crafted `?dashboard=5&table=12&mode=edit` URL loses the table's `mode` qualifier if the
  dashboard leaves (`DSET-F2`). No UI path produces this URL; it is reachable only by manually
  editing the address bar.

## Coverage Limitation — OIDC half of DSET-V122-07 not exercised live

**This is the headline caveat on this UAT record: "all checks pass" describes password mode only.**

`packages/server/.env` is `AUTH_MODE=password` on this instance. The operator's entire live
walkthrough — including Group G (UAT-117-G24 through G26, all recorded PASS) — therefore ran
exclusively in PASSWORD mode. The OIDC-specific half of `DSET-V122-07` was **not** exercised live
in a browser:

- `handleSignInCommit`'s OIDC-gated `sessionStorage` write (the `dashboardId`/`dashboardMode`
  fields on `kbi_returnTo`) never fires in password mode.
- The post-round-trip address-bar restore only matters when a full-page navigation to a real IdP
  destroys and later restores the query string — password-mode sign-in never unmounts the app, so
  the URL is never actually lost and restored the same way.

So the OIDC half of `DSET-V122-07` is covered by automated tests and by Plan 05's mutation probes
(all of which reddened as required — see `117-05-SUMMARY.md`), but it has **never been observed
working end-to-end in a real browser against a real IdP**, on this codebase, at any phase.
`UAT-117-G27` is therefore recorded as "not exercised (password mode)", **not** as a pass — the
operator's "all checks passed" report cannot cover a path their configuration does not reach. This
mirrors `115-UAT.md`'s Group C and `116-UAT.md`'s UAT-116-12 coverage-limitation wording exactly,
for the same standing gap.

This is a **STANDING debt item**, not new to this phase — see `.planning/REQUIREMENTS.md` §"Carried
Tech Debt", "OIDC never browser-verified".

**Recommendation:** re-verify the OIDC half of `DSET-V122-07` (and, while at it, `115-UAT.md`'s
Group C and `116-UAT.md`'s UAT-116-12) live whenever an OIDC-configured deployment is next
available, before treating any of them as battle-tested rather than test-suite-proven.

## Summary

total: 23 (23 pass; excluding G27, which is a mode-recording note, not exercised in this mode)
passed: 23
issues: 0
pending: 0
skipped: 0 (1 not exercised — G27, OIDC-gated, this instance is password-mode only)

## Gaps

None. All 23 checks (Groups A-F, G24-G26) reported PASS by the operator on 2026-09-15, including
the four highest-stakes checks: A1-A4 (DSET-V122-08, the bare link live in the wild), D15 (Cancel
from a deep-link arrival stays inside the app — the ejection bug), E20 (the failure banner legible
in BOTH themes — the one guard theme-guard's `global.css` exemption does not provide), and C11/C12
(the id-AND-mode deferred-timer guard). See "Coverage Limitation" above for the one honestly
unclosed gap — UAT-117-G27, not a gap in what was actually run.

---

**Operator verdict (2026-09-15):** all 23 checks (Groups A-F, G24-G26) reported PASS. Auth mode
used for Group G: password. UAT-117-G27 recorded as "not exercised (password mode)" — the OIDC
half of DSET-V122-07 remains a standing, pre-existing debt item, carried forward for future
re-verification. Phase 117 is closed on this UAT record.
