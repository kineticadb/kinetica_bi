---
phase: 83-branding-admin-ui
plan: 02
type: execute
wave: 2
depends_on: ["83-01"]
files_modified:
  - packages/web/src/components/settings/BrandColorPicker.tsx
  - packages/web/src/components/settings/wcag.ts
  - packages/web/src/components/settings/WcagBadge.tsx
  - packages/web/src/components/settings/BrandingSettingsPage.tsx
  - packages/web/src/components/settings/BrandingSettingsPage.css
  - packages/web/src/styles/theme-guard.spec.ts
  - packages/web/src/components/settings/__tests__/wcag.spec.ts
  - packages/web/src/components/settings/__tests__/BrandColorSection.spec.tsx
autonomous: true
requirements: [BRANDUI-02, BRANDUI-03, SECA-V116-02]
must_haves:
  truths:
    - "The Colors section shows 18 react-colorful pickers in two columns (Dark | Light): 9 token pairs (accent, accent-2, accent-text, bg, panel, text, muted, border, danger)."
    - "Changing a picker updates the draft, live-applies to :root, and re-skins the app for the currently active theme."
    - "WCAG warn-only badges render on critical pairs (text/bg, accent-text/accent) for BOTH dark and light columns simultaneously; Save is NOT blocked by a failing badge."
    - "The Fonts section lets the admin pick a body font and a display font from a curated select; selection live-applies and persists into the draft."
  artifacts:
    - path: "packages/web/src/components/settings/BrandColorPicker.tsx"
      provides: "Labeled HexColorPicker wrapper (react-colorful) with hex readout"
      contains: "HexColorPicker"
    - path: "packages/web/src/components/settings/wcag.ts"
      provides: "colord a11y contrast helper (extend([a11yPlugin]) once)"
      contains: "extend([a11yPlugin])"
    - path: "packages/web/src/components/settings/WcagBadge.tsx"
      provides: "warn-only pass/fail contrast badge"
      contains: "wcag-pass"
    - path: "packages/web/src/components/settings/BrandingSettingsPage.tsx"
      provides: "Colors + Fonts sections wired into the scaffold"
      contains: "BrandColorPicker"
  key_links:
    - from: "packages/web/src/components/settings/BrandColorPicker.tsx"
      to: "applyBrandTokens"
      via: "onChange -> handleDraftChange -> live :root apply"
      pattern: "handleDraftChange|applyBrandTokens"
    - from: "packages/web/src/components/settings/WcagBadge.tsx"
      to: "wcag.ts contrast()"
      via: "colord contrast ratio vs 4.5 threshold"
      pattern: "contrast"
---

<objective>
Build the Colors section (18 react-colorful pickers, dark|light side-by-side, with live WCAG warn-only badges on critical pairs) and the Fonts section (curated body + display font selects) inside the existing BrandingSettingsPage scaffold.

Purpose: BRANDUI-02 (edit dark+light palette via pickers with live WCAG indicator), SECA-V116-02 (surface contrast guardrails dark AND light simultaneously, warn-only), BRANDUI-03 (curated self-hosted font picks).
Output: A working color editor with live re-skin + non-blocking contrast feedback, and font selects, both wired to the draft + live-preview path from 83-01.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/83-branding-admin-ui/83-RESEARCH.md
@.planning/phases/83-branding-admin-ui/83-CONTEXT.md
@.planning/phases/83-branding-admin-ui/83-01-PLAN.md

<interfaces>
<!-- From 83-01 (already landed): -->
import { applyBrandTokens } from "../../store/brandStore";   // now EXPORTED
// BrandConfigPayload now has: primaryColor/accent2Color/bgColor/panelColor/textColor/mutedColor/borderColor/dangerColor
//   + their light* variants + accentTextColor/lightAccentTextColor + display/feel fields
// BrandingSettingsPage owns: draft, isDirty, saving, handleDraftChange(updates: Partial<BrandConfigPayload>)
//   handleDraftChange already does: setDraft + setIsDirty(true) + applyBrandTokens(next, activeTheme)
// Section placeholders: id="brand-colors", id="brand-fonts" (fill these)

<!-- react-colorful (installed by 83-01): -->
import { HexColorPicker } from "react-colorful";
<HexColorPicker color={hex} onChange={(hex: string) => void} />

<!-- colord (installed by 83-01): -->
import { colord, extend } from "colord";
import a11yPlugin from "colord/plugins/a11y";
extend([a11yPlugin]);              // ONCE at module level
colord(bg).contrast(fg) // -> number 1..21

<!-- Token map (RESEARCH §"Color Section"): 9 pairs total -->
// primaryColor/lightPrimaryColor -> --accent ;  accent2Color/lightAccent2Color -> --accent-2
// accentTextColor/lightAccentTextColor -> --accent-text ;  bgColor/lightBgColor -> --bg
// panelColor/lightPanelColor -> --panel ;  textColor/lightTextColor -> --text
// mutedColor/lightMutedColor -> --muted ;  borderColor/lightBorderColor -> --border
// dangerColor/lightDangerColor -> --danger
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: wcag.ts contrast helper + WcagBadge + BrandColorPicker + ALLOWLIST</name>
  <read_first>
    - 83-RESEARCH.md §"colord WCAG: Exact API" (extend([a11yPlugin]) ONCE, contrast() returns 1-21), §"Critical Pairs" table (text/bg, accent-text/accent at AA normal 4.5; on-accent #ffffff/accent), §"WCAG Badge Rendering" (wcag-pass/wcag-fail classes use var(--success)/var(--danger)), §"Code Examples > HexColorPicker + colord WCAG badge"
    - packages/web/src/components/charts/DataFilterConfigPanel.tsx (ds-field / ds-field-label / config-group class conventions)
    - packages/web/src/styles/theme-guard.spec.ts ALLOWLIST (line 37) + the "ALLOWLISTED but contains no hex" guard (~line 105)
  </read_first>
  <files>packages/web/src/components/settings/wcag.ts, packages/web/src/components/settings/WcagBadge.tsx, packages/web/src/components/settings/BrandColorPicker.tsx, packages/web/src/components/settings/__tests__/wcag.spec.ts, packages/web/src/styles/theme-guard.spec.ts</files>
  <behavior>
    - wcag.ts `contrastRatio("#000000","#ffffff")` returns 21 (±0.1); `contrastRatio("#777777","#777777")` returns 1.
    - wcag.ts `passesAA(ratio)` true for ratio >= 4.5, false below.
    - WcagBadge with fg/bg that fail (e.g. fg "#cccccc" bg "#ffffff") renders class "wcag-fail" and text containing "FAIL"; a passing pair renders "wcag-pass" + "AA".
  </behavior>
  <action>
    1. `wcag.ts`: `import { colord, extend } from "colord"; import a11yPlugin from "colord/plugins/a11y"; extend([a11yPlugin]);` at top (ONCE). Export `contrastRatio(bg: string, fg: string): number => colord(bg).contrast(fg)` and `passesAA(ratio: number, threshold = 4.5): boolean => ratio >= threshold`. (Critical text/UI pairs use AA-normal 4.5; large/UI 3.0 is available via the threshold param if a later pair needs it.)
    2. `WcagBadge.tsx`: props `{ fg: string; bg: string; threshold?: number }`. Compute `ratio = contrastRatio(bg, fg)`, `pass = passesAA(ratio, threshold)`. Render `<span className={pass ? "wcag-pass" : "wcag-fail"} title={...}>{ratio.toFixed(1)}:1 {pass ? "AA" : "FAIL"}</span>`. Warn-only — no disabling, no Save block.
    3. `BrandColorPicker.tsx`: props `{ label: string; value: string; fallback: string; onChange: (hex: string) => void }`. Render `<HexColorPicker color={value || fallback} onChange={onChange} />` + a `<label className="ds-field-label">{label}</label>` + a hex readout `<span className="brand-color-hex">{value || fallback}</span>`. The Aurora-default hex literals (fallbacks) are passed IN as props from the parent — but if any literal hex appears in this file, it must be ALLOWLISTED.
    4. theme-guard ALLOWLIST: add `"components/settings/BrandColorPicker.tsx",` (and `"components/settings/BrandingSettingsPage.tsx",` IF the parent ends up holding the Aurora default-hex map — see Task 2) each with a one-line justification comment `// brand color tooling authors literal hex default palette values`. Run the guard; if it complains an ALLOWLISTED file has no hex, remove that specific stale entry. The defaults map (Aurora hex per token) should live wherever the hex literals actually are — keep them in ONE file and ALLOWLIST exactly that file.
    5. Badge classes: add `.wcag-pass { color: var(--success); }` and `.wcag-fail { color: var(--danger); }` and `.brand-color-hex { ... }` to BrandingSettingsPage.css using ONLY tokens (no hex).
  </action>
  <verify>
    <automated>cd packages/web && grep -q "extend(\[a11yPlugin\])" src/components/settings/wcag.ts && grep -q "HexColorPicker" src/components/settings/BrandColorPicker.tsx && grep -q "wcag-fail" src/components/settings/WcagBadge.tsx && npx vitest run src/components/settings/__tests__/wcag.spec.ts && npx vitest run src/styles/theme-guard.spec.ts && echo OK</automated>
  </verify>
  <done>wcag.ts contrast/passesAA tested; WcagBadge + BrandColorPicker built; theme-guard green with ALLOWLIST entries justified.</done>
  <acceptance_criteria>
    - `src/components/settings/wcag.ts` contains "extend([a11yPlugin])", "contrastRatio", "passesAA"
    - `src/components/settings/__tests__/wcag.spec.ts` asserts contrastRatio black/white ≈ 21 and passesAA boundary at 4.5
    - `src/components/settings/WcagBadge.tsx` contains "wcag-pass" and "wcag-fail" and "FAIL"
    - `src/components/settings/BrandColorPicker.tsx` contains "HexColorPicker"
    - `src/styles/theme-guard.spec.ts` ALLOWLIST contains "BrandColorPicker.tsx" with a justification comment
    - `cd packages/web && npx vitest run src/components/settings/__tests__/wcag.spec.ts src/styles/theme-guard.spec.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Colors (18 pickers, dark|light) + WCAG critical-pair badges into BrandingSettingsPage</name>
  <read_first>
    - packages/web/src/components/settings/BrandingSettingsPage.tsx (the `id="brand-colors"` placeholder + `handleDraftChange` from 83-01)
    - 83-RESEARCH.md §"Color Section" (9-pair token map, the 16→18 picker note in Open Question 1, active-theme nuance), §"Critical Pairs" (which pairs get badges + thresholds)
    - packages/web/src/styles/global.css :root + :root[data-theme="light"] (the Aurora DEFAULT hex per token — use these as picker fallbacks)
  </read_first>
  <files>packages/web/src/components/settings/BrandingSettingsPage.tsx, packages/web/src/components/settings/BrandingSettingsPage.css, packages/web/src/components/settings/__tests__/BrandColorSection.spec.tsx</files>
  <behavior>
    - Rendering BrandingSettingsPage shows 18 color pickers (9 dark + 9 light) — assert by querying BrandColorPicker labels/count.
    - Changing a picker's value (simulate onChange) updates the draft and triggers applyBrandTokens (spy/mocked) — assert handleDraftChange path runs for the right field.
    - The critical-pair WcagBadge for text/bg renders in BOTH the dark column and the light column simultaneously.
  </behavior>
  <action>
    Fill the `id="brand-colors"` section with a two-column layout (Dark | Light). Define a `COLOR_FIELDS` array of 9 `{ token, label, darkField, lightField, darkDefault, lightDefault }` rows using the EXACT field names from the interfaces block and the Aurora default hex read from global.css. For each row render a `BrandColorPicker` in the Dark column (value=draft[darkField], fallback=darkDefault, onChange=(hex)=>handleDraftChange({ [darkField]: hex })) and one in the Light column (lightField). Keep the Aurora default-hex map in THIS file (the parent) OR in a small `brandDefaults.ts` — whichever you choose MUST be the file on the theme-guard ALLOWLIST (coordinate with Task 1; do not split hex literals across two un-allowlisted files).

    WCAG badges (warn-only) on the critical pairs from RESEARCH §"Critical Pairs", rendered in BOTH columns:
    - text on bg: `<WcagBadge fg={draft.textColor ?? darkTextDefault} bg={draft.bgColor ?? darkBgDefault} />` (dark) + the light variants (light column). Threshold AA-normal 4.5.
    - accent-text on accent: `<WcagBadge fg={draft.accentTextColor ?? ...} bg={draft.primaryColor ?? ...} />` (dark) + light variants. Threshold 4.5.
    - on-accent (#ffffff button text) on accent: `<WcagBadge fg="#ffffff" bg={draft.primaryColor ?? ...} />` (dark) + light. (The "#ffffff" literal lives in this ALLOWLISTED file.)
    Add a short note element explaining the active-theme nuance (editing the off-theme column won't change the live app until the theme is toggled — RESEARCH/CONTEXT). Save is NEVER blocked by a failing badge.

    CSS: two-column grid for `.brand-color-columns` using tokens only.
  </action>
  <verify>
    <automated>cd packages/web && grep -q "BrandColorPicker" src/components/settings/BrandingSettingsPage.tsx && grep -q "WcagBadge" src/components/settings/BrandingSettingsPage.tsx && grep -q "accentTextColor" src/components/settings/BrandingSettingsPage.tsx && npx vitest run src/components/settings/__tests__/BrandColorSection.spec.tsx && npx vitest run src/styles/theme-guard.spec.ts && npx tsc --noEmit && echo OK</automated>
  </verify>
  <done>18 pickers render in dark|light columns; critical-pair WcagBadges render in both columns; picker change flows through handleDraftChange; Save not blocked; theme-guard green; tsc clean.</done>
  <acceptance_criteria>
    - `src/components/settings/BrandingSettingsPage.tsx` contains "BrandColorPicker", "WcagBadge", "accentTextColor", and references all 9 token pairs (grep the field names)
    - `BrandColorSection.spec.tsx` asserts 18 pickers render and a text/bg WcagBadge appears in both dark and light columns
    - `cd packages/web && npx vitest run src/components/settings/__tests__/BrandColorSection.spec.tsx src/styles/theme-guard.spec.ts && npx tsc --noEmit` all exit 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Fonts section — curated body + display selects</name>
  <read_first>
    - 83-RESEARCH.md §"Fonts Section" (--font-body + --font-display already exist; self-hosted strategy; curated 4-6 option select; check installed @fontsource-variable)
    - packages/web/src/styles/global.css lines ~ where --font-body / --font-display are defined (the current Aurora family strings)
    - packages/web/src/components/charts/DataFilterConfigPanel.tsx (ds-select class)
  </read_first>
  <files>packages/web/src/components/settings/BrandingSettingsPage.tsx, packages/web/src/components/settings/BrandingSettingsPage.css</files>
  <action>
    Confirm which fonts are actually self-hosted: `ls packages/web/node_modules/@fontsource-variable/ 2>/dev/null` and grep the Phase-80 font imports (`grep -rn "fontsource" packages/web/src`). Build a `CURATED_FONTS` list of 4-6 entries `{ label, css }` where `css` is the full font-family stack string — MUST be limited to already-installed/self-hosted families plus generic fallbacks (NO arbitrary user URLs — REQUIREMENTS out-of-scope). Include the current Aurora defaults (Manrope Variable body, Space Grotesk Variable display) as the first options.

    Fill the `id="brand-fonts"` section with two `<select className="ds-select">`:
    - Body font: value `draft.fontFamily ?? <Aurora body default>`, onChange `handleDraftChange({ fontFamily: e.target.value })`.
    - Display font: value `draft.displayFontFamily ?? <Aurora display default>`, onChange `handleDraftChange({ displayFontFamily: e.target.value })`.
    Both live-apply via the existing handleDraftChange path (applyBrandTokens maps fontFamily→--font-body, displayFontFamily→--font-display per 83-01). No hex; tokens only in CSS.
  </action>
  <verify>
    <automated>cd packages/web && grep -q "displayFontFamily" src/components/settings/BrandingSettingsPage.tsx && grep -q "fontFamily" src/components/settings/BrandingSettingsPage.tsx && grep -q "ds-select" src/components/settings/BrandingSettingsPage.tsx && npx tsc --noEmit && npx vitest run && echo OK</automated>
  </verify>
  <done>Body + display font selects wired to draft; curated self-hosted list only; live-apply via handleDraftChange; web vitest 100%; tsc clean.</done>
  <acceptance_criteria>
    - `src/components/settings/BrandingSettingsPage.tsx` Fonts section contains two selects bound to `fontFamily` and `displayFontFamily` via handleDraftChange
    - `CURATED_FONTS` contains only self-hosted/installed families + generic fallbacks (no http/https URLs)
    - `cd packages/web && npx tsc --noEmit && npx vitest run` all exit 0 (100%)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd packages/web && npx vitest run` 100% (incl. wcag.spec, BrandColorSection.spec, theme-guard.spec).
- `cd packages/web && npx tsc --noEmit` clean.
- Colors: 18 pickers (dark|light); critical-pair badges in BOTH columns; Save not blocked by failing contrast.
- Fonts: body + display selects from a curated self-hosted list; live-apply.
</verification>

<success_criteria>
- BRANDUI-02: dark+light palette editable via pickers with live WCAG indicator on critical pairs.
- SECA-V116-02: contrast guardrails surfaced for dark AND light simultaneously, warn-only.
- BRANDUI-03: body + display fonts chosen from a curated self-hosted list, live preview.
</success_criteria>

<output>
After completion, create `.planning/phases/83-branding-admin-ui/83-02-SUMMARY.md`
</output>
