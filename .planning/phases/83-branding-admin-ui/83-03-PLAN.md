---
phase: 83-branding-admin-ui
plan: 03
type: execute
wave: 3
depends_on: ["83-02"]
files_modified:
  - packages/web/src/components/settings/FeelLevers.tsx
  - packages/web/src/components/settings/BrandPreviewCard.tsx
  - packages/web/src/components/settings/CustomCssEditor.tsx
  - packages/web/src/components/settings/BrandingSettingsPage.tsx
  - packages/web/src/components/settings/BrandingSettingsPage.css
  - packages/web/src/components/settings/__tests__/FeelLevers.spec.tsx
  - packages/web/src/components/settings/__tests__/CustomCssEditor.spec.tsx
  - packages/web/src/components/settings/__tests__/BrandingSave.spec.tsx
autonomous: true
requirements: [BRANDUI-04, BRANDUI-05, CSS-V116-01]
must_haves:
  truths:
    - "Adjusting density/radius/glow/type-scale/motion segmented controls visibly changes the live :root tokens and the preview card."
    - "A compact preview card renders button/chip/input/badge/nav-item that re-skin live as the admin edits."
    - "Save persists via PUT /api/branding (+ logo POST when a new logo was chosen) and reflects saved state via brandStore.update; Reset stages Aurora defaults live and stays dirty until Save."
    - "The Custom CSS CodeMirror editor injects debounced draft CSS app-wide via a SEPARATE kbi-brand-css-draft <style> (not the saved kbi-custom-css)."
    - "After Save, if the server returned cleaned customCss different from submitted, a 'stripped declarations' notice shows and the editor updates to the sanitized value."
  artifacts:
    - path: "packages/web/src/components/settings/FeelLevers.tsx"
      provides: "density/radius/glow/type-scale/motion segmented controls"
      contains: "densityPreset"
    - path: "packages/web/src/components/settings/BrandPreviewCard.tsx"
      provides: "live-reskinning representative-component preview"
      contains: "preview"
    - path: "packages/web/src/components/settings/CustomCssEditor.tsx"
      provides: "CodeMirror CSS editor + debounced draft inject + stripped notice"
      contains: "kbi-brand-css-draft"
  key_links:
    - from: "packages/web/src/components/settings/FeelLevers.tsx"
      to: "handleDraftChange"
      via: "preset onChange -> draft -> applyBrandTokens live"
      pattern: "handleDraftChange"
    - from: "packages/web/src/components/settings/CustomCssEditor.tsx"
      to: "kbi-brand-css-draft style element"
      via: "debounced textContent inject (separate from kbi-custom-css)"
      pattern: "kbi-brand-css-draft"
    - from: "packages/web/src/components/settings/BrandingSettingsPage.tsx"
      to: "updateBrandConfig + uploadBrandLogo + brandStore.update"
      via: "handleSave"
      pattern: "updateBrandConfig"
---

<objective>
Complete the page: Feel-lever controls (density/radius/glow/type-scale/motion), a compact live preview card, the full Save (PUT + logo POST + reflect)/Reset (stage Aurora defaults) behavior, and the Custom CSS CodeMirror editor with debounced live draft injection and a post-Save "stripped declarations" notice.

Purpose: BRANDUI-04 (feel levers), BRANDUI-05 (live preview + Save + Reset-to-default), CSS-V116-01 (custom CSS persisted + applied at runtime).
Output: A fully functional branding page (minus the dark-logo slot, which is 83-04).
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/83-branding-admin-ui/83-RESEARCH.md
@.planning/phases/83-branding-admin-ui/83-CONTEXT.md
@.planning/phases/83-branding-admin-ui/83-01-PLAN.md

<interfaces>
<!-- From 83-01 / 83-02 (already landed): -->
import { applyBrandTokens } from "../../store/brandStore";
import { updateBrandConfig, uploadBrandLogo, type BrandConfigPayload } from "../../api/client";
// BrandingSettingsPage owns: draft, isDirty, saving, handleDraftChange(updates), handleSave (skeleton), handleReset (skeleton)
// Feel-lever fields on BrandConfigPayload: densityPreset ("compact"|"comfortable"|"spacious"),
//   radiusPreset ("sharp"|"default"|"round"), glowEnabled (boolean), typeScaleBase (number),
//   motionSpeed ("none"|"reduced"|"default"|"fast")
// applyBrandTokens already maps all of these (83-01). Section placeholders: id="brand-feel", id="brand-css".

<!-- Save contract (server, confirmed packages/server/src/index.ts:598-617): -->
// PUT /api/branding  body { config }  -> returns { config: <full sanitized config_json>, updatedAt }
//   server runs sanitizeCssPostcss on config.customCss BEFORE storage and RETURNS the cleaned config.
//   => stripped notice = (submitted.customCss ?? "") !== (response.config.customCss ?? "")
// brandStore.update(config, logoUrl): applyBrandTokens + writes localStorage + notifyOtherTabs()

<!-- CodeMirror (DynamicViewsModal.tsx:43,798,851-854 pattern): -->
import CodeMirror, { oneDark } from "@uiw/react-codemirror";
import { css } from "@codemirror/lang-css";    // dep surfaced by 83-01
const editorTheme = useThemeStore((s) => s.theme) === "dark" ? oneDark : "light";
<CodeMirror value={..} onChange={..} extensions={[css()]} theme={editorTheme} minHeight="200px" />

<!-- BrandStyleInjector (BrandStyleInjector.tsx) owns <style id="kbi-custom-css"> (SAVED css). -->
<!-- Draft preview MUST use a SEPARATE element id="kbi-brand-css-draft" (Pitfall 3). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FeelLevers controls + BrandPreviewCard, wired into the Feel section</name>
  <read_first>
    - 83-RESEARCH.md §"Feel Levers Section" (control types table: density 3-button, radius 3-button, glow toggle, type-scale 3-button Small/Medium/Large, motion 4-button) + §"applyBrandTokens Extension" (the preset enum values + FLAGS: type-scale exposes presets not a slider; glow needs --glow-opacity which 83-01 added)
    - 83-RESEARCH.md §"BrandConfigPayload Extension Spec" (typeScaleBase Small=11/Medium=12/Large=14 presets)
    - packages/web/src/components/settings/BrandingSettingsPage.tsx (id="brand-feel" placeholder + handleDraftChange)
    - packages/web/src/components/charts/DataFilterConfigPanel.tsx (segmented/toggle-button class conventions if any; else use ds-* + a .feel-seg class)
  </read_first>
  <files>packages/web/src/components/settings/FeelLevers.tsx, packages/web/src/components/settings/BrandPreviewCard.tsx, packages/web/src/components/settings/BrandingSettingsPage.tsx, packages/web/src/components/settings/BrandingSettingsPage.css, packages/web/src/components/settings/__tests__/FeelLevers.spec.tsx</files>
  <behavior>
    - Clicking "Comfortable" density calls onChange with `densityPreset: "comfortable"`.
    - Toggling glow off calls onChange with `glowEnabled: false`.
    - Selecting type-scale "Large" calls onChange with `typeScaleBase: 14`.
    - The active preset button has an active/selected class.
  </behavior>
  <action>
    `FeelLevers.tsx`: props `{ draft: BrandConfigPayload; onChange: (u: Partial<BrandConfigPayload>) => void }`. Render 5 control groups (each a labeled segmented set of buttons / a toggle), using ONLY tokens (no hex):
    - Density: Compact/Comfortable/Spacious → onChange `{ densityPreset: "compact"|"comfortable"|"spacious" }`.
    - Radius: Sharp/Default/Round → `{ radiusPreset: "sharp"|"default"|"round" }`.
    - Glow: a toggle (on/off) → `{ glowEnabled: boolean }` (default treated as true/on when null).
    - Type scale: Small/Medium/Large → `{ typeScaleBase: 11 | 12 | 14 }`.
    - Motion: None/Reduced/Default/Fast → `{ motionSpeed: "none"|"reduced"|"default"|"fast" }`.
    Active button reflects current draft value (treat null as the Aurora default: compact / default radius / glow on / 12 / default motion). Each onChange flows up to BrandingSettingsPage.handleDraftChange → live applyBrandTokens.

    `BrandPreviewCard.tsx`: a compact card rendering representative components that consume the live tokens — a primary button, a chip/badge, a text input, an accent-text label, and a faux nav-item. Pure presentational using existing app classes / tokens so it re-skins live with :root (no hex). Place it inside the Feel section (or as a sticky aside) per CONTEXT.

    Wire both into the `id="brand-feel"` section of BrandingSettingsPage: `<FeelLevers draft={draft} onChange={handleDraftChange} />` + `<BrandPreviewCard />`.
  </action>
  <verify>
    <automated>cd packages/web && grep -q "densityPreset" src/components/settings/FeelLevers.tsx && grep -q "glowEnabled" src/components/settings/FeelLevers.tsx && grep -q "motionSpeed" src/components/settings/FeelLevers.tsx && grep -q "FeelLevers" src/components/settings/BrandingSettingsPage.tsx && grep -q "BrandPreviewCard" src/components/settings/BrandingSettingsPage.tsx && npx vitest run src/components/settings/__tests__/FeelLevers.spec.tsx && npx vitest run src/styles/theme-guard.spec.ts && echo OK</automated>
  </verify>
  <done>5 feel-lever controls dispatch the correct preset onChange; preview card renders representative components; both wired into the Feel section; theme-guard green.</done>
  <acceptance_criteria>
    - `src/components/settings/FeelLevers.tsx` contains "densityPreset", "radiusPreset", "glowEnabled", "typeScaleBase", "motionSpeed"
    - `FeelLevers.spec.tsx` asserts density "comfortable", glow off, and type-scale Large(14) onChange dispatches
    - `src/components/settings/BrandingSettingsPage.tsx` contains "FeelLevers" and "BrandPreviewCard"
    - `cd packages/web && npx vitest run src/components/settings/__tests__/FeelLevers.spec.tsx src/styles/theme-guard.spec.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Custom CSS editor (CodeMirror) + debounced draft inject + stripped-declarations notice</name>
  <read_first>
    - 83-RESEARCH.md §"Custom CSS Section" (CodeMirror setup mirroring DynamicViewsModal, css() extension, oneDark when dark, DEBOUNCE_MS≈400, kbi-brand-css-draft separate element, branding-page exemption note, stripped notice = submitted !== returned.config.customCss), §"Code Examples > CodeMirror CSS editor", Pitfall 3 + Pitfall 5
    - packages/web/src/components/DynamicViewsModal.tsx lines 43, 798, 851-854 (CodeMirror import, editorTheme, extensions, minHeight)
    - packages/web/src/components/BrandStyleInjector.tsx (it owns id="kbi-custom-css" for SAVED css — do NOT write to it from the draft)
  </read_first>
  <files>packages/web/src/components/settings/CustomCssEditor.tsx, packages/web/src/components/settings/BrandingSettingsPage.tsx, packages/web/src/components/settings/__tests__/CustomCssEditor.spec.tsx</files>
  <behavior>
    - Typing CSS into the editor calls onChange (updates draft.customCss) and, after the debounce, writes the value into a `<style id="kbi-brand-css-draft">` element (assert element textContent equals the typed value).
    - The draft element is REMOVED on unmount (cleanup) so leaving the page clears the draft preview.
    - Given a submitted CSS and a server-returned (cleaned) CSS that differ, a stripped-declarations notice renders; when equal, no notice.
  </behavior>
  <action>
    `CustomCssEditor.tsx`: props `{ value: string; onChange: (css: string) => void; strippedNotice: string | null }`.
    - `import CodeMirror, { oneDark } from "@uiw/react-codemirror"; import { css } from "@codemirror/lang-css";` `const editorTheme = useThemeStore((s)=>s.theme)==="dark" ? oneDark : "light";`
    - `<CodeMirror value={value} onChange={handleChange} extensions={[css()]} theme={editorTheme} minHeight="200px" maxHeight="400px" placeholder="/* Custom CSS — applies app-wide. Reset clears. */" />`
    - `handleChange(v)`: call `onChange(v)` immediately; debounce (~400ms) a write to a separate draft `<style id="kbi-brand-css-draft">` (create-or-reuse via getElementById; `el.textContent = v` — textContent, never innerHTML; Pitfall 3 — never touch kbi-custom-css).
    - useEffect cleanup: remove the `kbi-brand-css-draft` element (clears draft preview on leave).
    - Render `strippedNotice` (when non-null) as a small warning line (e.g. "Some declarations were removed by the server (url()/@import/@font-face/etc.)").
    - Branding-page exemption: per RESEARCH, the simplest honored approach is to NOT over-engineer scoping — the branding page header (Reset/Save) lives in normal flow and remains reachable; add a one-line code comment noting the page is intentionally not specially scoped and Reset is the recovery (CSS-V116-02 divergence, flagged for Phase 84). Do NOT attempt @scope.

    BrandingSettingsPage `id="brand-css"` section: render `<CustomCssEditor value={draft.customCss ?? ""} onChange={(v) => handleDraftChange({ customCss: v })} strippedNotice={strippedNotice} />` with a `strippedNotice` state. (handleSave wiring of the notice happens in Task 3.)
  </action>
  <verify>
    <automated>cd packages/web && grep -q "kbi-brand-css-draft" src/components/settings/CustomCssEditor.tsx && grep -q "@codemirror/lang-css" src/components/settings/CustomCssEditor.tsx && grep -q "CustomCssEditor" src/components/settings/BrandingSettingsPage.tsx && npx vitest run src/components/settings/__tests__/CustomCssEditor.spec.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <done>CodeMirror CSS editor injects debounced draft to a separate kbi-brand-css-draft element (removed on unmount); never writes kbi-custom-css; stripped notice renders when present; tsc clean.</done>
  <acceptance_criteria>
    - `src/components/settings/CustomCssEditor.tsx` contains "kbi-brand-css-draft", 'from "@codemirror/lang-css"', "oneDark", and a useEffect cleanup removing the draft element; does NOT contain "kbi-custom-css"
    - `CustomCssEditor.spec.tsx` asserts debounced draft injection into kbi-brand-css-draft AND cleanup-on-unmount AND notice-when-different
    - `cd packages/web && npx vitest run src/components/settings/__tests__/CustomCssEditor.spec.tsx && npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Full Save (PUT + logo POST + reflect + stripped diff) and Reset-to-Aurora</name>
  <read_first>
    - 83-RESEARCH.md §"Custom CSS Section > Stripped Declarations Notice" (savedCss !== submittedCss → setStrippedDeclarations + setDraftCss(savedCss)), §"API Client Functions", Pitfall 1 (Reset stages defaults + keeps isDirty true)
    - packages/server/src/index.ts:598-617 (PUT returns { config, updatedAt }) and :644-677 (POST logo returns { logoUrl })
    - packages/web/src/components/settings/BrandingSettingsPage.tsx (handleSave + handleReset skeletons from 83-01; draftLogoFile state if present)
    - packages/web/src/components/RolesPage.tsx handleSave (saving guard + try/finally pattern)
  </read_first>
  <files>packages/web/src/components/settings/BrandingSettingsPage.tsx, packages/web/src/components/settings/__tests__/BrandingSave.spec.tsx</files>
  <behavior>
    - handleSave: when a draftLogoFile exists, calls uploadBrandLogo(file, "primary") then updateBrandConfig(draft); else just updateBrandConfig(draft). On success it calls brandStore.update with the returned config, sets isDirty false, and sets strippedNotice when returned.config.customCss differs from submitted.
    - handleReset: sets draft to Aurora defaults (empty overrides {} for tokens, or explicit defaults), live-applies, sets isDirty TRUE (not false).
  </behavior>
  <action>
    Flesh out `handleSave` in BrandingSettingsPage (replace the 83-01 skeleton):
    - guard `if (saving) return; setSaving(true);` try/finally `setSaving(false)`.
    - `const submittedCss = draft.customCss ?? "";`
    - if a `draftLogoFile` exists (primary logo chosen this session — Logo section state), `await uploadBrandLogo(draftLogoFile, "primary")` and capture returned `logoUrl`.
    - `const resp = await updateBrandConfig(draft);`
    - `const savedCss = resp.config.customCss ?? ""; if (savedCss !== submittedCss) { setStrippedNotice("Some declarations were removed by the server (url()/@import/@font-face/etc.)."); setDraft(d => ({ ...d, customCss: savedCss })); } else setStrippedNotice(null);`
    - `useBrandStore.getState().update(resp.config, <new primary logoUrl ?? current logoUrl>);` (reflects saved state + notifies other tabs; also writes localStorage).
    - clear `draftLogoFile`, `setIsDirty(false)`.

    Flesh out `handleReset`:
    - Stage Aurora defaults LIVE: `applyBrandTokens({}, useThemeStore.getState().theme)` (empty config → removeProperty → Aurora defaults) and `setDraft({})` (or an explicit AURORA_DEFAULTS object if the UI needs the picker fallbacks populated). Per Pitfall 1: `setIsDirty(true)` so the reset is persistable. Do NOT call updateBrandConfig here.

    Keep Save `disabled={!isDirty || saving}`. (Logo section primary-slot UI may already exist from 83-02's Logo placeholder; if `draftLogoFile` state doesn't exist yet, add it here — 83-04 adds the dark slot.)
  </action>
  <verify>
    <automated>cd packages/web && grep -q "updateBrandConfig(draft)" src/components/settings/BrandingSettingsPage.tsx && grep -q "uploadBrandLogo" src/components/settings/BrandingSettingsPage.tsx && grep -q "setStrippedNotice\|strippedNotice" src/components/settings/BrandingSettingsPage.tsx && grep -q "getState().update" src/components/settings/BrandingSettingsPage.tsx && npx vitest run src/components/settings/__tests__/BrandingSave.spec.tsx && npx tsc --noEmit && npx vitest run && echo OK</automated>
  </verify>
  <done>Save PUTs config (+ logo POST when chosen), reflects via brandStore.update, computes stripped notice from the diff; Reset stages Aurora defaults live and keeps isDirty true; web vitest 100%; tsc clean.</done>
  <acceptance_criteria>
    - `src/components/settings/BrandingSettingsPage.tsx` handleSave calls `updateBrandConfig(draft)`, conditionally `uploadBrandLogo`, sets stripped notice on diff, and calls `useBrandStore.getState().update(...)`
    - handleReset live-applies Aurora defaults and sets isDirty TRUE
    - `BrandingSave.spec.tsx` mocks updateBrandConfig and asserts: PUT called, brandStore.update called, stripped notice set when returned css differs, isDirty false after save
    - `cd packages/web && npx vitest run && npx tsc --noEmit` exits 0 (100%)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd packages/web && npx vitest run` 100% (FeelLevers, CustomCssEditor, BrandingSave, theme-guard, wcag, BrandColorSection).
- `cd packages/web && npx tsc --noEmit` clean.
- Feel levers live-apply + preview card re-skins; Save round-trips + reflects + stripped notice; Reset stages defaults (dirty until Save); custom CSS injects via separate draft element only.
</verification>

<success_criteria>
- BRANDUI-04: density/radius/glow/type-scale/motion controls produce observable live changes.
- BRANDUI-05: live preview + Save + Reset-to-Kinetica-default complete.
- CSS-V116-01: custom CSS persisted via PUT + applied at runtime (draft live + saved via BrandStyleInjector); stripped-declarations feedback surfaced.
</success_criteria>

<output>
After completion, create `.planning/phases/83-branding-admin-ui/83-03-SUMMARY.md`
</output>
