---
phase: 83-branding-admin-ui
plan: 04
type: execute
wave: 4
depends_on: ["83-03"]
files_modified:
  - packages/server/src/db.ts
  - packages/server/src/index.ts
  - packages/server/tests/routes.branding.spec.ts
  - packages/web/src/store/brandStore.ts
  - packages/web/src/components/Sidebar.tsx
  - packages/web/index.html
  - packages/web/src/components/settings/LogoUploader.tsx
  - packages/web/src/components/settings/BrandingSettingsPage.tsx
  - packages/web/src/components/settings/BrandingSettingsPage.css
  - packages/web/src/components/settings/__tests__/LogoSection.spec.tsx
autonomous: true
requirements: [BRANDUI-06]
must_haves:
  truths:
    - "brand_config has logo_dark_data/logo_dark_mime/logo_dark_updated_at columns (fresh installs + existing Phase-81 deployments via PRAGMA-guarded ALTER)."
    - "POST /api/branding/logo with variant=dark writes the dark columns; GET /api/branding/logo?variant=dark serves them (404 when absent)."
    - "GET /api/branding returns logoDarkUrl when a dark logo exists, reusing the Phase-81 magic-byte + DOMPurify-SVG validation path."
    - "Sidebar shows the dark logo when theme is dark AND logoDarkUrl exists, else primary logo, else inline DefaultLogo."
    - "The settings page Logo section has two always-visible slots (primary required + dark optional), each previewed on its own mode background; Reset clears both."
  artifacts:
    - path: "packages/server/src/db.ts"
      provides: "logo_dark_* columns in SCHEMA_DDL + PRAGMA-guarded ALTER for brand_config"
      contains: "logo_dark_data"
    - path: "packages/server/src/index.ts"
      provides: "variant-aware logo upload/serve + logoDarkUrl in GET"
      contains: "logoDarkUrl"
    - path: "packages/server/tests/routes.branding.spec.ts"
      provides: "dark-logo route supertests (both auth modes)"
      contains: "variant"
    - path: "packages/web/src/components/settings/LogoUploader.tsx"
      provides: "single logo slot (file pick + preview on a given mode background)"
      contains: "onFileChosen"
  key_links:
    - from: "packages/web/src/components/Sidebar.tsx"
      to: "brandStore.logoDarkUrl + theme"
      via: "effectiveLogoUrl = theme==='dark' && logoDarkUrl ? logoDarkUrl : logoUrl"
      pattern: "logoDarkUrl"
    - from: "packages/server/src/index.ts"
      to: "logo_dark_* columns"
      via: "variant=dark UPDATE / SELECT"
      pattern: "logo_dark"
    - from: "packages/web/src/components/settings/BrandingSettingsPage.tsx"
      to: "uploadBrandLogo(file, 'dark')"
      via: "handleSave uploads dark slot when chosen"
      pattern: "\"dark\"|'dark'"
---

<objective>
BRANDUI-06 vertical slice (BOTH-stack): add optional dark-mode logo override. Server gains `logo_dark_*` columns + a `variant` param on upload/serve + `logoDarkUrl` in GET; client gains `brandStore.logoDarkUrl`, theme-aware Sidebar selection, FOUC favicon awareness, and a dual logo-slot Logo section in the settings page.

Purpose: BRANDUI-06 — uploaded logos are opaque `<img>`s that cannot auto-recolor per theme like the inline default; an admin uploads an optional dark variant shown in dark mode.
Output: A working dark-logo override end-to-end, reusing the Phase-81 validation path, with both-auth-mode supertests.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/83-branding-admin-ui/83-RESEARCH.md
@.planning/phases/83-branding-admin-ui/83-CONTEXT.md
@.planning/phases/83-branding-admin-ui/83-01-PLAN.md

<interfaces>
<!-- Server (confirmed reads): -->
// db.ts SCHEMA_DDL brand_config (db.ts:253-259): id, config_json, logo_data, logo_mime, logo_updated_at, updated_at, updated_by
//   (comment @252 says "NEW v1.16 table — no ALTER needed" — but a Phase-81 DB already deployed lacks the dark cols, so ADD a PRAGMA-guarded ALTER too; pattern at db.ts:277-292 sessions / 303-337 dashboard_layers)
// GET /api/branding (index.ts:416-427): returns { config, logoUrl, updatedAt }; logoUrl built from logo_mime+logo_updated_at
// GET /api/branding/logo (index.ts:431-441): SELECT logo_data, logo_mime; 404 when absent; Buffer.from(base64); nosniff + immutable cache
// POST /api/branding/logo (index.ts:627-679): multer single("logo"); content-sniff SVG -> DOMPurify; raster -> fileTypeFromBuffer magic-byte; else 415; writes logo_* cols; returns { logoUrl }
// requirePermission(PERMISSIONS.BRANDING_MANAGE) gates PUT + POST; GET routes are public (before requireAuth)

<!-- Client (from 83-01/03): -->
// client.ts: BrandingResponse already has logoDarkUrl (83-01); uploadBrandLogo(file, variant: "primary"|"dark") (83-01)
// brandStore BrandState: appName/logoUrl/customCss/hasLoaded/config + bootstrap()/update(config, logoUrl)
//   bootstrap reads data.config + data.logoUrl; localStorage key "kbi-brand-tokens"
// Sidebar.tsx:42 logoUrl = useBrandStore((s)=>s.logoUrl); :51-54 <img src={logoUrl}/> else <DefaultLogo/>
// index.html FOUC IIFE reads localStorage "kbi-brand-tokens"; uses brand.logoUrl for favicon <link rel="icon">
// BrandingSettingsPage (83-03) handleSave already conditionally uploads a primary draftLogoFile; Reset clears overrides
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server — dark-logo columns + variant-aware upload/serve + logoDarkUrl in GET</name>
  <read_first>
    - packages/server/src/db.ts lines 252-259 (brand_config SCHEMA_DDL) + 277-292 (PRAGMA-guarded ALTER pattern for sessions) + 303-337 (dashboard_layers ALTER pattern)
    - packages/server/src/index.ts lines 416-441 (GET branding + GET logo) and 627-679 (POST logo validation path — REUSE verbatim, only branch the target columns by variant)
    - 83-RESEARCH.md §"BRANDUI-06 Server Vertical Slice" (exact DDL, GET logoDarkUrl computation, GET logo variant branch, POST variant branch — copy the column DDL + route shapes), §"Don't Hand-Roll" (reuse Phase-81 SVG/magic-byte path)
  </read_first>
  <files>packages/server/src/db.ts, packages/server/src/index.ts</files>
  <action>
    db.ts:
    1. Add to the brand_config CREATE TABLE (after logo_updated_at): `logo_dark_data TEXT, logo_dark_mime TEXT, logo_dark_updated_at TEXT,` (so fresh installs get them).
    2. Add a PRAGMA-guarded ALTER block (mirror the sessions block @277-292) for existing Phase-81 DBs: read `PRAGMA table_info(brand_config)`, and for each of logo_dark_data/logo_dark_mime/logo_dark_updated_at not present, `instance.exec("ALTER TABLE brand_config ADD COLUMN logo_dark_data TEXT")` etc.
    3. Extend the `BrandConfigRow` type (index.ts ~327 / wherever declared) with `logo_dark_mime?: string | null; logo_dark_updated_at?: string | null;` as needed for the GET select.

    index.ts:
    4. GET /api/branding (@419 SELECT): add `logo_dark_mime, logo_dark_updated_at` to the SELECT; compute `const logoDarkUrl = row.logo_dark_mime && row.logo_dark_updated_at ? "/api/branding/logo?variant=dark&v=" + encodeURIComponent(row.logo_dark_updated_at) : null;` and include `logoDarkUrl` in BOTH the `if (!row)` branch (null) and the main `res.json`.
    5. GET /api/branding/logo (@431): read `const variant = (_req.query as Record<string,string>).variant === "dark" ? "dark" : "primary";` and SELECT/serve the dark columns when variant==="dark" (else the existing primary path). Keep the same 404 / Buffer.from / nosniff / immutable headers.
    6. POST /api/branding/logo (@644 handler body): after the validation produces `storedData`/`storedMime`, branch on `const variant = (req.body as Record<string,unknown>)?.variant === "dark" ? "dark" : "primary";`. For dark, UPDATE `logo_dark_data/logo_dark_mime/logo_dark_updated_at` and `return res.json({ logoDarkUrl: "/api/branding/logo?variant=dark&v=" + encodeURIComponent(ts) })`; else the existing primary UPDATE + `{ logoUrl }` response UNCHANGED. The multer + SVG-sniff + DOMPurify + magic-byte validation is IDENTICAL for both variants — do NOT duplicate or weaken it; only the target columns + response key differ. (multer reads text fields into req.body; `variant` is a form field appended by uploadBrandLogo.)
  </action>
  <verify>
    <automated>cd packages/server && grep -q "logo_dark_data" src/db.ts && grep -q "PRAGMA table_info(brand_config)" src/db.ts && grep -q "logoDarkUrl" src/index.ts && grep -q 'variant === "dark"' src/index.ts && npx tsc --noEmit && echo OK</automated>
  </verify>
  <done>Dark columns in DDL + PRAGMA ALTER; GET returns logoDarkUrl; logo serve + upload branch on variant reusing the Phase-81 validation; server tsc clean.</done>
  <acceptance_criteria>
    - `src/db.ts` contains "logo_dark_data", "logo_dark_mime", "logo_dark_updated_at" in the CREATE TABLE AND a `PRAGMA table_info(brand_config)`-guarded ALTER block
    - `src/index.ts` GET branding contains "logoDarkUrl"; GET logo + POST logo branch on `variant === "dark"` and write/serve the logo_dark_* columns
    - The POST handler's SVG content-sniff + DOMPurify + fileTypeFromBuffer magic-byte checks are reused (not removed/duplicated-weaker) for both variants
    - `cd packages/server && npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Server supertests — dark-logo routes in BOTH auth modes (set-based gate)</name>
  <read_first>
    - packages/server/tests/routes.branding.spec.ts FULL (existing primary-logo + GET/PUT supertests; the auth-mode harness — password + oidc; how it builds the app + authenticates a branding:manage session; SVG/raster fixtures)
    - STATE.md §"v1.16 Test Gates": server vitest is SET-BASED (failing files ⊆ TD-V16-TEST-ISOLATION — NEVER a fixed pass-count); supertests in BOTH auth modes
    - 83-RESEARCH.md §"BRANDUI-06 Server Vertical Slice" (route shapes to assert)
  </read_first>
  <files>packages/server/tests/routes.branding.spec.ts</files>
  <action>
    Add dark-logo supertests mirroring the existing primary-logo tests, in BOTH auth modes (reuse the existing both-mode harness/describe structure — do NOT invent a new pattern):
    - POST /api/branding/logo with a valid PNG/SVG fixture + `variant=dark` field, authenticated with branding:manage → 200 and response has `logoDarkUrl`.
    - GET /api/branding then reflects `logoDarkUrl` non-null.
    - GET /api/branding/logo?variant=dark → 200 with the stored Content-Type; GET ...?variant=dark on a fresh DB (no dark logo) → 404.
    - POST with `variant=dark` WITHOUT branding:manage → 403 (reuse the existing 403 assertion helper).
    - A malicious SVG (with <script>) uploaded as variant=dark is sanitized/rejected exactly like the primary path (reuse the existing SVG-sanitize assertion — confirms validation reuse).
    Keep all new tests inside the existing file so the set-based gate counts them; do not assert a global fixed pass-count.
  </action>
  <verify>
    <automated>cd packages/server && grep -q "variant" tests/routes.branding.spec.ts && grep -q "logoDarkUrl" tests/routes.branding.spec.ts && npx tsc --noEmit && npx vitest run tests/routes.branding.spec.ts && echo OK</automated>
  </verify>
  <done>Dark-logo upload/serve/GET/403/SVG-sanitize covered in both auth modes; routes.branding.spec.ts green; the broader server set-based gate has no NEW failing files outside TD-V16-TEST-ISOLATION.</done>
  <acceptance_criteria>
    - `tests/routes.branding.spec.ts` contains "variant" and "logoDarkUrl" and asserts: dark upload 200, GET reflects logoDarkUrl, ?variant=dark serve 200, absent→404, no-permission→403, malicious SVG handled
    - tests run in BOTH password and oidc auth modes (reuse existing harness)
    - `cd packages/server && npx vitest run tests/routes.branding.spec.ts` exits 0
    - `cd packages/server && npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Client — brandStore.logoDarkUrl + Sidebar theme selection + FOUC favicon awareness</name>
  <read_first>
    - packages/web/src/store/brandStore.ts (BrandState @9-19, bootstrap reading data.logoUrl @103-138, update() @147-167, what it writes to localStorage "kbi-brand-tokens")
    - packages/web/src/components/Sidebar.tsx lines 16,42,51-54 (DefaultLogo import, logoUrl read, <img>/<DefaultLogo> render) + useThemeStore import pattern
    - packages/web/index.html lines 16-52 (FOUC IIFE — brand.logoUrl used for favicon <link rel="icon">; isDark computed @24)
    - 83-RESEARCH.md §"BRANDUI-06 Server Vertical Slice > brandStore/Sidebar/FOUC" (effectiveLogoUrl selection; FOUC favicon dark-aware)
  </read_first>
  <files>packages/web/src/store/brandStore.ts, packages/web/src/components/Sidebar.tsx, packages/web/index.html</files>
  <action>
    brandStore.ts:
    - Add `logoDarkUrl: string | null;` to BrandState (next to logoUrl). Initialize null.
    - In `bootstrap()`, set `logoDarkUrl: data.logoDarkUrl ?? null` alongside logoUrl when the fetch succeeds.
    - In `update(config, logoUrl)`: leave the signature as-is (it is called widely). Add an OPTIONAL trailing param `logoDarkUrl?: string | null` defaulted to the current state value so existing callers are unaffected, OR have BrandingSettingsPage set logoDarkUrl via a small dedicated setter. Choose the minimal-churn option; if adding the optional param, update the 83-03 handleSave call is NOT required (it defaults). Ensure logoDarkUrl is included in whatever object is persisted to localStorage so the FOUC script can read it.

    Sidebar.tsx:
    - `const logoDarkUrl = useBrandStore((s) => s.logoDarkUrl);` and `const theme = useThemeStore((s) => s.theme);` (import useThemeStore).
    - `const effectiveLogoUrl = (theme === "dark" && logoDarkUrl) ? logoDarkUrl : logoUrl;`
    - Render `effectiveLogoUrl ? <img src={effectiveLogoUrl} .../> : <DefaultLogo .../>` (replace the current logoUrl check).

    index.html FOUC IIFE:
    - After computing `isDark` (@24), compute `var effectiveLogoUrl = (isDark && brand.logoDarkUrl) ? brand.logoDarkUrl : brand.logoUrl;` and use `effectiveLogoUrl` for the favicon `<link rel="icon">` (replace `brand.logoUrl` in that block). Keep it defensive (the field may be absent on old caches).
  </action>
  <verify>
    <automated>cd packages/web && grep -q "logoDarkUrl" src/store/brandStore.ts && grep -q "effectiveLogoUrl" src/components/Sidebar.tsx && grep -q "logoDarkUrl" index.html && npx tsc --noEmit && echo OK</automated>
  </verify>
  <done>brandStore holds + persists logoDarkUrl; Sidebar selects dark variant in dark mode; FOUC favicon is dark-aware; web tsc clean.</done>
  <acceptance_criteria>
    - `src/store/brandStore.ts` BrandState contains "logoDarkUrl" and bootstrap reads `data.logoDarkUrl`; logoDarkUrl is included in the localStorage-persisted object
    - `src/components/Sidebar.tsx` contains "effectiveLogoUrl" and selects logoDarkUrl when `theme === "dark"`
    - `index.html` FOUC script references "logoDarkUrl" for the favicon
    - `cd packages/web && npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Settings UI — dual logo slots (primary + dark) + app-name field + Reset clears both</name>
  <read_first>
    - packages/web/src/components/settings/BrandingSettingsPage.tsx (id="brand-logo" placeholder; draft state; handleSave already uploads a primary draftLogoFile @83-03; handleReset)
    - 83-CONTEXT.md §"Logo + optional dark override" (both slots always visible; each previewed on its own mode background; app-name field default "Kinetica BI"; Reset clears both)
    - 83-RESEARCH.md §"API Client Functions" (uploadBrandLogo(file, variant)) + Phase-81 validation reuse note
    - packages/web/src/components/charts/DataFilterConfigPanel.tsx (ds-field / ds-field-label / file input conventions)
  </read_first>
  <files>packages/web/src/components/settings/LogoUploader.tsx, packages/web/src/components/settings/BrandingSettingsPage.tsx, packages/web/src/components/settings/BrandingSettingsPage.css, packages/web/src/components/settings/__tests__/LogoSection.spec.tsx</files>
  <behavior>
    - The Logo section renders two LogoUploader slots labeled primary + dark, plus an app-name text input.
    - Choosing a file in the dark slot stores it as the dark draft file and marks dirty.
    - handleSave uploads the dark file via uploadBrandLogo(file, "dark") when one was chosen.
    - Reset clears both chosen files + the app-name override.
  </behavior>
  <action>
    `LogoUploader.tsx`: props `{ label: string; previewUrl: string | null; previewMode: "dark" | "light"; onFileChosen: (f: File | null) => void }`. A file `<input type="file" accept="image/*">` + a preview `<img>` shown on a swatch styled for `previewMode` (the slot's own mode background, via a CSS class — tokens only, no hex) so contrast issues are obvious. (Validation is server-side per Phase 81 — the client just previews via URL.createObjectURL for a chosen file, else previewUrl.)

    BrandingSettingsPage `id="brand-logo"` section:
    - Add state `draftLogoFile` (primary — may already exist from 83-03) and `draftDarkLogoFile`.
    - App-name `<input className="ds-input">` bound to `draft.appName ?? "Kinetica BI"` → handleDraftChange({ appName }).
    - `<LogoUploader label="Primary logo" previewUrl={primary preview} previewMode="light" onFileChosen={setDraftLogoFile} />` and `<LogoUploader label="Dark-mode override (optional)" previewUrl={dark preview} previewMode="dark" onFileChosen={setDraftDarkLogoFile} />` (each previewed on its own mode background per CONTEXT). Choosing a file calls setIsDirty(true).
    - handleSave (extend 83-03): when `draftDarkLogoFile` exists, `await uploadBrandLogo(draftDarkLogoFile, "dark")` (capture logoDarkUrl); thread the returned logoDarkUrl into brandStore (via the update() optional param or setter from Task 3). Clear both draft files after save.
    - handleReset: clear `draftLogoFile` + `draftDarkLogoFile` and reset appName to default (clearing the override) alongside the token defaults.
  </action>
  <verify>
    <automated>cd packages/web && grep -q "onFileChosen" src/components/settings/LogoUploader.tsx && grep -q "uploadBrandLogo" src/components/settings/BrandingSettingsPage.tsx && grep -qE "\"dark\"|'dark'" src/components/settings/BrandingSettingsPage.tsx && grep -q "draftDarkLogoFile" src/components/settings/BrandingSettingsPage.tsx && grep -q "appName" src/components/settings/BrandingSettingsPage.tsx && npx vitest run src/components/settings/__tests__/LogoSection.spec.tsx && npx vitest run src/styles/theme-guard.spec.ts && npx tsc --noEmit && npx vitest run && echo OK</automated>
  </verify>
  <done>Two always-visible logo slots (primary + dark) each previewed on its mode background + app-name field; Save uploads chosen variant(s); Reset clears both; web vitest 100%; theme-guard green; tsc clean.</done>
  <acceptance_criteria>
    - `src/components/settings/LogoUploader.tsx` renders a file input + preview and is mode-aware (previewMode prop)
    - `src/components/settings/BrandingSettingsPage.tsx` contains "draftDarkLogoFile", `uploadBrandLogo(..., "dark")`, an app-name input bound to draft.appName, and Reset clears both files
    - `LogoSection.spec.tsx` asserts two slots render and choosing a dark file marks dirty + drives a dark upload on save
    - `cd packages/web && npx vitest run && npx tsc --noEmit && npx vitest run src/styles/theme-guard.spec.ts` all exit 0 (100%)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd packages/server && npx tsc --noEmit` clean; `npx vitest run tests/routes.branding.spec.ts` green (both auth modes); broader server set-based gate has no NEW failing files outside TD-V16-TEST-ISOLATION.
- `cd packages/web && npx tsc --noEmit` clean; `npx vitest run` 100% (incl. LogoSection + theme-guard).
- Dark logo: upload (variant=dark) → GET logoDarkUrl → Sidebar shows it in dark mode; primary fallback; DefaultLogo fallback.
</verification>

<success_criteria>
- BRANDUI-06: optional dark-mode logo override uploadable; client shows dark variant in dark mode when present, else primary, else bundled default; reuses Phase-81 validation; both-auth-mode supertests; dual-slot UI with per-mode preview; Reset clears both.
</success_criteria>

<output>
After completion, create `.planning/phases/83-branding-admin-ui/83-04-SUMMARY.md`
</output>
