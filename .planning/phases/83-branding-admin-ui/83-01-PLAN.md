---
phase: 83-branding-admin-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/package.json
  - packages/web/src/api/client.ts
  - packages/web/src/store/brandStore.ts
  - packages/web/src/styles/global.css
  - packages/web/src/components/settings/BrandingSettingsPage.tsx
  - packages/web/src/components/settings/BrandingSettingsPage.css
  - packages/web/src/components/settings/brandPageGuard.ts
  - packages/web/src/components/Sidebar.tsx
  - packages/web/src/App.tsx
  - packages/web/src/styles/theme-guard.spec.ts
  - .planning/ROADMAP.md
autonomous: true
requirements: [BRANDUI-05]
must_haves:
  truths:
    - "A Branding nav entry appears under Settings ONLY for users with branding:manage (hidden otherwise)."
    - "Clicking Branding renders BrandingSettingsPage with Logo/Colors/Fonts/Feel/CustomCSS section placeholders and a header carrying Save (disabled until dirty) + Reset."
    - "applyBrandTokens maps every new payload field (display font, density, radius, glow, type-scale, motion, accent-text) to its real :root token."
    - "global.css body aurora-gradient alpha is gated by --glow-opacity so glowEnabled:false hides the wash."
    - "Navigating away from the page with unsaved changes prompts to confirm and reverts live :root to the saved brand."
  artifacts:
    - path: "packages/web/src/store/brandStore.ts"
      provides: "Extended BrandConfigPayload + applyBrandTokens mapping + exported applyBrandTokens + revertToSaved() action"
      contains: "export function applyBrandTokens"
    - path: "packages/web/src/components/settings/BrandingSettingsPage.tsx"
      provides: "Page scaffold with 5 section placeholders, dirty-tracking, Save/Reset header"
      min_lines: 60
    - path: "packages/web/src/components/settings/brandPageGuard.ts"
      provides: "Module-level dirty/revert refs read by App.tsx onSelect"
      contains: "brandPageIsDirty"
    - path: "packages/web/src/styles/global.css"
      provides: "--glow-opacity token interpolated into body radial-gradient alpha"
      contains: "--glow-opacity"
    - path: "packages/web/src/api/client.ts"
      provides: "updateBrandConfig() PUT + uploadBrandLogo(variant) POST + extended BrandConfigPayload"
      exports: ["updateBrandConfig", "uploadBrandLogo"]
  key_links:
    - from: "packages/web/src/App.tsx"
      to: "brandPageGuard.brandPageIsDirty"
      via: "onSelect intercept before setPage"
      pattern: "brandPageIsDirty"
    - from: "packages/web/src/components/Sidebar.tsx"
      to: "PERMISSIONS.BRANDING_MANAGE"
      via: "nav array entry with permission filter"
      pattern: "BRANDING_MANAGE"
    - from: "packages/web/src/components/settings/BrandingSettingsPage.tsx"
      to: "applyBrandTokens"
      via: "live preview on draft change + revertToSaved on leave"
      pattern: "applyBrandTokens|revertToSaved"
---

<objective>
Build the Phase 83 FOUNDATION: extend the brand payload + token-apply pipeline, add the missing `--glow-opacity` token, scaffold the permission-gated `BrandingSettingsPage`, wire the state-based leave-unsaved nav guard, and add the API client mutation functions. Every other Phase 83 plan builds on this.

Purpose: Without the extended `BrandConfigPayload`, the exported `applyBrandTokens` live-preview path, the `--glow-opacity` token, and the page scaffold + nav guard, no section (colors/fonts/feel/css/dark-logo) can be implemented.
Output: A reachable (gated) Branding page that live-previews + reverts brand changes; an extended brand store; new client mutation fns; new deps installed.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-branding-admin-ui/83-RESEARCH.md
@.planning/phases/83-branding-admin-ui/83-CONTEXT.md

<interfaces>
<!-- Current brandStore (packages/web/src/store/brandStore.ts) — applyBrandTokens is module-PRIVATE today (line 29, no export). -->
applyBrandTokens(config: BrandConfigPayload | null, theme: Theme): void   // PRIVATE — must be exported
BRAND_STORAGE_KEY = "kbi-brand-tokens"
BrandState.update(config: BrandConfigPayload, logoUrl: string | null): void  // calls applyBrandTokens + notifyOtherTabs + writes localStorage
BrandState.bootstrap(): Promise<void>
BrandState.config: BrandConfigPayload | null   // last-SAVED config
// applyBrandTokens currently ends at: set("--font-body", config.fontFamily)  (line 49)

<!-- Current BrandConfigPayload (packages/web/src/api/client.ts lines 132-143) -->
// 8 color pairs (primaryColor/lightPrimaryColor ... dangerColor/lightDangerColor) + fontFamily + fontUrl + appName + customCss

<!-- App.tsx (packages/web/src/App.tsx) -->
type Page = "dashboards" | "datasets" | "settings" | "users" | "roles" | "profile"  // line 24
const [page, setPage] = useState<Page>("dashboards")                                 // line 38
<Sidebar onSelect={(key) => { setPage(key as Page); setDashboardViewMode("list"); }} />  // line 255
{page === "settings" && (<div className="muted">Section coming soon.</div>)}         // lines 272-274 — REPLACE

<!-- Sidebar.tsx -->
const nav: NavItem[] = [ ... { label, key, icon, permission } ]                       // line 25
const visibleNav = nav.filter((item) => !item.permission || hasPermission(item.permission))  // line 44

<!-- permissions.ts -->
PERMISSIONS.BRANDING_MANAGE === "branding:manage"                                     // already exists

<!-- theme-guard.spec.ts -->
const ALLOWLIST: ReadonlyArray<string> = [ ... ]   // line 37 — relative paths under src/; HEX_RE matches 3/6-digit hex
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps + extend BrandConfigPayload + add client mutation fns</name>
  <read_first>
    - packages/web/src/api/client.ts lines 1-160 (BrandConfigPayload @132-143, BrandingResponse @144, fetchBranding @153, apiFetch @46, throwForStatus @88, API_BASE @6)
    - 83-RESEARCH.md §"BrandConfigPayload Extension Spec" (exact new field names + nullable types) and §"API Client Functions (New)" (exact updateBrandConfig + uploadBrandLogo bodies + server request shapes)
    - 83-RESEARCH.md §"Color Section" Pitfall 4 (accentTextColor field gap) and §"Standard Stack > New Web Dependencies"
  </read_first>
  <files>packages/web/package.json, packages/web/src/api/client.ts</files>
  <action>
    From `packages/web`, run `npm install react-colorful colord @codemirror/lang-css`. (react-colorful + colord are new; @codemirror/lang-css is already in package-lock at 6.3.1 as a transitive — this surfaces it in package.json. Do NOT add any other deps.)

    Extend `BrandConfigPayload` (client.ts ~line 132) with these EXACT new fields (all `?: ... | null`):
    - `displayFontFamily?: string | null;` `displayFontUrl?: string | null;`  (BRANDUI-03, maps to --font-display)
    - `accentTextColor?: string | null;` `lightAccentTextColor?: string | null;`  (maps to --accent-text — closes Pitfall 4; needed for WCAG accent-text check in 83-02)
    - `densityPreset?: "compact" | "comfortable" | "spacious" | null;`
    - `radiusPreset?: "sharp" | "default" | "round" | null;`
    - `glowEnabled?: boolean | null;`
    - `typeScaleBase?: number | null;`
    - `motionSpeed?: "none" | "reduced" | "default" | "fast" | null;`

    Extend `BrandingResponse` (~line 144) with `logoDarkUrl: string | null;` (BRANDUI-06 — type only; server lands in 83-04). Place it next to `logoUrl`.

    Add the two mutation fns (copy bodies verbatim from RESEARCH §"API Client Functions"):
    - `export type BrandingPutResponse = { config: BrandConfigPayload; updatedAt: string };`
    - `export const updateBrandConfig = async (config: BrandConfigPayload): Promise<BrandingPutResponse>` — `apiFetch` PUT `${API_BASE}/api/branding`, JSON body `{ config }`, `throwForStatus` on !ok.
    - `export type LogoUploadResponse = { logoUrl?: string; logoDarkUrl?: string };`
    - `export const uploadBrandLogo = async (file: File, variant: "primary" | "dark" = "primary"): Promise<LogoUploadResponse>` — FormData field `logo`, append `variant` when "dark", `apiFetch` POST `${API_BASE}/api/branding/logo` (no Content-Type header — let the browser set the multipart boundary), `throwForStatus` on !ok.
  </action>
  <verify>
    <automated>cd packages/web && grep -q '"react-colorful"' package.json && grep -q '"colord"' package.json && grep -q '"@codemirror/lang-css"' package.json && grep -q "accentTextColor" src/api/client.ts && grep -q "densityPreset" src/api/client.ts && grep -q "export const updateBrandConfig" src/api/client.ts && grep -q "export const uploadBrandLogo" src/api/client.ts && grep -q "logoDarkUrl" src/api/client.ts && echo OK</automated>
  </verify>
  <done>package.json contains react-colorful, colord, @codemirror/lang-css; BrandConfigPayload has all 9 new fields incl. accentTextColor; BrandingResponse has logoDarkUrl; updateBrandConfig + uploadBrandLogo exported.</done>
  <acceptance_criteria>
    - `grep -c "react-colorful\|colord\|@codemirror/lang-css" packages/web/package.json` ≥ 3
    - `src/api/client.ts` contains "accentTextColor", "lightAccentTextColor", "displayFontFamily", "densityPreset", "radiusPreset", "glowEnabled", "typeScaleBase", "motionSpeed"
    - `src/api/client.ts` contains "export const updateBrandConfig" and "export const uploadBrandLogo" and 'variant === "dark"'
    - `src/api/client.ts` BrandingResponse contains "logoDarkUrl"
    - `cd packages/web && npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend brandStore (token mapping + export applyBrandTokens + revertToSaved) and add --glow-opacity to global.css</name>
  <read_first>
    - packages/web/src/store/brandStore.ts FULL (applyBrandTokens body @29-49, BrandState @9, update() @147-167, bootstrap() @103, module-level theme subscription @186)
    - 83-RESEARCH.md §"applyBrandTokens Extension" (exact set() calls + the 4 helper fns applyDensityPreset/applyRadiusPreset/applyMotionPreset/applyTypeScalePreset — copy verbatim, they have the exact token names + preset values) and §"BrandConfigPayload Extension Spec" FLAGS
    - 83-RESEARCH.md Open Question 4 (applyBrandTokens visibility) + Open Question 2 (--glow-opacity)
    - packages/web/src/styles/global.css lines 161-190 (body background radial-gradient stops dark + light)
  </read_first>
  <files>packages/web/src/store/brandStore.ts, packages/web/src/styles/global.css</files>
  <action>
    In brandStore.ts:
    1. Change `function applyBrandTokens(...)` (line 29) to `export function applyBrandTokens(...)` — it must be importable by BrandingSettingsPage for live preview.
    2. After the existing `set("--font-body", config.fontFamily)` (line 49), add the new-field mappings EXACTLY as in RESEARCH §"applyBrandTokens Extension": `set("--font-display", config.displayFontFamily)`; `set("--accent-text", config.accentTextColor)` for dark + `set("--accent-text", config.lightAccentTextColor)` gated on theme === "light" (mirror how existing light* color tokens are theme-gated in the current body — match the existing dark/light branching in applyBrandTokens); then `applyDensityPreset(root, config.densityPreset ?? null)`, `applyRadiusPreset(...)`, the glow block (`if (config.glowEnabled === false) root.style.setProperty("--glow-opacity","0"); else root.style.removeProperty("--glow-opacity");`), `applyTypeScalePreset(...)`, `applyMotionPreset(...)`.
    3. Add the 4 pure helper functions from RESEARCH §"applyBrandTokens Extension" (applyDensityPreset with the --space-1..--space-10 TOKENS + comfortable/spacious arrays; applyRadiusPreset with --radius/--radius-sm/--radius-md/--radius-lg sharp/round maps; applyMotionPreset with --duration-fast/base/slow none/reduced/fast maps; applyTypeScalePreset setting --text-base) at module level (no imports).
    4. Add a `revertToSaved: () => void;` to `BrandState` (line 9) and implement it in the store: `revertToSaved: () => applyBrandTokens(get().config, useThemeStore.getState().theme)`. (Use the store's `get` — adjust the `create<BrandState>((set) => ...)` to `(set, get) =>` if needed.)

    In global.css:
    - Add `--glow-opacity: 1;` to the `:root` token block (near the other tokens, with a comment `/* ambient aurora glow on/off — brandable (Phase 83) */`).
    - Rewrite the dark body background radial-gradient alphas (lines 167-169) to interpolate the token: `rgba(127, 64, 237, calc(0.20 * var(--glow-opacity, 1)))`, `rgba(56, 189, 248, calc(0.12 * var(--glow-opacity, 1)))`, `rgba(217, 70, 239, calc(0.10 * var(--glow-opacity, 1)))`.
    - Rewrite the light body stops (lines 186-187) the same way: `rgba(127, 64, 237, calc(0.05 * var(--glow-opacity, 1)))`, `rgba(2, 132, 199, calc(0.03 * var(--glow-opacity, 1)))`.
    - Keep `var(--bg)` base + `background-attachment: fixed` unchanged. Use ONLY var()/calc()/existing rgba numbers — do NOT introduce any NEW hex literal (theme-guard).
  </action>
  <verify>
    <automated>cd packages/web && grep -q "export function applyBrandTokens" src/store/brandStore.ts && grep -q "revertToSaved" src/store/brandStore.ts && grep -q "applyDensityPreset" src/store/brandStore.ts && grep -q "applyRadiusPreset" src/store/brandStore.ts && grep -q "applyMotionPreset" src/store/brandStore.ts && grep -q -- "--glow-opacity" src/styles/global.css && grep -q "calc(0.20 \* var(--glow-opacity" src/styles/global.css && npx tsc --noEmit && echo OK</automated>
  </verify>
  <done>applyBrandTokens is exported and maps all new fields; revertToSaved action added; 4 helpers present; --glow-opacity defined in :root and interpolated into both dark+light body gradients; tsc clean.</done>
  <acceptance_criteria>
    - `src/store/brandStore.ts` contains "export function applyBrandTokens", "revertToSaved", "applyDensityPreset", "applyRadiusPreset", "applyMotionPreset", "applyTypeScalePreset", '--font-display', '--accent-text'
    - `src/styles/global.css` contains "--glow-opacity" in a :root block AND "var(--glow-opacity" inside the body radial-gradient (both dark and light)
    - No NEW raw hex added to global.css body block (the existing rgba numbers stay; theme-guard green)
    - `cd packages/web && npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Scaffold BrandingSettingsPage + nav entry + App.tsx leave-guard + theme-guard ALLOWLIST</name>
  <read_first>
    - packages/web/src/components/RolesPage.tsx lines 40-145 (isDirty/saving useState, window.confirm "Discard unsaved changes?", handleSave shape, Save `disabled={!isDirty || saving}`) and RolesPage.css for header/section class conventions
    - packages/web/src/components/Sidebar.tsx lines 25-44 (nav array NavItem shape + visibleNav permission filter)
    - packages/web/src/App.tsx lines 24,38,255,270-277 (Page type, page state, onSelect, settings render block to REPLACE)
    - packages/web/src/styles/theme-guard.spec.ts lines 22-45 (ALLOWLIST array + justification-comment convention + HEX_RE)
    - 83-RESEARCH.md §"Architecture Patterns > App Routing" + §"Page Scaffold" + Pitfall 2 (leave-revert race → intercept at onSelect) + Open Questions 3 & 5
  </read_first>
  <files>packages/web/src/components/settings/brandPageGuard.ts, packages/web/src/components/settings/BrandingSettingsPage.tsx, packages/web/src/components/settings/BrandingSettingsPage.css, packages/web/src/components/Sidebar.tsx, packages/web/src/App.tsx, packages/web/src/styles/theme-guard.spec.ts</files>
  <action>
    1. Create `brandPageGuard.ts` — a tiny module exposing mutable refs read by App.tsx (avoids prop-drilling through Sidebar):
       ```ts
       export const brandPageGuard = { isDirty: false, revert: null as null | (() => void) };
       ```
       (Comment: "State-based router has no useBlocker — App.tsx reads this before leaving the settings page.")

    2. Create `BrandingSettingsPage.tsx` scaffold mirroring RolesPage dirty-tracking:
       - state: `draft` (BrandConfigPayload, initialized from `useBrandStore.getState().config ?? {}`), `isDirty`, `saving`.
       - a `handleDraftChange(updates: Partial<BrandConfigPayload>)` helper: `const next = { ...draft, ...updates }; setDraft(next); setIsDirty(true); applyBrandTokens(next, useThemeStore.getState().theme);` (live preview — RESEARCH §"applyBrandTokens call from draft edit handler").
       - `handleSave` skeleton: `setSaving(true)` → call `updateBrandConfig(draft)` → on success `useBrandStore.getState().update(resp.config, useBrandStore.getState().logoUrl)` (reflect + notify tabs) → `setIsDirty(false)` → `setSaving(false)`. (Logo POST + stripped-CSS notice land in later plans.)
       - `handleReset` skeleton: stage Aurora defaults live (`applyBrandTokens({}, theme)` removes overrides → Aurora defaults) AND `setDraft({})` AND `setIsDirty(true)` (Pitfall 1 — Reset must keep isDirty true so it can be saved).
       - useEffect syncing `brandPageGuard.isDirty = isDirty` and `brandPageGuard.revert = () => { useBrandStore.getState().revertToSaved(); }`; cleanup sets `brandPageGuard.isDirty = false; brandPageGuard.revert = null`.
       - Header: `<h2>Branding</h2>` + Reset button + Save button `disabled={!isDirty || saving}` ({saving ? "Saving…" : "Save"}).
       - Five `<section>` placeholders with stable headings/ids so later plans target them: `id="brand-logo"` (Logo & app name), `id="brand-colors"` (Colors), `id="brand-fonts"` (Fonts), `id="brand-feel"` (Feel), `id="brand-css"` (Custom CSS). Each with an `<h3>` and a `{/* 83-0X fills this */}` comment.
       - Wrap the page root in `<div className="branding-admin" id="branding-admin-exempt">` (the id reserves the custom-CSS exemption hook for 83-03).
       - Use ONLY theme tokens / existing ds-* classes in the .tsx and .css — NO hex literals here (defaults live in the store/global.css). This keeps the scaffold off the ALLOWLIST until 83-02 adds the pickers.

    3. Create `BrandingSettingsPage.css` with header + section layout classes (`.branding-admin`, `.branding-header`, `.branding-section`) using ONLY var(--token) values. No hex.

    4. Sidebar.tsx: add `{ label: "Branding", key: "branding", icon: <pick an existing imported FontAwesome brand/paint icon, e.g. faPalette — import it>, permission: PERMISSIONS.BRANDING_MANAGE }` to the `nav` array. The existing `visibleNav` filter handles hide-don't-disable. Add `faPalette` (or similar) to the existing `@fortawesome/free-solid-svg-icons` import.

    5. App.tsx:
       - Add `"branding"` to the `Page` union (line 24) AND to the persisted-page validation allowlist (lines 187-192).
       - Replace the Sidebar `onSelect` (line 255) with a guarded handler:
         ```tsx
         onSelect={(key) => {
           if (page === "branding" && brandPageGuard.isDirty) {
             if (!window.confirm("Discard unsaved branding changes?")) return;
             brandPageGuard.revert?.();
           }
           setPage(key as Page);
           setDashboardViewMode("list");
         }}
         ```
       - Add a render branch `{page === "branding" && <BrandingSettingsPage />}` (import it). Leave the existing `page === "settings"` block as-is.

    6. theme-guard.spec.ts: append to `ALLOWLIST` (line 37) — `"components/settings/BrandingSettingsPage.tsx",` and `"components/settings/BrandColorPicker.tsx",` (the latter is created in 83-02; pre-adding it here in the foundation commit keeps the rule "same commit as introduction" satisfiable AND avoids a future CI break — see STATE locked theme-guard rule) each with a one-line justification comment: `// brand color tooling: authors literal hex default values (react-colorful)`.
       NOTE: if the guard fails because an ALLOWLISTED file "contains no hex literal" (guard line ~105), put the BrandColorPicker.tsx ALLOWLIST entry in 83-02 instead and leave only BrandingSettingsPage.tsx here — but ONLY add BrandingSettingsPage.tsx to the allowlist if the scaffold actually contains a hex; if it doesn't, defer BOTH entries to the plan that introduces the hex. Verify with: `grep -nE '#[0-9a-fA-F]{3,6}\b' src/components/settings/BrandingSettingsPage.tsx`. The safe path: scaffold contains NO hex → add NO allowlist entry here; 83-02 adds both when it introduces hex defaults.
  </action>
  <verify>
    <automated>cd packages/web && grep -q '"branding"' src/App.tsx && grep -q "brandPageGuard" src/App.tsx && grep -q "Discard unsaved branding changes" src/App.tsx && grep -q "BrandingSettingsPage" src/App.tsx && grep -q "BRANDING_MANAGE" src/components/Sidebar.tsx && grep -q "brand-colors" src/components/settings/BrandingSettingsPage.tsx && grep -q "revertToSaved\|brandPageGuard" src/components/settings/BrandingSettingsPage.tsx && npx tsc --noEmit && npx vitest run src/styles/theme-guard.spec.ts && echo OK</automated>
  </verify>
  <done>Branding nav entry gated on branding:manage; App.tsx renders BrandingSettingsPage at page==="branding" with a dirty-leave confirm+revert intercept; page scaffold has 5 section placeholders + Save(disabled until dirty)/Reset header; theme-guard green; tsc clean.</done>
  <acceptance_criteria>
    - `src/App.tsx` Page union contains "branding"; onSelect contains "brandPageGuard.isDirty" and "brandPageGuard.revert"; a `page === "branding"` render branch exists
    - `src/components/Sidebar.tsx` nav array contains a "Branding" entry with `permission: PERMISSIONS.BRANDING_MANAGE`
    - `src/components/settings/BrandingSettingsPage.tsx` contains "brand-logo", "brand-colors", "brand-fonts", "brand-feel", "brand-css", "applyBrandTokens", a Save button `disabled={!isDirty || saving}`, and a Reset handler that sets isDirty true
    - `src/components/settings/brandPageGuard.ts` exports `brandPageGuard` with `isDirty` + `revert`
    - `cd packages/web && npx tsc --noEmit` exits 0
    - `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Update ROADMAP Phase 83 plan list + run web gates</name>
  <read_first>
    - .planning/ROADMAP.md lines 147-153 (Phase 83 "Plans: TBD" + the 4 stub bullets)
    - MEMORY: "Parallel executors clobber ROADMAP" — ALL Phase-83 ROADMAP edits happen ONLY in this plan (83-01); no other 83 plan touches ROADMAP.
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
    Update the Phase 83 section: change `**Plans**: TBD` to `**Plans**: 4 plans (Wave 1: 83-01; Wave 2: 83-02; Wave 3: 83-03; Wave 4: 83-04 — page-building plans sequenced to share BrandingSettingsPage.tsx without clobbering)` and rewrite the `Plans:` checklist to:
    - `[ ] 83-01-PLAN.md — Foundation: extend BrandConfigPayload + applyBrandTokens (display-font, feel-levers, accent-text) + export applyBrandTokens + revertToSaved; --glow-opacity in global.css; page scaffold + branding nav (gated) + App.tsx leave-guard; updateBrandConfig/uploadBrandLogo client fns [BRANDUI-05]`
    - `[ ] 83-02-PLAN.md — Colors (18 react-colorful pickers, dark|light) + WCAG warn-only badges (colord) + Fonts (body+display curated selects) [BRANDUI-02, BRANDUI-03, SECA-V116-02]`
    - `[ ] 83-03-PLAN.md — Feel levers (density/radius/glow/type-scale/motion) + compact preview card + full Save/Reset + Custom CSS editor (CodeMirror lang-css, debounced draft inject, stripped-declarations notice, page exempt) [BRANDUI-04, BRANDUI-05, CSS-V116-01]`
    - `[ ] 83-04-PLAN.md — BRANDUI-06 dark-logo: server logo_dark_* columns + variant upload/serve + logoDarkUrl in GET (+ both-auth supertests); client brandStore.logoDarkUrl + Sidebar/FOUC theme selection + dual logo-slot UI [BRANDUI-06]`
  </action>
  <verify>
    <automated>grep -q "83-02-PLAN.md — Colors" .planning/ROADMAP.md && grep -q "83-04-PLAN.md — BRANDUI-06" .planning/ROADMAP.md && cd packages/web && npx tsc --noEmit && npx vitest run && echo OK</automated>
  </verify>
  <done>ROADMAP Phase 83 lists the 4 refined plans with requirement tags; web tsc clean; web vitest 100%.</done>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phase 83 Plans section lists 83-01..83-04 with the refined objectives + requirement IDs
    - `cd packages/web && npx tsc --noEmit` exits 0
    - `cd packages/web && npx vitest run` exits 0 (100%)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd packages/web && npx tsc --noEmit` clean.
- `cd packages/web && npx vitest run` 100% (incl. theme-guard.spec.ts green).
- Branding nav entry visible only with branding:manage; hidden otherwise (hide-don't-disable filter).
- Editing a draft live-applies via exported applyBrandTokens; leaving with isDirty prompts + reverts via revertToSaved.
- `--glow-opacity` exists in :root and gates both dark + light body aurora alphas.
</verification>

<success_criteria>
- BRANDUI-05 (foundation slice): live-preview pipeline + Save/Reset skeleton + dirty-tracking + leave-revert all wired; full Save/Reset behavior completed in 83-03.
- BrandConfigPayload + applyBrandTokens cover every new token; deps installed; client mutation fns exported; ROADMAP updated.
</success_criteria>

<output>
After completion, create `.planning/phases/83-branding-admin-ui/83-01-SUMMARY.md`
</output>
