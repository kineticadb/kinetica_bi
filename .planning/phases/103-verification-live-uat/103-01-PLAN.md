---
phase: 103-verification-live-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/103-verification-live-uat/103-GATES.md
autonomous: true
requirements: [VERIFY-V119-01]
must_haves:
  truths:
    - "Web automated gates are green: web vitest reports 0 failed (Test Files/Tests passed), web tsc --noEmit exits clean, theme-guard.spec.ts passes."
    - "Server automated gates hold to the SET-BASED bar: server tsc --noEmit clean; every failing server vitest file is ⊆ TD-V16-TEST-ISOLATION (OIDC-mock / db.smoke / dev-.env-leak / wms-credential set), and the v1.19 server touch (custom_metrics routes + /api/auth/me maxBarGroupBySeriesCap) PASSES IN ISOLATION."
    - "The sole-materialize-trigger invariant holds across all five features: no renderer/helper in packages/web/src/components/charts/ AND no v1.19 lib helper (customMetricSql, yAxisScale, customWhere, barGroupedSeries) calls materializeFilter/dropFilterView — only useCombinationOrchestrator (materialize) + App/DashboardsPage (drop) are authorized call sites."
    - "A GATES record captures the exact command output + the pass/fail verdict per gate, ready to feed the VERIFICATION.md per-SC attestation in Plan 02."
  artifacts:
    - path: ".planning/phases/103-verification-live-uat/103-GATES.md"
      provides: "SC1 automated-gate evidence: web tsc/vitest/theme-guard verdicts, server tsc + set-based vitest classification, sole-materialize-trigger grep result across all 5 features"
      contains: "sole-materialize-trigger"
  key_links:
    - from: "web vitest run"
      to: "0-failed assertion (Test Files / Tests passed)"
      via: "assert on the passed line, NOT a fixed count; ignore the InfoCardRenderer 401 unhandled-rejection log"
      pattern: "Tests.*passed|Test Files.*passed"
    - from: "server vitest run"
      to: "failing files ⊆ TD-V16-TEST-ISOLATION + v1.19 specs pass in isolation"
      via: "classify each failing file; re-run custom_metrics + /api/auth/me specs alone"
      pattern: "TD-V16-TEST-ISOLATION"
    - from: "packages/web/src/components/charts/ + v1.19 lib helpers"
      to: "zero materializeFilter/dropFilterView CALL sites"
      via: "recursive grep, comment/import lines excluded"
      pattern: "materializeFilter|dropFilterView"
---

<objective>
Run and ATTEST the SC1 automated gates for v1.19 on BOTH stacks, and re-assert the sole-materialize-trigger invariant across all five features. This is the pre-walk green-gate that must pass before the blocking live operator walk-through in Plan 02.

This plan writes NO feature code. It runs the gate commands, classifies results against the repo's documented set-based bar, runs the invariant grep, and records everything in a 103-GATES.md evidence file that Plan 02 folds into the VERIFICATION.md per-SC attestation.

Purpose: Prove Success Criterion 1 (green automated gates on both stacks + invariant) before the walk.
Output: `.planning/phases/103-verification-live-uat/103-GATES.md` with per-gate verdict + the invariant-grep result.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-verification-live-uat/103-CONTEXT.md
@CLAUDE.md

<gate_facts>
Gate commands (from CLAUDE.md + STATE.md v1.19 Test Gates + 103-CONTEXT.md):
  WEB (run from packages/web):
    - npx tsc --noEmit          → must exit clean (no errors)
    - npx vitest run            → assert on "Test Files ... passed" / "Tests ... passed" with 0 FAILED. Do NOT assert a fixed pass-count.
    - npx vitest run src/styles/theme-guard.spec.ts → green (theme tokens only, no raw hex)
  SERVER (run from packages/server):
    - npx tsc --noEmit          → must exit clean
    - npx vitest run            → SET-BASED gate. NEVER a fixed pass-count. Every failing FILE must be ⊆ the documented TD-V16-TEST-ISOLATION set.

Known non-failing NOISE (do NOT treat as a failure):
  - Web: src/components/charts/InfoCardRenderer.spec.tsx logs a 401 "unhandled rejection" error to the console but the TESTS PASS — assert on the passed line with 0 failed, not on the absence of a logged error.

Documented TD-V16-TEST-ISOLATION / known-flaky server set (from STATE.md + 96-UAT.md Final Gate Result):
  - TD-V11-04 OIDC issuer-mock set: auth.oidc, auth.routes, boot.wipe, bootstrap, oidc.module (pre-existing since v1.3).
  - db.smoke: schema-snapshot drift (pre-existing; includes cb_config/track_config + v1.18 filter_scope + v1.19 custom_metrics DDL — the FILE was already failing pre-v1.19).
  - TD-V16-TEST-ISOLATION cross-mode contamination (PASS alone, fail in full run): e.g. routes.dashboard-layers-patch, routes.info-query.
  - routes.wms (2 credential-forwarding tests): pre-existing, untouched since v1.17.
  - dev-.env-leak: routes.filter-materialize / dynamic-view / filter-materialize-dv fail ONLY when packages/server/.env sets DEFAULT_VIEW_TTL_MINUTES=3 (tests expect the code default 5). Run vitest in a CLEAN env (temporarily set the dev .env aside, or unset DEFAULT_VIEW_TTL_MINUTES) so this environmental leak does not mask the real verdict.

v1.19 server touch that MUST pass in isolation (NOT in the flaky set):
  - custom_metrics CRUD routes (Phase 99) — dual-auth (password + oidc) supertests.
  - /api/auth/me maxBarGroupBySeriesCap field (Phase 102) — dual-auth.

Sole-materialize-trigger invariant (verified at plan time — this is the expected clean state):
  - The five features' renderers all live under packages/web/src/components/charts/ (CalendarRenderer, TimelineRenderer, NumericLineRenderer, the bar path inside WidgetRenderer) + the v1.19 pure lib helpers packages/web/src/lib/{customMetricSql,yAxisScale,customWhere,barGroupedSeries}.ts.
  - AUTHORIZED call sites (the ONLY ones): materializeFilter( at packages/web/src/hooks/useCombinationOrchestrator.ts (~533, ~566); dropFilterView( at packages/web/src/App.tsx (~108) + packages/web/src/components/DashboardsPage.tsx (~503). Definitions live in packages/web/src/api/client.ts.
  - In packages/web/src/components/charts/ the tokens appear ONLY in COMMENTS (CalendarRenderer.tsx lines 24 + 491 "NO import of materializeFilter/dropFilterView"; WidgetRenderer.tsx line 500 comment) — NO call-site open-paren on a non-comment line. The 4 lib helpers import/call NEITHER token.
</gate_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run + classify the WEB automated gates</name>
  <files>.planning/phases/103-verification-live-uat/103-GATES.md</files>
  <read_first>
    CLAUDE.md §"Test gates"; 103-CONTEXT.md §"SC1 — Automated gates"; the <gate_facts> block above (InfoCardRenderer noise note).
  </read_first>
  <action>
    From packages/web run, capturing full output:
      1. `cd packages/web && npx tsc --noEmit` — record exit status; any error output.
      2. `cd packages/web && npx vitest run` — record the final "Test Files … passed" + "Tests … passed" summary lines and the FAILED count. The pre-existing InfoCardRenderer.spec.tsx 401 unhandled-rejection is console noise — the gate passes iff FAILED == 0 on both Test Files and Tests. Do NOT record or assert a fixed pass-count (specs grew across 97-102).
      3. `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` — record passed/failed; must be 0 failed.
    Begin 103-GATES.md with a `## Web Gates` section: for each of the 3 commands, record the exact command, the verdict (PASS/FAIL), and the evidence line (the summary line or the error). If ANY web gate fails, do NOT attempt feature fixes here — record the failure verbatim; Plan 02 (the walk) is where in-session gap-fixes happen. (A pre-walk gate failure is itself a finding to surface to the operator.)
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/styles/theme-guard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    103-GATES.md `## Web Gates` records all 3 commands with PASS/FAIL + evidence lines. Web tsc clean; theme-guard 0 failed; `npx vitest run` shows 0 FAILED (asserted on the passed line, NOT a fixed count; InfoCardRenderer 401 log explicitly noted as non-failing noise).
  </acceptance_criteria>
  <done>Web gate verdicts (tsc / vitest 0-failed / theme-guard) recorded in 103-GATES.md with evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Run + classify the SERVER automated gates (SET-BASED, no fixed pass-count) + confirm the v1.19 server touch passes in isolation</name>
  <files>.planning/phases/103-verification-live-uat/103-GATES.md</files>
  <read_first>
    103-CONTEXT.md §"SC1" server bullet; the <gate_facts> block (TD-V16-TEST-ISOLATION set, the dev-.env-leak note, the v1.19 server-touch isolation requirement); STATE.md v1.19 Test Gates + the 96-UAT.md "Final Automated Gate Result" for the exact classification precedent.
  </read_first>
  <action>
    From packages/server:
      1. `cd packages/server && npx tsc --noEmit` — record exit status + any errors.
      2. Run the server suite in a CLEAN env so the dev-.env DEFAULT_VIEW_TTL_MINUTES=3 leak does not mask results — e.g. temporarily move packages/server/.env aside (or run with DEFAULT_VIEW_TTL_MINUTES unset), then `cd packages/server && npx vitest run`. Restore .env afterward. Record the list of FAILING FILES.
      3. CLASSIFY every failing file against the documented TD-V16-TEST-ISOLATION set (OIDC issuer-mock set / db.smoke / cross-mode isolation / routes.wms / dev-.env-leak). The gate PASSES iff the failing set ⊆ the documented set AND ZERO v1.19-introduced spec fails. NEVER assert a fixed pass-count.
      4. Prove the v1.19 server touch passes IN ISOLATION — run the custom_metrics route spec(s) and the /api/auth/me spec(s) alone in BOTH auth modes (mirror how prior server specs are invoked; grep for the spec file names, e.g. `find packages/server -iname "*custom*metric*.spec.*" -o -iname "*auth*me*.spec.*"` and the routes covering maxBarGroupBySeriesCap). Record: v1.19 specs = PASS in isolation.
    Append a `## Server Gates` section to 103-GATES.md: tsc verdict; the full failing-file list; the per-file classification (which item of the documented set each belongs to); the v1.19-in-isolation result; and the final SET-BASED verdict (PASS iff ⊆ documented set + v1.19 clean). Explicitly state you asserted the set-membership, NOT a pass-count.
  </action>
  <verify>
    <automated>cd packages/server && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    103-GATES.md `## Server Gates` records: tsc clean; the failing-file list; each failing file classified as a member of the documented TD-V16-TEST-ISOLATION set (verdict PASS iff ⊆ that set); the v1.19 custom_metrics + /api/auth/me specs attested PASS in isolation in both auth modes; verdict wording is SET-BASED (no fixed pass-count).
  </acceptance_criteria>
  <done>Server tsc clean; server vitest classified set-based (failing ⊆ TD-V16-TEST-ISOLATION) with v1.19 specs passing in isolation; recorded in 103-GATES.md.</done>
</task>

<task type="auto">
  <name>Task 3: Re-assert the sole-materialize-trigger invariant across all five features</name>
  <files>.planning/phases/103-verification-live-uat/103-GATES.md</files>
  <read_first>
    103-CONTEXT.md §"SC1" sole-materialize-trigger bullet + §"Static-grep invariant"; the <gate_facts> block (authorized call sites + the charts/ comment-only lines).
  </read_first>
  <action>
    Run the invariant grep across the five features' render surface + the v1.19 pure helpers:
      1. `grep -rnE "(materializeFilter|dropFilterView)\(" packages/web/src/components/charts/` — expect ONLY comment lines (CalendarRenderer.tsx ~24/~491, WidgetRenderer.tsx ~500). Confirm NO call-site open-paren appears on a non-comment line. (If any new call site appears in a renderer, that is a BLOCKER finding — record it; do not fix feature code here, surface it to the walk.)
      2. `grep -nE "materializeFilter|dropFilterView" packages/web/src/lib/customMetricSql.ts packages/web/src/lib/yAxisScale.ts packages/web/src/lib/customWhere.ts packages/web/src/lib/barGroupedSeries.ts` — expect ZERO matches (the 4 v1.19 helpers neither import nor call).
      3. Confirm the authorized call sites are unchanged: materializeFilter( at packages/web/src/hooks/useCombinationOrchestrator.ts; dropFilterView( at App.tsx + DashboardsPage.tsx. `grep -rnE "(materializeFilter|dropFilterView)\(" packages/web/src --include=*.ts --include=*.tsx | grep -v spec` and confirm every non-comment call site is one of those authorized locations.
    Append a `## Sole-Materialize-Trigger Invariant` section to 103-GATES.md: the grep commands, their output, and the verdict (PASS = charts/ has only comment mentions, the 4 helpers are token-free, and the only call sites are the authorized orchestrator + cleanup ones — AggregatedWidgetRenderer / orchestrator remain the sole trigger).
  </action>
  <verify>
    <automated>cd packages/web && test -z "$(grep -rEl '(materializeFilter|dropFilterView)\(' src/lib/customMetricSql.ts src/lib/yAxisScale.ts src/lib/customWhere.ts src/lib/barGroupedSeries.ts 2>/dev/null)" && ! grep -rnE '^[^/]*\b(materializeFilter|dropFilterView)\(' src/components/charts/*.tsx | grep -vE '//' | grep -qE '(materializeFilter|dropFilterView)\('</automated>
  </verify>
  <acceptance_criteria>
    103-GATES.md `## Sole-Materialize-Trigger Invariant` records the grep commands + output + PASS verdict: the 4 v1.19 lib helpers have ZERO materialize/drop tokens; packages/web/src/components/charts/ shows the tokens ONLY in comments (no call-site); the only real call sites are useCombinationOrchestrator (materialize) + App/DashboardsPage (drop). AggregatedWidgetRenderer / orchestrator confirmed sole trigger.
  </acceptance_criteria>
  <done>Invariant re-asserted across all 5 features + recorded in 103-GATES.md with PASS verdict.</done>
</task>

</tasks>

<verification>
- Web: `cd packages/web && npx tsc --noEmit` clean; `npx vitest run` 0 failed (Test Files + Tests passed lines; InfoCardRenderer 401 log is non-failing noise); `npx vitest run src/styles/theme-guard.spec.ts` green.
- Server: `cd packages/server && npx tsc --noEmit` clean; `npx vitest run` (clean env) failing files ⊆ TD-V16-TEST-ISOLATION documented set, v1.19 custom_metrics + /api/auth/me specs PASS in isolation (both auth modes). SET-BASED verdict — NEVER a fixed pass-count.
- Invariant: `grep -rnE "(materializeFilter|dropFilterView)\(" packages/web/src/components/charts/` → comment lines only; the 4 v1.19 lib helpers token-free; authorized call sites = orchestrator (materialize) + App/DashboardsPage (drop) only.
- 103-GATES.md exists with all three gate sections + verdicts.
</verification>

<success_criteria>
- SC1 (partial — evidence): all automated gates recorded with PASS verdicts on both stacks (web tsc/vitest/theme-guard; server tsc + set-based vitest), and the sole-materialize-trigger invariant re-asserted across all five features.
- 103-GATES.md is the evidence artifact Plan 02 folds into VERIFICATION.md's SC1 attestation.
</success_criteria>

<output>
After completion, create `.planning/phases/103-verification-live-uat/103-01-SUMMARY.md`.
</output>
