# v1.19 Visualization Customization — Live Operator UAT Record

**Phase:** 103 — Verification + Live UAT
**Status:** ✅ PASS — operator attested 2026-07-02 (all scenarios; 5 gaps found + fixed in-session, repro-test-driven, re-walked to PASS)
**Started:** 2026-07-02
**Environment:** operator's live deployed Kinetica (password mode)

> Living record — updated during the walk. Final PASS attestation + per-scenario sign-off is written here + in `103-VERIFICATION.md` once the full matrix passes.

## SC1 — Automated gates (both stacks) — ✅ PASS

Recorded in `103-GATES.md`:
- Web: `tsc` clean · vitest 138 files / 3175 tests, 0 failed (InfoCardRenderer 401 unhandled-rejection noise noted, non-failing) · theme-guard 136/136.
- Server: `tsc` clean · vitest SET-BASED — failing files ⊆ TD-V16-TEST-ISOLATION (OIDC issuer-mock set, db.smoke drift, cross-mode contamination); v1.19 specs (custom_metrics CRUD 23/23, `/api/auth/me` cap fields) pass in isolation.
- Sole-materialize-trigger invariant: PASS — charts renderers + the 4 v1.19 lib helpers (customMetricSql/yAxisScale/customWhere/barGroupedSeries) are token-free; only the 4 authorized call sites reference materialize.

*(Gates re-run green after each in-session fix — latest full web suite 3195/3195.)*

## Scenario Matrix — walk status

| ID | Scenario | Status | Notes |
|----|----------|--------|-------|
| F1 | Calendar smart domain control | ✅ PASS | 3 gaps found + fixed (viewer dropdown, 3-way control consolidation, allowed-scales multi-select); re-walked PASS |
| F2 | Per-viz custom WHERE (incl. invalid-error isolation) | ✅ PASS | |
| F3 | Custom metrics (CRUD, picker, no-AGG-wrapper, orphan, gate) | ✅ PASS | 1 gap found + fixed (timeline/line grouped top-N pre-query); re-walked PASS |
| F4 | Smart / log Y-axis (line/timeline/bar) | ✅ PASS | log zero/neg clamp unit-covered |
| F5 | Multi-column bar group-by (nested, grouped/stacked, cap-truncation) | ✅ PASS | 1 gap found + fixed (truncation-note overlap); re-walked PASS |
| X | Cross-feature combos | ✅ PASS | |
| R | Backward-compat sweep (pre-v1.19 dashboards) | ✅ PASS | |

Legend: ✅ PASS · ◐ fixed, awaiting re-confirm · ○ pending · ✗ failed

**Operator attestation:** all scenarios PASS (2026-07-02). 5 in-session gaps fixed with regression tests + re-walked. Plus a deploy-doc improvement (`.env.example` tuning knobs).

## In-session gap log

| # | Feature | Gap | Fix | Commit(s) | Regression test |
|---|---------|-----|-----|-----------|-----------------|
| 1 | F1 | Smart mode only exposed the Time-scale picker in config, not on the widget — viewer couldn't select the scale | Added a viewer-facing "Time scale" dropdown on the calendar widget in smart mode (restricted to `allowedSmartScales`, transient view-local override); delivers CALSMART-V119-03 as written | `a28afdb` (+`f1d22ee` RED) | CalendarRenderer.spec: smart control bar options = allowed set; advanced unchanged |
| 2 | F1 | Overlapping controls (Advanced/Smart selector + domain/subdomain dropdowns + Allowed scales + a separate "Show domain/subdomain controls" checkbox) — confusing | Consolidated into a single 3-way "Time grouping control": Advanced (domain+subdomain) / Advanced (…, dashboard adjustable) / Smart (single time scale, dashboard adjustable); removed the checkbox; legacy `showDomainSubdomainControls:true` → advanced-adjustable | `2e47e31` | Config panel 3-option select + checkbox gone; renderer legacy back-compat mapping |
| 3 | F1 | "ALLOWED TIME SCALES" header + 4 big accent checkboxes looked like a separate config grouping | Replaced with a compact multi-select box (extracted the shared `MultiSelectChips` used by DataFilter + added an optional `formatOption` for capitalized labels); min-1 guard + smart-scale re-derive preserved | `eedfb3d` (extract) + `2260283` (use) | CalendarConfigPanel.spec combobox tests; MultiSelectChips.spec formatOption; DataFilter spec unchanged |
| 4 | F3 | Custom metric on a GROUPED timeline / numeric-line emitted `AVG()` (empty) → Kinetica "Invalid number of arguments"; the inline top-N series ranking pre-query bypassed `resolveMetricExpr` (Phase 100 integration gap; main query was fine) | Routed both renderers' inline top-N pre-query aggExpr through `resolveMetricExpr(metric0.metricId, realAgg0, tableId)` | `4744314` | Timeline/NumericLine specs: custom→resolved expr (not `AVG()`), real→unchanged, static wiring |
| 5 | F5 | The multi-series "Showing top N of M series" truncation note overlapped the bar plot (note in a relative box; chart wrapper was absolute inset:0 filling the whole box) | Made the bar render wrapper a flex column: note = flex-shrink:0 row above a flex:1 chart region that keeps the absolute-inset ResponsiveContainer fill | `681d5ed` | WidgetRenderer 118/118 green (single/multi paths); full suite 3195/3195 |

## Notes
- **Deploy docs (`01f0fa4`):** documented the boot-read tuning env vars in `packages/server/.env.example` — `DEFAULT_VIEW_TTL_MINUTES`, `TTL_KEEPALIVE_LEAD_MINUTES` (v1.15), `MAX_COMBINATION_VIEWS_PER_TABLE`, `DISABLE_DV_FILTER_SCOPE` (v1.18), `MAX_BAR_GROUP_BY_SERIES` (v1.19, default 12). They were previously only discoverable in code. Doc-only (no runtime change).
- All FEATURE fixes FRONTEND-ONLY (zero `packages/server` diff); gates re-run green after each.
- Log-axis zero/negative clamp: unit-locked in `yAxisScale.spec.ts`; live-reproduce only if the operator has non-positive data (else attest positive-data behavior + note).

---
*UAT record started 2026-07-02; updated live during the walk.*
