---
phase: 110-designer-settings-ui-verification-live-uat
plan: 02
type: execute
wave: 2
depends_on: ["110-01"]
files_modified:
  - .planning/phases/110-designer-settings-ui-verification-live-uat/110-GATES.md
  - .planning/phases/110-designer-settings-ui-verification-live-uat/110-UAT.md
  - .planning/phases/110-designer-settings-ui-verification-live-uat/110-VERIFICATION.md
autonomous: false

requirements: [VERIFY-V120-01]

must_haves:
  truths:
    - "Web tsc is clean; web vitest is 0 failures in a clean parallel run (or, if only the known-unrelated flaky files fail, they are confirmed green via --no-file-parallelism); theme-guard green."
    - "Server tsc is clean; server vitest is judged with the dev-.env TTL leak neutralized (DEFAULT_VIEW_TTL_MINUTES=\"\"); the failing-file set is a SUBSET of the documented TD-V16-TEST-ISOLATION set (never a fixed pass-count)."
    - "The sole-materialize-trigger grep across packages/web/src/components/ finds only the authorized call sites — the filter panel / clear-all handler is NOT among them."
    - "A blocking live operator walk-through exercises the full v1.20 feature matrix (FSET/FPANEL/FSCOPE/FCLEAR) PLUS the 109.1/109.2 filter-scope-on-custom-panel-charts behavior, in light + dark + narrow viewport, and attests PASS."
    - "Any gap found during the walk is fixed in-session with a regression test and re-walked to PASS; all v1.20 requirements trace to Complete."
  artifacts:
    - path: ".planning/phases/110-designer-settings-ui-verification-live-uat/110-GATES.md"
      provides: "Automated both-stack gate evidence + sole-materialize grep output"
    - path: ".planning/phases/110-designer-settings-ui-verification-live-uat/110-UAT.md"
      provides: "Operator UAT checklist results + any in-session gap fixes + final PASS attestation"
    - path: ".planning/phases/110-designer-settings-ui-verification-live-uat/110-VERIFICATION.md"
      provides: "SC1-SC4 milestone attestation + v1.20 requirement traceability table"
  key_links:
    - from: "110-GATES.md automated evidence"
      to: "110-VERIFICATION.md SC1/SC2"
      via: "gate verdicts consolidated into the milestone attestation"
      pattern: "SET-BASED"
    - from: "110-UAT.md operator attestation"
      to: "110-VERIFICATION.md SC3/SC4"
      via: "walk-through PASS + gap-fix loop rolled into attestation"
      pattern: "PASS"
---

<objective>
Final v1.20 milestone verification (VERIFY-V120-01): prove the whole Filter Panel milestone green on BOTH stacks with the sole-materialize-trigger invariant intact, then run a BLOCKING live operator walk-through of the complete feature — including the 109.1/109.2 filter-scope-on-custom-panel-charts additions and the light/dark + narrow-viewport visual checks automated gates cannot catch. Any gap found is fixed in-session (with a regression test) and re-walked to PASS.

Purpose: closes VERIFY-V120-01 and ships v1.20. Mirrors the v1.19 verification format (`103-GATES.md` / `103-VERIFICATION.md` / `103-UAT.md`), especially the server SET-BASED gate + dev-.env-leak neutralization and the SC3 in-session gap-fix loop.
Output: `110-GATES.md`, `110-UAT.md`, `110-VERIFICATION.md`.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-designer-settings-ui-verification-live-uat/110-CONTEXT.md
@.planning/phases/103-verification-live-uat/103-GATES.md
@.planning/phases/103-verification-live-uat/103-VERIFICATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run + record automated gates on BOTH stacks + the sole-materialize-trigger grep → 110-GATES.md</name>
  <read_first>
    - .planning/phases/103-verification-live-uat/103-GATES.md (format to mirror: per-gate command + evidence lines + set-membership table + sole-materialize grep section).
    - CLAUDE.md "Test gates" section (web tsc/vitest/theme-guard; server tsc + SET-BASED vitest ⊆ TD-V16-TEST-ISOLATION; never a fixed pass-count).
    - .planning/STATE.md v1.20 Test Gates + Invariant lines (the exact grep and the dev-.env TTL neutralization note).
  </read_first>
  <files>.planning/phases/110-designer-settings-ui-verification-live-uat/110-GATES.md</files>
  <action>
    Run and capture verbatim summary lines for each gate, then write `110-GATES.md` mirroring the 103-GATES.md structure.

    WEB (from packages/web):
    1. `cd packages/web && npx tsc --noEmit` — expect clean exit 0.
    2. `cd packages/web && npx vitest run` — DEFAULT PARALLEL run. Assert 0 Test Files failed / 0 Tests failed.
       KNOWN FLAKINESS CAVEAT (do NOT let it block): a handful of UNRELATED files (DatasetsPage / actionEngine.canary / BrandColorSection / InfoCardRenderer) can intermittently fail under parallelism but pass in isolation. IF and ONLY IF the only failures are among those exact files, re-run `cd packages/web && npx vitest run --no-file-parallelism` and record it green as the tie-breaker. Frame the web-vitest verdict as: "0 failures in a clean parallel run OR, if those exact unrelated files flake, confirmed green via --no-file-parallelism." The v1.20-touched specs (DashboardSettingsModal, DashboardsPage, FilterPanel/FilterChip, WidgetCard, useReverseFilterMap/computeReverseFilterMap, calendar/timeline/numericline config + renderer, useCombinationOrchestrator) MUST pass unconditionally — call them out explicitly.
    3. `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` — assert green (0 failed).

    SERVER (from packages/server) — SET-BASED, dev-.env TTL neutralized:
    4. `cd packages/server && npx tsc --noEmit` — expect clean exit 0.
    5. `cd packages/server && DEFAULT_VIEW_TTL_MINUTES="" npx vitest run` — the `""` neutralizes the dev `.env` `DEFAULT_VIEW_TTL_MINUTES=3` leak (documented dev-.env-leak pattern; see 103-GATES §Server Gates #2). Enumerate the failing FILES and classify each against the documented TD-V16-TEST-ISOLATION set (OIDC issuer-mock / db.smoke snapshot drift / cross-mode contamination / routes.wms). Verdict = SET-BASED PASS iff failing-set ⊆ documented set. NEVER assert a fixed pass-count. If any file OUTSIDE the documented set fails, that is a real regression → STOP and fix before proceeding (v1.20 is frontend-only beyond Phase 106, so no new server failures are expected).

    SOLE-MATERIALIZE-TRIGGER INVARIANT:
    6. `grep -rnE "(materializeFilter|dropCombinationView|dropFilterView)\(" packages/web/src/components/` — record output. Confirm the ONLY real (non-comment) call sites are the authorized ones (App.tsx / DashboardsPage.tsx cleanup drops; the orchestrator is under hooks/, not components/). Explicitly confirm the v1.20 panel + clear-all handler + FilterPanel/FilterChip import NO `materialize*` / `dropCombinationView` and never call `filterStore.reset()` live.
    7. Also run `grep -rnE "(materializeFilter|dropCombinationView)\(" packages/web/src/hooks/useCombinationOrchestrator.ts` to record the authorized orchestrator call sites for completeness.

    Write `110-GATES.md` with a per-gate section (command + verbatim evidence lines + verdict), a server failing-file classification table, the grep outputs with analysis, and a final Summary table (mirror 103-GATES.md's closing table).
  </action>
  <acceptance_criteria>
    - `110-GATES.md` exists and records: web tsc PASS, web vitest verdict (parallel result + the flakiness-caveat framing + --no-file-parallelism tie-breaker if used), theme-guard PASS, server tsc PASS, server vitest SET-BASED verdict with a failing-file→TD-V16-TEST-ISOLATION classification table (no fixed pass-count asserted), and the sole-materialize grep output + analysis.
    - The web v1.20-touched specs are individually named and marked PASS.
    - The server verdict states "failing set ⊆ TD-V16-TEST-ISOLATION" (or "0 failures") — never "N passed".
    - The sole-materialize grep section confirms zero unauthorized call sites in packages/web/src/components/.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/styles/theme-guard.spec.ts; cd ../server && npx tsc --noEmit; cd ../.. && grep -rnE "(materializeFilter|dropCombinationView|dropFilterView)\(" packages/web/src/components/ || true</automated>
  </verify>
  <done>110-GATES.md records both-stack automated evidence (web parallel + flakiness caveat + sequential fallback; server SET-BASED with dev-.env neutralized) and the clean sole-materialize grep; all v1.20-touched specs green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Blocking live operator UAT walk-through + in-session gap-fix loop → 110-UAT.md</name>
  <read_first>
    - 110-GATES.md (Task 1 output — automated gates must be green before the walk).
    - .planning/phases/110-designer-settings-ui-verification-live-uat/110-CONTEXT.md "Live UAT scope" checklist (the four groups).
    - .planning/phases/103-verification-live-uat/103-UAT.md (prior operator-walk format + the SC3 in-session gap-fix loop to mirror).
  </read_first>
  <files>.planning/phases/110-designer-settings-ui-verification-live-uat/110-UAT.md</files>
  <action>
    Ensure Task 1's automated gates are green (110-GATES.md), then run the app on both stacks and walk the operator checklist below with the operator. Record each group's PASS/FAIL + the operator's final attestation in 110-UAT.md. If any group fails, run the in-session gap-fix loop (fix + regression test + re-run the affected gate green + re-walk to PASS) and record it. This is a BLOCKING checkpoint — do not proceed to Task 3 until the operator attests PASS on every group.
  </action>
  <what-built>
    The complete v1.20 Filter Panel milestone across all shipped phases: per-dashboard filter display mode with the new designer Settings toggle (110-01), the right-side panel + chips + reflow + XOR switch (107), applies-to list + on-canvas highlight (108), global clear-all (109), and filter scope on custom-panel charts (109.1/109.2). Automated gates on both stacks are green (Task 1 / 110-GATES.md).
  </what-built>
  <how-to-verify>
    Run the app (both stacks), open a real multi-widget dashboard (e.g. `Ookla Dash Vs2`), and walk this checklist. Use the new in-app Settings toggle to switch modes (replaces the manual DB flip). Attest each group PASS/FAIL in `110-UAT.md`.

    1. FSET (designer toggle — 110-01): open the toolbar "Settings" button → segmented toggle Top bar ↔ Right panel. Switching flips the dashboard surface LIVE (no reload); the choice persists on reload; a viewer WITHOUT DASHBOARDS_EDIT sees the designer's chosen mode AND no Settings button.
    2. FPANEL (107): panel on the right; chips grouped table / dynamic-view / spatial; collapsible; remove a single chip; per-group "Clear all"; collapse → thin rail + count badge; collapse state persists on reload; empty state at zero filters.
    3. FSCOPE (108): each filter shows "applies to N" + an expandable list (incl. layer names); hover a filter → accent ring on exactly the affected widgets (no flicker on unaffected widgets/maps); click → scroll to topmost + flash all; expanded-row click → scroll+flash that one.
    4. FCLEAR (109): "Clear all filters" appears when filters are active → clears ALL tables + dynamic views + spatial draws in one step; every per-combination `_c{hash}` Kinetica view is DROPped by ref-count (network tab); hidden at zero filters.
    5. FILTER SCOPE ON CUSTOM-PANEL CHARTS (109.1/109.2): on a Calendar Heatmap / Timeline / Numeric-Line widget, disabling a source (e.g. Technology) in Filter Scope leaves the OTHER filters applied to that chart (verify on the live read path, not just config persistence); those charts appear in the panel's "applies to N" count and get highlighted/scrolled on hover/click; the legacy `respondToFilters` toggle is GONE from the config UI; a legacy calendar with old `respondToFilters:false` still ignores filters (migrated to empty allow-list).
    6. LIGHT + DARK + NARROW: panel / rail / chips / ring / flash / count badge / Settings toggle are readable in BOTH themes; narrow viewport (<900px) auto-collapses to the rail and expanding OVERLAYS (no reflow). (Undefined classes + rgba/wrong tokens pass all automated gates but render broken — this is the check they cannot catch.)
    7. BACKWARD-COMPAT: a topbar/unset dashboard is byte-identical (top bar unchanged, grid layout intact); switching a dashboard to panel and back behaves cleanly with no layout cascade/staircase (the 6c6eb3e reflow fix + breakpoint="lg" pin hold).
    8. MULTI-MAP + INFO POPUP: on a real multi-map dashboard, re-confirm multi-map info-popup scoping (activeWidgetId, commit 9652182) and panel-mode grid reflow (commit 6c6eb3e) still hold.

    GAP-FIX LOOP (mirror 103 SC3): if any group FAILs, fix it in-session, add/adjust a regression test, re-run the affected automated gate green, and re-walk that group to PASS. Record each gap + fix + re-walk in 110-UAT.md. Do not mark the milestone PASS until every group is PASS.
  </how-to-verify>
  <resume-signal>Operator types "approved" (all groups PASS) or lists the failing group(s) + observed behavior for the in-session gap-fix loop.</resume-signal>
  <acceptance_criteria>
    - `110-UAT.md` exists with a PASS/FAIL line for each of the 8 groups above and the operator's final attestation.
    - Every group is PASS (after any in-session gap fixes); each gap is recorded with its fix + regression test + re-walk result.
    - If any gap was fixed, the affected automated gate was re-run green and noted in 110-UAT.md (and 110-GATES.md updated if a gate command output changed).
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/110-designer-settings-ui-verification-live-uat/110-UAT.md</automated>
  </verify>
  <done>Operator has walked the full extended checklist and attested PASS on all groups; any gaps fixed in-session with regression tests and re-walked to PASS; 110-UAT.md records the results.</done>
</task>

<task type="auto">
  <name>Task 3: Consolidate SC1-SC4 milestone attestation + v1.20 requirement traceability → 110-VERIFICATION.md</name>
  <read_first>
    - 110-GATES.md (Task 1) + 110-UAT.md (Task 2).
    - .planning/phases/103-verification-live-uat/103-VERIFICATION.md (attestation format to mirror).
    - .planning/STATE.md "v1.20 Requirement Coverage" table + the Phase 110 Success Criteria in ROADMAP.md.
  </read_first>
  <files>.planning/phases/110-designer-settings-ui-verification-live-uat/110-VERIFICATION.md</files>
  <action>
    Write `110-VERIFICATION.md` mirroring 103-VERIFICATION.md. Attest each Phase 110 success criterion:
    - SC1 (FSET-V120-01): designer with DASHBOARDS_EDIT can pick the mode; non-permitted user cannot; NO new permission (note the permission catalog is byte-unchanged — no rbac spec count moved). Reference 110-01-SUMMARY + the UAT FSET group.
    - SC2 (automated gates): consolidate 110-GATES.md verdicts — web vitest 100% (with the parallel-flakiness caveat framing), web + server tsc clean, theme-guard green, server vitest SET-BASED ⊆ TD-V16-TEST-ISOLATION, sole-materialize grep clean.
    - SC3 (blocking operator walk): reference 110-UAT.md — full matrix incl. 109.1/109.2 + light/dark/narrow attested PASS.
    - SC4 (gap-fix loop): record any in-session gaps fixed + re-walked to PASS (or "no gaps found").
    Include a v1.20 requirement traceability table (FSET-V120-01/02/03, FPANEL-V120-01..09, FSCOPE-V120-01..05, FCLEAR-V120-01, VERIFY-V120-01) all = Complete, with the phase each landed in.
    Finish with an overall milestone verdict line.
  </action>
  <acceptance_criteria>
    - `110-VERIFICATION.md` exists with SC1-SC4 each explicitly attested and cross-referencing 110-GATES.md + 110-UAT.md.
    - The requirement traceability table lists every v1.20 requirement ID = Complete (19 rows = STATE.md's 17 + FSCOPE-V120-04 + FSCOPE-V120-05, the two mid-milestone inserted-phase requirements).
    - The server verdict is stated as SET-BASED (⊆ TD-V16-TEST-ISOLATION), not a fixed pass-count.
    - An overall verdict line declares v1.20 verified green on both stacks with operator PASS.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/110-designer-settings-ui-verification-live-uat/110-VERIFICATION.md && grep -q "VERIFY-V120-01" .planning/phases/110-designer-settings-ui-verification-live-uat/110-VERIFICATION.md</automated>
  </verify>
  <done>110-VERIFICATION.md consolidates SC1-SC4 with the full v1.20 requirement traceability all Complete and an overall milestone PASS verdict.</done>
</task>

</tasks>

<verification>
- 110-GATES.md, 110-UAT.md, 110-VERIFICATION.md all exist.
- Web: `cd packages/web && npx tsc --noEmit` clean; `npx vitest run` 0 failures (parallel, or unrelated-flake confirmed via --no-file-parallelism); theme-guard green.
- Server: `cd packages/server && npx tsc --noEmit` clean; `DEFAULT_VIEW_TTL_MINUTES="" npx vitest run` failing set ⊆ TD-V16-TEST-ISOLATION.
- `grep -rnE "(materializeFilter|dropCombinationView)\(" packages/web/src/components/` — no unauthorized call sites.
- Operator attested PASS on every UAT group; any gaps fixed in-session + re-walked.
</verification>

<success_criteria>
- v1.20 is proven green on both stacks with the sole-materialize-trigger invariant intact and the server gate judged SET-BASED (never a fixed pass-count).
- A blocking operator walk-through of the full feature matrix (incl. 109.1/109.2 filter scope on custom-panel charts + light/dark/narrow visual checks) attests PASS.
- Any gap found is fixed in-session with a regression test and re-walked to PASS.
- Every v1.20 requirement traces to Complete in 110-VERIFICATION.md.
</success_criteria>

<output>
After completion, create `.planning/phases/110-designer-settings-ui-verification-live-uat/110-02-SUMMARY.md`.
</output>
