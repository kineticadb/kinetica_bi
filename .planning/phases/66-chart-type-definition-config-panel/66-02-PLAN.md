---
phase: 66-chart-type-definition-config-panel
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/charts/registry.ts
  - packages/web/src/components/charts/ChartConfigPanel.tsx
autonomous: true
requirements: [CAL-V113-01]
must_haves:
  truths:
    - "A CustomConfigPanel receives the dashboard's dynamicViews list as a prop (ConfigPanelProps.dynamicViews)"
    - "ChartConfigPanel forwards its dynamicViews prop into the <Custom> panel slot"
    - "Existing custom panels (Timeline/DataFilter/Legend/Map) compile and pass unchanged (dynamicViews is optional)"
  artifacts:
    - path: "packages/web/src/components/charts/registry.ts"
      provides: "ConfigPanelProps.dynamicViews optional field"
      contains: "dynamicViews"
    - path: "packages/web/src/components/charts/ChartConfigPanel.tsx"
      provides: "dynamicViews forwarded to <Custom>"
      contains: "dynamicViews={dynamicViews}"
  key_links:
    - from: "packages/web/src/components/charts/ChartConfigPanel.tsx"
      to: "CustomConfigPanel (<Custom> slot, line ~436)"
      via: "dynamicViews prop forward"
      pattern: "dynamicViews=\\{dynamicViews\\}"
---

<objective>
Thread the dashboard's `dynamicViews` list into the `CustomConfigPanel` slot so the upcoming `CalendarConfigPanel` (Plan 66-03) can render a dv-aware data-source picker (CAL-V113-01: calendar binds to a base table OR a dynamic view).

Purpose: `TimelineConfigPanel` never built a dv binding, so `ConfigPanelProps` only carries `tables` — NOT `dynamicViews`. ChartConfigPanel already RECEIVES `dynamicViews` as its own prop (line 35) but does NOT forward it to `<Custom>` (line 436). This is the small plumbing gap CONTEXT.md flagged. This plan is the minimal additive change; it must not alter any existing panel behavior.
Output: `ConfigPanelProps.dynamicViews` (optional) added to the registry type, and `<Custom dynamicViews={dynamicViews} />` forwarding in ChartConfigPanel.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-chart-type-definition-config-panel/66-CONTEXT.md

<interfaces>
<!-- From packages/web/src/api/client.ts (DynamicViewRow — already exported) -->
export type DynamicViewRow = {
  id: number;
  dashboard_id: number;
  source_table_id: number;
  name: string;
  template_sql: string;
  max_records: number;
  columns_json: { name: string; type: string }[] | string | null;
  created_at: string;
  updated_at: string;
};

<!-- From packages/web/src/components/charts/ChartConfigPanel.tsx -->
//  Props.dynamicViews?: DynamicViewRow[]   (line ~35 — ALREADY received)
//  <Custom config={draft} columns={allColumns} tables={tables} widgets={widgets} isValid={...} onChange={...} />  (line ~436 — does NOT forward dynamicViews today)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dynamicViews to ConfigPanelProps and forward it through ChartConfigPanel</name>
  <files>packages/web/src/components/charts/registry.ts, packages/web/src/components/charts/ChartConfigPanel.tsx</files>
  <read_first>
    - packages/web/src/components/charts/registry.ts (ConfigPanelProps type, lines ~41-78 — see how `tables`, `widgets`, `isValid` are declared as optional props with JSDoc; mirror that style for `dynamicViews`)
    - packages/web/src/components/charts/ChartConfigPanel.tsx lines 1-44 (Props type — note `dynamicViews?: DynamicViewRow[]` is ALREADY a prop, line ~35) and lines 334-456 (the `if (chartDef.CustomConfigPanel)` branch and the `<Custom .../>` JSX at line ~436 where props are passed)
    - packages/web/src/api/client.ts (DynamicViewRow export — the type to reference; registry.ts already imports WidgetDto from "../../api/client", add DynamicViewRow to that import)
  </read_first>
  <action>
    Two additive edits — do NOT change any existing field or behavior.

    1) In `registry.ts`:
       - Extend the existing `import type { WidgetDto } from "../../api/client";` to also import `DynamicViewRow`:
         `import type { WidgetDto, DynamicViewRow } from "../../api/client";`
       - Add an OPTIONAL field to the `ConfigPanelProps` type (place it after `widgets?: WidgetDto[];`), with a JSDoc comment in the same style as the surrounding fields:
         ```
         /**
          * Phase 66 (CAL-V113-01): dashboard-scoped dynamic-view list, forwarded from
          * ChartConfigPanel's own `dynamicViews` prop into the CustomConfigPanel slot.
          * CalendarConfigPanel uses this to render a dv-aware data-source dropdown
          * (table OR dynamic view) — TimelineConfigPanel never built a dv binding so
          * this prop was previously absent from ConfigPanelProps. Optional — panels
          * that bind base-table-only (Timeline) ignore it.
          */
         dynamicViews?: DynamicViewRow[];
         ```

    2) In `ChartConfigPanel.tsx`, in the `<Custom .../>` JSX (line ~436-451), add the forward prop alongside the existing `tables={tables}` / `widgets={widgets}`:
       `dynamicViews={dynamicViews}`
       (`dynamicViews` is already destructured from Props at line ~74 — no new destructure needed.)

    Do NOT touch the dual-write `handleTableChange` logic, the `dataSourceOptions` memo, or the generic Data Source section — those are for `usesDataSource !== false` charts. The calendar (usesDataSource:false) renders its OWN picker inside CalendarConfigPanel using this forwarded `dynamicViews` prop.
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/components/charts/TimelineConfigPanel.spec.tsx src/components/charts/ChartConfigPanel.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - registry.ts contains `dynamicViews?: DynamicViewRow[]` inside ConfigPanelProps
    - registry.ts import line contains `DynamicViewRow`
    - ChartConfigPanel.tsx `<Custom` JSX contains `dynamicViews={dynamicViews}`
    - `cd packages/web && npx tsc --noEmit` clean (no new errors; existing panels still satisfy ConfigPanelProps since the field is optional)
    - Existing ChartConfigPanel + TimelineConfigPanel specs still pass (no behavior change)
  </acceptance_criteria>
  <done>ConfigPanelProps carries an optional dynamicViews list and ChartConfigPanel forwards it to the custom panel slot; all existing panels compile and their specs pass unchanged.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx tsc --noEmit` — clean
- `cd packages/web && npx vitest run src/components/charts/` — all chart specs still green (no regression)
</verification>

<success_criteria>
- ConfigPanelProps.dynamicViews exists (optional)
- ChartConfigPanel forwards dynamicViews to <Custom>
- Zero behavior change for existing custom panels
</success_criteria>

<output>
After completion, create `.planning/phases/66-chart-type-definition-config-panel/66-02-SUMMARY.md`
</output>
