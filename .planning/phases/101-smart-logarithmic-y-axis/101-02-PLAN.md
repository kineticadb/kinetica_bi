---
phase: 101-smart-logarithmic-y-axis
plan: 02
type: execute
wave: 2
depends_on: ["101-01"]
files_modified:
  - packages/web/src/components/charts/TimelineConfigPanel.tsx
  - packages/web/src/components/charts/NumericLineConfigPanel.tsx
  - packages/web/src/components/charts/definitions/bar.ts
  - packages/web/src/components/charts/TimelineRenderer.tsx
  - packages/web/src/components/charts/NumericLineRenderer.tsx
  - packages/web/src/components/charts/WidgetRenderer.tsx
  - packages/web/src/components/charts/TimelineRenderer.spec.tsx
  - packages/web/src/components/charts/NumericLineRenderer.spec.tsx
  - packages/web/src/components/charts/WidgetRenderer.spec.tsx
autonomous: true
requirements: [YAXIS-V119-01, YAXIS-V119-02, YAXIS-V119-03, YAXIS-V119-04]

must_haves:
  truths:
    - "A designer can pick Zero-based / Smart / Logarithmic from a 'Y-axis scale' <select> on timeline, numeric-line, and bar config (YAXIS-V119-01)"
    - "Smart applies domain=['auto','auto'] to EVERY value axis incl. multi-metric ungrouped (YAXIS-V119-02)"
    - "Log applies scale='log' + clamped-positive-min domain + allowDataOverflow to every value axis (YAXIS-V119-03)"
    - "Absent yAxisScale → NO domain/scale/allowDataOverflow props on any axis → byte-identical for all 3 charts (YAXIS-V119-04)"
    - "Log with no positive data degrades gracefully (helper returns {} → no props, no crash)"
    - "Pie / calendar untouched; zero server diff"
  artifacts:
    - path: "packages/web/src/components/charts/TimelineConfigPanel.tsx"
      provides: "yAxisScale? on TimelineConfig type + 'Y-axis scale' <select> after the yAxisFormat control"
      contains: "yAxisScale"
    - path: "packages/web/src/components/charts/NumericLineConfigPanel.tsx"
      provides: "yAxisScale? on NumericLineConfig type + <select> after yAxisFormat control"
      contains: "yAxisScale"
    - path: "packages/web/src/components/charts/definitions/bar.ts"
      provides: "yAxisScale select ConfigField after yAxisFormat + defaultConfig entry"
      contains: "yAxisScale"
    - path: "packages/web/src/components/charts/TimelineRenderer.tsx"
      provides: "helper props spread onto single + multi-metric value axes (YAxis horizontal / XAxis vertical)"
      contains: "yAxisScaleProps"
    - path: "packages/web/src/components/charts/NumericLineRenderer.tsx"
      provides: "helper props spread onto single + multi-metric value axes"
      contains: "yAxisScaleProps"
    - path: "packages/web/src/components/charts/WidgetRenderer.tsx"
      provides: "helper props spread onto bar value axis: <YAxis> vertical layout + <XAxis type=number> horizontal layout"
      contains: "yAxisScaleProps"
  key_links:
    - from: "config panels + definitions/bar.ts (yAxisScale field)"
      to: "renderers reading cfg.yAxisScale"
      via: "optional config field, absent → helper returns {}"
      pattern: "yAxisScale"
    - from: "renderers"
      to: "packages/web/src/lib/yAxisScale.ts"
      via: "yAxisScaleProps(cfg.yAxisScale, values) spread onto axis elements"
      pattern: "yAxisScaleProps\\("
---

<objective>
Wire the Y-axis scale mode end-to-end: add the optional `yAxisScale` config field to all three
config surfaces (TimelineConfigPanel, NumericLineConfigPanel, definitions/bar.ts) as a "Y-axis scale"
<select> placed right AFTER the v1.17 yAxisFormat control, and spread `yAxisScaleProps(...)` (from
plan 101-01) onto every value-axis element in TimelineRenderer, NumericLineRenderer, and the bar path
in WidgetRenderer. Add a byte-identical regression test per renderer plus smart/log behavior tests.

Purpose: deliver YAXIS-V119-01/02/03 while guaranteeing YAXIS-V119-04 (absent → no props → byte-identical)
via explicit per-renderer tests. Pure render-config; no SQL/materialize change; pie/calendar untouched.

Output: 3 config files + 3 renderers modified; 3 renderer spec files extended.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-smart-logarithmic-y-axis/101-CONTEXT.md

<interfaces>
<!-- The helper from plan 101-01 (already implemented + unit-tested). Use directly. -->
From packages/web/src/lib/yAxisScale.ts:
```typescript
export type YAxisScaleMode = "zero" | "smart" | "log";
export type YAxisScaleAxisProps = { domain?: [number|"auto", number|"auto"]; scale?: "log"; allowDataOverflow?: boolean };
// mode undefined → {}  |  "zero" → {domain:[0,'auto']}  |  "smart" → {domain:['auto','auto']}
// "log" → {scale:'log', domain:[posMin,'auto'], allowDataOverflow:true}  |  log w/ no positive data → {}
export function yAxisScaleProps(mode: YAxisScaleMode | undefined, values: number[]): YAxisScaleAxisProps;
```

<!-- v1.17 yAxisFormat precedent — the EXACT wiring template to mirror in each file. -->
TimelineConfig type: TimelineConfigPanel.tsx ~47 `yAxisFormat?: FormatSpec;` — add `yAxisScale?: YAxisScaleMode;` beside it.
TimelineConfigPanel control: ~576-584 `<FormatSpecEditor .../>` inside a `config-group-label` "Y-AXIS FORMAT" block — place the new <select> AFTER it (before the "CUSTOM FILTER" block at ~586).
NumericLineConfig type: NumericLineConfigPanel.tsx ~43. NumericLineConfigPanel control: ~541-549 (same shape).
definitions/bar.ts: yAxisFormat ConfigField at line 25 (group "Display"); defaultConfig `yAxisFormat: null` at line 42.

<!-- Renderer value-data variables already present (use for the positive-min `values` arg). -->
TimelineRenderer.tsx: `data` (feeds <ComposedChart data={data}> ~537); `yValueKeys` (~511) = grouped? top.series : metrics.map((_,i)=>`metric_${i}`); numeric values already flatMapped at ~513: `data.flatMap(row => yValueKeys.map(k => Number(row[k])))`.
NumericLineRenderer.tsx: identical pattern — `data` (~502), `yValueKeys` (~476), flatMap (~478).
WidgetRenderer.tsx (bar): `data` (BarRenderer, ~970 data={data}); value key `y` (~824 `keys.includes("value") ? "value" : keys[1] ?? "y"`); horizontal→ `<XAxis type="number">` (~984) is value axis; vertical→ `<YAxis>` (~990) is value axis.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the "Y-axis scale" select to all 3 config surfaces</name>
  <files>packages/web/src/components/charts/TimelineConfigPanel.tsx, packages/web/src/components/charts/NumericLineConfigPanel.tsx, packages/web/src/components/charts/definitions/bar.ts</files>
  <read_first>
    - packages/web/src/lib/yAxisScale.ts (import YAxisScaleMode for the config types)
    - packages/web/src/components/charts/TimelineConfigPanel.tsx (type ~41-56; yAxisFormat control ~575-584; destructure/patch pattern; existing `ds-select`/`config-group-label`/`config-hint` usage)
    - packages/web/src/components/charts/NumericLineConfigPanel.tsx (type ~37-51; yAxisFormat control ~540-549)
    - packages/web/src/components/charts/definitions/bar.ts (yAxisFormat ConfigField ~25 + defaultConfig ~42; check the registry FieldRenderer supports a "select"/"options" field type)
  </read_first>
  <action>
    Union type: use `"zero" | "smart" | "log"`, imported as `YAxisScaleMode` from `../../lib/yAxisScale`.

    (A) TimelineConfigPanel.tsx:
      - Add `yAxisScale?: YAxisScaleMode;` to the `TimelineConfig` type (right after `yAxisFormat?` ~47), with a comment `// Phase 101 (YAXIS-V119-01): absent → no axis props (byte-identical)`.
      - Import `YAxisScaleMode` from `../../lib/yAxisScale`.
      - Destructure `yAxisScale` from config alongside the other fields (mirror how `yAxisFormat` is read).
      - Immediately AFTER the yAxisFormat `<FormatSpecEditor>` + its `config-hint` (~584), BEFORE the "CUSTOM FILTER" block (~586), insert:
        ```tsx
        <div className="ds-field">
          <span className="ds-field-label">Y-axis scale</span>
          <select
            className="ds-select"
            aria-label="Y-axis scale"
            value={yAxisScale ?? ""}
            onChange={(e) => patch({ yAxisScale: (e.target.value || undefined) as YAxisScaleMode | undefined })}
          >
            <option value="">Default</option>
            <option value="zero">Zero-based</option>
            <option value="smart">Smart</option>
            <option value="log">Logarithmic</option>
          </select>
        </div>
        ```
        Empty-string option maps back to `undefined` (absent → no-props path). Use ONLY `ds-select` / `ds-field` / `ds-field-label` (all exist in global.css — do NOT invent classes; `config-hint-warning` is a known UNDEFINED class, forbidden).

    (B) NumericLineConfigPanel.tsx: identical change — add `yAxisScale?: YAxisScaleMode;` to `NumericLineConfig` (~43), import, destructure, and insert the SAME `<select>` block after its yAxisFormat control (~549) and before its CUSTOM FILTER block.

    (C) definitions/bar.ts: after the `yAxisFormat` ConfigField (line 25, group "Display"), add:
        ```typescript
        { key: "yAxisScale", label: "Y-axis scale", type: "select",
          options: [
            { value: "", label: "Default" },
            { value: "zero", label: "Zero-based" },
            { value: "smart", label: "Smart" },
            { value: "log", label: "Logarithmic" },
          ],
          defaultValue: "", group: "Display",
          hint: "Zero-based forces a 0 baseline; Smart uses the data range; Logarithmic uses a log scale." },
        ```
        and add `yAxisScale: "",` to `defaultConfig` (after `yAxisFormat: null,` line 42).
        FIRST confirm the registry ConfigField shape supports `type: "select"` + `options`. If the
        generic FieldRenderer has no select type, add a minimal `"select"` case to the FieldRenderer
        (options → `<select className="ds-select">` mapping value/label) rather than inventing markup —
        grep the registry/FieldRenderer for existing `type ===` cases first and mirror the closest one.
        The bar renderer reads `""` as absent (empty string → treat as undefined in Task 4).
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/styles/theme-guard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "yAxisScale" packages/web/src/components/charts/TimelineConfigPanel.tsx` ≥ 2 (type + control).
    - `grep -c "yAxisScale" packages/web/src/components/charts/NumericLineConfigPanel.tsx` ≥ 2.
    - `grep -c "yAxisScale" packages/web/src/components/charts/definitions/bar.ts` ≥ 2 (field + defaultConfig).
    - `grep -q "aria-label=\"Y-axis scale\"" packages/web/src/components/charts/TimelineConfigPanel.tsx` and same in NumericLineConfigPanel.tsx.
    - No invented classes: the new markup uses only `ds-select`/`ds-field`/`ds-field-label`; `grep -q "config-hint-warning" packages/web/src/components/charts/TimelineConfigPanel.tsx` returns NON-zero (absent).
    - `cd packages/web && npx tsc --noEmit` clean; theme-guard green.
  </acceptance_criteria>
  <done>All 3 surfaces expose a "Y-axis scale" select (Default/Zero-based/Smart/Logarithmic); empty → undefined/"" absent path; tsc + theme-guard green.</done>
</task>

<task type="auto">
  <name>Task 2: Spread yAxisScaleProps onto every value axis in TimelineRenderer + NumericLineRenderer</name>
  <files>packages/web/src/components/charts/TimelineRenderer.tsx, packages/web/src/components/charts/NumericLineRenderer.tsx</files>
  <read_first>
    - packages/web/src/lib/yAxisScale.ts (yAxisScaleProps signature)
    - packages/web/src/components/charts/TimelineRenderer.tsx (yValueKeys ~511 + value flatMap ~513; value axes: grouped vertical XAxis ~596 / grouped horizontal YAxis ~606 / ungrouped vertical XAxis ~621 / ungrouped horizontal YAxis ~633)
    - packages/web/src/components/charts/NumericLineRenderer.tsx (yValueKeys ~476 + flatMap ~478; value axes: grouped vertical XAxis ~562 / grouped horizontal YAxis ~572 / ungrouped vertical XAxis ~587 / ungrouped horizontal YAxis ~598)
    - .planning/phases/101-smart-logarithmic-y-axis/101-CONTEXT.md §"Multi-axis application" (uniform to ALL value axes)
  </read_first>
  <action>
    In BOTH renderers (identical shape):
      1. Import `{ yAxisScaleProps }` from `"../../lib/yAxisScale"` and read `yAxisScale` from config (mirror how `yAxisFormat` is destructured). The `cfg.yAxisScale` value is `undefined` when unset.
      2. Compute the numeric value array ONCE (reuse the existing pattern — there is already a flatMap
         `data.flatMap(row => yValueKeys.map(k => Number(row[k])))` at ~513/~478 used for width estimation;
         extract/reuse that same array, e.g. `const scaleProps = useMemo(() => yAxisScaleProps(yAxisScale, <that numeric array>), [yAxisScale, data, yValueKeys]);`). Do NOT re-fetch data — use the already-resolved `data`.
      3. Spread `{...scaleProps}` onto EVERY value-axis element (the value axis is the YAxis when
         horizontal and the XAxis when vertical), in ALL FOUR branches:
           - grouped + vertical  → the `<XAxis xAxisId={AXIS_IDS[0]}>` (value axis)
           - grouped + horizontal→ the `<YAxis yAxisId={AXIS_IDS[0]}>`
           - ungrouped + vertical→ the per-metric `<XAxis xAxisId={AXIS_IDS[i]}>` inside metrics.map (spread on EACH — all value axes get the same mode uniformly)
           - ungrouped + horizontal→ the per-metric `<YAxis yAxisId={AXIS_IDS[i]}>` (spread on EACH)
         Do NOT spread onto the category/bucket axis (the non-value XAxis at ~581 / non-value axis) — only value axes.
      4. When `yAxisScale` is undefined, `scaleProps` is `{}` so `{...{}}` emits NO domain/scale/allowDataOverflow
         → byte-identical (YAXIS-V119-04). Do not add any conditional wrappers; the empty-spread IS the guarantee.
    Add `{...scaleProps}` positioned so an explicit prop of the same name (there are none today) would not
    be overridden unexpectedly — place it as the last prop on each axis element.
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/components/charts/TimelineRenderer.spec.tsx src/components/charts/NumericLineRenderer.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.\.\.scaleProps" packages/web/src/components/charts/TimelineRenderer.tsx` ≥ 4 (all four value-axis branches).
    - `grep -c "\.\.\.scaleProps" packages/web/src/components/charts/NumericLineRenderer.tsx` ≥ 4.
    - `grep -q "yAxisScaleProps" packages/web/src/components/charts/TimelineRenderer.tsx` and same in NumericLineRenderer.tsx.
    - `cd packages/web && npx tsc --noEmit` clean; existing renderer specs still pass.
  </acceptance_criteria>
  <done>Both renderers spread the helper props onto all 4 value-axis branches; undefined mode = empty spread; tsc + existing specs green.</done>
</task>

<task type="auto">
  <name>Task 3: Spread yAxisScaleProps onto the bar value axis (both layouts) in WidgetRenderer</name>
  <files>packages/web/src/components/charts/WidgetRenderer.tsx</files>
  <read_first>
    - packages/web/src/lib/yAxisScale.ts
    - packages/web/src/components/charts/WidgetRenderer.tsx (BarRenderer: `data`/`y`/`x` ~823-824; horizontal `<XAxis type="number">` ~984 = value axis; vertical `<YAxis>` ~990 = value axis; how `config`/`cfg` fields are read)
    - .planning/phases/101-smart-logarithmic-y-axis/101-CONTEXT.md §"Multi-axis application" (bar value axis flips to X in horizontal layout — apply to whichever is the value axis)
  </read_first>
  <action>
    In BarRenderer (WidgetRenderer.tsx):
      1. Import `{ yAxisScaleProps }` (top of file) and read the bar's config value:
         `const yAxisScale = (config.yAxisScale as ("zero"|"smart"|"log"|"")) || undefined;` — the bar
         config default is `""` (empty string from definitions/bar.ts), so coalesce `""` → `undefined`
         to hit the no-props path.
      2. Compute the value array from `data` using the value key `y`:
         `const scaleProps = yAxisScaleProps(yAxisScale, data.map(r => Number((r as Record<string,unknown>)[y])));`
         (place it near the existing `valueAxisTickFormatter` definition ~946-958).
      3. Spread `{...scaleProps}` onto the VALUE axis in BOTH layouts:
           - horizontal: the `<XAxis type="number" ...>` at ~984 (value axis in horizontal bars).
           - vertical:   the `<YAxis ...>` at ~990 (value axis in vertical bars).
         Do NOT spread onto the category axis (`<YAxis type="category">` ~985 / `<XAxis dataKey={x}>` ~989).
      4. Absent (`""`→undefined) → `scaleProps` is `{}` → no domain/scale/allowDataOverflow emitted →
         bar stays exactly zero-based-default (YAXIS-V119-04). Note: current bar has NO explicit domain,
         so emitting nothing preserves recharts' current zero-based default byte-identically.
    Place `{...scaleProps}` as the last prop on each of the two value-axis elements.
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/components/charts/WidgetRenderer.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.\.\.scaleProps" packages/web/src/components/charts/WidgetRenderer.tsx` ≥ 2 (horizontal XAxis + vertical YAxis).
    - `grep -q "yAxisScaleProps" packages/web/src/components/charts/WidgetRenderer.tsx`.
    - The `""` → undefined coalesce is present: `grep -q "|| undefined" packages/web/src/components/charts/WidgetRenderer.tsx` (near the yAxisScale read).
    - Category axes NOT touched: `...scaleProps` does not appear on the `type="category"` YAxis or the `dataKey={x}` XAxis lines (manual grep-verify placement).
    - `cd packages/web && npx tsc --noEmit` clean; WidgetRenderer spec green.
  </acceptance_criteria>
  <done>Bar value axis in both layouts spreads the helper props; empty-string default coalesces to no-props; tsc + spec green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Byte-identical + smart/log tests per renderer (the YAXIS-V119-04 lock)</name>
  <files>packages/web/src/components/charts/TimelineRenderer.spec.tsx, packages/web/src/components/charts/NumericLineRenderer.spec.tsx, packages/web/src/components/charts/WidgetRenderer.spec.tsx</files>
  <read_first>
    - packages/web/src/components/charts/TimelineRenderer.spec.tsx (THE HARNESS PRECEDENT — note the `vi.mock("recharts")` at ~lines 15-23 stubs ONLY ResponsiveContainer, and the STATIC-SOURCE-ASSERTION pattern at ~lines 258-272: `readFileSync(rendererPath,"utf8")` + regex on the .tsx source. recharts in JSDOM does NOT render dimension-dependent SVG, so axis props are NOT capturable via a spy — do NOT attempt a prop-capture mock.)
    - packages/web/src/components/charts/NumericLineRenderer.spec.tsx (same static-assertion harness)
    - packages/web/src/components/charts/WidgetRenderer.spec.tsx (existing bar test setup)
    - packages/web/src/lib/yAxisScale.spec.ts (the helper's runtime math — byte-identical `{}`, smart, log-clamp, no-positive — is ALREADY runtime-locked here in Plan 101-01; Task 4 does NOT re-test the math, only that the spread reaches the right axes)
  </read_first>
  <behavior>
    Task 4 uses the codebase's STATIC-SOURCE-ASSERTION pattern (readFileSync of the renderer .tsx + regex), NOT runtime recharts prop capture (JSDOM can't expose axis props — see RESEARCH/precedent). The mode math is already runtime-tested in `yAxisScale.spec.ts` (Plan 101-01). Per renderer (Timeline, NumericLine, bar via WidgetRenderer), assert structurally:
    - BYTE-IDENTICAL (YAXIS-V119-04): the renderer reads `cfg.yAxisScale` and derives axis props ONLY via `yAxisScaleProps(...)` (which returns `{}` when absent → empty spread → no domain/scale/allowDataOverflow). The structural guarantee is: the value axes receive `{...scaleProps}` (a spread) rather than a hardcoded `domain=`/`scale=` literal, so an unconfigured widget emits nothing. This IS the backward-compat lock (combined with the helper's runtime `{}`-for-absent test).
    - SMART/LOG reach every value axis: `{...scaleProps}` (or the helper's result var) appears on ALL value-axis elements and NOT on the `type="category"` axes.
  </behavior>
  <action>
    Extend the three existing renderer spec files (additive; keep pre-existing tests green). Follow the STATIC-SOURCE-ASSERTION pattern already in TimelineRenderer.spec.tsx (~258-272): `const src = readFileSync(<rendererPath>, "utf8")`, then assert with regex/`.match()`/`includes`. Do NOT add a recharts prop-capture spy (JSDOM limitation). Add cases:
      - `it("value axes derive scale props only via yAxisScaleProps (byte-identical when absent)")` — assert `src` imports/calls `yAxisScaleProps` and that each value-axis element carries the `{...scaleProps}` spread (count the spread occurrences === the number of value-axis branches). Assert NO hardcoded `domain={[0` / `scale="log"` literal is written directly on an axis element (props must come from the helper so absent→`{}`).
      - `it("scale-props spread reaches every value axis, not the category axis")` — assert the spread count matches the value-axis count (Timeline 4 branches, NumericLine 4, bar 2 = vertical `<YAxis>` + horizontal `<XAxis type="number">`); assert the `type="category"` axis element does NOT carry the spread.
      - `it("reads cfg.yAxisScale")` — assert the renderer references `yAxisScale` from config.
    The mode-output math (smart `['auto','auto']`, log `[posMin,'auto']`+`scale:'log'`+`allowDataOverflow`, no-positive→`{}`, absent→`{}`) is asserted at RUNTIME in `yAxisScale.spec.ts` (Plan 101-01) — reference it in a comment; do NOT duplicate that math here.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/TimelineRenderer.spec.tsx src/components/charts/NumericLineRenderer.spec.tsx src/components/charts/WidgetRenderer.spec.tsx src/lib/yAxisScale.spec.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 3 renderer spec files uses static-source assertion for the scale wiring: `grep -q "readFileSync" <specfile>` and asserts the `{...scaleProps}` (or helper-result) spread on value axes.
    - The spread count asserted per renderer matches its value-axis branch count (Timeline 4, NumericLine 4, bar 2); the `type="category"` axis is asserted to NOT receive the spread.
    - No hardcoded axis-domain/scale literal on axis elements — props flow through `yAxisScaleProps` (so absent config → `{}` → byte-identical). The helper's `{}`-for-absent / smart / log-clamp / no-positive math is runtime-locked in `src/lib/yAxisScale.spec.ts` (101-01) — `grep -q "yAxisScaleProps" src/lib/yAxisScale.spec.ts` exits 0.
    - `cd packages/web && npx vitest run src/components/charts/{TimelineRenderer,NumericLineRenderer,WidgetRenderer}.spec.tsx src/lib/yAxisScale.spec.ts` exits 0.
    - Full web suite green: `cd packages/web && npx vitest run` exits 0 (0 failed).
    - `cd packages/web && npx tsc --noEmit` clean; theme-guard green: `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` exits 0.
    - Zero server diff: `git diff --name-only packages/server | wc -l` = 0.
  </acceptance_criteria>
  <done>All 3 renderers have static-source assertions locking the yAxisScaleProps spread onto every value axis (not category) + reading cfg.yAxisScale; the mode math is runtime-locked in yAxisScale.spec.ts; full web suite + tsc + theme-guard green; zero server diff.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx vitest run` → 0 failures (full web suite).
- `cd packages/web && npx tsc --noEmit` → clean.
- `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` → green (no raw hex, only ds-select/ds-field classes).
- `git diff --name-only packages/server | wc -l` → 0 (FRONTEND-ONLY).
- Static: pie/calendar renderers not in files_modified — untouched.
- Sole-materialize-trigger unaffected: `grep -rE "materializeFilter|dropFilterView" packages/web/src/components/charts/TimelineRenderer.tsx packages/web/src/components/charts/NumericLineRenderer.tsx packages/web/src/components/charts/WidgetRenderer.tsx` unchanged from baseline (no new materialize calls added).
</verification>

<success_criteria>
- Designer can pick Zero-based / Smart / Logarithmic on timeline, numeric-line, and bar config (YAXIS-V119-01).
- Smart derives non-zero data-range bounds on all value axes (YAXIS-V119-02).
- Logarithmic renders value axis on a log scale with a clamped positive-min lower bound (YAXIS-V119-03).
- Absent scale mode → no axis props → byte-identical existing widgets, locked by a test per renderer (YAXIS-V119-04).
- Pie / calendar untouched; zero server diff; theme-tokens/classNames reused only.
</success_criteria>

<output>
After completion, create `.planning/phases/101-smart-logarithmic-y-axis/101-02-SUMMARY.md`.
</output>
