---
phase: 80-token-foundation-aurora-default-theme
plan: 02
type: execute
wave: 2
depends_on: ["80-01"]
files_modified:
  - packages/web/package.json
  - packages/web/src/main.tsx
  - packages/web/index.html
  - packages/web/src/styles/global.css
autonomous: false
requirements: [THEME-V116-01, THEME-V116-02, TOKENS-V116-04]
must_haves:
  truths:
    - "Both fonts are self-hosted via @fontsource-variable (no Google Fonts CDN request at runtime)"
    - "Body text renders in Manrope Variable; display headings render in Space Grotesk Variable"
    - "The app background shows the hex-mesh + aurora-glow treatment in dark mode and a much subtler branded version in light mode"
    - "Accent-colored text/icons use --accent-text (not --accent fills) so they stay readable on both backgrounds"
  artifacts:
    - path: "packages/web/src/main.tsx"
      provides: "Self-hosted font imports before global.css"
      contains: "@fontsource-variable/manrope"
    - path: "packages/web/index.html"
      provides: "No Google Fonts links; FOUC guard preserved"
    - path: "packages/web/src/styles/global.css"
      provides: "Font tokens point at Variable family names; hex-mesh + glow body treatment; --accent-text applied to accent text surfaces"
      contains: "Manrope Variable"
  key_links:
    - from: "main.tsx font imports"
      to: "global.css --font-body / --font-display"
      via: "@fontsource-variable Variable family names"
      pattern: "Manrope Variable"
    - from: "accent text surfaces (numbers/links/icons)"
      to: "--accent-text token"
      via: "color: var(--accent-text)"
      pattern: "var\\(--accent-text\\)"
---

<objective>
Ship the Aurora visual polish on top of the token foundation: self-host both fonts (removing the Google Fonts CDN), wire `--font-body`/`--font-display` to the Variable family names, apply Space Grotesk to display headings, add the hex-mesh + aurora-glow body treatment (dark vivid / light subtle), and apply the two-tier accent `--accent-text` to accent-colored text/numbers/icons across the app.

Purpose: This makes the app actually LOOK like the approved Aurora mockup — the distinctive, branded, glassmorphic command-center feel — not just token-clean. Self-hosting fonts also closes the GDPR/air-gap gap.
Output: GDPR-safe self-hosted fonts, Space Grotesk display type, the signature hex-mesh + glow background, and readable two-tier accent text in both modes.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/design/CHOSEN-DIRECTION.md
@.planning/design/direction-A-aurora.html
@.planning/phases/80-token-foundation-aurora-default-theme/80-CONTEXT.md
@.planning/phases/80-token-foundation-aurora-default-theme/80-RESEARCH.md
@packages/web/index.html
@packages/web/src/main.tsx
@packages/web/src/styles/global.css

<notes>
- This plan depends on 80-01 (the token vocabulary + `--font-body`/`--font-display`/`--accent-text` must already exist).
- The visual source of truth for dark mode is `.planning/design/direction-A-aurora.html` — OPEN IT and match the hex-mesh density, glow positions/colors, compact spacing, and Space Grotesk display usage.
- FONT GOTCHA (RESEARCH Pitfall 1 + Pattern 1): the variable packages register family names `"Manrope Variable"` and `"Space Grotesk Variable"` — NOT `"Manrope"`/`"Space Grotesk"`. Every font-family reference must use the Variable name or the browser silently falls back to the system font.
- Confirmed versions (RESEARCH): `@fontsource-variable/manrope` 5.2.8, `@fontsource-variable/space-grotesk` 5.2.10.
- The import goes in `main.tsx` BEFORE `./styles/global.css` (so @font-face is registered before the cascade). Use the `/wght.css` variable entrypoint.
- index.html currently has 3 Google Fonts lines (preconnect googleapis, preconnect gstatic, the Manrope stylesheet `<link>` at lines 18-23) — REMOVE all three. Do NOT touch the inline FOUC `<script>` (lines 7-17) — it stays verbatim.
- This plan is autonomous:false because it ends in a human-verify checkpoint (visual fidelity to the mockup can't be asserted by a command).
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Self-host both fonts (fontsource) + wire Variable family names + remove Google Fonts CDN</name>
  <files>packages/web/package.json, packages/web/src/main.tsx, packages/web/index.html, packages/web/src/styles/global.css</files>
  <read_first>
    - packages/web/src/main.tsx (the import block — fonts go before global.css)
    - packages/web/index.html (lines 18-23 = the 3 Google Fonts links to remove; lines 7-17 = FOUC guard to KEEP)
    - packages/web/src/styles/global.css (the --font-body / --font-display tokens from 80-01, plus the body font-family ref at ~:2025)
    - .planning/phases/80-token-foundation-aurora-default-theme/80-RESEARCH.md (Pattern 1: Font Loading Migration — exact import lines + family names; Pitfall 1 + Pitfall 5)
  </read_first>
  <action>
    1. Install the self-hosted variable font packages (run from packages/web so they resolve in the web workspace):
       `npm install @fontsource-variable/manrope@5.2.8 @fontsource-variable/space-grotesk@5.2.10`
       (workspace install lands them in the root node_modules — that's expected.)

    2. In `packages/web/src/main.tsx`, add these two imports ABOVE the existing `import "./styles/global.css";`:
       ```ts
       import "@fontsource-variable/manrope/wght.css";
       import "@fontsource-variable/space-grotesk/wght.css";
       ```

    3. In `packages/web/index.html`, REMOVE the three Google Fonts lines (the `<link rel="preconnect" ... fonts.googleapis.com>`, the `<link rel="preconnect" ... fonts.gstatic.com crossorigin>`, and the `<link href="https://fonts.googleapis.com/css2?family=Manrope...">` stylesheet). Leave the inline FOUC `<script>` block UNTOUCHED.

    4. In `packages/web/src/styles/global.css`, update the font tokens (defined in 80-01) to the Variable family names:
       ```css
       --font-body: "Manrope Variable", "Segoe UI", system-ui, -apple-system, sans-serif;
       --font-display: "Space Grotesk Variable", "Segoe UI", system-ui, -apple-system, sans-serif;
       ```
       And update any remaining literal `"Manrope"` body font-family references (e.g. the rule near :2025) to `var(--font-body)`. Do NOT change the monospace/`"JetBrains Mono"` code-editor stacks.
  </action>
  <verify>
    <automated>cd packages/web && grep -q '@fontsource-variable/manrope/wght.css' src/main.tsx && grep -q '@fontsource-variable/space-grotesk/wght.css' src/main.tsx && ! grep -q 'fonts.googleapis' index.html && ! grep -q 'fonts.gstatic' index.html && grep -q '"Manrope Variable"' src/styles/global.css && grep -q '"Space Grotesk Variable"' src/styles/global.css && grep -q 'data-theme' index.html && echo FONTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep '@fontsource-variable/manrope/wght.css' src/main.tsx` and `@fontsource-variable/space-grotesk/wght.css` both present, ABOVE the global.css import.
    - `packages/web/package.json` lists both `@fontsource-variable/manrope` and `@fontsource-variable/space-grotesk` in dependencies.
    - `grep 'fonts.googleapis' index.html` and `grep 'fonts.gstatic' index.html` return NOTHING (all 3 Google Fonts lines removed).
    - The inline FOUC guard is intact: `grep 'data-theme' index.html` still matches the `setAttribute("data-theme"...)` script.
    - `grep '"Manrope Variable"' src/styles/global.css` and `grep '"Space Grotesk Variable"' src/styles/global.css` both present in the font tokens.
    - `npx tsc --noEmit` clean; `npm run build` (or `vite build`) succeeds — confirms Vite bundles the WOFF2 files (no broken import).
  </acceptance_criteria>
  <done>Both fonts self-hosted via fontsource; no Google Fonts CDN reference remains; font tokens reference the Variable family names; FOUC guard preserved; build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Space Grotesk display type + hex-mesh/aurora-glow body treatment + two-tier --accent-text</name>
  <files>packages/web/src/styles/global.css</files>
  <read_first>
    - .planning/design/direction-A-aurora.html (VISUAL SOURCE OF TRUTH — open in a browser: copy the hex-mesh SVG/gradient technique, glow radial positions+colors, which headings use the display font)
    - packages/web/src/styles/global.css (the body background rule + :root[data-theme="light"] body rule from 80-01; heading/title selectors; any accent-colored text)
    - .planning/design/CHOSEN-DIRECTION.md (Background = hex mesh + aurora glows; Type = Manrope body + Space Grotesk display; two-tier accent rule)
    - .planning/phases/80-token-foundation-aurora-default-theme/80-CONTEXT.md (light mode = KEEP hex mesh + faint violet glow, much subtler)
  </read_first>
  <action>
    1. DISPLAY FONT: Apply `font-family: var(--font-display)` to display surfaces — major headings (page titles, the wordmark/app name in the topbar, big-number widget values, section headers / modal titles). Match the mockup's usage — body/labels/inputs stay `--font-body`; display is for headline-scale type only. Use `--text-2xl`/`--text-xl` + `--font-weight-semibold/bold` + `--tracking-tight` per the compact mockup.

    2. HEX-MESH + AURORA-GLOW BODY: Recreate the mockup's background on `body`. Layer (back-to-front): `var(--bg)` base, the aurora radial glows (violet/blue/magenta `radial-gradient`s already stubbed in 80-01 — tune positions/colors to match the mockup), and a hex-mesh layer (the mockup technique — typically a repeating hexagon SVG as a `background-image` data-URI or a CSS pattern at low opacity). The hex-mesh is a fixed structural treatment (not brandable). For the `:root[data-theme="light"] body` rule: KEEP the hex mesh + a faint violet glow wash at MUCH lower opacity (per CONTEXT — subtle, still branded, not a flat light theme). If the hex SVG is a data-URI, it may contain a stroke color — keep it neutral/token-driven or pragma-justify it for the 80-03 guard. Use `background-attachment: fixed` if the mockup does, so the mesh doesn't scroll with content.

    3. TWO-TIER ACCENT TEXT: Audit accent-colored TEXT/numbers/icons and switch them from `var(--accent)` to `var(--accent-text)`. Targets: any `color: var(--accent)` on text/links/icons, big-number/metric values that use the accent, `.text-accent` utility (point it at `--accent-text`), active nav labels, accent-colored counts/badges-as-text. LEAVE `--accent` on FILLS (button backgrounds, swatch fills, focus-ring borders, the logo bars, `background`/`border-color` uses). This is the rule that makes accent text readable on both the near-black dark bg and the off-white light bg. Verify the violet numbers that were unreadable (the bug caught during mockup iteration) now use `--accent-text`.

    Keep everything token-driven — no new raw hex (the 80-03 guard will enforce this). Any unavoidable literal (e.g. a hex-mesh SVG stroke) gets a `/* theme-guard-ignore: <reason> */` pragma added here or in 80-03.
  </action>
  <verify>
    <automated>cd packages/web && grep -q 'var(--font-display)' src/styles/global.css && grep -q 'var(--accent-text)' src/styles/global.css && grep -qiE 'hex|hexagon|radial-gradient' src/styles/global.css && npx tsc --noEmit && echo POLISH_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'var(--font-display)' src/styles/global.css` present on heading/title/big-number selectors.
    - `grep 'var(--accent-text)' src/styles/global.css` present; the `.text-accent` utility resolves to `--accent-text` (not `--accent`); no accent-colored TEXT still reads `color: var(--accent)`.
    - The `body` rule layers `var(--bg)` + aurora `radial-gradient`s + a hex-mesh layer; the `:root[data-theme="light"] body` rule keeps the mesh + a fainter glow.
    - `npx tsc --noEmit` clean; `npx vitest run` existing tests green; existing hex theme-guard green (any hex-mesh SVG literal is pragma-justified).
  </acceptance_criteria>
  <done>Display headings render Space Grotesk; the signature hex-mesh + aurora-glow background is live (vivid dark / subtle light); accent text/numbers/icons use the readable `--accent-text` while fills keep `--accent`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of Aurora dark + light against the mockup</name>
  <action>Human-verify checkpoint — verify the live app against the approved mockup per the steps below (no automated action).</action>
  <what-built>
    Aurora visual polish: self-hosted Manrope + Space Grotesk (no Google CDN), Space Grotesk display headings, the hex-mesh + aurora-glow background, and the two-tier `--accent-text` rule applied to accent text — in both dark and light mode.
  </what-built>
  <how-to-verify>
    1. Run the web app (`npm run dev` from packages/web) and open it in a browser, side-by-side with `.planning/design/direction-A-aurora.html`.
    2. DARK MODE: confirm the near-black bg shows the hex-mesh + violet/blue/magenta aurora glows; panels look glassmorphic; headings use Space Grotesk; body uses Manrope; spacing is compact — matching the mockup.
    3. Open DevTools > Network, hard-reload: confirm NO request to `fonts.googleapis.com` / `fonts.gstatic.com`. In the Elements > Computed font panel, confirm body = "Manrope Variable" and a heading = "Space Grotesk Variable" (not a system fallback).
    4. Toggle to LIGHT MODE: confirm warm off-white panels (not pure white), the hex mesh + a much subtler violet glow still present, and accent numbers/links are the DARKER readable violet (not the unreadable bright fill violet).
    5. Spot-check accent text readability in both modes (the previously-unreadable violet numbers must now be legible).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual gaps vs the mockup (mesh density, glow color/position, font fallback, accent-text readability).</resume-signal>
</task>

</tasks>

<verification>
- Fonts self-hosted; zero Google Fonts network requests; computed fonts are the Variable families (not system fallback).
- Hex-mesh + aurora-glow background matches the mockup in dark; subtler-but-present in light.
- Display headings use Space Grotesk; body uses Manrope.
- Two-tier accent: accent text/numbers/icons use `--accent-text` (readable both modes); fills keep `--accent`.
- `npx tsc --noEmit` + `npx vitest run` + existing hex guard all green; `vite build` succeeds.
</verification>

<success_criteria>
The app visually matches the approved Aurora baseline in dark mode and presents a coherent warm-branded light mode; fonts are GDPR-safe self-hosted; the two-tier accent keeps accent text readable in both modes. (THEME-V116-01 full visual fidelity, THEME-V116-02 light face, TOKENS-V116-04 accent-text applied.)
</success_criteria>

<output>
After completion, create `.planning/phases/80-token-foundation-aurora-default-theme/80-02-SUMMARY.md`.
</output>
