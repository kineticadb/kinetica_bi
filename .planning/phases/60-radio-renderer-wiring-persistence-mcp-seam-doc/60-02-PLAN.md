---
phase: 60-radio-renderer-wiring-persistence-mcp-seam-doc
plan: 02
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - packages/web/src/components/charts/RadioGroupRenderer.tsx
  - packages/web/src/components/charts/RadioGroupRenderer.spec.tsx
  - packages/web/src/components/charts/WidgetRenderer.tsx
  - packages/web/src/components/charts/definitions/radio-group.ts
autonomous: true
requirements: [RADIO-V111-03]

must_haves:
  truths:
    - "On dashboard open the renderer applies the configured defaultOptionId's action transiently (neutral if none)"
    - "Selecting an option applies its action LIVE — the target widget/layer/dv updates in the same render cycle with no remount and no PATCH"
    - "Re-selecting a different option REPLACES this control's contribution — fields the prior option set that the new one does not revert to baseline (ties to the 60-01 store refactor)"
    - "On dashboard reload/unmount the overlay clears (reset) — a viewer's runtime click does NOT persist; the configured default re-applies on open"
    - "The renderer reads widget.config LIVE in the component body and applies via an effect keyed on the resolved selectedOptionId — NEVER a mount snapshot"
    - "A dangling target / rejected action surfaces the Phase 58 typed no-op + toast — no crash"
    - "The renderer is statically decoupled from filter/materialize (no filter-store imports, no filterVersion) — DataFilterRenderer precedent"
  artifacts:
    - path: "packages/web/src/components/charts/RadioGroupRenderer.tsx"
      provides: "Radio group runtime: renders options (vertical/horizontal) + optional title; default-on-open; select→applyWidgetAction(controlId=widget.id)"
      min_lines: 60
    - path: "packages/web/src/components/charts/RadioGroupRenderer.spec.tsx"
      provides: "default-on-open, select→live apply (derived overlay / no remount), switch reverts unset fields, dangling/rejected toast, reset clears, orientation/title render, decoupling grep"
      contains: "switch"
  key_links:
    - from: "packages/web/src/components/charts/WidgetRenderer.tsx"
      to: "packages/web/src/components/charts/RadioGroupRenderer.tsx"
      via: "effectiveWidget.type === 'radiogroup' dispatch branch"
      pattern: "radiogroup"
    - from: "packages/web/src/components/charts/RadioGroupRenderer.tsx"
      to: "packages/web/src/components/DashboardContext.tsx"
      via: "useDashboardContext().applyWidgetAction(action, widget.id)"
      pattern: "applyWidgetAction"
    - from: "packages/web/src/components/charts/definitions/radio-group.ts"
      to: "packages/web/src/components/charts/RadioGroupRenderer.tsx"
      via: "runtime renderer dispatch (radiogroup short-circuits in WidgetRenderer)"
      pattern: "radiogroup"
---

<objective>
Build the `RadioGroupRenderer` runtime: it reads the Phase 59 `RadioGroupConfig` LIVE at render time, applies the configured default option on mount, and on viewer select fires that option's action through `applyWidgetAction` (control id = the radio widget's id) so the target updates LIVE with no remount and no PATCH. Wire it into the `radiogroup` registry-def runtime and the `WidgetRenderer` dispatch.

Purpose: This is the v1.11 payoff — a viewer clicks a radio option and the map LIVE-switches render mode / class-break config with no permission friction and no mutation of the shared dashboard (RADIO-V111-03).

Output: `RadioGroupRenderer.tsx` + spec, a `WidgetRenderer` dispatch branch, and registry-def wiring. The `DashboardContext.applyWidgetAction` signature `(action, controlId)` is a 60-01 OUTPUT this plan CONSUMES — 60-02 does not edit DashboardContext.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-radio-renderer-wiring-persistence-mcp-seam-doc/60-CONTEXT.md
@.planning/phases/60-radio-renderer-wiring-persistence-mcp-seam-doc/60-01-PLAN.md

<interfaces>
From packages/web/src/lib/radioGroupConfig.ts (the config the renderer consumes):
```typescript
export type RadioOrientation = "vertical" | "horizontal";
export type RadioOption = { id: string; label: string; action: WidgetAction };
export type RadioGroupConfig = {
  title?: string;
  orientation: RadioOrientation;
  defaultOptionId?: string;
  options: RadioOption[];
};
```

From packages/web/src/components/DashboardContext.tsx (widened by 60-01 — CONSUME, do NOT edit here):
```typescript
// applyWidgetAction now takes (action, controlId)
applyWidgetAction: (action: WidgetAction, controlId: number) => WidgetActionResult;
const { applyWidgetAction } = useDashboardContext();
```

From packages/web/src/lib/widgetAction.ts:
```typescript
export type WidgetAction = { target: { kind: "widget"|"layer"|"dynamicView"; id: number }; configPatch: Record<string, unknown> };
export type WidgetActionResult = { status: "applied"|"target_not_found"|"rejected"; ... };
```

WidgetRenderer dispatch precedent (WidgetRenderer.tsx ~270, datafilter branch):
```tsx
} else if (effectiveWidget.type === "datafilter") {
  body = <DataFilterRenderer widget={effectiveWidget} tables={tables} />;
}
```

DataFilterRenderer decoupling precedent (DataFilterRenderer.tsx header): renderer NEVER imports the materialize function from client.ts; sole-materialize-trigger invariant comment block at top of file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RadioGroupRenderer.tsx — live config read, default-on-open, select→applyWidgetAction, decoupled, theme tokens</name>
  <files>packages/web/src/components/charts/RadioGroupRenderer.tsx</files>
  <read_first>
    - packages/web/src/lib/radioGroupConfig.ts (RadioGroupConfig/RadioOption — full file)
    - packages/web/src/components/charts/DataFilterRenderer.tsx (renderer scaffold + sole-materialize-decoupling comment block + theme-token class usage like ds-input/btn-primary)
    - packages/web/src/components/DashboardContext.tsx (useDashboardContext + applyWidgetAction signature widened in 60-01 — read to confirm the (action, controlId) shape; do NOT edit)
    - packages/web/src/components/charts/WidgetRenderer.tsx (lines 228-296 — effectiveWidget + dispatch-by-type; how widget is threaded)
    - .claude/skills (theme tokens / green accent — if present)
  </read_first>
  <behavior>
    - Renders nothing-crashing empty state when options.length === 0 (config hint), like DataFilterRenderer's empty gate.
    - Reads `widget.config` (cast to RadioGroupConfig) LIVE in the component body each render — orientation, title, defaultOptionId, options.
    - selectedOptionId is component-local transient useState (Claude's discretion per CONTEXT); initialized to defaultOptionId (or first option, or undefined if none).
    - Default-on-open: an effect keyed on the RESOLVED selectedOptionId (NOT a mount-only []), calls applyWidgetAction(option.action, widget.id) for the resolved option. On mount with a default → that option's action applies transiently. Neutral (no dispatch) if no resolved option.
    - On select: setSelectedOptionId(optionId) → the effect re-fires → applyWidgetAction(option.action, widget.id) → live target update, no remount, no PATCH. Re-selecting a different option REPLACES this control's contribution (handled by the 60-01 store via controlId).
    - Dangling/rejected: applyWidgetAction returns target_not_found/rejected and already fires the toast (Phase 58); the renderer does not crash — it still shows the option as selected (the toast is the feedback).
    - Renders options as radio inputs (role="radio" or native radios) honoring orientation (vertical/horizontal via a class), optional title, active-option indication. Theme tokens only, green accent (no hardcoded hex).
  </behavior>
  <action>
    Create `RadioGroupRenderer.tsx`. Props: `{ widget: WidgetDto }`.

    Copy the SOLE-MATERIALIZE-TRIGGER + ACTION-ENGINE decoupling comment block style from DataFilterRenderer's header verbatim-in-spirit: state that this file NEVER imports filter-store symbols (materializeFilter/dropFilterView/addFilter/setBulkFilters/filterVersion) and never bumps filterVersion — it is a pure action-engine consumer.

    Read config live: `const cfg = (widget.config ?? {}) as unknown as RadioGroupConfig;` then `orientation`, `title`, `options`, `defaultOptionId`.

    selectedOptionId: `useState<string | undefined>(() => defaultOptionId ?? options[0]?.id)`. (component-local transient — do NOT persist to config).

    Consume the widened context dispatch: `const { applyWidgetAction } = useDashboardContext();` (signature `(action, controlId) => WidgetActionResult`, widened by 60-01).

    Effect keyed on `[selectedOptionId]` (read options + applyWidgetAction via refs or include in deps carefully to avoid stale closure — read widget.config live in the effect body): resolve the option by id; if found, call `applyWidgetAction(option.action, widget.id)`.

    NEVER snapshot config at mount — the effect MUST re-read the current options each run (read from `widget.config` live, or from a ref synced each render) so a config edit + live re-render is reflected (read-once-at-mount trap; GAP-24-01-A / 54-01..09 / 58.1 lineage).

    Render: optional `<div className="...title...">{title}</div>`; a container with an orientation class (e.g. `radiogroup--vertical`/`radiogroup--horizontal`); for each option a label + radio input with `checked={selectedOptionId === opt.id}`, `onChange={() => setSelectedOptionId(opt.id)}`, `aria-label={opt.label}`. Use existing theme-token classes (accent checkbox/radio convention, green accent) — NO hardcoded colors. Add a stable `data-testid="radiogroup-renderer"` and per-option `data-testid={`radiogroup-option-${opt.id}`}`.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/RadioGroupRenderer.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "useDashboardContext\|applyWidgetAction(.*widget.id)" packages/web/src/components/charts/RadioGroupRenderer.tsx` shows the dispatch threads widget.id as controlId.
    - `grep -n "materializeFilter\|setBulkFilters\|addFilter\|filterVersion\|useFilterStore" packages/web/src/components/charts/RadioGroupRenderer.tsx` returns NOTHING (decoupled).
    - `grep -n "useState\|useEffect" packages/web/src/components/charts/RadioGroupRenderer.tsx` shows selectedOptionId state + a non-empty-dep effect (NOT `[]`-only mount snapshot).
    - `grep -nE "#[0-9a-fA-F]{3,8}" packages/web/src/components/charts/RadioGroupRenderer.tsx` returns NOTHING (theme tokens only, no hardcoded hex).
    - `cd packages/web && npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>RadioGroupRenderer reads config live, applies default on open, dispatches select via applyWidgetAction(controlId=widget.id), decoupled, theme-token styled; spec green; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire radiogroup into WidgetRenderer dispatch + registry runtime; write RadioGroupRenderer spec</name>
  <files>packages/web/src/components/charts/WidgetRenderer.tsx, packages/web/src/components/charts/definitions/radio-group.ts, packages/web/src/components/charts/RadioGroupRenderer.spec.tsx</files>
  <read_first>
    - packages/web/src/components/charts/WidgetRenderer.tsx (lines 254-296 — the dispatch-by-type chain; add a radiogroup branch alongside datafilter)
    - packages/web/src/components/charts/definitions/radio-group.ts (registry def — note Phase 60 ships the runtime renderer; reconcile any renderer/runtime pointer)
    - packages/web/src/components/charts/actionEngine.canary.spec.tsx (CASE A pattern — DashboardContextProvider with applyWidgetAction prop; mounting WidgetRenderer; using the real widgetActionStore + derived overlays for assertions. NOTE: 60-01 rewrote this spec to the setControlContribution API — read the post-60-01 version, not a stale mental model)
    - packages/web/src/components/DashboardContext.tsx (CONSUME the widened (action, controlId) signature — confirm how the provider applyWidgetAction prop is supplied for the spec harness; do NOT edit DashboardContext)
    - packages/web/src/components/charts/DataFilterRenderer.tsx (decoupling Test precedent — readFileSync source-grep style in its spec, mirror for the renderer's decoupling assertion)
  </read_first>
  <action>
    Add a `radiogroup` branch to `WidgetRenderer` (alongside the datafilter branch, before the AggregatedWidgetRenderer fallback): `} else if (effectiveWidget.type === "radiogroup") { body = <RadioGroupRenderer widget={effectiveWidget} />; }`. Import RadioGroupRenderer at top.

    Reconcile the registry def `radio-group.ts`: the def already declares the type + CustomConfigPanel; the RUNTIME is dispatched by WidgetRenderer (mirrors datafilter/legend which also short-circuit in WidgetRenderer, not via a registry `renderer` field). Add/keep the comment noting Phase 60 wired the runtime via WidgetRenderer. No registry interface change needed if datafilter follows the same WidgetRenderer-dispatch pattern — confirm and document.

    DashboardContext is NOT edited here — 60-01 already widened `applyWidgetAction` to `(action, controlId) => WidgetActionResult`. This plan READS that widened signature (verify via the read_first DashboardContext + actionEngine.canary.spec) and CONSUMES it through `useDashboardContext().applyWidgetAction(action, widget.id)`.

    Write `RadioGroupRenderer.spec.tsx` mirroring the canary spec infra (DashboardContextProvider with a real `applyWidgetAction` closure that calls the real `applyWidgetAction(action, lookups, controlId)` against the real widgetActionStore; assert at the DERIVED-overlay store boundary). Cover:
      1. default-on-open: mount with defaultOptionId set → after mount, the derived widgetOverrides/layerOverrides reflect the default option's patch (applied transiently).
      2. select→live apply: fire change on a second option → derived overlay reflects the new option's patch; assert the renderer DOM node identity is unchanged (no remount).
      3. switch-replace reverts unset fields: option A sets { renderMode + cb_config } on a layer; select option B that sets only { renderMode } → derived layerOverrides[targetId] no longer contains cb_config (reverted to baseline). Ties to 60-01.
      4. dangling/rejected: option targets a non-existent layer → applyWidgetAction returns target_not_found, toast fires (spy useToastStore.showToast), renderer does not crash.
      5. reset clears: after a selection writes an overlay, call useWidgetActionStore.getState().reset() → derived overlays empty (reload/unmount semantics; viewer click does not persist).
      6. orientation + title render: vertical and horizontal produce the expected class; title renders when set.
      7. decoupling grep: readFileSync RadioGroupRenderer.tsx and assert no `materializeFilter`/`setBulkFilters`/`addFilter`/`filterVersion`/`dashboardLayersStore` import strings (DataFilterRenderer Test-14 precedent).
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/components/charts/RadioGroupRenderer.spec.tsx src/components/charts/actionEngine.canary.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "radiogroup" packages/web/src/components/charts/WidgetRenderer.tsx` shows the dispatch branch; `grep -n "RadioGroupRenderer" packages/web/src/components/charts/WidgetRenderer.tsx` shows the import.
    - RadioGroupRenderer.spec.tsx contains all 7 cases — `grep -n "default-on-open\|switch\|revert\|target_not_found\|reset\|orientation\|decoupl" packages/web/src/components/charts/RadioGroupRenderer.spec.tsx` shows the switch-replace + dangling + reset + decoupling cases present.
    - `cd packages/web && npx vitest run src/components/charts/RadioGroupRenderer.spec.tsx` green.
    - `cd packages/web && npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>radiogroup dispatched by WidgetRenderer to RadioGroupRenderer; registry runtime reconciled; renderer spec covers default/select/switch-replace/dangling/reset/orientation/decoupling — green; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Full deterministic gate — vitest 100% green + web tsc clean + zero server diff</name>
  <files>packages/web (full vitest + tsc gate; no new files)</files>
  <read_first>
    - packages/web/src/components/charts/definitions/index.ts (confirm registerRadioGroup still registered)
  </read_first>
  <action>
    Run the full frontend deterministic gate after wiring the renderer. Fix any spec broken by the radiogroup dispatch branch or the DashboardContext signature change. Confirm NO server files changed.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/web && npx vitest run` → 0 failures (100% green; >= 1914 baseline + new renderer specs).
    - `cd packages/web && npx tsc --noEmit` → clean.
    - `git status --porcelain packages/server` returns NOTHING (zero server diff).
  </acceptance_criteria>
  <done>Frontend vitest 100% green, web tsc clean, zero server changes.</done>
</task>

</tasks>

<verification>
- Default option applies on dashboard open (transient); viewer select applies LIVE with no remount, no PATCH; switch-replace reverts unset fields; reset clears on reload/unmount.
- Renderer reads config live (effect keyed on resolved selectedOptionId, not a mount snapshot); dangling/rejected → typed no-op + toast, no crash; statically decoupled from filter/materialize.
- radiogroup wired into WidgetRenderer dispatch + registry runtime reconciled.
- Frontend vitest 100% green; web tsc clean; zero server diff.
</verification>

<success_criteria>
- `cd packages/web && npx vitest run` 100% green.
- `cd packages/web && npx tsc --noEmit` clean.
- RadioGroupRenderer exists, dispatched by WidgetRenderer, applies default-on-open + live select via applyWidgetAction(controlId=widget.id), decoupled, theme-token styled.
- No server changes.
</success_criteria>

<output>
After completion, create `.planning/phases/60-radio-renderer-wiring-persistence-mcp-seam-doc/60-02-SUMMARY.md`
</output>
