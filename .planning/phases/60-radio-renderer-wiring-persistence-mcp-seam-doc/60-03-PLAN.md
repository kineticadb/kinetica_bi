---
phase: 60-radio-renderer-wiring-persistence-mcp-seam-doc
plan: 03
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - packages/web/docs/mcp-action-seam.md
  - packages/web/src/lib/applyWidgetAction.ts
  - packages/web/src/lib/applyWidgetAction.spec.ts
autonomous: true
requirements: [SEAM-V111-01]

must_haves:
  truths:
    - "A markdown doc documents applyWidgetAction + the { target, configPatch } envelope as the future AI/MCP hook"
    - "The doc names the allow-list as the safety boundary an AI/MCP layer is bound by (ALLOW_LIST_VERSION)"
    - "The doc gives the concrete MCP tool shape: the zod WidgetActionSchema as inputSchema; an MCP tool calls the EXISTING PATCH /api/widgets/:id + layer/dynamic-view PATCH routes"
    - "The doc states NO AI widget and NO MCP server are built this milestone"
    - "applyWidgetAction carries a code-comment pointer to the seam doc"
    - "ZERO server changes — this is a docs deliverable; its test is existence + content asserts, not a runtime test"
  artifacts:
    - path: "packages/web/docs/mcp-action-seam.md"
      provides: "The MCP/AI seam design doc: where applyWidgetAction lives, the envelope contract, allow-list safety boundary, concrete MCP tool shape (inputSchema + existing PATCH routes), explicit NOT-BUILT scope"
      contains: "inputSchema"
    - path: "packages/web/src/lib/applyWidgetAction.spec.ts"
      provides: "An existence + content-assert test for the seam doc (readFileSync the markdown, assert required sections present)"
      contains: "mcp-action-seam"
  key_links:
    - from: "packages/web/src/lib/applyWidgetAction.ts"
      to: "packages/web/docs/mcp-action-seam.md"
      via: "code-comment pointer in the module header"
      pattern: "mcp-action-seam"
---

<objective>
Document `applyWidgetAction` + the `{ target, configPatch }` envelope as the hook a future AI chat widget / MCP server reuses (SEAM-V111-01). Produce a markdown doc covering the contract, the allow-list as the safety boundary, and the concrete MCP tool shape (the zod schema as `inputSchema`; an MCP tool calls the EXISTING PATCH routes), plus a code-comment pointer at `applyWidgetAction`. Build NO AI, NO MCP server.

Purpose: This is the bridge to the deferred AI milestone (AI-V2-01/02) — the envelope + allow-list are framed as exactly what an AI/MCP tool emits, so the future server can be built on top without re-architecting the contract.

Output: `packages/web/docs/mcp-action-seam.md`, a code-comment pointer in `applyWidgetAction.ts`, and an existence/content-assert test. DOC-ONLY — zero runtime behavior, zero server changes.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-radio-renderer-wiring-persistence-mcp-seam-doc/60-CONTEXT.md

<interfaces>
The seam to document (do NOT change its behavior — DOC only; the code-comment pointer is the only code edit):

From packages/web/src/lib/widgetAction.ts:
```typescript
export const WidgetActionTargetSchema = z.object({ kind: z.enum(["widget","layer","dynamicView"]), id: z.number().int().positive() });
export const WidgetActionSchema = z.object({ target: WidgetActionTargetSchema, configPatch: z.record(z.unknown()) });
export type WidgetAction = z.infer<typeof WidgetActionSchema>;   // { target, configPatch }
```

From packages/web/src/lib/actionAllowList.ts:
```typescript
export const ALLOW_LIST_VERSION = "v2" as const;
export function validateActionPatch(kind, widgetType, configPatch): { valid: true } | { valid: false; reasons: string[] };
export function getFieldLocation(kind, widgetType, field): "layer.config" | "layer" | "widget.config" | null;
```

From packages/web/src/lib/applyWidgetAction.ts:
```typescript
export function applyWidgetAction(action: WidgetAction, lookups: ActionLookups, controlId: number): WidgetActionResult;
// TRANSIENT-ONLY in-app; an MCP tool would instead call the existing PATCH routes for persistence.
```

Existing server routes that an MCP tool would call (server surface ALREADY exists — do NOT add routes):
- PATCH /api/widgets/:id   (widget.config persistence)
- the existing dashboard-layer PATCH route (layer config incl. top-level track_config/cb_config)
- the existing dynamic-view PATCH route (dynamic-view config)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write packages/web/docs/mcp-action-seam.md + code-comment pointer in applyWidgetAction.ts</name>
  <files>packages/web/docs/mcp-action-seam.md, packages/web/src/lib/applyWidgetAction.ts</files>
  <read_first>
    - packages/web/src/lib/applyWidgetAction.ts (the seam entry — module header where the pointer goes; full file)
    - packages/web/src/lib/widgetAction.ts (the WidgetAction/WidgetActionSchema envelope — the inputSchema source)
    - packages/web/src/lib/actionAllowList.ts (lines 1-70 + 194-270 — ALLOW_LIST_VERSION, validateActionPatch, getFieldLocation — the safety boundary)
    - .planning/phases/60-radio-renderer-wiring-persistence-mcp-seam-doc/60-CONTEXT.md (the MCP-seam decision section — design + document ONLY)
  </read_first>
  <action>
    Create `packages/web/docs/mcp-action-seam.md` (create the `packages/web/docs/` directory). Sections:

    1. **Title + scope banner**: "AI / MCP Action Seam (design + documentation only)" — state explicitly NO AI widget and NO MCP server are built in v1.11; this documents the hook a future AI-V2-01 / AI-V2-02 reuses.
    2. **Where it lives**: `applyWidgetAction` in `packages/web/src/lib/applyWidgetAction.ts` is the single dispatch entry; the envelope is `WidgetAction` in `packages/web/src/lib/widgetAction.ts`.
    3. **The contract (envelope)**: show the `{ target: { kind, id }, configPatch }` shape; note it is fully serializable (no closures/refs) — survives JSON round-trip — which is precisely why an AI/MCP agent can emit it.
    4. **The safety boundary (allow-list)**: `validateActionPatch` + `ALLOW_LIST_VERSION` ("v2") gate which fields are patchable per target kind / widget type; permanently-blocked meta/proto keys; `getFieldLocation` as the single source of truth for where a field lives. State that the allow-list IS the contract an AI/MCP layer is bound by — no free-form Object.assign.
    5. **Concrete MCP tool shape**: the zod `WidgetActionSchema` is used directly as the MCP tool `inputSchema`. Give a concrete tool sketch (e.g. an `apply_widget_action` tool) whose handler validates with the allow-list then calls the EXISTING PATCH routes for persistence:
         - widget target → `PATCH /api/widgets/:id`
         - layer target → the existing dashboard-layer PATCH route (incl. top-level track_config/cb_config)
         - dynamic-view target → the existing dynamic-view PATCH route
       Make clear: NO new routes, no WebSocket, no action-log table — the existing server surface suffices.
    6. **In-app vs MCP difference**: in-app `applyWidgetAction` is TRANSIENT (session overlay, no PATCH); an MCP/AI persistence path would call the PATCH routes. Same envelope + same allow-list, different sink.

    Add a code-comment pointer in `applyWidgetAction.ts`'s module header (a single comment line referencing the doc path `packages/web/docs/mcp-action-seam.md` as the MCP/AI seam documentation). Do NOT change any runtime behavior — comment only.
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/lib/applyWidgetAction.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f packages/web/docs/mcp-action-seam.md`.
    - Content asserts: `grep -ni "inputSchema" packages/web/docs/mcp-action-seam.md` AND `grep -ni "ALLOW_LIST_VERSION\|allow-list" packages/web/docs/mcp-action-seam.md` AND `grep -ni "PATCH /api/widgets" packages/web/docs/mcp-action-seam.md` AND `grep -ni "no AI\|not built\|NO MCP\|design.*only" packages/web/docs/mcp-action-seam.md` all return matches.
    - Pointer: `grep -n "mcp-action-seam" packages/web/src/lib/applyWidgetAction.ts` shows the code-comment pointer.
    - No runtime diff in applyWidgetAction beyond the comment: `git diff packages/web/src/lib/applyWidgetAction.ts` shows only comment-line additions.
  </acceptance_criteria>
  <done>Seam doc written with all required sections; code-comment pointer added; no runtime behavior change.</done>
</task>

<task type="auto">
  <name>Task 2: Add the seam-doc existence/content-assert test; run gate; confirm zero server diff</name>
  <files>packages/web/src/lib/applyWidgetAction.spec.ts</files>
  <read_first>
    - packages/web/src/lib/applyWidgetAction.spec.ts (where the doc-existence test is added — mirrors the readFileSync source-grep precedent used by LayersLegendPanel.spec Test 14)
  </read_first>
  <action>
    Add a `describe("MCP action seam doc (SEAM-V111-01)")` block to `applyWidgetAction.spec.ts` that `readFileSync`s `packages/web/docs/mcp-action-seam.md` (resolve relative to the spec dir) and asserts the required sections are present: the envelope `{ target, configPatch }`, the words `inputSchema`, `ALLOW_LIST_VERSION` (or "allow-list"), `PATCH /api/widgets`, and an explicit not-built statement. Also assert `applyWidgetAction.ts` source contains the `mcp-action-seam` pointer comment.

    Run the full frontend gate and confirm zero server changes (DOC-only deliverable).
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "mcp-action-seam\|SEAM-V111-01\|inputSchema" packages/web/src/lib/applyWidgetAction.spec.ts` shows the doc-assert test present.
    - `cd packages/web && npx vitest run` → 0 failures (100% green).
    - `cd packages/web && npx tsc --noEmit` → clean.
    - `git status --porcelain packages/server` returns NOTHING (zero server diff — MCP seam is DOC only).
  </acceptance_criteria>
  <done>Doc-assert test green; frontend vitest 100%; web tsc clean; zero server changes.</done>
</task>

</tasks>

<verification>
- mcp-action-seam.md documents applyWidgetAction + the envelope, the allow-list safety boundary, the concrete MCP tool shape (inputSchema + existing PATCH routes), and the explicit NOT-BUILT scope.
- Code-comment pointer at applyWidgetAction.
- Doc existence/content-assert test green.
- ZERO server changes; frontend vitest 100% green; web tsc clean.
</verification>

<success_criteria>
- `packages/web/docs/mcp-action-seam.md` exists with all required sections.
- `cd packages/web && npx vitest run` 100% green (incl. the doc-assert test).
- `cd packages/web && npx tsc --noEmit` clean.
- No AI / no MCP server built; no server changes.
</success_criteria>

<output>
After completion, create `.planning/phases/60-radio-renderer-wiring-persistence-mcp-seam-doc/60-03-SUMMARY.md`
</output>
