---
phase: 107-panel-shell-reflow-xor-switch-chips
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/FilterChip.tsx
  - packages/web/src/components/FilterChip.spec.tsx
  - packages/web/src/lib/resolveProvenance.ts
  - packages/web/src/lib/resolveProvenance.spec.ts
  - packages/web/src/components/DashboardsPage.tsx
  - packages/web/src/styles/global.css
autonomous: true
requirements:
  - FPANEL-V120-09
  - FPANEL-V120-03
  - FPANEL-V120-08
must_haves:
  truths:
    - "The existing top bar renders filter chips byte-identically to pre-Phase-107 (no visual/behavior regression) — proven by the existing DashboardsPage.spec.tsx staying green UNMODIFIED"
    - "A single shared FilterChip component exists and is imported + rendered by DashboardsPage for all 3 top-bar chip call sites (column, spatial, dv)"
    - "FilterChip variant='topbar' renders an outer element whose className is exactly 'filter-bar-chip'"
    - "resolveProvenance(sourceWidgetId, widgets) returns 'from {title}' for a resolvable widget and undefined when the id is undefined or unresolved"
    - "The panel-variant chip CSS classes exist in global.css (harmless while unreferenced) ready for Plan 107-02"
  artifacts:
    - path: "packages/web/src/components/FilterChip.tsx"
      provides: "Shared chip component with topbar + panel variants"
      exports: ["FilterChip"]
    - path: "packages/web/src/lib/resolveProvenance.ts"
      provides: "Pure 1-hop sourceWidgetId -> 'from {title}' resolver"
      exports: ["resolveProvenance"]
    - path: "packages/web/src/styles/global.css"
      provides: "Panel-variant chip classes"
      contains: ".filter-panel-chip"
  key_links:
    - from: "packages/web/src/components/DashboardsPage.tsx"
      to: "packages/web/src/components/FilterChip.tsx"
      via: "import + <FilterChip variant='topbar' ... /> at all 3 top-bar chip sites"
      pattern: "FilterChip"
    - from: "packages/web/src/components/FilterChip.tsx (variant='topbar')"
      to: "packages/web/src/styles/global.css .filter-bar-chip"
      via: "verbatim reuse of existing chip className"
      pattern: "filter-bar-chip"
---

<objective>
Extract ONE shared `FilterChip` component (topbar + panel variants) and refactor the existing top filter bar to render every chip through it — with BYTE-IDENTICAL markup so the shipped top bar has zero visual/behavior change. Also add the pure `resolveProvenance` helper (consumed by the panel in Plan 107-02) and pre-add the panel-variant chip CSS classes to `global.css`.

Purpose: FPANEL-V120-09 requires the top bar and the future panel to render chips via ONE component (no drift). This plan lands that component + proves top-bar parity via the existing spec staying green unmodified, so Plan 107-02 can build the panel on a trusted, shared chip. Partially satisfies FPANEL-V120-03 (per-chip remove flows through `onRemove`) and FPANEL-V120-08 (provenance prop + resolver exist; rendered in the panel in Plan B).

Output: `FilterChip.tsx` (+ spec), `resolveProvenance.ts` (+ spec), refactored top-bar chip sites in `DashboardsPage.tsx`, and new `.filter-panel-chip*` rulesets in `global.css`.

SCOPE GUARDRAILS (do NOT build here): no panel, no rail, no XOR switch, no reflow, no grouping, no localStorage/matchMedia — all Plan 107-02. No applies-to list / highlight (Phase 108). No global clear-all (Phase 109). No mode-toggle UI (Phase 110). Do NOT import Phase 108's `resolveWidgetsForFilter` — provenance is a plain 1-hop lookup.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/107-panel-shell-reflow-xor-switch-chips/107-CONTEXT.md
@.planning/phases/107-panel-shell-reflow-xor-switch-chips/107-RESEARCH.md
@.planning/phases/107-panel-shell-reflow-xor-switch-chips/107-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs — use directly, no codebase exploration required. -->

buildChipText (packages/web/src/lib/columnTypes.ts:200) — already covers eq/in/between/datetime:
```ts
export function buildChipText(column, value, dataType, operator): string
```

ActiveFilter (packages/web/src/store/filterStore.ts:17-40):
```ts
export type ActiveFilter = { column: string; value: ...; dataType: ...; operator: ...; sourceWidgetId?: number; /* line 38 */ }
```

Shape (packages/web/src/store/spatialFilterStore.ts:39-52) — HAS NO sourceWidgetId (spatial chips never show provenance, by data model):
```ts
export type Shape = { id: string; type: "bbox"|"lasso"|"circle"; wkt: string; label: string; measurement: string; ... }
```

WidgetDto (packages/web/src/api/client.ts, imported in DashboardsPage line 24) — has `.id` and `.title`.

Existing top-bar chip JSX to move VERBATIM into FilterChip variant='topbar' (DashboardsPage.tsx):
- Column chip: lines 997-1006 — `<span className="filter-bar-chip">{buildChipText(...)}<button className="filter-bar-chip-dismiss" aria-label={`Remove filter ${f.column}`} ...><FontAwesomeIcon icon={faXmark}/></button></span>`
- Spatial chip: lines 1011-1021 — same shell, text `${shape.label} (${shape.measurement})`, aria-label `Remove spatial filter ${shape.label}`
- DV chip: lines 1078-1088 — same shell as column, onClick removeDvFilter(dvId, f.column)

Existing CSS (global.css) — REUSE, DO NOT MODIFY: `.filter-bar-chip` (1399), `.filter-bar-chip-dismiss` (1417). faXmark already imported at DashboardsPage.tsx:4.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared FilterChip + resolveProvenance helper + panel-chip CSS</name>
  <files>packages/web/src/components/FilterChip.tsx, packages/web/src/components/FilterChip.spec.tsx, packages/web/src/lib/resolveProvenance.ts, packages/web/src/lib/resolveProvenance.spec.ts, packages/web/src/styles/global.css</files>
  <read_first>
    - packages/web/src/components/DashboardsPage.tsx:994-1021 (column + spatial chip JSX to copy verbatim into variant='topbar'), :1078-1088 (dv chip JSX), :4 (faXmark import)
    - packages/web/src/lib/columnTypes.ts:200 (buildChipText — caller passes its OUTPUT as `text`, FilterChip never calls it itself)
    - packages/web/src/api/client.ts (WidgetDto shape: .id, .title) — resolveProvenance signature
    - packages/web/src/store/filterStore.ts:17-40 (ActiveFilter.sourceWidgetId?: number)
    - packages/web/src/styles/global.css:1399-1440 (.filter-bar-chip / .filter-bar-chip-dismiss — the topbar variant reuses these unchanged)
    - .planning/phases/107-panel-shell-reflow-xor-switch-chips/107-UI-SPEC.md "Class Manifest / ADD to global.css" — the exact rulesets for .filter-panel-chip, .filter-panel-chip-row, .filter-panel-chip-value, .filter-panel-chip-provenance (copy verbatim, incl. lines 98-101)
    - packages/web/src/components/DashboardsPage.spec.tsx:1-20 (test stub patterns — no new stubs needed for FilterChip.spec)
  </read_first>
  <behavior>
    FilterChip.spec.tsx (renders the component, no OL/matchMedia stubs needed):
    - Test 1: `<FilterChip variant="topbar" text="region = West" removeAriaLabel="Remove filter region" onRemove={fn} />` renders an outer element with className EXACTLY "filter-bar-chip" (use `.className === "filter-bar-chip"`, not toContain), containing text "region = West" and a button with aria-label "Remove filter region" and class "filter-bar-chip-dismiss".
    - Test 2: clicking that dismiss button calls `onRemove` exactly once.
    - Test 3: `variant="topbar"` with a `provenance="from X"` prop does NOT render the provenance text (topbar ignores it — parity).
    - Test 4: `<FilterChip variant="panel" text="region = West" removeAriaLabel="..." onRemove={fn} provenance="from Sales map" />` renders `.filter-panel-chip` outer, `.filter-panel-chip-value` with `title` attribute equal to the full text, and a `.filter-panel-chip-provenance` element containing "from Sales map".
    - Test 5: `variant="panel"` WITHOUT provenance renders NO `.filter-panel-chip-provenance` element (graceful omit — never "from Unknown").
    resolveProvenance.spec.ts (pure, no React):
    - Test 6: resolveProvenance(5, [{id:5,title:"Sales map",...}]) === "from Sales map".
    - Test 7: resolveProvenance(undefined, [...]) === undefined.
    - Test 8: resolveProvenance(99, [{id:5,...}]) === undefined (unresolved id).
  </behavior>
  <action>
    Create `packages/web/src/lib/resolveProvenance.ts`:
    ```ts
    import type { WidgetDto } from "../api/client";
    export function resolveProvenance(sourceWidgetId: number | undefined, widgets: WidgetDto[]): string | undefined {
      if (sourceWidgetId === undefined) return undefined;
      const w = widgets.find((w) => w.id === sourceWidgetId);
      return w ? `from ${w.title}` : undefined; // literal template locked by 107-CONTEXT.md
    }
    ```
    Create `packages/web/src/components/FilterChip.tsx` exporting `FilterChip` with props EXACTLY:
    ```ts
    type FilterChipProps = {
      text: string;              // buildChipText(...) output OR `${shape.label} (${shape.measurement})` — caller computes
      removeAriaLabel: string;   // EXACT existing copy, caller supplies
      onRemove: () => void;
      variant: "topbar" | "panel";
      provenance?: string;       // only rendered when variant==="panel" AND defined
    };
    ```
    - `variant === "topbar"`: return the EXACT existing chip subtree COPY-PASTED from DashboardsPage.tsx:997-1006 — `<span className="filter-bar-chip">{text}<button type="button" className="filter-bar-chip-dismiss" aria-label={removeAriaLabel} onClick={onRemove}><FontAwesomeIcon icon={faXmark} /></button></span>`. Import faXmark + FontAwesomeIcon. `provenance` is unused in this branch. Do NOT add any wrapper element or restructure — a stray `<div>` would silently break flex-wrap parity while passing existing assertions.
    - `variant === "panel"`: render `<div className="filter-panel-chip"><div className="filter-panel-chip-row"><span className="filter-panel-chip-value" title={text}>{text}</span><button type="button" className="filter-bar-chip-dismiss" aria-label={removeAriaLabel} onClick={onRemove}><FontAwesomeIcon icon={faXmark} /></button></div>{provenance && <span className="filter-panel-chip-provenance">{provenance}</span>}</div>`.
    Add to `packages/web/src/styles/global.css` (ADD ONLY — do not touch any `.filter-bar-*` rule; place the new block AFTER the existing `.filter-bar-*` block ~line 1455) the four rulesets copied VERBATIM from 107-UI-SPEC.md Class Manifest lines 98-101: `.filter-panel-chip`, `.filter-panel-chip-row`, `.filter-panel-chip-value`, `.filter-panel-chip-provenance`. Use theme tokens + `color-mix(...)` exactly as specified — NO raw `#hex`, NO `rgba(`.
    Create `FilterChip.spec.tsx` and `resolveProvenance.spec.ts` implementing the <behavior> tests.
    Do NOT wire these into DashboardsPage yet (Task 2). Do NOT compute buildChipText inside FilterChip.
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/components/FilterChip.spec.tsx src/lib/resolveProvenance.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/web/src/components/FilterChip.tsx` AND `grep -q "export function FilterChip\|export const FilterChip" packages/web/src/components/FilterChip.tsx` (FilterChip exported)
    - `grep -q "export function resolveProvenance" packages/web/src/lib/resolveProvenance.ts`
    - Every panel-chip class referenced in FilterChip.tsx exists in global.css: `grep -q "\.filter-panel-chip " packages/web/src/styles/global.css` AND `grep -q "\.filter-panel-chip-row" ...` AND `grep -q "\.filter-panel-chip-value" ...` AND `grep -q "\.filter-panel-chip-provenance" ...`
    - No raw hex / rgba in the new CSS: `grep -nE "#[0-9a-fA-F]{3,6}|rgba\(" packages/web/src/styles/global.css | grep "filter-panel-chip"` returns NOTHING
    - FilterChip.spec.tsx Test 1 asserts `.className === "filter-bar-chip"` (exact, not toContain)
    - `cd packages/web && npx tsc --noEmit` exits 0
    - `npx vitest run src/components/FilterChip.spec.tsx src/lib/resolveProvenance.spec.ts` all green
  </acceptance_criteria>
  <done>FilterChip (both variants) + resolveProvenance exist with passing unit tests; panel-chip CSS classes exist in global.css using tokens only; nothing wired into the top bar yet.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor top-bar chip call sites to use FilterChip (top-bar parity)</name>
  <files>packages/web/src/components/DashboardsPage.tsx</files>
  <read_first>
    - packages/web/src/components/DashboardsPage.tsx:997-1006 (column chip), :1011-1021 (spatial chip), :1078-1088 (dv chip) — the 3 sites to replace
    - packages/web/src/components/DashboardsPage.tsx:1000-1002,1014-1016,1085 (existing onClick handlers: removeFilter(tableId,f.column) / removeShape(shape.id) / removeDvFilter(dvId,f.column)) — reuse VERBATIM as onRemove
    - packages/web/src/components/FilterChip.tsx (from Task 1 — props contract)
    - packages/web/src/components/DashboardsPage.spec.tsx:380-540 (existing chip/clear assertions on .filter-bar-chip / .filter-bar-chip-dismiss / aria-labels — MUST stay green UNMODIFIED)
    - .planning/phases/107-panel-shell-reflow-xor-switch-chips/107-RESEARCH.md "Regression Risk to the Existing Top Bar" section
  </read_first>
  <action>
    In `packages/web/src/components/DashboardsPage.tsx`, replace ONLY the three inline chip `<span className="filter-bar-chip">...</span>` subtrees inside the top-bar IIFE with `<FilterChip variant="topbar" .../>`, preserving the exact same `key`, text, aria-label, and remove handler:
    - Column chips (~997-1006): `<FilterChip key={`col-${f.column}`} variant="topbar" text={buildChipText(f.column, f.value, f.dataType, f.operator)} removeAriaLabel={`Remove filter ${f.column}`} onRemove={() => useFilterStore.getState().removeFilter(tableId, f.column)} />`
    - Spatial chips (~1011-1021): `<FilterChip key={`spatial-${shape.id}`} variant="topbar" text={`${shape.label} (${shape.measurement})`} removeAriaLabel={`Remove spatial filter ${shape.label}`} onRemove={() => useSpatialFilterStore.getState().removeShape(shape.id)} />`
    - DV chips (~1078-1088): `<FilterChip key={`dv-${dvId}-${f.column}`} variant="topbar" text={buildChipText(f.column, f.value, f.dataType, f.operator)} removeAriaLabel={`Remove filter ${f.column}`} onRemove={() => useFilterStore.getState().removeDvFilter(dvId, f.column)} />`
    Add `import { FilterChip } from "./FilterChip";`. Do NOT touch: the `.filter-bar` / `.filter-bar-item` / `.filter-bar-table` / `.filter-bar-clause` / `.filter-bar-chips` wrappers, the `.filter-bar-clear` buttons, the grouping IIFE logic, or the containerRef/grid block. `faXmark` may still be used elsewhere in the file (line 1197) — leave that import.
    CRITICAL PARITY RULE: do NOT edit any assertion in DashboardsPage.spec.tsx. If a test goes red, parity is broken — fix FilterChip's topbar markup, NOT the test.
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/components/DashboardsPage.spec.tsx src/styles/theme-guard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import { FilterChip }" packages/web/src/components/DashboardsPage.tsx`
    - `grep -c "variant=\"topbar\"" packages/web/src/components/DashboardsPage.tsx` returns 3
    - No inline `<span className="filter-bar-chip">` remains at the 3 refactored sites: `grep -c "className=\"filter-bar-chip\"" packages/web/src/components/DashboardsPage.tsx` returns 0
    - `git diff --stat packages/web/src/components/DashboardsPage.spec.tsx` shows NO changes (existing spec unmodified — top-bar parity proof)
    - `cd packages/web && npx tsc --noEmit` exits 0
    - `npx vitest run src/components/DashboardsPage.spec.tsx` 100% green
    - `npx vitest run src/styles/theme-guard.spec.ts` green
  </acceptance_criteria>
  <done>All 3 top-bar chip sites render via `<FilterChip variant="topbar">`; the existing DashboardsPage.spec.tsx passes UNMODIFIED (byte-identical top bar); tsc + theme-guard green.</done>
</task>

</tasks>

<verification>
- `cd packages/web && npx tsc --noEmit` exits 0.
- `cd packages/web && npx vitest run` 100% green (whole suite), including the UNMODIFIED DashboardsPage.spec.tsx.
- `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` green.
- Every new `.filter-panel-chip*` class referenced in FilterChip.tsx exists in global.css (grep both).
- `git diff packages/web/src/components/DashboardsPage.spec.tsx` is empty.
</verification>

<success_criteria>
- FPANEL-V120-09: ONE shared FilterChip renders every top-bar chip; ready for panel reuse in 107-02.
- FPANEL-V120-03 (partial): per-chip remove flows through `onRemove` (existing store actions).
- FPANEL-V120-08 (partial): `resolveProvenance` + the `provenance` prop exist (rendered in the panel in 107-02).
- Top-bar parity: existing spec green unmodified; zero visual/behavior regression.
</success_criteria>

<output>
After completion, create `.planning/phases/107-panel-shell-reflow-xor-switch-chips/107-01-SUMMARY.md`.
</output>
