---
phase: 107-panel-shell-reflow-xor-switch-chips
plan: 02
type: execute
wave: 2
depends_on:
  - "107-01"
files_modified:
  - packages/web/src/components/FilterPanel.tsx
  - packages/web/src/components/FilterPanel.spec.tsx
  - packages/web/src/components/FilterPanelRail.tsx
  - packages/web/src/components/DashboardsPage.tsx
  - packages/web/src/components/DashboardsPage.panel.spec.tsx
  - packages/web/src/styles/global.css
autonomous: false
requirements:
  - FPANEL-V120-01
  - FPANEL-V120-02
  - FPANEL-V120-03
  - FPANEL-V120-04
  - FPANEL-V120-05
  - FPANEL-V120-06
  - FPANEL-V120-07
  - FPANEL-V120-08
must_haves:
  truths:
    - "A dashboard with filter_display_mode='panel' renders the right-side drawer and NOT the top bar; a topbar/unset dashboard renders the top bar and NO panel DOM (mutually exclusive)"
    - "In panel mode the widget grid is wrapped in a flex layout so it shrinks to make room for the panel (in-flow sibling, NOT a fixed overlay on wide screens)"
    - "The panel lists eq/in/datetime-between column chips + spatial chips, grouped by source in the STABLE order tables -> dynamic views -> spatial"
    - "Each chip has a remove control and each group has a Clear all control, both calling the existing filter-store actions"
    - "Collapsing swaps the panel for a thin rail showing an active-filter count badge; the collapse state persists across reload via localStorage keyed by dashboard id"
    - "An expanded panel with zero active filters shows the empty state; the collapsed rail shows the '0' empty badge variant"
    - "Panel chips show provenance ('from {widget}') when sourceWidgetId resolves; spatial chips never show provenance"
    - "topbar/unset mode is byte-identical to pre-Phase-107 (containerRef div has no className, no filter-panel-layout wrapper)"
  artifacts:
    - path: "packages/web/src/components/FilterPanel.tsx"
      provides: "Expanded drawer: grouped chips, per-group clear, empty state, provenance, collapse button"
      exports: ["FilterPanel"]
      min_lines: 60
    - path: "packages/web/src/components/FilterPanelRail.tsx"
      provides: "Collapsed thin rail with count badge + expand button"
      exports: ["FilterPanelRail"]
    - path: "packages/web/src/components/DashboardsPage.tsx"
      provides: "isPanelMode XOR branch, gridBlock wrap, localStorage + matchMedia collapse state, activeFilterCount"
      contains: "isPanelMode"
    - path: "packages/web/src/styles/global.css"
      provides: "Panel/rail/group/layout classes + --filter-panel-width/--filter-panel-rail-width tokens + 900px overlay media query"
      contains: ".filter-panel-rail"
  key_links:
    - from: "packages/web/src/components/DashboardsPage.tsx"
      to: "packages/web/src/components/FilterPanel.tsx / FilterPanelRail.tsx"
      via: "isPanelMode ? (panelCollapsed ? <FilterPanelRail/> : <FilterPanel/>) : gridBlock"
      pattern: "isPanelMode"
    - from: "packages/web/src/components/FilterPanel.tsx"
      to: "packages/web/src/components/FilterChip.tsx"
      via: "<FilterChip variant='panel' provenance={resolveProvenance(...)} />"
      pattern: "variant=\"panel\""
    - from: "packages/web/src/components/DashboardsPage.tsx"
      to: "localStorage"
      via: "kbi_filterPanelCollapsed_${dashboard.id} read on init / write on change"
      pattern: "kbi_filterPanelCollapsed_"
    - from: "packages/web/src/components/FilterPanel.tsx / FilterPanelRail.tsx"
      to: "packages/web/src/styles/global.css .filter-panel-*"
      via: "every referenced class exists in global.css"
      pattern: "filter-panel-"
---

<objective>
Build the collapsible right-side filter panel that renders INSTEAD of the top bar when `dashboard.filter_display_mode === "panel"` — an in-flow flex sibling that shrinks the grid container so `react-grid-layout` auto-reflows. Includes: the expanded drawer (`FilterPanel`) with source groups (stable order tables -> dynamic views -> spatial), per-chip remove, per-group clear, provenance subtitles, and empty state; the collapsed thin rail (`FilterPanelRail`) with a count badge; the XOR switch in `DashboardsPage`; localStorage collapse persistence (per dashboard) with a `matchMedia` narrow-viewport default; and all the `.filter-panel-*` CSS classes + tokens.

Purpose: Completes Phase 107 — FPANEL-V120-01 (XOR), 02 (chip coverage + reflow), 03 (remove, in panel), 04 (per-group clear), 05 (collapse + count badge), 06 (empty state), 07 (group-by-source stable order), 08 (provenance, rendered). Reuses the shared FilterChip + resolveProvenance from 107-01.

Output: `FilterPanel.tsx` (+ spec), `FilterPanelRail.tsx`, wired `DashboardsPage.tsx`, new `.filter-panel-*` rulesets/tokens in `global.css`, a new `DashboardsPage.panel.spec.tsx`.

SCOPE GUARDRAILS (do NOT build): applies-to list / on-canvas highlight — no hover handler, no filterHighlightStore, no `resolveWidgetsForFilter` import (Phase 108). Global clear-all — `.filter-panel-header-actions` renders EMPTY except the collapse button (Phase 109). Mode-toggle UI — this plan only READS `filter_display_mode`, never writes it (Phase 110). Static `views[].filter_clause` WHERE text is OUT OF SCOPE for the panel (top bar keeps exclusive display). NEVER read `filterCombinationStore` for the chip list.
</objective>

<execution_context>
@/Users/rydelpereira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rydelpereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/107-panel-shell-reflow-xor-switch-chips/107-CONTEXT.md
@.planning/phases/107-panel-shell-reflow-xor-switch-chips/107-RESEARCH.md
@.planning/phases/107-panel-shell-reflow-xor-switch-chips/107-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs — use directly. -->

From 107-01 (already merged):
- FilterChip (packages/web/src/components/FilterChip.tsx): props { text, removeAriaLabel, onRemove, variant: "topbar"|"panel", provenance? }
- resolveProvenance (packages/web/src/lib/resolveProvenance.ts): (sourceWidgetId?: number, widgets: WidgetDto[]) => string | undefined

DashboardsPage.tsx existing in-scope values (DashboardOpen component):
- widgets: WidgetDto[] (state, line 410) — has .id, .title (for provenance)
- { width, mounted, containerRef } = useContainerWidth() (line 487)
- allStoreFilters = useFilterStore(s => s.filters) (line 584) — Record<tableId, ActiveFilter[]>
- allDvFilters = useFilterStore(s => s.dvFilters) (line 589) — Record<dvId, ActiveFilter[]>
- shapes = useSpatialFilterStore(s => s.shapes) (line 596)
- targetsByTable (line 597, Map) + tableIdsWithSpatialChips (line 606, Set) — spatial group hides dashboard-wide when targetsByTable.size === 0
- associatedTables, views, dynamicViews — for group source-name resolution (srcName recipe at lines 970-974; dvName recipe at line 1074)
- dashboard: DashboardDto — dashboard.filter_display_mode (api/client.ts:329, "topbar"|"panel")
- Grid block: containerRef div at line 1118, ResponsiveGridLayout at 1120; DashboardContextProvider at 1110

Store actions (reuse VERBATIM): useFilterStore.getState().removeFilter(tableId, column) / clearFilters(tableId) / removeDvFilter(dvId, column) / clearDvFilters(dvId); useSpatialFilterStore.getState().removeShape(id).

localStorage pattern to mirror (App.tsx:39,45-51,60-64): try/catch read on useState init, try/catch write in useEffect.

CSS to REUSE unchanged: .sidebar-toggle (global.css:247 — rail expand + panel collapse buttons), .config-group (1222 — group wrapper), .filter-bar-clear (1440 — per-group Clear all), --sidebar-width-fixed pattern (96). Icons available in @fortawesome/free-solid-svg-icons: faAnglesLeft, faAnglesRight (rail), faChevronDown, faChevronRight (group collapse), faXmark (already used).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add panel CSS + build FilterPanel + FilterPanelRail components</name>
  <files>packages/web/src/styles/global.css, packages/web/src/components/FilterPanel.tsx, packages/web/src/components/FilterPanel.spec.tsx, packages/web/src/components/FilterPanelRail.tsx</files>
  <read_first>
    - .planning/phases/107-panel-shell-reflow-xor-switch-chips/107-UI-SPEC.md "Class Manifest / ADD to global.css" (lines 78-101) + "Media-query behavior" (104-121) — the EXACT rulesets + tokens to copy verbatim
    - packages/web/src/styles/global.css:96 (--sidebar-width-fixed token + theme-guard-ignore comment pattern), :247-273 (.sidebar-toggle — reuse), :1222-1236 (.config-group — reuse), :1440-1455 (.filter-bar-clear — reuse), :210 & :1773 (existing 900px media queries — same breakpoint)
    - packages/web/src/components/DashboardsPage.tsx:967-1099 (existing grouping IIFE — reference for srcName/dvName resolution, but DO NOT lift; build 3 separate collections)
    - packages/web/src/components/FilterChip.tsx + packages/web/src/lib/resolveProvenance.ts (from 107-01)
    - packages/web/src/store/spatialFilterStore.ts:39-52 (Shape — no sourceWidgetId)
    - .planning/phases/107-panel-shell-reflow-xor-switch-chips/107-RESEARCH.md "Grouping assembly (FPANEL-V120-07)" + "New pitfall: grouping-shape mismatch"
    - packages/web/src/components/DashboardsPage.spec.tsx:1-25 (ResizeObserver stub pattern — copy into new spec; add matchMedia stub)
  </read_first>
  <behavior>
    FilterPanel.spec.tsx (render FilterPanel directly with prop-injected data — no DashboardsPage, no OL; stub ResizeObserver + matchMedia at top of file):
    - Test 1 (groups + stable order): given table filters + dv filters + shapes, the group titles appear in DOM order tables -> dynamic views -> spatial (assert via querySelectorAll('.filter-panel-group-title') index order).
    - Test 2 (chip coverage): an eq/in column chip, a datetime-between chip, and a spatial chip all render as `.filter-panel-chip` with buildChipText / `${label} (${measurement})` text.
    - Test 3 (per-chip remove): clicking a chip dismiss calls the passed remove callback with the right args.
    - Test 4 (per-group clear): clicking a group's "Clear all" (class .filter-bar-clear, copy "Clear all") calls the passed clear callback; the spatial group's clear removes ALL shapes.
    - Test 5 (provenance): a chip whose filter.sourceWidgetId resolves shows `.filter-panel-chip-provenance` "from {title}"; an unresolved/absent one shows none; spatial chips never show it.
    - Test 6 (empty state): with zero filters, `.filter-panel-empty` renders with heading "No active filters" and NO `.filter-panel-group`.
    - Test 7 (group collapse): clicking a group's collapse toggle unmounts that group's chip list (`.filter-panel-chips` gone for that group).
    - Test 8 (spatial orphan hide): shapes present but targetsByTable empty -> no spatial group renders (the parent passes no spatialGroup prop).
    FilterPanelRail: count>0 shows `.filter-panel-rail-badge` with the number; count===0 shows `.filter-panel-rail-badge--empty` with "0".
  </behavior>
  <action>
    STEP A — global.css (ADD ONLY; do not modify any existing rule):
    - Add tokens next to `--sidebar-width-fixed` (line ~96): `--filter-panel-width: 300px;` and `--filter-panel-rail-width: 44px;`, each with the `/* theme-guard-ignore: structural layout constant, not a spacing token */` comment (matches the existing exception pattern).
    - Append (after the existing `.filter-bar-*` block and the 107-01 `.filter-panel-chip*` block) EVERY remaining class from the UI-SPEC "ADD to global.css" table, copied VERBATIM: `.filter-panel-layout`, `.filter-panel-grid-wrap`, `.filter-panel`, `.filter-panel-header`, `.filter-panel-title`, `.filter-panel-header-actions`, `.filter-panel-body`, `.filter-panel-rail`, `.filter-panel-rail-badge`, `.filter-panel-rail-badge--empty`, `.filter-panel-empty`, `.filter-panel-group-header`, `.filter-panel-group-title`, `.filter-panel-group-header-actions`, `.filter-panel-group-toggle`, `.filter-panel-chips`. Plus the `@media (max-width: 900px)` overlay block for `.filter-panel` (UI-SPEC lines 108-119). Tokens/`color-mix` only — NO `#hex`, NO `rgba(`.
    - Do NOT add `transition: width` to `.filter-panel`/`.filter-panel-rail` (Pitfall 3 — discrete render, no thrash).
    STEP B — `packages/web/src/components/FilterPanelRail.tsx` exporting `FilterPanelRail`, props `{ count: number; onExpand: () => void }`:
    - `<div className="filter-panel-rail">` containing a `.sidebar-toggle` expand button (icon faAnglesLeft, aria-label/title "Expand filter panel", onClick onExpand) + a badge `<span className={count > 0 ? "filter-panel-rail-badge" : "filter-panel-rail-badge filter-panel-rail-badge--empty"} title={count > 0 ? `${count} active filters` : "No active filters"}>{count}</span>`.
    STEP C — `packages/web/src/components/FilterPanel.tsx` exporting `FilterPanel`. Props (data injected by DashboardsPage — the panel owns NO store subscription): `{ tableGroups, dvGroups, spatialGroup, count, onCollapse }` where each group carries { title, chips:[{ text, removeAriaLabel, onRemove, provenance? }], onClearAll }. Render:
    - `<div className="filter-panel">` with `.filter-panel-header` (`.filter-panel-title` "Filters" + `.filter-panel-header-actions` containing ONLY the `.sidebar-toggle` collapse button — icon faAnglesRight, aria-label "Collapse filter panel", onClick onCollapse; leave the actions slot otherwise EMPTY for Phase 109).
    - `.filter-panel-body`: if count === 0 render `.filter-panel-empty` (heading "No active filters", body "Filters you apply from charts, tables, or the map will appear here."). Else render groups in order tableGroups, then dvGroups, then spatialGroup (if present). Each group = `.config-group` wrapper > `.filter-panel-group-header` (`.filter-panel-group-title` + `.filter-panel-group-header-actions` with the `.filter-bar-clear` "Clear all" button calling onClearAll + a `.filter-panel-group-toggle` chevron button, aria-label `${collapsed?"Expand":"Collapse"} ${title} filters`, icon faChevronDown/faChevronRight, local useState collapse) > when expanded, `.filter-panel-chips` mapping each chip to `<FilterChip variant="panel" text=... removeAriaLabel=... onRemove=... provenance=... />`.
    - Group collapse = conditional render (unmount), not display:none.
    STEP D — FilterPanel.spec.tsx implementing the <behavior> tests. Stub ResizeObserver + `vi.stubGlobal("matchMedia", vi.fn().mockImplementation((q) => ({ matches:false, media:q, addEventListener: vi.fn(), removeEventListener: vi.fn(), addListener: vi.fn(), removeListener: vi.fn() })))` at file top.
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/components/FilterPanel.spec.tsx src/styles/theme-guard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function FilterPanel\|export const FilterPanel" packages/web/src/components/FilterPanel.tsx` AND same for FilterPanelRail
    - global.css contains `--filter-panel-width` AND `--filter-panel-rail-width` tokens AND `.filter-panel-rail` AND `.filter-panel-layout` AND `.filter-panel-empty` AND `.filter-panel-group-title` AND `.filter-panel-grid-wrap` (`grep -q "\.filter-panel-grid-wrap" packages/web/src/styles/global.css`)
    - Class-presence gate: for EVERY `filter-panel-` className referenced in FilterPanel.tsx / FilterPanelRail.tsx, the class exists in global.css (grep each; zero misses)
    - No raw hex/rgba in new panel CSS: `grep -nE "#[0-9a-fA-F]{3,6}|rgba\(" packages/web/src/styles/global.css | grep -i "filter-panel"` returns NOTHING (tokens/color-mix only)
    - FilterPanel renders NO import of `filterCombinationStore` and NO `resolveWidgetsForFilter`: `grep -E "filterCombinationStore|resolveWidgetsForFilter" packages/web/src/components/FilterPanel.tsx` returns NOTHING
    - `.filter-panel-header-actions` contains only the collapse button (no global clear-all): `grep -c "Clear all dashboard\|clearAll(" packages/web/src/components/FilterPanel.tsx` returns 0 (per-group onClearAll props are fine)
    - `cd packages/web && npx tsc --noEmit` exits 0; `npx vitest run src/components/FilterPanel.spec.tsx` green; theme-guard green
  </acceptance_criteria>
  <done>All `.filter-panel-*` classes + tokens + overlay media query exist in global.css (tokens only); FilterPanel (groups/empty/collapse/clear/provenance) + FilterPanelRail (count badge) built with passing specs; no forbidden store/lib imports; global clear-all slot empty.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire XOR switch + reflow + collapse persistence into DashboardsPage</name>
  <files>packages/web/src/components/DashboardsPage.tsx</files>
  <read_first>
    - packages/web/src/components/DashboardsPage.tsx:1110-1130 (DashboardContextProvider + containerRef div at 1118 + ResponsiveGridLayout — the gridBlock to extract), :945-1102 (top-bar IIFE — gate behind !isPanelMode), :584-609 (allStoreFilters/allDvFilters/shapes/targetsByTable/tableIdsWithSpatialChips), :410 (widgets), :970-974 (srcName recipe), :1074 (dvName recipe)
    - packages/web/src/api/client.ts:329 (filter_display_mode)
    - packages/web/src/App.tsx:39,45-51,60-64 (localStorage read-init/write-effect pattern to mirror)
    - .planning/phases/107-panel-shell-reflow-xor-switch-chips/107-RESEARCH.md "Recommended XOR + reflow wiring", "Collapse-state persistence", "Count badge", Open Questions #2 (stored pref WINS) & #3 (spatial orphan hide)
    - .planning/phases/107-panel-shell-reflow-xor-switch-chips/107-UI-SPEC.md Interaction/State Contract table
    - packages/web/src/components/FilterPanel.tsx / FilterPanelRail.tsx (from Task 1 — prop contracts)
  </read_first>
  <behavior>
    Behavioral assertions for this wiring live in DashboardsPage.panel.spec.tsx (Task 3): XOR render, backward-compat (topbar), collapse persistence round-trip, matchMedia narrow default + stored-pref-wins, count badge. This task's own gate is tsc clean + the existing suite staying green UNMODIFIED (topbar byte-identical).
  </behavior>
  <action>
    In `DashboardsPage.tsx` (DashboardOpen), make the `isPanelMode` ternary the ONLY new top-level conditional (Pitfall 8 — topbar path must stay textually identical):
    - `const isPanelMode = dashboard.filter_display_mode === "panel";`
    - Gate the existing top-bar IIFE render with `{!isPanelMode && (...existing IIFE unchanged...)}`.
    - Extract the EXISTING grid block into `const gridBlock = (<DashboardContextProvider ...><div ref={containerRef as React.RefObject<HTMLDivElement>} className={isPanelMode ? "filter-panel-grid-wrap" : undefined}>{...unchanged ResponsiveGridLayout...}</div></DashboardContextProvider>);` — in topbar mode `className` is `undefined` so the div is byte-identical to today (no wrapper, no class).
    - Render: `{isPanelMode ? (<div className="filter-panel-layout">{gridBlock}{panelCollapsed ? <FilterPanelRail count={activeFilterCount} onExpand={() => setPanelCollapsed(false)} /> : <FilterPanel {...groupProps} count={activeFilterCount} onCollapse={() => setPanelCollapsed(true)} />}</div>) : gridBlock}`.
    - `activeFilterCount` = sum over allStoreFilters arrays + allDvFilters arrays + shapes.length (compute in DashboardOpen from existing subscriptions; do NOT re-subscribe in child components).
    - Collapse state: `const collapsedKey = \`kbi_filterPanelCollapsed_${dashboard.id}\`;` + `useState<boolean>` init reading localStorage (try/catch); if a stored value exists it WINS (Open Q #2); only when absent, default to `isNarrowViewport()` where `isNarrowViewport = () => { try { return typeof window.matchMedia === "function" && window.matchMedia("(max-width: 900px)").matches; } catch { return false; } }`. Persist with a `useEffect` writing `localStorage.setItem(collapsedKey, String(panelCollapsed))` (try/catch), dep `[collapsedKey, panelCollapsed]`.
    - Build the group props (3 collections, stable order — do NOT lift the top-bar IIFE): tableGroups from `allStoreFilters` entries with length>0 (title via the existing srcName recipe, chips = column chips only, onClearAll = clearFilters(tableId)); dvGroups from `allDvFilters` entries with length>0 (title via dvName recipe, onClearAll = clearDvFilters(dvId)); ONE spatialGroup only if `shapes.length > 0 && targetsByTable.size > 0` (Open Q #3 — orphan hide), title e.g. "Spatial draws", chips = all shapes flat (text `${label} (${measurement})`, no provenance), onClearAll = loop removeShape over all shape ids. Column-chip provenance = `resolveProvenance(f.sourceWidgetId, widgets)`.
    - Import FilterPanel, FilterPanelRail, resolveProvenance. Do NOT touch the top-bar IIFE internals, the store subscriptions, or filterCombinationStore.
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run src/components/DashboardsPage.spec.tsx src/styles/theme-guard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "const isPanelMode" packages/web/src/components/DashboardsPage.tsx` AND `grep -q "filter-panel-layout" packages/web/src/components/DashboardsPage.tsx`
    - `grep -q "kbi_filterPanelCollapsed_" packages/web/src/components/DashboardsPage.tsx` (localStorage key includes dashboard id)
    - `grep -q "matchMedia" packages/web/src/components/DashboardsPage.tsx` (narrow-viewport default)
    - containerRef div is classless in topbar mode: the className expression is `isPanelMode ? "filter-panel-grid-wrap" : undefined` (grep `filter-panel-grid-wrap`)
    - No ADDED line references the derived store: `git diff packages/web/src/components/DashboardsPage.tsx | grep -E "^\+" | grep -c "filterCombinationStore"` returns 0
    - Existing DashboardsPage.spec.tsx (topbar fixtures) stays green UNMODIFIED (`git diff --stat` shows no change to that file)
    - `cd packages/web && npx tsc --noEmit` exits 0; existing suite + theme-guard green
  </acceptance_criteria>
  <done>DashboardOpen renders top-bar XOR panel by filter_display_mode; grid wrapped in filter-panel-layout only in panel mode (topbar byte-identical); collapse state persists via localStorage keyed by dashboard id with a matchMedia narrow default; activeFilterCount + 3-collection group props computed from the existing input-store subscriptions.</done>
</task>

<task type="auto">
  <name>Task 3: Panel behavior specs (XOR, backward-compat, persistence, matchMedia, badge)</name>
  <files>packages/web/src/components/DashboardsPage.panel.spec.tsx</files>
  <read_first>
    - packages/web/src/components/DashboardsPage.spec.tsx:1-40 (ResizeObserver + OL mocks + store imports to mirror in the new spec), :380-540 (existing chip/clear patterns), :359-362 (fixtures omit filter_display_mode = implicit topbar)
    - .planning/phases/107-panel-shell-reflow-xor-switch-chips/107-RESEARCH.md "Test Strategy" + "Suggested Plan Split / Plan B new specs" + Open Question #4 (new tests MUST set filter_display_mode explicitly)
    - packages/web/src/components/DashboardsPage.tsx (Task 2 wiring — isPanelMode branch, collapsedKey)
    - packages/web/src/components/FilterPanelRail.tsx (badge classes)
  </read_first>
  <action>
    Create `packages/web/src/components/DashboardsPage.panel.spec.tsx` (new file — mirror the ResizeObserver/OL stubs + seed helpers from DashboardsPage.spec.tsx; ADD the `vi.stubGlobal("matchMedia", ...)` stub from Task 1's spec). Every dashboard fixture MUST set `filter_display_mode` explicitly (Open Q #4). Tests:
    - XOR panel: fixture `filter_display_mode:"panel"` -> `document.querySelector(".filter-panel-layout")` is non-null AND `.filter-bar` is null (top bar not rendered).
    - XOR topbar / backward-compat: fixture `filter_display_mode:"topbar"` with active filters -> `.filter-panel-layout` is null AND the existing `.filter-bar` chips render (top bar unchanged).
    - Chip coverage in panel: seed an eq/in filter + a spatial shape (with an eligible target) -> both render as `.filter-panel-chip` in panel mode.
    - Per-chip remove + per-group clear in panel: click a `.filter-bar-chip-dismiss` inside the panel -> corresponding store filter removed; click a group "Clear all" -> that group's filters cleared (assert via store getState()).
    - Empty state: panel mode, no active filters -> `.filter-panel-empty` present, "No active filters" text.
    - Count badge + collapse: collapse the panel -> `.filter-panel-rail` with `.filter-panel-rail-badge` showing the active count; empty -> `.filter-panel-rail-badge--empty` "0".
    - Collapse persistence round-trip: set localStorage `kbi_filterPanelCollapsed_{id}="true"` before render -> panel renders collapsed (rail); then mock localStorage.setItem and toggle -> written with the dashboard-scoped key.
    - matchMedia narrow default: no stored pref + `matchMedia("(max-width:900px)").matches === true` -> initial render is collapsed (rail); stored pref "false" WINS over narrow default (Open Q #2) -> expanded even when matches true.
    - Group order: panel with table + dv + spatial filters -> `.filter-panel-group-title` DOM order is tables, then dv, then spatial.
    Add a lightweight CSS class-presence assertion confirming each new `.filter-panel-*` class appears in global.css (Pitfall 6 mitigation) — e.g. read global.css and assert the class-name list is a substring set.
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit && npx vitest run && npx vitest run src/styles/theme-guard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/web/src/components/DashboardsPage.panel.spec.tsx`
    - New spec stubs matchMedia: `grep -q "stubGlobal(\"matchMedia\"" packages/web/src/components/DashboardsPage.panel.spec.tsx`
    - Spec asserts panel NOT rendered in topbar mode (querySelector('.filter-panel-layout') null) AND panel rendered in panel mode
    - Spec asserts collapse persists via a key containing the dashboard id (`kbi_filterPanelCollapsed_`)
    - Spec asserts stored pref wins over matchMedia narrow default
    - `cd packages/web && npx tsc --noEmit` exits 0
    - `cd packages/web && npx vitest run` 100% green (whole suite, existing DashboardsPage.spec.tsx UNMODIFIED)
    - `npx vitest run src/styles/theme-guard.spec.ts` green
  </acceptance_criteria>
  <done>DashboardsPage.panel.spec.tsx covers XOR, backward-compat, chip coverage, remove/clear parity, empty state, count badge, collapse persistence (dashboard-scoped key), and matchMedia narrow default with stored-pref-wins precedence; full web suite + theme-guard green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verification — light/dark theme + narrow viewport</name>
  <files>N/A — verification only (no file changes unless issues are found)</files>
  <what-built>The panel-mode filter drawer: expanded drawer with grouped chips + provenance + per-group clear + empty state, the collapsed count-badge rail, grid reflow on wide screens, and the narrow-viewport overlay. All automated gates (tsc, full vitest, theme-guard) are green, and the top bar is byte-identical in topbar mode.</what-built>
  <action>Human-only visual verification (checkpoint) — automated gates cannot catch the CSS-class-trap, wrong-token/rgba theming, or reflow/overlay correctness (project memory: css-bugs-evade-tests-and-theme-guard). See how-to-verify for the exact walkthrough; fix any issue in-session and re-run the automated gates before resuming.</action>
  <how-to-verify>
    Manually verify in the running app:
    1. Open a dashboard set to `filter_display_mode: "panel"` on a WIDE viewport with several active filters (from a chart drill, a dynamic view, and a spatial draw). Confirm: the drawer shows on the right, chips are grouped tables -> dynamic views -> spatial, provenance subtitles read "from {widget}", chips are styled (accent tint, NOT default browser chrome), and the widget grid visibly SHRINKS to make room (no clipped rightmost widget).
    2. Collapse the panel: confirm the thin rail with the count badge; reload the page and confirm it stays collapsed (localStorage). Expand and reload -> stays expanded.
    3. Clear a group and remove a single chip -> widgets update; empty the panel -> friendly empty state shows.
    4. NARROW viewport (< 900px): confirm the panel auto-collapses to the rail on load; expanding OVERLAYS the charts (position:fixed, solid background, readable) and does NOT reflow the grid.
    5. Toggle LIGHT and DARK theme in both expanded + collapsed states: confirm panel/rail background, chip tint, group-title accent-text, badge, and provenance all read correctly (no invisible text, no washed-out panel) in BOTH themes.
    6. Open a `topbar`/unset dashboard: confirm the top bar is unchanged and NO panel appears.
  </how-to-verify>
  <verify>Manual (human-verify checkpoint) — no automated command; blocking gate.</verify>
  <done>Operator confirms panel renders correctly in panel mode (grouped, styled, reflowing) and top bar is unchanged in topbar mode, in BOTH light + dark themes and at wide + narrow viewports; any issues fixed in-session and re-verified.</done>
  <resume-signal>Type "approved" or describe any visual/reflow/theme issues to fix in-session.</resume-signal>
</task>

</tasks>

<verification>
- `cd packages/web && npx tsc --noEmit` exits 0.
- `cd packages/web && npx vitest run` 100% green (whole suite); existing DashboardsPage.spec.tsx UNMODIFIED.
- `cd packages/web && npx vitest run src/styles/theme-guard.spec.ts` green.
- Every `.filter-panel-*` class referenced in TSX exists in global.css (grep both); no `rgba(`/`#hex` in the new rules.
- Manual: panel renders in panel mode, top bar in topbar mode (never both); reflow on wide, overlay on narrow; light + dark correct.
</verification>

<success_criteria>
- FPANEL-V120-01: panel XOR top bar by filter_display_mode; topbar/unset byte-identical.
- FPANEL-V120-02: eq/in + datetime-between + spatial chips render; grid reflows.
- FPANEL-V120-03/04: per-chip remove + per-group clear via existing store actions.
- FPANEL-V120-05: collapse to rail with active-filter count badge; persists per dashboard.
- FPANEL-V120-06: empty state (expanded) + '0' rail badge (collapsed).
- FPANEL-V120-07: groups in stable order tables -> dynamic views -> spatial.
- FPANEL-V120-08: provenance subtitle rendered when resolvable; spatial never.
</success_criteria>

<output>
After completion, create `.planning/phases/107-panel-shell-reflow-xor-switch-chips/107-02-SUMMARY.md`.
</output>
